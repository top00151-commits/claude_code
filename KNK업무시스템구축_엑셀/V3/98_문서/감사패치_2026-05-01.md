# V2 도입 차단 결함 패치 보고

- **일자**: 2026-05-01
- **세션**: 100_HAIST_WORKS_엑셀 (빅터)
- **소요**: 약 15분

---

## ✅ A-1. .bat 18개 `\a` BEL 버그 패치

### 패치 내용
- 패턴: `b"scripts\x07pply_standard.py"` → `b"scripts\\apply_standard.py"`
- 즉 `apply_standard.py` 호출 라인 정상 복원

### 패치 결과
| 항목 | 결과 |
|---|---|
| 패치 시도 | 18개 |
| 성공 | **18/18** ✓ |
| 실패 | 0 |
| BEL(0x07) 잔존 | 0 |
| 신규 정상 패턴 적용 | 18 |

### 백업 위치
`80_logs/bat_backup_20260501/` — 패치 전 18개 .bak 보존

### 재발 방지
- BAT 생성 소스 코드는 V2 폴더에서 발견되지 않음 (이전 세션에서 직접 작성된 결과물)
- **향후 BAT 자동 생성 추가 시 반드시 raw-string 사용**:
  ```python
  content = r"python -X utf8 scripts\apply_standard.py"   # ← raw 'r' 필수
  # 또는
  content = "python -X utf8 scripts\\apply_standard.py"  # 백슬래시 2개
  ```

### V1 동일 버그 (참고 — 패치 보류)
- V1 18개 .bat도 동일 BEL 버그 존재 (V1은 스냅샷이라 운영 영향 없음)
- 만약 V1을 fallback으로 사용하면 별도 패치 필요

---

## ✅ B-6. sync.py SyntaxWarning 정리

### 수정 내용
| 파일 | 라인 | 변경 전 | 변경 후 |
|---|---|---|---|
| `01_검사기/scripts/sync.py` | 695 | `f"  동기화 완료\!"` | `"  동기화 완료!"` |
| `02_자동화/scripts/sync.py` | 667 | `f"  동기화 완료\!"` | `"  동기화 완료!"` |

### 검증
- compile() 시 SyntaxWarning **0건** (변경 전 1건씩)

---

## 🟢 종합 결과

| 결함 | 상태 |
|---|---|
| **A-1 (도입 차단)** | ✅ 해소 |
| **B-6 (경고)** | ✅ 해소 |
| B-1~B-5 (스펙 문서 갱신) | ⏳ 별도 트랙 (대표 결재 대기) |
| 데이터 운영 상태 (104·23·0건) | ⏳ 대표 결재 대기 |

---

## 📌 다음 단계 (대표 검증 요청)

### 즉시 확인 가능
1. `01_검사기_완제품/실행_PMS(영업·PM).bat` 더블클릭
   - 기대: 한글 정상 + sync OK + apply_standard OK + "완료!" 표시
2. 다른 부서 .bat 1~2개도 같은 방식 확인

### 결과 보고 부탁드립니다
- ✓ 정상 작동 → 나머지 16개도 동일 동작 추정 (백업 있어 롤백 가능)
- ✗ 에러 발생 → 화면 캡처 주시면 추가 진단

---

## 🛡 롤백 절차 (필요 시)

```cmd
cd C:\Users\top00\JR\Claude 코드\KNK업무시스템구축_엑셀\V2\80_logs\bat_backup_20260501
:: 원래 위치로 복사 (확장자 .bak를 .bat로 복원)
```

또는 빅터에게 "롤백" 한 마디만 주시면 18개 일괄 복원합니다.

---

㈜케이엔케이 │ HAIST Innovation │ Human & AI create the Best  
**빅터** (100_HAIST_WORKS_엑셀 단독 세션)
