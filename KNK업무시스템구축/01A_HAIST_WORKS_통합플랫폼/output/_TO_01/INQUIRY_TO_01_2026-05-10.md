# 📨 빅터(01) 앞 1차 문의서 — 실무팀1(통합플랫폼)

**발신:** 실무팀1 (통합플랫폼) — 워크트리 `sad-johnson-eddf3c`
**수신:** 빅터 (01 통합실무팀)
**라우팅 부탁:** 김정락 대표이사
**일자:** 2026-05-10
**작업 진행:** 코드 수정 0건 / Tier 1(사전 정리) 완료 후 회신 대기

---

## 배경

발주서 `_ORDERS/실무팀1_통합플랫폼.md` 수신, 22개 통합플랫폼 페이지 시안1 적용 작업 착수 전 사전 스캔 완료.
세 가지 결정적 갭이 발견되어 빅터 회신을 받은 뒤 실작업에 들어가는 것이 안전합니다.

---

## 문의 1 — 시스템 버전 시점 갭 (z40 ~ z53)

**상황:**
- 발주서 시점: `v5H226z53`
- 이 워크트리(`sad-johnson-eddf3c`) 끝점: `v5H226z39` (commit 7c173f6, 2026-05-08 23:45 롤백 포인트)
- `git log --all --grep="v5H226z"` 결과: z2~z19, z fix, z39만 존재. **z20~z38, z40~z53 commit 없음**
- 그러나 `_v5_partials/design_quiet_v3.html` 파일 헤더에 `v5H226z48 (2026-05-09)` 표기 존재 → **uncommitted 작업이 워킹디렉토리에 있는 상태**

**문의:**
1. v5H226z40~z53 작업의 정식 위치(다른 워크트리/branch)는 어디입니까?
2. 실무팀1이 작업할 베이스라인(commit 또는 워크트리)을 어디로 잡아야 합니까?
   - (a) 현재 워크트리(`sad-johnson-eddf3c`, z39) 그대로 진행
   - (b) 다른 워크트리에서 z48 이후 상태를 가져와 진행 (이 경우 가져오는 절차 안내 부탁)
   - (c) 빅터가 z53 상태를 main 또는 별도 branch에 commit 후 알려주면 그 위에서 진행
3. 현재 워크트리에 uncommitted 상태로 design_quiet_v3.html(z48 헤더)이 들어와 있는데, 이걸 베이스로 사용해도 되는지?

---

## 문의 2 — 디자인 핸드오프 자료 부재

**상황:**
- 발주서 5장 "참고 자료": `01_HAIST_WORKS/HAIST WORKS디자인변경/design_handoff_haist_works/` (screenshots/, specs/, tokens.css)
- 실제 검색: 이 워크트리 + 다른 워크트리에서 `design_handoff_haist_works` 폴더, `design_handoff*` 폴더 **모두 부재**
- 다른 위치(예: 05 디자인팀 세션 폴더)에 있을 가능성도 검토했으나 매치 0건

**문의:**
1. 통합플랫폼 22개 페이지 각각의 시안1 PNG/specs는 어디에 있습니까?
2. "시안1"의 정의가 다음 중 어느 것입니까?
   - (a) 별도 PNG/specs 산출물이 있는데 이 워크트리에 미동기화된 상태 → 위치 알려주시면 수령
   - (b) PNG/specs 별도 산출물 없음. **현재 home/daily/weekly의 `v5H226z48` 상태를 시안1 기준선으로 간주**하고 나머지 19개 페이지를 동일 패턴으로 적용
   - (c) 05 디자인팀이 이번 작업에서 신규 제작 중 (도착 대기)
   - (d) 그 외

---

## 문의 3 — Quiet Tone v3 적용 범위 정의

**상황 (이 부분은 빅터 사전 회신 없이도 가능하나, 인지 일치 위해 확인):**
- `_v5_partials/design_quiet_v3.html` partial 존재 → chrome.html / styles.html이 include하므로 **모든 페이지에 토큰 자동 로드**
- 즉 사이드바/상단/`.tbl`/`.kpi-card`/`.sb-item` 등 **공통 컴포넌트는 이미 v3 적용 완료**
- 그러나 22 페이지의 **인라인 `<style>` / 페이지 고유 클래스**는 여전히 v5 시절 토큰(`--paper`/`--ink`/`--mute` 등) 사용 → **시각적 불일치 발생 중**

**문의 (빅터 인지 일치만 필요):**
1. 실무팀1의 페이지별 시안1 적용 작업의 정확한 의미는 다음 중 어느 것입니까?
   - (a) 페이지 인라인 CSS의 v5 토큰 → v3 토큰 마이그레이션 (`--paper` → `var(--qv-surface-2)`, `--ink` → `var(--qv-ink)` 등)
   - (b) 위 (a) + 페이지 고유 클래스를 공통 `.kpi-card`/`.bento`/`.tbl`/`.chip-status`/`.mgmt-no`/`.page-head`로 표준화
   - (c) 위 (b) + `data-dn="영역명"` 디버그 라벨 부착 (모든 주요 영역)
   - (d) 위 (c) + 빈 스켈레톤 6개(changes_list/change_form/issue_form/board_list/board_form/board_teams)는 골격(.page-head + 핵심 영역) 정의부터 실시
   - (e) 위 (d) 전체

작업 의도 확인 차원의 문의입니다. **빅터 별도 회신 없으면 (e) 전체로 간주하고 진행**하겠습니다.

---

## 답신 요청 형식

가능하면 같은 폴더에 회신 파일을 두시면 자동으로 수령하겠습니다:
- `01A_HAIST_WORKS_통합플랫폼/output/REPLY_FROM_01_2026-05-10.md`

또는 대표님께서 워크트리에 직접 메시지 남기시면 그것도 수령합니다.

---

## 부록 — 회신 도착 전까지 실무팀1 행동

- ❌ 22 페이지 코드 수정 절대 금지(차단 풀릴 때까지)
- ✅ PROGRESS.md 22행 채워둠 (`01A_HAIST_WORKS_통합플랫폼/PROGRESS.md`)
- ✅ 초기 스캔 메모 작성 (`01A_HAIST_WORKS_통합플랫폼/notes/SCAN_2026-05-10_초기현황.md`)
- ⏸ 차단 1·2 회신 도착 시 즉시 우선순위 페이지(home → daily → weekly) 시안1 적용 착수
