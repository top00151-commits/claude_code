# 회의록 세 화면 — 로그인 안 된 채 들어오면 로그인 뒤 원래 화면으로 · WORKS 준비 완료(배포 대기)

- **작성**: 2026-09-17 (목) 17:17 KST
- **세션**: 16_KNK_Meeting (회의록 담당)
- **발단(세션 10 요청 · 대표 전달)**: 「WORKS 회의록 화면 세 곳(/meetings, /meetings/{id}, /meetings/{id}/doc)은 로그인이 안 된 상태면 /login 으로만 보내 목적지를 잃는다.
  휴대폰에서 「▶ 회의 시작」 자동 녹음과 「📋 회의록 보기」가 WORKS 첫 화면으로 샌다. 제작요청서 페이지(z816)처럼 /login?next=<경로+쿼리>로.
  이음의 휴대폰 경로는 토큰 없는 /sso/land 를 열 수 없어(「입장 토큰이 없습니다」) 상세 주소를 직접 연다.」
- **진행**: 코드 읽기 → 이해 내용 보고 → 대표 **「진행해」**(16:5x · 질문 창) → 구현·시험
- **상태**: 준비·시험 **완료** · ⏳ **업무 시간 뒤 대표 「배포해」 대기** — 녹음 화면 문구(z1109)와 한 번에 올림(이것은 z1110 예정)
- **준비물 폴더**: `16_KNK_Meeting/작업기록/2026-09-17_1703_회의록_로그인뒤원래화면_WORKS준비물/`

---

## 1. 조사 (origin/main `95d035d7` · main.py blob `ca162ba3`)

| 화면 | 미로그인일 때(전) |
|---|---|
| `/meetings` (목록 · `?tab=cards`) | `/login` — 목적지 잃음 |
| `/meetings/new` | 이미 `/login?next=` (2026-07-03 대표 지시) |
| `/meetings/{id}` (회의록 화면 · `?autorec=1`) | `/login` — 목적지 잃음 |
| `/meetings/{id}/doc` (양식) | `/login` — 목적지 잃음 |

- 회의록 화면 주소(GET)는 이 네 개가 전부. `/login` 은 `next` 를 `_safe_next_path` 로 거른 뒤 이음 로그인 주소의 돌아올 곳(`/sso/land?next=…`)에 싣고, `/sso/land` 도 같은 검사 뒤 그 주소로 보낸다 → **빠진 것은 세 라우트뿐**(쿼리 `?autorec=1` 도 통과하는 규칙 — `/mail/inbox?app=1` 이 이미 씀).
- 같은 방식(z816) 확인: 제작요청서 편집 · 소모품 제작요청서 보기.
- 참고(범위 밖): WORKS 에 bare `/login` 으로 보내는 곳이 약 370곳 — 손대지 않음(요청 범위 = 회의록 세 화면).

## 2. 바꾼 것 — `app/main.py`

- 공용 `_meeting_login_redirect(req)` 하나: `/login?next=` + `quote(경로 + ("?" + 쿼리), safe="")` — z816 과 같은 줄.
- `/meetings` · `/meetings/{id}` · `/meetings/{id}/doc` 의 미로그인 분기만 이 함수로 교체(`/meetings/new` 는 원래대로).
- 로그인 확인이 DB 조회보다 먼저라 **없는 회의 번호도 먼저 로그인으로**(있는지 알려주지 않음 — 전과 같음).
- 결과 파일: sha256 `90c8318142ca3378d1cab176ee1e94f70f228ab12e2a600ed1dd1607b4baddc7` · git blob `3ceabb46` · `patch_main_next.py` 가 세 곳이 딱 한 번씩 맞을 때만 바꾸고 문법(ast)까지 확인.

## 3. 검증 — 전부 통과 (시험 사본 = origin/main 트리 + 이 파일 + 녹음 화면 새 파일 · 425개 대조 다른 것 2개)

| 묶음 | 결과 | 내용 |
|---|---|---|
| 로그인 복귀(새 `verify_login_next.py`) | **20/20** | ① 미로그인 7경우 → `/login?next=` 정확한 글자(목록·`?tab=cards`·회의록·`?autorec=1`·양식·없는 번호·새 회의 그대로) ② `/login` → 이음 로그인 주소 · 돌아올 곳 `/sso/land` · next = `/meetings/N?autorec=1` · 양식 주소 · 바깥 주소(`//…`) → `/home` ③ `/sso/land`(가짜 토큰) → `/meetings/N?autorec=1` → 로그인된 채 화면 200 · 양식·목록 200 · 틀린 토큰 → 안내 화면·세션 없음 ④ 줄바꿈 넣은 주소도 감싸서 넘김 · 쿠키 끼어들기 없음 |
| 고치기 전 코드로 같은 시험 | **8건 실패**(기대대로) | 세 화면이 `/login` 만 돌려줌 — 시험이 결함을 잡는 것 확인. ②③ 은 고치기 전에도 통과(로그인·이음 복귀 흐름은 원래 됨) |
| 양식(z1102) | **48/48** | 「미로그인 → `/login`」 기대값을 `/login?next=%2Fmeetings%2FN%2Fdoc` 로 고침(의도한 변경) |
| 회의록 보기(z1103) | **22/22** | |
| 이미지(z1105) | **22/22** | 같은 기대값 고침 |
| 모아보기(z1108) | **80/80** | 「미로그인 → /login…」은 앞머리 비교라 그대로 통과 |
| 녹음 화면(z1109 새 파일 함께) | 안내 글 **16/16** · 기존 **25/25** | |
| 나갈 때 알림(z1106) | **62/62** | |
| 표준 검사기 | 통과 | |

- 🔴 시험은 운영 이음에 닿지 않게: 이음 주소를 닫힌 로컬 자리(127.0.0.1:9)로 · 토큰 검증·계정 맞춤·로그아웃 조회는 가짜.
- 실제 휴대폰 흐름(이음 → 크롬 → 이음 로그인 → 회의록)은 **실측 없음** — 배포 뒤 대표 휴대폰 확인 필요.

## 4. 배포 계획 (대표 「배포해」를 받은 뒤 · z1109 와 한 번에)

1. `git fetch` → origin/main 의 main.py blob `ca162ba3` · meeting_form.html blob `370a9162` 그대로인지 확인(바뀌었으면 패치 스크립트를 새 기준에 다시 적용하고 시험 다시)
2. z 번호 = `git log --all` 최댓값 + 1, + 2 (지금 기준 z1109 · z1110) · BAT: `patch_bat9.py` → `patch_bat10.py` 차례로
3. 임시 인덱스로 커밋 두 개(z1109 녹음 화면 → z1110 main.py) → 올리기 **전** `/api/version` 기준값 → 한 번에 push(재시작 1번) → HTTP 200 + 형식 + 기준값과 다름
4. 운영 컨테이너(읽기 전용): main.py sha256 `90c83181…` · meeting_form.html `df1cfeb6…` · 배포 저장소 `mirror @ <z1110 커밋>`
5. 운영 확인(미로그인): `/meetings/1?autorec=1` → 303 `/login?next=%2Fmeetings%2F1%3Fautorec%3D1` · `/meetings/1/doc` · `/meetings?tab=cards` 같은 방식 · `/login?next=…` → 이음 로그인 주소에 next
6. 세션 10 에 z 번호·시각 알림(대표 전달) → 대표 휴대폰으로 「▶ 회의 시작」·「📋 회의록 보기」 확인

- 준비 때 만든 커밋 두 개(base `95d035d7` → z1109 `8f7f63a9` → z1110 `751ef8e4`) — **올리지 않음**, 배포 때 그때의 main 위에 다시 만든다.

## 5. 남은 일

- [ ] 대표 「배포해」(업무 시간 뒤 · 이음 v801 과 같은 때)
- [ ] 배포 뒤 운영 확인(위 5) · 세션 10 알림
- [ ] 대표 휴대폰 실사용: 이음 「▶ 회의 시작」·「📋 회의록 보기」가 로그인 뒤 그 회의로 가는지
