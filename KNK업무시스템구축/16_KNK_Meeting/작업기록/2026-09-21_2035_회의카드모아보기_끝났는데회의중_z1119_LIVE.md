# WORKS 「🗓 회의 카드 모아보기」 — 끝났는데 「지금 회의 중」 고침 (z1119 · LIVE)

- **작성**: 2026-09-21 (월) 20:35 KST
- **발단**: 대표 신고(사진) — 이음 회의 카드 「라이프밸류 제품디자인 신규업체[코엔] 미팅」(11:00~12:00)이 15:51 에도 「🔴 회의 중」
  → 세션 16 원인 진단·요청문(16:42) → **이음 v815 LIVE 17:43**(세션 10) → 세션 10 회신: **「WORKS 모아보기 탭에도 같은 원인」**
- **결과**: ✅ **LIVE 2026-09-21 20:33:14** · 라이브 `9ed1299` (모노 `36ddf3a6`) · WORKS 재기동 20:33(supervisord · 자동 배포)

---

## 1. 정정 — 요청문 §4 「WORKS 쪽 바꿀 것 없음」은 틀렸다

16:42 요청문에 「WORKS 쪽 — 바꿀 것 없음」이라고 적었는데, **WORKS 「🗓 회의 카드 모아보기」 탭(z1108 · 내가 만든 화면)이 이음 v795 판정을 그대로 복사해 쓰고 있었다.** 세션 10 이 짚어 줬다. 이 탭에서도:

- 녹음 없이 시작만 된 회의(#40·#33·#31·#28)가 **끝나고 12시간 「🔴 지금 회의 중」 묶음**에 남고,
- 카드 아래 줄은 **며칠이 지나도** 「🔴 회의 진행 중 · 이름 녹음」,
- 탭이 받는 자료 `/api/meetings/msg-cards` 에 **`recording` 칸이 없어** 녹음 중과 「시작만 누름」을 가를 수도 없었다(이음이 쓰는 `msg/status` 에는 z1114 부터 있음).

## 2. 고친 것 — 이음 v815 와 같은 규칙·같은 글

| 곳 | 고침 |
| --- | --- |
| `main.py` `/api/meetings/msg-cards` | 카드 `minutes` 에 **`recording`·`rec_secs`** (이음 `msg/status` 와 같은 값 — `_rec_view`) · 회의록 없는 카드는 예전 그대로 |
| `meetings.html` 윗줄 `headState` | `recording === false` 면 **끝 시각에 종료** · 끝 뒤 12시간 「회의 중」은 **녹음 중일 때만** · 시작 전·회의 시간 안에 누른 회의는 그대로 「회의 중」 · 칸 없는 옛 응답은 예전 그대로 |
| `meetings.html` 아래 줄 `minutesHtml` | 녹음 중 **「🔴 녹음 중 · 이름 · N분 저장됨」**(1분 미만은 분 없음) · 녹음 없음·끝 전 **「🔴 회의 진행 중 · 이름 · 녹음 안 됨」** · 녹음 없음·끝 뒤 **「⏹ 회의 끝남 · 이름 · 녹음 없음」**(흐린 글 · 대표 결정 문구) |

- 아래 줄이 **윗줄 판정(hs)을 따라가게** 해서, 30초마다 도는 다시 그리기 때 **두 줄이 항상 함께** 바뀐다.
- 참고: 녹음이 켜진 채 **끝나고 12시간이 지난** 회의는 윗줄 「✅ 회의 종료」 + 아래 줄 「🔴 녹음 중 · … 저장됨」 — 이음과 같다(서버에 녹음이 아직 열려 있다는 사실을 그대로 보여 줌 · 회의록 화면에서 끝낼 수 있음).

## 3. 검증 — 전부 통과 (합계 324)

| 시험 | 결과 |
| --- | --- |
| 서버 `verify_cards_rec.py`(가짜 이음 · TestClient) | **17/17** — 시작만=recording false·0초 · 녹음 중=true·600초 · 30초 · 정리 끝/녹음됨=false · 회의록 없는 카드 예전 그대로 · **이음 `msg/status` 와 5회의 모두 같은 값(단계·녹음·초)** |
| 화면 `ui_cards_rec.py`(실제 크롬 · 시계 고정) | **23/23** — A 시작만·끝남(종료·「회의 끝남·녹음 없음」·흐림·진행한 회의 묶음) · B 시작만·진행 중 · C 녹음 중·끝남(12시간 연장) · D 30초(분 없음) · E 옛 응답(예전 그대로) · F 시작 전 누름(회의 중) · G 녹음 중 13시간 전 끝(종료) · H 정리 끝(회의록 보기) · **I 끝 시각을 넘기면 30초 다시 그리기에 두 줄 함께 바뀌고 「진행한 회의」로 이동** · 스크립트 오류 0 |
| 회귀 모아보기 `verify_cards.py` | 80/80 |
| 회귀 모아보기 원래 화면 `ui_test.py`(8921+가짜 이음 8922) | 43/43 |
| 회귀 이음 상태 `verify_msg_rec.py` | 14/14 |
| 회귀 녹음 서버 `verify_rec.py` | 55/55 |
| 회귀 공유 받기 `verify_share.py` | 48/48 |
| 회귀 휴대폰 녹음기·목록 표시줄 `ui_phrec.py` | 44/44 |
| 표준 검사기(고친 트리 · 인라인 script 194) | 통과 |

## 4. 배포

| 단계 | 값 |
| --- | --- |
| 기준 blob 대조 | main.py · meetings.html · BAT = origin/main 과 같음(그사이 다른 세션 변경 없음) |
| 배포 전 기준값 | **20:31:32 · `{"v":"f84d620"}` HTTP 200** |
| push | 20:31:59 · `36ddf3a6` (파일 3개 · +23 −6) |
| 라이브 | **20:33:14 · `{"v":"9ed1299"}` HTTP 200 · 바뀜** |
| NAS 미러 | `9ed1299 mirror: claude_code 01_HAIST_WORKS @ 36ddf3a` |
| 운영 파일 sha256 | main.py `643dc2ad2a36a78b` · meetings.html `4acda5d60e017d69` = 준비물과 일치 |
| 재기동 | supervisord 가 자동 배포(cron)로 knk-works 재기동 · 기동 로그 오류 없음 |

> 참고: 아침 장애 뒤 멈췄던 **supervisord·cron 은 전산(최보현 상무)이 15:09 복원** — 옛 메일·메신저 설정도 빠졌다. 이번 배포가 자동 배포 경로 정상 복구를 실제로 확인한 첫 배포다.

## 5. 남은 것

- [ ] 대표님 확인: WORKS 회의록 → 「🗓 회의 카드 모아보기」 탭에서 오늘 「라이프밸류…[코엔] 미팅」이 **「✅ 진행한 회의」** 묶음에 「⏹ 회의 끝남 · 김동후 대표이사 총괄 · 녹음 없음」으로 보이는지.
- 되돌리기: 모노 `ec3cf5e6` 의 main.py·meetings.html 로 되돌려 push(DB 변화 없음 · API 에 칸만 늘었으므로 이음 영향 없음).

## 6. 준비물

`16_KNK_Meeting/작업기록/2026-09-21_1830_회의카드모아보기_끝났는데회의중_WORKS준비물/`
(patch_cards.py · patch_bat19.py · verify_cards_rec.py · ui_cards_rec.py · ui_test_cards_z1108.py · run_preview_z1119.py · run_phrec_app.py · main.py · meetings.html · KNK_시작.bat)
패치 순서: 운영(`f84d620` 기준) → `patch_cards.py` → `patch_bat19.py`
