# 회의록 화면 「🗓 회의 카드 모아보기」 탭 — WORKS 쪽 준비 완료 · 이음 입구 대기

- **작성**: 2026-09-17 (목) 08:45 KST
- **세션**: 16_KNK_Meeting (회의록 담당)
- **발단**: 대표 지시(사진 2장) — "사진1 이음 메신저에서 회의 등록한 회의 카드(회의 정보·회의록 등)를
  사진2 이음웍스 회의록 페이지에서 **회의 카드(회의록) 모아보기 탭**으로 한번에 모아서 볼 수 있게"
- **진행**: 구조 조사 → 실제 WORKS 화면에 탭을 붙인 미리보기(PC·휴대폰 사진) 보고 → 대표 **"진행해"**
- **상태**: ✅ **LIVE — z1108 (2026-09-17 13:14 · `f5c8149` → 라이브 `52799fb`)** · 이음 입구 v799 먼저 LIVE(12:51 · 세션 10) · ⏳ 대표 계정으로 첫 실제 연결 확인 대기 (7절)
- **갱신**: 2026-09-17 (목) 13:15 KST — 배포 결과 추가(7절) · 남은 일 정리(8절) / 13:28 WORKS→이음 길 확인 / 13:59 탭 사용 기록·세션 10 회신
- (08:45 당시 상태: WORKS 쪽 코드·시험 완료(운영 반영 전) · 세션 10 지시서 작성 완료 → 대표 전달 → 이음 입구 배포 대기)

---

## 1. 대표 결정 (미리보기 보고 때 추천안 그대로 승인)

| # | 결정 |
|---|---|
| ① | **보기 전용** — 참석 응답·수정·삭제·「▶ 회의 시작」은 지금처럼 이음 회의 카드에서 |
| ② | **보이는 회의 = 이음 🗓 회의와 똑같이** — 판단은 이음 서버 한 곳(`api_meetings_list` 규칙). 남이 만든 비공개 회의는 대표 계정에도 안 보임(이음과 같음) |
| ③ | 회의록 버튼은 지금처럼 **WORKS 열람 권한**(`_can_view_meeting`) — 회의 알림 카드(`msg/status`)와 같은 판단·같은 착지 |
| ④ | **순서 = 이음 입구 먼저 배포 → WORKS 탭 배포** (탭이 처음부터 제대로 나오게) |

※ 같은 「진행해」가 앞서 여쭌 **「회의록 더 자세하게 정리」** 까지 포함하는지는 분명하지 않아 그쪽은 **손대지 않음**(4단어 규약 — 임의 확대 금지). 대표께 다시 여쭘.

## 2. 왜 이음 쪽 작업이 필요한가 (조사 결과)

- 이음 회의 자료는 **이음 DB**(`meetings`·`meeting_attendees`)에만 있다. WORKS 에는 「▶ 회의 시작」을 누른 회의의 회의록만 있다.
- WORKS → 이음 서버끼리 통로는 **직원 명부(`/api/sso/directory`)·업무 알림(`/api/works/notify`)** 등뿐 — 회의 목록을 주는 입구가 없다.
- 이음 화면용 `GET /api/meetings` 는 이음 로그인(쿠키) 전용이라 WORKS 서버가 부를 수 없다.
- DB 직접 읽기는 컨테이너 분리(v5H226z493) 뒤 불가 · 공개범위 규칙을 WORKS 가 흉내내게 되므로 부적절.
- ⇒ 이음에 **서버 전용 입구 1개** `POST /api/works/meetings` (공유키) — **세션 10 담당**.
  지시서: `16_KNK_Meeting/작업기록/2026-09-17_0756_세션10전달_회의카드모아보기_이음회의목록입구_작업지시서.md`

## 3. WORKS 쪽에 만든 것 (운영 반영 전 · 준비물 폴더에 보관)

| 파일 | 내용 |
|---|---|
| `app/sso_client.py` | `fetch_msg_meetings(employee_no, from, to, timeout=8)` — 이음 입구 호출 · 오류를 사람말로(403 거부 / 404+`viewer_not_found` 직원 없음 / 그 밖 404 입구 없음 / 그 밖 코드 / 연결 실패 `code:"conn"`) |
| `app/main.py` | ① `meetings_page` — `?tab=cards` 를 **서버가** 판정해 템플릿에 `tab` 전달 (화면 전환 장치가 주소를 바꾸기 전에 스크립트를 돌릴 수 있어서) ② `GET /api/meetings/msg-cards` — 이음 결과 정리(`_msg_card_clean`: 필요한 칸만·길이 제한·이상한 값 기본값·같은 번호 한 장) + WORKS 회의록 단계(`_meeting_msg_stage`)·열람(`_can_view_meeting`)·주소(정리 끝=양식 `/doc`, 그 밖=회의록 화면)·결정/할 일 수(볼 수 있을 때만) |
| `app/templates/meetings.html` | 탭 2개(📝 회의록 N / 🗓 회의 카드 모아보기) · 카드 화면(보기 전용) · 기존 목록은 그대로 |

### 동작 규칙
- 기간: 처음 = 오늘-60일 ~ 오늘+366일(보는 사람 소속 날짜 · 본사 +9 · 베트남 +7) / 「▼ 지난 회의 더 보기」 = 그 앞 60일씩 / 한 번에 450일까지.
- 묶음: 🔴 지금 회의 중 → 🔜 진행할 회의(가까운 순) → ✅ 진행한 회의(최근 순) — 이음 🗓 목록과 같은 나눔 + 회의 중 묶음.
- 카드 윗줄 상태 = **이음 v795 `_mtgHeadState` 와 같은 판단**(녹음 끝나면 종료 · 녹음 중이면 끝나고 12시간까지 회의 중 · 그 밖은 시각).
- 시각 표기 = 이음과 같이 **등록한 사람 소속 시각 + (한국/베트남)** · 시작~끝(다음 날이면 월-일) · 진행 시간(1일 1시간 30분).
- 응답 현황(✅❌🔄⬜)은 **등록 담당에게만**(이음과 같음) · 참석자는 「내 응답」.
- 회의록: 정리 끝 = 「📋 회의록 보기」(**새 창** — 회의록 화면의 「📄 회의록 양식」과 같은 방식) + 결정·할 일 수 / 녹음 중·정리 중·실패 = 상태 글 + 「📝 회의록 화면」 / 「회의 시작」 안 누른 지난 회의 = 「회의록 없음」 / 볼 수 없는 회의록 = 아무것도 안 보임(이음과 같음).
- **WORKS 보호**: 이음 호출은 스레드(`run_in_threadpool`) · 8초 제한 · 사람+기간별 30초 저장 · 연결 실패 뒤 15초는 다시 부르지 않음 · 탭을 열 때만 부름(회의록 탭은 안 부름).
- 화면: 30초마다 윗줄만 확인(바뀐 카드 있을 때만 다시 그림) · 2분마다 단계 새로 받기 · 화면을 떠나면 멈춤 · 보이지 않는 창에선 안 함 · 실패하면 탭에만 안내 + 「다시 시도」(카드가 이미 있으면 알림만 · 카드 유지).
- 사람 참조 = **사번만**(`users.employee_no`) — 사번 없는 계정은 이음에 묻지 않고 안내.

## 4. 검증 — 전부 통과

| 묶음 | 결과 | 방법 |
|---|---|---|
| API·화면 종단 | **79/79** | 실제 WORKS 앱(TestClient) + 가짜 이음 입구(`httpx` 바꿔 끼움): 탭 판정·로그인·기간(450/451일·형식 오류·베트남 날짜)·보낸 내용(사번·기간·공유키·주소·8초)·오류 7종 안내·실패 저장 안 함·연결 실패 쉼·30초 저장·닫힌 포트 실연결·단계 5종·권한(대표·남·참석자·등록 담당·전체 공개)·자료 정리(틀린 항목·길이·응답값·기본값·외부 30명·참석자 300명·같은 번호·회의 내용/줌 링크 안 넘김) |
| 진짜 크롬 | **38/38** | 실제 WORKS 서버 + 가짜 이음 서버(느리게·거부·직원 없음 조절): **이음 4초 느려도 다른 요청 0.5초 응답**(일꾼 1개 보호) · 9초면 8초에 끊고 안내 · 탭 전환(주소 `?tab=cards`)·카드 수·**악성 글자 실행 안 됨(이스케이프)** · 새 창 양식 · 시계 조작으로 **시작 시각 지나면 회의 중으로** · 2분 뒤 새로 받기 · 더 보기(바로 앞 60일) · 새로 고침 뒤 더 불러온 회의 유지 · 회의록 화면 이동 · **떠난 뒤 멈춤** · 뒤로 가기 · 오류→다시 시도 · 불러오는 중 더 보기 숨김 · 0건 안내 · 휴대폰(본문 넘침 없음·탭 반반·누르기) · 콘솔 오류 0 |
| 회귀(TestClient) | **92/92** | 양식 z1102 48 · 회의록 보기 z1103 22 · 이미지 z1105 22 — 새 main.py 로 |
| 회귀(진짜 크롬) | **62/62** | z1106 저장 안 됨·나갈 때 알림 시험 — 새 코드(목록 화면에 탭 생김)로 |
| 표준 검사기 | 통과 | `deploy/check_standards.py` 전체(JS 문법 191블록 등) · **일부러 스크립트를 깨뜨리면 meetings.html 에서 잡힘** 확인 · `node --check` 통과 |

### 시험 중 찾아 고친 것 (배포 전)
1. **버튼 이름 `main` 충돌** — 「📋 회의록 보기」를 `class="mc-btn main"` 으로 했더니 **본문 틀 `.main` 의 스타일**(flex·배경·아래 여백 ::after)이 붙어 흰 글자 타원이 됨. `.main` 은 화면 전환 장치가 본문을 찾는 이름이기도 함 → `mc-go` 로 바꿈. ⚠ **WORKS 화면 안 요소에 `main` 클래스를 쓰지 말 것.**
2. **공용 `.btn` 의 display 가 `hidden` 속성을 덮음** — 오류·불러오는 중에도 「더 보기」가 보임 → `#mcMore[hidden]{display:none}`.
3. 탭·카드 좌우 여백 — 기존 `.bento`·`.section` 처럼 `var(--content-pad)`.
4. 사번이 빈 계정을 로그인ID로 대신 찾던 부분 제거(WORKS users 에 username 칸 없음 · 사람 참조 = 사번).

### 시험 환경 관찰 (이번 변경과 무관 · 운영 실측 없음)
- 시험 크롬 휴대폰 화면에서 **상단 줄 아이콘이 오른쪽으로 넘침**(문서 폭 745) — 연락처 화면도 같음. 설치 안내 창(`worksInstallHint`)이 시험 중 화면을 덮기도 함.
- 이 PC 에서 로컬 HTTP 요청마다 약 0.5초가 더 걸림(윈도우 httpx 연결 만들기) — 운영(리눅스)과 다름.
- 시험 크롬은 화면을 연 직후 한 번 더 읽음(z1106 때와 같음) → 시험은 `networkidle`+1.5초 기다림.

## 5. 준비물 (다른 세션·나중에 이어서 할 때)

폴더: `16_KNK_Meeting/작업기록/2026-09-17_0845_회의카드모아보기_WORKS준비물/`

| 파일 | 쓰임 |
|---|---|
| `patch_main.py` | `py patch_main.py <원본 main.py> <결과>` — 기준 origin/main `f79e4ed8` (main.py blob `fb894061`) · 앵커가 안 맞으면 멈춤 |
| `patch_sso.py` | `py patch_sso.py <원본 sso_client.py> <결과>` — 기준 blob `5511098a` |
| `meetings.html` | 새 목록 화면 통째(기준 blob `c6ab1afa` 에서 바뀐 것: 탭·카드 화면·배지 2개 nowrap) · sha256 `27078be8…` (09:54 잘림 이어보기 반영 · 그 전 `460c1cf9`) |
| `verify_cards.py` | 앱 사본 폴더에서 `py -3.13 ../verify_cards.py` (79) |
| `ui_test.py` · `run_preview.py` | 미리보기·크롬 시험 서버(WORKS 8921 + 가짜 이음 8922 · 조절 `/__ctl`) · `py -3.12 ui_test.py` (38 → 잘림 이어보기 뒤 43) |
| `compare_tree.py` | (배포 때 추가) `py compare_tree.py <커밋> <저장소 안 경로> <사본 폴더>` — 시험 사본이 배포될 트리와 같은지 git blob 해시로 대조 |
| `patch_bat8.py` | (배포 때 추가) BAT LAST UPDATE z1108 줄 — `py patch_bat8.py <원본 bat> <결과 bat> z1108` |

결과 파일 해시(기준에서 만든 것): main.py `f8219175…`(blob `ca162ba3`) · sso_client.py `aa999c73…`(blob `f45cc838`) · meetings.html `27078be8…`(blob `2e28b140`)

## 6. 세션 10 회신 반영 (09:40 회신 → 09:54 반영)

- 세션 10 회신: `POST /api/works/meetings` **지시서 계약 그대로 구현(이음 v799 · 대표 결정 전 직원 · 이음 게이트 없음)**,
  세션 16 준비본(해시 main.py `f8219175` · sso_client.py `aa999c73` · meetings.html `460c1cf9`)을 이음 시연 서버에 붙여 크롬 **14/14**
  (대표 140장 = 입구 140건 · 직원 126장 = 126건 · 없는 사번 안내 · 콘솔 오류 0). **이음 배포는 업무 시간 뒤 · 그 전에 WORKS 배포 금지.**
  상세: `10_KNK_Messenger/작업기록/2026-09-17_0937_Eum_v799_WORKS회의카드모아보기_이음회의목록입구_배포대기.md`
- 회신에서 정한 세부 중 WORKS 에 걸리는 것:
  - 응답 없음 = **`null`** → `_msg_card_clean` 이 `null`·`""` 둘 다 빈 응답으로 바꿈(수정 불필요) · 시험용 가짜 입구도 `None` 으로 맞춤 · 시험 추가(E1b)
  - 옛 응답 값 `accept`(시연 DB 에만 · 운영 실측 없음) → WORKS 에선 ⬜(이음 🗓 화면도 ⬜ 로 셈 — 같음)
  - 🔴 **300건이 넘으면 시작이 이른 회의부터 빠짐(truncated)** → 준비본은 「더 보기」가 **잘린 날짜를 건너뛰고** 그 앞 기간부터 물어 **빠진 회의가 영영 안 보일 수 있었음**
    → **고침(meetings.html `460c1cf9` → `27078be8`)**: 잘렸으면 받은 것 중 **가장 이른 날짜까지만 '빠짐없음'** 으로 보고, 더 보기는 **그 날짜를 포함해** 다시 묻는다(겹치는 회의는 번호로 한 장).
    새로 고침 때도 그 경계 기준으로 더 불러 둔 회의를 남김. 한 날짜에만 300건이 넘는 경우(날짜로는 더 못 나눔)는 되풀이하지 않고 넘어감.
    안내 글: 「회의가 많아 한 번에 다 불러오지 못했습니다 — 「▼ 지난 회의 더 보기」로 이어서 볼 수 있습니다.」
  - 계약·서버 코드(main.py·sso_client.py) 변경 없음 → 세션 10 이 다시 맞춰 볼 필요 없음
- 재검증: API·화면 종단 **80/80**(+null·accept) · 진짜 크롬 **43/43**(+잘림 이어보기 5: 한 번에 5건만 주는 가짜 입구로 5→9→10장 · 중복 없음 · 이어 묻기가 잘린 날짜 포함 · 새로 고침 뒤 유지) · 표준 검사기 통과 · `node --check` 통과
  (크롬 시험 기준 조정: 이 PC 는 요청마다 0.5초+ 더 걸려 '막히지 않음' 기준을 2초로, 끊김 시험은 이음 지연 15초로 — 8초에 끊겼는지 구별되게)

## 7. 배포 — ✅ LIVE z1108 (대표 「지금 바로」)

- **발단 (대표 전달 · 세션 10)**: 이음 입구 운영 반영 **12:51:04** · 커밋 `9b99316`(v800 과 함께) · 되돌림 태그 `rollback-20260917-1250` ·
  키 없이 `POST https://msg.knknara.co.kr/api/works/meetings` → **403 `forbidden`** 확인 → 「이제 WORKS 탭을 배포해도 됩니다」 ·
  ⚠ 이음은 운영 공유키로 목록을 받아 본 적 없음 → **대표 계정 확인이 첫 실제 연결**
  (회신 문서 `10_KNK_Messenger/작업기록/2026-09-17_0940_세션16회신_…_배포대기.md` 맨 위)
- **올릴 시각**: 알림을 받은 12:56 은 곧 업무 시간(13:00)이라 대표께 여쭘(저녁 7시 30분 이후 / 지금 바로) → 대표 **「지금 바로」**
- **다시 확인 (12:56~13:11)**
  - origin/main `e50678fd` — 준비 기준 `f79e4ed8` 뒤 바뀐 것은 z1107(meeting_form.html·BAT)과 기록 1개뿐 → 제 3개 파일의 기준 blob 그대로(main `fb894061` · sso `5511098a` · meetings `c6ab1afa`)
  - 시험 사본 3벌(카드 · 나갈 때 알림 · 마이크) = `e50678fd` 트리 + 이 3개 파일 — `compare_tree.py` 로 425개 파일 blob 대조(다른 것 = 이 3개뿐 · z1107 녹음 화면 포함)
  - 종단(TestClient) **80/80** · 크롬 화면 **43/43** · 나갈 때 알림(z1106) **62/62** · 녹음 화면 마이크 안내(z1107) **25/25** · 표준 검사기 통과
  - 준비물 스크립트로 기준 blob 에서 다시 만든 파일 = 시험한 파일과 **바이트 동일**
- z 번호: **z1108** (`git log --all` 최댓값 z1107 + 1)
- 올리기 **전** 기준값 (13:12:51): HTTP 200 · `{"v":"38935ef"}`
- 모노레포 `origin/main` push (13:12:55): `e50678fd` → **`f5c8149`** (빨리감기 · 임시 인덱스 · 제 작업 폴더 무접촉) — main.py · sso_client.py · meetings.html · KNK_시작.bat
  - 13:14:06 재시작 중 안내 화면(시놀로지 · 성공으로 보지 않음)
- **라이브 확인 (13:14:11)**: HTTP **200** · `{"v":"52799fb"}` · 기준값과 **다름**
- **운영 컨테이너 실물** (읽기 전용 SSH)
  - `app/main.py` sha256 `f8219175…` · `app/sso_client.py` `aa999c73…` · `app/templates/meetings.html` `27078be8…` = **시험한 파일과 바이트 동일** (13:14:03 갱신)
  - `app/templates/meeting_form.html` `94628b1b…` = z1107 그대로
  - 배포 저장소 `52799fb` = `mirror: claude_code 01_HAIST_WORKS @ f5c8149` (제 커밋)
  - WORKS 서버(uvicorn) 13:14:03 재시작
- 미로그인 확인 (13:14:43): `/api/meetings/msg-cards` → **401** 「로그인이 필요합니다.」(배포 전 404) · `/meetings?tab=cards`·`/meetings`·`/meetings/1` → 303 `/login` · `/login` → 이음 SSO
- **WORKS → 이음 길 확인 (13:27 무렵 · 읽기 전용)**: WORKS 프로그램(pid 14583 · `/opt/knk_haist`)이 쓰는 이음 주소 = 내부 `http://172.17.0.1:5052`
  (`KNK_MESSENGER_SSO_INTERNAL_BASE` · 이름만 골라 읽음) → WORKS 컨테이너 안에서 **키 없이** `POST /api/works/meetings` → **403 `{"error":"forbidden"}`**
  = 입구 있음·길 열림(자료 안 받음). 공유키가 맞는지는 대표 계정 확인으로(같은 공유키를 쓰는 기존 기능 = 명부 `/api/sso/directory`·업무 알림 `/api/works/notify` · 오늘 따로 실측은 안 함).
  - ※ 첫 시도는 같은 컨테이너의 **포털 프로그램**(pid 2191 · `/opt/knk_portal`)을 잘못 집어 설정 이름 2개만 읽음(둘 다 비어 있음 · 비밀값 출력 없음 · 바꾼 것 없음) → 프로그램 경로로 다시 집어 확인.

## 8. 남은 일

- [x] 대표 → 세션 10 지시서 전달 → 이음 입구 구현(09:40 회신) → **이음 배포 12:51 · `9b99316`**
- [x] **세션 16 WORKS 배포 z1108** (13:14 · `f5c8149` → 라이브 `52799fb`) — 7절
- [ ] **대표 계정으로 「🗓 회의 카드 모아보기」 탭 열기** — 이음 🗓 목록과 같은 회의가 나오는지(운영 공유키 첫 실제 연결).
      안내 글이 나오면 그 글 그대로 받아 원인 구분: 「이음이 요청을 거부했습니다(연결 키 확인 필요).」 / 「이음에서 내 계정(사번)을 찾지 못했습니다.」 / 「이음에 회의 목록 입구가 아직 없습니다…」 / 「이음에 연결하지 못했습니다…」
  - (13:59 확인) WORKS 서버 기록 `/opt/knk_haist/logs/uvicorn.log`(읽기만 · 주소 가림): 배포 뒤 **로그인한 사용자의 `GET /meetings?tab=cards` 200 → `GET /api/meetings/msg-cards` 200 한 번** · `uvicorn-error.log` 에 `[MSG-CARDS]` 연결 실패 없음.
    ⚠ 이 API 는 이음이 거부해도 200 + 안내 글(`ok:false`)로 답하므로 **기록만으로는 카드가 보였는지 모름**. 세션 10 공용 메모에는 「대표 계정 실제 연결 확인(09-17 오후)」 → **대표께 한 번 여쭤 닫기**.
    (그 앞 404·401·303 줄 = 제 배포 전후 확인 요청)
- [x] 대표님을 통해 세션 10 에 WORKS 배포(z1108 · 13:14) 알림 — 세션 10 확인(운영 `52799fb`) · **빅터 사용법 한 줄은 이음 v801 에 넣어 오늘 업무 시간 뒤 배포**(세션 10 · 배포 시각은 배포 뒤 알림)
- [ ] 대표 확인 대기: 「회의록 더 자세하게 정리」 반영 여부(비교본 보고함 · 09-17 「진행해」 포함 여부 불분명 → 미착수)
