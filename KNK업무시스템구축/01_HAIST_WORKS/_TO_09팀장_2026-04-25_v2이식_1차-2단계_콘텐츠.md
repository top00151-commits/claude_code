# TO 09팀장 — 01 v2 이식 1차-2단계 (페이지 콘텐츠) 완료
- 일시: 2026-04-25
- 발신: 01 메인(실무팀) 빅터
- 범위: 페이지 콘텐츠 영역 폰트 토큰 잔여 적용 (홈/일일/달력/피드 main 영역) + BAT 갱신

## 1. 페이지별 v2 정합 점검 결과 (마크업 변경 0건)
| 템플릿 | v2 클래스 정합 상태 | 변경 |
|---|---|---|
| `app/templates/home.html` | `hm2-*` v2 풀세트 적용 (탭 / hm2-kpi / hm2-quick) | 0 |
| `app/templates/daily.html` | `.page-head` / `.task-grid` / `.task-card` / `.empty` (line 4277 sage 토큰화) | 0 |
| `app/templates/calendar.html` | `.page-head` / `.summary-grid` / `.cal-tbl` (line 327 `var(--text-sm)`) | 0 |
| `app/templates/feed.html` | `.page-head` / `.headline-grid` / `.dense-list` / `.dl-header` | 0 |

→ 페이지 콘텐츠 마크업 자체는 이미 시안 12B + 힐링 sage 톤 정합 완료 (commit `c2fd06f` / `f457fdd`). 본 2단계는 **CSS 토큰 잔여 적용**에 집중.

## 2. 폰트 토큰 잔여 적용 매핑 (style.css 본체 외부, 4287~4574 무수정)
| line | 클래스 | 변경 전 | 변경 후 |
|---|---|---|---|
| 713 | `.dl-header,.dl-row` | `font-size:15px` | `font-size:var(--text-sm)` |
| 714 | `.dl-header` | `font-size:14px` | `font-size:var(--text-sm)` |
| 722 | `.dl-team-head` | `font-size:15.5px` | `font-size:var(--text-sm)` |

→ 모두 피드 페이지 dense-list 영역 (페이지 콘텐츠). 사이드바 = `--text-sm`, 메뉴 ≤ 본문 비례 유지.
→ `.page-head/.section-head/.empty` 등은 line 3925~ 후속 cascade에서 이미 `--fs-fluid-*` 토큰화 완료.

## 3. v2 본체 (line 4287~4574) 무수정 검증
- 시작 라인 (4287): `/* C안 (시안 12B 통째 이식) overrides — 2026-04-25 healing-c */` 그대로
- 본 2단계 변경은 모두 line 713/714/722 (본체 외부) — **본체 0줄 수정 절대 준수**

## 4. 변경 라인 수 (이번 사이클 vs 누적 미커밋 분리)
| 구분 | 파일 | +ins | -del | 합 |
|---|---|---|---|---|
| **이번 사이클** | static/style.css | +3 | -3 | 6 |
| **이번 사이클** | app/templates/base.html | +2 | -2 | 4 |
| **이번 사이클 합계** | — | **+5** | **-5** | **10** |
| **누적 미커밋** (since `f457fdd`) | static/style.css | +224 | -187 | 411 |
| **누적 미커밋** | app/templates/base.html | +13 | -10 | 23 |
| **누적 미커밋 합계** | — | **+237** | **-197** | **434** |

→ 누적은 1단계(헤더) + 2단계(콘텐츠) + 사이클 14 이전 이식 작업의 미커밋 합산.

## 5. 핫패치 G1~G5 보존 grep
| ID | 패턴 | 기대 | 실측 | 결과 |
|---|---|---|---|---|
| G1 | `class="sb-toggle"` (base.html) | 0 | 0 | PASS |
| G2 | `^\.dock\s*\{[\s\S]*?top:\s*0\s*[;}]` | 0 | 0 | PASS |
| G3 | `^body\.dock-open\s+\.topbar\s*\{` | 0 | 0 | PASS |
| G4 | `\.dock\s*\{[^}]*top:\s*var\(--topbar-h` | ≥1 | 1 | PASS |
| G5 | `body\.dock-open\s+\.main-wrap\s*\{\s*margin-right:\s*420px` | ≥1 | 1 | PASS |

→ **5/5 PASS**.

## 6. 자기검증
- v2 본체 (line 4287~4574) 자체 수정: **0줄** (절대)
- 외부 자산: **0건**
- `.hm2-fixed` 미접촉: home.html top_fixed 블록 무수정 (3단계 잠금 영역)
- 도크 본체 (`.dock` line 3650~) 미접촉

## 7. BAT 갱신
- base.html line 7~8:
  - `{# LAST UPDATE: 2026-04-25 v2이식-1차-2단계-콘텐츠 — 페이지 콘텐츠 폰트 토큰 잔여 적용 (.dl-header/.dl-row/.dl-team-head) — G1~G5 보존 #}`
  - `<link rel="stylesheet" href="/static/style.css?v=20260425v2-stage2-content">`

## 8. 다음 단계 예고
- **v2 1차-3단계** = `.hm2-fixed` (home.html top_fixed 영역 → main-wrap 외부 G1 핫패치 영향) + 도크 본체 (`.dock` line 3650~) 시안 12B 정합
- 작업 시간 추정: 15~20분
- 잠금 해제 필요 (3단계에서만)

## 9. 04 회귀팀 핸드오프
- 변경 표면: base.html 2줄 (LAST UPDATE + 캐시 키) + style.css 3줄 (피드 dense-list 폰트 토큰)
- 회귀 포커스:
  1. 피드 페이지 (`/feed`) dense-list 헤더/행 폰트 시각 (--text-sm = clamp(13~15px))
  2. 캐시 무효화 (`?v=20260425v2-stage2-content`)
  3. G1~G5 5건 (위 §5 명령 그대로)
  4. v2 본체 4287~4574 무수정 (line 4287 시작 주석 확인)
