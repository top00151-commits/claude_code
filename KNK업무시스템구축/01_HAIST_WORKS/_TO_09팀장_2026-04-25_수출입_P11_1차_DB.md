# 01 → 09 회신 — 수출입 P11 1차 (DB 스키마 + 라우트 골격)

**작성**: 빅터 (01 메인) · **일시**: 2026-04-25 · **사이클**: 09 발주 → 01 작업 → 04 회귀 대기

---

## 1. 신규 테이블 6개 (한국 수출 표준)

`app/database.py` 끝부분 INIT_SQL 에 idempotent CREATE TABLE IF NOT EXISTS 6건 추가.

| # | 테이블 | 핵심 컬럼 |
|---|--------|----------|
| 1 | `export_orders` | `order_id` (orders 참조) / `buyer` / `shipping_terms` (FOB/CIF/EXW/DDP) / `payment_terms` (T/T·L/C·D/P·D/A) / `port_of_loading` / `port_of_discharge` / `status` (DRAFT/BOOKED/SHIPPED/CLEARED/CLOSED/CANCELLED) |
| 2 | `commercial_invoices` (CI) | `invoice_no` UNIQUE / `export_order_id` / `issue_date` / `total_amount` / `currency` / `signed_by` / `status` (DRAFT/ISSUED/CANCELLED) |
| 3 | `packing_lists` (PL) | `pl_no` UNIQUE / `export_order_id` / `total_packages` / `total_weight` (kg) / `total_volume` (CBM) |
| 4 | `packing_items` | `pl_id` / `part_id` / `qty` / `package_type` (CARTON/PALLET/WOODEN CASE) / `weight` / `volume` |
| 5 | `bills_of_lading` (B/L) | `bl_no` UNIQUE / `export_order_id` / `shipping_company` / `vessel` / `departure_date` / `arrival_date` / `tracking_no` / `status` (DRAFT/ISSUED/IN_TRANSIT/DELIVERED/CANCELLED) |
| 6 | `customs_declarations` | `declaration_no` UNIQUE / `export_order_id` / `hs_code` / `fta_origin` (KR/VN) / `declared_value` / `cleared_at` / `status` (DRAFT/SUBMITTED/CLEARED/REJECTED/CANCELLED) |

인덱스: 각 테이블 핵심 FK·status 12개 idempotent 생성. 기존 orders/customers/parts/users 테이블 참조 — **v2 본체 미접촉**.

## 2. 신규 라우트 12개 (`app/main.py` 말미 추가)

권한 가드 단일 정의 — `_export_guard()`:
- `admin/ceo/executive` → 항상 허용
- `team_id == 12` (베트남법인 = P11) → 허용
- `can_use_sales(u)` 영업권한자 → 허용
- 그 외 → None

| 메서드 | 경로 | 용도 |
|---|---|---|
| GET  | `/export` | 수출 메인 (수주 목록) |
| GET  | `/export/orders/new` | 등록 폼 (orders 100건 드롭다운) |
| POST | `/export/orders` | 수출 수주 INSERT (**매출 자동 채움**: `order_id` 검증 → orders 조회) |
| GET  | `/export/orders/{eo_id}` | 상세 (CI/PL/BL/관세 4탭) |
| GET  | `/export/ci/{eo_id}` | CI 폼 (orders.total_amount 자동 채움) |
| POST | `/export/ci` | CI INSERT 골격 (다음 사이클) |
| GET  | `/export/pl/{eo_id}` | PL 폼 |
| POST | `/export/pl` | PL INSERT 골격 |
| GET  | `/export/bl/{eo_id}` | BL+관세 통합 폼 |
| POST | `/export/bl` | BL INSERT 골격 |
| GET  | `/export/customs/{eo_id}` | → BL/관세 통합 페이지로 redirect |
| POST | `/export/customs` | 관세 INSERT 골격 |

기존 `/export/weekly` (CSV 내보내기) 와 충돌 없음 — FastAPI 경로 매칭 분리 확인.

## 3. 신규 템플릿 6종 (모두 `base_logi.html` 확장 + WIP placeholder)

`export_home.html` (47) · `export_order_form.html` (65) · `export_order_detail.html` (48) · `export_ci.html` (38) · `export_pl.html` (38) · `export_bl_customs.html` (78)

## 4. BAT 갱신 (절대준수)

- `KNK업무시스템구축/START.bat` LAST UPDATE → `2026-04-25 수출입-P11-1차`
- `KNK업무시스템구축/KNK_시작.bat` 동일 갱신

## 5. 핫패치 G1~G5 grep — 보존 확인

```
static/style.css:99   /* A2 폰트 토큰 G17 */
static/style.css:2253 /* 핫패치 G1·G5 영향 없음 */
static/style.css:3634 /* G1 margin-right 적용 */
```
style.css 본 사이클 미접촉 → G1~G5 5건 모두 보존.

## 6. 변경량 — 이번 N줄 / 누적 M줄 분리 표기

| 파일 | 이전 | 이후 | **이번 N** |
|---|---:|---:|---:|
| `app/database.py` | 5239 | 5337 | **+98** |
| `app/main.py` | 5254 | 5492 | **+238** |
| 6 템플릿 (신규) | 0 | 314 | **+314** |
| START.bat / KNK_시작.bat | - | - | **+6** |
| **이번 합계** | | | **+656줄** |
| **누적 (database+main+templates)** | | **약 11,143줄** | |

## 7. 자기검증 의무 점검

- [x] G1~G5 핫패치 5건 보존 (style.css 미접촉)
- [x] v2 본체 미접촉 (orders/customers/parts 컬럼 변경 0)
- [x] 외부 자산 0건 (운송회사 API 미도입 — 운송사명 수동 입력 명시)
- [x] DB 스키마 idempotent (CREATE TABLE IF NOT EXISTS 6건 + INDEX 12건)
- [x] 신규 라우트 권한 분기 (`_export_guard()` 단일 정의 12건 일관 적용)
- [x] BAT 갱신 (2개 BAT 모두)

## 8. 다음 사이클 예고 — 수출입 2차

- UI 본문 (탭 콘텐츠 · 라인 항목 packing_items 그리드)
- CI 매출 자동 채움 핸들러 본문 (orders.total_amount + currency 변환)
- PL/BL/관세 INSERT 본문 (월별 시퀀스 PL-YYYYMM-####, CI-YYYYMM-####)
- export_orders.status 라이프사이클 전이 (DRAFT → BOOKED → SHIPPED → CLEARED → CLOSED)
