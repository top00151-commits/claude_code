fastapi
uvicorn[standard]
jinja2
python-multipart
itsdangerous

# v5H226z108n17~n19 (2026-05-21) — AI 연동 (Victor 고도화·한↔베 번역·문서요약)
# 공급사 무관: Claude(anthropic) 또는 OpenAI(openai) 중 결제되는 쪽 선택 (KNK_AI_PROVIDER).
# 키는 OS 환경변수로만 등록 (KNK_AI키등록.bat). 둘 다 옵션 — 미설치/미등록 시 자동 비활성.
anthropic>=0.40
openai>=1.40

# v5H210 (2026-05-08) — 한국·베트남 공휴일 자동 계산 (음력 환산·대체공휴일 규칙 포함)
# 라이브러리 업데이트만으로 매년 자동 갱신 — 수동 입력 불필요
holidays>=0.40

# v5H226z108n20 (2026-05-28) — 자재 QR 코드 SVG 생성
qrcode

# v5H226z117 (2026-05-31) — SSO Phase 2: 메신저 JWT(RS256) 토큰 검증
# cryptography 는 RS256 디코딩에 필요 (PyJWT extras로 자동 설치)
PyJWT[crypto]>=2.8.0
httpx>=0.27.0  # 메신저 SSO public-key / userinfo 호출 (anthropic 가 끌어와도 명시)

# v5H57 (2026-05-03) — 사업자등록증 자동 파서
pdfplumber          # 텍스트 PDF 추출 (홈택스/정부24 발급분)
Pillow              # 이미지 처리

# 옵션 (이미지/스캔본 OCR — Tesseract 별도 설치 필요)
pytesseract         # Python wrapper (Tesseract 바이너리 별도)
pdf2image           # 스캔 PDF → 이미지 변환 (poppler 별도)

# 비고:
# - pdfplumber 만으로 홈택스/정부24 발급 PDF 90% 자동 추출 가능 (외부 의존 0)
# - pytesseract + pdf2image 는 Python 설치만, 추가 바이너리 필요:
#   · Tesseract OCR (Windows 설치 시 Korean 언어팩 체크)
#     https://github.com/UB-Mannheim/tesseract/wiki
#   · Poppler (스캔 PDF → 이미지 변환용)
#     https://github.com/oschwartz10612/poppler-windows/releases
# - 미설치 시 PDF만 동작. 이미지 업로드 시 친절한 안내 메시지 표시.
