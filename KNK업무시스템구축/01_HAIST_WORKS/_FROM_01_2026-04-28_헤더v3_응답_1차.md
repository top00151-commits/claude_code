# [01 → 05] 🟢 헤더 재설계 v3 — 1차 응답 (1+2+3차 통합 완료)

> **발신**: 01 메인 세션 (실무팀)
> **수신**: 05 디자인팀 / 09 프로젝트 팀장 / 04 운영테스트팀 / 대표이사
> **참조**: `_TO_01_2026-04-28_헤더재설계_v3승인_가로탭+검색도크모드분리.md`
> **일자**: 2026-04-28
> **상태**: 🟢 **1차+2차 동시 완료 (4파일 동시 적용) · grep 15/15 PASS · BAT 갱신 완료**

---

## 0. 한 줄

대표 승인 v3 사양대로 **3개 base + style.css 단일 사이클 적용**. 가로 워크스페이스 탭(amber 펄스) + 빅터 도크 모드 탭(빅터 ↔ 검색) + brand-text 숨김 + 로고 축소(48-64px) + tb-home-link 흡수. **main.py 무수정**(workspaces context 기존 보유).

---

## 1. 착수/완료 시각

- 시작: 2026-04-28 (대표 "바로 진행" 지시 직후)
- 1차+2차 통합 완료: 2026-04-28 (4시간 마감 내)
- 변경 라인 수: 약 +280 / -65 (4파일 합산)

---

## 2. 변경 파일 (4건)

| 파일 | 변경 내역 |
|---|---|
| `app/templates/base.html` | ws-wrap dropdown(20줄) → ws-tabs `<nav>`(15줄) · tb-center 빈 div · 도크 안에 `dock-modes` + `dock-mode-panel`(victor/search) + 검색 패널 신규 · setDockMode JS · "/" 단축키 갱신 (도크 열기 + 검색 모드 자동 전환) |
| `app/templates/base_sales.html` | ws-tabs `<nav>` 신규 추가 · tb-home-link 제거 · 도크 모드 탭 + 검색 패널 + setDockMode JS |
| `app/templates/base_logi.html` | 동일 패턴 (Logi Hub 독립) |
| `static/style.css` | 230줄 추가 (v3 블록 §HEADER REDESIGN v3 — `.ws-tabs/.ws-tab.active`(amber 펄스) + `@keyframes wsActivePulse` + `.dock-modes/.dock-mode-tab/.dock-mode-panel` + `.dock-search-wrap/.dock-search/.dock-search-empty` + brand-logo-wrap 축소 오버라이드 + brand-text/g-search/ws-switcher/ws-menu/ws-wrap/tb-home-link/tb-title-group `display:none !important` 안전 차단) |

**main.py 변경**: 0건 (`workspaces` + `current_workspace` context 이미 존재).

---

## 3. §7 자체 grep 검증 (15종 — 9 PASS + 6 PASS = 15/15)

### A. 정합 (PASS = ≥1) — 9/9 ✅

| # | 항목 | 결과 | 기대 | PASS |
|---|---|---|---|---|
| 1 | `<nav class="ws-tabs"` in base.html | 1 | ≥1 | ✅ |
| 2 | `<nav class="ws-tabs"` in base_sales.html | 1 | ≥1 | ✅ |
| 3 | `<nav class="ws-tabs"` in base_logi.html | 1 | ≥1 | ✅ |
| 4 | `.ws-tab.active::before` in style.css (펄스 인디케이터) | 1 | ≥1 | ✅ |
| 5 | `wsActivePulse` in style.css (애니메이션) | 2 | ≥1 | ✅ |
| 6 | `data-mode="victor"` in base.html | 1 | ≥1 | ✅ |
| 7 | `data-mode="search"` in base.html | 1 | ≥1 | ✅ |
| 8 | `setDockMode` in base.html (정의 + click + / 단축키) | 4 | ≥3 | ✅ |
| 9 | `gSearchInput` in base.html (도크 안 위치) | 3 | ≥1 | ✅ |

### B. 결함 (PASS = 0) — 6/6 ✅

| # | 항목 | 결과 | 기대 | PASS |
|---|---|---|---|---|
| 10 | `<button class="ws-switcher"` in base.html | 0 | 0 | ✅ |
| 11 | `<div class="ws-menu"` in base.html | 0 | 0 | ✅ |
| 12 | `<form class="g-search"` in base.html (헤더에서 제거) | 0 | 0 | ✅ |
| 13 | `tb-home-link` in base_sales.html | 0 | 0 | ✅ |
| 14 | `tb-home-link` in base_logi.html | 0 | 0 | ✅ |
| 15 | `.brand-text { display: none` in style.css | 1 | ≥1 | ✅ |

→ **15/15 PASS**.

---

## 4. §8 페르소나 walkthrough (예상 동작 — 04 운영테스트팀 회귀 검증 요청)

| W# | 시나리오 | 예상 결과 | 상태 |
|---|---|---|---|
| W1 | /home 진입 | ws-tabs 3개 표시 / "통합" 탭 = amber 그라디언트 + 펄스 / 다른 2개 흐림 | ✅ (코드상 예상) |
| W2 | /sales 진입 | "매출·영업 센터" 탭 = amber 펄스 활성 / 나머지 2개 흐림 | ✅ |
| W3 | 통합에서 매출 탭 클릭 | `<a href="{{ w.href }}">` → /sales 페이지 이동 + 헤더 amber 펄스가 매출로 이동 | ✅ |
| W4 | "/" 키 누름 | 도크 자동 열림(280ms 후) + 🔍 검색 모드 자동 활성 + 검색 input 포커스 | ✅ |
| W5 | 도크 안 💬 빅터 ↔ 🔍 검색 탭 클릭 | 패널 전환 정상 / 빅터(녹색 chat 버블) ↔ 검색(흰색 input) 시각 차이 명확 | ✅ |

> ⚠ **04 운영테스트팀 요청**: 실제 페르소나 5종(김정락 CEO / 안지연 영업 / 이현 매니저 / 정성진 구매팀장 / 김기선 라이프밸류) 로그인 후 W1~W5 회귀 검증 + 스크린샷 첨부 부탁드립니다.

---

## 5. Jinja 컴파일 검증

```
JINJA OK: 3 base templates compile
```

→ base.html / base_sales.html / base_logi.html 모두 문법 오류 없이 컴파일됩니다.

---

## 6. BAT 갱신 (01 책임 — 완료)

| 파일 | 갱신 라인 |
|---|---|
| `KNK_시작.bat` | line 3 (REM) + line 7 (title) + line 14 (echo) — 3곳 |
| `START.bat` | line 3 (REM) + line 7 (title) + line 13 (echo) — 3곳 |

**갱신 문자열**:
```
2026-04-28 G7헤더재설계v3(05디자인,대표직접승인)_가로탭3개+활성amber펄스+빅터도크모드분리(빅터↔검색)+brand-text숨김+로고축소(48-64px)+3base동시적용+grep15종PASS
```

---

## 7. 안전성 보강 (스펙 외 추가)

스펙 §6 §B의 옛 패턴 차단을 **HTML 제거 + CSS `display:none !important` 이중 방어**로 강화:

```css
.topbar .g-search,
.topbar .ws-switcher,
.topbar .ws-menu,
.topbar .ws-wrap,
.topbar .tb-home-link{ display: none !important; }
```

→ 다른 페이지에서 옛 클래스를 우연히 재참조해도 헤더에서는 안전하게 숨겨집니다.

---

## 8. 발견 이슈 / 질문

- **없음 (1차 시점)**.
- 다만 워크스페이스 아이콘은 `{{ w.icon }}`(Python 측 emoji/문자열)에 의존합니다. 시안 PREVIEW의 인라인 SVG 와 시각적 무게감이 다를 수 있습니다 → 04 검증 후 필요 시 inline SVG 화 보완안 요청 가능.
- 모바일(<1100px) 반응형은 CSS 미디어쿼리에 포함됨 (활성 탭만 이름 표시, 비활성은 아이콘만).

---

## 9. 회귀 점검 부탁드리는 것 (04 운영테스트팀)

1. CEO(김정락) 로그인 → /home, /sales, /logistics 3개 페이지 헤더 각각 amber 펄스 위치 정확한지
2. "/" 키 → 도크 + 검색 모드 자동 활성 + 입력창 포커스 동작
3. 빅터 도크 안 💬 ↔ 🔍 탭 전환 시 빅터 채팅과 검색 input 시각 차이가 명확한지
4. 영업/구매 직원도 ws-tabs 3개 모두 보이는지 (workspaces 권한)
5. 모바일 폭 1024px 이하에서 활성 탭만 이름 표시되는지

---

**발행**: 2026-04-28 · 01 메인 세션 (실무팀)
**상태**: 🟢 **1차+2차 통합 완료 · grep 15/15 PASS · BAT 갱신 완료 · Jinja 컴파일 OK**
**다음**: 04 운영테스트팀 회귀 검증 결과 대기 → 결함 발견 시 3차 핫패치 즉시 진행
