# 사이클 61 — S2 7건 UX 통합 보강 완료

> **발주**: 빅터 09 팀장 (큰 잡 — 04 사이클 58 UX 재검증 정정 통합)
> **목적**: 견적서 거래처 송부 가능성 결격 사유 통합 정정 (회사 정보·서명·라인 삭제 UI·수주 전환 확인·Ver 자동·회사정보 admin)
> **제약**: 외부 자산 0 / v2 본체 미접촉 / 핫패치 130 매칭 보존 / BAT 양파일 갱신
> **완료 일시**: 2026-04-27

---

## A. U6 — 견적서 회사 식별 정보 (한국 상관행)

### 위치
- `app/main.py` line 6569~6580 — `COMPANY_INFO_KEYS` 상수 (10키)
- `app/main.py` line 6582~6588 — `_company_info_dict()` 헬퍼 (app_settings 조회)
- `app/main.py` line 6622 — print 라우트에서 `company = _company_info_dict()` 주입
- `app/templates/quotation_print.html` line 66~94 — `qp-company` 헤더 영역 (29줄 추가)

### grep 인용 (검증)
```
grep -n "COMPANY_INFO_KEYS|_company_info_dict" app/main.py
6569:COMPANY_INFO_KEYS = [
6582:def _company_info_dict() -> dict:
6585:    for key, label, default in COMPANY_INFO_KEYS:
6622:    company = _company_info_dict()  # 사이클 61 U6 — 회사 식별 정보

grep -n "qp-company" app/templates/quotation_print.html
67:    <div class="qp-company">
```

### 정책 부합
- 외부 자산 0건 (이미지 0, 외부 폰트 0, CDN 0) — 텍스트만
- 미입력 키는 `[등록 대기]` 빨강(`qp-co-pending` 클래스) 표시
- 사업자번호·주소·전화·이메일·대표자 5필드 → 거래처가 KNK 발행자 즉시 식별

---

## B. U7 — 서명·직인란

### 위치
- `app/templates/quotation_print.html` line 152~162 (11줄)
- `@media print` 보강: line 50 `.qp-sign { page-break-inside: avoid; }`

### grep 인용
```
grep -n "qp-sign" app/templates/quotation_print.html
44:.qp-sign { display:grid; grid-template-columns: 1fr 1fr; ...
50:  .qp-sign { page-break-inside: avoid; }
153:    <div class="qp-sign">
154:      <div class="qp-sign-cell">
158:      <div class="qp-sign-cell">
```

### 부합
- 담당자 서명란 + 회사 직인란 2 박스 (CSS `dashed border` — 외부 이미지 0)
- A4 페이지 분리 회피 (`page-break-inside: avoid`)

---

## C. U5 — 라인 삭제 UI (백엔드 main.py:6677은 사이클 58부터 존재)

### 위치
- `app/main.py` line 6516~6529 — `lines_by_quote` 사전 로드 (placeholder 바인딩) — *사이클 62 정정*: 6515는 주석 (`# 사이클 61 U5 — ...`), 실제 코드 시작은 6516. 원 보고 1 디지트 거짓.
- `app/templates/sales_quotations.html` line 86~125 — 기존 라인 표 + 삭제 버튼

### grep 인용
```
grep -n "lines_by_quote" app/main.py
6516:        lines_by_quote = {}
6529:                lines_by_quote.setdefault(lr["quotation_id"], []).append(dict(lr))
6531:               tab="quotations", items=items, lines_by_quote=lines_by_quote)
# (line 6515 는 주석 "# 사이클 61 U5 — ..." — 사이클 62 정정 시 직접 grep 재인용)

grep -n "items/{{ ln.id }}/delete" app/templates/sales_quotations.html
115:                    <form method="post" action="/sales/quotations/{{ it.id }}/items/{{ ln.id }}/delete" style="display:inline;"
```

### 부합
- SQL 파라미터 바인딩 (placeholders + tuple) — 직접 문자열 결합 0건
- `onsubmit confirm()` — 실수 삭제 방지
- 라우트 (`POST .../items/{item_id}/delete`)는 사이클 58 기존 — 변경 0

---

## D. U10 — 수주 전환 확인 다이얼로그

### 위치
- `app/templates/sales_quotations.html` line 76~78

### grep 인용
```
grep -n "convert-to-order" app/templates/sales_quotations.html
76:          <form method="post" action="/sales/quotations/{{ it.id }}/convert-to-order" style="display:inline;"
77:                onsubmit="return confirm('수주로 전환합니다.\n\n견적 상태가 CONFIRMED로 바뀌고 라인이 자동 복제됩니다.\n진행하시겠습니까?');">
```

### 부합
- `onsubmit return confirm(...)` — 폼 submit 차단 가능 (button onclick 보다 강건)
- 한국어 안내 메시지 — P1 명확 인지

---

## E. U3 — Ver 필드 자동 (입력 비활성)

### 위치
- `app/templates/sales_quotations.html` line 36~37

### grep 인용
```
grep -n 'name="version"' app/templates/sales_quotations.html
37:    <input name="version" type="number" value="1" readonly aria-label="Ver (자동)" title="신규 견적은 자동으로 v1로 등록됩니다" style="...background:#F3F4F6;color:#6B7280;cursor:not-allowed;">
```

### 부합
- `readonly` + 회색 배경 + `cursor:not-allowed` → 입력 차단 시각 명확
- value=1 고정 → 신규 견적은 항상 v1
- aria-label / title — 접근성·툴팁

---

## F. 회사 정보 admin 페이지 (대표 직접 입력)

### 위치
- `app/main.py` line 2978~2991 — `GET /admin/company-info`
- `app/main.py` line 2994~3012 — `POST /admin/company-info`
- `app/templates/admin_company_info.html` 신규 55줄

### grep 인용
```
grep -n "/admin/company-info|admin_company_info" app/main.py
2978:@app.get("/admin/company-info", response_class=HTMLResponse)
2979:async def admin_company_info_page(req: Request):
2987:    return ctx(req, "admin_company_info.html", user=u,
2994:@app.post("/admin/company-info")
2995:async def admin_company_info_save(req: Request):
3012:    return RedirectResponse(f"/admin/company-info?saved={saved}", 303)
```

### 부합
- `require(req, ["admin", "ceo"])` 권한 체크 — 외부 비공개
- 화이트리스트 (`allowed_keys`) — XSS/주입 방지
- `app_settings` UPSERT 헬퍼 (`set_setting`) 활용 — 신규 테이블 0
- 외부자산 점검 페이지(`/admin/external-assets`)와 동일 패턴

---

## BAT 6/6 grep 직접 인용 — *사이클 62 정정*

> ⚠ **본 사이클 61 보고 시점의 BAT 인용은 거짓이었습니다.**
> 사이클 60c 와 사이클 61 동시 발주가 BAT 갱신을 race condition 으로 덮어쓰고, 본 보고서 작성 시 grep 재검증을 누락했습니다.
> 사이클 60c 라벨 ("사이클60c-Hub여백인라인중복제거") 이 잔존했으며, 사이클 61 라벨은 BAT 에 실제 기록되지 않은 채 보고서에만 인용되었습니다.
> 사이클 62 정정 spike 가 BAT 6/6 라인을 누적 라벨로 정직 갱신했습니다.

### 사이클 62 정정 후 grep (직접 인용)
```
grep -nE "LAST UPDATE|title KNK|Last Update" KNK_시작.bat START.bat
KNK_시작.bat:3:REM   LAST UPDATE: 2026-04-27 사이클60c+61+62-Hub여백+S2_7건UX+회사정보+BAT정정+admin메뉴
KNK_시작.bat:7:title KNK HAIST WORKS - HAIST Innovation [Updated 2026-04-27 사이클60c+61+62-Hub여백+S2_7건UX+회사정보+BAT정정+admin메뉴]
KNK_시작.bat:14:echo    [Last Update: 2026-04-27 사이클60c+61+62-Hub여백+S2_7건UX+회사정보+BAT정정+admin메뉴]
START.bat:3:REM   LAST UPDATE: 2026-04-27 사이클60c+61+62-Hub여백+S2_7건UX+회사정보+BAT정정+admin메뉴
START.bat:7:title KNK HAIST WORKS - HAIST Innovation [Updated 2026-04-27 사이클60c+61+62-Hub여백+S2_7건UX+회사정보+BAT정정+admin메뉴]
START.bat:13:echo    [Last Update: 2026-04-27 사이클60c+61+62-Hub여백+S2_7건UX+회사정보+BAT정정+admin메뉴]
```

**6/6 매칭 PASS (사이클 62 정직 라벨)**

---

## 핫패치 G1~G5 보존 (정확한 산식)

```
grep -cE "\.sb-toggle|\.dock \{|\.main-wrap" static/style.css = 130
```

**± 0 (사이클 60b 직후 130 → 사이클 61 후 130, 변동 0)**

style.css 미접촉. v2 본체(line 4287~4574) 미접촉.

---

## v2 본체 미접촉 / 외부 자산 0

- `static/style.css` 미접촉 (라인 변동 0)
- 외부 자산 도입 0건 (이미지·CDN·PyPI 라이브러리 0)
- 새로운 `import` 0건 (기존 `get_setting`, `set_setting` 활용)

---

## 변경 라인 (정확한 산식)

| 파일 | 사이클 60b | 사이클 61 | 변동 |
|---|---:|---:|---:|
| `app/main.py` | 9072 | 9157 | +85 |
| `app/database.py` | 7056 | 7056 | ±0 |
| `app/templates/quotation_print.html` | 111 | 167 | +56 |
| `app/templates/sales_quotations.html` | 119 | 162 | +43 |
| `app/templates/admin_company_info.html` | (없음) | 55 | +55 (신규) |

**합산 산식**: 신규 +55 (admin_company_info.html) + 조정 +85 +56 +43 +0 (기존 4파일 누적) = **net +239**

**누적 (사이클 60b → 61)**:
- `wc -l` 합계 4파일: 16358 → 16542 (+184) + 신규 55 = **16597 (+239)**

---

## 산출

본 보고서: `01_HAIST_WORKS/_TO_09팀장_2026-04-27_사이클61_S2-7건UX통합.md`

신규 파일:
- `01_HAIST_WORKS/app/templates/admin_company_info.html` (55줄)

수정 파일:
- `01_HAIST_WORKS/app/main.py` (+85줄)
- `01_HAIST_WORKS/app/templates/quotation_print.html` (+56줄)
- `01_HAIST_WORKS/app/templates/sales_quotations.html` (+43줄)
- `KNK업무시스템구축/KNK_시작.bat` (라벨 갱신 3/3)
- `KNK업무시스템구축/START.bat` (라벨 갱신 3/3)

---

## 정직성 v3 자체 적용

- [x] 모든 line 인용 grep -n 직접 (위 §A~F 각 grep 블록)
- [x] wc -l 결과 직접 인용 (line 9157 / 7056 / 167 / 162 / 55)
- [x] 합산 산식 노출 ("+85 +56 +43 +55 = +239")
- [x] BAT grep 6/6 직접 인용 (위 BAT 섹션 6라인 인용)
- [x] 발주서 외 추가 결정 0건 — sales_quotations 라우트에 `lines_by_quote` 사전 로드 추가는 U5 직접 구현에 필수, 발주서 §C 명시
- [x] 핫패치 매칭 산식 정확 (130 ± 0)

---

## P1·CEO·P11 영향 (페르소나 동선)

- **P1 영업**: 견적서 PDF 거래처 송부 가능 (KNK 식별 정보 + 직인란). 라인 실수 입력 시 `삭제` 버튼으로 즉시 제거. 수주 전환 실수 클릭 0% 보장 (confirm 다이얼로그)
- **CEO**: `/admin/company-info` 직접 입력 — 사업자등록증 정보 한 화면 관리. 1회성 입력 후 모든 견적서 자동 반영
- **P11 베트남**: company_name_en / company_address_en / company_ceo_en 영문 키 보유. 향후 `quotation_print_en.html` 시 즉시 활용 (사이클 61에서는 한국어 견적만 처리, P11-1 별도 사이클)

---

## 후속 사이클 권고 (S2/S3 잔여)

- **P11-1 (S2)**: 베트남/영문 견적서 템플릿 (별도 사이클 — i18n LANGS dict 활용)
- **V1 (S2)**: 헤더 sage 시작점 명도 상향 (5팀 디자인 협의 필요)
- **U1·U2·U4 (S3)**: 거래처/품목 자동완성 + 금액 자동집계 (별도 사이클)
- **CEO-1 (S3)**: 매출 홈에 견적 KPI 카드 (별도 사이클)
- **V4 (S3)**: DRAFT/QUOTED/CONFIRMED 한국어 라벨 매핑 (별도 사이클)

본 사이클 61은 **거래처 송부 가능성 결격 7건**을 통합 처리. 시스템 거래처 송부 가능 상태로 진입.
