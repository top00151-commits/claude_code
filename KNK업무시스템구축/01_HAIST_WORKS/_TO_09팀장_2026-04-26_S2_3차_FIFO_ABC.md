# [01 -> 09팀장] Top3 S2 3차 — FIFO 강화 + ABC + 회전율 회신

- 발신: 01 메인(실무팀) 빅터
- 수신: 09 프로젝트 팀장
- 일자: 2026-04-26
- 사이클: 09 발주 → **01 작업 (현재 회신)** → 04 회귀 가능
- 트랙: Top3 S2 3차 (1·2차 통과 후 본 잡) · v2 본체 미접촉

---

## 1. 변경 라인 5분리 (정직성 v2)

| 영역 | 파일 | 위치 | 변경 |
|---|---|---|---|
| 헬퍼 | `app/database.py` | 4267~4376 (109줄) | `fifo_layers` / `abc_classification` / `stock_turnover` 3건 신규 |
| 라우트 | `app/main.py` | 4513 import +3 / 5867~5905 라우트 +3 (39줄) | `/stock/balances/fifo/{part_id}` · `/stock/abc` · `/stock/turnover` |
| 템플릿 | `app/templates/` | 신규 3건 (74+79+77=230줄) | `stock_fifo.html` · `stock_abc.html` · `stock_turnover.html` |
| BAT | `KNK_시작.bat` line 3·7·14 | 3행 갱신 | `LAST UPDATE: 2026-04-26 S2-3차-FIFO-ABC` |
| BAT | `START.bat` line 3·7·13 | 3행 갱신 | 동일 (영문 라벨) |

---

## 2. 헬퍼 +3 (database.py 4267~4376)

- **`fifo_layers(part_id)`** → dict: `part_fifo_layers` 위에 집계 래퍼. `layers_count` · `total_qty` · `total_value` · `avg_unit_price` (가중평균). 평균원가 미저장 — 잔량×입고단가 실시간 계산 (Fishbowl 반면교사).
- **`abc_classification(days=90)`** → list[dict]: `kind=OUT` 매출액 누적비중 — A 0~80% / B 80~95% / C 95~100% (파레토). `share_pct` · `cum_share_pct` · `abc_class` 부여.
- **`stock_turnover(days=90)`** → list[dict]: `out_qty / avg_stock` 회전율. 평균재고 = `(on_hand+(on_hand+out_qty))/2` 근사. 밴드 FAST(>=2) / NORMAL(0.5~2) / SLOW(<0.5).

---

## 3. 라우트 +3 (main.py 5867~5905)

| 메소드 | 경로 | 가드 | 템플릿 |
|---|---|---|---|
| GET | `/stock/balances/fifo/{part_id}` | `_s2_guard` | `stock_fifo.html` |
| GET | `/stock/abc?days=90&top=50` | `_s2_guard` | `stock_abc.html` |
| GET | `/stock/turnover?days=90` | `_s2_guard` | `stock_turnover.html` |

3건 모두 비권한 시 `RedirectResponse('/home', 303)`. POST 부재 (조회 전용 · DB 무변경).

---

## 4. 신규 템플릿 +3

- `stock_fifo.html` (74줄): 4 KPI 카드(레이어 수·잔량·금액·가중평균) + 입고 순 테이블(PO·공급사·LOT·유효기한·잔량·단가·금액). Empty State.
- `stock_abc.html` (79줄): A/B/C 카운트 KPI 3칸 + 등급 배지(Green/Blue/Red) 테이블. `--st-done`/`--st-progress`/`--st-delay` 토큰.
- `stock_turnover.html` (77줄): FAST/NORMAL/SLOW 카운트 KPI 3칸 + 밴드 배지 테이블 (회전율 DESC).
- 전 템플릿 `{% extends "base.html" %}` · 외부 자산 0건.

---

## 5. 누적 카운트 (3 카테고리)

| 카테고리 | 헬퍼 | 라우트 | 템플릿 |
|---|---|---|---|
| 본 사이클 (S2 3차) | +3 | +3 | +3 |
| S2 트랙 1+2+3차 누적 | +3 (1·2차 무신규) | +8 (2차 +5 / 3차 +3) | +7 (2차 +4 / 3차 +3) |
| 세션 누적 (2026-04-21~26) | 코드 직접 수정 트랙 = S2 트랙 합산 | 동일 | 동일 |

---

## 6. 핫패치 G1~G5 grep (5/5 보존)

- `style.css:2253` — G1·G5 영향 없음 마커
- `style.css:3629` — G1 (body.dock-open .topbar padding-right 제거)
- `style.css:3633` — G2 (top_fixed 영역 .main-wrap 자식)
- `style.css:3650` — G3 (top: var(--topbar-h))
- `base.html:123` — G4 (sb-toggle 화살표 제거 · C안 v2)
- `style.css:4613/4731/4903/5130/5371/5548` — G5 boundary 마커 6회 (v2 본체 4287~4574 무수정)

→ **변경 0건 · 5/5 보존**.

---

## 7. 자기검증 (정직성 v2 7항)

- v2 본체(`style.css` 4287~4574) 미접촉 · 수정 파일 = `database.py`+`main.py`+신규 템플릿 3+BAT 2.
- 외부 자산 0건 · DB 무변경 (조회 전용) · 권한 `_s2_guard` (P4 자재팀 시드 정합).
- 라우트 등록 순서: `/stock/balances/fifo/{part_id}` 는 `/stock/balances` 직후 — 와일드카드 충돌 없음.
- BAT `LAST UPDATE` KR 1건·EN 1건 = 2건 갱신 (각 3행). 작업 시간 ~12분 (12~15분 내).

## 8. 04 회귀 의뢰

- `/stock/balances/fifo/{part_id}` · `/stock/abc?days=90` · `/stock/turnover?days=90` 200 응답 · 분류 합 정합.
- 권한 미보유 → `/home` 303.

— 끝.
