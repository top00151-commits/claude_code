# 빅터도크 우측 가림 핫패치2 — 적용 회신

**일시**: 2026-04-25 22:00경 (대표 직접 관찰 결함)
**작업자**: 01 메인 + 05 디자인팀 통합
**소요**: 약 6분

---

## 1. 진단 결과

### 도크 토글 클래스
- `base.html` line 825~826: `document.body.classList.toggle('dock-open', open)`
- 토글 대상: **`<body>`**

### 메인 컨테이너 셀렉터 구조 (base.html line 92~217)
```
<body class="dock-open">
  <div class="layout-wrap">          ← flex 컨테이너 (라인 92)
    <aside class="sidebar">...</aside>
    <div class="main-wrap">          ← flex child (라인 210)
      {% block top_fixed %}          ← .hm2-fixed 위치 (헤더 직속 자식 X)
      <main class="main has-sidebar"> ← G1 적용 대상 (라인 212)
        {% block body %}
      </main>
    </div>
  </div>
</body>
```

### 결함 재현
- `home.html` line 51~54 `+ 새 업무 등록` 버튼 → `.hm2-fixed > .hm2-head > .hm2-actions` (top_fixed 블록)
- `home.html` line 82 `등록` 버튼 → `.hm2-fixed > .hm2-quick` (top_fixed 블록)
- 어제 G1: `body.dock-open .main.has-sidebar { margin-right: 420px }` — **`.main`만 시프트**
- 그러나 `.hm2-fixed`는 `.main` 외부의 **`.main-wrap` 직속 자식** → 시프트 못 받음 → 도크에 가림

---

## 2. 수정 라인 (style.css)

추가 위치: line 3631~3641 (G1 직후)

```css
/* 핫패치 2026-04-25 22:00 — top_fixed 영역(`.hm2-fixed` 등 `.main-wrap` 직속 자식)
   은 `.main` 외부에 있어 G1 margin-right 적용을 안 받음 → 도크 열림 시 우측 끝
   `+ 새 업무 등록` / `등록` 버튼이 도크에 가려짐. `.main-wrap` 자체에도 동일 시프트
   적용 (대표 직접 관찰 22:00 → "빅터도크 열렸을 때 우측 버튼 가림" 시정 명령) */
.main-wrap { transition: margin-right 400ms cubic-bezier(0.33, 1, 0.68, 1); }
body.dock-open .main-wrap { margin-right: 420px; }
body.dock-open .main.has-sidebar { margin-right: 0; }  /* main-wrap에서 이미 시프트됨, 이중 적용 방지 */
```

모바일 분기 (line 3645): `body.dock-open .main-wrap { margin-right: 0; }` 추가.

**총 추가 라인**: 5줄 (주석 제외) / 11줄 (주석 포함)

---

## 3. BAT 갱신 결과

| 파일 | LAST UPDATE 라벨 |
|---|---|
| `KNK업무시스템구축/KNK_시작.bat` | `2026-04-25 빅터도크-우측가림-핫패치2 (.main-wrap margin-right 시프트)` |
| `KNK업무시스템구축/START.bat` | `2026-04-25 빅터도크-우측가림-핫패치2 (.main-wrap margin-right shift)` |

title 라인 + echo 라인 모두 동기화.

---

## 4. 어제 17:30 핫패치 G1~G4 보존 grep

```
3625: body.dock-open .main.has-sidebar { margin-right: 420px; }    ← G1 (margin-right 메인)
3627: /* 핫패치 2026-04-25 17:30 — body.dock-open .topbar { padding-right: 420px } 제거. ← G2 (topbar 시프트 제거 주석)
3648:    핫패치 2026-04-25 17:30 — top: 0 → top: var(--topbar-h)   ← G3 (도크 top 변경 주석)
3651:   position: fixed; top: var(--topbar-h, 84px); bottom: 0;    ← G4 (도크 top 실제 라인)
```
**4건 모두 보존 PASS.**

---

## 5. 새 회귀 grep

```bash
$ grep "body\.dock-open \.main-wrap" static/style.css
3636: body.dock-open .main-wrap { margin-right: 420px; }
3641:   body.dock-open .main-wrap { margin-right: 0; }
```
**2건 매칭 (데스크톱 + 모바일) PASS.**

---

## 6. 무접촉 보장
- v2 본체 토큰/팔레트/타이포 미접촉
- 외부 자산 0건
- BAT 갱신 누락 0건
