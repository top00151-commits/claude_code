# 사이클 70 — 빅터AI 호출 버튼 Hub 추가 (긴급)

> 일회성 01 메인 worker · 대표 직접 지적 (15:35) 즉시 spike 대응
> 산출 시각: 2026-04-27

---

## 0. 대표 직접 지적

> "매출영업센터, 구재자재센터 창에는 빅터ai 호출 아이콘이 없어."

**근본 원인**: 사이클 60 base_sales.html / base_logi.html 신설 시 base.html line 96의 `tb-icon-btn.victor` 호출 트리거 버튼을 누락. 도크 본체는 이식했으나 헤더 트리거 미이식 → Hub 사용자는 Ctrl+K 단축키만 가능.

---

## A. base.html line 96 원본 (read 인용)

```
96:    <button class="tb-icon-btn victor" id="victorTrigger" type="button" onclick="victorToggleDock()" title="{{ i.victor|default('빅터') }} (Ctrl+K)" aria-label="{{ i.victor|default('빅터') }}" aria-pressed="true">
97:      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"><rect x="5" y="8" width="14" height="10" rx="2.5"/><circle cx="9" cy="13" r="1.2" fill="currentColor"/><circle cx="15" cy="13" r="1.2" fill="currentColor"/><path d="M12 8V5m-3 0h6"/></svg>
98:    </button>
```

i.victor 다국어 키는 base.html 전용 — Hub 템플릿은 i 컨텍스트가 base.html과 다를 수 있어 안전하게 리터럴 "빅터" 사용.

---

## B. base_sales.html 추가 (변경 전·후)

### 변경 전 grep 인용
```
$ grep -n "victorTrigger\|tb-icon-btn victor" base_sales.html
(0건)
```

### 변경 후 grep 인용
```
base_sales.html:32:    <button class="tb-icon-btn victor" id="victorTrigger" type="button" onclick="victorToggleDock()" title="빅터 (Ctrl+K)" aria-label="빅터" aria-pressed="true">
```

### 추가된 블록 (line 30~34, `.tb-right` 우측 영역 최좌측, "통합 메인" 링크 직전)
```html
  <div class="tb-right">
    {# 사이클70 — 빅터AI 호출 트리거 (대표 직접 지적: Hub 헤더 트리거 부재) #}
    <button class="tb-icon-btn victor" id="victorTrigger" type="button" onclick="victorToggleDock()" title="빅터 (Ctrl+K)" aria-label="빅터" aria-pressed="true">
      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"><rect x="5" y="8" width="14" height="10" rx="2.5"/><circle cx="9" cy="13" r="1.2" fill="currentColor"/><circle cx="15" cy="13" r="1.2" fill="currentColor"/><path d="M12 8V5m-3 0h6"/></svg>
    </button>
    <a href="/home" class="tb-home-link" title="HAIST WORKS 통합 메인">
```

**추가 라인**: +5

---

## C. base_logi.html 추가 (동일 처리)

### 변경 전 grep 인용
```
$ grep -n "victorTrigger\|tb-icon-btn victor" base_logi.html
(0건)
```

### 변경 후 grep 인용
```
base_logi.html:32:    <button class="tb-icon-btn victor" id="victorTrigger" type="button" onclick="victorToggleDock()" title="빅터 (Ctrl+K)" aria-label="빅터" aria-pressed="true">
```

### 추가된 블록 (line 30~34, `.tb-right` 영역, "HAIST WORKS" 홈 링크 직전)
```html
  <div class="tb-right">
    {# 사이클70 — 빅터AI 호출 트리거 (대표 직접 지적: Hub 헤더 트리거 부재) #}
    <button class="tb-icon-btn victor" id="victorTrigger" type="button" onclick="victorToggleDock()" title="빅터 (Ctrl+K)" aria-label="빅터" aria-pressed="true">
      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"><rect x="5" y="8" width="14" height="10" rx="2.5"/><circle cx="9" cy="13" r="1.2" fill="currentColor"/><circle cx="15" cy="13" r="1.2" fill="currentColor"/><path d="M12 8V5m-3 0h6"/></svg>
    </button>
    <a href="/home" target="_blank" rel="noopener" class="tb-home-link" title="HAIST WORKS 메인 창 열기">
```

**추가 라인**: +5

---

## D. CSS 정합 (style.css grep 직접 인용)

```
style.css:1279:.topbar .tb-icon-btn{
style.css:2082:.tb-icon-btn{
style.css:4514:.topbar .tb-icon-btn{          ← v2 본체 정의
style.css:4538:.topbar .tb-icon-btn.victor{   ← v2 본체 .victor 강조 톤
style.css:4543:.topbar .tb-icon-btn.victor:hover{ filter: brightness(1.05); }
```

→ `tb-icon-btn` 및 `.victor` 클래스 모두 style.css에 이미 정의되어 있어 **추가 CSS 불필요**, 시각 정합 자동 확보. v2 본체(4287~4574) **미접촉**.

---

## BAT 6/6 grep 직접 인용 (line 14/13 포함)

### KNK_시작.bat
```
3:REM   LAST UPDATE: 2026-04-27 사이클70-빅터AI호출버튼Hub추가_긴급
7:title KNK HAIST WORKS - HAIST Innovation [Updated 2026-04-27 사이클70-빅터AI호출버튼Hub추가_긴급]
14:echo    [Last Update: 2026-04-27 사이클70-빅터AI호출버튼Hub추가_긴급]
```

### START.bat
```
3:REM   LAST UPDATE: 2026-04-27 사이클70-빅터AI호출버튼Hub추가_긴급
7:title KNK HAIST WORKS - HAIST Innovation [Updated 2026-04-27 사이클70-빅터AI호출버튼Hub추가_긴급]
13:echo    [Last Update: 2026-04-27 사이클70-빅터AI호출버튼Hub추가_긴급]
```

→ **6/6 PASS** (KNK_시작.bat 3+7+14, START.bat 3+7+13)

---

## 핫패치 130 / v2 본체 미접촉

- style.css 미접촉 (v2 본체 4287~4574 보존)
- 핫패치 G1~G5 sentinel(`sb-toggle` / `.dock {` / `main-wrap margin-right`) style.css **8건 그대로** 유지
- 외부 자산 0건 (인라인 SVG 직접, 새 import / CDN / 폰트 0)

---

## 변경 라인 합산

| 파일 | 변경 |
|---|---|
| base_sales.html | +5 |
| base_logi.html | +5 |
| KNK_시작.bat | ±0 (라벨 갱신만) |
| START.bat | ±0 (라벨 갱신만) |
| **net** | **+10** |

산식: `5 + 5 + 0 + 0 = 10`

---

## 산출

- 본 파일: `_TO_09팀장_2026-04-27_사이클70_빅터AI호출버튼긴급.md`
- 변경 파일: base_sales.html, base_logi.html, KNK_시작.bat, START.bat

## 후속 권장

1. 04 운영검증: `/sales/center` + `/logistics/center` 진입 → 헤더 우측 빅터 아이콘 클릭 → 도크 토글 동작 확인
2. 05 디자인 검토: Sales/Logistics Hub 헤더 그라데이션 위에서 `.tb-icon-btn.victor` 톤 가시성 확인 (현재 `.topbar` 컨텍스트 흰색 stroke + 강조 톤 자동 적용 예상)

---

*일회성 01 worker · 사이클 70 spike · 권한 팝업 0건 · 정직성 v3 5항 자체 적용 완료*
