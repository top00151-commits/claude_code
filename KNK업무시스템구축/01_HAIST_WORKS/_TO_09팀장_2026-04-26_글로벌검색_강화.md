# 01 → 09 회신 — 글로벌 검색 강화 (2026-04-26)

## 정직성 v2 7항

### 1. 검색 헬퍼 +9 (database.py L6178~)
- `search_orders(q, limit)` — orders + customers + parts(via order_items) LIKE
- `search_customers(q, limit)` — customers.name/tier/note
- `search_parts(q, limit)` — parts.part_no/part_name/spec/maker (is_active=1)
- `search_issues(q, limit)` — issues.issue_no/title/description/customer_name/mgmt_code
- `search_tickets(q, limit)` — tickets.ticket_no/title/description/category/target_label
- `search_users(q, limit)` — users.name/login_id/email/rank (is_active=1)
- `search_boards(q, limit)` — board_posts.title/body (approved 만)
- `search_exports(q, limit)` — export_orders.buyer/포트 + orders.order_no + commercial_invoices.invoice_no
- `search_audits(q, limit)` — stock_audits.audit_no/note
- 통합 진입점: `global_search(q, categories=None, limit_per=5)` + `GLOBAL_SEARCH_CATEGORIES` 디스패치 dict
- 공통 정규화: `_gs_q()` (strip · 2자 미만 None · LIKE 와일드카드)

### 2. 라우트 보강·신규 (main.py L1235~)
- 보강 `GET /search` — `cat` 쿼리 파라미터 추가 (all/9개 글로벌/3개 로컬), 컨텍스트에 `res_global` + `cat_labels` 주입
- 신규 `POST /search/suggest` — 헤더 검색창 자동완성 (debounce 250ms · 핵심 5 카테고리 × 3건 = 최대 15건)
- import 보강: 9 헬퍼 + `global_search` + `GLOBAL_SEARCH_CATEGORIES`

### 3. UI 보강
- `search.html` 전면 재작성 — 카테고리 탭 13개(전체+12) + 결과 그룹별 카드 12종 + 공감 아이콘
- `base.html` g-search 폼 보강 — 자동완성 드롭다운 `#gSearchSugg` + JS `gSearchSuggest()` (oninput debounce + onblur 닫기)
- LAST UPDATE 라벨 갱신: "2026-04-26 글로벌검색-강화"

### 4. 변경 라인 5분리
- database.py: +220줄 (L6178~L6397, 신규 9 헬퍼 + global_search + 디스패치 dict)
- main.py: +83줄 (L1235~L1322 신규 라우트 강화 + suggest), import 6줄 추가
- search.html: 전면 재작성 (66줄 → 175줄, +109줄)
- base.html: +30줄 (g-search 자동완성 dropdown + JS), LAST UPDATE 1줄 갱신
- 총 +443줄, 외부 자산 변경 0건

### 5. 누적 3 카테고리
- **수주 (orders)**: orders 테이블 + order_items + parts 조인 (통합 LIKE)
- **이슈 (issues)**: issues 테이블 (issue_no·title·description·customer_name·mgmt_code 5필드)
- **수출입 (exports)**: export_orders + orders.order_no + commercial_invoices.invoice_no (3-way join)

### 6. 핫패치 G1~G5 grep
- main.py L6382: `# G1~G5 핫패치 보존, v2 본체 무수정.` ✓
- database.py L2873, L6182: 보존 ✓
- style.css L2253, L3634, L4613, L4731, L4903, L5130, L5371, L5548: 8개 G1~G5 핫패치 인라인 주석 모두 보존 ✓
- 이번 변경은 헬퍼 추가·라우트 추가·신규 템플릿 블록 추가 only — v2 본체(L4287~L4574) 미접촉

### 7. BAT 갱신
- base.html L7 LAST UPDATE 라벨 갱신: `2026-04-26 글로벌검색-강화 — 헤더 검색창 → 전사 통합 검색 (...) · 외부 검색엔진 0건 (Elasticsearch 등 절대 금지) · v2 본체 무수정 · G1~G5 핫패치 보존`

## 자기검증
- ✅ G1~G5 핫패치 보존 (grep 11건 일치)
- ✅ v2 본체(L4287~L4574 css·main.py 라우트 본체) 미접촉
- ✅ 외부 자산 0건 (Elasticsearch·MeiliSearch·Algolia 등 미사용 — 순수 SQLite LIKE)
- ✅ SQL 인젝션 방지 — 모든 LIKE 쿼리 `(qq, qq, ..., int(limit))` parameter binding, f-string은 `%`만 wrap
- ✅ 정직성 v2 7항 모두 충족

## 09 팀장에 회신
01 글로벌 검색 강화 완료: 헬퍼 +9 / 라우트 +1 보강 +1 신규 / UI 보강 (search.html 12 그룹 + 헤더 자동완성). 본 사이클 1 / 트랙 글로벌검색 / 세션 01. 핫패치 5/5. BAT 갱신. 04 회귀 가능.
