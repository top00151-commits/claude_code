# WP-02 이관 리허설 키트 (재현용)

> ERP V1 전환 WP-02 · 게이트 지적 **F-01(재현 불가)** 대응
> ⛔ **운영 도구가 아닙니다.** 앱이 import 하지 않으며, 실행해도 운영 DB에는 접근하지 않습니다.
> ⛔ 원본은 항상 `mode=ro` 로만 엽니다. 쓰기는 전부 **시험용 PostgreSQL** 에서만 일어납니다.

## 무엇을 위한 것인가

"SQLite 업무 데이터를 PostgreSQL로 옮기면 어떻게 되는가"를 **누구든 같은 결과로 다시 확인**할 수 있게 만든 도구입니다. 문서의 숫자를 믿으라고 하지 않고, 직접 돌려서 확인할 수 있게 합니다.

## 준비

1. PostgreSQL (시험용 · 운영 NAS 아님)
2. `pip install psycopg2-binary`
3. 운영 DB **사본** — 만드는 법은 아래 §사본 만들기

## 실행

```bash
python deploy/wp02_migration/migrate.py <사본.db> --pg-port 55432
```

끝나면 `results/` 에 산출물이 쌓입니다.

| 파일 | 내용 |
|---|---|
| `summary.csv` | 표·행수·실패·시간 요약 |
| `ddl.sql` | 생성한 PostgreSQL DDL 전문 |
| `type_mapping.csv` | 컬럼별 타입 매핑과 **근거 규정** |
| `object_matrix.csv` | 표별 기본키·FK·인덱스 현황과 이전 여부 |
| `rowcount.csv` | 표별 이관 전후 행 수 |
| `sums.csv` | 금액·수량 합계 대조 (**Decimal 정확 비교**) |
| `cleaned.csv` | 정제한 값 목록(빈칸→NULL 등) |
| `run.log` | 실행 로그(시각·소요시간) |

> 산출물에는 **개인정보·거래 내용이 들어가지 않습니다** — 표·컬럼 이름과 집계 숫자만 기록합니다.

## 사본 만들기 (운영 서버에서 · 읽기 전용)

```bash
timeout 300 python3 -c "
import sqlite3,time,os
S='/opt/knk_haist/data/knk.db'; D='/tmp/wp02_rehearsal.db'
M=('mail_messages','mail_attachments','mail_aliases','mail_signatures','user_mail_creds','mail_large_files','mail_rules','mail_blocklist','mail_fetch_accounts','mail_fetch_seen')
t0=time.time(); src=sqlite3.connect('file:'+S+'?mode=ro',uri=True); dst=sqlite3.connect(D); src.backup(dst); src.close()
print('BACKUP_SEC',round(time.time()-t0,1),'FULL_BYTES',os.path.getsize(D))
n=0
for t in M:
    try: n+=dst.execute('DELETE FROM ['+t+']').rowcount
    except Exception: pass
dst.commit(); dst.execute('VACUUM'); dst.close()
print('MAIL_ROWS_REMOVED',n,'FINAL_BYTES',os.path.getsize(D)); print('SRC_BYTES_NOW',os.path.getsize(S))
"
```

- 원본은 `mode=ro` — **쓰기가 물리적으로 불가능**합니다(시도 시 `attempt to write a readonly database`).
- 삭제·압축은 **새로 만든 사본에서만** 일어납니다.
- 메일 10개 표를 비우는 이유: 메일이 용량의 99.4%이고 ERP 전환 범위 밖이기 때문입니다.

가져올 때는 `scp`·`sftp` 가 서버 로그인 메시지 때문에 실패하므로, 압축+텍스트 변환으로 받고 지문을 대조합니다:

```bash
ssh -p <SSH_PORT> <SSH_USER>@<OPERATIONS_HOST> "sha256sum /tmp/wp02_rehearsal.db; echo BEGIN64; gzip -c /tmp/wp02_rehearsal.db | base64 -w0; echo; echo END64" | Out-File -Encoding ascii "받을경로.b64"
```

> 접속 정보(`<SSH_USER>`·`<OPERATIONS_HOST>`·`<SSH_PORT>`)는 **저장소에 적지 않습니다.** 승인된 운영 비밀·환경 관리 문서에서만 확인하세요.

## 타입 매핑의 근거 — 기존 KNK 규정 그대로

새로 만든 규칙은 없습니다. 전부 **이미 확정된 규정**을 따릅니다.

| 대상 | PostgreSQL 타입 | 근거 |
|---|---|---|
| **수량** | `bigint` | **z1048**(2026-07-24 대표 지시) 수량=정수 · 수량 후보 **29개** 매핑 / 값이 있는 **10개**에서 소수 0건 확인(빈 19개는 Reference Scenario로 추가 확인) |
| **금액** | `numeric` (정밀도 제한 없음) | **z1026**(2026-07-21 대표 확정) 원화=정수·외화 2자리 / **z1024** 왕복 반올림 금지 |
| 환율·마진·중량 | `numeric` | z1048 예외(소수가 정당한 칸) |
| **날짜·시각** | `text` (글자 그대로) | **WP-02 대표 결정**(2026-07-24) 1차는 원형 보존 · **KNK 시간대 표준 v1** 은 애플리케이션에서 판정 |
| 그 외 정수 | `bigint` | — |
| 단일 정수 기본키 | `bigint GENERATED BY DEFAULT AS IDENTITY` | SQLite AUTOINCREMENT 대응 |
| 복합 기본키 | `PRIMARY KEY (a, b)` | 실측 6개 표 |

### 금액에 정밀도를 지정하지 않는 이유 (실측 근거)

처음에 `numeric(20,2)`로 시도했다가 **두 가지 실제 문제**를 만났습니다.

1. **소수부가 최대 16자리인 값이 실재**합니다(부동소수점 계산 찌꺼기). `numeric(20,2)`로 받으면 반올림되어 합계가 달라집니다 — `order_items.unit_price` 에서 **0.0145 차이** 실측.
2. **정수부 20자리 이상값**이 있어 `numeric(20,2)`는 `numeric field overflow` 로 **이관 자체가 실패**했습니다.

1차 이관의 최우선 원칙은 **원본 보존**이므로(날짜를 글자 그대로 두기로 한 대표 결정과 같은 철학), 값을 그대로 담고 **z1026 규정은 애플리케이션에서 강제**합니다 — 현행과 동일합니다. 자릿수 정리와 이상값 판정은 별도 정제 단계에서 근거를 남기고 수행합니다.

> 참고: `double precision`(부동소수점)으로 옮기면 큰 금액에서 **19,460원 오차**가 발생합니다(실측). 금액에는 절대 쓰지 않습니다.

## 이 키트로 확인된 것 (2026-07-24 · 3회 반복)

| 항목 | 결과 |
|---|---|
| 표 | **149 / 149** 성공 |
| 행 | **25,152행** · 실패 0 |
| 행 수 대조 | **불일치 0** |
| 금액·수량 합계 대조 | **36종 중 34종 완전 일치**(Decimal 기준 차이 0 · 수량 정규식 정정 후) |
| 나머지 2종 | `project_items.unit_price`·`amount` — **원본 SQLite 저장값 자체가 부정확**(유령행 1건이 `2^53` 초과) · 이관 결함 아님 |
| 정제한 값 | 36건(숫자 칸의 빈 문자열 → NULL) |
| 소요 | 2.71 ~ 2.86초 (DB생성 0.4 · 스키마 1.1 · 데이터 0.7 · 검증 0.2 · Rollback 0.3) |

## 스키마 객체 2차 이관 (`--with-constraints` · 게이트 P0-04)

데이터 적재 후 제약·인덱스·FK·뷰를 건다. 실측(2026-07-25):

| 객체 | 이관 | 비고 |
|---|---:|---|
| NOT NULL / DEFAULT | 263 / 468 | DEFAULT 의 `datetime('now')` 는 시간대 명시 변환 |
| UNIQUE / 일반 인덱스 | 5 / 243 | **원본 CREATE INDEX 문 재사용** → WHERE 부분 인덱스·표현식 보존(게이트 5.3) |
| **CHECK** | **21** | 상태값·진행률 제한 — CREATE TABLE 에서 추출(게이트 5.2) |
| **FK** | **221** | 7건 미이전(아래) |
| VIEW | 1 | 2 |
| TRIGGER | 0 | 2 **결정 대기** |

**미이전/대기 10건은 `object_failed.csv` 에 사유별로 남긴다**(누락을 숨기지 않음):

| 사유 | 건수 | 처리 |
|---|---:|---|
| **데이터 무결성** — 없는 부모를 참조(유령행) | 2 | §데이터 정제에서 유령행 42건 처리 후 FK 성립 |
| 참조 양쪽 타입 불일치 | 3 | 컬럼 타입 정렬 필요 |
| 참조 대상에 UNIQUE 없음(`node_code`) | 2 | 원본 스키마 설계 확인 |
| SQLite 전용 날짜 산술 뷰(`weekly_summary`) | 1 | PG 문법 재작성(사람 판단) |
| **트리거 2개**(`trg_oi_due_orig_*` 최초 납기일 보존) | 2 | **결정 대기** — PG 트리거 이관 vs 애플리케이션 변경이력(WP-08)으로 대체 |

**실증**(2026-07-25): CHECK → 잘못된 상태값 저장 차단 확인 · 조건부 인덱스 WHERE 보존 → 빈 `co_no` 2건 저장 확인(SQLite와 동일 동작).

> **의미**: "테이블 149/149 생성"은 껍데기일 뿐, FK를 실제로 걸면 **데이터·설계 문제가 드러난다.** 유령행 2건은 데이터 정제가 이관의 선행조건임을 실증한다.

## 데이터 변환 승인 (게이트 §3 · 행별)

- 승인 키 = **표 + PK값 + 컬럼 + 원본값** (같은 빈 문자열도 행마다 별도 승인)
- `--approved <csv>`(표·PK값·컬럼·원본값·**사유·승인자·승인일시** — 필수값 검증) 없으면 **DB 생성 전 중단**
- 실행 승인목록의 **SHA-256** 을 로그에 남김 · `cleaned.csv` 에 PK·변환값·승인자·승인일시 기록

## 불일치 2건 증명 (게이트 §6 · `prove_mismatch.py`)

`python prove_mismatch.py <사본> <이관DB>` — **실제 이관된 PG 표**를 id 기준으로 **707행 전부 행별 대조**(불일치 0행). 2^53 초과 1건은 원본 double 저장값이 이미 근사값임을 행별 제시(승인 예외).
