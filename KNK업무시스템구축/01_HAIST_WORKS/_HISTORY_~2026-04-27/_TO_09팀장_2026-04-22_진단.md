# 01 메인 1단계 진단 — 2026-04-22

> **작성 출처**: 01 세션 진단 대행 Explore 에이전트. 09 팀장 clerical 저장.
> **근거**: main.py 라우트 143개 전수 실측 + templates + database.py + 04 테스트 로그

## 1. 라우트 × 영역 태깅 (총 143개)

### 영역 0 공통 인프라 (9)
`/login` `/logout` `/` `/profile` `/profile/change-password` `/api/set-lang` `/api/translate` `/api/notifications` `/api/notification/{nid}/read`

### 영역 1 전사공통 (16)
`/home` `/summary` `/board/company` `/board/team` `/board/new` `/board/post/*` `/feed` `/search` `/notifications` `/api/activities` `/api/sidebar-tree` `/weekly` `/export/weekly` `/api/users/search`

### 영역 2 부서별 (19)
`/daily` `/team` `/dashboard` `/calendar` `/api/team-summary` `/now` `/history` `/api/task/*` `/api/carry-forward` `/api/task/{tid}/delegate` `/api/delegation/{did}/resolve`

### 영역 3 매출 (8)
`/project/{pid}` `/projects` `/projects/new` `/projects/{pid}/edit` `/projects/{pid}/delete` `/customer/{cid}` `/api/project/{pid}/retro` `/sales` (미구현 폴백)

### 영역 4 구매자재 (42) — **최대 규모**
`/parts*` (5) `/suppliers*` (4) `/po*` (6) `/rates*` (2) `/parts/{pid}/prices*` (2) `/stock/*` (3) `/logistics` + 관련 API

### 영역 5 근태 (0) — **전무**
### 영역 6 자금 (0) — 외부 (더존)
### 영역 7 메일 (0) — 외부 (가비아)
### 영역 8 메신저 (0) — 외부 (카톡), 하이웍스 토큰은 DB
### 영역 9 전자결재 (0) — 외부 (가비아/하이웍스)
### 영역 10 수출입 서류 (0) — **전무**

### 나머지 (38)
변경 관리(7) · 티켓(10) · 이슈·AS(8) · 진행률(6) · 관리자(7) · AI(/api/victor/ask 1)

## 2. 중복·고아 식별

### 중복 후보
| 쌍 | 차이 | 평가 |
|---|---|---|
| /home · /daily · /team · /dashboard | KPI / 개인상세 / 팀요약 / 팀종합 | 용도 분리 양호, leader /dashboard 폴백은 버그 |
| /search · /feed · /api/activities | 텍스트검색·시간순·API | 분리 양호 |
| /project/{pid} · /progress/{project_id} | 프로젝트정보 vs 공정진행 | 분리 양호 |
| /sales · /logistics · /home | 영업·구매·홈 | ⚠️ /sales·/logistics 폴백 문제 |

### 고아 후보
- `/cockpit` — 라우트 존재하나 네비 링크 유입 불명확
- `/bottlenecks` — 동일
- `/now` — 대시보드와 차별성 불명

## 3. 관리자 UI 커버리지

### 존재
- `admin.html` (3 탭): 대시보드 / 데이터 / 보안
- `admin_settings.html`: 하이웍스 URL·토큰·알림 스위치
- `/admin/health.html`: 건전성
- `/api/admin/*` 9 건: user / project / customer / import-mgmt / regenerate-passwords / **user-logistics**

### 팀장용 팀원 권한 설정 UI — **없음** ⚠️
- 현: `/admin` 탭에서 admin 만 체크박스 토글 (can_use_logistics 등)
- 팀장이 자기 팀원을 권한 위임할 UI 부재

## 4. 권한 위임 3계층 확장 규모 — **중**

### 현 스키마
- `users.role` 4단계 (member/leader/executive/ceo)
- `users.can_use_logistics` 0/1
- `task_delegations` (업무 위임 전용)

### 필요 추가 (구현 3~5일 추정)
- DB: `users.delegated_by` · `users.delegated_roles(JSON)` · `users.delegation_end_date`
- 테이블: `team_permissions(team_id, user_id, permission_key, delegated_by, created_at)`
- 라우트 5개: `/admin/team/{team_id}/permissions` · `/api/admin/team/{team_id}/permissions` · `/api/admin/delegation/{did}/revoke` · 만료 체크 배경 등
- 화면: 팀장용 팀원 권한 매트릭스

## 5. 10 영역별 3분류

| 영역 | 분류 | 근거 |
|---|---|---|
| 0 공통 | **유지** | 인프라 완성 |
| 1 전사공통 | **유지** | 기본 완비, 주간보고 export 고도화 여지 |
| 2 부서별 | **조정** | leader /dashboard 회귀 · /sales·/logistics 폴백 · 팀장 권한 위임 UI 신규 |
| 3 매출 | **유지** | 프로젝트·고객사·회고 기본 구조 |
| 4 구매자재 | **유지** (+ 시드 해결) | 42 라우트 충분. 권한 시드 정책 OPS-011 해결 필요 |
| 5 근태 | **신규** | 전무 |
| 6 자금 | **신규(최소)** | 엑셀 등록 수준만 |
| 7 메일 | **신규(링크만)** | 외부 |
| 8 메신저 | **조정** | 카카오워크 잔재 정리 + 하이웍스 푸시 |
| 9 전자결재 | **조정(링크만)** | 외부 유지 확정. 상태 조회 UI 후순위 |
| 10 수출입 | **신규** | 별도 모듈 |

## 6. 못 하는 것 / 안 해도 되는 것

1. **근태는 1단계 후속** — 더존/하이웍스 HR 토큰 획득 필요. 당장 전면 구축 X.
2. **자금 모듈 X — 엑셀 등록으로만** — 더존 유지 방침 준수.
3. **자체 전자결재 X** — 하이웍스 유지 확정. "상태 조회 UI" 만 Phase 2.
4. **수출입은 설계부터** — 영업·물류 협력 필요. 지금은 게시판 대체 불가.

## 7. 우선순위 제안 (01 관점)

| 순 | 작업 | 규모 | 효과 |
|----|------|------|-----|
| 1 | leader /dashboard 권한 수정 (PS-02/07) | 소 | 팀장 대시 접근 |
| 2 | 팀장 팀원 권한 위임 UI 신규 | 중 | 3계층 실효화 |
| 3 | /progress 12 공정 라벨 데이터 | 소 | 진행률 시각화 |
| 4 | /sales·/logistics 폴백 원인 수정 | 소 | 영업·구매 접근 |
| 5 | 근태 시스템 기본 설계 | 대 | 1~2 개월 |

## 8. 페르소나 근거 (테스트 로그 기반 8 PS)
PS-01 구매팀 매니저 / PS-02 관리팀장 / PS-03 영업 이사 / PS-04 전장설계팀장 / PS-05 제조2팀장 / PS-06 가공팀장 / PS-07 품질팀장 / PS-08 SW팀장
— 각자의 라우트·고통 기록 (04 테스트 로그 근거)

## 9. 요약
- 영역 1~4 기본 완성 / 영역 5·10 신규 / 영역 6~9 외부 유지
- 빅뱅 회피 경로: 핫픽스 3건 → 팀장 권한 UI → 근태는 후순위
