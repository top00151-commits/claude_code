# [TO 09 팀장] Top3 S3 3차 — 권한 그룹 관리 + 매트릭스 + 그룹 위임 완료

- 발신: 01 메인 빅터 / 수신: 09 팀장 / 일자: 2026-04-26
- 발주: S3 3차 — 그룹 관리 + 매트릭스 + 그룹별 위임 (정직성 v2 7항)
- 시안: `05_HAIST_WORKS_디자인팀/_TO_01_정식시안_Top3_S3_v1.md` §5 그룹 상속 ③
- 트랙: 1차(골격 6+6+4) + 2차(UI본문+RBAC+grant/revoke+audit+Cascade) 위에 그룹/매트릭스 이식.

---

## 1. 신규 라우트 7건 (main.py 5210~5499)

### 그룹 관리 라우트 +5
| 메서드 | 경로 | 핸들러 | 비고 |
|---|---|---|---|
| GET | `/admin/permissions/groups` | `admin_permissions_groups_list` | 목록 + perm_count + user_count |
| POST | `/admin/permissions/groups` | `admin_permissions_groups_create` | name UNIQUE 가드 → INSERT |
| GET | `/admin/permissions/groups/{id}` | `admin_permissions_groups_detail` | 상세(권한 + 멤버 + 추가가능 카탈로그) |
| POST | `/admin/permissions/groups/{id}/permissions` | `admin_permissions_groups_perms` | DELETE-then-INSERT 트랜잭션 재기록 |
| POST | `/admin/permissions/groups/{id}/users` | `admin_permissions_groups_users` | DELETE-then-INSERT 트랜잭션 재기록 |

### 매트릭스 라우트 +1
| 메서드 | 경로 | 핸들러 | 비고 |
|---|---|---|---|
| GET | `/admin/permissions/matrix?dept=&q=` | `admin_permissions_matrix` | 사용자×권한 CSS grid · 부서/검색 필터 · D/G/T 3색 · JS 0건 |

### 그룹 위임 라우트 +1
| 메서드 | 경로 | 핸들러 | 비고 |
|---|---|---|---|
| POST | `/admin/permissions/grant-group` | `admin_permissions_grant_group` | 그룹 멤버 일괄 토큰 + audit (단일 트랜잭션) |

---

## 2. 신규 템플릿 +2

| 템플릿 | 라인수 | 핵심 |
|---|---|---|
| `admin_permissions_groups.html` | 105 | 좌 그룹목록+신규폼 / 우 상세(권한 체크박스+멤버 체크박스+그룹위임 폼) |
| `admin_permissions_matrix.html` | 70 | 부서/q 필터 · CSS grid 매트릭스 · D=#15803D / G=#1D4ED8 / T=#FCD34D 3색 · 범례 |

`admin_permissions.html` 상단 탭 +2링크 (그룹 관리 / 매트릭스).

---

## 3. 변경 라인 5분리

| 파일 | 라인 | 종류 |
|---|---|---|
| `app/main.py` | 5210~5499 (+283) | 신규 7라우트 (S3 트랙) |
| `app/templates/admin_permissions_groups.html` | 1~105 | 신규 파일 |
| `app/templates/admin_permissions_matrix.html` | 1~70 | 신규 파일 |
| `app/templates/admin_permissions.html` | 18~22 | 탭 +2링크 |
| `app/templates/base.html` | 7 | LAST UPDATE 갱신 |
| `KNK_시작.bat` / `START.bat` | REM/title/echo 각 3 | LAST UPDATE 갱신 |

---

## 4. 누적 (3 카테고리)

- **본 사이클 (S3 3차)**: 라우트 +7 / 템플릿 +2 / 본체수정 0 (탭 링크 +2 / LAST UPDATE 1줄만)
- **S3 트랙 (1+2+3차)**: 테이블 6 + 라우트 6+0+7 = 13 / 템플릿 4+0+2 = 6 / RBAC 컬럼 분리 + Cascade + audit immutable + 그룹 상속 + 매트릭스
- **세션 누적**: S1+S2+S3 트랙 + JS placeholder + i18n 셀렉터 + 매출 KPI + 환율 + 재고실사 + QMS Pareto/CAPA + S3 3차

---

## 5. 자체검증 (grep PASS)

- 핫패치 G1~G5 grep `핫패치 2026-04-25 17:30` → **3/3 라인 보존** (style.css:3629, style.css:3650, base.html:95)
- v2 본체 미접촉 (main.py 5210~5499 신규 묶음만, 타 본문 변경 0)
- 외부 자산 도입 **0건** (브랜드명·라이선스·차트·JS 라이브러리 0)
- 매트릭스 = CSS grid + Jinja 정적 렌더 (JS 0건 확인)
- 트랜잭션 무결성 — `db_session()` 단일 with 블록 내 INSERT/DELETE/UPDATE + audit (그룹 위임 시 멤버별 토큰+audit 동시 발행)
- BAT 갱신 — `KNK_시작.bat` / `START.bat` 양쪽 동시, REM/title/echo 3라인 각각
- 5 체크리스트 (외부 상표권/라이선스 정책) 통과

---

## 6. 04 회귀 가능

S3 3차 산출 = 그룹 관리 5라우트 + 매트릭스 1라우트 + 그룹위임 1라우트 + 신규 템플릿 2 + 본체 무수정. 04 페르소나 시뮬레이션(P1=CEO 그룹 생성·매핑·일괄위임, P3=팀원 매트릭스 차단, CEO=매트릭스 부서별 조회) 회귀 진행 가능.
