# TO 09팀장 — v2 이식 2차-3단계 (모달·드롭다운·토스트·알림) 적용 회신

- 발신: 01 메인(실무팀) 빅터
- 일자: 2026-04-26
- 사이클: 09발주 → **01작업(완료)** → 04회귀 → 00감사 → 99
- 잡: v2 정식 이식 2차-3단계 — 모달/드롭다운/토스트/알림 영역

## 1. 작업 요약
- 적용 위치: `static/style.css` L4894~L5118 신규 블록 (§1~§9)
- v2 본체(4287~4574) **자체 수정 0줄** (절대 준수 · L4287/L4574 경계 verify OK)
- HTML 변경 0줄 (CSS 셀렉터 일괄 정합 — 사이클 20·21 패턴 유지)
- 외부 자산 0건

## 2. 시안 클래스 매핑 (시안 12B 인용)
| v2 시안 톤 | 적용 셀렉터 | 토큰 |
|---|---|---|
| 모달 22px 라운드 + sage 백드롭 | `.main .modal-card`, `.main-wrap .modal-card`, `.hm-modal`, `.card-modal-card` | `--text-lg`(헤더) |
| 모달 백드롭 sage tone | `.modal-bg / .hm-modal-bg / .card-modal-bg` | blur(2px) |
| 모달 헤더 800 굵게 | `.modal-title / .hm-modal-top h3 / .cm-modal-head` | `--text-lg` |
| 모달 본문 16px 권장 | `.hm-modal-body / .cm-modal-body` | `--text-md` |
| 모달 닫기 버튼 sage | `.hm-modal-x / .btn-close-modal` | `--text-sm` |
| 토스트 라운드 14px | `.flash / .flash-ok / .flash-err` | `--text-sm` |
| 헤더 알림 벨 배지 | `.hm2-bell-badge` | `--text-xs` |
| 사이드바 알림 카운트 | `.sb-badge` | `--text-xs` |
| 알림 리스트 sage | `.notif-list / .notif-item / .unread` | `--text-sm` |
| 피드 알림 밴드 + ac-badge | `.alert-band / .ac-badge` | `--text-sm/xs` |
| 드롭다운 (셀렉터) | 2차-2단계 §2 `.field select` 흡수 (재흡수 0줄) | `--text-md` |

> 드롭다운: 헤더 ws-switcher/lang-sel은 v2 본체(L4399~L4458, L4546~L4557) 자체 적용 완료 → 본 단계 스킵. 페이지 셀렉터(`.field select`, `.filter-bar select`)는 2차-2단계 §2·§3 에서 이미 정합 — 본 단계 추가 0줄.

## 3. 적용 페이지 카버리지 (CSS 일괄)
- 모달
  - `daily.html` — 업무카드 추가 모달 (`.modal / .modal-card / .modal-title / .modal-btns`)
  - `home.html` — 일일 카드 상세 모달 (`.hm-modal / .hm-modal-bg / .hm-modal-top / .hm-modal-body / .hm-modal-x`)
  - `base.html` 공통 cardModal — 부적합 처리(S2) / Cascade 미리보기(S3) / 견적·수주(S1) / CI·PL·BL·관세(P11) / 게시판 댓글 (`.card-modal-card / .cm-modal-head / .cm-modal-body / .btn-close-modal`)
- 토스트: `profile.html` 외 `.flash / .flash-ok / .flash-err` (저장 성공·실패, 권한 위임 안내, 매출 자동 채움)
- 알림 배지: `home.html` 헤더 벨, `base.html` 사이드바 메뉴 카운트 (`.hm2-bell-badge / .sb-badge`)
- 알림 리스트: `notifications.html` (`.notif-list / .notif-item / .unread`)
- 피드 알림: `feed.html / team.html` (`.alert-band / .ac-badge`)

총 영향 페이지 약 12+개.

## 4. 토큰 사용 결과
- 모달 헤더: `--text-lg` (clamp 17~19px) 굵게 800
- 모달 본문: `--text-md` (clamp 15~17px) — 한국어 본문 16px 권장
- 모달 닫기/버튼: `--text-sm` (clamp 13~15px)
- 토스트(.flash): `--text-sm`
- 알림 배지(hm2-bell-badge / sb-badge / ac-badge): `--text-xs` (clamp 11~13px)
- 알림 리스트 본문(.notif-item): `--text-sm`

## 5. CSS 변경 라인 수
- **이번 신규: +226 줄** (L4894~L5118, §1 토큰 ~ §9 모바일)
- **이번 조정: ±0 줄** (기존 행 비변경 · 신규 셀렉터만 추가)
- **이번 net: +226 줄**
- **누적(2차 합계)**: +118(2차-1단계 카드) + 168(2차-2단계 폼테이블버튼) + 226(2차-3단계) = **+512 줄**

## 6. 핫패치 G1~G5 보존 grep 결과
| ID | 검증 grep | 결과 | 상태 |
|---|---|---|---|
| G1 | `sb-toggle` (사이드바 토글 정의) | **5건** (L644-645/L857/L1369-1370) | OK |
| G2 | `.dock` baseline top:0 | L2584 (이후 G4 override) | OK |
| G3 | `.topbar` 활성 | L143 / L4303-4313 (v2 본체) | OK |
| G4 | `topbar-h` 토큰 사용 | **9건** (L3653 dock top, L4295 root) | OK |
| G5 | `main-wrap` margin-right | **59건** (L3637-3639 핵심) | OK |

v2 본체(L4287~L4574) diff: **0줄** (L4287 `}` + comment, L4574 `color: var(--sage-700);` verified).

## 7. BAT 갱신
`app/templates/base.html` L7:
```
LAST UPDATE: 2026-04-26 v2이식-2차-3단계-모달드롭토스트 — style.css L4894+ 신규 §1~§9
(.modal/.modal-card/.hm-modal/.card-modal-card/.cm-modal-head/.btn-close-modal/.flash/
.hm2-bell-badge/.sb-badge/.notif-list/.notif-item/.alert-band/.ac-badge
시안12B 정합 · 22px 라운드 · sage 백드롭 · --text-lg/md/sm/xs 토큰)
— G1~G5 보존, v2본체(4287~4574) 무수정, 페이지 HTML 0줄 변경
```
캐시 버스터: `?v=20260426v2c2-2` → `?v=20260426v2c2-3`

## 8. 14px 광범위 영향군
사이클 20·21 결정 유지 — **본 단계에서도 14px raw 직타 미수정** (배지·토스트는 `--text-xs/sm` 토큰으로 자연 흡수).

## 9. 다음 단계 예고
**2차-4단계 = 그래프·차트·KPI 시각요소** (Chart.js 캔버스 톤·축 색·게이지 시안 12B 정합 · 작업 시간 12~15분 추정).

---
**04 회귀 가능.**
