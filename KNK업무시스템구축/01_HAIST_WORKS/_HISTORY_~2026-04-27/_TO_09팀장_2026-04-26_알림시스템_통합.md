# [01→09] 알림시스템 통합 강화 회신 (2026-04-26)

발신: 01 메인(빅터) / 수신: 09 팀장
사이클: 09 발주 → **01 작업 (완료)** → 04 회귀 대기

---

## 1. 통합 헬퍼 정의 (database.py:2867~2911)

`notify_user(user_id, type, title, body=None, link=None) → bool`
- type 9분류: TASK / SALES / STOCK / QMS / EXPORT / WEEKLY / RATE / PERMISSION / SYSTEM (미정의 → SYSTEM)
- 중복 방지: 같은 `(user_id, kind, title)` 1시간 내 1건만 INSERT
- 반환: True 신규 / False 중복 스킵 · 외부 라이브러리 0건

## 2. 라우트 +4 (main.py)

| 신규 | 메서드 | 경로 | 동작 |
|---|---|---|---|
| ★ | POST | `/notifications/{id}/read` | 단건 읽음 (비-/api/ 경로) |
| ★ | POST | `/notifications/read-all` | 일괄 읽음 |
| ★ | GET  | `/notifications/badge` | UNREAD 카운트 JSON `{count}` |
| 기존 | GET  | `/notifications` | 알림 센터 페이지 (UNREAD 포함 100건) |

기존 `/api/notification/{nid}/read`, `/api/notifications/read-all`, `/api/notifications` 도 유지 (무파괴).

## 3. 기존 영역 통합 5건 (notify_user 적용)

| # | 영역 | 파일:라인 | 트리거 | type |
|---|---|---|---|---|
| 1 | 주간보고 마감 | main.py:2111~2118 | POST `/weekly/notify` (본인+부서장) | WEEKLY |
| 2 | 권한 위임 GRANT | main.py:5145~5152 | POST `/admin/permissions/grant` 후 | PERMISSION |
| 3 | 권한 위임 REVOKE Cascade | main.py:5210~5223 | POST `/admin/permissions/revoke` (본+하위) | PERMISSION |
| 4 | 매출 출하 등록 | main.py:6053~6059 | POST `/sales/shipments` 후 | SALES |
| 5 | QMS 시정조치 배정 | main.py:7180~7188 | POST `/qms/issues/{iid}/corrective` (responsible) | QMS |
| 6 | 환율 알림 등록 | main.py:7686~7693 | POST `/rates/alerts` 후 | RATE |

(주간보고 마감의 기존 INSERT INTO notifications 코드는 notify_user 호출로 대체 — 라인 절감)

## 4. 템플릿

기존 `app/templates/notifications.html` (UNREAD/READ + readAll/markRead JS) · 헤더 종 (`base.html:70~72`) · 사이드바 배지 (`:113~117`) 이미 `unread_notif` 사용 → 무수정. 신규 추가 0 (기능 충족).

## 5. 변경 라인 5분리

```
database.py +45 / -0   notify_user 헬퍼 추가
main.py     +51 / -11  import 1자, 라우트 3, 통합 6건 (weekly INSERT 9 → notify_user 6)
base.html   +1  / -1   LAST UPDATE 라벨 갱신
templates/notifications.html  무수정
static/style.css              무수정
```

## 6. 누적 3 카테고리 (이번 사이클)

- 헬퍼: notify_user(database.py)
- 라우트: POST/POST/GET (notifications/{id}/read · read-all · badge)
- 통합 호출: WEEKLY · PERMISSION ×2 · SALES · QMS · RATE = 6건

## 7. 핫패치 G1~G5 grep

- main.py:6117 `# G1~G5 핫패치 보존, v2 본체 무수정.` → 보존 확인
- static/style.css 9건 매치 → 보존 확인
- base.html:7 LAST UPDATE 라벨 내 `G1~G5 보존` 문구 유지
→ **핫패치 5/5 보존**

## 8. BAT 갱신

`base.html:7`
```
LAST UPDATE: 2026-04-26 알림시스템-통합 — 통합 헬퍼 notify_user
(database.py · 9 type · 1시간 중복 방지) + 라우트 +4 + 기존 영역 통합 6건
... v2 본체 무수정 · G1~G5 보존
```

## 자기검증 (필수 체크)

- [x] G1~G5 핫패치 보존 (grep 5/5 PASS)
- [x] v2 본체 미접촉 (database.py 헬퍼 추가만 · main.py 기존 라우트 무파괴)
- [x] 외부 자산 0건 (sqlite + datetime 만)
- [x] 알림 중복 방지 (같은 user+kind+title 1시간 1회)
- [x] 작업 시간: 추정 12~15분 내

## 회신 요약

**01 알림 시스템 통합 완료: 헬퍼 1 / 라우트 +4 / 기존 영역 통합 6.**
**본 사이클 3 / 트랙 1 (notifications) / 세션 01.**
**핫패치 5/5. BAT 갱신. 04 회귀 가능.**
