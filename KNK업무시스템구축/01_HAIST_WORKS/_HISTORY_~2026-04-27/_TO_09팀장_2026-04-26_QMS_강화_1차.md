# 01 → 09 회신 — QMS 강화 1차 (DB + 라우트 + UI 골격)

**작성**: 빅터 (01 메인) · **일시**: 2026-04-26 · **사이클**: 09 발주(갭서베이 Top10 #6) → 01 작업 → 04 회귀 대기

---

## 1. DB 스키마 신규/보강 (idempotent)

`app/database.py` 번다운 인덱스(L1015~1016) 직후 +47줄. `issues` 테이블 컬럼 변경 0건 (ALTER ADD COLUMN 5건은 init_db 마이그레이션 블록).

| 분류 | 항목 |
|---|---|
| 신규 테이블 3 | `qms_audit_log` (immutable: UPDATE/DELETE 안 함) / `corrective_actions` / `preventive_actions` |
| ALTER ADD 5 | `issues.sla_hours INTEGER DEFAULT 24` / `issues.detected_at TEXT` / `issues.responded_at TEXT` / `issues.recurrence_id TEXT` / `issues.sla_breached INTEGER DEFAULT 0` |
| ALTER ADD 1 | `users.can_use_quality INTEGER DEFAULT 0` (품질팀 자동 부여 + admin/ceo/executive 자동) |
| 신규 인덱스 | `idx_qmsa_issue` / `idx_qmsa_action` / `idx_ca_issue` / `idx_ca_status` / `idx_pa_ca` / `idx_iss_recurrence` (6건) |

가드: `PRAGMA table_info(issues)` 검사 후 ALTER · `CREATE TABLE/INDEX IF NOT EXISTS` · 모든 try/except.

## 2. 라우트 신규 5개 + 권한 가드 단일 정의

`app/main.py` 말미(L5921 직후) +204줄. `_qms_guard(req)` — admin/ceo/executive OR `can_use_quality=1` OR 팀명 LIKE '%품질%'. `_qms_sla_status(issue)` 헬퍼 (외부 라이브러리 0).

| 메서드 | 경로 | 용도 |
|---|---|---|
| GET  | `/qms` | 품질 대시 (severity/SLA/재발 KPI 6종 + SLA 위반 위험 목록) |
| GET  | `/qms/issues/{iid}/sla` | 단일 이슈 SLA 현황 JSON (elapsed_h/remaining_h/breached) |
| POST | `/qms/issues/{iid}/corrective` | 시정조치 추가 + qms_audit_log 자동 기록 |
| POST | `/qms/issues/{iid}/preventive` | 예방조치 추가 (corrective_id 종속) + 감사로그 |
| GET  | `/qms/recurrence` | 재발 추적 (recurrence_id 명시 그룹 + root_cause 텍스트 그룹 fallback) |

**페이지 카버리지 grep** (`@app\.(get|post)\("/qms`):
```
5980:@app.get("/qms", response_class=HTMLResponse)
6021:@app.get("/qms/issues/{iid}/sla", response_class=HTMLResponse)
6034:@app.post("/qms/issues/{iid}/corrective")
6060:@app.post("/qms/issues/{iid}/preventive")
6087:@app.get("/qms/recurrence", response_class=HTMLResponse)
```
QMS 라우트 5/5 등록.

## 3. 신규 템플릿 2종 (외부 차트 라이브러리 0건)

- `qms_dashboard.html` (80줄) — KPI Strip 6종 (전체/미해결/치명·심각/SLA 위반/재발 그룹/CA 진행) + SLA 위반 위험 테이블 (잔여 h<4 노란 / breached 빨강 + ⚠)
- `qms_recurrence.html` (76줄) — recurrence_id 그룹 트리 (각 그룹의 이슈 리스트 펼침) + root_cause SUBSTR(1,60) 텍스트 그룹 fallback 표

## 4. 변경 라인 분리 (정직성 정책 5항)

**HTML 변경 라인** (qms_dashboard 80 + qms_recurrence 76 = **신규 156줄** / 조정 0 / **net +156**). `base.html`/기존 issue 템플릿 미접촉.
**CSS 변경**: `static/style.css` **본 사이클 미접촉** — 신규 0 / 조정 0 / **net 0** (모든 시각 요소 인라인 style).
**Python**: `database.py` 5428→5520 (**+92**) · `main.py` 5922→6126 (**+204**).
**본 사이클 합계**: **+452줄** (HTML 156 + Python 296).
**누적(이전 간트번다운 1차 +409 + 본 +452)**: **약 +861줄**.

## 5. 핫패치 G1~G5 grep — 직접 인용 (사이클 29 권고)

```
static/style.css:2253  핫패치 G1·G5 영향 없음 (margin-right 시프트만)
static/style.css:4613  v2 본체(4287~4574) 무수정 · 핫패치 G1~G5 보존
static/style.css:4731  v2 본체(4287~4574) 무수정 · 핫패치 G1~G5 보존
static/style.css:4903  v2 본체(4287~4574) 무수정 · 핫패치 G1~G5 보존
static/style.css:5130  v2 본체(4287~4574) 무수정 · 핫패치 G1~G5 보존
static/style.css:5371  v2 본체(4287~4574) 무수정 · 핫패치 G1~G5 보존
static/style.css:5548  v2 본체(L4287~L4574) 미접촉 / 핫패치 G1~G5 영향 없음
```
style.css 5610줄 본 사이클 미접촉 → **G1~G5 5/5 보존**.

## 6. BAT 갱신 (절대준수)

- `KNK업무시스템구축/START.bat` L3 → `2026-04-26 QMS-강화-1차` · L7 title · L13 echo
- `KNK업무시스템구축/KNK_시작.bat` L3 → `2026-04-26 QMS-강화-1차` · L7 title · L14 echo

## 7. 자기검증

- [x] G1~G5 5/5 보존 (style.css 미접촉)
- [x] v2 본체(L4287~L4574) 미접촉
- [x] 외부 자산 0건 (인라인 style + 외부 차트 0)
- [x] DB idempotent (CREATE IF NOT EXISTS 3테이블 + 6인덱스 + ALTER ADD 6컬럼은 PRAGMA 검사 후)
- [x] `_qms_guard()` 단일 정의 (5 라우트 일관 적용)
- [x] BAT 2개 갱신 (3행 × 2파일)

## 8. 다음 사이클 예고 — QMS 2차

- 시정조치 due_date 임박/지연 알림 (하이웍스 푸시)
- recurrence_id 자동 추천 (root_cause embedding cosine ≥ 0.85)
- ISO 9001 8.7 부적합품 관리 / 10.2 시정조치 보고서 PDF 출력
- P2 품질팀 주2회 SLA 8h/16h/24h 정책 시드
