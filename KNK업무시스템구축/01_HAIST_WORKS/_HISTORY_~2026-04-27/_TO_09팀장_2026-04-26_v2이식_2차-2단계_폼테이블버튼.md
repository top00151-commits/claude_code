# TO 09팀장 — v2 이식 2차-2단계 (폼·테이블·버튼) 적용 회신

- 발신: 01 메인(실무팀) 빅터
- 일자: 2026-04-26
- 사이클: 09발주 → **01작업(완료)** → 04회귀 → 00감사 → 99
- 잡: v2 정식 이식 2차-2단계 — 폼/테이블/버튼 영역 시안 정합

## 1. 작업 요약
- 적용 위치: `static/style.css` L4724+ 신규 블록 (§1~§9)
- v2 본체(4287~4574) **자체 수정 0줄** (절대 준수)
- HTML 변경 0줄 (CSS 셀렉터 일괄 정합 — 사이클 20 패턴 유지)
- 외부 자산 0건

## 2. 시안 클래스 매핑 (시안 12B 인용)
| v2 시안 톤 | 1차 적용 셀렉터 | 토큰 |
|---|---|---|
| sage 라운드 12px 인풋 | `.main .field input/select/textarea`, `.main-wrap .field …` | `--text-md`, `--v2-ctrl-radius` |
| 라벨 (--text-sm 굵게) | `.main .field label` | `--text-sm` |
| 테이블 헤더 sage-50 | `.main .table th` | `--text-sm` 대문자 |
| 테이블 본문 hover sage | `.main .table td`, `.main .table tr:hover` | `--text-sm` |
| 기본 버튼 sage 톤 | `.main .btn` | `--text-sm` |
| Primary sage 그라디언트 | `.main .btn-primary` | `linear-gradient(sage-600→sage-700)` |
| 보조/위험 버튼 | `.btn-sm .btn-xs .btn-ghost .btn-danger` | `--text-sm` |
| 필터바 인풋 | `.main .filter-bar input/select` | `--text-sm` |

## 3. 적용 페이지 카버리지 (CSS 일괄)
- 홈 / 일일 / 달력 / 피드 (`home.html`, `daily.html`, `calendar.html`, `feed.html`)
- Top3 S1 매출 4탭 (`sales_*.html`)
- Top3 S2 재고 4탭 (KPI 테이블 + 부적합 모달 폼)
- Top3 S3 권한 3탭 (`admin_permissions*.html` — 폼 + Cascade + 감사 테이블)
- 수출입 6탭 (`export_*.html` — 다중 라인 그리드 + 5단계 진행도 + CI/PL/BL 테이블)
- 게시판·이슈·티켓 (`board_*.html`, `issues_*.html`, `changes_*.html`)
- admin / dashboard / history / base_logi 등 공통 영역

총 영향 페이지 약 20+개 (`.btn / .table / .field` 셀렉터가 main 영역에서 사용 중인 모든 화면).

## 4. 토큰 사용 결과
- 폼 라벨: `--text-sm` (clamp 13~15px)
- 폼 입력: `--text-md` (clamp 15~17px) — 한국어 16px 권장 충족
- 테이블 헤더: `--text-sm` 대문자 + sage-700 색
- 테이블 본문: `--text-sm`
- 버튼: `--text-sm`
- 버튼 색: `--sage-600/700` 그라디언트 + 시안 12B `--v2-btn-shadow`

## 5. CSS 변경 라인 수
- **이번 신규: +168 줄** (L4725~L4892, §1 토큰 ~ §9 모바일)
- **이번 조정: ±0 줄** (기존 행 비변경, 셀렉터 우선순위만 추가)
- **이번 net: +168 줄**
- **누적(2차 합계): +118(2차-1단계 카드) + 168(2차-2단계) = +286 줄**

## 6. 핫패치 G1~G5 보존 grep 결과
| ID | 검증 grep | 라인 | 상태 |
|---|---|---|---|
| G1 | `sb-toggle` 정의 | L644-645 / L857 / L1369-1370 | OK |
| G2 | `.dock top:0` 베이스 | L2584 (이후 G4가 override) | OK |
| G3 | `.topbar` 활성 | L143 / L4303-4313 (v2 본체) | OK |
| G4 | `.dock` `top: var(--topbar-h)` | L3653 | OK |
| G5 | `.main-wrap margin-right` | L3637-3639 | OK |

v2 본체(L4287~L4574) diff: **0줄** (verified by line offset).

## 7. BAT 갱신
`app/templates/base.html` L7:
```
LAST UPDATE: 2026-04-26 v2이식-2차-2단계-폼테이블버튼 — style.css L4724+ 신규 §1~§9
(.btn/.btn-primary/.btn-sm/.btn-ghost/.btn-danger/.table/.field input·select·textarea
시안12B 정합 · sage 그라디언트 · 12px 라운드 · --text-sm/md 토큰)
— G1~G5 보존, v2본체(4287~4574) 무수정, 페이지 HTML 0줄 변경
```
캐시 버스터: `?v=20260425v2c2` → `?v=20260426v2c2-2`

## 8. 14px 광범위 영향군
사이클 20 결정 유지 — **본 단계에서도 14px raw 직타 미수정** (토큰 `--text-sm` clamp 13~15px 으로 자연 흡수).

## 9. 다음 단계 예고
**2차-3단계 = 모달 / 드롭다운 / 토스트 / 알림** (`.modal / .dropdown / .toast / .notif` 시안 12B 정합 예정, 작업 시간 12~15분 추정).

---
**04 회귀 가능.**
