# [01 → 09] 재고 실사·조정 워크플로 1차 (Top10 #10 · P4 자재팀)

작성: 2026-04-26 | 빅터(01 메인) | 갭서베이 Top10 #10 — 분기 1회 실사 워크플로

## 요약
- 갭: 기존 `/stock/adjust` 단일 폼 (검수 승인·증명서 부착·차이 추적 없음)
- 본 사이클: DB 테이블 +4 / 인덱스 +8 / 라우트 +7 / 템플릿 +2
- 기존 `stock_movements` + `stock_balances` VIEW 그대로 활용 — 승인 시 ADJUST 행 자동 생성
- 시퀀스 `AUD-YYYYMM-####` idempotent · 외부 자산 0건

## 1. DB 스키마 (database.py L1062~L1122)
- L1064~L1076 `stock_audits` 헤더 + idx_sa_status / idx_sa_start
- L1078~L1094 `stock_audit_items` 라인 (system_qty 스냅샷 / variance 자동 / status PENDING·MATCHED·SHORT·OVER) + idx 3종
- L1096~L1111 `stock_adjustments` 조정 (PENDING·APPROVED·REJECTED · movement_id FK) + idx 2종
- L1113~L1121 `audit_attachments` 증명서 첨부 + idx 1종
- 모두 `CREATE TABLE IF NOT EXISTS` · `CREATE INDEX IF NOT EXISTS` (idempotent)

## 2. 라우트 +7 (main.py L4570~L4683)
- L4582 GET /stock/audits — 실사 목록 (`_audit_guard`)
- L4595 POST /stock/audits/new — 신규 실사 (시퀀스 `gen_audit_no`)
- L4606 GET /stock/audits/{id} — 실사 상세 (라인 그리드)
- L4626 POST /stock/audits/{id}/items — 실측 라인 추가/수정 (변동 자동 PENDING 조정 생성)
- L4647 GET /stock/adjustments — 조정 목록 (status 필터)
- L4660 POST /stock/adjustments/{id}/approve — 승인 (UPDATE + INSERT stock_movements ADJUST)
- L4674 POST /stock/adjustments/{id}/reject — 반려

## 3. 권한 분기 (main.py L4570~L4584)
- L4575 `_audit_guard(u)` — `can_use_logistics(u)` 위임 (자재팀·구매팀·admin·ceo·executive 허용)
- L4580 `_audit_approve_guard(u)` — admin/ceo/executive **OR** team_id==10 leader (자재팀장만 조정 승인)
- 검증: `Grep _audit_guard|_audit_approve_guard /main.py` → 21 hits

## 4. 신규 템플릿 +2 (templates/)
- `stock_audit.html` — mode=list (실사 이력 + 신규 시작) / mode=detail (라인 입력 + 그리드)
- `stock_adjustment.html` — 조정 승인 화면 (status 필터 · 승인/반려 버튼 권한 분기)

## 5. DB 헬퍼 (database.py L3697~L3886, +191 LOC)
- `gen_audit_no` / `stock_audit_create` / `stock_audits_list` / `stock_audit_get`
- `stock_audit_item_upsert` (variance 자동 + 차이≠0 시 PENDING 조정 자동 생성)
- `stock_adjustments_list` / `stock_adjustment_approve` (→ stock_movement_create kind=ADJUST) / `stock_adjustment_reject`

## 6. 변경 라인 5분리 (정직성 정책 v2 §3)
| 분류 | 파일 | 위치 | 라인 |
|---|---|---|---|
| HTML 본 사이클 | base.html | L7 (LAST UPDATE 라벨만) | +0 / -0 (1행 교체) |
| HTML 본 사이클 | stock_audit.html | 신규 | +106 |
| HTML 본 사이클 | stock_adjustment.html | 신규 | +73 |
| Python 본 사이클 | database.py | L1062~L1122, L3697~L3886 | +258 |
| Python 본 사이클 | main.py | L4256~L4260, L4570~L4683 | +124 |
| CSS 본 사이클 | style.css | (미접촉) | +0 |

## 7. 누적 3 카테고리 (정직성 정책 v2 §7)
- **본 사이클 한정**: HTML +179 / Python +382 / CSS 0 = 561 LOC (5파일)
- **Top10 #10 트랙 누적**: 본 사이클 1차이므로 = 561 LOC (트랙 = 본 사이클)
- **세션 전체 누적**: 본 사이클은 추가 회차 / 직전 BAT는 "본문14px-토큰일괄" — 세션 전체 누적치는 별도 보고서에 기록 (본 회신 한정 미집계)

## 8. 핫패치 G1~G5 grep 보존 검증
- `Grep G[1-5] static/style.css` → **9 hits** 보존 (L2253, L3634, L4613, L4731, L4903, L5130, L5371, L5548 + base.html L7 미집계)
- v2 본체 L4287~L4574 (style.css) — 본 사이클 미접촉 (라우트는 main.py / 템플릿은 templates/, CSS 0건)
- base.html L7 LAST UPDATE 라벨 외 무수정 · L95 핫패치 주석 (sb-toggle) 보존

## 9. BAT 갱신
- base.html L7: `LAST UPDATE: 2026-04-26 재고실사조정-1차 — Top10 #10 (P4 자재팀 분기 1회): DB +4 ... v2본체(4287~4574) 무수정 · G1~G5 보존 · 외부자산 0건`
- 캐시 버전 미변경 (CSS 0건이므로 `?v=20260426text14tok` 유지)

## 10. 자기검증
- G1~G5 grep 9 hits 보존 — OK
- v2 본체 (style.css L4287~L4574) 미접촉 — OK
- 외부 자산 0건 (라이선스 첨부·로고·아이콘 신규 0) — OK
- 시퀀스 idempotent (`AUD-YYYYMM-####` · 동일 월 내 max(seq)+1) — OK
- CREATE TABLE/INDEX 전건 IF NOT EXISTS — OK
- ALTER 불필요 (신규 테이블만, 기존 테이블 컬럼 추가 0건)

## 11. 후속 사이클 (2차 예정)
- 증명서 파일 업로드 라우트 (audit_attachments INSERT — 본 1차는 테이블만 생성)
- 실사 close 워크플로 (status REVIEW → CLOSED)
- 04 운영테스트팀 회귀 후 UX 보강

## 04 회귀 가능 여부
- 가능. /stock/audits, /stock/adjustments 양 화면 + 권한 분기 (자재팀원 vs 자재팀장) 페르소나 P4 시나리오 수행 가능.
