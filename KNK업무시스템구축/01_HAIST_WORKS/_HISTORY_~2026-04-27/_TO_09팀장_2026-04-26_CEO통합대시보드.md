# 01 → 09팀장 회신 — CEO 통합 대시보드 (2026-04-26)

발주: `_FROM_09팀장_2026-04-26_CEO통합대시보드` (Top10 P1 CEO 매일)
작업 시간: 약 12분 (목표 15분 이내)

---

## 1. ceo_dashboard_kpis 헬퍼 (database.py 신규 단일 헬퍼)

`app/database.py` 말미에 `def ceo_dashboard_kpis(user_id: int = 0) -> dict:` 추가.
9 영역 통합 KPI dict 반환:

| 영역 | 키 | 데이터 소스 (테이블) |
|---|---|---|
| 매출 | `sales` (month/prev_month/growth/unpaid) | orders · invoices · receipts_payment |
| 재고 | `stock` (on_hand_kinds/qc_open) | stock_balances VIEW · qc_inspections |
| 권한 | `perms` (active_tokens/expiring_7d) | delegation_tokens |
| 수출입 | `exports` (in_progress/ship_soon) | export_orders |
| QMS | `qms` (open/sla_violation) | issues |
| 주간 | `weekly` (completed/in_progress/delayed/rate) | tasks |
| 간트 | `gantt` (delayed_projects) | projects |
| 환율 | `fx` (usd_rate/alerts_active) | exchange_rates · rate_alerts |
| 알림 | `notif` (unread) | notifications |

각 카테고리 try/except 폴백 — 테이블 미존재·시드 0건이어도 0 반환, 예외 비전파.

## 2. 라우트 보강·신규 (main.py)

- `from .database import (..., ceo_dashboard_kpis)` 추가 (1줄).
- `@app.get("/ceo")` 신규 — `RedirectResponse("/dashboard", 303)` 별칭.
- `@app.get("/dashboard")` 본체 보강:
  - 기존 권한 가드(`require(req, ["ceo","admin","executive"])`) 무수정.
  - return 직전 `ceo_kpis = ceo_dashboard_kpis(user_id=u["id"])` 1줄.
  - `notifications` UNREAD 5건 SELECT 1쿼리 (`unread_notifs`).
  - ctx 컨텍스트 인자 2개 추가(`ceo_kpis`, `unread_notifs`).

## 3. UI 변경 (dashboard.html)

기존 헬로 배너 직후 `{% if ceo_kpis %}` 가드 블록 추가:

- **9 KPI 그리드** (`kpi-grid kpi-grid-9`) — 영역별 카드 + 메인 페이지 링크
  (매출·재고·권한·수출입·QMS·주간·간트·환율·알림).
- **최근 미확인 알림** — `unread_notifs` 5건 리스트 (있을 때만 표시).
- **빠른 액션** — 8칩 그리드 (sales / parts / permissions / export / qms /
  gantt / rates / weekly).

기존 14팀 테이블 / TOP10 지연 / 고객사별 공수 섹션 무수정 (하단 공존).

## 4. 변경 라인 5분리

1. `app/database.py` — 함수 1개 신규 (~190 lines, 모듈 말미).
2. `app/main.py` — import 1행 추가, `/ceo` 라우트 6줄, `/dashboard` ctx 보강 7줄.
3. `app/templates/dashboard.html` — `{% if ceo_kpis %}` 블록 추가 (~80 lines).
4. `KNK업무시스템구축/KNK_시작.bat` — LAST UPDATE 라벨 / title / echo 갱신.
5. `KNK업무시스템구축/START.bat` — LAST UPDATE 라벨 / title / echo 갱신.

## 5. 누적 3 카테고리

- 본 사이클 1건 (CEO 통합 대시보드)
- 본 트랙 (Top10 P1) 1건
- 본 세션 14건 (Top3 S1·S2·S3 + QMS + 간트 + 알림 + 검색 + v2이식 등 이전 누적)

## 6. 핫패치 G1~G5 grep

`Grep "G1|G2|G3|G4|G5"` → static/style.css 9 / base.html 1 / main.py 1 /
database.py 4 / sales_dashboard.html 1 = 총 16건 모두 보존 확인 (5/5 marker
파일 무수정).

## 7. BAT 갱신

`KNK_시작.bat` & `START.bat` 양쪽 LAST UPDATE 라벨 = "2026-04-26 CEO통합대시보드"
+ 상세 변경 요약 1줄 (한/영 동기) · title · echo 모두 동기.

---

## 자기검증 (정직성 v2 7항)

- [x] G1~G5 핫패치 16건 보존 (수정 0)
- [x] v2 본체(시안 토큰·base 헤더/사이드바) 미접촉
- [x] 외부 자산 0건 (CDN/이미지/폰트 도입 없음)
- [x] DB 스키마 무변경 (CREATE TABLE / ALTER 0건)
- [x] 단일 헬퍼 + 안전 폴백 (테이블 미존재 → 0 반환)
- [x] 권한 분기 무변경 (기존 ceo·admin·executive 가드 유지)
- [x] BAT LAST UPDATE 동기 갱신 (2종)

04 회귀 가능. 시드 미확보 영역(invoices·delegation_tokens 등)도 폴백으로 0 표기.
