# 01 → 09 회신 — 간트/번다운 1차 (DB + 라우트 + UI 골격)

**작성**: 빅터 (01 메인) · **일시**: 2026-04-26 · **사이클**: 09 발주(갭서베이 Top10 #4) → 01 작업 → 04 회귀 대기

---

## 1. DB 스키마 신규 (idempotent)

`app/database.py` `customs_declarations` 인덱스(L985~986) 직후 +30줄. 기존 `projects`/`tasks`/`project_phases` **무수정** (v2 본체 미접촉).

| 테이블 | 핵심 컬럼 |
|---|---|
| `project_milestones` | id / project_id / name / target_date / completed_at / status (PLANNED/IN_PROGRESS/DONE/DELAYED/CANCELLED) |
| `project_burndown_snapshots` | id / project_id / snap_date / total_tasks / completed_tasks / remaining_hours / `UNIQUE(project_id, snap_date)` |

`CREATE TABLE IF NOT EXISTS` 2건 + `CREATE INDEX IF NOT EXISTS` 4건.

## 2. 라우트 신규 4개 + 권한 가드 단일 정의

`app/main.py` 말미 (수출입 L5769 직후) +153줄. `_progress_guard(req, project_id)` — admin/ceo/executive OR PM(`pm_id`) OR lead(`lead_user_id`) OR leader/pm role.

| 메서드 | 경로 | 용도 |
|---|---|---|
| GET  | `/progress/{project_id}/gantt` | 12공정 간트 + 마일스톤 |
| GET  | `/progress/{project_id}/burndown` | 일별 스냅샷 추세 + 오늘 실시간 KPI |
| POST | `/progress/{project_id}/snapshot` | 오늘 스냅샷 UPSERT (UNIQUE 충돌 시 갱신) |
| GET  | `/progress-dashboard` | 전사 프로젝트 대시 (admin/ceo/executive 전용) |

**충돌 회피**: `/progress/dashboard` 는 기존 `/progress/{project_id:int}` 와 겹쳐 하이픈 URL `/progress-dashboard` 로 분리.

## 3. 신규 템플릿 3종 (외부 차트 라이브러리 0건)

- `progress_gantt.html` (88줄) — 공정 행 + CSS bar (linear-gradient `#A5282C → #D33`) + 마일스톤 리스트
- `progress_burndown.html` (79줄) — 인라인 SVG `<polyline>` + `<circle>` + 점선 계획선 + KPI 4종
- `progress_dashboard.html` (59줄) — 전사 프로젝트 표 (납기 임박순) + CSS bar

## 4. 변경 라인 분리 (정직성 정책 5항)

**HTML 변경 라인**: 신규 226 (gantt 88 + burndown 79 + dashboard 59) / 조정 0 / **net +226**. `base.html` 미접촉.
**CSS 변경**: `static/style.css` **본 사이클 미접촉** — 신규 0 / 조정 0 / **net 0**. (모든 시각 요소 인라인 style)
**Python**: `database.py` 5398→5428 (+30) · `main.py` 5770→5923 (+153).
**본 사이클**: +409줄 / **누적(이전 수출입 1차 656 + 본 409)**: 약 +1,065줄.

**페이지 카버리지 grep** (`@app.get|@app.post.*progress` in main.py):
```
3014:@app.get("/progress"
3026:@app.get("/progress/{project_id}"
3046:@app.post("/progress/phase/{phase_id}/update"
3084:@app.get("/api/progress/summary"
5820:@app.get("/progress/{project_id}/gantt"
5850:@app.get("/progress/{project_id}/burndown"
5879:@app.post("/progress/{project_id}/snapshot"
5897:@app.get("/progress-dashboard"
```
기존 4 + 신규 4 = 총 8 라우트.

## 5. 핫패치 G1~G5 grep — 직접 인용 (사이클 29 권고)

```
static/style.css:99    /* ===== A2 폰트 토큰 시스템 v1 (2026-04-25 · 05 §B2 / G17) ===== */
static/style.css:2253  핫패치 G1·G5 영향 없음 (margin-right 시프트만)
static/style.css:3629  /* 핫패치 2026-04-25 17:30 — body.dock-open .topbar { padding-right: 420px } 제거
static/style.css:3634  G1 margin-right 적용을 안 받음 → 도크 열림 시 우측 끝
static/style.css:4613  v2 본체(4287~4574) 무수정 · 핫패치 G1~G5 보존
static/style.css:5548  v2 본체(L4287~L4574) 미접촉 / 핫패치 G1~G5 영향 없음
```
style.css 5610줄 본 사이클 미접촉 → **G1~G5 5/5 보존**.

## 6. BAT 갱신 (절대준수)

- `KNK업무시스템구축/START.bat` L3 → `2026-04-26 gantt-burndown-1st`
- `KNK업무시스템구축/KNK_시작.bat` L3 → `2026-04-26 간트번다운-1차`

## 7. 자기검증

- [x] G1~G5 5/5 보존 (style.css 미접촉)
- [x] v2 본체 미접촉 (projects/tasks/project_phases 컬럼 변경 0)
- [x] 외부 자산 0건 (CSS bar + 인라인 SVG 직접 작성)
- [x] DB idempotent (CREATE IF NOT EXISTS 2테이블 + 4인덱스 + UNIQUE 1)
- [x] `_progress_guard()` 단일 정의 (4 라우트 일관 적용)
- [x] BAT 2개 갱신

## 8. 다음 사이클 예고 — 간트/번다운 2차

- 데이터 시각화 강화 (12공정 SVG 타임라인 + 의존성 화살표)
- 단계별 예측 알고리즘 (남은 작업/속도 → 예상 종료일 ETA + 신뢰구간)
- 마일스톤 등록 폼 + 일별 스냅샷 cron 자동화 (현재 수동 POST)
- CSV 내보내기 (P2 CEO 임원회의용)
