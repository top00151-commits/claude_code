# 01 → 09 팀장 회신 — 주간업무보고 자동집계 1차 (2026-04-26)

갭서베이 Top10 #5 (P1~P10 11명 매일 사용) 자동집계 1차 완료.

## 1. DB — VIEW 신설 (테이블 신설 0건)
- `database.py` L987 직전 삽입 — `CREATE VIEW IF NOT EXISTS weekly_summary` (idempotent)
- 컬럼: user_id / team_id / week_start (월요일) / week_end (일요일) / total_tasks / completed / in_progress / delayed / total_hours
- 자동 집계: `tasks` GROUP BY user_id + team_id + week_start
- SQLite weekday 보정: `DATE(work_date, '-' || ((CAST(strftime('%w', work_date) AS INT)+6)%7) || ' days')`
- 테이블 신설 0건 (대표 권장 안 채택)

## 2. 라우트 보강 (3개 신규 + 1개 보강)
| 라우트 | 메서드 | 권한 | 비고 |
|---|---|---|---|
| `/weekly`, `/weekly/{wk_mon}` | GET | 본인 | **보강** — 4주 트렌드 + 부서비교 데이터 추가 |
| `/weekly/team` | GET | leader/executive/ceo/admin | 신규 (302 → `/weekly/{wk_mon}?scope=team`) |
| `/weekly/company` | GET | ceo/admin/executive | 신규 (302 → `/weekly/{wk_mon}?scope=company`) |
| `/weekly/refresh` | POST | 인증 사용자 | 신규 — `ANALYZE weekly_summary` 캐시 갱신 |

## 3. 정직성 정책 5라인 분리 (본 사이클 한정)

### A. 본 사이클 한정 (이번 작업만)
| 파일 | 신규 | 조정 | net |
|---|---:|---:|---:|
| `app/database.py` | +25 | 0 | +25 |
| `app/main.py` | +89 | 0 | +89 |
| `app/templates/weekly.html` | +59 | -1 | +58 |
| `static/style.css` | +67 | 0 | +67 |
| `KNK_시작.bat` | 0 | 3 | 0 |
| `START.bat` | 0 | 3 | 0 |
| **합계** | **+240** | **-1** | **+239 줄** |

### B. 누적 unstaged (HEAD 7ddb9da 기준 — 본 사이클 + 이전 미커밋분)
| 파일 | 추가 | 삭제 |
|---|---:|---:|
| `app/database.py` | +494 | 0 |
| `app/main.py` | +1,236 | -1 |
| `app/templates/weekly.html` | +59 | -1 |
| `static/style.css` | +1,256 | -213 |

### C. CSS-only vs HTML 동시 변경
- **CSS-only**: `static/style.css` L5544~L5610 (.wk-trend / .wk-dept 블록 1건)
- **HTML 동시**: `weekly.html` (재집계 버튼, 4주 트렌드 SVG, 부서비교 바, 빈상태 버튼)
- **JS 인라인**: `weekly.html` `weeklyRefresh()` 함수 6줄 (외부 JS 파일 0건)

### D. HTML 변경 라인 의무 명시 (`weekly.html`)
- L13~L23 — 재집계 버튼 + `<script>weeklyRefresh()</script>` (10줄 신규)
- L51~L73 — `{% if my_trend %}` 4주 트렌드 SVG 블록 (22줄 신규)
- L75~L94 — `{% if dept_compare %}` 부서별 비교 바 차트 (20줄 신규)
- L99~L102 — 빈상태에 [재집계] 버튼 인라인 (3줄 조정)

### E. 페이지 카버리지 실측 grep
- `weekly_summary` VIEW 사용처: `app/main.py` 2곳 (개인 4주 트렌드 / 부서비교)
- `wk-trend`, `wk-dept` 클래스 정의: `style.css` L5550·L5569
- `/weekly/team`, `/weekly/company`, `/weekly/refresh` 라우트: `app/main.py` 신규 3건

## 4. 권한 분기 (요건 충족)
- `/weekly` = 모든 로그인 사용자 (본인만)
- `/weekly/team` = `leader`/`executive`/`ceo`/`admin` (그 외 → `/weekly` 리디렉트)
- `/weekly/company` = `ceo`/`admin`/`executive` (그 외 → `/weekly` 리디렉트)
- `/weekly/refresh` = 인증만 (모든 사용자 본인 캐시 새로고침 가능)

## 5. 자기검증 (G1~G5 핫패치 보존 grep)
- `style.css` 내 `핫패치|G1~G5` 마커 12건 모두 보존 (L99·L2253·L3629·L3633·L3650·L4613·L4731·L4903·L5130·L5371·L5548)
- v2 본체 L4287~L4574 무수정 (이번 추가 블록은 L5544+ 파일 끝)
- 외부 자산 0건 (인라인 SVG + CSS only · 차트 라이브러리 절대 미사용)
- VIEW idempotent (`CREATE VIEW IF NOT EXISTS`)

## 6. BAT 갱신
- `KNK_시작.bat` L3·L7·L14 → `2026-04-26 주간보고-자동집계-1차`
- `START.bat` L3·L7·L13 → `2026-04-26 weekly-auto-aggregate-1st`

## 7. 산출 요약 (09 회신용)
01 주간보고 자동집계 1차 완료: VIEW 1개 신설 + 라우트 3 신규 (team/company/refresh) + UI 4주 트렌드 SVG + 부서비교 바차트. 본 사이클 +239줄 (DB +25 / main.py +89 / weekly.html +58 / CSS +67). 누적 unstaged +3,045 / -215. 핫패치 G1~G5 12건 보존. v2 본체(L4287~L4574) 미접촉. 외부 차트 라이브러리 0건. BAT 양쪽 갱신. 04 회귀 가능.
