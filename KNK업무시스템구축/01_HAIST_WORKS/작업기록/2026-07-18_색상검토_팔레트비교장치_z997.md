# 눈 편한 색상 검토안 검증 + 디자인키트 팔레트 비교 장치 (z997)

- **일자**: 2026-07-18
- **배경**: 대표가 CODEX로 작성한 `JR\CODEX\김정락 브랜딩\KNK_ERP_EYE_COMFORT_COLOR_REVIEW.md`
  (장시간 사용 눈 피로 완화 — **Soft Neutral Sage** 팔레트안) 상세 검토 지시.

## 1. 검토 결과 (대표 보고됨)
- **수치 전수 재검산**: 문서의 대비(WCAG) 주장 10건을 직접 계산 → **10건 전부 정확히 일치**
  (보조글자/크림 3.72:1·Muted/Surface 4.91:1·흰글자/빨강 7.14:1 등). 신뢰 가능 문서.
- **진단 = 우리 실측과 일치**: 세 배경 체계 혼재([[knk_works_css_palette]])·--qv 비보장·전역 재매핑 금지
  (z990d/z993에서 실제 겪은 함정과 동일 결론)·적용 순서(키트→parts→폼→셸)와 LIVE 임시 적용 비교 = 우리 표준.
- **⚠주의 발견(추가 검산)**: 브랜드 빨강(#A5282C) ↔ 위험 빨강(#A33A3E) **상호 대비 1.1:1** = 색만으론
  구분 불가 → 채택 시 '문구+아이콘 병행'을 절대 규칙으로. Muted 4.91:1은 통과지만 여유 적음(11px 라벨=Secondary 권장).
- 대표 선택: **"비교 장치부터"**.

## 2. 구현 — 디자인키트 팔레트 비교 (z997)
- `/admin/design-kit` 맨 위 **[현행 팔레트] [Sage 후보]** 버튼 — `<style media>` 토글로 :root의
  `--knk-*` 22개를 문서 §5 값으로 덮음 → 아래 모든 예제(버튼·입력·표·배지·알림·빈상태) 즉시 전환.
  이 페이지에서만·저장 안 됨·다른 화면 영향 0. 셸(헤더·사이드바)은 4단계 별도라 그대로(비교 안내문에 명시).
- 문서 반영: 통일 가이드 §3.2에 '색상 방향 후보' 블록(채택 무관 유효 규칙 6개 포함 — Muted 4.5:1·앰버 제한·
  빨강 면적 제한·브랜드/위험 병행 규칙·표 상태 구분·--qv 비보장) + 현황판 §5(비교 방법·채택 절차·주의).

## 3. 검증
- Jinja ✅ esprima(토글 703자) ✅ **Sage 22개 값 = 문서 §5 정확 대조 일치** ✅
  (제외 2: text-inverse=기존과 동일 흰색·focus=신규 토큰이라 채택 시 도입)
- 배포: `636b448e` → LIVE §4

## 4. LIVE 실증 + z997b 핫픽스 (Chromium input 고착 규명)
- **토글 정상**(LIVE 실측): 현행 surface #ffffff ↔ Sage #FAFBF9·text #27302B·success #2F6B4F·
  표 머리글 #EAEEE9·배지 색 전환 ✅ 브랜드 버튼은 Sage에서도 KNK 빨강 유지 ✅ 현행 복귀 ✅
- **🔴발견: 기존 input류만 테두리 색 불변** — 원인 규명 과정(전부 LIVE 실측):
  ①transition 의심 → 300ms 후에도 불변(기각) ②레거시 !important 오염 의심 → 규칙 전수 스캔(중첩 @media
  재귀 포함) 결과 매칭 규칙 1개뿐(기각) ③:root 변수·요소가 보는 변수 모두 Sage 값 정상 ④명시 border-color
  규칙(문서 끝)도 무시, **인라인 지정까지 무시** ⑤신규 생성 input은 정상 →
  **결론: Chromium이 `<style media>` 토글 시 '기존 input류'의 계산 스타일을 고착**(재계산 누락 버그성 동작).
- **해법(z997b·d52cd954)**: 팔레트 전환 시 `.knk-input/.knk-select/.knk-textarea`에 display 토글로
  재계산 강제(동기 1프레임·깜빡임 없음) — display 토글·clone 교체 모두 즉시 해결됨을 실측 후 가벼운 쪽 채택.
- **검증기 함정 2건도 기록**: ⑴규칙 스캔 시 CSS 중첩 지원 브라우저에선 모든 규칙에 cssRules가 존재 —
  `if(r.cssRules) 재귀` 하면 스타일 규칙 자체 검사를 건너뜀(길이 확인 필수) ⑵프로브 결과에 '='가 많으면
  DLP 확장이 차단 — 표기 문자를 바꿔 반환.

## 5. 다음
- **대표: 디자인킷에서 비교(밝은/어두운 환경 각 15분 권장) → 채택 여부 결정**
- 채택 시: knk-foundation 값 교체(키트+자재 목록 즉시 반영) → --knk-focus 토큰 도입 → 폼 → 셸(별도 승인)
