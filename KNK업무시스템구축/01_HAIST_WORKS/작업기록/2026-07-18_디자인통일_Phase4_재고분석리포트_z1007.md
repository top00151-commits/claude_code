# 디자인 통일 Phase 4 — 재고 분석 리포트 3종(재주문·ABC·FIFO) knk 전환 (z1007)

- **일자**: 2026-07-18
- **배경**: 대표 "진행해" — Phase 4 핵심(협력사·발주·재고·BOM) 이후 **재고 잔여 화면** 정리 시작.
  잔여 10종 중 **읽기 전용 분석 리포트 3종**부터(가장 단순·균일). 이력·감사 5종(출고이력·조정감사·실사·QC)은 다음(z1008).
- **대상**: `stock_reorder`(재발주 권고)·`stock_abc`(ABC 분류)·`stock_fifo`(FIFO 레이어). 전부 z1004 재고 목록과 동일 패턴.

## 1. 전환 원칙 — z1005/z1006과 동일(셸만 knk, 표·KPI·칩 유지+브리지)

| 전환(knk) | 유지(업무 특화·색만 Sage 브리지) |
|---|---|
| 머리글 → knk-page-head + Tabler(target·packages·plus·clipboard-list) | **tbl-erp 표**(sticky·zebra·행클릭·num/center/code 셀) |
| 빈상태 → knk-empty(circle-check·chart-bar·stack-2) | **KPI 스트립**(의미색: HIGH/MID/LOW·A/B/C 등급) |
| 버튼 → knk-btn(ghost/primary) | 우선순위 칩(chip-pri)·등급 칩(chip-abc)·부족분(shortage) |
| :root → pq→knk Sage 브리지 | tfoot 합계(fifo)·레이어바/누적바(layer-bar/cum-bar)·행 액션(btn-po-line) |

- **JS 없음**(순수 서버 렌더·onclick 인라인 네비게이션) → 스크립트 무관.
- z1004(재고 목록)가 tbl-erp를 knk-table--dense로 바꿨던 것과 달리, z1005/z1006 이후 정착한 **"업무 표=유지+브리지"** 판단을 적용(브리지로 색은 동일 Sage·구조 무손상·다파일 리스크 최소).

## 2. 세부

- **제목 이모지 제거**(📚 등) — 깔끔한 knk-page-title(형제 화면 통일).
- **죽은 CSS 정리**: `.sr-/.ab-/.fifo-head` 군·`.po-empty` 3종·**reorder 미디어쿼리 잔재 `.sr-head{padding}`**까지 → `.{prefix}-page > .knk-page-head` 배경 보정 1줄만.
- 3파일 :root 서로 다른 부분집합(reorder만 line-3/ink-2/danger 정의) → **풀 브리지**로 통일(미정의 토큰 명시 Sage).

## 3. 검증 (배포 전 전부 통과)

- Jinja parse ✅ · **JS 없음(script 0)** ✅ · `<style>` 중괄호 균형 ✅ · div/span 균형 ✅
- 옛 클래스 잔재 0: sr/ab/fifo-head·po-empty·btn-dark·btn-ghost(마크업+미디어쿼리) ✅
- knk 전환: page-head·breadcrumb·title 각 1 · actions(reorder/fifo 1·abc 없음) · knk-btn(3/0/2) · knk-empty(1/1/2) ✅
- :root 브리지(surface·ink)·업무 특화 보존(tbl-erp·kpi-strip·chip-pri/chip-abc·tfoot·layer/cum-bar·shortage·btn-po-line) ✅
- Tabler 글리프 7종(target·packages·plus·circle-check·chart-bar·clipboard-list·stack-2) 실존 ✅
- 배포: `65cb9d09`(3파일 +96/−124, 죽은 CSS 정리로 순감) → 목표 해시 감시

## 4. LIVE 실증

- (배포 완료 후 채움 — 3화면 머리글·Sage 색·표·KPI 의미색·빈상태 실측.)

## 5. 관련 / 다음

- z1004(재고 목록 패턴)·z1005·z1006 · [[knk_design_guide_unified]]
- **다음**: z1008 = 재고 이력·감사 5종(출고이력 issues·조정감사 adjustment·실사 audit(s)·QC — 상세/조치 모드 있음) + 안전재고 설정(safety·입력 있으면 폼 취급)·회전율(turnover). 이후 suppliers_import·po_qr_print → Phase 5.
