# 수주 삭제 후 프로젝트 완전삭제 차단 — 판정(설계대로) + 수주액 잔존 결함 수정 (z1077)

- **작성**: 2026-08-05 (수) 11:05 KST
- **신고**: 안지연 프로 (대표 전달·스크린샷 2장) — *"수주전 사항인데 삭제가 안되는데"* (SOCKET·관리번호 None·수주예정 프로젝트의 위험구역 폐기(완전삭제)가 차단)
- **상태**: ✅ 판정 완료(차단=설계대로·결함 아님) + 부수 결함 z1077 수정·배포 · 🔶 정책 판단 1건 대표 보고
- **배포**: monorepo `62c2383c` → 배포저장소 (LIVE 반영 감시 로그 참조)
- **변경 파일**: `app/project_workflow.py` 만 (⛔ WP-01 완전삭제 가드 `projects_delete_logi` 무접촉)

---

## 1. 무슨 일이 있었나 (LIVE 실측·id 1051 동일 프로젝트 확인)

1. 어제(08-04) z1076 건의 그 프로젝트(006T2607 SOCKET)에서, 안지연 프로가 **수주(SO T-260720-2)를 삭제**
2. 수주 삭제의 설계된 동작(v5H70): **마지막 수주 삭제 → 관리번호 회수(None)·상태 '수주예정'·제안 단계 복원** — 그래서 화면이 "None SOCKET·수주 내역 0건"
3. 이어서 **위험구역 [폐기(완전삭제)]** 시도(비밀번호까지 입력) → **WP-01 가드가 차단**:
   > 업무 이력이 있어 삭제할 수 없습니다 — prod_requests 1건 · project_history 23건 · project_items 1건

## 2. 판정 — 차단은 **설계대로** (결함 아님)

| 프로젝트 상태 | 완전삭제 규칙 (WP-01 · ERP 게이트 v3 F-02) |
|---|---|
| 관리번호 **있음** | 실거래(출하·세금계산서·수금) 있으면 잠금 · **0이면 허용** (z1060b·대표 지시 07-27) |
| 관리번호 **없음** ← 지금 | **업무 이력 참조 1건이라도 있으면 잠금** (ERP 원안 유지·대표 결정 07-24) |

이 건은 번호가 회수돼 '관리번호 없음' 갈래로 들어갔고, 제작요청서 1건·변경 이력 23건·장비 항목 1건이 남아 있어 차단. 가드 문구가 안내하는 '취소/보류' 처리가 설계된 대안.

### 🔶 대표 판단 필요 (정책 비대칭 관찰 — 임의 수정 안 함)

- **비대칭**: 관리번호 있는 상태(+실거래 0)면 지울 수 있는데, 수주를 지워 번호를 회수한 **더 이른 단계**가 되면 자기 이력만으로 못 지움 — 사용자에겐 "수주 전인데 왜?"로 보임
- 완화안(예: 관리번호 없어도 **타 업무 연결 0**(수주·변경서·품질이슈·소모품연결·자식) + 실거래 0 + 자기 부속물(자기 이력·자기 항목·자기 제작요청서)뿐이면 비밀번호 2단계로 허용)은 **게이트 승인된 F-02 가드의 정책 변경** → 대표 결정 + WP-01 시험 갱신 필요. **지시 주시면 진행합니다.**

## 3. 안지연 프로 실무 안내 (지금 바로 되는 길 — 삭제 불필요)

1. **이 프로젝트를 그대로 다시 쓰면 됩니다** — 이미 제안 단계로 복원됐으므로 [✓ 수주 확정] 다시 누르고 항목을 다시 넣으면 끝. 제작요청서·변경 이력도 그대로 살아 있어 오히려 유리
2. **006T2607 번호를 그대로 쓰고 싶으면**: 자동 발급은 최대순번+1이라 006이 다시 안 나옴 → **수주 확정 화면의 수동 관리번호 입력**에 006T2607 입력(회수된 빈 번호라 검증 통과 — z1059·z1060 실증 경로). ⚠수주번호(T-260720-2)는 새로 발급됨
3. 그래도 프로젝트 자체를 지워야 하면: **대표 지시로 서버 스위치 임시 해제**(WP-01 공식 통로) — 대표 결정 사항

## 4. 부수 결함 수정 (z1077) — 수주 삭제 후 수주액 잔존

- **증상**: 신고 화면 상단 "수주액 $6,830.00" — 수주 내역 0건인데 옛 합계가 남아 혼란 유발
- **원인**: `delete_order` 가 orders 는 지우면서 `projects.order_amount` 재동기화 누락
- **수정**: 삭제 직후 남은 수주 합으로 갱신(마지막 수주면 0) — 편집·확정 경로와 같은 규칙
- **시험(임시 DB) PASS 5/0**: SO 2건 중 1건 삭제=남은 합 · 마지막 삭제=0+번호 회수+수주예정 복원(안지연 케이스 그대로)
- 기존 1051의 $6,830 잔존값은 **재확정 시 자동 교정**(확정 경로가 order_amount 를 새로 씀) — 운영 데이터 직접 보정 안 함

## 5. 경계 준수

- WP-01 가드(`projects_delete_logi`)·게이트 산출물 무접촉 — 정책 변경은 §2 대표 판단 대기
- 운영 데이터 무접촉(LIVE 는 읽기 확인만) · WP-04 브랜치 무접촉
