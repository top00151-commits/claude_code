# z1104 배포 결과 — 저장하면 «저장한 그 화면» (LIVE)

- **작성**: 2026-09-16 (수) 16:47 KST
- **앞 기록**: `2026-09-16_1640_z1104_저장후_보던화면_유지_배포대기.md` (원인·설계·시험 전문)
- **요청**: 이새롬 프로 · **대표 승인 «지금 바로»** (2026-09-16 16:4x)

## 배포

| | |
|---|---|
| 모노 main | `448aa471` → **`309abb46`** |
| 라이브 | `d5bbf9f` → **`3ccf810`** |
| 되돌림 지점 | `rollback-20260916-1645` (= `448aa471`) |
| DB 백업 | **없음 — 필요 없음**(화면 파일만 · 서버 코드·스키마 무접촉) |

## 라이브 검증 (네 겹 전부 실측)

1. **기준값을 푸시 전에** 먼저 쟀다 — `d5bbf9f` (15분 캐시를 피해 쿼리 붙여 두 번 확인).
2. **HTTP 200** — 운영 로그 실물 `GET /api/version HTTP/1.1" 200 OK`.
3. **값이 달라짐** — `{"v":"3ccf810"}` ≠ `d5bbf9f`.
4. **원본 대조** — 라이브 커밋 `3ccf810 mirror: claude_code 01_HAIST_WORKS @ 309abb4`
   → 내가 만든 커밋 `309abb46` 이 그대로 올라갔다.

### 배포된 파일을 직접 확인 (컨테이너 실물)

```
app/templates/_v5_partials/chrome.html      knkReloadKeepView  2곳 (정의 + 중복 방어)
app/templates/project_detail.html           knkReloadKeepView  8곳
app/templates/project_detail.html           옛 방식(pathname+'?_t=')  0곳
app/templates/_v5_partials/chrome.html      위치복원 열쇠에서 _t 제외  1곳
```

### 앱 상태

- `knk-works RUNNING` (재기동 직후 uptime 59초) · **오류·트레이스백 0건**
- 실사용 트래픽 정상(회의 상태 200 · 로그인 리다이렉트 303)

## 이제 이렇게 된다

- 작업일정표에서 관리번호 클릭 → 그 수주 카드 → 수정·저장 → **같은 카드 그대로**(`?so=` 유지).
- 저장해도 **보던 화면 위치 그대로**(위치복원 열쇠가 저장 전후 같아짐).
- 조건 없이 들어온 화면은 **지금까지와 똑같다**.

## 남은 일

- ⏭ **이새롬 프로 실사용 확인** — 신고한 그 화면(`011T2606 PBA`)에서 저장해 보기.
- ⏭ 화면이 예전 파일을 물고 있으면 한 번 **Ctrl+F5**(브라우저 캐시). 서버는 이미 새 파일이다.
