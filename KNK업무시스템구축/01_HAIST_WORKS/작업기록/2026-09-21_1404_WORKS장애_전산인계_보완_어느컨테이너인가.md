# [보완] WORKS 장애 전산 인계 — 어느 컨테이너인가 · 정정 1건

- **작성**: 2026-09-21 (월) 14:04 KST
- **받는 분**: 최보현 상무님 (회신 「어떤 컨테이너인지 주체를 모르겠음」에 대한 답)
- **앞 문서**: `2026-09-21_0919_WORKS장애_supervisord기동거부_전산인계.md` — 이 문서가 그것을 **보완·정정**합니다.

---

## 1. 대상 컨테이너 — 이렇게 찾으시면 됩니다

시놀로지 **Container Manager(컨테이너 매니저)** 에서 아래 조건에 맞는 컨테이너입니다. (서버 안에서 직접 확인한 값)

| 확인 항목 | 값 |
| --- | --- |
| **도커 컨테이너 ID** | **`360a56b0aed5`** (전체 `360a56b0aed50896196d25d5e5e606bc2be8682dbf9bd551cf31478d05cdf14a`) |
| 컨테이너 안 hostname | `KNKHAIST` |
| NAS 쪽 연결 폴더 | **`docker/Haist/opt`** → 컨테이너 `/opt` |
| SSH 접속 | `o.knknara.co.kr` 포트 **32201** |
| 안에서 도는 것 | **WORKS**(works.knknara.co.kr · 5051) + **포털 앱들**(PM2 — 출퇴근·차량·검사·제조2·품질·프로젝트이력·홈·게이트웨이) + nginx(8080·6700) |
| 표시 이름(추정) | 2026-06-18 분리 때 기록상 **`KNKHAIST.WORK`** — 단 컨테이너 안 hostname 이 `KNKHAIST` 로 남아 있어, 매니저에는 `KNKHAIST` 로 보일 수도 있습니다. **ID `360a56b0aed5` 로 확인하시는 게 확실합니다.** |

**헷갈리기 쉬운 다른 두 컨테이너** (이번 장애 대상 아님):

| | 메신저(이음) | 메일 |
| --- | --- | --- |
| 이름 | `KNKHAIST.MSG` | `KNKHAIST.MAIL`(기록상) |
| 도커 ID | `6f4e3a38a613` | (접속 권한 없음 — 미확인) |
| NAS 폴더 | `docker/HaistMessenger/opt` | — |
| SSH | 32202 | 32203 |
| 상태 | 정상 (msg.knknara.co.kr 200) | 정상 (kmail.knknara.co.kr 200) |

**주체(담당)**: 이 컨테이너의 **WORKS 앱**은 대표님 쪽(빅터)이 배포·관리합니다. 같은 컨테이너에 **포털 앱(PM2)** 도 함께 들어 있습니다(포털 담당은 제가 확인하지 못했습니다).

## 2. 정정 — 앞 문서 §6 「메일 서비스가 죽어 있습니다」는 틀렸습니다

- 메일은 6월 18일부터 **자기 컨테이너(`KNKHAIST.MAIL`)** 에서 돌고 있고, **지금 정상**(`kmail.knknara.co.kr/healthz` 200)입니다.
- WORKS 컨테이너 안에 있던 `/opt/knk_mail`·`/opt/knk_messenger` 는 **분리 전의 옛 사본**이었습니다.
  메신저도 자기 폴더(`docker/HaistMessenger/opt`)에서 따로 돕니다.
- 즉 **옛 사본을 지운 것 자체는 맞는 정리**였고, **supervisor 설정 두 개를 같이 빼지 않은 것만** 문제였습니다.
- 앞 문서에서 메일을 걱정하시게 만든 점 바로잡습니다.

## 3. 새로 확인된 사실 — 07:31 에 **도커 전체**가 재시작됐습니다

WORKS 컨테이너와 메신저 컨테이너가 **정확히 같은 초(07:31:14)** 에 재시작됐습니다(두 컨테이너의 1번 프로세스 기동 시각이 같음).
→ 컨테이너 하나가 아니라 **Container Manager(도커) 또는 NAS 자체가 재시작**된 것으로 보입니다. 월요일 02:00~07:30 백업 시간대가 끝난 직후입니다.

- 메신저·메일은 부팅 때 스스로 뜨는 장치가 있어 살아났고,
- **WORKS 만** supervisord 가 옛 설정 때문에 기동을 거부해 죽었습니다.

**☞ 전산에서 확인 부탁드릴 것**: DSM **로그 센터**에서 **2026-09-21 07:30 전후**에 무슨 일이 있었는지(패키지 업데이트·Container Manager 재시작·NAS 재부팅·백업 작업 후 재시작 등). 컨테이너 안에서는 이 기록이 보이지 않습니다.

## 4. 해 주실 일 (앞 문서 §5 를 다듬은 것)

> ⚠ 업무시간(특히 08:50~09:30) 피해서. 지금 WORKS 는 정상이라 급하지 않습니다.

**① 옛 설정 두 개 빼기** — 앞으로 재시작할 때 같은 일이 안 나게 하는 근본 조치(메일도 옛 사본이 확인됐으니 둘 다 뺍니다)

```bash
ssh -p 32201 root@o.knknara.co.kr "cd /etc/supervisor/conf.d; mv knk-mail.conf knk-mail.conf.disabled-20260921; mv knk-messenger.conf knk-messenger.conf.disabled-20260921"
```

**② WORKS 를 supervisord 관리로 되돌리기** (지금은 임시로 제가 직접 띄운 프로세스라 죽으면 자동 복구가 안 됩니다)

> 🔴 **supervisord 가 죽어 있는 동안 `cron` 도 멈춰 있습니다**(14:05 확인 — cron 이 supervisord 아래에서 돕니다).
> 그래서 **WORKS 자동 배포(1분마다 새 코드 반영)도 지금 멈춘 상태**입니다. 이 ② 를 하시면 cron 도 함께 되살아납니다.

```bash
ssh -p 32201 root@o.knknara.co.kr "pkill -f '/opt/knk_haist/.venv/bin/uvicorn'"
```

```bash
ssh -p 32201 root@o.knknara.co.kr "supervisord -c /etc/supervisor/supervisord.conf"
```

```bash
ssh -p 32201 root@o.knknara.co.kr "sleep 15; supervisorctl status; curl -s -o /dev/null -w '%{http_code}\n' http://127.0.0.1:5051/api/version"
```

- `knk-works` **RUNNING** + 마지막 줄 **200** 이면 끝입니다.
- `knk-nginx` 가 **FATAL** 로 보이는 것은 **원래 정상**입니다(진짜 nginx 는 supervisor 밖에서 이미 돌고 있음).
  🔴 **이미 떠 있는 nginx(현재 pid 236)는 절대 끄지 마세요** — 포털·차량·출퇴근이 함께 끊깁니다.

**③(권장) 재발 방지**: 컨테이너 설정의 **자동 재시작(restart=always)** 과, 앞서(6월) 요청드린 **DSM 작업 스케줄러 워치독** 등록이 아직이면 함께 부탁드립니다.

## 5. 지금 상태 (14:04)

| 주소 | 상태 |
| --- | --- |
| works.knknara.co.kr | ✅ 200 (08:43 복구 · 임시 프로세스) — ⚠ 자동 복구·자동 배포(cron)는 ② 전까지 멈춤 |
| msg.knknara.co.kr (이음) | ✅ 200 |
| kmail.knknara.co.kr (메일) | ✅ 200 |
