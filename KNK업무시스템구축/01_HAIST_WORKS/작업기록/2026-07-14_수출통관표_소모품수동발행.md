# 2026-07-14 작업기록 — 수출 통관 표 완성 & 소모품 관리번호 수동발행 착수

## ✅ 완료 (LIVE 배포) — 수출 통관 입력 표(export_prep_detail) 일련 개선

| z | 내용 | 모노레포 커밋 | LIVE(미러) |
|---|---|---|---|
| z946 | 수출 서류 워크플로 연결(상세 '📦수출서류' 버튼·출하건도 목록·인보이스 CI 품목명세 `_export_ci_build`+/invoice·xlsx) | 6176042 | 817413a |
| z947 | 251품목 세로 스크롤(`.tbl-wrap` overflow:auto·max-height calc(100vh-290px)) | 2d5bf8b | 2a671a7 |
| z948 | (되돌림) 표준 `.tbl` 룩으로 통일 → z949에서 대표 반려 | 7bb9add | 0b4c325 |
| z949 | 작업일정표식 **엑셀 그리드**+단위(money(v,ccy)·qtyfmt·셀안 $/%/kg) | 585bc28 | 94e0ce0 |
| z950 | 셀 hover/선택색 진하게(hover #dbeafe·클릭 #bfdbfe+2px) | a8eb262 | 913baf0 |
| z951 | 컬럼 폭조절·숨김·순서 + 계정별저장(범용 view_prefs key='export_customs_cols'·POST /export/prep/cols·키[a-z_]검증) | 98482b1 | 629c785 |
| z952 | 수출 통관 목록 **폐기(is_archived) 필터 누락** 수정(001T2607 잔존) | 9c20613 | 29c2b85 |
| z953 | 컬럼 숨김 안되던 문제 — `.ex-grid` 셀 **overflow:hidden 누락** | 436207b | 069e34b |

- **검증**: 각 py_compile·Jinja·(z951)esprima JS 문법·LIVE `/api/version` 폴링(구해시→새해시) 대조.
- **환율**(z945 행별환율 978532e·세션 초반 확인): 005M2511은 상품양식(환율 채운 엑셀) **재업로드**로 반영. 원인=과거 업로드 시 `orders.exchange_rate`/`order_items.cost_krw·sell_krw` 컬럼 부재로 유실→z942 마이그+저장로직으로 해결됨.
- 관련 메모리: [[knk_export_module]] [[feedback_excel_like_data_grid]] [[knk_qty_price_format]] [[knk_project_archive_edit]](z952 교훈: projects_list_logi 안 쓰는 목록 SQL은 is_archived 필터 수동 필수).

## 🔄 진행 중 — 소모품 관리번호 수동발행 (z954 예정·대표 "진행해")

- **배경(대표 지시)**: 소모품 등록(직접·엑셀) 둘 다 관리번호 **자동발급만** → 예전 데이터 이관 시 번호 못 맞춤. 프로젝트 등록엔 z885 수동발행 있으나 소모품엔 없음.
- **현재 상태**:
  - 직접등록: `generate_mgmt_code("C")` 자동만 (consumables.py:656).
  - 엑셀: 관리번호 칸을 **안 읽음** (consumables.py:144 "관리번호·수주번호는 시스템 자동 부여(읽지 않음)"). 양식 안내문(795 "있으면 그 코드 사용")과 실제 불일치.
- **제안(대표 승인)**:
  1. **엑셀 = 채우면 그 번호(검증)/비우면 자동발급** + 업로드 미리보기·결과에 **발급된 관리번호 표시**(신규는 미리 알 필요 없음).
  2. **직접등록 = 프로젝트식 "🔧 관리번호 직접 지정"** 옵션(권한 3명 사번 5·67·306·검증).
  3. **검증 `_validate_manual_mgmt_code`(main.py:25486) 확장**: 현재 `projects` 중복만 검사 → **`consumable_orders`도** 검사 추가(소모품 번호는 거기 저장).
- **재사용 자산**: `_validate_manual_mgmt_code`·`_can_manual_mgmt`·`_expected_mgmt_letter`·`/api/mgmt-code/check`(main.py 25443~25523). 형식=NNN+사업부문자+YYMM·재사용 허용(z940). 소모품=문자 'C'([[biz_div_code_standard]]).
- **미착수(다음 단계)**:
  - [ ] `_validate_manual_mgmt_code`에 consumable_orders 중복 검사 추가(항상 양쪽 검사).
  - [ ] consumables.py 소모품 등록 함수(≈620)에 `manual_mgmt_code` 파라미터 → 있으면 검증된 코드, 없으면 generate.
  - [ ] 소모품 직접등록 폼(consumable_form_upload.html)에 수동발행 UI(권한자만·검증 버튼) — 프로젝트 폼 패턴 참고.
  - [ ] 소모품 엑셀 파서: 관리번호 칸 읽기(144 수정) → 채우면 검증·비우면 자동.
  - [ ] 엑셀 업로드 미리보기/결과에 발급된 관리번호 표시.
- 관련 메모리: [[knk_manual_mgmt_code]] [[knk_consumable_bulk_form]] [[knk_registration_entry_points]].

## 소모품 상세 실무자 4개 요청 (z956·z957) — ✅ LIVE
- **#3 외화(USD) 소수 2자리 (통화 규정 [[knk_qty_price_format]])** — z956: `consumable_detail.html` 금액·합계·tfoot을 `money(v, ccy)` 필터로(=`$1,973.00`·`₩…`), JS 재계산도 `moneyFmt` 헬퍼(USD 2자리·KRW 정수). LIVE 35d3008.
- **#2 소모품 수주번호 첫 건부터 -1** — z957: `consumables.py generate_co_no` 첫 건 `base`(무접미) 반환 제거 → 항상 `C-YYMMDD-N`(첫 건 -1). LIVE 5ca7b12.
- **#4 합계 라인 금액 칸 아래 정렬** — z957: tfoot `colspan=12→10`(18컬럼·금액=11열이라 합계가 금액 밑으로).

## 소모품 고객사 선택 = 검사기 등록식 검색형 선택창 (z958) — ✅ LIVE cd83661
- **배경(대표 지시·실무자 #1)**: 소모품 고객사 선택이 **평범한 드롭다운**(사진1)이라 찾기 불편. 검사기 등 다른 사업부 등록(사진2)은 **시스템명(별칭) 검색형 선택창**이라 편함 → 소모품도 사진2처럼.
- **원인**: 소모품은 공용 검색 위젯을 안 쓰고 열등 UI 2곳.
  - 직접등록 폼: `<input list=datalist>`(정식명만·검색·시스템명 없음).
  - 상세: native `<select data-field=customer_id>`(검색 불가·시스템명 없음) = 사진1 스크린샷.
- **해결**: 검사기 등록이 쓰는 공용 **`KNKCust`** 위젯(`_v5_partials/customer_picker.html`·`data-cust-picker`) 이식 — 시스템명(별칭) 굵게+정식명+종사업장번호+즉시검색+`customer_id` 정확바인딩. 백엔드(`/consumables/create-direct`)는 이미 `customer_id` 수신 → **main.py 변경 없음**.
- **변경 파일(3·templates만)**:
  - `_v5_partials/customer_picker.html`: ①선택 '확정' 시 `change` 발화(setSel·free) — transient clearSel엔 미발화(타이핑 중 저장 방지). ②`data-cust-initial-id`(id 우선 정확 프리셀렉트). **하위호환**(폼 제출용 기존 사용처 무해).
  - `consumable_form_upload.html`: datalist→`data-cust-picker` 교체 + 위젯 include + 직접등록 fetch에 hidden `customer_id`/`customer_name` 전송. (미선택 허용 — 상세에서 선택 가능).
  - `consumable_detail.html`: native select→picker 교체 + hidden `#coCustIdField`(`.ef data-field=customer_id`) + **브리지 스크립트**(picker change→hidden 복사→change 재발화→기존 `.ef` 자동저장·💾수정반영 그대로). 셀 폭 CSS 추가.
- **검증**: 3파일 Jinja 파싱 OK·`customer_picker.html`/브리지/등록 JS esprima OK.
- **커밋/배포**: 90f5324 + b8ad4ab(마무리·상세 .ef 룩). LIVE **cd83661** = 미러 `mirror: … @ b8ad4ab` 대조 확인.
- 관련 메모리: [[knk_customer_subbiz_linking]] [[knk_dept_user_picker_standard]] [[feedback_user_ref_by_id]].

## 소모품 엑셀 업로드 관리번호 직접 지정 (z959·Part 2) — ✅ LIVE 8f930f4
- **배경(대표 지시)**: 예전 소모품 데이터를 엑셀로 올릴 때 옛 관리번호를 맞춰야 함. 양식엔 '관리번호' 칸이 있고 안내문도 *"있으면 그 코드 사용"*인데 **실제론 무시**됨 — 파서는 읽으나([consumables.py:1062]) confirm이 `co_create`에 override 미전달([main.py:37238])→무조건 자동발급. 말·동작 불일치.
- **해결(직접등록 Part1·z954와 동일 규칙)**:
  - 채우면 → 검증(권한 3명 사번 5·67·306·형식 C·중복[projects+consumable_orders]·**배치(한 파일) 내 중복**) 통과분만 그 번호로.
  - 비우면 → 자동발급(현행 유지).
  - 잘못/중복 → **그 발주만 오류로 막음**(자동발급 대체 안 함 = 예전 번호 맞추기 보전).
  - 미리보기: ✓지정 / 자동발급 / ✗(사유는 오류줄). 결과 알림: 발급된 관리번호(수동/자동) 표시(20건↓ 전부·많으면 목록에서).
- **변경(2파일)**:
  - `main.py /consumables/import-bulk`(미리보기): parse 후 관리번호 검증 루프→`_errors`/`_mgmt_auto`/`_mgmt_manual_ok` 주입.
  - `main.py /consumables/import-bulk-confirm`: 루프서 재검증 + `mgmt_code_override` 전달 + 발급번호 회수(`_batch_used_mgmt` 배치중복). `created`에 `mgmt_code`·`manual_mgmt`.
  - `consumable_form_upload.html`: 미리보기 헤더 관리번호 칩 + 결과 알림 발급번호 요약(`acc.issued`).
- **검증**: main.py py_compile · 템플릿 Jinja · 클라 JS(21803자) esprima 통과. 파서가 `_errors=[]` 항상 초기화([consumables.py:1075])라 setdefault 안전.
- **커밋/배포**: a9a57b9. LIVE **8f930f4** = 미러 `mirror: … @ a9a57b9` 대조.
- 관련 메모리: [[knk_manual_mgmt_code]] [[knk_consumable_bulk_form]] [[knk_registration_entry_points]].

## 소모품 라인표 칸숨김 버그 + 단가/금액 통화기호 (z960·사진1) — ✅ LIVE 61c91a2
- **칸 숨김 버그**: '칸 표시/숨기기'로 사진(LOC)을 숨겼는데 옆 사진(PIC)의 📷가 납품일 칸으로 흘러나옴.
  - 원인: `applyView`가 **colgroup col폭(0) + 셀 display:none 을 혼용**. display:none 셀은 컬럼모델에서 빠지는데 colgroup `<col>`은 그대로라, 숨긴 칸 뒤부터 셀↔col 매핑이 한 칸씩 밀림(사진PIC가 0폭이 되고 📷가 납품일로).
  - 해결: **셀은 그대로 두어 컬럼모델 유지**(thead/tbody/tfoot 합계 colspan 정합) + col폭 0 + **숨김칸에만 `.itm-hidden`**(padding·border·overflow 제거)로 접음. ⛔전역 overflow:hidden 금지 — 셀 내 자재/관리번호 검색 드롭다운(position:absolute) 잘림 → display:none 대신 클래스 토글.
- **단가/세금계산서금액 통화 규정(z750/z758)**: 편집 입력칸에 `ccysym` 기호 prefix가 없어 ₩/$ 안 보였음(금액 셀은 money()로 이미 표시). → `<span class="amt-inp"><span class="ccy-pre">{ccysym}</span><input …pricefmt></span>` 패턴(project_detail 표준)+CSS. 숫자는 pricefmt(KRW정수·외화2자리) 유지.
- 파일: consumable_detail.html. 검증: Jinja·칸뷰 JS(esprima) 통과. LIVE **61c91a2** = 미러 @ 0fdcbe4.

## 등록 폼 통화 콤마+공휴일 달력 규정 통일 (z961·사진2) — ✅ LIVE f415780
- **반전(조사 결과·Explore+직접검증)**: '프로젝트' vs '작업일정표'에서 시작한 T/M/L/E 등록 폼은 **이미 동일**(`project_quick_form.html`·POST `/projects/new`·`/project/{id}`) — embed(모달) 여부만 차이. z732 통일 유지. "경로마다 다르다"=작업일정표는 모달(iframe)로 떠서 시각적으로 달라 보인 것.
- **진짜 규정 미적용**: 간편 등록 폼(`project_quick_form`)이 **`knk_inputs`(실시간 콤마)·`knk_datepicker`(공휴일 달력)를 누락** → 단가 콤마 없음·날짜가 밋밋한 브라우저 달력. 정식 폼(`project_form`)엔 둘 다 있음(=화면마다 통화/날짜 UX 달라 보이던 원인).
- **해결(대표 승인: 간편+소모품 폼 모두)**: `project_quick_form.html`·`consumable_form_upload.html`에 두 partial include. 간편 폼은 이미 `knkScanMoney` 가드호출+계산부 콤마제거(`.replace(/,/g,'')`) 설계라 **include만 누락**됐던 것. `knk_datepicker`는 **모든 `input[type=date]` 자동부착**(z438)+MutationObserver(동적행)+전역 1회 가드. 공휴일 데이터=전역(main.py:521).
- 파일: project_quick_form.html·consumable_form_upload.html. 검증: Jinja 파싱·중복 include 0. LIVE **f415780** = 미러 @ e213277.
- 관련 메모리: [[knk_form_input_regs]] [[knk_qty_price_format]] [[knk_calendar_build_standard]] [[knk_registration_entry_points]].

## 등록 폼 진입경로별 잔여 불일치 (사진 비교로 대표 지적) — 소모품 폼만
> ⚠교훈: 앞서 "폼은 embed 여부만 차이"로 결론냈으나, **embed(모달)의 부작용**이 두 개 더 있었음(초기 분석에서 놓침). 소모품 등록 폼(consumable_form_upload)은 project_quick_form과 다른 별도 템플릿이라 공통요소가 어긋나 있었음.

- **z962 — 파일 드롭존 누락 (✅ LIVE 91a159b @ faa5a72)**: 전역 드롭존(`dragdrop.html`)은 **chrome.html:498**이 로드. 소모품 폼은 `{% if not embed %}chrome{% endif %}`라 **모달(작업일정표=embed)에선 chrome 미로드 → '드래그&드롭 파일칸' 안 보임**(전체화면=프로젝트엔 있음). 해결: `{% if embed %}{% include dragdrop.html %}{% endif %}` 직접 추가(비-embed는 chrome이 이미 로드·중복방지). ⚠project_quick_form의 파일칸은 `data-knk-no-dropzone`라 원래 제외 → 프로젝트 폼은 무관. [[works_file_input_dropzone]]
- **z963 — 타이틀 위치(가운데→좌측) (✅ LIVE f53f7b6 @ bb23f8ab)**: `v5_base.css:833` `.page-head{display:flex;justify-content:space-between}`. 검사기 폼은 crumbs+제목을 **한 div로 묶어**(자식1개) 좌측. 소모품 폼은 crumbs·제목·링크를 **직접**(자식3개) → space-between으로 **제목이 가운데로 퍼짐**. 해결: crumbs+제목을 한 div로 묶음(링크는 우측 유지) → 검사기와 동일 좌측정렬.

## 소모품 라인표 헤더 높이 '칸 숨기면 커짐' 의혹 (z964) — 실측: 코드 문제 아님(줌 차이)
- **대표 지적**: 연결관리번호 칸을 숨기면 데이터 헤더행 높이가 커진다(사진1↔2).
- **가설(오답)**: 폭0 칸의 헤더 글자가 세로 줄바꿈돼 행 높이 증가 → nowrap로 수정.
- **실측(독립 HTML 재현 + in-app 브라우저 렌더)**: 같은 표 구조·같은 숨김 로직으로 헤더행 높이 측정 → **표시/숨김 모두 34.5px 동일**. nowrap 없어도(overflow:hidden만으로) 안 커짐. **즉 z960의 overflow:hidden이 이미 세로넘침을 막고 있어 숨김이 높이를 안 바꿈.**
- **진짜 원인**: 두 사진은 **표 위 정보칸(기본/고객/거래 — 칸숨김과 무관)도 사진2가 더 큼** → **브라우저 줌 배율 차이**(전체가 커 보임). 헤더 높이차는 줌.
- **조치**: 그래도 숨김칸이 절대 높이에 영향 못 주도록 `.itm-hidden`에 `white-space:nowrap` **하드닝**(수출 통관표와 동일 접근·크로스브라우저 안전). z964. ⚠교훈: 화면 '이상' 신고는 줌/캐시 가능성도 실측으로 배제.
- 테스트 스크립트: scratchpad/test_colhide.html (표시/숨김 헤더높이 비교·good=nowrap vs bad=nowrap없음, 둘 다 34.5px).

## 통관표 컬럼 여러 개 숨기면 되살아나는 버그 (z965·대표 지적) — ✅ LIVE df913bc
- **증상**: `.ex-grid`(통관 입력표)에서 인보이스단가·인보이스금액·관세 등 여러 컬럼 숨김 체크해제해도 데이터가 그대로 보임.
- **원인(실측 확정)**: `.ex-grid`에 `min-width:1280px`. 컬럼 숨김=폭0인데, **여러 개 숨겨 '남은 보이는 컬럼 폭 합 < 1280'**이 되면 브라우저가 표를 1280으로 억지로 늘리며 **폭0 접은 칸까지 되살림**(table-layout:fixed 분배). 7개 숨기니 남은 합<1280 → 전부 부활.
- **왜 지금까지 안 걸림**: 1~2개만 숨기면 남은 합>1280이라 정상 접힘. 여러 개 숨겨야 재현.
- **해결**: 숨김/폭변경마다 표 폭을 **'보이는 컬럼 폭 합'으로 동적 지정**(`grid.style.width`+`grid.style.minWidth` 덮어써 CSS min-width 무력화). `colDefW()`+`fitTableWidth()` 추가, applyOne 끝에서 호출. 작업일정표·소모품표(table.items)는 원래 `table.style.width=합계`라 무문제 — `.ex-grid`만 min-width 의존이라 버그.
- **검증법(신규 표준)**: 독립 HTML repro로 실제 렌더 폭 측정(in-app 브라우저). 수정 전 숨긴칸 198px(안 접힘)→수정 후 0px. "이상" 신고는 이렇게 **숫자로 실측**해 원인 특정(헤더높이 건=줌 오인, 이건=진짜 버그로 판별).
- 파일: export_prep_detail.html. 커밋 166f7d3b. 관련 메모리: [[feedback_excel_like_data_grid]].

## 베트남 HS코드 사전 — 19개 최종파일 전수분석 (대표 지시 · 구축 승인대기)
- **배경**: 안지연 프로 수출 워크플로(①구매팀 통관품목목록 ②영업팀 Invoice&PL — HS·베트남어명 공란 ③베트남 법인 기재 후 회수 ④신고). 베트남 직원 WORKS 접속 불가(완성 후 허용) → 대표 희망 "빅터가 HS 채우기".
- **분석 대상**: `참고자료\기술영업팀\베트남인보이스 HS코드\` 19개(2025-11-30~2026-05-07, `~$` 잠금파일 제외). 전부 파싱 성공(헤더 자동탐지 — HS열 M/N/O 3가지 변형·2604xx는 Tên hàng 헤더 공란이라 HS+1열 폴백).
- **결과**: 2,961행(HS 100% 기재) · **고유부품 741종 → 고유 HS 82개 수렴** · 형식오류 0 · **충돌 11종(1.5%)** — SQL 튜브류(39174000 vs 74122091)·EW50AAG(85362012 vs 85362011)·ITV1050(84818082 vs 84812090)·MHF2-8D2(84123100 vs 케이블 85444294 오분류 1회) 등 → 안지연 1회 확정 필요.
- **자동 채움 시뮬(시간순)**: 누적 62.1% · 후반부 10개 79.6% · 반복 수출 93~99.6%(251224=99.6%·260121=97.3%·260311/260312=93%대). 0% 파일 = 최초 2건+신규 장비 첫 수출(251215·260401).
- **산출물**: `_HS사전_추출본.csv`(같은 폴더 · 741종 · 베트남어명 91% · 충돌표시 · 최신파일 출처) — DB 시딩용 시드.
- **확정 설계(3겹)**: 🟢사전 일치=자동확정(세관 통과 실적값) → 🟡AI 추정(신규만·'검토 필요') → 안지연 확정 시 사전 자동 등록(다음부턴 🟢). 통관은 법적 신고라 AI 단독 확정 금지 — 최종 확인=사람.
- **다음(승인 시)**: HS 사전 테이블 + 최종파일 업로드 화면(사전 계속 성장) + 통관화면 [🤖 HS 자동 채움] + 베트남어명 열(order_items.description 재사용) + CI/PL 실물양식(헤더블록) + 완제품용 통관품목 업로드. 관련 메모리 [[knk_export_module]].
- 분석 스크립트: scratchpad/hs_full_analysis.py (재실행 가능).
- **안지연 확인 시트 생성**(퇴근으로 내일 확인 예정): `_HS충돌_11종_확인요청_안지연프로.xlsx` — 11종 × 후보별 사용이력(횟수·기간) + 빅터 추천(확실 2·권장 5·재질확인 4) + ✅확정 기입칸(노랑). ⭐검수 중 발견: **26년 1월부터 여러 부품 HS가 일괄 변경**(ITV1050 84818082→84812090·SHF10·SQL피팅 39174000→74122091) = 베트남 측 재분류로 추정 → **사전 원칙 = '다수'가 아니라 '최신 확정값' 우선**(CSV·추천 모두 반영).

## HS 사전 시스템 구축 (z966~z968 · 대표 승인 "A. 지금 시작") — 3커밋 연속 배포
- **z966 — 사전+자동 채움** (a41d84de): ①`export_hs_dict` 테이블(part_key UNIQUE·status CONFIRMED/PENDING/AI·seen_count·이력) + **시드 자동적재** `app/seed/hs_dict_seed.csv`(741종·730 확정·11 PENDING) — ⚠app/data는 .gitignore(`data/`)라 배포 누락 → **app/seed로 회피**. ②통관화면 **[🤖 HS 자동 채움]**: 사전 CONFIRMED=빈 칸만(HS·베트남어명·원산지·세율 숫자해석분) 서버 저장+화면 즉시 반영·PENDING 제외 카운트. ③신규 품목 **AI 추정**(`ai_client.ai_chat`·40개/호출·200 상한): DB에 안 쓰고 **제안만** 반환→노란 행+dirty→사람 검토 후 [전체 저장](자동 확정 금지)·사전엔 status='AI' 기록. ④베트남어 품명 열(order_items.**description** 재사용 — 저장 라우트 기지원). ⑤판매단가→인보이스 **계수%**(실무 ×1.16 규칙). 검증=스크래치 sqlite에 함수 ast 추출·실행(시드 741·멱등·매칭 3케이스).
- **z967 — 사전 관리+업로드** (0230385a): `/export/hs-dict`(상태칩 PENDING/AI/CONFIRMED·검색·**인라인 확정**=자동 채움 입장권·400건 우선순위 표시) + **최종파일 업로드**(여러 개): 19개 전수분석과 동일 헤더탐지 파서(`_hs_parse_invoice_xlsx`) — 신규=CONFIRMED / 같은 HS 재출현=갱신+**AI→CONFIRMED 승격**(실적 확인) / **다른 HS=PENDING 전환+note 충돌기록(몰래 교체 금지)**. 실파일 검증: 260312=188행·260402(Tên hàng 헤더 공란 양식)=699행.
- **z968 — 신고용 실물 양식+서류 정보** (0d3b7cd9): `/export/prep/{pid}/invoice-pl.xlsx` — **실물 Invoice&packinglist_VN과 동일 배치 시트 2장**(INVOICE: B관리번호·D규격·F수량·H단가·I금액 USD·K원산지·M CATEGORY·N HS·O Tên hàng + TOTAL / PACKING: Net·Gross·박스요약 L/M/N). `export_doc_meta`(프로젝트당 1행·upsert) 헤더블록: Invoice No 자동 `KNK-YYMMDD-1`·Consignee(기본=고객사)·Notify(SAME AS ABOVE)·Incheon→Hanoi·Carrier·Sailing·L/C·T/T·INCOTERMS C.I.P — 통관화면 접이식 **'신고 서류 정보'** 카드. Shipper 상수=실물 원문 추출(주소·TEL/FAX·ATTN 안지연 풀텍스트). CI빌더에 description 추가. 기존 내부용 xlsx/인쇄는 불변('잘 되는건 안 건드린다').
- **미검증 정직 고지**: LIVE에서 실제 프로젝트로 [🤖 자동 채움]·AI 추정·invoice-pl.xlsx 다운로드는 실사용 검증 전(코드·스크래치 검증만). 안지연 프로 확인 필요: ①충돌 11종 확정 ②실물 양식 셀 배치 만족도(병합·서식은 근사) ③Net-Weight(데이터 1칸이라 Gross만 채움 — Net 수기).
- 다음 후보(승인 대기): 완제품 수출용 '통관 품목 목록(1번 양식) 업로드'(3단계) · 통관표 CATEGORY 열 · HTML 인쇄 실물화.

## z969~z970 — 용어 수정 + 박스별 무게 (대표 지적 연속 반영)
- **z969 (416d1fa2)**: '계수%'가 어렵다는 대표 지적 → **'가산율%(신고가 올림)'**으로 개명 + 안내를 실제 숫자 예시(4,000원→16% 올려 4,640원→$3.20)로. [[human_friendly_terms]] 규정 위반이었음.
- **z970 (262a6525)**: "부품별 중량은 안 재고 **박스 단위로 계량**" 실무 반영 — 실물 PL도 품목 중량 없음·첫 행 전체 Gross·우측 박스별 G/W(구매팀 BOX시트도 박스당 무게 1개).
  - `export_boxes` 테이블(project_id+box_no PK·저장=프로젝트 전체 교체·무게 없으면 NULL·대소문자 중복 스킵).
  - 통관화면 **📦 '박스별 무게·크기' 접이식 카드**: [표에서 박스 불러오기]=그리드 '박스' 칸 번호 자동 수집(대소문자 무시 중복 제거)→박스마다 무게(kg)·크기만 입력·담긴 품목 수 자동 표시(카드 펼칠 때 갱신).
  - 신고용 Invoice&PL 패킹 시트: **박스 무게 입력분 우선**(우측 요약 L/M/N=export_boxes·첫 행 Gross=박스 합·TOTAL 동일)·없으면 기존 품목별 합산 폴백. 품목별 중량 칸은 유지(안 쓰면 [⚙ 컬럼 설정] 숨김).
  - 검증: 컴파일·Jinja·JS(11,368자)+스크래치 DB(정제 5케이스·합계 457.5) ALL PASS.

## z971 — 부품 추가 업로드·전체 교체: 편집모드 게이트 + 변경 이력 기록 (대표 지적 2건)
- **①증상**: 프로젝트 상세 수주 카드에서 [✏️ 수정]을 안 눌렀는데 [📤 부품 추가 업로드]·[🔄 전체 교체]가 눌림.
  - 원인: 두 버튼(project_detail.html:585-586)이 `{% if _editable %}`(권한)만 통과하면 항상 활성 — 편집모드(`unlocked` 클래스·✏️수정 토글)와 무관.
  - 수정: `knkOwClick` 시작에 가드(카드 `unlocked` 아니면 안내 alert 후 중단) + 잠금 상태 버튼 흐림 CSS(`.so-card:not(.unlocked) .ow-need-edit{opacity:.4}`) + 툴팁에 "[✏️ 수정]을 먼저 누른 뒤 사용" 명시. 다른 버튼(＋부품 추가·💾모두 저장 등)은 무변경(잘 되는건 안 건드림).
- **②증상**: 전체 교체를 실행했는데 '프로젝트 변경 이력'에 안 남음(등록 시 초기값 15건만).
  - 원인: `sales_orders_overwrite_product`(main.py·z740/z943)에 **이력 기록 호출 자체가 없었음**(다른 수정 경로는 전부 `log_project_change` 기록).
  - 수정: 교체/추가 전 부품 개수 캡처(`_old_cnt971`) → 성공 시 이력 1줄: 항목="부품 전체 교체"/"부품 추가 업로드", 변경="부품 N개→M개", note=수주번호·엑셀 파일명·이번 반영 건수·"메타는 엑셀값으로 갱신". 금액은 미기록(개수 중심 — 이력 금액 마스킹 정책과 무관하게 안전). 이력 기록 실패해도 업로드는 성공 처리(try·로그).
- 검증: py_compile·Jinja·knkOwClick 블록 esprima·가드 삽입 확인 ALL PASS. 커밋 5c509995.

## 전역 '처리 중' 피드백 표준 KNKBusy (z972~z973 · 대표 지시·규정)
- **대표 지시(규정)**: 저장·변경이 바로 반영 안 되고 시간 걸리면 "처리 중이니 기다리라"를 **정확히** 표현. WORKS에 많이 빠져 있음 → **전체 제작 시 규정**. (⭐ + 배포 전 확인 규칙: 공유파일은 push 전 대표 확인 — [[feedback_auto_deploy]])
- **z972 (LIVE 90c6b4e)**: `_v5_partials/knk_busy.html` 신규 → chrome.html 1회 include로 전 페이지 `window.KNKBusy`. ①**상단 진행바=모든 fetch 250ms↑ 자동 감지**(monkey-patch·기존/미래 비동기 앰비언트 표시=근본 해결) ②**오버레이**(저장 중…진행률 n/total·클릭 차단) ③**공용 토스트**+flashOnReload(저장→reload 후 완료토스트) ④**KNKBusy.btn** 버튼 busy. project_detail [모두저장]·[수정→저장] 적용(saveAllRows에 onProgress 콜백). **브라우저 렌더 실검증**(오버레이·진행률 3/10·토스트·버튼·fetch감지) ALL PASS.
- **z973 (배포 진행)**: 서브에이전트 스윕 → 11곳(7파일) 적용: schedule_board(일괄출하·세금계산서발행)·consumable_form_upload(엑셀업로드·일괄등록 진행률)·consumable_detail(수정저장·통보)·sales_shipments_receipts(수금)·customers_list(엑셀업로드·일괄등록)·daily(업무보내기)·rnd_detail(과제저장). **내 전수검토**: 순수 피드백 추가(로직/엔드포인트/alert 불변·KNKBusy 없으면 폴백)·오버레이 6곳 전 경로 close/fail(화면차단 위험0)·Jinja+JS 독립 7/7 PASS. 커밋 bbe1361e.
- **⚠공유 인덱스 교훈(재확인)**: 커밋 시 인덱스에 타 세션 스테이징 .md 다수 섞여 있었으나 `git commit -- <내 7파일>`로 격리(7 files만 커밋). 동시 세션 활성 = 배포 게이트·경로한정 커밋 필수.
- **남은 감사 로드맵**: project_quick_form·meeting_form(네이티브 폼·저장실패 버그 주의)·part_form·customer_form·prod_request_edit·sales_opportunity_form 등(대부분 네이티브 폼 submit·페이지이동 직전 오버레이 가능하나 embed/리다이렉트 확인 필요) → 다음 배치. 규정 메모리 [[knk_busy_feedback_standard]].

## 배포 규칙(재확인)
모노레포(claude_code) push → GitHub Actions 미러(knk-haist-works) → NAS. LIVE `/api/version`(git short hash) 폴링으로 반영 확인. `git add` 내 경로만·force-push 금지.
