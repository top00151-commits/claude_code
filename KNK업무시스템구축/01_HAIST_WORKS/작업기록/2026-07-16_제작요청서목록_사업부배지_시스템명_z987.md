# 제작요청서목록 카드 — 소모품 사업부 배지 + 고객사 시스템명 (z987)

- **일자**: 2026-07-16
- **지시**: 김정락 대표 — (목록 캡처와 함께) ①"소모품 카드도 카드 제일 오른쪽 위쪽 '자동화/검사기' 표기하는 곳에 사업부 표기로 — 등록할 때 진행사업부 선택된 걸로 표현(소모품이라고 하지 말고)" ②"고객사명: 시스템명 적용"
- **결정(대표)**: 필터 칩은 불변 — 표기만(검사기 칩에 검사기-소모품 포함 안 함)
- **승인**: 대표 "지금 배포 OK"

## 1. 내용
### ① 소모품 카드 사업부 배지
- 소모품 발주에는 **진행 사업부(biz_div·v5H218 T/M)** 가 이미 있음(신규 등록 모달·양식 업로드 폼에서 필수 선택).
  카드 매핑이 `biz_label="소모품"/biz="C"` 하드코딩이라 표기가 안 됐던 것 → **biz_div 값으로 배지·색 띠 표기**
  (T=검사기 파랑 · M=자동화 보라 · 빈값=기존 '소모품' 앰버). 카드 2행의 '소모품' 종류 칩(po_type)은 유지.
- ⚠발견: **통합 일괄등록(표준양식) 경로는 biz_div를 빈값으로 저장**(main.py '진행사업부 미사용' 주석) —
  6/26 일괄등록분 등은 값이 없어 배지가 '소모품'으로 남음 → 아래 ③으로 채울 수단 제공.
### ② 고객사 = 시스템명
- 프로젝트 카드: `cu.name` → `COALESCE(NULLIF(cu.alias,''), cu.name)` (customers.alias=시스템명·z550).
- 소모품 카드: customers LEFT JOIN 추가 — `alias 우선 → 저장 상호(customer_name) → 정식명` 3단 폴백.
- 검색어도 시스템명 매칭 포함(프로젝트 8→9·소모품 5→6 파라미터) — 화면에 보이는 이름으로 검색되게.
### ③ 소모품 상세 '진행 사업부' 선택칸 (미지정분 채우기)
- 상세 '거래' 줄에 select 추가(미지정(소모품)/🔬 검사기/🤖 자동화) — 기존 `.ef[data-field]` 공용
  자동저장·💾수정반영(save-all)·변경이력에 그대로 연결.
- 서버: `_ORDER_FIELD_LABELS`에 `biz_div: 진행 사업부` 추가 + `co_update_order_field`에 T/M/'' 검증 분기.

## 2. 파일
- `app/main.py` (+20/−8: /prod-requests 두 쿼리+매핑) · `app/consumables.py` (+7: 허용목록·검증) ·
  `app/templates/consumable_detail.html` (+3: select)

## 3. 검증
- py_compile 2파일 + Jinja 2템플릿 ✅ / SQL 픽스처 실행: 시스템명 3단 폴백(alias/저장상호/정식명)·배지 매핑
  (T→검사기·빈→소모품·M→자동화)·시스템명 검색 적중 ✅
- 배포: 커밋 `83f8584a` → 미러 `4873fbf` → LIVE 60초 확인 ✅
- **LIVE 실증(대표 계정·데이터 무변경)**:
  - 배지: 소모품 카드 91건 중 **50건 '검사기' 표기**(biz_div 저장분 — 신규등록 경로)·41건 '소모품'(빈값=통합
    일괄등록분 — 상세에서 채우면 반영). 109C2602 = biz_div T → 카드 '검사기' ✅
  - 시스템명(대표 사진 카드 그대로 대조): 109C2602 "주식회사 에스알씨"→**"에스알씨"** ✅ ·
    008C2603 "DREAMTECH ELECTRONICS INDIA"(3줄)→**"DREAMTECH(IN)"**(1줄) ✅ · "(주)아이마켓코리아"→"아이마켓코리아" ✅
  - 상세: /consumables/114 '거래'줄 진행 사업부 select(미지정(소모품)/🔬 검사기/🤖 자동화·현재값 T 정확) ✅

## 4. 후속(대기)
- 통합 일괄등록 양식에 '진행 사업부' 열 추가 여부 — 대표 논의 후(현재는 상세에서 채우는 방식).

## 5. 관련
- 진행 사업부 원본: v5H218(biz_div T/M·매출 집계) · 카드 사업부 색: z907 · 소모품 카드 병합: z890
- 시스템명: z550(customers.alias)·z647(보드 표시=별칭/저장=정식) — 메모리 `knk_customer_subbiz_linking`
