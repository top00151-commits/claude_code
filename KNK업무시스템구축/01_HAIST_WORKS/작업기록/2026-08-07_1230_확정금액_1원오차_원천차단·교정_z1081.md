# 확정 금액 1원 오차 — 원천 차단(z1081) + 데이터 교정 완료

- **작성**: 2026-08-07 (금) 12:30 KST
- **지시**: 대표 — *"후속예정 이부분 진행해.. 금액이 다른건 급한거야."* (12:1x)
- **상태**: ✅ 완료 — 코드 이중 방어 배포(LIVE `808387f` · 12:18) + 014T2606 확정·예상 금액 정렬 + LIVE 검증
- **변경 파일**: `app/templates/project_form.html`(폼 JS) · `app/main.py`(edit POST)

---

## 1. 원인 — 반올림 평균이 금액 원천을 오염

| 층 | 동작(결함) |
|---|---|
| 수정 폼 JS | 로드 즉시 `평균단가(반올림 표시값) × 수량` 재계산 → 표시·hidden 을 3,741,999 로 덮음 (서버가 내려준 정확값 3,742,000 무시) |
| edit POST | `unit_price × unit_qty` 재계산 → `projects.order_amount` 저장 |
| projects_update_logi | `expected_amount` 미전달 시 order_amount 폴백 → **예상 금액까지 3,741,999** |

08-07 11:40 이새롬 프로 저장(간편 수정)으로 014T2606 확정·예상 모두 3,741,999 — 목록에 "3,742,000 / 예상 3,741,999(-0%)" 병존.

## 2. 수정 (z1081 · 이중 방어)

1. **폼 JS**: 수주 발행(has_orders) 프로젝트는 재계산·덮어쓰기 금지 — 서버 합계값 그대로 표시
2. **서버(edit POST)**: 수주 ≥1건이면 `amt = Σ(orders.total_amount)` 로 확정(폼 계산값 무시) — 편집·확정·수주삭제(z1077) 경로와 같은 단일 원천. expected 폴백도 이 값을 따라 자동 정렬. **수주 0건(제안 단계) = 기존 그대로**(단가×수량 = 예상 매출)
- 시험(임시 DB) PASS 4/0: 실사고 값(3,741,999) 전송 → 3,742,000 확정 · 제안 단계 예상 유지

## 3. 데이터 교정 (014T2606 · 화면 정상 경로)

- 방법: 간편 수정 화면을 실제로 열어 **고객사1 을 picker 로 '드림텍(아산) [0005]' 재선택** 후 [수정 저장] — z1081 로직이 확정·예상을 Σ(수주 합)=3,742,000 으로 정렬
  - ⚠피커 재선택 이유: 폼 프리필이 정식명 "(주) 드림텍"(미매칭 경고)이라 그대로 저장하면 고객 연결(customer_id)이 끊길 위험 — 재선택으로 [0005] 연결 유지
- **검증**: 목록 금액 단일 3,742,000(3,741,999 소멸)·예상 표기 사라짐·고객사 드림텍(아산) 유지·일정표 3행 무손상(z1080 날짜 보존 확인)

## 4. 전수 조사 한계와 판단

- 서버 DB 직접 조회(SSH)는 **비밀번호 대화 입력**이 필요해 이 세션에서 불가 — 다른 프로젝트의 잠재 1원 오차 전수 목록은 못 뽑음
- 다만 ①발생 조건이 좁고(수주 발행 후 [정보 수정] 저장) ②이번 배포로 원천 차단 ③기존 오차도 **그 프로젝트를 다음에 정상 저장하는 순간 자동 정렬**(z1081)됨 — 알려진 실사례(014T2606)는 교정 완료
- 필요 시: 대표 입회(비번 입력)로 읽기 전용 1회 전수 조회 가능 — 지시 시 진행

## 5. 오늘(08-07) 연쇄 작업 정리

z1079(발주처 편집) → z1079b(시스템명 기준) → z1080(날짜 소실 차단+복구) → **z1081(1원 오차 차단+교정)** — 전부 이새롬 프로 실무 흐름에서 드러난 연쇄 결함. 남은 실무자 몫 = 발주처 본사→아산 3건 판단·정정뿐.
