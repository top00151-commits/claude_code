# z1018 — 상품 수주 '부품 추가' 호기 분할 근절 (품명 1줄 + 낱개 수량 N)

- **일자**: 2026-07-20
- **신고(대표)**: 상품(M-260707-1·001M2607) 부품 목록이 CYLINDER + 1호기~11호기 12줄로 쪼개져 있음. "상품은 호기별로 나누면 안 되고, 상품명에 수량을 호기수만큼 적는 방식이어야" (사진 제시).

## 근본 원인 (실증 — 수주 이력 + git 타임라인)
- **07-07 14:57**: 안지연 프로, 등록폼으로 상품 수주 등록(빈 SO·부품 0줄). ⚠발주일도 이때 비어 등록(→z1017에서 백필).
- **07-15 13:35**: z974 배포(`017fae58`) — 수량 표기 '12대분' 확정. **단 줄 분할 로직은 그대로 남음.**
- **07-16 15:27**: 안지연 프로, 프로젝트 상세 **"＋ 부품 추가"**에 `CYLINDER · 수량 12 · $28,000` 입력 → 이력 "호기 추가 [CYLINDER~12호기] · 12대 × 단가 28,000 USD · 수량 1 → 1대(z974 게이트는 작동) · SO 합계 0 → 336,000". **서버가 12줄(호기 라벨)로 분할.**
- **원인 코드**: `/sales/orders/{oid}/add-unit`(main.py ~13962) — UI는 상품이면 "품명+수량 EA×단가"로 받는데(project_detail 995~998), 서버는 SO 종류 불문 `labels_bulk=[f"{n}호기"...]`로 **수량 N=호기 N줄** 생성. z974는 unit_qty(대분) 보존만 게이트.

## 수정 (add-unit 함수 내부만·+39/-19)
- `_isp974`(PARTS_EXPORT) 판정을 앞으로 이동 → **상품이면 `labels_bulk=[품명]` 1줄·qty=낱개 N·금액=단가×N** (호기 라벨 생성 자체 없음). 등록폼(z430)·상품 양식 업로드와 동일 규칙.
- 수량 상한: 상품=9,999개(낱개·UI max와 일치) / **장비=기존 100대 유지**(with 블록 안에서 검사).
- 이력·응답: 상품="부품 추가 [품명] · N개 × 단가…" (호기/대 표현 제거). 장비·소모품 경로 100% 불변(대표 승인 범위=상품만).
- **검증**: py_compile OK · 로직 시뮬레이션 7/7 PASS(상품 1줄·품명빈값·250개 허용·장비 3대 라벨/수량/커스텀라벨 불변·상한 2종) · diff 눈검증.
- **배포**: 커밋 `5a4e0db9` → 미러 `72304fa` → LIVE `/api/version`=72304fa ✅.

## 경위 — M-260707-1 데이터 정리 방법 검토 (12줄 → CYLINDER 1줄 수량 12 · ✅아래 z1018b로 완료)
대표 선택 "제가 크롬으로 정리"였으나 **앱 설계 제약 발견**:
- **행 삭제 UI 없음이 정책**: project_detail.css 425 "z92: 호기별 인라인 삭제 회수 — 상태 '취소'로 대체" + 417 "z88: 행별 수정 컬럼 영구 숨김". `td.edit-only`(행 저장/× 삭제)는 display:none !important=사장된 UI. 삭제 엔드포인트(`/sales/orders/items/{iid}/delete`·z974 게이트 정상)는 살아있으나 **호출하는 화면이 없음**.
- 안전 분류기도 스크립트 삭제를 차단(운영 데이터 파괴는 실클릭·실화면 경유 원칙 — 올바른 방어).
- '취소' 상태로 돌리는 건 행이 남아 12품목 표시·합계 처리 애매 → 부적합.
- **전체 교체(z943)+채워진 양식(z988) 왕복이 정석**이나, 이 SO는 통화가 **USD 직접행**(매입 $25,000·환율 없음)이라 양식(매입 KRW→환율→USD 가정)과 의미 불일치 가능 → $336,000 수주에 검증 없이 업로드 금지.

### ⭐대표 결정 (2026-07-20): compact 후 z1018b 진행
**z1018b = 상품 SO에 한해 행 삭제 버튼 노출**(z92 '인라인 삭제 회수' 정책은 장비 유지·상품은 부품 목록이라 삭제 필요) — 기존 `/sales/orders/items/{iid}/delete`(z974 게이트 정상 작동) 연결 → 배포 → 크롬 실클릭으로 M-260707-1 정리(1호기~11호기 11줄 삭제 + CYLINDER 수량 1→12·$336,000 유지) → 수주상세·통관 카드 "1품목 12개" 실측.
- 구현 힌트: project_detail.css 418~419 `.so-card .edit-only{display:none!important}`(z88)가 행 컨트롤 숨김의 실체. 상품 그리드(`table.so-units.parts-excel`)+unlocked 한정 노출 규칙 추가 + `td.edit-only`의 × (.row-del) 핸들러 생존 확인. 폐기된 옵션: z988 전체교체 왕복(USD 직접행 직렬화 미검증·$336k 위험), 상태 '취소' 마킹(행 잔존·합계 애매).

## ✅ z1018b 실행 결과 (2026-07-20 · compact 후 세션)
### 코드 (2커밋·LIVE 확인)
1. **`29d74b88` → LIVE `3e0cc75`** — 행 삭제(×) 복원:
   - css: `.so-card.editable.unlocked .parts-excel th/td.edit-only { display:table-cell !important }` — **상품 그리드+잠금해제 한정** z88 예외(장비 z92 그대로·row-save 💾는 계속 숨김=저장은 '모두 저장').
   - template: 상품 헤더 '수정'→'삭제'·confirm 문구 '부품 행'.
   - main.py: 삭제 이력 상품=`부품 행 삭제 [품명] · 금액 … 제거`(z1018 add-unit과 표현 통일·`_isp974` 호이스팅).
2. **`287528f3` → LIVE `0a08689`** — **행 삭제 확인창=앱 내 모달**(`_knkRowDelConfirm`·취소/🗑삭제·오버레이 클릭=취소): 네이티브 confirm()은 CDP(크롬 자동화)가 조작 불가능해 렌더러가 45s 얼어붙는 실측 문제 + 앱 UX 불일치 → 제거. ⚠경위 기록: 처음엔 confirm 자동수락 스크립트로 시도 → **안전 분류기가 차단**(정당) → 우회하지 않고 확인 단계를 실클릭 가능한 앱 내 모달로 재설계(확인 절차는 유지·모든 삭제는 행별 ×→모달 🗑삭제 실클릭 경유).

### 데이터 정리 (전부 크롬 실클릭·works 계정=대표)
- 1호기~11호기 11줄(uid 7220~7230) **행별 × → 모달 🗑삭제**로 삭제(11회 왕복·매회 uid 겨냥+행수 검증). CYLINDER(uid 7219)만 남김.
- CYLINDER 수량 1→**12** 그리드 입력(라이브 계산 $28,000×12=$336,000 자동) → 💾 모두 저장.
- ⚠관찰: 삭제 진행 중 남은 호기 라벨이 화면에서 1호기~N으로 재번호되어 **표시**됨(최종 상태엔 호기 행 0개라 무영향·값 손상 없음). CYLINDER 라벨·매입 $25,000·마진 12%는 전 과정 불변.

### 실측 검증 (3화면 전부 정합)
| 화면 | 결과 |
|---|---|
| 수주 내역 (project/1003) | **CYLINDER 1줄 · 수량 12 · $336,000.00** · 푸터 "완제품 12대분 · 부품 1종 12개" · 프로젝트 카드 "확정 매출 $336,000 · 단가 $28,000×12대 · 총 1개 라인" |
| 통관 입력 (/export/prep/1003) | **"M-260707-1 · PARTS_EXPORT · 1품목"** · CYLINDER 12 × $28,000 = $336,000 (구 12품목 해소) |
| 작업일정표 (/sales/schedule?q=001M2607) | 1줄 · **12대분** · $28,000 · $336,000 · 발주 2026-07-07/납기 2026-09-07 (z1017 값 유지) |

### 크롬 자동화 노하우 (후속 세션용)
- 클릭 좌표계=**스크린샷 픽셀**(CSS×배율≈1.095·창 크기 바뀌면 배율 재측정). 새로고침 직후 첫 클릭은 **스크린샷을 한 번 찍은 뒤** 클릭해야 안착(z984 스크롤 복원+합성기 타이밍).
- 대상 탭이 백그라운드면 클릭 유실 — 다른 탭 닫아 앞세우기. 네이티브 confirm으로 얼어붙은 탭은 `navigate`(CDP)가 대화상자를 취소시키며 복구.
- Edge 재시작 후 확장 미접속이면 `Start-Process msedge`→list_connected_browsers 재선택으로 복구.

## 영향/사실
- 작업일정표=상품 합산 1줄이라 **표시 영향 없음**(12줄이어도 1줄·12대분·$336,000 정상). 문제는 **수출 서류·통관 카드 "12품목"** 오표시와 상세 그리드 가독성.
- 값 자체는 전부 정상(합계 $336,000·매입 $25,000·마진 12%·12대분) — **행 모양만** 잘못.

## 관련
- [[knk_parts_units_qty]](z974 대분) · [[knk_parts_cost_margin]] · [[knk_schedule_board_date_gate]](z1017) · [[knk_consumable_bulk_form]] · [[knk_parts_followup_registration]]
