# 추가 발주(제품) 표시수량 누락 수정 — 일정표 2줄 갈라짐 (z1088b)

- **작성**: 2026-08-20 (수) 12:21 KST
- **신고**: 이새롬 프로 (대표 경유 2026-08-20) — *"추가 건인데 등록하면 (작업일정표에) 2건으로 나눠서 나옵니다. 신규 등록일 경우에는 나눠지지 않고 제대로 등록 됩니다."* + 대표 *"완료한 후에 직접 검증을 해서 결과 보고해"*
- **상태**: ✅ 원인 실측 확정 · 수정 · 시험 49/0
- **변경 파일**: `app/main.py` 만

---

## 1. 원인 (운영 DB 읽기 전용 실측 — 027T2602)

| 수주 | 등록 시각(created_at) | 경로 | board_qty | 일정표 |
|---|---|---|---|---|
| T-260202-3 (신규) | 08-20 11:07:56 | 신규 등록(z1050) | **1 EA 저장됨** | 한 줄 "1 EA" ✓ |
| T-260408-8 (추가) | 08-20 11:47:16 | **추가 발주(z164/z1084)** | **NULL (버려짐)** | 품명별 2줄 ✗ |

- z1088(어제)이 표시수량 저장을 **신규 등록 경로에만** 붙였고, 같은 폼을 쓰는 **추가 발주 경로**(add_followup_order)는 빠뜨림 → 칸은 보이는데 값이 조용히 버려짐
- 이새롬 프로가 오늘 오전 두 건 다 새로 등록하며 정확히 이 차이를 재현·신고

## 2. 수정 (2곳)

1. **추가 발주 경로**: `add_followup_order` 반환의 `order_id`에 board_qty/board_unit 저장 (제품+표시수량 입력 시)
2. **빠른 폼 수정 동기화**: 'SO 1건일 때만' → **최신 SO(id DESC)** — 납품위치(z354)·수정화면 GET 표시와 같은 규칙. 기존 규칙으로는 수주 2건인 027T2602를 수정으로도 교정 불가(화면엔 값이 보이는데 저장 무시 — 같은 유형의 구멍)였음

## 3. 검증

- 시험 **49/0** (기존 z1088 37건 전부 유지 + 신규 12건): 추가 발주 5 SET 저장→일정표 한 줄 "5 SET" · 표시수량 없이 추가 발주=기존 품명별 2줄(신고 재현·회귀 0) · **다중 SO 프로젝트 빠른 폼 수정→최신 SO 반영→갈라졌던 줄이 한 줄 "7 EA"**(027T2602 교정 경로 그대로)
- check_standards 전부 통과

## 4. 걸린 실건(027T2602) 교정 방법 — 실무자 화면 경로

프로젝트 **수정(빠른 폼)** → 작업일정표 수량·단위 입력 → 저장 = **최신 수주(추가 건 T-260408-8)**에 반영 → 일정표 한 줄. (⛔서버에서 데이터 직접 수정 안 함 — 교정은 화면 경로 원칙)
