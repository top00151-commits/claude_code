# 디자인 통일 Phase 4 ④(2차) — 재고 폼(출고·조정) knk 전환 (z1005)

- **일자**: 2026-07-18
- **배경**: 대표 "나머지 진행하자"(compact 후) — Phase 4 ④ 재고의 **입력 폼** 2개.
  z1004에서 재고 목록 3화면(잔고·이동·입고이력)을 마쳤고, 이번은 실제 입력이 이뤄지는 **JS 큰 폼**.

## 1. 대상 실체 확정 (라우트로 검증)

| 파일 | 라우트 | 성격 | 이번 전환 |
|---|---|---|---|
| `stock_issue.html` | GET → POST `/stock/issue` | **출고 등록 폼** | ✅ 대상 |
| `stock_adjust.html` | GET → POST `/stock/adjust` | **재고 조정 폼**(±부호) | ✅ 대상 |
| `stock_issues.html`(복수) | `/stock/issues` | 출고 **이력 목록**(issues_out) | ✗ 별개(추후) |
| `stock_adjustment.html` | 조정 감사 | 조정 **감사 목록**(audit_attachments) | ✗ 별개(추후) |

- 단수(_issue/_adjust)=폼, 복수/‑ment=목록·감사. 헷갈리기 쉬워 **main.py `ctx(..., "..._..html")` 렌더 지점으로 확정**.

## 2. 전환 원칙 — po_receive 판단 (셸만 knk, 폼 본문 유지+브리지)

두 폼 모두 **자재 선택 → 실시간 미리보기**가 JS의 생명선이라, z1003 입고(po_receive)와 **동일 판단**:
머리글·제출 바·버튼만 knk로 바꾸고 **:root pq→knk Sage 브리지**로 폼 전체 색을 통일. **JS 0줄 수정.**

| 전환(knk) | 유지(업무 특화·색만 Sage 브리지) |
|---|---|
| 머리글 → knk-page-head + Tabler(이력 `ti-history`·재고현황 `ti-packages`) | 자재 선택 select + **part-info 카드**(JS `innerHTML`에 `.u` span) |
| 하단 액션 → knk-submit-bar(sticky) | **실시간 미리보기**(preview-card·pv*) |
| 버튼 → knk-btn(ghost/primary) | 출고: `.big.mono` 수량 입력 + `.over` 토글 |
| :root → pq→knk Sage 브리지 | 조정: **부호 토글**(sign-toggle ±·setSign)·`qtyHidden`(부호적용 제출값) |
| `.field input:focus` → Sage 포커스 `#3E6F89` | 사이드(요약·가이드·⚠주의) |

## 3. ⭐JS를 건드리지 않은 이유와 증명

- 두 폼의 `onPartChange`·`onQtyChange`(+조정의 `setSign`)가 **innerHTML로 KPI/미리보기를 다시 씀**:
  `piStock.innerHTML = fmt(stock) + '<span class="u">' + unit + '</span>'` — `.u` span·`.big`·`.over/.pos/.neg`·
  `#partSelect`/`#qtyInput`/`#previewCard`/`#pvAfter` 클래스·ID에 의존. JS 문자열 안에 `var(--pq-err/ok/warn)`
  색을 직접 삽입 → **KPI/미리보기를 knk 변형으로 바꾸면 `.u`가 깨짐** → 유지가 정답(입고 KPI와 같은 근거).
- **증명(검증 수치)**: `onPartChange/onQtyChange`·`#partSelect`·`part-info`·`'<span class="u">'` 템플릿·
  (조정)`sign-toggle/setSign/qtyHidden/signPlus/signMinus`·(출고)`priceInput/submitBtn/previewCard/pvAfter`
  전부 보존 확인. `:root` 브리지가 JS 인라인 `--pq-*` 색까지 자동 Sage화.

## 4. 세부

- **제목 이모지 제거**(`📤`/`⚙️`) — 형제 재고 화면(z1004)·po_form과 통일된 **깔끔한 knk-page-title**. 아이콘은 Tabler 버튼이 담당.
- **죽은 CSS 정리**: 머리글/액션바를 knk로 바꾼 뒤 `.si-head`/`.adj-head`/`.si-title`/`.action-bar`/`.btn` 군 삭제
  → `.si-page > .knk-page-head` 배경 보정 + `.knk-submit-bar__note b` 1줄만 남김(가이드 §6 "미사용 선택자" 선제 정리).
- `.field` 입력 래퍼는 **유지**(특수 입력 `.big`/sign-toggle이 JS 토글과 얽혀 있어) — 브리지로 Sage 색만.

## 5. 검증 (배포 전 60여 항목 전부 통과)

- Jinja parse ✅ · **esprima(양쪽 script 전체)** ✅ · `<style>` 중괄호(60/60·68/68) ✅ ·
  div 여닫(63/63·47/47) · span 여닫(30/30·25/25) ✅
- 옛 클래스 잔재 0: `si-head`/`adj-head`/`si-title`/`adj-title`/`si-actions`/`adj-actions`/`action-bar`/`.btn` ✅
- knk 전환: page-head·breadcrumb·page-title·page-meta·page-actions 각 1 · submit-bar(sticky) 1 · 버튼 4 ✅
- :root Sage 브리지(surface/danger)·Sage 포커스 이식 ✅ · JS 생명선 보존(위 §3) ✅
- Tabler 글리프 `ti-history`·`ti-packages` 실존 ✅
- ⚠자가수정: 검증 스크립트의 `knk-btn` 기대 카운트 오류(버튼4×2 + CSS주석1 = 9). **전환 자체는 정확**(`class="knk-btn "`=4).
- 배포: `ce52f208`(2파일 +74/−96, 죽은 CSS 정리로 순감) → 목표 해시 감시

## 6. LIVE 실증 (LIVE `463eab5` = 미러 목표 해시 · 대표 Chrome JS 프로브)

- **출고 `/stock/issue`**: 제목 "자재 출고 등록"(이모지 제거)·knk-page-head·breadcrumb·Tabler(history/packages)·버튼 4·
  submit-bar(sticky)·옛 클래스(si-head/action-bar/btn) 잔재 0 ✅ / 색: body `#F3F5F2`·머리글 `#FAFBF9`·
  `--pq-surface→#FAFBF9`·`--pq-err→#A33A3E`·`--knk-focus #3E6F89` ✅ / **JS 생명선**: 자재 3,195개 중 실선택 →
  part-info 표시 + `piStock="0<span class="u">EA</span>"`(**`.u` innerHTML 재작성 정상**) → 수량 3 → 미리보기 표시·
  "재고 부족" 경고·금액 **₩210,000** 계산 정상 ✅
- **조정 `/stock/adjust`**: 제목 "재고 조정"·머리글·Tabler·버튼 4·submit-bar·잔재 0·Sage 색 동일 ✅ /
  **부호토글 핵심 실증**: 자재 선택→part-info(`.u`) → 수량 5 → `qtyHidden="5"` → `setSign(-1)` → `qtyHidden="-5"`
  (**부호 적용 제출값 정확**)·`neg` 클래스·pvAfter "재고 음수 — 조정 불가" 경고·sumSign "－ 감소" ✅
- 결론: 두 폼 디자인(Sage)·구조·JS 전부 LIVE 완벽 동작.

## 6-1. ⚠미러 실행 누락 → 재트리거 (운영 교훈)

- **증상**: `ce52f208`(z1005 템플릿) push 후 **미러(배포 리포) HEAD가 20분+ `512089f` 정지** — LIVE 반영 안 됨.
  push 트리거 조건은 충족(경로 `01_HAIST_WORKS/**` 일치·원격 main 반영)·NAS cron 정상(세션 중 LIVE 실제 전진).
  → GitHub Actions 미러 실행이 **일시 누락**(드롭). gh 미인증이라 Actions 로그 직접 조회 불가.
- **대응(대표 승인 "재트리거")**: ⭐**진짜 빈 커밋은 무효** — 변경 파일 0이라 미러 워크플로 경로 필터(`paths: 01_HAIST_WORKS/**`)에
  안 걸림. 대신 **경로 안의 문서(작업기록·현황판) 커밋**(`67c06eed`)으로 재발화 → 미러는 **전체 WORKS 서브트리 export**라
  이 push로 z1005 템플릿까지 함께 실려 배포. **결과: 미러 `463eab5` 즉시 생성 → LIVE `463eab5` 도달.** 토큰 사망 아님(드롭).
- **교훈**: ①미러가 안 움직이면 '토큰 사망'으로 단정 말고 **경로-터치 커밋으로 재트리거** 먼저(드롭이면 해결).
  ②재트리거는 **반드시 경로 필터에 걸리는 파일 변경**(빈 커밋 ✗). ③동시세션 인덱스 레이스로 `작업기록/`에 다른 삭제분이
  스테이징돼 있어도 **`git commit -- <경로>` 경로 한정**이면 내 2파일만 커밋(실증: `67c06eed`=2파일 70+/3−).

## 7. 관련 / 다음

- z1003(입고 = 같은 판단)·z1004(재고 목록) · [[knk_design_guide_unified]] · [[knk_works_css_palette]]
- **다음**: Phase 4 ⑤ **BOM**(`bom_upload.html`·`bom_upload_report.html`) → 잔여 재고 화면(audit/qc/reorder/safety/abc/fifo/turnover·출고이력·조정감사 목록)
