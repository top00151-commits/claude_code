# 작업기록 2026-06-13 — KNK 메일 전기능 구축 + 달력 표준화 + 메일 앱(독립 창) 모드 (종합)

> 작성: 빅터(Claude) / 세션: HAIST WORKS 빅터(01) / 시스템: `01_HAIST_WORKS`
> 모든 변경은 **2-repo 배포**(knk-haist-works push → NAS 1분 cron 반영) + 모노레포 백업. 운영확인 `/api/version`.
> 검증은 **운영과 동일한 코드를 로컬 임시 SQLite로 호출**(실메일·운영DB 무접촉) + esprima(JS)·Jinja·Python 문법.
> ⚠ z번호는 동시작업 세션과 충돌해 **실제 식별은 커밋 SHA**.

---

## 0. 한 줄 요약
자체 메일(15_KNK_Mail)의 **POC를 '실무용 메일 클라이언트'급으로 완성** — 받기·보내기·AI·원본 서식·서식편집기(이미지/표)·대용량첨부·아웃룩 편의기능·**독립 창(앱) 모드**까지. 더불어 **WORKS 전 달력을 빌드 표준으로 통일**.

---

## 1. 메일 받기/보내기 검증 + 잠복버그 2건 수정  [배포 3f8c4c7]
대표 지시 "여러 상황 가정해 받기/보내기 테스트". 운영 코드로 52종 시나리오 자동검증 → 버그 2건 확증·수정.
- **A. 한글/베트남어 본문 깨짐**: `parse_raw_email` 이 `message_from_string`(문자열) 사용 → 인코딩 안 된 8bit 본문이 raw-unicode-escape 로 깨짐. **→ `message_from_bytes`(바이트)**. base64/qp(지메일류)는 원래 정상이라 안 드러났던 잠복버그.
- **B. `default_owner` 가 없는 컬럼 `username='kjr'` 조회**(실제 `login_id`) → try/except로 가려져 이름폴백(z383 동명계정 오라우팅 원인 일부). **→ `login_id='kjr'`.**
- 회귀테스트 `15_KNK_Mail/_검증/mail_pipeline_test.py` (52/52). 상세: `15_KNK_Mail/작업기록/작업기록_2026-06-13_*.md`.

## 2. 받은 메일 '원본처럼 보기'(HTML 서식·서명 이미지·첨부)  [f3970cf → d36fb34 → f09dd91]
대표 발견 "서명 로고·서식이 원본과 다르게(텍스트만) 보임".
- 첨부를 **바이트·content_id·인라인여부**까지 추출 → `mail_attachments`(data BLOB) 저장.
- **sanitize_html_for_view**: 위험 태그·on*·javascript: 제거 + `cid:`→`/mail/{id}/att/{aid}` 치환. `<style>` 보존(아웃룩 서명 줄간격).
- mail_view: 원본 HTML 을 **sandbox iframe**(스크립트 불가)로 격리 렌더 + 텍스트 토글 + 첨부칩.
- 진단 `GET /mail/{id}/_diag`(본인 메일)로 "이미지 안뜸" 원인=구메일 미저장 즉시 확정.
- **견고화**(대표 "동일 증상 문제없게"): 누락 이미지→깔끔한 자리표시(🖼)·원격이미지 보존·지메일식·텍스트전용. 검증 33/33.

## 3. KNK 대용량 첨부  [b574e0e]  (정책: 30일·링크만)
- 메일쓰기 '대용량 첨부' → `POST /mail/large/upload` 로 NAS `data/mail_large/{token}` 스트리밍 업로드(개당 ≤500MB) → 본문에 다운로드 링크 블록.
- `GET /dl/{token}` **공개**(외부 수신자도)·**30일 만료·다운로드 카운트**. 정적 비공개 경로 보관→/dl 라우트로만 서빙(만료 우회 차단). 검증 20/20.

## 4. 메일 편의기능 4종  [52093ad]
- 🔍 **검색**(받은/보낸함 보낸사람·제목·본문 LIKE) · ⭐ **정리 필터**(별표만/안읽음만) · ✍️ **서명**(1인1개, 쓰기/답장/전달 시 자동삽입) · 📇 **받는사람 자동완성**(전사 연락처 shared_contacts) · ↪ **전달**. 검증 20/20.

## 5. 아웃룩 편의기능 4종  [fa06768]
- 📝 **임시저장(Drafts)**(direction='draft', 임시보관함, 발송 시 자동삭제) · ↩↩ **전체 답장** · 🙈 **숨은참조(BCC)**(send_message 가 Bcc헤더 제거) · 👁 **읽음/안읽음 수동표시** · ✒️ **서식 본문(HTML)**(contenteditable+툴바, multipart/alternative 발송, 발신 전 sanitize). mail_messages.bcc 컬럼+마이그레이션. 검증 20/20.

## 6. 첨부 UI 정리(자동 분배)  [ce215ca → c8bbbae]
- 큰 드롭존 2개 → **「📎 첨부」 클릭 메뉴**(파일 선택 / 드래그 영역). 전역 dragdrop 래핑은 `data-knk-no-dropzone`로 제외(메모리 [[works-file-input-dropzone]]).
- **10MB↑ 파일 자동 대용량(NAS 링크)** 분배. 칩 목록(📎일반/🔗대용량).

## 7. '✨ AI로 정리'  [f356702]
- 본문에 요점만 적고 누르면 `POST /api/mail/ai-compose`(ai_client.ai_chat)가 정중한 업무메일 {subject,body}로 다듬음. **메모에 없는 사실 지어내기 금지·서명 미삽입·원문 언어 유지.** 서명/원본인용 구분선 앞부분만 정리, 제목 비면 AI제안, ↩되돌리기.

## 8. 본문 이미지 편집  [b938cc4 → 71d2fe1]
- 이미지 클릭 → 도구바: 작게/중간/크게·−/＋·정렬·↻회전(canvas)·✂자르기(canvas 모달)·삭제.
- **회전/자르기 시 조정한 크기 유지**(width 초기화 버그 수정). 회전/자르기는 붙여넣은(data URL) 이미지만(외부 http=canvas taint).
- ⚠ 미해결: 본문 붙여넣은 data URL 이미지는 수신측(지메일 등) 차단 가능 → '인라인 CID 첨부 변환' 후속 필요(대표 안내·대기).

## 9. 엑셀/표 붙여넣기 + 표 편집  [496dab2 → bb64c27]
- 엑셀 영역 붙여넣기 감지(TSV/`<table>`) → **'📊 표로 / 📝 텍스트로' 선택 팝업**. 표로=인라인 테두리 HTML 표(발송 sanitize 통과·보낸함 렌더).
- **표 편집 메뉴**(셀 클릭): 행/열 추가·삭제 · 🔗병합/해제(Shift+클릭 범위) · 헤더행 · 🎨채움색·✏️글자색 · 테두리(실선/굵게/점선/없음+선색) · 표삭제. 셀 직접 편집.

## 10. 메일 '앱(독립 창)' 모드  [a99aeb0]
대표 방향 "WORKS 사이드바 말고 KNK Eum 아이콘으로 별도 실행"(메신저 정의서 '메일 관문' 비전 일치).
- `ctx()`: `?app=1`→세션 `mail_app=True`(내부 이동 유지)·`?app=0` 해제.
- mail_app 시 chrome(사이드바) 대신 슬림 헤더(`_v5_partials/mail_app_head.html`)+본문 전체폭. 적용=받은편지함/보기/쓰기/서명. ⤢ WORKS 전체화면으로 복귀.
- **KNK Eum은 `works.knknara.co.kr/mail/inbox?app=1`을 새 창으로 열면 됨.**
- ⚠ **후속(메신저 세션 조율)**: ①**SSO 자동로그인**(재로그인 없이) ②KNK Eum 📧 메일 아이콘.

## 11. 달력 빌드 표준 일괄 적용  [7f56d84]  (대표 지시)
대표 지시 "달력은 사양 .md와 같이 만들어 적용"(절대준수) + "지금 적용된 달력들 모두 변경". 사양: `작업기록/달력_캘린더_빌드_조건_사양_2026-06-13.md`.
- Explore 감사 → ①수주 납기 달력=이미 기준(무변경). ②/calendar 일일: 오늘 호박테두리 `#d4a574`·다른달 opacity 0.5→0.25. ③데이트픽커: 오늘 `#f59e0b`→`#d4a574`·opacity .25. ④수주 미니: 오늘색·opacity·**공휴일 K/V 배지 신규**·날짜숫자 일/공휴일 빨강·토 파랑·D-day 색 기준값 정정(overdue/past 분리).
- 백엔드 무변경(_build_month 데이터 기존 제공). 사양 §9 변경이력에 기록.

---

## 12. 배포·검증 이력 (SHA = 실제 식별자)
| # | 작업 | 배포 SHA(knk-haist-works) | 검증 |
|---|---|---|---|
| 1 | 받기 본문 깨짐 수정 | 3f8c4c7 | 52/52 |
| 2 | 원본 HTML 렌더 | f3970cf | 22/22 |
| 2 | 줄간격·진단 | d36fb34 | — |
| 2 | 견고화 | f09dd91 | 33/33 |
| 3 | 대용량 첨부 | b574e0e | 20/20 |
| 4 | 편의기능 4종 | 52093ad | 20/20 |
| 5 | 아웃룩 4종 | fa06768 | 20/20 |
| 6 | 첨부 UI 정리·메뉴 | ce215ca·c8bbbae | — |
| 7 | AI로 정리 | f356702 | — |
| 8 | 이미지 편집·크기유지 | b938cc4·71d2fe1 | esprima |
| 9 | 엑셀/표·표편집강화 | 496dab2·bb64c27 | esprima |
| 11 | 달력 표준 일괄 | 7f56d84 | Jinja |
| 10 | 메일 앱(독립 창) | a99aeb0 | py+Jinja |

회귀 테스트 5종 보존: `15_KNK_Mail/_검증/mail_{pipeline,html_render,large,features,outlook}_test.py` (52+33+20+20+20=145).

## 13. 메모리/규칙 갱신
- [[project-knk-mail]] 전 기능 반영 · [[works-file-input-dropzone]](전역 드롭존 함정) 신규 · [[feedback-no-rework-working]](잘 되는 건 안 건드린다) 신규 · [[knk-calendar-build-standard]] 절대준수 강화.

## 14. 남은 일 / 후속
- **메일 진입점=KNK Eum 아이콘**: ①SSO 자동로그인(메신저 세션 조율) ②KNK Eum 📧 아이콘.
- 본문 붙여넣은 이미지 **인라인 CID 첨부 변환**(수신측 안정 표시).
- 잔손질(대표가 보류 결정): 중복 Cloudflare API 토큰·Worker 진단로그 — **기능 무관 미관이라 안 해도 됨**([[feedback-no-rework-working]]).
- 정의서 §12 로드맵: POC 골격 완성 → Phase 0 베트남 5명 pilot(선행: #6 데이터안전 결재 + 전산질문지 회신).
