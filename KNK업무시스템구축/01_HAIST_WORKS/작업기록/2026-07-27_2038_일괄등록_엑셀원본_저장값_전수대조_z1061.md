# 일괄등록 엑셀 원본 ↔ LIVE 저장값 전수 대조 (z1061) — compact 전 정리

- **작성**: 2026-07-27 (월) 20:38 KST
- **신고**: 대표 — *"사진1(업로드 엑셀)과 사진2(작업일정표)가 다르게 올라갔어.. 왜?"* → *"전수대조리포트부터"*
- **상태**: **진단·전수대조 완료 · 리포트 발송 · ⏳고침 2건 미착수(대표 결정 대기)**

---

## 0. 이 세션 전체 작업 지도 (compact 대비 · 각 상세 .md 있음)

| z | 무엇 | LIVE | 상세 .md |
|---|---|---|---|
| z1051/z1052 | 소모품 매출 117억 = 통화 오등록 2건(96.8%)·진단화면·재발방지 | `8615f4f` | `2026-07-24_소모품매출_통화오등록_z1051_z1052.md` |
| z1058 | 형태 수주별 표시(작업일정표 행 + 상세헤더 '혼합') · 공용 so_form_label_key() | `ebe260c` | `2026-07-27_1529_형태_수주별표시_일정표행_상세헤더혼합_z1058.md` |
| z1059 | 수동 관리번호 검증 '어디서 쓰는지'+링크(001T2607=숨김보관 #1002) | `db48435` | `2026-07-27_1630_수동관리번호_검증_어디서쓰는지_z1059.md` |
| z1060/z1060b | 완전삭제 운영잠금 좁힘 — **실거래0** 이면 허용(is_archived 요구 제거) | `11986c5` | `2026-07-27_1906_완전삭제_운영잠금_숨김보관실거래0만허용_z1060.md` |
| **z1061/b/c** | **SO 저장값 전수 덤프 + 일괄등록 대조** (이 문서) | `0e1c155` | 이 파일 |

> 현황판 §4 규칙: z1058=**42**, z1059=**43**, z1060=**44** 등재됨. z1061은 아직(고침 확정 후 등재).
> ⚠ 작업번호 레이스: 병렬 세션(ERP WP-03/04)이 z1053~z1057 선점 → 내 형태작업이 z1053→z1055→**z1058** 두 번 밀림. 커밋 전 `git ls-remote`+현황판 최신번호 확인 필수.

---

## 1. 신고 — 무엇이 달라 보였나 (005T2601)

대표가 업로드한 엑셀(사진1)과 작업일정표(사진2)를 대조하니 금액·기타사항·형태·발주일·통화는 맞는데 **모델명·장비명·(제품)수량**이 어긋나 보였다.

## 2. 대표의 핵심 통찰 (정확)

*"엑셀 업로드 후 한번더 비교하는 검증 단계를 넣은걸로 아는데, 이 자료는 그 규정 전에 올린거라.."*
→ 맞다. 이 배치는 **z981(수주별 모델·장비 저장·표시)·z982(재업로드 중복차단)·z1025(원본 셀 대조) 도입 전**에 올린 것.

## 3. 원본 파일

`C:\Users\top00\Downloads\KNK_프로젝트_일괄등록_검사기_260707.xlsx`
- 시트 '일괄등록' · **644 데이터행 · 248 고유 관리번호**(검사기 T). 005T2601도 이 안에 있음(14행).
- 헤더(1행): 관리번호·수주번호·프로젝트명·모델명·장비명·기타사항·수량·단가·금액·통화·거래구분·PO유형·형태·고객사1·2·부서·담당자·연락처·납품위치·발주일.

## 4. 대조 방법 (z1061 — ⛔LIVE DB 직접 조회 금지 준수)

작업일정표 「엑셀로 저장」 export 는 **현재 월만** 뽑아 전수 대조 불가 → **읽기전용 덤프 엔드포인트** 신설.

- **`GET /admin/so-audit-dump?like=T[&dl=1]`** (main.py · admin/ceo · 전부 SELECT):
  프로젝트 SO별 저장값 JSON — 관리번호·수주번호·발주일·**model/equip(화면 표시값=SO값 우선, 없으면 프로젝트 대표 폴백)·so_model/so_equip(SO 자체값·빈값 판별)**·so_form·total·통화·**qty=SUM(order_items.qty)**·oi_n·notes(=line_note/unit_label).
  `dl=1` → `Content-Disposition: attachment`(get_page_text 5만자 잘림 회피).
- 크롬(대표 세션)으로 `dl=1` 다운로드 → `C:\Users\top00\Downloads\so_audit_dump.json`(632 SO·160KB).
- 로컬 Python 으로 엑셀 644행 ↔ 덤프 632 SO 대조(관리번호 안에서 기타사항→금액 매칭). 리포트 엑셀 생성.

**배포**: z1061 `c5fa90e` → z1061b(orders에 없는 is_export/po_type 칸 제거 `OperationalError`) `8390e2e` → z1061c(dl 다운로드) `0e1c155`.

## 5. 🔑 결과 — 두 가지가 섞여 있었고 하나는 **내 오해였다**

### ① 모델명·장비명 = **진짜 데이터 갭** 🔴  (313칸 · 관리번호 99/248)
- 덤프의 **so_model/so_equip 이 전부 빈 값** → 화면 `COALESCE(so값, 프로젝트 대표)` 가 **대표값으로 폴백**.
- 즉 추가 SO 에 **수주별 모델·장비가 저장 안 됨**(z981 前 업로드). 예 034T2511: 엑셀 장비명 여러 개(…MODIFY·A형)인데 전부 `기능검사기(UK4S)` 로 보임.
- ⛔**그 값이 DB에 없다** → 화면만 고쳐선 복구 불가. **엑셀 재업로드(z981 백필=빈 곳만 채움)** 로 채워야 함.

### ② 수량 = **데이터는 정상 · 작업일정표 표시만 버그** 🟡  (110건 · 관리번호 48개)
- 🔴**내가 앞서 "수량 눌림(N→1)"이라 한 건 틀렸다.** 덤프의 `qty=SUM(order_items.qty)` 실측:
  005T2601 **R05=2 · 마킹위치=4 · 핀블럭=4 · 제품테이블=5** — **엑셀 그대로 저장돼 있다.**
- 작업일정표 `_board_split_lines_map._collapse_line` 이 `qty=orders.unit_qty`(=1) 를 표시해서 1×총액 으로 **보이기만** 함. order_items(수량·단가·금액)는 정확.
- ✅**표시만 고치면 됨**(order_items 수량합 기준). 데이터 무변경·재업로드 불필요.

### ✅ 안전
- **금액(총액) 전부 일치** — 매출 숫자 안 틀어짐. 통화 불일치 1건뿐.
- ⚠ 완제품 여러 호기를 한 SO 로 묶은 수량차이는 '구조'(손상 아님) → 리포트에서 제외.

## 6. 산출물

- 리포트 엑셀: `C:\Users\top00\Downloads\2026-07-27_일괄등록_대조리포트_260707.xlsx` (대표에게 발송).
  - 시트: **요약**(원인·고치는 법) · **① 모델·장비 갭**(313) · **② 수량 표시버그**(110) · **③ 통화**(1).
- 로컬 중간산출: `01_HAIST_WORKS/_dump_stored.json`, `_audit_result.json`(임시·git 제외).

## 7. ⏳ 미해결 — 고침 2건 (대표 결정 대기)

| | 무엇 | 방법 | 위험 |
|---|---|---|---|
| **②** | 작업일정표 수량 표시 고침 | `_collapse_line` 의 `qty`/`price` 를 **order_items 수량합** 기준으로(제품·부품 1줄 SO). 완제품 호기 그룹은 현행 유지 | 낮음·표시만·데이터 무변경 |
| **①** | 모델·장비 재업로드 backfill | **한 관리번호로 재업로드 실측**(z981 백필이 빈 so_equip 채우는지·z982 로 SO 중복 안 생기는지) → 되면 전체 배치 | 데이터 변경 → 실측·대표 승인 후 |

- 제안 순서: **② 먼저(안전) → ① 한 건 실측 후 전체**.
- ⚠①: z981 백필은 `COALESCE(NULLIF(equip_name,''),?)` = **빈 곳만 채움**. so_equip 이 비어 있어야 채워짐(현재 비어 있음=채워질 것). 이미 값 있으면 안 덮음.

## 8. 후속 정리

- z1061 덤프 엔드포인트: 진단용 상시 유지(읽기전용). 필요시 `like` 로 M/기타 사업부도.
- 고침 확정되면 현황판 §4 규칙 등재(다음 번호 45~) + z번호 원격 재확인.
- 로컬 임시 파일(_dump_stored.json 등) 커밋 금지.
