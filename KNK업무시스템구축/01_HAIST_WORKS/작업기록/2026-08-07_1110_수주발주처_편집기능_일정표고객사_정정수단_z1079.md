# SO 발주처 인라인 편집 — 일정표 고객사1 잘못 표기를 실무자가 바로잡는 수단 (z1079)

- **작성**: 2026-08-07 (금) 11:10 KST
- **신고**: 이새롬 프로 기술영업팀 (대표 전달·10:42) — *"작업일정표상 드림텍(본사)로 표기되어 있는데 프로젝트 정보에는 드림텍(아산)으로 표기. 일정표상에도 드림텍(아산)으로 입력되어 있어야 합니다."* (015T2607)
- **상태**: ✅ 기능 배포 완료 · ⏳ 데이터 확정은 실무자 몫(안내 §4)
- **배포**: monorepo `5adb2fc2` (LIVE 반영은 감시 로그·보고 참조)
- **변경 파일**: `app/main.py`(quick-edit) · `app/templates/project_detail.html`(SO 카드 메타)

---

## 1. 원인 — 표시 사슬은 전부 설계대로, "고칠 수단"만 없었다

| 단계 | 동작 | 판정 |
|---|---|---|
| 08-04 개조 발주 등록 | 신규 등록 화면에서 기존 관리번호 015T2607 + 고객사 칸 **"드림텍(본사)"** → 추가 발주 전환 시 그 고객사를 **SO 발주처**로 기록 (z202: 한 프로젝트에 발주처 여러 곳 허용·정확 일치 연결) | 설계대로 |
| 작업일정표 고객사1 | **SO 발주처가 있으면 우선 표시** (z705: 같은 상호 종사업장 구분용 — 드림텍 본사/아산이 정확히 그 용도) | 설계대로 |
| 프로젝트 정보 | 프로젝트 고객사 = 드림텍(아산) 그대로 | 정상 |
| **SO 발주처 정정** | 카드에 **표시만 되고 편집 입력이 없음** → 실무자가 바로잡을 방법이 없어 신고 | 🔴 이번 수정 |

## 2. 수정 (z1079)

- **SO 카드 메타에 발주처 편집칸** 추가 — 기존 so-meta-edit 패턴(✏️ 수정 → 입력 → 모두 저장·JS 무수정). 발주처가 없을 때도 "(프로젝트 고객)" 표시 + 새로 지정 가능
- **quick-edit 서버**: `order_customer` — 등록 고객사 **정확 일치**(정식명 또는 시스템명)만 저장. 오타·미등록 = 400 거부("고객사 화면의 정식명 그대로…" 안내) — 연결 없는 이름 저장 금지(데이터 연결성 안전). **빈 값 = 해제**(프로젝트 고객 상속)
- 일정표 고객사1 은 SO 발주처 우선(z705·무수정)이므로 **여기서 고치면 일정표도 즉시 따라옴**

## 3. 검증 — 시험(임시 DB) PASS 6/0

신고 상태(프로젝트=아산·SO=본사) 재현 → 정식명 저장=아산 교체 ✓ · 시스템명(alias) 인정 ✓ · 오타=400+무변경 ✓ · 빈값=NULL(상속) ✓. Jinja·check_standards 전부 통과.

## 4. 📌 이새롬 프로 안내 (015T2607 정정 절차 — 1분)

1. 015T2607 상세 → **T-260804-4** 수주 카드 → [✏️ 수정]
2. 메타 줄 **🏢 발주처** 칸의 "드림텍(본사)"를 **"드림텍(아산)"** 으로 고침 (또는 비우면 프로젝트 고객사를 따름 — 결과 동일)
3. [💾 모두 저장] → 작업일정표 고객사1도 드림텍(아산)으로 바뀜

⚠ 데이터 확정은 실무자 판단 — 시스템이 임의 보정하지 않음(개조 발주처가 정말 본사인 건이면 그대로 두는 게 맞음). 일정표의 다른 드림텍(본사) 행들(005/006/007/013/014T2606 등)도 같은 방법으로 각 건 판단·정정 가능.

## 5. 참고

- 등록 폼(추가 발주) 고객사 기본값을 프로젝트 고객사로 미리 채울지는 별도 개선 후보 — 전환기엔 엑셀 표기를 그대로 치는 흐름이라 현행 유지가 안전할 수 있어 보류(대표/실무 의견 시 진행)
- WP-04 통합후보는 main 전진(z1079) — 세션 05 재통합 시점 참고
