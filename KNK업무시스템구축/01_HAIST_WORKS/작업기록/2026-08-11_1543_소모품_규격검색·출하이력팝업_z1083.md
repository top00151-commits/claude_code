# 소모품 품명·규격 검색 + 규격별 출하 이력 팝업 (z1083)

- **작성**: 2026-08-11 (화) 15:43 KST
- **지시**: 대표 (2026-08-11) — 실측 답변의 개선 2안 *"둘다 진행해"* (ⓐ검색에 품명·규격 ⓑ품목별 이력 보기)
- **상태**: ✅ 구현·시험 PASS 12/0 · 배포(LIVE 확인은 보고 참조)
- **변경 파일**: `app/consumables.py`(목록 검색) · `app/main.py`(일정표 매칭·이력 API) · `app/templates/consumable_detail.html`(버튼·팝업·JS)

---

## 1. ⓐ 검색에 품명·규격 추가 — 3경로

| 경로 | 수정 |
|---|---|
| 소모품 목록(`co_list`) | 품명·규격 EXISTS + **관리번호도 함께**(기존에 없었음) |
| 작업일정표 찾기 | SQL 프리필터 + 최종 매칭(`_sched_cust_hit`)에 행별 품명·규격 묶음(`parts_blob`) — 본선·**엑셀로 저장** 경로 동일 |
| 프로젝트 엑셀 export | `co_list(q)` 경유라 자동 개선 |

→ "KNK-P-4847" 검색 시 그 품목이 담긴 소모품 발주가 목록·일정표 모두에서 나옴.

## 2. ⓑ 규격별 출하 이력 팝업

- **진입**: 소모품 상세 라인 그리드 — 규격 칸 옆 **[📊]** 버튼 (규격이 입력칸이라 클릭 충돌 없는 버튼 방식)
- **팝업 내용**: ①업체별 요약(출하 횟수·최근 출하일·최근 단가 — 통화별 기호 ₩/$/₫/€/¥/元) ②전체 발주 이력(일자·업체·상태 한글·발주번호·관리번호·수량·단가·금액). 출하 없는 규격은 안내 + 전체 이력만
- **API**: `GET /consumables/spec-history?spec=…` — 로그인 + 물류/영업 사용 권한(소모품 화면과 동일). 일자 = **상태발생일 → 납품일 → 발주일**(실측 답변과 동일 규칙·요약은 '출하' 상태만)
- XSS 이스케이프·배경 클릭 닫기 포함

## 3. 검증 — 시험(임시 DB) PASS 12/0

목록 3검색(규격·품명·관리번호) · 일정표 규격 노출 · 요약 2업체(KRW/USD·상태발생일 우선/납품일 폴백 각각) · 상태 한글 · 일자 내림차순 · 미로그인 401 · 빈 규격 400. Jinja·check_standards(JS 게이트 186 스크립트) 통과.

## 4. 이력·참고

- 발단: 같은 날 실측 답변 기록 `2026-08-11_1505_소모품_출하이력·업체별단가_실측답변.md` (031C2607 PCB 8종)
- 매칭 함정 1건: 일정표는 SQL이 아니라 **파이썬 최종 매칭**(`_sched_cust_hit`)이 판정 — SQL만 고치면 안 나옴(시험이 잡음 → parts_blob 주입으로 해소)
- 홑따옴표 JS 문구 1건 자체 발견·즉시 교정(z1073 유형)

## 5. z1083b — 규격 칸 어긋남 정정 (대표 지적 · 커밋 `eb46d327` · LIVE `595842a` 15:56)

- 증상: 버튼 추가 후 규격 칸이 위로 쏠리고 행 높이가 틀어짐
- 원인: `<td>` 자체에 `display:flex` — 표 셀(table-cell) 규칙이 깨져 높이·정렬 붕괴
- 수정: td 는 표 셀 그대로, 안쪽 wrapper(`.spec-wrap`)만 flex (JS 는 버튼 부모=wrapper 라 무수정)
- 검증: **LIVE 화면 캡처 실측** — 8행 규격 전부 세로 중앙 정렬·버튼 나란히·타 칸 무변화 ✓
- 🔴 교훈(재확인): 상시 화면 레이아웃은 **렌더 실측 없이 배포 금지** — z1083 1차 배포 때 화면 캡처를 생략하고 API·버튼 존재만 확인한 것이 원인. td 에 display 를 바꾸는 수정은 표에서 금물
