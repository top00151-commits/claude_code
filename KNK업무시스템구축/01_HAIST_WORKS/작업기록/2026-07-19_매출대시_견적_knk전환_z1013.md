# z1013 — 매출 대시보드 + 견적(목록·상세) knk Sage 전환 · Phase 5③

- **일자**: 2026-07-19
- **대상(3)**: `sales_dashboard`(매출 강화 대시)·`sales_quotations`(견적 목록)·`sales_quote_detail`(견적 상세).
- **전환**: z1011~1012와 동일 레시피 — `--qv` Sage 브리지 + 머리글 `.page-head`→`knk-page-head`(액션 knk-btn).

## 화면별
- **sales_dashboard**: 머리글 액션 없음. KPI bento·통화별 분포/미수금·Pareto Top5·파이프라인 표 유지(데이터색 amber/success/red).
- **sales_quotations**: 머리글 액션 `📊 엑셀`(ghost)·`＋ 새 견적`(primary). mgmt-pill·pill 상태·행 클릭 이동 유지. 빈 상태 버튼은 전역 `.btn` 유지.
- **sales_quote_detail**: 머리글 mgmt-pill lg + 고객사명 + 상태 pill(meta로), 액션 `🖨 인쇄`(ghost)·`목록`(primary). 장식용 "QUOTATION DETAIL" 라벨 제거. 라인표·메타표·history_card·tfoot 합계 유지.

## 검증 (`scratchpad/verify_z1013.py` 3파일 전부 통과)
- Jinja·`<style>` 중괄호·div/header 균형·knk-page-head 1·breadcrumb·title·Sage 배경밴드·content-pad ✅
- `--qv` 브리지(surface·ink·line) ✅ · 잔재 0(page-head/title/crumbs·#ffffff·#0f172a·QUOTATION DETAIL·page-meta) ✅
- 데이터/위젯 유지: dashboard(kpi-card·bento·Pareto·pipeline)·quotations(knk-btn 2·mgmt-pill·새 견적)·quote_detail(meta pill·knk-btn 2·mgmt-pill lg·history_card·인쇄) ✅

## 배포 / 실측
- monorepo push → 미러 → LIVE. 배포 해시: 미러 `dc22d14` · LIVE 일치.
- LIVE 실측(크롬확장): 견적 목록·상세·대시보드 Sage·knk-page-head 정상. ⭐이때 **셸 좁은창 우측 잘림**(머리글 액션 버튼 안 보임) 발견 → z1014로 분리 해결 ([[knk_live_render_verify_chrome]]).

## 관련 / 다음
- [[knk_design_guide_unified]] · z1011(홈 70ab4fc)·z1012(리포트 844a195)
- **Phase 5 남은 매출영업(6)**: sales_orders(+detail·고정flex)·sales_quote_form·sales_opportunities(+form). (quotation_print=인쇄 예외)
