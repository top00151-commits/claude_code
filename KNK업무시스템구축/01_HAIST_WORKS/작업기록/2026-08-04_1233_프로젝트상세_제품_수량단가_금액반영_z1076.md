# 프로젝트 상세 — 제품(SEMI) 수량·단가 변경이 금액·합계에 반영 안 되던 결함 해결 (z1076)

- **작성**: 2026-08-04 (화) 12:33 KST
- **신고**: 안지연 프로 (대표 전달·스크린샷) — *"수량/단가 변경 적용이 되지 않음"* (006T2607 SOCKET · 수량 10·단가 $30인데 금액 $30·합계 $6,830)
- **상태**: ✅ 완료 — 서버 사슬 시험 PASS 8/0 · LIVE 검증(읽기 전용)
- **배포**: monorepo `b8e1cd13` → 배포저장소 `95a5b29` (LIVE 12:31 · HTTP 200 확인)
- **변경 파일**: `app/templates/project_detail.html` 만 (서버 무수정)

---

## 1. 원인 — 등록(z1050)과 인라인 편집이 서로 다른 모델

| 층 | 모델 |
|---|---|
| 등록(z1050·올바름) | `unit_price=단가` · `amount=단가×수량(총액)` · SO합계=Σamount |
| 인라인 편집(결함) | ①단가 칸에 **amount(총액)** 표시 ②저장 시 입력값을 **amount로만** 전송(×수량 없음) |

**사고 경로(006T2607 재구성)**: 등록 직후 $68,300(정상) → 편집 화면 단가 자리에 총액($300/$68,000)이 보임 → 사용자가 "단가"로 고쳐(30/6,800) 저장 → **총액이 단가로 덮임**($6,830) → 이후 수량·단가를 바꿔도 금액 사슬(금액 칸·SO 합계·수주액)에 수량 미반영 = *"적용 안 됨"*.

## 2. 수정 — 인라인 편집을 z1050 모델에 정렬 (3곳·템플릿만)

1. **단가 편집칸**: 제품(SEMI) 행은 `unit_price`(1대 단가) 표시 (완제품·상품 기존 그대로)
2. **저장 JS**: 제품 행(수량 칸 있음 + 상품 표 아님) → `unit_price=단가` · `amount=단가×수량` 전송. 서버는 이미 두 값을 받게 돼 있어 무수정
3. **금액 칸 라이브 재계산**: 단가·수량 입력 즉시 `단가×수량` 표시(통화기호·KRW 정수/외화 2자리 = money() 표준)

## 3. 검증

- **서버 사슬(임시 DB·TestClient) PASS 8/0**: ①z1050 등록 상태 재현 ②옛 JS 결함 재현(합계 $6,830 — 신고 화면 그대로) ③새 JS로 복구(라인·SO합계 $68,300·프로젝트 수주액 동기화) ④수량 10→12 → 합계 $68,360 반영
- **LIVE(읽기 전용·저장 안 함)**: 단가칸 unit_price 표시(30.00/6,800.00) ✓ · z1076 JS 로드 ✓ · 수량 12 입력 시 금액 $360.00 라이브 표시·원복 ✓ · **서버 전송 0건**(performance 확인)
- Jinja 파싱·check_standards(JS 문법 186개 포함) 전부 통과
- 엑셀 다운로드는 qty·unit_price·amount를 각각 내보내 자동으로 맞음 / 크게 보기는 `amount||qty×unit_price` 폴백 기존 보유

## 4. 📌 안지연 프로 안내 (기존 데이터 교정 — 저장 한 번)

006T2607은 지금 **금액이 단가로 덮인 상태**($6,830). 화면에서 **[수정] → 수량·단가 확인 → [모두 저장]** 한 번 누르면 금액 $300/$68,000 · 합계 **$68,300**으로 교정됩니다(새 저장 로직이 자동 계산). ⚠수주액이 $68,300이 맞는지는 업무 확인 후 저장 — 시스템이 임의 보정하지 않았습니다.

## 5. 남은 관찰(후속·이번 범위 밖)

- 제품 SO의 [＋추가](add-unit)는 여전히 장비 취급(수량 N=호기 N줄) — 제품 항목 추가에 맞는지 대표 확인 후 별도 건
- 변경 이력 문구가 amount를 '단가'로 표기(완제품 관행) — 제품에선 '금액'이 정확. 경미·후속
- 같은 유형 다른 프로젝트가 있는지 실태 조회는 필요 시 별도(운영 읽기 전용)
