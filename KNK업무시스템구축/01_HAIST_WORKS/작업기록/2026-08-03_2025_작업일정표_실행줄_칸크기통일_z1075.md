# 작업일정표 — 상단 실행 줄 5칸 크기 통일 (z1075·z1075b)

- **작성**: 2026-08-03 (일) 20:25 KST
- **지시**: 대표 — "전사일정표·자동저장됨·저장·엑셀로저장·신규등록 이 라인 디자인을 균일하게 변경(칸 사이즈 동일하게)" (2026-08-03, 스크린샷 첨부)
- **상태**: ✅ 완료·LIVE 실측(5칸 전부 150×34px 확인)
- **배포**: z1075 `bacd5d46`→`ace784d` · z1075b `708968c8`→**`1532fd6`** (LIVE 20:25:22)
- **변경 파일**: `app/templates/schedule_board.html` · `static/css/schedule_board.css`

---

## 1. 문제

- 저장 버튼만 초광폭(z594: `#sbSaveBtn{padding:6px 80px !important}` — 당시 '저장 버튼 크게' 지시) ·
  자동 저장됨만 작음(글자 12px·패딩 5px 10px) → 줄이 들쭉날쭉.

## 2. 수정

- **공용 `.sb-act` 클래스** 신설(`schedule_board.css`): `min-width:150px · height:34px · 글자 13px/800 ·
  내용 중앙정렬 · 모서리 7px`. 5개 요소(전사 일정표 링크·자동 저장됨 상태·저장·엑셀로 저장·신규 등록)에 적용.
- 인라인 스타일은 **색만 남김**(기능 구분: 남색 링크/초록 상태·엑셀/파랑 저장/빨강 신규) — 크기 관련 속성 제거.
- 자동 저장됨은 상태 표시라 `cursor:default`(`.sb-stat`). 상태 문구가 "저장됨 · 시각"으로 길어질 수 있어
  고정폭이 아닌 **min-width**(넘치면 그 칸만 소폭 확장·잘림 방지).
- **z1075b**: z594의 `#sbSaveBtn` 초광폭 `!important` 규칙이 공용 클래스를 눌러 저장만 213px로 남던 것 →
  규칙 폐지(z594는 이번 균일화 지시로 대체됨을 주석으로 명기).

## 3. LIVE 검증

- 5칸 실측: **폭 종류 [150] · 높이 종류 [34]** — 전부 동일 ✓ (1차 측정에서 저장만 213px → z1075b 후 150px)
- 화면 캡처로 육안 확인 ✓ · 저장/엑셀/신규 버튼 클릭 동작 코드 무변경(스타일만) · check_standards 전부 통과.

## 4. 참고

- z594(저장 버튼 크게)는 본 지시로 **대체 폐지**. 좁은 창에서는 줄이 자동 줄바꿈(flex-wrap 기존 동작).
