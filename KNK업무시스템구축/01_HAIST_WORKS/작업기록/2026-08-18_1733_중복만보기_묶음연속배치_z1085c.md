# 중복만 보기 — 같은 묶음끼리 연속 배치 (z1085c)

- **작성**: 2026-08-18 (화) 17:33 KST
- **지시**: 대표 — z1085b LIVE 직후 *"중복만보기 누르면 중복된 항목들끼리 묶음으로 나오면 보기가 편할 것 같아.. 바로 연속적으로 나오면 보기가 쉬우니깐"*
- **상태**: ✅ 구현·시험 18/18·표준검사·렌더 실측 완료
- **변경 파일**: `app/templates/project_detail.html` 만 (버튼 안내문 + 토글 JS — CSS 무변경)

---

## 1. 동작

- **「🔎 중복만 보기」 켜면**: 중복 줄만 남는 것에 더해 **같은 묶음끼리 연속 배치**(묶음 번호순 · 묶음 안은 원래 순서). 예: ①①·②②②·③③… — 짝을 위아래로 바로 대조.
- **끄면**: **원래 순서 그대로 복원**(처음 순서를 기억해 두었다가 되돌림). 행번호(#)는 원래 번호를 그대로 들고 다녀 원위치 식별 가능.
- 줄을 화면에서 옮길 뿐 — 내용·저장·서버 무관. 새로고침=항상 원래 순서 전체(기존 원칙 유지).

## 2. 검증

- 시험 18/18 (기존 17 + 묶음 배치 JS·안내문 존재)· check_standards 전부 통과
- **렌더 실측**: 켜면 보이는 행 묶음 순서 `1,1,2,2,2,3,3,4,4,5,5`(3줄짜리 묶음도 전부 연속) · 끄면 행 uid 순서가 초기와 **완전 동일** 복원 · 16줄 전체 복귀 · **켠 상태에서 배지 클릭(ⓑ) 순환 1→2→3→1 정상**(상호작용 무결)

## 3. 배포

- z1085/b 와 동일 plumbing: 원격 main(`0daf36e3`) 직계 커밋 → 해시 지정 push → 미러·NAS 자동. 워킹트리(wp04 브랜치)는 커밋 후 원복.
- 이로써 안지연 프로 요청 중복 확인 기능 = **ⓐ묶음 번호 + ⓑ배지 클릭 이동 + ⓒ중복만 보기(묶음 연속)** 완성형.
