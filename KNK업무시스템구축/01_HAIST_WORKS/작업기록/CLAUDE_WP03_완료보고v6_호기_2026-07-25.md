# WP-03 완료 보고 v6 — 프로젝트 호기 (최종 승인 조건 반영)

> 브랜치 `wp03-project-unit` @ `265717e6` — **main 미반영 · LIVE 무변화**
> 게이트 v5 최종판정("수정 후 최종 승인 · 병합 1회 보류") 대응 · **§9 최종 승인 테스트 10항** 순서

## 0. 판정 확인 — 두 지적 모두 재현으로 사실 확인
| 지적 | 자가 재현 결과 |
|---|---|
| **P0-01** 소급 변경의 '변경 전 값'이 틀림 | 5월 소급 정정이 **`3호기 → 4호기`**로 기록됨. 5월 직전 실제 값은 **2호기** 🔴 |
| **P0-02** 미래 예약 도래 후 현재값 갱신 없음 | 미래 예약 **입력은 되는데** 도래 시 `current_unit_no`를 바꾸는 경로가 없음 🔴 |

## 1. 소급 변경 `old_unit_no` = 적용시점 직전 값 (§9-1)
- `old_unit_no`를 **현재값이 아니라 `_unit_no_at_tx(적용시점)`**으로 계산하도록 교정.
- 실증: 3월 `2호기` → 6월 `3호기` 등록 후 **5월 소급 정정** →

| 기록된 이력 | 적용시점 |
|---|---|
| (없음) → 2호기 | 2026-03-01 |
| 2호기 → 3호기 | 2026-06-01 |
| **2호기 → 4호기** ✅ | 2026-05-01 |

→ ECR/ECO 변경 근거가 실제와 일치합니다.

## 2. 미래 적용일 입력 차단 (§9-2)
- **V1은 미래 예약을 지원하지 않습니다.** `effective_from`은 **현재시각 이하만** 허용, 미래 입력은 명확한 메시지로 거부:
  > "미래 날짜로 호기번호를 예약할 수 없습니다. 지금 적용하거나 지난 날짜로 정정하세요(미래 적용 예약은 다음 단계에서 제공)."
- 화면 날짜칸에 `max=오늘` + 안내 문구.
- **문서 문구 교체**(게이트 §5 지시): v5 보고의 "V1은 미래예약을 허용하며 검증했다" → **삭제**하고 아래로 대체합니다.
  > **WP-03 V1의 호기번호 Effectivity는 즉시 적용과 승인된 소급 정정만 지원한다. 미래 적용 예약은 WP-04 Change 승인 및 자동 활성화 구조가 연결될 때 구현한다.**

## 3. 즉시 변경 정상 처리 (§9-3)
- 적용시점을 비우면 **지금 적용** → `current_unit_no` 즉시 반영 ✅
- 미래 입력 거부 후에도 기존 현재값이 그대로 유지됨 ✅

## 4. 소급 정정 후 시점별 조회 유지 (§9-4)
| 적용구간 | |
|---|---|
| 2호기 | 2026-03-01 ~ 2026-05-01 |
| **4호기** | **2026-05-01 ~ 2026-06-01** |
| 3호기 | 2026-06-01 ~ (열림) |

조회: **4월 → 2호기 · 5/15 → 4호기 · 7월 → 3호기** ✅ (역전 구간 0)

## 5. 관리자 후보 일괄반영 Audit (§9-5)
- 누락돼 있던 **후보 일괄반영 경로에 감사 기록 추가**.
- 기록 내용: **신규 생성 수 · 기존 Unit 연결 수 · 거부 건수 · 대상 관리번호(`project#N`) · 사용자 입력 사유**.
- 실증: 관리자 일괄반영 시 감사 1건 추가 + 위 항목 전부 포함 확인 ✅

## 6. 관리자 우회 사유 필수 (§9-6)
- 시스템 관리자가 기술영업팀 업무를 대신 수행하면 **'요청 부서·요청자' + '우회 사유'가 필수** — 없으면 **작업 자체를 거부**(감사에도 남지 않음, 아무 일도 일어나지 않음).
- 기록 형식: `[관리자 비상복구] <작업> · 요청: <요청자> · 우회 사유: <사유>` — **일반 기술영업 업무와 구분**(정상 업무는 비상복구 감사에 남기지 않음, 실증).
- 화면: 상단 한 곳에 입력 → 그 화면의 **모든 저장 요청에 자동 전달**(폼별 누락 방지).

## 7. 자식 이력 있는 구버전 DB 보정 후 FK 무결성 (§9-7)
자식 데이터(호기번호 이력 1 · 수주연결 1 · 분할관계 1 · 호기 2)가 있는 **구버전 DB fixture**로 보정 실행:

| 검증 | 결과 |
|---|---|
| 자식 데이터 보존 | ✅ (1·1·1·2 그대로) |
| 자식 FK 대상이 `project_units` (임시표 아님) | ✅ |
| `PRAGMA foreign_key_check` 위반 | ✅ **0건** |
| 보정 후에도 법인 생략 INSERT 실패 | ✅ |

## 8. WP-03 전체 테스트 (§9-8)
| 검사 | 결과 |
|---|---|
| `tests/test_wp03_project_unit.py` | **104 / 104 PASS** |

## 9. WP-01 회귀 (§9-9)
| 검사 | 결과 |
|---|---|
| `tests/test_wp01_p0_guards.py` | **98 / 98 PASS** |
| `deploy/check_standards.py` · py_compile | ✅ |

## 10. 상태 (§9-10)
- `265717e6` — **main 미병합 · LIVE 무변화** · origin `wp03-project-unit` 반영
- 버전(z1053)·BAT·현황판 §4는 **병합 시** 처리

---

## WP-03 V1 최종 범위 (확정)
| 지원 | 미지원(다음 단계) |
|---|---|
| 개발호기 생성(번호 없이) · 후보 확인 반영 · 수주 다중 연결 | 미래 적용 예약 → **WP-04** Change 승인·자동 활성화와 함께 |
| 호기번호 **즉시 지정·변경**, **승인된 소급 정정** | 분할·통합 **실행·승인** → WP-04 영향분석 후 |
| 호기 확정·취소(물리삭제 없음) · 변경이력·적용구간 | BOM·발주·입고·투입·출하 영향분석 연동 → WP-04~08 |

**소유부서**: 호기 기준정보는 **기술영업팀**(생성·번호·연결) / **기술영업팀장·대표·임원**(확정·취소) / 타 부서는 조회.
