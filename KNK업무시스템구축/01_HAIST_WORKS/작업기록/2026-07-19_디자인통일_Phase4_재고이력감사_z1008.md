# 디자인 통일 Phase 4 — 재고 이력·감사 7종 knk 전환 (z1008)

- **일자**: 2026-07-19
- **배경**: 대표 "다음 진행해"(compact 직후) — Phase 4 재고 분석 리포트(z1007) 이후 **재고 잔여 화면 7종**을 한 배포로 마무리.
  이번으로 Phase 4(자재·구매) **재고 전 화면 knk 완성**.
- **대상**: `stock_turnover`(회전율)·`stock_issues`(출고이력)·`stock_audits`(실사 목록)·`stock_safety`(안전재고 설정)·`stock_adjustment`(조정 이력+첨부·양모드)·`stock_qc`(검수+부적합·양모드)·`stock_audit`(실사 상세).

## 1. 전환 원칙 — z1005~z1007 정착(셸만 knk, 업무 위젯 유지+브리지)

| 전환(knk) | 유지(업무 특화·색만 Sage 브리지) |
|---|---|
| 머리글 → knk-page-head(breadcrumb·title·meta) | **tbl-erp 표**·**KPI 스트립**(의미색 fast/slow·pending/issued·open/closed 등) |
| 단순 네비 버튼 → knk-btn(ghost/primary) | **액션 폼**: 승인/반려(act-btns)·첨부 업로드(upload-form)·실사 발의·라인 추가·마감·판정(status-pick)·처분(disp-pick)·인라인 저장(inline-input) |
| 빈상태 → knk-empty(refresh·file-export·clipboard-list·target·history·paperclip) | 상태 칩(chip-iss/st/au/band)·배너·토스트·필터바·사이드 카드(def·help-tip·warn-tip) |
| :root pq→knk Sage **풀 브리지** · 입력 포커스 → Sage focus(#3E6F89) | 조정 상태 필터(sa-actions 세그먼트)·마감 버튼(btn-ok 성공색·disabled 로직) |

- **JS 없음**(7종 모두 `<script>` 0 — POST 폼·inline onclick confirm만) → 스크립트 무관.
- **POST 동작 전수 보존**: `/stock/audits/new`·`/approve`·`/reject`·`/attach`·`/stock/adjust`·`/stock/qc/disposition`·`/stock/qc/{id}`·`/items`·`/close`·`/stock/safety/{id}` 그대로.
- 안전재고·QC·조정·audit은 지역 :root가 전역 pq 상속(부분 정의)이라 **풀 브리지**로 통일.

## 2. 세부 (파일별 처리)

- **turnover/issues/audits/safety**(Group A): 머리글·버튼·빈상태 knk. issues=네비 3버튼(재고현황·입고이력 ghost / 출고등록 primary)+빈상태 CTA. audits=실사 발의 폼 버튼 primary(폼 유지)+빈상태 발의 폼. safety=네비 2버튼 ghost+인라인 저장폼/필터/토스트 유지+포커스 Sage.
- **adjustment/qc/audit**(Group B·양모드/상세): 머리글만 knk, 업무 폼 전부 유지. adjustment=양모드 머리글 조건부(목록/첨부)·sa-actions(상태 필터+CTA) 유지·빈상태 2곳(목록·첨부). qc=2모드(검수/부적합) 머리글 2개·판정/처분/액션바 폼 유지·포커스 Sage. audit=상세 머리글(코드+상태칩 유지)·라인추가/마감 폼 유지·마감 btn-ok 재스코프(.knk-page-actions .btn-ok 성공색·disabled)·포커스 Sage.
- **제목 이모지 제거**(🔁📤📋🎯📎📜⚠️🔬📤 등) → 형제 화면 통일. **죽은 CSS 정리**(각 head 군·po-empty·미디어쿼리 `.xx-head` 잔재).

## 3. 검증 (배포 전 전부 통과 · verify_z1008.py)

- 7종 Jinja parse ✅ · **JS 없음(script 0)** ✅ · `<style>` 중괄호 균형 ✅ · div/span/**header** 균형 ✅
- 옛 머리글 잔재 0: tn/gi/au/ss/sa/qc-head·-title·-actions·btn-dark·btn-ghost·po-empty ✅
- knk 전환수 정확: page-head(qc 2·나머지 1)·breadcrumb·title(class=)·knk-btn(0/4/2/2/1/2/1)·knk-empty(1/1/1/1/2/0/0) ✅
- :root 브리지(surface·ink) ✅ · **업무 폼 POST 동작 10경로 보존** ✅ · 입력 포커스 Sage(safety·qc·audit) ✅
- Tabler 글리프 11종(refresh·packages·download·plus·file-export·clipboard-list·target·paperclip·history·arrow-back-up·send) 실존 ✅
- ⚠ 검증 중 1차 지적 2건(audits 머리글 닫는 `</div>`를 `</header>` 미변환 → div 16/17·header 1/0) 즉시 수정. 1건은 검증식 오탐(audit `.knk-page-title .code` CSS까지 카운트 → `class="knk-page-title"`로 정밀화).
- 배포: `fe4c9b67`(7파일 +242/−276, 죽은 CSS 정리로 순감) → 미러 목표 해시 감시.

## 4. LIVE 실증 (미러 `f920bb60` = LIVE `/api/version` f920bb6 일치 · 대표 Chrome JS 프로브)

- **출고이력 `/stock/issues`**: 머리글·breadcrumb·body #F3F5F2·머리글 #FAFBF9·버튼4·**primary #A5282C(KNK 빨강)**·KPI4·knk-empty '출고 이력 없음'·옛 잔재 0 ✅
- **회전율 `/stock/turnover`**: **실데이터 3194행 + 밴드칩 3194개 전부 렌더**·머리글·Sage 색·KPI4·잔재 0 ✅ (표 유지+브리지가 대량 실데이터로 입증)
- **실사목록 `/stock/audits`**: 머리글·Sage·버튼2(발의 primary·**폼 안**)·KPI3·knk-empty '실사 이력 없음'·`/stock/audits/new` POST 폼 보존·잔재 0 ✅
- **조정이력 `/stock/adjustments`**: 머리글·Sage·상태필터 5(sa-actions 유지)·knk-empty '조정 이력이 없습니다'·잔재 0 ✅
- **QC 검수 `/stock/qc/1`**: 머리글·Sage·버튼1·판정토글 4·카드 3·액션바 — 폼 구조 무손상·잔재 0 ✅
- **실사상세 `/stock/audits/1`**: 데이터 없어 목록으로 **안전 리다이렉트**(500 아님). 본문은 데이터 부재로 LIVE 미실행·구조는 오프라인 검증 통과 ⏳
- **안전재고 `/stock/safety`**: 🔴 **기존 잠복 버그로 500** 발견 → z1008b 수정 후 재검증(§4b).

### 4b. 🔴 발견·수정 — /stock/safety 기존 500 (z1008b)

- **증상**: `GET /stock/safety` → `TypeError: '<' not supported between instances of 'int' and 'str'`. 활성 자재(~3천)가 있으면 **항상 500**.
- **원인(내 변경 무관·z1008 이전부터 잠복)**: 본문 `{% set below_safety = items|selectattr('on_hand','lt','safety_stock')|list|length %}` — `selectattr`의 3번째 인자가 **문자열 리터럴 'safety_stock'**이라 on_hand(int)를 문자열과 비교. 게다가 이 `below_safety`는 **어디에도 안 쓰이는 죽은 변수**(KPI는 루프의 `ns.below_safety` 사용).
- **수정(z1008b)**: 죽은 버그 줄 삭제(표시 로직 무변화). Jinja parse·구조 재검증 통과.
- **교훈**: **오프라인 Jinja parse는 문법만** 검사 → `selectattr` 리터럴 비교 같은 **런타임 타입오류는 못 잡음**. 재설계/전환 화면은 반드시 **실데이터 LIVE 렌더**까지 확인([[feedback_verify_before_done]]). z1008 전환 자체는 7종 전부 정상 — 이 버그는 별개 잠복 결함.

## 5. 관련 / 다음

- z1004(재고 목록)·z1005(재고 폼)·z1006(BOM)·z1007(분석 리포트) · [[knk_design_guide_unified]] · [[knk_works_css_palette]]
- ⚠ 배포 시 인덱스에 **다른 세션이 스테이징한 작업기록 .md 7개 삭제**가 있어 경로 한정(`git commit -- <7 템플릿>`)으로 제외. 그 .md 삭제 건은 별도 확인 필요.
- **다음**: Phase 4 재고 완성 → 잔여 Phase 4 = `suppliers_import`(협력사 일괄)·`po_qr_print`(인쇄 별도 규칙). 이후 **Phase 5(전사 화면)** = 매출영업·프로젝트·고객사·홈·공통 Modal/Toast.
