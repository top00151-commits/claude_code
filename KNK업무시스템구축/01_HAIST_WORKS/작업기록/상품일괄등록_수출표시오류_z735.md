# 상품 일괄등록 수출 건 표시오류 6건 수정 — z735 (2026-06-30 · 대표 지시)

## 신고 (005M2511 BIM LINE·수출·251라인)
엑셀 등록값과 화면이 다름: ①매입/판매가 KRW인데 USD로 ②USD 소수 2자리 안 됨 ③모델명·장비명 빈칸 ④상태 출하인데 진행중 ⑤라인 납기 ⑥프로젝트 발주일·납기 없음.

## 진단 (파서는 정상 — 확정/표시가 문제)
대표 파일을 **파서에 직접 실행**(스크래치 `test_parse_005m.py`) → 파서는 6개 다 정확히 읽음(model='BIM LINE'·equip='ASSEMBLY MC'·order 2025-11-27·due 2026-04-01·라인 status='SHIPPED'·라인 due 2026-02-06·cost_krw 2119000·sell_krw 2542800). 즉 **확정 저장·상세 표시**가 원인.
- **1 (KRW→USD)**: 수출이면 상세가 매입/판매를 **환율 역산(cost_price USD × so.exchange_rate)**으로 KRW 표시(z601). 그런데 005M2511은 **옛 코드 등록이라 so.exchange_rate 미저장 → `_pfx=0` → USD로 표시**(화면 환율칸 '—'). 게다가 ×fx는 입력 KRW와 1~2원 오차.
- **2 (USD 4자리)**: 파서 `cost_store = round(cost_krw/fx, 4)`.
- **3·6 (모델/장비/발주일/납기)**: 확정이 **같은 관리번호 기존 프로젝트면(`_is_followup`) `projects_create_logi` 건너뜀** → 프로젝트 모델/장비/발주일/납기 안 채움. + **SO 납기를 `confirm_order_multi`에 안 넘김** → orders.due_date 빔.
- **4·5 (상태·라인납기)**: 현 코드는 정상(unit_status=SHIPPED·라인 due 저장). 대표 업로드가 **z729(상태칸)·z600(환율저장) 배포 전**이라 누락된 것 → 재업로드 시 정상.

## 수정
대표 확정 모델: **매입·판매 = KRW 원본(우리 보기용 참고값) / 인보이스 = USD(실제 판매가=판매KRW÷환율) / 금액·수주액 = USD.**
1. `database.py`: order_items에 **`cost_krw`·`sell_krw`** 칸 추가(입력 KRW 원본 보존·환율 역산 오차 제거).
2. `main.py` 파서: `cost_store` round(4→**2**).
3. `main.py` 확정(`projects_import_product_confirm`): ①order_items row에 cost_krw/sell_krw 저장 ②orders UPDATE에 **due_date·order_date** 추가(SO 납기) ③`_is_followup`이어도 프로젝트 model_name/equip_name/order_date/due_date가 **비어 있으면** 엑셀값으로 채움(기존값 보존).
4. `project_detail.html`: `_ckrw`/`_skrw` set → 수출(`_is_fx_view`)이면 매입/판매 = **KRW 원본 그대로**(`{:,.0f}` _ckrw/_skrw), 없으면 기존 ×환율/USD 폴백. 인보이스 USD·금액 USD는 유지.

## 검증
- 파서 실측: cost_price USD=1454.85(2자리)·cost_krw/sell_krw=2119000/2542800(exact). AST OK·order_items에 cost_krw/sell_krw 생성·project_detail.html Jinja 파싱 OK.
- ⚠확정은 앱 DB 필요해 standalone 미검증 → 코드 리뷰 + 배포 후 재업로드로 LIVE 검증.
- ⚠상세 **편집(마진정리) 입력칸**은 아직 ×fx KRW 표시(표시 span만 KRW원본) → 수출 부품 단가 재편집 시 cost_krw 동기화는 후속(드문 케이스).

## 배포
커밋 `c8a062d` → push `e26dcaf..c8a062d` → Actions 미러 → NAS.

## ⚠ 기존 005M2511 처리 (대표 안내)
수정은 **앞으로의 확정에만** 적용 → 이미 잘못 저장된 005M2511은 안 바뀜. **삭제 후 재업로드(클린)**가 정답. ⛔재업로드만 하면 같은 관리번호 = **추가발주(2번째 SO·251라인 중복)**. 그러므로 먼저 005M2511 삭제 → 재업로드하면 6개 전부 정상.
