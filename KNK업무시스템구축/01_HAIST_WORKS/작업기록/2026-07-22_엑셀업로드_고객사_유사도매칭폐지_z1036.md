# 엑셀 업로드 고객사 — '80% 유사도 자동연결' 폐지 (z1036)

| | |
|---|---|
| 일자 | 2026-07-22 |
| 신고 | 이새롬 프로(기술영업팀) → 대표 |
| LIVE | `ee0aff4` |

---

## 0. 신고

> "대표님 엑셀 업로드한 파일들이 다 꼬였습니다. 그래서 금액이 저렇게 나온 것 같습니다..
>  **고객사에 등록된 이름과 일치하지 않으면 다른 업체로 매칭되어 업로드 됩니다.**"
> "엑셀 업로드 양식에 고객사명을 **드림텍**으로 입력하여 업로드했더니 시스템에서 **vina로 매칭**시키면서 금액이 다 틀려졌습니다."
> "**(주)드림텍 입력하지 않으면** 다른 업체로 인식해버립니다. 드림텍 뿐만 아니라 몇군데가 그렇게 바뀌어서 업로드 된것 같습니다."

대표 지시: *"내가 다음부터는 이렇게 올리면 **알람 울리던지** 하도록 해야할것 같아"*

---

## 1. 실측 — 신고는 **절반만** 맞았다

LIVE 고객사 **75건**을 받아 `_match_customer`(z238)와 `resolve_customer_id` 를 **그대로 재현**했다.

### ① '드림텍' 은 VINA 로 간 게 아니라 — **연결이 안 된 것**

| 입력 | 매칭 | 최종 연결 |
|---|---|---|
| `드림텍` | `(주) 드림텍` 100% | **연결 안 됨** |
| `(주)드림텍` | `(주) 드림텍` 100% | **연결 안 됨** |
| `드림텍(본사)` | `드림텍(본사)` 100% | **id 74 연결** ✅ |

**`(주) 드림텍` 이 두 개**다 — 본사(74)·아산(75) **종사업장**. 이름만으론 어느 쪽인지 알 수 없어
안전장치(`resolve_customer_id` = 후보 1개일 때만 연결)가 **일부러 연결하지 않았다.**

→ **"(주)드림텍을 넣어도 안 된다."** 정답은 **시스템명** `드림텍(본사)` / `드림텍(아산)`.
   그런데 **화면이 그 이유도, 방법도 말해주지 않았다.** 그래서 신고자는 "다른 업체로 매칭됐다"고 이해했다.

### ② 진짜 위험 — 80% 문턱을 넘는 **이름 충돌 15쌍**

| 충돌 | 점수 | 위험 |
|---|---|---|
| `XUGUANG(CN)` ↔ `XUGUANG(VN)` | **0.889** | **중국 ↔ 베트남** 별개 법인 |
| `TOP ENGINEERING` ↔ `TOP ENGINEERING VINA` | **0.978** | **국내 ↔ 베트남** |
| `(주)에스제이아이` ↔ `에스제이아이티` | 0.986 | **아예 다른 회사** |
| `신성델타테크` ↔ `신성델타테크(평택)` | 0.980 | 사업장 다름 |
| `지엔테크놀로지` ↔ `지엔테크놀로지스` | 0.988 | 한 글자 차이 |
| `구딕스테크놀로지` ↔ `구딕스…코리아` / AMS 2건 | 0.97~0.98 | |

**"vina로 매칭"의 실체는 이쪽**일 가능성이 높다 — `TOP ENGINEERING` ↔ `TOP ENGINEERING VINA` 처럼.

---

## 2. 원인 — 좋은 뜻이 만든 함정

z238(대표 지시)로 넣은 **"80% 이상 비슷하면 자동 연결"** 이,
**오타를 구제하는 대신 '남의 회사'를 조용히 붙이는** 쪽으로 작동했다.

경고(`고객사 자동매칭: 'X' → 'Y' (98%)`)는 미리보기에 뜨고 있었지만
**막지 않았고**, 수십 줄이면 묻혔다.

> **한 글자 차이가 다른 회사인 업계**(VINA·평택·CN/VN 접미)에서 유사도 매칭은 위험한 도구다.

---

## 3. 고친 것 — 이름이 **꼭 같을 때만** 연결

| | |
|---|---|
| 연결 | 괄호·(주)·띄어쓰기만 무시했을 때 **이름이 같고** 그 이름의 고객사가 **정확히 1곳** |
| 0.8~0.99 (비슷하기만) | ⛔**연결 안 함** — 이름은 글자로 보존 + *"다른 회사일 수 있어 연결하지 않았습니다"* |
| 같은 상호 여러 종사업장 | ⛔연결 안 함 + **시스템명을 알려준다** (`드림텍(본사) / 드림텍(아산)`) |
| 미등록 | 기존대로 미연결 + 가장 가까운 곳 참고 표시 |

**알람 (대표 지시)**
- 미리보기에서 그 줄을 **공용 `knk-row-flag` 로 덧칠**(amber = 확인해 보세요)
- **[등록 확정] 누를 때 확인창** — 몇 건인지·어느 이름인지·몇 줄인지 나열하고
  *"고객사 없이(이름만 글자로) 저장됩니다 · 매출·미수금 집계에 안 잡힙니다"* 를 명시한 뒤 재확인

**부수 수정**: 미리보기 오류문구가 엑셀 값을 그대로 `innerHTML` 에 넣고 있었다 → `escapeHtml` 적용
(`_warn` 은 이미 escape 돼 있었는데 `_errors` 만 빠져 있었음 · [[works_template_xss_esc]])

---

## 4. 검증

**로컬 — 실제 양식(.xlsx)을 만들어 파서(`_proj_import_parse_xlsx`)를 그대로 호출. 7/7 기대대로.**

| 입력 | 결과 |
|---|---|
| `지테스트` (종사업장 2곳) | 미연결 + **"시스템명(지테스트(본사) / 지테스트(아산)) 중 하나로"** ✅ |
| `지테스트(본사)` | **연결** ✅ |
| `(주) 지테스트` | 미연결 + 시스템명 안내 ✅ |
| `ZQ ENGINEERING` | **연결** ✅ |
| `ZQ ENGINEERIN` (99%) | ⛔**연결 금지** ✅ |
| `ZQ ENGINEERING VIN` (99%) | ⛔**VINA 로 안 붙음** ✅ ← 사고 재현 |
| `전혀없는회사이름` | 미연결 ✅ |

**LIVE (`ee0aff4`)**: 확인창 문구 · 줄 강조 · 시스템명 예시 · escape 적용 · 옛 '고객사 자동매칭' 문구 소멸 — 전부 확인 ✅
구문·JS(인라인 178개)·표준검사 전항목 통과.

---

## 5. ⚠ 아직 확인 안 된 것 (정직히)

- **"고객사가 바뀌어서 금액이 틀려졌다"는 인과는 검증하지 않았다.** 대표 첫 메시지는 *"USD 입력 잘못한듯"* 이었고
  **통화 문제로 별개일 수 있다.** 어제 매출 조사에서 근거 없이 추측했다가 틀렸으므로 단정하지 않는다.
- **문제의 원본 엑셀이 없다** — 신고자: *"업로드 파일은 계속 덮어쓰기하여 최종 자료만 있습니다."*
  → 원인 규명은 **DB 에 남은 결과로 역추적**해야 한다.
- **이미 들어간 데이터의 오연결 건은 손대지 않았다.** 어느 게 잘못 붙은 것인지 확실히 알 수 없다(추정 금지).
  필요하면 별도 조사 후 목록으로 보고 → 화면에서 수정.
- 연결된 **매출 대시보드 이상**(소모품 539억 · `108C2602` KNK VINA USD 19,931,000 등)은 **대표가 보류**한 건이다.

## 6. 이새롬 프로께

> **비슷한 이름으로 남의 회사에 붙는 일은 이제 없습니다.**
> · 이름이 **정확히 같을 때만** 연결됩니다. 비슷하기만 하면 **연결하지 않고** 왜 안 붙였는지 알려드립니다.
> · **드림텍처럼 사업장이 여러 곳**인 고객사는 **시스템명**으로 적어주세요 — `드림텍(본사)` 또는 `드림텍(아산)`.
>   (`드림텍`·`(주)드림텍` 은 어느 쪽인지 알 수 없어 연결되지 않습니다)
> · 연결 안 되는 줄은 미리보기에서 **노란색으로 표시**되고, **[등록 확정]을 누르면 확인창**이 뜹니다.
>
> **다음 업로드부터 적용됩니다. F5 한 번 눌러주세요.**
