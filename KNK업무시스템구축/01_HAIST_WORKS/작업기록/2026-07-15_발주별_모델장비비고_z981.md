# 발주(수주)별 모델명·장비명·기타사항 — 저장·표시·백필 (z981)

- **일자**: 2026-07-15
- **신고**: 김정락 대표 — 999M2603 스크린샷 2장: "엑셀(업로드 파일)과 작업일정표 내용이 달라… 이런 게 너무 많은 것 같아. 정확하게 WORKS에 자료가 정리될 수는 없는 거야?"
- **승인**: 대표 "구조 개선 진행" (z979 전수 대조 후속)
- **결론 한 줄**: 모델명·장비명·기타사항이 "관리번호(프로젝트)당 1개"만 저장·표시되던 구조를 **발주(수주번호)별 저장·표시**로 확장 — 남아 있던 불일치 14건 전부 이 원인이었고, 재업로드 한 번으로 자동 교정되게 백필 경로 포함.

---

## 1. 전수 재대조 결과 (42행 × 20칸 ≈ 840칸)
z979에서 수량·금액·날짜·세금계산서(숫자 칸)는 전부 맞춰졌고, 글자 칸 재대조에서 **불일치 14건**:

| 칸 | 건수 | 사례 |
|---|---|---|
| 모델명 | 3 | 999M2603 2~4번째 발주가 전부 첫 발주 값("AGV설비 Connectro Cable교체")으로 표시 |
| 장비명 | 4 | 위 3건 + 999M2604 '인도 출장'→'MES 고도화 프로그램 개발'로 표시 |
| 기타사항 | 7 | PO번호가 첫 발주 값으로 표시(002M2602×2·005M2602) 또는 빈칸(002M2509·999M2603×2·999M2604) |

- 프로젝트명·거래구분·PO유형·형태·고객사·영업담당자·수량·단가·금액·발주/납품일·상태·명세·세금 1/2/3차: **42행 전부 일치**.
- SAMSUNG INDIA ELECTRONICS→SIEL 3건 = 등록 고객사 별칭 연결(정상). 'EM 28Nm␣␣LMS' 1건=공백 차이(무시).

## 2. 원인 — 프로젝트 단위 필드의 구조 한계
- 한 관리번호(프로젝트)에 모델명·장비명·기타사항 저장칸이 **1개뿐** → 첫 행 값이 대표값이 되고, 작업일정표의 모든 발주 줄이 그 값으로 표시.
- 업로드 시 추가발주(2번째 이후 행)의 **장비명은 아예 저장 안 됨**, 모델명은 호기 라벨로만, 기타사항은 호기 비고(line_note)로 저장은 되나 **화면이 프로젝트 값(logi_note)만 읽음**.

## 3. 조치 (z981)
### ① 저장 — orders에 model_name·equip_name 칸 신설 ([database.py](../app/database.py) 마이그레이션)
일괄등록 3경로 모두 행 값을 그 수주에 저장([main.py](../app/main.py)):
- 신규 수주: 그대로 저장 / **추가발주·재업로드: 빈 곳만 채움**(COALESCE — 화면에서 고친 값은 재업로드가 못 덮음 · 병합 수주는 첫 값 보존) → **같은 엑셀 재업로드 = 기존 어긋남 자동 백필**.

### ② 표시 — 작업일정표·엑셀로 저장·프로젝트 상세가 수주값 우선
- 공용 헬퍼 `_board_split_lines_map`: SO의 model/equip + 그 줄 호기 비고(so_note) 적재. 병합 수주는 비고 여러 개를 ' · '로 합침(`_join_notes981` — PO 두 개 다 보임). **상품(PARTS) 수주는 부품 비고가 기타사항으로 새지 않게 제외**.
- 두 빌더(화면 + 엑셀로 저장 공용) 행 조립에서 SO값 있으면 덮고 없으면 기존 프로젝트값(폴백) — 기존 화면 불변.
- 프로젝트 상세 SO 카드: '📦 모델/장비' 표시(프로젝트 값과 다르면 강조).

### ③ 편집 — 그 발주 것만 고쳐지게
- 작업일정표에서 수주 분할 행의 모델명·장비명·기타사항 클릭 수정 → `/sales/orders/{id}/quick-edit`(확장: model_name·equip_name·line_note)로 **그 수주에만 저장** (기존엔 프로젝트 전체가 바뀌었음). 기타사항은 그 수주 호기 비고 전체 교체.

## 4. 검증
- py_compile 3파일 + Jinja 2템플릿 + esprima(신규 startSoTextEdit·수정 startEdit) ✅
- 픽스처 테스트 4종 ✅: SO값 override/프로젝트 폴백 · 병합줄 비고 합침("715 · 712") · 상품 비고 미누출 · oid 라우팅
- z981 이전 스키마(칸 없음) 폴백 ✅ (PRAGMA 가드)

## 5. 배포 후 절차
1. 배포 확인(/api/version) → 2. **대표 엑셀(KNK_프로젝트_일괄등록_260626.xlsx) 그대로 재업로드 1회** → 모델명·장비명 백필 (기타사항 7건은 배포 즉시 표시 교정 — 데이터는 이미 호기에 있음) → 3. 42행 재대조로 0건 확인.

## 6. 관련
- 선행: [2026-07-15_일괄등록_엑셀대조_진단_z979.md](2026-07-15_일괄등록_엑셀대조_진단_z979.md)
- ⚠ 작업번호: 같은 날 다른 세션이 z980(구매팀 권한)을 사용해 **z981**로 발번.
- 남은 손일(대표·안지연 프로): LMS 4건+005M2510 3차 세금계산서를 마스터 엑셀에 채우기 / 003M2602 실제 37대 확인.
