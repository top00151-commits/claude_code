# z1015 — 견적 작성폼(sales_quote_form) knk Sage 전환 · Phase 5④ · 폼 --paper 브리지 레시피 확립

- **일자**: 2026-07-19
- **대상**: `sales_quote_form`(견적 작성 폼).
- **⭐새 발견(크롬 실측)**: 매출영업 화면은 두 부류 —
  - **대시보드/목록**: 로컬 컴포넌트가 `--qv`(흰색 재정의) 사용 → z1011~1013 `--qv→Sage` 브리지로 전환됨.
  - **폼**: `.form-card`/`.fld`/`.lines-table`가 **전역 `--paper` 계열(레거시 따뜻한 종이색)** 사용 → `--qv` 브리지로는 안 바뀜(따뜻한 베이지로 남아 있었음).

## 폼 레시피 (신규 확립)
- **`.main` 스코프 `--paper→Sage` 브리지**(셸 미영향 — 사이드바/상단바는 `.main` 밖):
  ```css
  .main { --paper:#FAFBF9; --paper-2:#F6F7F4; --paper-3:#FAFBF9; --line:#D4DAD4; --ink:#27302B; --mute:#657169; }
  ```
  → form-card·입력칸·라인표·합계행 전부 Sage. **크롬 실측으로 셸 안 깨짐 + 폼 Sage화 확인**(적용 전후 스크린샷).
- 추가: `body{--qv→Sage}` 브리지(일관성)·머리글 `.page-head`→`knk-page-head`(장식 "QUOTATION NEW" 제거)·`.main > .knk-page-head` Sage 밴드.
- **유지**: `--knk-red`(브랜드)·라인 추가/합계 JS(addLine/recalc)·`knk_inputs`(콤마 포맷)·POST `/sales/quotes/new`·입력 포커스(2px knk-red).

## 검증 (`scratchpad/verify_z1015.py` 전부 통과)
- Jinja ✅ · esprima JS(1블록 1772자·템플릿리터럴) ✅ · style 중괄호 24/24 ✅ · div/header 균형 ✅
- knk-page-head 1·breadcrumb·title·Sage 밴드 ✅ · `--qv`·`.main --paper`·`--line` 브리지 ✅
- 잔재 0(page-head/title/meta·QUOTATION NEW·#ffffff·--paper:#) ✅ · form-card·lines-table·knk_inputs·addLine·recalc·knk-money·POST 유지 ✅
- ⭐배포 후 크롬 실측으로 화면 확인.

## 배포 / 다음
- monorepo → 미러 → LIVE. 배포 해시: 미러 `56e45df`. 크롬 실측: form-card·입력·라인표 전부 Sage·셸 그대로 확인 ✅.
- ⭐**전역 vs 로컬 판별(크롬 실측)**: 전역 컴포넌트(.kpi-card/.tbl/.card)는 quiet_v3가 Sage로 재정의 → --qv만 브리지해도 Sage(대시보드·상세). **로컬 커스텀 클래스가 var(--paper) 직접 사용**할 때만 warm → `.main --paper 브리지` 필요(폼류). [[knk_design_guide_unified]] 반영 완료.
- **Phase 5 남은(5)**: 수주(sales_orders 고정flex·sales_order_detail)·영업기회(sales_opportunities·sales_opportunity_form=하드코딩색 별도 기법). (quotation_print=인쇄 예외)
