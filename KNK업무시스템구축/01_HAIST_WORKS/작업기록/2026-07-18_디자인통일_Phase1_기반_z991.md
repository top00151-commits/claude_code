# 디자인 통일 Phase 1 — knk 기반 토큰·공통 컴포넌트·기준 예제 (z991)

- **일자**: 2026-07-17~18
- **배경**: 대표가 CODEX로 작성한 **통합 디자인 가이드**(`JR\CODEX\김정락 브랜딩\KNK_ERP_UNIFIED_DESIGN_GUIDE.md`)
  검토 지시 → 빅터 검토·정정 6건 반영 → 대표 지시로 **22~25장 신설**(데이터 표기·공통 입력 위젯·동작·용어 표준
  — 흩어진 절대준수 규정을 문서로 통합, 체크리스트 7→12항목) → **Phase 0 대표 확정 3건**:
  ①공식 용어=**'자재'**(기술 식별자만 '부품 코드') ②아이콘=**Tabler Icons**(자체 호스팅·신규 화면부터)
  ③기준=**Chrome·Edge 최신/PC 최소 1024px·폰 360px** → Phase 1 착수 승인.

## 1. 내용 (기존 화면 영향 0이 핵심 요구)
- **`static/css/knk-foundation.css`** (신규): 시맨틱 토큰 — 배경/글자/테두리/브랜드/상태 + 모서리(6/8/10/999)
  + 그림자 3종 + 간격(4px 배수) + 컨트롤 높이(36/32/40/36). **전 토큰 폴백 병기**([[knk_works_css_palette]]의
  '미정의 토큰=선언 무효' 함정 원천 차단). `:root` 변수 + `.knk-num` 뿐.
- **`static/css/knk-components.css`** (신규): 가이드 §8·§9 명세 그대로 —
  `.knk-page-head/breadcrumb/title/meta/actions` · `.knk-btn`(primary/secondary/ghost/danger/sm/disabled/**is-loading**
  스피너·너비 유지·중복 제출 차단) · `.knk-field/input/select/textarea`(hover/focus/readonly/disabled/error/success·
  `--num`=우측정렬) · `.knk-form-section/grid`(4열→1023px 2열→767px 1열) · `.knk-kpi-strip`(선택=밑줄 2px·위험색만 경고)
  · `.knk-table(-wrap/--dense)`(헤더 sticky·40/36px·약한 zebra·hover) · `.knk-badge` 5종(22px)
  · `.knk-alert` 4종 · `.knk-empty` · `.knk-submit-bar`(767px↓ 세로).
- **`_v5_partials/styles.html`**: 전 페이지 head에 링크 2줄(v5_main→…→knk 순서, APP_VERSION 캐시버스터).
- **`/admin/design-kit`** (라우트+`admin_design_kit.html`, 관리자·대표만): **살아있는 기준 예제** —
  토큰 스와치·헤더·버튼 8상태·폼(오류/읽기전용/비활성 포함)·KPI·표(사업부 병기 T 검사기/이름 '이름 직책 부서'/
  ₩정수·$2자리·🔒마스킹·'3대분')·알림 4종·빈 상태·제출 바·12항목 체크리스트. 새 화면은 여기서 복사.

## 2. 안전장치 (영향 0 보장)
- **스코프 검사기**(verify_z991.py): CSS 중괄호 깊이 추적으로 전 선택자 수집 → components **98블록 전부
  `.knk-` 접두사**·foundation은 `:root`+`.knk-num`뿐·**`!important` 0** → 기존 DOM에 닿는 선택자 0개.
- box-sizing을 컴포넌트에 명시(전역 리셋 의존 제거 — 미리보기 실측 38px→36px로 잡아낸 뒤 수정).

## 3. 검증
- 스코프 검사기 ✅ · py_compile ✅ · Jinja(design_kit+styles) ✅ · 링크 순서 ✅ · 라우트 단일 ✅
- 정적 미리보기 실측(인앱 브라우저): 버튼 36px·Primary=KNK빨강·모서리 6px·입력 36px·표 헤더 sticky·
  그리드 4열·배지 22px·KPI 활성=잉크 밑줄·로딩 스피너 knk-spin ✅ (스크린샷 캡처는 로컬 페이지 차단 →
  육안 확인은 LIVE에서)
- 배포: `f9173259`(5파일 +483) → LIVE §4

## 4. LIVE 실증 (LIVE `60eccc7` · 대표 Chrome)
- **/admin/design-kit**: 9섹션 전부 렌더 ✅ knk CSS 로드 ✅ 버튼 36px·Primary=KNK빨강(165,40,44) ✅
  입력 36px ✅ 표 헤더 sticky ✅ 배지 22px ✅ 셸 정상(사이드바+책갈피 탭줄) ✅
  — 이 페이지 자체가 책갈피 탭에 자동 등록됨(z989 전역 기능이 신규 화면에 자동 적용됨을 실증) ✅
- **기존 화면(/prod-requests) 무변화**: knk CSS 로드 상태에서 페이지 DOM에 `.knk-*` 클래스 **0개** =
  구조적으로 변경 불가 + 실측 동일(워크스페이스 알약 베이지 245,235,217/활성 빨강/999px·책갈피 8px·
  격자 3행·제목 26px — z990d 확정값 그대로) ✅
- 참고: 크롬 확장 스크린샷 캡처가 이날 간헐 타임아웃 → 육안 확인은 수치 실측+대표 직접 열람으로 대체.

## 5. 다음 단계
- **Phase 2**: parts.html(자재 마스터 목록)을 기준 화면으로 전환 — 페이지 헤더·버튼·KPI·필터·표·배지·빈 상태,
  용어 '자재' 적용. Tabler Icons 자산 다운로드(대표 승인 필요: 출처 github.com/tabler/tabler-icons·webfont)는
  Phase 2 착수 시.
- 관련: [[knk_design_guide_unified]] · [[knk_works_css_palette]] · 가이드 §17 Phase 계획
