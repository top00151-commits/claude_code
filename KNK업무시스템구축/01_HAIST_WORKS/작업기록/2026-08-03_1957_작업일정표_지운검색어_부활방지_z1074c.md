# 작업일정표 — 지운 검색어가 '월 보기'에서 되살아나던 문제 해결 (z1074c)

- **작성**: 2026-08-03 (일) 19:57 KST
- **지시**: 대표 — "찾기일괄에 입력된 관리번호(001t2601)를 지우고 월 보기를 클릭하면 지운 관리번호가 다시 나오고 그 번호만 검색됨" (2026-08-03 저녁, 스크린샷 첨부 신고)
- **상태**: ✅ 완료·LIVE 신고 시나리오 재현 검증
- **배포**: monorepo `035ea155` → 배포저장소 **`67314c1`** (LIVE 19:55 반영 확인)
- **변경 파일**: `app/templates/schedule_board.html` 만

---

## 1. 원인

- '월 보기'·'월 이동(←/→/오늘)'·'사업부 탭'·'기간해제' **링크와** '엑셀로 저장'·'기간으로 찾기' 버튼에는
  **서버가 페이지를 그린 시점의 검색어(`cust=`)가 박혀** 있음(z371 "필터 유지하며 이동" 설계).
- 입력칸을 지우는 것은 **화면(클라이언트)만** 지우는 것 — 링크는 옛 검색어를 그대로 들고 이동
  → 월 보기를 누르면 `?ym=…&cust=001t2601` 로 가서 지운 번호가 부활.
- z862 자동 전환 시절에는 지우면 0.8초 뒤 자동으로 검색 없는 URL로 이동해 이 어긋남이 가려져 있었고,
  z1074(명시 실행 전환)로 어긋남이 드러난 것.

## 2. 수정 — 원칙: "입력칸에 보이는 검색어 = 어디로 가든 들고 가는 검색어 (비우면 완전 해제)"

- **`_schedSyncNavLinks(rawQ)`** 신설: `.sched-nav a`(월 보기·←·→·오늘) · `.sched-tabs a.sbm`(사업부 탭 6개) ·
  `a.period-clear`(기간해제) — 총 8개 링크의 `cust` 파라미터를 입력칸 값으로 재작성(비면 제거).
- 호출 시점 2곳: ①필터 적용 시(`_schedApplyFilter`, 입력 후 0.2초) ②**링크 클릭 순간**(디바운스 전 0.2초 안 클릭·Ctrl/휠 새 탭 열기 대비).
- **엑셀로 저장**: 서버 렌더 시점 `cust` 고정값 → **클릭 순간 입력칸 값을 직접 읽어** 내보내기 URL 구성
  (지운 뒤 저장했는데 옛 검색어로 걸러져 나오는 불일치도 함께 방지).
- **기간으로 찾기(schedRangeGo)**: `_SCHED_CUST`(서버 시점 값) → 입력칸 값 직접 읽기로 변경.

## 3. LIVE 검증 (신고 시나리오 그대로 · ?cust=001t2601)

1. 진입: 검색칸 001t2601 채워짐·월 보기 링크에 검색어 포함(기존 동작)·전 기간 10행 ✓
2. **검색칸 지움** → 0.2초 뒤 **링크 8/8 전부에서 검색어 제거** 확인 ✓
3. 새 검색어("측정") 입력 → **링크 8/8 새 검색어로 갱신** ✓ (양방향)
4. 지운 상태에서 **월 보기 실제 클릭 이동** → `?ym=2026-08`(검색어 없음)·검색칸 빈칸·이번 달 달력 13행 전체 표시 — **지운 번호 부활 없음** ✓
5. 엑셀 버튼 = 입력칸 라이브 읽기 방식 확인 ✓ · Jinja 파싱·check_standards 전부 통과

## 4. 참고

- z371의 "이동해도 필터 유지" 설계는 그대로 — 유지 기준이 '서버 시점 값'에서 **'입력칸에 지금 보이는 값'**으로 바뀐 것뿐.
- 같은 날 연속 작업: z1074(검색 먹힘·월이동 속도) → z1074b(2초 멈춤) → z1074c(본 건). 기록 각각 별도 .md.
