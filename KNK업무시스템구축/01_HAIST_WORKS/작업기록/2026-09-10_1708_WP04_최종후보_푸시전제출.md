# WP-04 최종 후보 — 푸시 전 제출 (변경 목록 · 시험 결과 · 복구 지점)

- **작성**: 2026-09-10 (목) 17:08 KST
- **세션**: 05 자재구매 (빅터)
- **근거**: 대표 확정 2026-09-10 16:18 — 미분류 원본 **제한형 채택** · *"푸시 전에 최종 변경 목록·시험 결과·복구 지점을 한 번에 제출하세요."*
- **가지**: `wp04-perm3` · 기준 `868aac98` (최신 origin/main) · **8커밋**
- **상태**: ✅ **배포 완료 LIVE** (2026-09-10 17:14 · 라이브 `1d90caa` → `e57323e`)
- **푸시**: `868aac98..83c3ad96 wp04-perm3 -> main` (대표 직접 실행)

> 🔴 **내 확인 방법이 틀려 20분을 허비했다.** 푸시 **전에 기준값을 안 잡고** 푸시 직후 처음 읽었는데,
> 미러가 **18초**에 끝나 그때 이미 새 값(`e57323e`)이었다. 그걸 옛값으로 착각해 「미반영」이라 보고했고,
> 대표께 GitHub Actions 확인과 **프로덕션 SSH 실행까지** 시켰다.
> 진실은 NAS 커밋 메시지에 있었다 — `e57323e mirror: claude_code 01_HAIST_WORKS @ 83c3ad9` = **내 배포본**.
> ⭐ 규칙은 「이전값과 다름」을 보라는데 나는 **이전값을 모른 채** 판단했다 — 규칙을 반쪽만 지켰다.
> ⭐ 라이브 SHA 는 **배포저장소 SHA** 이지 모노레포 SHA 가 아니다. 커밋 메시지의 `@ <모노7자리>` 로 대조하면 오판이 안 난다.

---

## 1. 복구 지점

| 태그 | 가리키는 곳 | 들어 있는 것 |
|---|---|---|
| **`rollback-20260910-1706`** | `868aac98` | 사진 ✅ · 세션01 z1097/z1097b ✅ |

⛔ **옛 태그 `rollback-20260910-1203` 을 쓰지 마십시오** — `05f37d2d` = **사진 배포 전**입니다.
그걸로 되돌리면 사진 기능까지 사라집니다.

---

## 2. 🔴 푸시 직전에 잡은 것 — 남의 작업을 지울 뻔했습니다

변경 목록을 만들다가 제 후보가 **세션01의 z1097b 작업을 삭제하는 것으로** 나왔습니다.

원인: 제가 후보를 만든 뒤 세션01이 **2건을 더 올렸습니다**(`0ababb07`, `868aac98`).
제 후보는 그 이전(`c4f1ffb5`) 기준이라, 최신 main 과 견주면 그들의 새 파일이 「삭제」로 보입니다.

조치: **최신 main 위로 다시 얹었습니다**(rebase · 충돌 없음). 강제 덮어쓰기는 하지 않았습니다.

| 확인 | 재정렬 전 | 재정렬 후 |
|---|---|---|
| 삭제되는 파일 | **5건** 🔴 | **0건** ✅ |
| `app/main.py` 변경 | +256줄 (남의 코드 되돌림 포함) | **+53줄** (내 것만) |
| 세션01 `check_standards.py` | 506 → 441줄 🔴 | **506줄 그대로** ✅ |
| 세션01 통화 시험 파일 | 없어짐 🔴 | **있음** ✅ |

> 📌 실제로는 푸시가 non-fast-forward 로 **거부**됐을 것입니다. 그래도 제출 전에 잡아야 할 문제였습니다.
> ⭐ **교훈: 변경 목록은 「최신 origin/main 대비」로 뽑아 봐야 남의 작업 침범이 드러난다.**

---

## 3. 최종 변경 목록 (기준 `868aac98`)

### 커밋 8건

| # | 커밋 | 성격 | 내용 |
|---|---|---|---|
| 1 | `826ba31a` | 코드 | 구매 작업 **시작 전** 정보권한 확인 + 마이그레이션 실패 노출 |
| 2 | `2ec28063` | 코드 | 안내문도 권한을 따라가게 + 시험 화면 생성기 |
| 3 | `75de76f2` | 문서 | 시험 화면 자원 내장 + 9/9 기록·전달문 |
| 4 | `d3236e63` | 문서 | 사진 기능 작업기록 (이미 LIVE 인 `bc09a599` 의 기록) |
| 5 | `83a0d8f0` | 문서 | 세션05 하루 정리 |
| 6 | `5e6995f1` | 시험 | **헛돌던 시험 두 개 복구** (스키마·단가) |
| 7 | `3c6f7f7d` | 문서 | 통합 후보 기록 |
| 8 | `382db3fb` | 코드 | **미분류 원본 「본인 되받기」 제한형** |

### 파일 14개 · **+1,850 / −26** · 삭제 0건

| 파일 | 변화 |
|---|---|
| `app/main.py` | +53 — 사전 권한검사 · 사진 보고문 · **본인 되받기 예외** |
| `app/database.py` | +26 — 마이그레이션 실패를 남김 (`except: pass` 제거) |
| `app/templates/bom_tools.html` | +6 — 안내문이 권한을 따라감 |
| `deploy/check_bom_schema.py` | **신설** 184 — 배포 전 스키마 확인 |
| `_검증/_표본/make_price_fixtures.py` | **신설** 95 — 비식별 단가 표본 생성기 |
| `_검증/test_bom_self_original_20260910.py` | **신설** 221 — 본인 되받기 15건 |
| `_검증/make_bom_perm_screens.py` | **신설** 222 — 시험 화면 생성기 |
| `_검증/test_bom_fill_prices.py` | ±43 — 사라진 표본 → 비식별 표본 |
| `_검증/test_bom_permissions_20260908.py` | +33 — 사전 차단 3건 |
| 작업기록 `.md` 5개 | 신설 |

⛔ **실제 거래파일(수불부·마스터·도면·사진)은 하나도 커밋하지 않았습니다.**

---

## 4. 예상 밖 권한 확대 — **없음** (코드로 확인)

| 권한 판정 함수 | 변경 |
|---|---|
| `_bt_may_see` · `can_view_field` · `_bom_can_purchase` · `_bom_can_upload` · `can_use_logistics` · `can_view_logistics` · `load_field_access_policy` | **7개 전부 0줄** |

예외는 **다운로드 경로 한 곳**에만 있고, `f != "out"` 일 때만 계산되므로 **산출물에는 걸릴 수 없습니다.**

### 여는 것 — 넷을 **동시에** 만족할 때만

① 입력 원본 요청 · ② 그 원본이 **미분류**(양식 미지원) · ③ `created_by` == 요청자 · ④ 그 실행의 그 입력 하나

⛔ **표시 이름·관리번호·파일명으로 판단하지 않습니다 — `created_by` 만 봅니다.**

### 열지 않는 것

산출물 · 보고문 · 남이 올린 원본 · 구매 실행권한 · 단가/구매처 전체 열람 ·
같은 관리번호의 다른 파일 · 인계받은 파일 · 업로더 기록 없는 과거 자료

### 일반 열람권한 — **그대로**

기본 조회 + 단가·구매처 두 권한을 갖춘 사람의 경로는 손대지 않았습니다.

---

## 5. 시험 결과 — 이 후보에서 전부 재실행 (옛 결과 합산 아님)

**종료코드 16개 전부 0** (어느 시험이든 하나라도 실패하면 1을 냅니다)

| 시험 | 결과 |
|---|---|
| `check_bom_schema` (스키마·마이그레이션) | **13 / 0** |
| `test_bom_self_original` (본인 되받기·신설) | **15 / 0** |
| `test_bom_photos` (사진) | **27 / 0** |
| `test_bom_permissions` (권한) | **38 / 0** |
| `test_bom_tools_routes` (화면) | 32 / 0 |
| `test_menu_bom_entries` (세션01 메뉴) | 30 / 0 |
| 인벤터 · 합치기 · 개정 | 19/0 · 21/0 · 20/0 |
| `test_bom_fill_prices` (단가) | **17 / 0** |
| 견적 · 발주 | 17/0 · 23/0 |
| 결함 회귀 · 기준전환 | 9/0 · 40/0 |
| **`test_ccy_mix_total` (세션01 통화)** | **64 / 0** |
| `check_standards` | 전부 통과 |

**BOM 계열 합계 321건 + 세션01 통화 64건 — 실패 0.**

대표 지시 네 항목:

- **사진 기능** ✅ 보존 (`photos.py` · 보고문 3곳 · 27/0)
- **권한** ✅ 네 구매작업 모두 시작 전 차단 · 기록·파일 0
- **단가** ✅ 미완료 1건 → 17/0 (비식별 표본)
- **마이그레이션** ✅ 실패를 **일부러 만들어** 확인 (13/0)

---

## 6. 세션01 확인용 — 같은 후보로 다시 찍은 화면

📂 `작업기록\2026-09-10_시험화면_BOM권한_통합후보\` → `index.html`

⛔ 재정렬로 코드가 바뀌었으므로 **최종 후보에서 다시 찍었습니다.**

| 확인 | 값 |
|---|---|
| 화면 | 10장 |
| 못 넣은 바깥 자원 | **0개** |
| 차단 뒤 남은 기록·파일 | **0건 / 0개** |

세션01이 볼 것: 공통 메뉴·프로젝트 버튼·설계/구매/조회 연결 + **기존 수정 보존**
(세션01 메뉴 시험 30/0 · 통화 시험 64/0 이 후보에서 통과).

---

## 7. 📥 세션01에서 새로 넘어온 건 — **이번 후보에 넣지 않았습니다**

`2026-09-10_1559_TO세션05_환산없는금액합산_발주목록_전달.md`

**제 소유 파일**에 매출 쪽과 같은 결함이 있다고 알려 왔습니다.

| 위치 | 문제 |
|---|---|
| `app/templates/po_list.html:92` | 발주 목록 합계가 `currency` 를 보지 않고 더함 |
| `app/templates/po_list.html:93` | 위 값을 나눈 평균도 함께 틀림 |

- **지금 피해 0건** — 발주 1건 · 전부 원화 (세션01 운영 실측)
- 다만 **외화 발주가 처음 등록되는 순간 조용히 틀리기 시작**합니다
- 매출 쪽 실제 피해: 통화 섞인 25건 · 오차 약 41억 · **24건이 실제보다 작게** 나와 아무도 신고하지 않았음
- 공용 도구(`_ccy_breakdown` · `knk_ccy_total.html`)는 이미 배포돼 있어 **갖다 쓰면 됨**
- `check_standards` 면제 목록에 `po_list.html {"ccy": 1}` 로 등재돼 있음 — 고치면 0으로 내려야 함

⛔ **승인 범위가 아니라 손대지 않았습니다.** 별도 지시를 주시면 진행하겠습니다.
아울러 `stock_balances` 가 원화 전제여도 되는지도 세션01이 제게 물었습니다 — **업무 판단이 필요합니다.**

---

## 8. 푸시 명령 (PowerShell · 한 줄에 하나)

세션01 확인이 끝나면 실행해 주십시오. 분류기가 막아 제가 못 합니다.

```
cd "C:\Users\top00\JR\Claude 코드\.wt_deploy"
```

```
git push origin wp04-perm3:main
```

```
git push origin rollback-20260910-1706
```

거부되면(그 사이 누가 또 올린 경우) 아래를 먼저 하고 다시 푸시해 주십시오.
⭐ 그 경우 **제게 알려 주십시오 — 변경 목록과 시험을 다시 봐야 합니다.**

```
git fetch origin
```

```
git rebase origin/main
```

푸시 후 제가 `works.knknara.co.kr/api/version` 으로 반영을 확인하겠습니다.

---

## 9. 하지 않은 것

| | 지켰나 |
|---|---|
| main 병합 · 배포 | ✅ 안 함 — 제출까지만 |
| 강제 덮어쓰기 | ✅ 안 함 — 최신 위로 재정렬 |
| 예외 범위 확대·축소 | ✅ 안 함 — 대표 확정 문구 그대로 |
| 일반 열람권한 변경 | ✅ 안 함 |
| 보관함 정리 | ✅ 아무것도 안 함 |
| 실제 거래파일 커밋 | ✅ 안 함 |
| 실패를 SKIP 으로 바꿔 통과 | ✅ 안 함 |
| 옛 시험 결과 합산 | ✅ 안 함 — 이 후보에서 전부 재실행 |
| 발주 목록 통화 결함 (§7) | ✅ 안 함 — 승인 범위 밖 |
| 차단 우회 | ✅ 안 함 |
