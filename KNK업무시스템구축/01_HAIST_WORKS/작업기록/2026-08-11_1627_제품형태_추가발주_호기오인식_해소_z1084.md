# 형태='제품' 추가 발주가 완제품(호기)으로 저장되던 결함 해소 (z1084)

- **작성**: 2026-08-11 (화) 16:27 KST
- **신고**: 이새롬 프로 (대표 전달·16:19) — 012T2606 방수검사기 개조건: *"제품으로 선택하여 등록하였습니다! 엑셀 업로드 파일은 제품으로 인식하여 업로드 되었는데, 새로 등록한 검사기 MODIFY건은 완제품·검사기로 인식됩니다"* (①완제품 배지 ②2호기·3호기 발번)
- **상태**: ✅ 원천 수정·시험 PASS 8/0·배포 · 🔶 기존 2건 정리 안내 + 대수 표기 규칙 별건
- **변경 파일**: `app/main.py` (추가 발주 분기 1곳)

---

## 1. 원인 — 추가 발주 경로만 형태 '제품'을 무시

| 경로 | 형태=제품 처리 |
|---|---|
| 신규 등록 | ✅ z1050 — 품목 줄(품명·규격·수량·단가) |
| 엑셀 업로드 | ✅ 제품 인식 정상 (이새롬 실측과 일치) |
| **추가 발주**(기존 관리번호+비신규 PO유형) | 🔴 PARTS(상품·z436)만 특별 처리, **SEMI(제품)는 장비(호기 분할) 경로로** → 완제품 배지(z699: 호기 모양 행 기준 계산)·"N호기" 자동 발번 |

`add_followup_order` 는 이미 items(z1050)를 지원 — **라우트가 연결을 안 했던 것**.

## 2. 수정 — 제품 항목을 추가 발주에도 연결

- 형태=제품이면 항목(it_*)을 읽어 items 로 저장: **호기 발번 없음** · 줄=품명·규격·수량·단가(금액=수량×단가) · 형태 배지 '제품' 계산
- 항목을 안 적은 제품 폴백도 호기 분할 금지(1줄·라벨=모델명/'제품')
- 형태=완제품 선택 시 기존 호기 발번 그대로(무영향)
- 시험 PASS 8/0: 실사례 재현(제품테이블,PUSHER 199,000 + R04 MODIFY 76,000 → 품명 2줄·호기 0건·SO합 275,000)·폴백·완제품 보존

## 3. 기존 저장 2건(T-260701-4 "2호기"·T-260716-2 "3호기") 정리

- T-260701-4 는 **세금계산서 1차 발행됨**(07-21 ₩199,000) → 소급 구조 변경 ⛔
- **라벨만 정정 가능**: 각 수주 카드 [✏️ 수정] → 호기 칸 "2호기"→"제품테이블,PUSHER" 등 품명으로 → [모두 저장] (발행 이력 무손상·화면 배지도 표시 계산이라 따라 바뀜)
- 실무자(이새롬 프로) 판단으로 정정 — 시스템 임의 보정 안 함

## 4. 🔶 별건(대표 결정 대기) — 수주액 패널 '×N대' 산정 규칙

- 현재 '×N대' = 수주 라인 행 수 → 개조·제품 줄도 '대'로 세짐(012T2606 이 "×3대"로 보인 이유·기존 제품 프로젝트도 동일)
- 완제품(호기)만 '대'로 셀지 / 제품·개조 줄은 '항목 N건'으로 나눠 표기할지 — **업무 규칙 결정 필요**
- ⚠ B단계 V7 기준 "장비대수 = 정식 호기 라인 행 수"(세션 05 실측)와 맞닿음 — 개조 행이 호기 라벨로 저장되면 자재 소요 대수도 오염 소지 → **세션 05 에 사실 공유 필요**(V7 정합 검토는 세션 05 몫)
