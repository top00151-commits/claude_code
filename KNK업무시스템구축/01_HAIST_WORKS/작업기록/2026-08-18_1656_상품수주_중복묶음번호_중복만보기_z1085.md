# 상품(부품) 수주 내역 — 중복 묶음 번호 + 중복만 보기 (z1085)

- **작성**: 2026-08-18 (화) 16:56 KST
- **요청**: 안지연 프로 (메신저 16:05 → 대표 전달) — 005M2511 BIM LINE 부품 1,194개 화면: *"중복된 항목을 쉽게 확인하기 어려움, 중복된 품목끼리 넘버링 또는 다르게 표시하여 확인하기 쉽게 요청"*
- **대표 지시**: 제안 ⓐⓑⓒ 중 **"a,c를 적용해줘"** (ⓑ 배지 클릭 이동은 제외)
- **상태**: ✅ 구현·시험 16/16·표준검사·렌더 실측 완료 · ⏳ **push 분류기 차단 — 대표 push 대기** (커밋 객체 `94e81c3a` 준비됨)
- **변경 파일**: `app/templates/project_detail.html` · `static/css/project_detail.css`

---

## 1. 배경 — z1026 의 후속

「중복 N」 배지 자체가 안지연 프로 요청으로 만든 기능(z1026·같은 규격이 한 수주에 2줄 이상이면 연한 강조+배지). 이번 신고 = 1,194줄에서 **모든 중복 줄이 같은 색·같은 배지라 짝을 못 찾는다**.

## 2. 구현 (화면만 — 서버·DB 무변경)

- **ⓐ 묶음 번호**: 배지 「중복 2」 → **「중복 ① · 2줄」** — 같은 규격끼리 같은 번호(첫 등장 순서 ①~㊿·51묶음부터 `#51` 폴백). 행 툴팁에도 묶음 번호. `<tr data-dupgrp="N">` 속성 신설.
- **ⓒ 중복만 보기**: 컬럼 그룹 칩 바에 단추 **「🔎 중복만 보기 (N묶음·M줄)」** — 누르면 `data-dupgrp` 없는 행 숨김(누른 동안만·⛔일부러 기억 안 함 — 새로고침=전체. 줄이 사라졌다는 오해 방지), 다시 누르면 「전체 보기」→복귀.
- 기존 의미 유지: 중복=오류 아님(납기 분할 정상) 문구 그대로. 빈 규격은 묶음 대상 아님.

## 3. 검증

- **시험 16/16** (임시 DB·TestClient): 묶음 번호 부여(①②)·행 속성 4행·버튼 문구 (2묶음·4줄)·유일 규격=요소 없음·**㊿/#51 폴백**·완제품 화면 무영향. ⚠경로 함정: `/projects/{id}` 는 `/project/{id}` 로 303 별칭
- **표준검사** `check_standards.py` 전부 통과 (JS 문법 게이트 186 스크립트 포함)
- **렌더 실측** (z1083b 교훈 — 레이아웃은 렌더 없이 배포 금지): TestClient 렌더 + CSS 인라인 → launch.json 정적 서버로 브라우저 로드. 기하 측정 — **배지 칸 침범 0건** · 토글 켜면 16→11줄(중복만)·끄면 복귀·라벨 전환 정상 · 행 높이 = 기존 방식(배지가 규격 아래 줄) 그대로. ⚠브라우저 팬이 화면에 없으면 픽셀 캡처 불가 → 기하(getBoundingClientRect) 실측으로 대체
- ⚠ `python`(3.12)엔 fastapi 없음 — **`py -3.13`** 사용

## 4. 🔴 브랜치 사건 — 내 실수와 복구 (동시세션)

1. 커밋했더니 **`wp04-baseline-transition` 브랜치에 들어감**(`36b4e28d`) — 다른 세션이 체크아웃해 둔 상태를 확인 안 함. ⭐**커밋 전 `git branch --show-current` 확인 필수**
2. 로컬 main = 원격(45b4a561) + 문서커밋(498461fc) + **0단계 커밋 5개(push 보류·WP-04 소관)** — 로컬 main 을 통째로 push 하면 **승인 안 된 0단계 코드가 운영 배포**되므로 ⛔
3. 해법 = **워킹트리·인덱스 무접촉 plumbing**: worktree add 는 이 저장소에서 "Could not reset index file" 로 결정적 실패(2회 재현) → 임시 `GIT_INDEX_FILE` + read-tree(원격 main) + update-index(--cacheinfo 로 blob 4개) + commit-tree
   - `5315d293` = 문서 2건(월간보고서 타당성실측+안내문·8/11 작성분) · `94e81c3a` = **z1085 코드 2파일** (원격 main 45b4a561 직계·diff 검증 = 정확히 4파일)
4. wp04 브랜치는 `git reset --keep HEAD~1` 로 원상복구(HEAD=edac8e0b·해시 확인 후 실행·타 파일 무손상)

## 5. ⏳ 남은 일 — 배포

- `git push origin 94e81c3a…:main` 이 **권한 분류기에 차단**(이날 3회) → **대표가 직접 push** 하면 미러→NAS 반영. 반영 후 LIVE 검증(버튼·배지·005M2511 실화면) 예정
- 이 기록 자체도 같은 plumbing 방식으로 후속 커밋 예정
