# 일괄등록 재업로드 완전 중복차단 (z982)

- **일자**: 2026-07-15
- **배경**: 대표 질문 "엑셀 추가로 업로드해도 중복은 걸러내는 거지?" → 정직한 답: **절반만**. 출하(SHIPPED)·송장·수금 상태 수주는 z820 재사용 후보(_REUSE_ST)에 없어, 같은 파일을 재업로드하면 그 행마다 시스템이 '새 발주'로 오인해 **중복 수주(SO)를 생성**하던 구멍(z659 `/admin/ti-from-xlsx`가 존재하던 이유). 대표 승인 "진행".
- **결론 한 줄**: 상태 무관(취소만 제외) 2차 탐색을 추가해 "같은 행=건너뜀"을 출하 수주까지 완성 — 이제 관리 엑셀을 통째로 재업로드해도 중복 0.

## 1. 동작 (main.py 일괄등록 followup 경로)
1. **1차 탐색(기존 z820)**: 활성 상태(_REUSE_ST) 수주에서 〔관리번호+발주일+납기+납품지+통화+담당자〕 일치 → 같은 호기(단가+비고/라벨) 있으면 행 건너뜀·없으면 호기 N줄 추가(z979). **변경 없음.**
2. **2차 탐색(z982 신설·1차 미발견 시)**: 같은 조건을 **취소 제외 전체 상태**에서 다시 찾음:
   - 같은 호기 있음 = 같은 파일 재업로드 → **행 건너뜀**(reused). 세금계산서(z979 '이 행 몫 호기' 겨냥)·so_form·모델명/장비명(z981 빈 곳 백필)만 수행하고, **상태·통화·담당자는 미변경**(출하 완료 기록 보존 — 옛 파일 재업로드로 출하가 '진행중'으로 되돌아가는 사고 차단).
   - 같은 호기 없음 = 출하 끝난 수주에 호기 추가 시도 → **차단 + 경고 표면화**: "출하 완료된 수주(M-…)와 발주 조건이 같은 행 — … 실제 새 발주라면 발주일/납기를 실제 값으로 올리거나 화면 '➕ 호기'로 추가하세요."
3. 부품·기타·소모품(1줄형, z678 금액키)은 원래 취소만 제외라 이미 안전 — 변경 없음.

## 2. 검증 — ✅ LIVE 실확정 입증 완료
- py_compile + 모듈 임포트 스모크(기존 z981 픽스처 4종 재통과) ✅
- **LIVE 인수 테스트**: 대표 엑셀(260626) 42행 통째 재업로드 확정 실행 →
  **42행 전부 reused(건너뜀)·신규 수주 0·신규 프로젝트 0·실패 0·경고 0** ✅
  전체 수 279 프로젝트/660 수주/1,670 호기/965 세금호기/649 명세호기 = **사전 스냅샷과 완전 동일** ✅
  병합 수주 세금계산서 멱등 ✅ (002M2602 TI1 6.7732억+TI2 1.4514억 · 005M2602 TI1 6.9692억 — 불변)
  의심 중복 목록 6건 그대로(기존 T코드 4 + 정상 동일금액 M 2) ✅
- 배포 체인: z982 `bb37247a`→미러 `1f8e305` / z982b `4fd772de`→미러 `fcf7b2a` → LIVE 확인 ✅

## 2-1. z982b — 실전 시험이 잡아낸 z978 잠복 버그 (같은 날 핫픽스)
첫 확정 시도가 **500**: z978이 확정(import-confirm)에서 **파서 지역변수 `_z978_hdr_fail`을 참조**(NameError) — z978 배포 이후 모든 일괄등록 확정이 죽는 상태였음(어제 py_compile·라우트 게이트 검증만 하고 실확정 미실행으로 놓침 · 500 지점이 행 루프 앞이라 **데이터 변경은 0**). 수정: 헤더 실패 시트=파서가 `_errors` 오류 행으로 실어 미리보기에서 즉시 표면화 + 확정은 오류 행(경고만 제외) 건너뜀(서버 안전망). **교훈: 파서/확정 같은 2단 흐름은 실행 검증까지가 검증.**

## 3. 운영 지침 변화
- (구) "새로 추가하는 행만 남기고 업로드" → (신) **"관리 엑셀 통째로 올려도 됨"** — 중복은 전부 걸러지고, 이상 상황(같은 조건인데 호기 구성이 다른 행)은 경고로 표면화.
- 재업로드로 갱신되는 것: 세금계산서/거래명세서(발행일·금액), 모델명·장비명(빈 곳), 활성 수주의 상태·통화·담당자. 출하 수주의 상태·통화·담당자는 보존.

## 4. 관련
- 선행: [z979 진단·병합수정](2026-07-15_일괄등록_엑셀대조_진단_z979.md) · [z981 수주별 모델장비비고](2026-07-15_발주별_모델장비비고_z981.md)
