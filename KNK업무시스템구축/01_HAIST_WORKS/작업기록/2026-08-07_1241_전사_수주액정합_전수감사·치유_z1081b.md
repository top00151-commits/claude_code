# 전사 수주액 정합 전수 감사 + 치유 — 76건 → 0건 (z1081b)

- **작성**: 2026-08-07 (금) 12:41 KST
- **지시**: 대표 — *"이거 진행해.. 비밀번호 내가 입력해줄께"* (z1081 §4 전수 조사)
- **상태**: ✅ 완료 — 감사 → 76건 발견 → 설계된 자가치유 실행 → **재감사 전 항목 0건**
- **접속**: SSH 열쇠 인증이 이미 설정돼 있어 **비밀번호 입력 불필요**했음 · **DB 읽기 전용**(mode=ro·SELECT만) 2회 + 치유는 **화면 경로(수정 폼 GET)** 로만

---

## 1. 감사 방법 (읽기 전용)

- 대상: 전사 프로젝트 319건(수주 보유 316건) — `projects.order_amount`·`expected_amount` vs `Σ(orders.total_amount)`
- 실행: SSH 표준입력 1회 스크립트(`scratchpad/audit_amount_mismatch.py`) · `mode=ro` URI · 서버 파일 0 · 쓰기 0
- 출력 최소화: 관리번호·상태·금액 3종·차이만 (품명·고객명 등 미출력)

## 2. 결과 ①: 1원(반올림) 오차 = **전사 0건**

z1081 유형(평균단가 반올림×수량) 오차는 확정·예상 모두 **0건** — 실사례는 014T2606 하나뿐이었고 이미 교정 완료. **z1081 원천 차단이 유일 사례까지 정리한 상태였음을 확인.**

## 3. 결과 ②: 별개 유형 76건 — 추가 발주 누적분이 헤더에 미반영(치유 대기)

- 전부 상태 '출하' · **order_amount < Σ(수주 합)** (추가 발주가 쌓였는데 프로젝트 헤더가 옛 값에 머묾 — 자가치유는 상세/수정 화면을 열어야 도는데 출하 완료 건이라 아무도 안 열었던 것)
- 규모: 최대 -8,038만(015T2601: 410만 vs 실제 8,448만) 포함 76건 — **목록·order_amount 소비처에서 수주액이 실제보다 낮게 보이던 상태**
- 원인 유형이 z1081(반올림)과 **다름**: 데이터 갱신 지연이지 계산 오류 아님. 코드 결함 아님(자가치유 설계 그대로)

## 4. 치유 (설계된 경로 그대로)

- 방법: 각 프로젝트의 **수정 폼 GET**(`/projects/{id}/edit`) 1회 호출 — 이 화면의 자가치유가 `order_amount = Σ(수주 합)` UPDATE (사용자가 화면을 열면 일어나는 동작과 동일·내 관리자 세션·본문 폐기)
- 76건 전부 호출 성공(실패 0) · SSH 로는 쓰기 안 함
- **재감사: 반올림 0 · 대폭 어긋남 0 · 예상 반올림 0 — 전 항목 0건** ✓

## 5. 남는 관찰 (후속 후보·급하지 않음)

- '출하' 종결 건은 앞으로도 아무도 안 열면 새 어긋남이 다시 쌓일 수 있음(추가 발주 시 헤더 동기화는 add-unit 등 경로가 하지만, 과거처럼 빠지는 경로가 또 있을 수 있음) → 근본은 **쓰기 시점 동기화 일원화**(z1077·z1081 방향). 새 어긋남이 관찰되면 그 생성 경로를 잡는 것이 정석 — 주기 감사 스크립트는 이번 것 재사용 가능
- 감사 스크립트 위치: `scratchpad`(세션 임시) — 필요 시 `_검증/`에 정식 등재 가능(대표 지시 시)
