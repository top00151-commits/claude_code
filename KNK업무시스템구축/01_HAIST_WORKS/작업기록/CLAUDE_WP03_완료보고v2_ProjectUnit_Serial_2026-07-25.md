# WP-03 완료 보고 v2 — 프로젝트 호기·일련번호 (검토판정 P0/P1 반영)

> ERP V1 WP-03 · 참조 시나리오 RS-01 · 게이트 검토판정(2026-07-25) "수정 후 승인" 대응
> 브랜치 `wp03-project-unit` @ `e9a7d1ae` (origin 반영) — **main 미반영 · LIVE 무변화**

## 0. 판정 확인
게이트가 제 테스트(19/19·98/98)를 재실행하고 시범 데이터(23=23)까지 재확인한 뒤, **P0 4건 + P1 다수**를 지적했습니다. **전부 사실이었고**(특히 "전참조 스캔"이 실제론 고정 목록 하나였던 과장), 자가 재현으로 확인 후 전부 수정했습니다.

## 1. P0 대응 (main 병합 전 필수)
| P0 | 무엇이 문제였나 (자가 재현) | 조치 | 실증 |
|---|---|---|---|
| **01 일련번호 생애주기** | `UNIQUE ... WHERE active=1`이라 **비활성 일련번호를 다른 호기에 재사용 가능**(재현됨). 호기당 활성 2개도 DB가 안 막음. 종료 메타 없음 | `UNIQUE(entity, serial_no COLLATE NOCASE)` **영구·대소문자무시 유일** + `UNIQUE(project_unit_id) WHERE active=1` **호기당 활성 1건** + 종료메타(`deactivated_at/by/reason`) + `supersedes` 연결 + `get_serial_history()` 조회 API · FK NO ACTION | §10-2·3·4·7 PASS |
| **02 씨앗 수량=1 미강제** | 정식 호기 라인 `qty=3`인데 **호기 1개만 생성**(수량 무시·재현됨) | 씨앗 전 정식 호기 라인 수량=1 **전수 검사 → 위반 시 전체 중단**(부분 씨앗 금지·위반 라인 표시) | §10-1 PASS(부분 생성 0) |
| **03 교체가 Change 이력 아님** | 누가·언제·왜 교체했는지 없음 | 교체 시 **사유 필수** + 이전행 종료메타 + 이전↔신규 연결 + 이력 조회 API/UI 사유칸 | §10-4 PASS |
| **04 법인 KOR 기본** | entity에 NOT NULL/CHECK 없음·충돌 무검사 | `entity NOT NULL CHECK(KOR/VN)` 양 표 + **법인 충돌 시 중단**. 빈값→KOR는 **국내 시범 한정**(확산 전 대표 확정 필요) | §10-6 PASS |

## 2. P1 대응
| P1 | 조치 | 실증 |
|---|---|---|
| 7.1 삭제 가드 = 진짜 전참조 | **FK 메타데이터 동적 스캔**으로 교체(고정 목록 폐지) — WP-04+ 새 표 FK 자동 포함 | §10-10 PASS(가짜 WP-04 표 참조→삭제 차단) |
| 7.2 원본 출처 소실 | `seed_order_no`·`seed_unit_label` **불변 스냅샷**(수주·품목 삭제돼도 보존) | §10-11 PASS |
| 8.1 씨앗 카운트 부정확 | `seed_order_item_id` 기준 **미씨앗 라인** 정확 계산 | §10-8 PASS |
| 8.2 skipped 뭉뚱그림 | `created`/`already_seeded`/`unit_no_conflicts` **분리 반환·표시** | §10-9 PASS |
| 8.3 권한 노출 | 상세 호기 링크를 **조회 권한자에게만** 노출 | §10-12 PASS |
| 9.1 unit_no 정규화 | 공백 제거 + `01호기`·`1 호기`→`1호기` | create/seed 반영 |
| 9.2 일련번호 대소문자 | `COLLATE NOCASE`로 `sn-1`=`SN-1` 동일 취급 | §10-3 반영 |
| 9.3 동일 재입력 | 같은 호기·같은 일련번호 재입력 = **무동작**(중복 이력 방지) | §10-5 PASS |
| 9.4 테이블 미생성 | 표 없으면 런타임 오류 대신 **'준비 중' 안내** | `_table_ready()` |

## 3. ⭐ 대표 결정 필요 (게이트가 대표에게 넘긴 2건)
1. **호기·일련번호 권한 세분화(게이트 8.3)** — 현재는 자재사용 권한 하나(`can_use_logistics`=제조7·구매10·admin/ceo/executive)로 생성·일련번호·삭제 전부 가능. 게이트는 **①호기 생성/씨앗 ②일련번호 발급/교체 ③호기 취소/삭제 ④조회 전용**을 나눌지 대표 확정을 요구. (현행은 시범용 최소안)
2. **전체 확산 시 법인(KOR/VN) 단일 기준(게이트 P0-04)** — 국내 시범(005T2601=KOR)은 승인. 285개 확산 전, 프로젝트에서 법인을 확정적으로 가져오는 기준과 "빈값·충돌 시 중단" 정책을 대표가 확정해야 함.

## 4. 검증
| 검사 | 결과 |
|---|---|
| `tests/test_wp03_project_unit.py` | **38 / 38 PASS**(함수 12 + 게이트 §10 12종 + 라우트/권한 매트릭스) |
| 시범 005T2601(신규 코드) | 호기 수 대조 **23 = 23** |
| WP-01 회귀 | **98 / 98 PASS** |
| `check_standards.py` | **전부 통과** |
| py_compile | ✅ |

## 5. 변경·커밋
- `86949ac1` WP-03 초판 · `d27fc903` 완료보고 · **`e9a7d1ae` 게이트 P0/P1 반영**
- **origin `wp03-project-unit` 반영 완료**(원격 백업·리뷰 — 게이트 §11-4)
- 파일: 마이그레이션·`project_unit.py`·`main.py`·`project_units.html`·`project_detail.html`·테스트

## 6. 남은 것·정직 고지
- 일련번호 **전체 이력 UI**는 WP-08(상태이력)과 함께 — 지금은 **데이터·조회 API 계약 확정**(활성 1건 + 개수 표시).
- **오등록 정정·장비 간 일련번호 이전**은 일반 재입력으로 불가(영구 유일). 필요 시 **별도 정정/이전 승인 흐름**을 후속으로(게이트 P0-01-3).
- 전체 285개 확산·버전(z1053)·BAT·§4는 **재검토 PASS + 대표 승인 후 병합 시**.

## 7. 다음 단계 (게이트 §11 순서)
P0/P1 수정 ✅ → 추가 테스트 ✅ → 완료보고 V2 ✅ → 원격 브랜치 ✅ → **재검토 판정** → 대표 승인 → main 병합 → 국내 시범 확인 → (법인 기준 확정 후) 전체 확산 → WP-04.
