# 규격 출하 이력 — 발주 바로가기를 「새 팝업창」으로 (z1089b)

- **작성**: 2026-08-24 (월) 13:32 KST
- **지시**: 대표 (2026-08-24) — z1089(같은 창 이동) 배포 직후 확인: *"관리번호를 누르면 신규 팝업창으로 보여달라는 것 같은데.. 비교해서 볼 수 있게"*
- **상태**: ✅ 구현·시험 14/0·표준검사·브라우저 클릭 실측 완료
- **변경 파일**: `app/templates/consumable_detail.html` 만

---

## 1. 동작 (z1089에서 바뀐 점)

- 팝업의 발주번호·관리번호 클릭 = 같은 창 이동(z1089) → **새 팝업창**(1280×860·크기조절 가능)으로 그 발주 상세를 엶
- **지금 화면은 그대로** — 두 발주를 나란히 놓고 비교 가능 (요청 취지)
- 발주별 창 이름(`knkco_{id}`): 같은 발주를 또 누르면 그 창 재사용, 다른 발주는 각각 새 창 → 여러 건 동시 비교 가능
- **팝업 차단 시 폴백**: 브라우저가 팝업을 막으면 기존처럼 같은 창 이동(기능 상실 방지)
- 안내문·툴팁도 "새 팝업창 · 나란히 비교"로 갱신. (현재) 표기는 그대로

## 2. 검증

- 시험 14/0 (z1089 10건 유지 + 4건: `_shPop`/window.open 존재·링크 onclick 연결·창 이름/폴백·안내문)
- check_standards(JS 문법 게이트) 전부 통과
- 브라우저 클릭 실측: window.open 스텁 후 실제 클릭 → 호출 인자 `(/consumables/1 · knkco_1 · width=1280,height=860,resizable,scrollbars)` 캡처 + **원래 화면 이동 없음** 확인
- 🔴 자체 발견(배포 전 수정): 창 이름을 `a.href` 숫자로 만들면 절대 URL의 IP·포트 숫자까지 섞임 → `a.pathname` 기준으로 견고화

## 3. 배포

- plumbing(원격 main f4de5097 직계) → 해시 지정 push → 미러·NAS 자동
