# 헤더 정리 — 워크스페이스 탭을 책갈피 줄로 이동 (z990)

- **일자**: 2026-07-17
- **배경**: 대표 캡처(창 폭 ~1040px) — 헤더 한 줄에 [로고]+[워크스페이스 탭 3개]+[KNK Eum WORKS 27px]+
  [아이콘 묶음]이 전부 있어 좁은 창에서 '자재구매센터'와 'KNK Eum WORKS'가 겹침. 측정 결과 탭+아이콘만으로
  ~1180px 필요 → 글자 축소만으론 근본해결 불가.
- **대표 선택**: "책갈피 줄로 이동 (권장)" — 참고 ERP와 같은 구조(고정 메뉴 탭+페이지 탭 한 줄).

## 1. 동작
- **PC(≥1024px)**: 헤더=[로고 — KNK Eum WORKS(중앙) — 아이콘] / 둘째 줄(z989 책갈피 줄)=
  **[통합 플랫폼|매출영업센터|자재구매센터 ┃ 📌책갈피 탭들… 🕘]**. 워크스페이스 탭은 항상 표시(책갈피 없어도),
  🕘만 책갈피 0개일 때 숨김.
- **폰(≤1023px)**: 기존 그대로 — 헤더 탭 유지(책갈피 줄 자체가 폰에서 숨김이므로 이동 없음).

## 2. 구현 (`chrome.html`, +19/−4)
- 마크업: 책갈피 줄 맨앞 `#knkPtabsWs` 추가.
- **단일 소스=헤더 `.top-tabs`**: spa_nav가 화면 전환마다 헤더 탭 active를 서버 렌더값으로 갱신하는 기존
  구조를 그대로 이용 — IIFE `syncWs()`가 초기+`knk:navigated`마다 헤더 탭 innerHTML을 복사(중복 정의 0,
  active 판정 로직 복제 없음). 헤더 탭은 ≥1024px에서 `display:none`(DOM엔 남아 소스 역할).
- 헤더 제목: `.tb-headline`에 ≥1024px `flex:1·중앙정렬·min-width:0·overflow:hidden` — 남는 공간만 쓰고
  절대 밀치지 않음.
- 책갈피 줄: `.on` 접힘 게이트 제거(워크스페이스 탭이 항상 있으므로 상시 표시)·패딩/테두리 기본화.

## 3. 검증
- 구문 3종(py_compile·Jinja·esprima 8,543자) ✅
- 동작 하니스 **19케이스 PASS**(기존 17 + z990 ①초기 복사 3탭·active=통합 ②헤더 active 변경 후 전환 시
  재복사=매출영업센터) ✅
- 레이아웃 하니스(실 design_v2+실 z989/990 CSS) **3개 폭**: 1280px·**1040px(대표 창 폭)** =
  3행(56/37/707)·겹침 0·헤더 탭 숨김·제목 중앙 / 800px = 2행 복귀·줄 숨김·헤더 탭 표시 ✅
- 배포: `dcb608e4` → LIVE `/api/version` 갱신 + 실화면 확인(§4)

## 4. z990b — 제목 실종 → 진범 발견 (8908653a)
- **증상**: z990 반영 후 'KNK Eum WORKS' 제목이 아예 안 보임(computed display:none, 규칙 출처 불명).
- **추적**: 스타일시트 전수 스캔(브라우저에서 cssText grep) → **`static/css/v5_main.css`**(템플릿 폴더 밖이라
  grep 범위에서 빠졌던 파일)에 2개 규칙 발견:
  ① `.tb-headline{position:absolute; left:50%; top:50%; …}` — 제목이 flex 흐름 밖 '화면 정중앙 절대배치'.
    **← 대표가 겪은 '탭과 글자 겹침'의 진짜 원인**(1500px대 폭에서 절대배치 제목 위로 탭이 지나감).
  ② v5H44: `@media(max-width:1499px){ .tb-headline{display:none !important} }` — 겹침 회피용 과거 대표 지시.
- **z990b 판단(→ 틀림)**: "탭이 내려갔으니 겹칠 대상이 사라졌다"고 보고 숨김만 해제(절대배치 유지).
  **LIVE 실측에서 반증**: `gap_to_icons = -29px` — 제목이 우측 아이콘 묶음과 여전히 29px 겹침.
  진짜 원인은 탭이 아니라 **절대배치 자체**였음(우측 아이콘 묶음 실측 **509px**로 중앙까지 침범).
  v5H44의 1499px↓ 숨김은 탭+아이콘 양쪽 때문이었고, 탭 이동만으론 절반만 해결됐던 것.

## 4-1. z990c — 근본 해결 (b01172de)
- **수정**: `.tb-headline`을 절대배치에서 해제하고 **헤더 flex 흐름 안**에 배치(≥1024px):
  `position:static·transform:none·flex:1 1 auto·min-width:0·text-align:center·overflow:hidden·text-overflow:ellipsis`.
  → 로고와 아이콘 '사이 남는 공간'만 차지 = **물리적으로 겹칠 수 없음**. 공간 부족 시 숨김이 아니라 말줄임(…).
- **검증(LIVE 실측·CSS 임시 주입 방식)**: 대표 창 폭 1029px에서 제목 표시 ✅ 폭 421px·글자 전부 표시 ✅
  로고와 12px·아이콘과 12px 여백 ✅ **겹침 0** ✅ → 프로브 제거 후 정식 배포.
- **교훈 2가지**:
  ① 헤더/셸 CSS는 템플릿(`_v5_partials`)만이 아니라 **`static/css/v5_main.css`에도 존재** — 셸 스타일 수정 전
    grep 범위에 static 포함 필수. 규칙 출처 불명이면 브라우저 스타일시트 전수(cssText) 스캔이 가장 빠름.
  ② **하니스가 실제와 다르면 즉시 버리고 LIVE 실측**: 이번 헤더 하니스는 우측 그룹을 2104px(실제 509px)로
    왜곡 → 신뢰 불가. 대신 **LIVE에 후보 CSS를 임시 주입해 측정 후 원복**하는 방식이 정확하고 빠름(서버 영향 0).
  ③ 겹침 문제의 정답은 대개 '숨기기/줄이기'가 아니라 **겹칠 수 없는 배치(흐름 배치)로 바꾸기**.

## 5. LIVE 실증 (대표 Chrome·창 폭 1029px·works.knknara.co.kr)
- **헤더**: [로고] — **KNK Eum WORKS**(표시 ✅·글자 전부 ✅·position static) — [아이콘 묶음],
  로고와 12px·아이콘과 12px 여백, **겹침 0** ✅ (대표가 보고한 증상 해소)
- **책갈피 줄**: [통합 플랫폼 | **매출영업센터**(활성) | 자재구매센터] ┃ [📌제작요청서목록 ×][일일업무 ×]
  [알림함 ×][C-260715-1 · 소모품… ×] — 워크스페이스 탭 3개 정상 복사·active 정확 ✅
- **격자**: 탭줄이 헤더 아래 자기 행, 사이드바·본문은 그 아래 — 겹침 0 ✅
- 스크린샷으로 육안 확인 완료(제목 중앙·탭 한 줄·본문 정상).
- 배포 흐름: `dcb608e4`(z990) → `8908653a`(z990b·부분수정) → **`b01172de`(z990c·근본해결)** → LIVE `0512bae`

## 6. 관련
- z989/z989b(책갈피 탭줄) 후속 — `2026-07-17_책갈피_페이지탭_z989.md` · 메모리 [[knk_page_tabs_bookmark]]
- ⭐교훈 재확인: hover 출렁임 금지(z989b)·.app 격자 행 추가 시 .sidebar/.main 명시 이동(z989 핫픽스)
