# 이번 세션 전체 정리 — 영업/업로드검증 작업 & '자재구매(세션 05)'와의 경계선

- **작성**: 2026-07-28 (화) 11:37 KST
- **목적**: 이 세션(HAIST WORKS 영업·프로젝트 측)이 한 일을 상세 기록 + **동시 진행 중인 세션 05(자재구매/구매팀·WP-04)와 업무가 중복·혼선되지 않도록 경계선을 분명히** 함.
- **핵심 한 줄**: 이 세션은 **영업/수주(매출) 측 화면·업로드 검증**만 손댔고, **자재·BOM·공급사·구매 코드는 한 줄도 안 건드렸다(읽기만).**

---

## 0. ⭐ 중복·혼선 방지 요약 (대표님 먼저 볼 것)

| 구분 | 이 세션(현재 창) | 세션 05(자재구매) |
|---|---|---|
| 영역 | 영업/프로젝트·수주(매출)·**엑셀 업로드 원본↔저장 검증 규정** | 자재구매(구매팀)·BOM·묶음A 실행승인·**구매팀 파서검증** |
| 건드린 테이블 | `orders`·`order_items`·`projects`·`customers` | `parts`(자재 마스터)·`bom`·`suppliers` (내 세션 **미접촉**) |
| 건드린 업로드 | 프로젝트 일괄등록·고객사·소모품/부품 **라인(수주 안)** | 자재 마스터(`/parts/import`)·BOM(`/bom/upload`)·공급사(`/suppliers/import`) |
| 대표적 커밋 | z1062~z1067 | 641a62d9·9bc84f72 (구매팀 최종본 파서검증·BOM 시범) |

**🔴 혼동 주의 (이름이 비슷함)**
- 내가 검증 붙인 **"부품 라인"(`import-parts-lines`)** = 프로젝트 **수주(SO)에 붙는 PACKING LIST 줄** = **`order_items` 테이블 = 영업/출하**.
- 세션 05의 **"자재"(`/parts/import`)** = **`parts` 테이블 = 구매 자재 마스터**.
- **둘은 이름만 비슷할 뿐 테이블도 목적도 다르다.** "소모품 라인"(`import-consumable-lines`)도 수주 order_items(매출)이지 자재/구매가 아니다.

**🔴 규정은 공용, 구현 주체는 조율 필요**
- 대표규정(§4 규칙47) "엑셀 업로드 원본↔저장 검증"은 **모든 업로드**에 적용 → 자재·BOM·공급사에도 적용돼야 함.
- 그건 **세션 05 영역**이라 **내가 ④(자재·BOM·공급사 검증)를 시작하지 않고 멈췄다.** → 이 세 업로드의 검증은 **세션 05가 넣거나 대표가 조율**. (규정 문구·공용 헬퍼는 준비돼 있음: §5 참조.)

**🔴 공유 파일 = `app/main.py`** (두 세션이 함께 편집)
- 매 푸시 전 `git pull`(merge)·커밋 전 **원격 z번호 확인** 필수. 이번 세션 내내 그렇게 처리(충돌 0).
- **`app/database.py`는 이 세션이 안 건드림**(자재/구매 로직은 거기 많음 → 세션 05 몫).

---

## 1. 이번 세션 작업 전체 (시간순 · z1062~z1067)

> 앞선 상세 .md: `2026-07-27_2038_..._z1061.md`(전수대조) · `2026-07-28_0923_..._z1063_z1064.md`(수량/모델장비) · `2026-07-28_0933_..._규정.md`(업로드검증 규정).

### 1-A. 소모품 매출/일괄등록 후속 (z1062~z1064) — 영업 측
- **z1063 소모품 수량 표시버그 고침**: 작업일정표 소모품 합산줄이 `orders.unit_qty(=1)`을 그려 실수량(order_items.qty=2·4·5…)보다 적게 보이던 것 → **order_items 수량합**으로. (데이터는 원래 정상·표시만 고침·부품 대분은 z974라 미변경.) 지상검증=덤프 `mode=board`.
- **z1064 모델·장비 빈칸 부분수정**: z981 前 일괄등록건이 수주별 모델·장비 미저장 → 화면이 프로젝트 대표값 폴백. **원본 재업로드(상태 리셋 위험)** 대신 **부분수정**으로 `orders.model_name/equip_name` 빈칸 **244건**만 채움(상태·통화·세금계산서·고객사 무변경).
  - 관련 파일/엔드포인트: `/admin/so-audit-dump`(+mode=board·+oid)·`/admin/so-fill-model-equip`·`/admin/model-equip-fix`·`/admin/so-fill-model-equip-run`·`app/model_equip_backfill_z1064.json`. **전부 orders/projects(영업)**·자재 무관.

### 1-B. 엑셀 업로드 '원본↔저장' 검증 규정 + 구현 (z1065~z1067)
- **규정 등재(§4 규칙47·절대준수)**: 모든 엑셀 업로드는 확정 후 원본↔저장 전항목 대조 → `✅ 일치 / ⚠ 불일치 목록`. 불일치=**경고+목록**(차단 아님·대표 확정).
- **① 프로젝트 일괄등록 = z1065**: `POST /projects/import-verify` + `_proj_import_verify_saved`(관리번호+금액≈+비고 매칭·단일품목만·병합/모호 '대조 보류'로 거짓경고 방지) → 모델·장비·수량·금액·통화·발주일 대조. 프런트 `_v5_partials/bulk_import.html`.
- **③ 고객사 = z1066**: `POST /customers/import-verify` + `_cust_import_verify_saved`(고객사명 정확일치·엑셀에 적은 칸만). 프런트 `customers_list.html`.
- **② 소모품·부품 라인 = z1067**: `import-consumable-lines/confirm`·`import-parts-lines/confirm` — 확정 시점에 **방금 넣은 order_items만 그 행과 1:1 인라인 대조**(파일 재전송 불필요·매칭 모호성 0) → 응답 `verify` → `project_detail.html` 확정 성공부에서 `knkVerifyMsg1024` alert.
- 공용 포매터 `_v5_partials/knk_verify_msg.html`(`window.knkVerifyMsg1024`) 재사용. **제품 상품 업로드는 z1025로 이미 검증 존재.**

**→ 현재 5/8 업로드 검증 LIVE**(①프로젝트·②소모품라인·②부품라인·③고객사·제품). 전부 **영업/수주(order_items·customers)**.

---

## 2. 이 세션이 '건드린 파일' 전체 (세션 05가 피해야 할 겹침 확인용)

- `app/main.py` — 위 z1063~z1067 함수/엔드포인트(전부 영업/수주/고객사). ⚠**공유파일**.
- `app/model_equip_backfill_z1064.json` — 일회성 데이터(orders 모델·장비 244쌍). 자재 무관.
- `app/templates/_v5_partials/bulk_import.html`(프로젝트 일괄등록) · `customers_list.html`(고객사) · `project_detail.html`(소모품·부품 라인 확정 표시).
- 작업기록 `.md` 다수 · `작업기록/디자인통일_현황판.md`(§4 규칙 45·46·47).

**건드리지 않은 것(=세션 05/구매 영역)**: `app/database.py`(`parts_bulk_import_excel` 등), `/parts/import*`·`/bom/upload*`·`/suppliers/import`, `parts`·`bom`·`suppliers` 테이블, `parts_import.html` 등 구매 화면. **읽기만(구조 파악)** 했고 수정 0.

---

## 3. ④ (자재·공급사·BOM) — 대표 지시 '검증층만' 진행 (2026-07-28 추가)

- **대표 결정**: 이 세션이 ④의 **검증층만** 진행(세션 05의 구매·파서·**저장 로직은 무접촉**).
- ✅ **공급사 z1068 · 자재 z1069 완료(LIVE)**: `database.py:suppliers/parts_bulk_import_excel`를 **안 고치고** `dry_run=True`로 파싱행만 얻어 저장값과 대조(공급사=이름·자재=part_no 정확일치). 폼POST라 확정 후 **결과 화면에 ✅/⚠ 서버렌더 + 목록 링크**. 자재 금액(환율·마진 산출)은 **세션 05 파서검증 영역**이라 텍스트필드(품명·규격·제작사·단위·통화)만 대조(거짓경고 방지).
- 🔴 **BOM만 잔여**: `/bom/upload/apply`·`app/bom.py` — **세션 05가 묶음A(BOM 시범검증)로 가장 활발히 편집 중**·프로젝트별 버전형 bom_items라 복잡. → **세션 05와 조율 후/신규 컨텍스트**에서 같은 '검증층만' 패턴(bom.py 무접촉·`bom_*` 파싱 재사용)으로. **이 세션은 bom.py를 건드리지 않았다.**
- (원문) 이들 업로드도 §4 규칙47(원본↔저장 검증) 대상. 공급사·자재는 위처럼 완료.
- ⚠**배선도 다름**: 자재는 **폼 POST+`/parts?` 리다이렉트**(fetch 아님) → 앞의 5곳(JS-fetch)과 방식이 달라, 검증 결과를 리다이렉트 랜딩 화면에 실어 보여주는 별도 배선이 필요.
- **권고**: 이 세 업로드의 원본↔저장 검증은 **세션 05(구매팀 파서검증과 같은 맥락)에서 함께 구현**하거나, 대표가 담당 세션을 지정. 공용 헬퍼(`knkVerifyMsg1024`)·규정 문구는 재사용 가능.

---

## 4. 배포·검증·산출물

- **LIVE 해시**: z1063 `300a7a0` · z1064 `d4d4195` · z1065 `2b55d37` · z1066 `c097b91` · z1067 `95b8f16`.
- **검증 방식**: 매 배포 monorepo→미러→NAS 반영 폴링 + `api/version` 확인. z1063=board 불일치 27→0. z1064=244/244 채움·② 회귀0. z1065=로컬대조 502행·보류142·불일치3(발주일 실갭·거짓경고0).
- **읽기전용 진단**: `/admin/so-audit-dump`(원본↔저장 덤프·`&mode=board`·`&dl=1`).
- **일회성 자산(정리 가능)**: `/admin/so-fill-model-equip-run`(apply=RUN-z1064 토큰)·`model_equip_backfill_z1064.json` — 남겨도 무해(멱등·빈칸만).

## 5. 재발방지 메모리(갱신됨)

- `feedback_excel_upload_verify_match`(절대준수·규칙47·진행 5/8) · `knk_so_level_model_equip_z981`(재업로드=소모품 상태리셋 위험→부분수정) · 현황판 §4 규칙 45·46·47.

## 6. 결론(대표 조율 포인트)

1. 이 세션 = **영업/수주·고객사 업로드 검증**(완료 5/8). 자재/BOM/공급사·구매 코드 **무접촉**.
2. **"부품 라인 ≠ 자재 마스터"**(order_items vs parts) — 이름만 비슷·서로 다른 것.
3. **④ 자재·BOM·공급사 업로드 검증**은 세션 05(자재구매)와 겹치므로 **담당·시점 조율 필요**. 규정·헬퍼는 준비됨.
4. 공유파일 `main.py`만 양 세션 공동 편집 → pull+merge·z번호 확인으로 충돌 0 유지 중. `database.py`는 이 세션 무접촉.
