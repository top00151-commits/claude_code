# [세션05 → 세션01] BOM 권한 — 통합 후보 1개 + 시험 화면 전달

- **작성**: 2026-09-09 (수) 09:08 KST
- **보내는 곳**: 세션05 자재구매 (빅터)
- **근거**: 대표 지시 2026-09-09 — *"완료하면 하나의 통합 후보와 시험 화면을 세션01에 전달하세요."*
- ⛔ **main 병합·운영 배포는 하지 않았습니다** (대표 별도 승인 전까지 금지)

---

## 1. 통합 후보 — 하나입니다

| 항목 | 값 |
|---|---|
| 가지 | `wp04-perm2` |
| 기준점 | `05f37d2d` (세션01 z1096b 포함본) |
| 커밋 | `b642a694` → `310493a0` (**2개**) |
| origin/main 대비 | **2 커밋 앞 · 병합 안 함** |
| 만진 파일 | `app/main.py` · `app/database.py` · `app/templates/bom_tools.html` · `deploy/check_bom_schema.py`(신설) · `_검증/` 2개 |

### 무엇이 바뀌나 — 두 줄 요약

1. **구매 서류 작업(단가·견적·발주·설계변경 반영)은 시작하기 전에** 단가·구매처 열람 권한까지 봅니다.
   예전엔 다 만든 뒤 **내려받는 자리에서야** 막아서, 사람에게 헛수고를 시키고 파일만 남겼습니다.
2. **DB 등급 칸 추가가 실패하면 크게 남깁니다.** 예전엔 `except: pass` 라 실패해도 아무도 몰랐고,
   그러면 등급이 늘 「불명」이 되어 **모든 파일이 조용히 제한**됐습니다.

### 세션01 이 특히 봐 주셨으면 하는 것

| # | 확인 부탁 | 왜 세션01 인가 |
|---|---|---|
| 1 | **메뉴·입구 연결이 그대로인가** — `test_menu_bom_entries_20260908.py` **30/0** 재확인함 | z1096·z1096b 는 세션01 작업물 |
| 2 | **홈 카드 3갈래**(구매/설계/조회)가 의도대로인가 — 화면 ①②③ | 공동 확인으로 닫은 항목 |
| 3 | 영업·매출 쪽 화면에 **영향이 없는가** | 세션01 소유 영역 |

⚠ **제가 만지지 않은 것**: `/admin/fx-rates*` · `_fx_load_rates` · `/fx/rates` · 매출/영업 경로 — 전부 무접촉입니다.

---

## 2. 시험 화면 — 서버 없이 파일만 열면 됩니다

📂 `C:\Users\top00\JR\Claude 코드\KNK업무시스템구축\01_HAIST_WORKS\작업기록\2026-09-09_0902_시험화면_BOM권한\`
👉 **`index.html` 부터 여십시오.**

> 그림이 아니라 **실제 화면을 그대로 찍은 HTML** 입니다.
> CSS·로고까지 파일 안에 넣어서, 서버를 띄우지 않아도 **모양 그대로** 보입니다.
> (넣지 못한 바깥 자원 **0개** 를 생성기가 스스로 확인합니다 — 아니면 실패로 끝납니다.)

| 파일 | 무엇을 보나 |
|---|---|
| `01_홈_구매.html` | 홈 카드가 **「구매 서류 만들기」** 로 뜬다 |
| `02_홈_설계.html` | 홈 카드가 **「설계 BOM 만들기」** 로 뜬다 |
| `03_홈_조회.html` | 홈 카드가 **「BOM 자료 보기」** 로 뜬다 |
| `04_도구_구매.html` | 구매 작업 묶음(단가·견적·발주)이 보인다 |
| `05_도구_설계.html` | 구매 작업 묶음이 **화면에 아예 없다** |
| `06_도구_SW.html` | 설계 작업만 · 남의 구매 자료는 🔒 자물쇠 |
| `07_도구_조회.html` | 만들기 없이 보기만 |
| **`08_시작전차단_단가.html`** | ⭐ **일을 시작하기 전에** 막는다 |
| **`09_시작전차단_발주.html`** | ⭐ 네 작업 **모두** 같은 검사를 지난다 |
| `10_정상통과_구매.html` | 대조 — 권한 갖춘 구매 담당은 **막히지 않는다** |

⛔ 전부 **시험용 가상 계정**(「시험○○」)이고, **운영 DB 는 열지 않았습니다.**
실명·연락처·거래내용은 한 건도 들어 있지 않습니다.

### 화면에 나오는 차단 문구

> ⛔ 이 작업은 **구매단가 · 구매처** 열람 권한이 있어야 시작할 수 있습니다.
> 만들고 나서 받지 못하는 일을 막기 위해 미리 확인합니다 — 구매팀에 문의해 주십시오.

### 헛수고를 시키지 않는다는 증거

생성기가 매번 스스로 확인하고, 아니면 실패로 끝납니다.

```
확인  파일 안에 못 넣은 바깥 자원 0개 (0 이어야 모양이 온전하다)
확인  차단 뒤 남은 기록 0건 · 남은 파일 0개 (둘 다 0 이어야 함)
```

다시 만들려면: `python _검증/make_bom_perm_screens.py`

---

## 3. 실측 — 이 변경으로 막히는 사람 **0명**

운영 DB **읽기 전용**으로 세어 본 결과입니다.

| 구분 | 인원 | 단가 열람 | 구매처 열람 | 새로 막히나 |
|---|---|---|---|---|
| 구매 실행권한 보유 | **16명** | 16명 | 16명 | **0명** |
| └ 본사 (팀 1·10·11·15) | 14명 | 정책 보유 | 정책 보유 | 아니오 |
| └ 법인 (팀 19·24) | 2명 | `role='admin'` | `role='admin'` | 아니오 |

**새 권한을 만들지 않았습니다.** 2026-07-05 부터 쓰던 `field_access_policy` 를 그대로 읽습니다.
달라진 것은 **보는 시점**뿐입니다(끝 → 시작).

---

## 4. 시험 결과 (전부 재실행)

| 시험 | 결과 |
|---|---|
| `deploy/check_bom_schema.py` (신설) | **6 / 0** |
| `test_bom_permissions_20260908.py` | **38 / 0** |
| `test_bom_tools_routes.py` | **32 / 0** |
| `test_bom_wp04_defects_20260907.py` | **9 / 0** |
| `test_menu_bom_entries_20260908.py` (세션01) | **30 / 0** |
| `test_material_stage.py` | **40 / 0** |
| `deploy/check_standards.py` | 전부 통과 |

> 📌 중간에 2건이 실패처럼 보였는데 **콘솔 글자표(cp949)** 로 결과를 찍다가 죽은 것이고
> 코드 결함이 아니었습니다. `PYTHONIOENCODING=utf-8` 로 재실행해 30/0 · 40/0 확인했습니다.

---

## 5. 곁들여 고친 것 1건 — 안내문

「전체 흐름 보기」 안내문이 **설계 담당자에게** *"단가·견적·발주서로 바로 들어가시면 됩니다"*
라고 적혀 있었습니다 — 권한을 나누기 **전에 쓰인 글**이라 이제는 못 하는 일을 하라는 안내였습니다.
구매는 그대로, 설계·조회는 *"단가·견적요청서·발주서는 구매 담당자가 맡습니다"* 로 바꿨습니다.
기능 변경이 아니라 말과 실제를 맞춘 것이며, 불필요하다면 이 한 건만 되돌리면 됩니다.

---

## 6. 아직 열려 있는 것 — 대표님 판단 대기 1건

**미분류 원본 열람 규칙**은 **기존 승인 근거가 없습니다.** 제가 2026-09-08 에 정한 것이고,
대표님께 그 항목만 따로 판단을 청해 둔 상태입니다.

- 지금: 우리 양식으로 읽히지 않는 **원본 파일**은 단가·구매처가 둘 다 든 것으로 간주 →
  **구매팀만 받을 수 있고 설계는 못 받습니다.**
- 걸리는 점: 설계자가 **자기가 올린 원본**을 되받지 못할 수 있습니다.
- ⚠ 실제로 몇 번 일어나는지는 **실측 없음**(보관함 기록 6건 · 원본 재다운로드 시도 기록 없음).

세션01 에서 손댈 것은 없고, 대표님 판단이 나오면 그때 반영하겠습니다.

---

## 7. 하지 않은 것

| 지시 | 지켰나 |
|---|---|
| 새 기능 추가 금지 | ✅ 기존 정책을 보는 **시점**만 옮김 |
| 법인 확산 금지 | ✅ 법인 권한 구성 무접촉 |
| VN 재고 계산 변경 금지 | ✅ 11-2 그대로 |
| main 병합 금지 | ✅ `wp04-perm2` 에만 |
| 운영 배포 금지 | ✅ 배포 명령 실행 안 함 |
