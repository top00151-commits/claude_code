# z1016 — 수주 상세(sales_order_detail) knk Sage 전환 · Phase 5⑤

- **일자**: 2026-07-19
- **대상**: `sales_order_detail`(수주 상세 — KPI·라인·송장/수금·상태이력·프로젝트 이력).
- **판별(크롬 실측)**: 전역 컴포넌트(.kpi-card/.tbl/.card/.bento/.section)만 사용 = quiet_v3가 Sage로 재정의 → **표준 --qv 브리지면 Sage**(로컬 --paper 커스텀 없음). sales_dashboard 실측으로 확인한 원리.

## 전환
- `body{--qv→Sage}` 브리지 · 머리글 `.page-head`→`knk-page-head`(breadcrumb·title·**meta 행 유지**=mgmt-pill·프로젝트·사업부·모델·날짜·배송지·통화·**so_status_pill**) · `@media .page-title`→`.knk-page-title` · `.main > .knk-page-head` Sage 밴드.
- 장식 "SALES ORDER DETAIL" 제거·title em 제거.
- **spot-fix**: 프로젝트 이력 필드 pill의 인라인 `var(--paper-2)`(전역 warm 토큰 직접 사용) → `var(--knk-bg-inset,#F6F7F4)`.
- **유지**: KPI 4·라인표·송장/수금 2단·상태이력·프로젝트 이력표·납품OVER 경고(#fff5f5)·다통화 경고(#fff8f7)=시맨틱 경고색·**금액 권한 마스킹(can_sales_amt·🔒)**·mgmt-pill·amber-deep 코드색.

## 검증 (전부 통과)
- Jinja ✅ · style 중괄호 7/7 ✅ · div 44/44·header 균형 ✅ · knk-page-head 1·breadcrumb·title·meta ✅
- --qv 브리지·Sage 밴드 ✅ · 잔재 0(page-head/title/meta·SALES ORDER DETAIL·#ffffff·var(--paper-2)) ✅ · kpi-card·tbl·so_status_pill·mgmt-pill·can_sales_amt 유지 ✅
- 크롬 LIVE 실측: 배포 후 /sales/orders → 상세로 확인.

## 배포 / 다음
- monorepo → 미러 → LIVE. (해시: 배포 후 기입)
- **Phase 5 남은(4)=전부 하드코딩 색 다수**(별도 per-color 기법·수고로움): sales_orders(hex133·분할/캘린더/드래그)·sales_shipments_receipts(hex57)·sales_opportunities(hex27)·sales_opportunity_form(hex76). → 깨끗한 컨텍스트 권장.
