# WP-01 P0 가드 시험 — z1060b 현행화로 6건 FAIL 해소 (세션 05 요청 · 대표 전달)

- **작성**: 2026-08-04 (화) 11:55 KST
- **요청**: 세션 05 자재구매 → 세션 01 (2026-08-04 11:17 요청서 · 대표 전달) — *"최신 main 에서 `tests/test_wp01_p0_guards.py` 6건 실패. ⓐ시험 노후 / ⓑ가드 회귀 판정 후 정리해 main 반영해 달라. WP-04 A단계가 ①에서 멈춰 있다"*
- **상태**: ✅ 완료 — **판정 ⓐ(시험 노후)** · 시험 갱신 후 **PASS 99 / FAIL 0**
- **변경 파일**: `tests/test_wp01_p0_guards.py` **한 개만** (⛔ `app/` 코드·`ui/bom-board-align`·`wp04-bom-parser` 브랜치·bom.py 등 세션 05 소관 전부 무접촉)

---

## 1. 판정 = ⓐ 시험 노후 (가드 회귀 아님) — 근거 4가지

| # | 근거 | 출처 |
|---|---|---|
| 1 | 시험 기대값은 **07-24 게이트 F-02** 기준: *"관리번호 있으면 무조건 완전삭제 잠금"* | 시험 파일 마지막 변경 `2f551ff0` (07-24) |
| 2 | **07-27 대표 지시 z1060→z1060b 로 정책이 정밀화**: #1002 빈 껍데기가 001T2607 번호를 쥔 교착 → **"실거래(출하·세금계산서·수금) 있으면 잠금, 0이면 완전삭제 허용"** | 작업기록 `2026-07-27_1906_완전삭제_운영잠금_숨김보관실거래0만허용_z1060.md` · 커밋 `848e5565`(z1060)·`ea284fd9`(z1060b) · 코드 `app/database.py` 7584~7618 |
| 3 | **대표가 직접 실사용 확인**: z1060b 후 #1002 삭제 성공 → 001T2607 재사용 가능 (작업기록 §5) | 같은 문서 |
| 4 | 실패 픽스처 `002M2599` 는 BOM만 있고 **실거래 0** → 현행 정책상 "삭제 허용"이 **올바른 동작**. 시험만 옛 기대값 | 시험 코드 + 실행 로그 |

**연쇄 구조**: 6건 중 [A7] 1건이 진짜 분기점(실거래 0 픽스처가 정당하게 삭제됨) → 픽스처가 사라져 [B4] 1건·[B5] 4건이 연쇄 실패. 한 원인, 여섯 증상.

**ⓑ 배제 확인**: 나머지 삭제 가드 전부 PASS 유지 — 이력 참조 차단·소모품 연결 차단·부모-자식 차단·혼합 열거 차단·비관리자 차단·환경 스위치 잠금·999/A 접두 판별. z1060b 당시 로컬 4케이스 검증(출하=차단·세금계산서=차단)도 기록에 있음. **삭제 차단이 뚫린 게 아니라, 대표 지시로 기준이 바뀐 것.**

## 2. 시험 갱신 내용 (현행 정책의 양면을 다 검증)

1. **픽스처 승격**: `002M2599`(PJ_REAL)에 **세금계산서 발행 수주 1건**을 붙여 '실거래 있는 운영 프로젝트'로 → [A7]·[B4]·[B5] 차단 기대 6건이 **현행 정책 그대로** 전부 유효해짐. 차단 문구 검사도 `"관리번호"+"실거래"` 로 강화.
2. **신규 검증 +1**: `998M2598` 실거래 0 껍데기(#1002 유형) → **force 완전폐기 허용** 확인 — z1060b 핵심(교착 해소)의 회귀 방지. → 총 98건 → **99건**.
3. 헤더에 갱신 이력·근거 명기(독립 문서로 읽히게).

## 3. §3.4 "종료코드 0" 재보고 — 시험은 이미 올바름

- 시험 마지막 줄이 원래부터 `sys.exit(1 if fail else 0)` — **실측: 수정 전 92/6 상태에서 종료코드 1 정상 반환**.
- 세션 05 가 0 을 본 원인(재현됨): `python … | tail` 처럼 **파이프를 걸면 셸 `$?` 가 tail 의 종료코드(항상 0)** 를 잡음. 나도 첫 실행에서 같은 함정에 빠졌음.
- 조치: 시험 파일 헤더에 ⚠경고 명기(자동화는 파이프 없이 실행 또는 `PIPESTATUS` 확인). 시험 로직 수정 불필요.

## 4. 검증

- 갱신 후 전체 실행 **PASS 99 / FAIL 0 · 종료코드 0** (2회 재현: 11:36 · 11:55)
- `deploy/check_standards.py` 전부 통과
- 임시 DB(`tempfile`)만 사용 — 운영 DB 무관

## 5. main 반영·후속

- 커밋: 시험 파일 + 본 작업기록 (해시는 push 후 완료 보고에)
- tests/ 파일이라 **운영 서버 동작 무영향** (미러·재배포는 표준 절차로 확인)
- **세션 05 는 ② 단일 통합후보 구성으로 진행 가능** — A단계 완료조건 ①(세션01 WP-01 시험 정리 최신 main 반영) 충족
