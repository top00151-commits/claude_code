# 검사기(T) 등록 — 프로젝트명 분야 드롭다운 (z1087)

- **작성**: 2026-08-19 (수) 17:05 KST
- **지시**: 대표 (2026-08-19) — 간편 등록 화면 캡처와 함께 *"검사기 등록시 'T' 프로젝트명은 'TSP, PBA, SENSOR, 전장'로만 정해지는 부분이라 드롭다운으로"* → 2차 확장 *"검사기 T일 때 **모든 형태** 동일 적용·수기 입력도 가능하도록 열어두고"*
- **상태**: ✅ 구현·시험 14/14·표준검사·렌더 실측 완료
- **변경 파일**: `app/templates/project_quick_form.html` 만 (서버·검증 무변경)

---

## 1. 동작

- **신규 등록(간편) 화면·검사기(T) 탭**: 프로젝트명 칸이 **분야 드롭다운** — `TSP · PBA · SENSOR · 전장 · 직접 입력…` (모든 형태 공통: 완제품·제품·상품·기타)
- **직접 입력…** 선택 시 자유 입력칸이 열림(개조·A/S 서술명 등 — 대표 "수기로 입력도 가능하도록")
- 제출 칸은 기존 `input[name=name]` 하나 그대로 — 드롭다운은 그 값을 채우는 역할만이라 **서버·검증·저장 무변경**
- 미적용: 다른 탭(자동화 등·기존 자유 입력) · **추가 발주**(이름은 기존 프로젝트 상속 — 드롭다운 자동 꺼짐) · **수정(ed) 화면**(기존 서술형 이름 보존)
- 배경: 검사기 제품 판매(AMS 소켓 등)의 프로젝트명이 분야명(PBA/SENSOR/TSP)으로 통일돼 있음(LIVE 실측 — SEMI 44건 대부분). ⭐분야명 표준화는 **월간보고서 분야 분류(PBA/TSP/SENSOR…) 자동화의 토대**가 됨(보류 중인 그 건 재개 시 활용)

## 2. 검증

- 시험 14/14 (임시 DB): 검사기 화면 드롭다운·옵션 4+직접입력·JS 연결 · 수정 화면엔 없음 · POST 등록(이름 PBA·제품·T) 정상 · 완제품 자유 이름 회귀 — ⚠시험 함정: USD 등록은 `fx_rate` 폼값 필수(fx_rate_required)
- check_standards(JS 게이트) 전부 통과
- **렌더 실측**(브라우저 JS): 검사기 초기(완제품)부터 드롭다운·SENSOR 선택=이름 저장·직접 입력 열림·형태 전환에도 수기 이름 보존·**추가발주 전환 시 꺼짐/신규 복귀 시 켜짐**·자동화(M) 탭은 형태 무관 미적용
- 🔴 자체 발견 결함 1건(배포 전 수정): `QF_BIZ` 전역이 초기 `qSyncShip()` 호출보다 **아래에서 선언**돼 참조 순서 위험 → DOM hidden(`#qBizDiv`)에서 직접 읽도록 변경

## 3. 배포

- z1085 패턴 plumbing(원격 main 직계·워킹트리 blob 사전 대조 일치 확인 후 편집) → 해시 지정 push → 미러·NAS 자동
