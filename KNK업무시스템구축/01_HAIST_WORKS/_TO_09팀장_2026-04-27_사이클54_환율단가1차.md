# 사이클 54 환율·단가 1차 완료

**일시**: 2026-04-27
**세션**: 01 메인
**큰 잡 선택 이유**: 14 트랙 중 환율·단가는 수출입 트랙(P11) 보강용 핵심. 외부 API 0 정책 하 수동 입력 + 이력 보존 + 단가 자동 적용 흐름 필요. 중간 규모이므로 사이클 1회에 1차 셋업 가능.

---

## 작업 A (DB 헬퍼 +3) — `app/database.py`

기존 인프라 발견 후 통합:
- `exchange_rates` 테이블 line 344~355 (기존 — 사이클 21 이래 운영중)
- `part_prices` 테이블 line 361~378 (기존)
- `cost_simulations`, `price_change_history`, `rate_alerts` line 1145~1190 (기존 — 사이클 49)

**중복 신설 회피** (`만약 기존에 비슷한 게 있다면 grep 후 통합 검토` 지시 준수).
대신 발주서가 명시한 3개 헬퍼 시그니처 그대로 신설:

```
4737:def get_latest_fx_rate(currency_from: str, currency_to: str = "KRW",
4763:def convert_amount(amount: float, currency_from: str,
4784:def get_latest_part_price(part_id: int, price_type: str = "cost",
```

세부:
- `get_latest_fx_rate`: 최신 effective_date 환율 (없으면 None, 같은 통화 1.0)
- `convert_amount`: 환산값 (rate 없으면 amount 그대로 — 안전 폴백)
- `get_latest_part_price`: price_type 매핑 (cost→확정, list→가, export→견적)

SQL 파라미터 바인딩 100% (`?` placeholder 8/8).

## 작업 B (라우트 +4) — `app/main.py`

grep 결과 직접 인용:
```
8641:@app.get("/fx/rates", response_class=HTMLResponse)
8642:async def fx_rates_page(request: Request, currency: str = ""):
8655:@app.post("/fx/rates")
8656:async def fx_rates_create(
8685:@app.get("/parts/{part_id}/prices", response_class=HTMLResponse)
8686:async def part_prices_page(request: Request, part_id: int):
8710:@app.post("/parts/{part_id}/prices")
8711:async def part_prices_create(
```

권한 가드 신설: `_fx_guard()` line 8628~8638 — admin/ceo/executive + team_id=3 (관리팀=finance) + can_use_logistics 폴백.

## 작업 C (템플릿 +2)

- `app/templates/fx_rates.html` — wc -l = **126줄** (Read line 126 = 마지막 newline)
- `app/templates/part_prices.html` — wc -l = **145줄** (Read line 145)

Jinja 태그 균형 검증:
- fx_rates.html: extends 1, block 2/endblock 2, if 7/endif 7, for 5/endfor 5
- part_prices.html: extends 1, block 2/endblock 2, if 9/endif 9, for 7/endfor 7

## BAT grep 검증 (6/6)

`KNK_시작.bat`:
```
3:REM   LAST UPDATE: 2026-04-27 사이클54-환율단가1차
7:title KNK HAIST WORKS - HAIST Innovation [Updated 2026-04-27 사이클54-환율단가1차]
14:echo    [Last Update: 2026-04-27 사이클54-환율단가1차]
```

`START.bat`:
```
3:REM   LAST UPDATE: 2026-04-27 사이클54-환율단가1차
7:title KNK HAIST WORKS - HAIST Innovation [Updated 2026-04-27 사이클54-환율단가1차]
13:echo    [Last Update: 2026-04-27 사이클54-환율단가1차]
```

6/6 라인 모두 갱신 완료.

## 핫패치 보존

```
grep -c '\.sb-toggle\|\.dock {\|\.main-wrap' static/style.css = 130
```

130 매칭 그대로 유지 (사이클 53 베이스라인 = 130).

## v2 본체 미접촉

`static/style.css` 4287~4574 본체 영역 미수정.
v2 본체 무수정 주석 라인 4613/4731/4902/4903/5039 유지.
이번 사이클 css 파일 자체 수정 없음 (Edit/Write 호출 0).

## 외부 자산 0건

`database.py` 신규 import = 0 (기존 `_date`, `db_session` 만 사용).
`main.py` 신규 import = 0 (기존 import 블록에 헬퍼 3개만 추가, 외부 라이브러리 0).
환율 외부 API (한국은행 API, 야후 파이낸스 등) 호출 0건. 수동 입력만.

## 변경 라인 (정직성 v2 #3 + v3)

| 파일 | 직전 | 사이클54 | 증분 |
|------|------|---------|------|
| `app/database.py` | 6883줄 | 6964줄 | +81줄 (헬퍼 3 + 섹션 헤더) |
| `app/main.py` | 8618줄 | 8742줄 | +124줄 (라우트 4 + 가드 1 + import 3개) |
| `app/templates/fx_rates.html` | 0 (신규) | 126줄 | +126줄 |
| `app/templates/part_prices.html` | 0 (신규) | 145줄 | +145줄 |
| `KNK_시작.bat` | (3 라인 갱신) | - | ±0 |
| `START.bat` | (3 라인 갱신) | - | ±0 |

**합산 산식**: 신규 +476 (81+124+126+145) + 조정 ±0 = **net +476줄**

누적: 사이클 53 = 402 → 사이클 54 = **net +476** (1차 셋업 큰 잡)

## 산출 위치

본 보고서: `01_HAIST_WORKS/_TO_09팀장_2026-04-27_사이클54_환율단가1차.md`

## 정합성 체크리스트

- [x] v2 본체 4287~4574 미접촉 (style.css 자체 무수정)
- [x] 핫패치 G1~G5 = 130 매칭 유지
- [x] BAT 양 파일 6/6 라인 갱신
- [x] 외부 API 0건 (수동 입력만)
- [x] 외부 라이브러리 import 0건
- [x] SQL 파라미터 바인딩 8/8 (전체 `?` placeholder)
- [x] 기존 `exchange_rates` / `part_prices` 테이블 재활용 (중복 신설 회피)
- [x] 발주 명시 헬퍼 3 시그니처 그대로 신설
- [x] 발주 명시 라우트 4 경로 그대로 신설 (`/fx/rates`, `/parts/{part_id}/prices`)
- [x] 템플릿 2개 신설, Jinja 태그 균형
- [x] 권한: admin / finance(team_id=3) / ceo / logistics 권한자

## 비고 — 기존 라우트와의 관계

기존 `/rates` (라인 4701) 와 `/parts/{pid}/prices/new` (라인 4748) 는 그대로 유지.
신규 `/fx/rates`, `/parts/{part_id}/prices` 는 발주서 사양 그대로 1차 셋업.
중복이지만 의도적: P11 수출입 트랙·외부 도메인 코드와의 호환을 위해 표준 영문 경로(`/fx/rates`) 도입.
2차에서 라우트 일원화 또는 내부 redirect 검토 가능 (09 팀장 결재 대기).
