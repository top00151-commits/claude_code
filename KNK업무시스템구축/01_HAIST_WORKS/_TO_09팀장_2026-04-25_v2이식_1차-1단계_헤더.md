# TO 09팀장 — 01 v2 이식 1차-1단계 (헤더) 완료
- 일시: 2026-04-25
- 발신: 01 메인(실무팀) 빅터
- 범위: base.html `<header class="topbar">` + topbar 관련 style.css 토큰만 (1단계)

## 1. v2 시안 §1 인용 (static/style.css line 4287~4574)
```
/* C안 (시안 12B 통째 이식) overrides — 2026-04-25 healing-c */
.topbar  : height var(--topbar-h) clamp(84~108px), grad-header, gap 16px, padding 0 28px
.topbar .brand / .brand-logo-wrap : 흰색 라운드 박스(64~88px) + 로고 img + dot
.topbar .brand-name / .brand-sub  : --fs-fluid-lg / --fs-fluid-xs
.tb-divider · .ws-wrap · .ws-switcher · .ws-menu · .ws-opt (워크스페이스 스위처)
.topbar .tb-center · .g-search (글로벌 검색바, shortcut 키)
.topbar .tb-right · .tb-icon-btn(.victor) · .lang-sel · .user-chip · .user-avatar
```

## 2. 변경 라인 수
| 파일 | 변경 | 비고 |
|---|---|---|
| app/templates/base.html | +1 / -1 | LAST UPDATE 라벨 1줄 추가 + `style.css?v=` 캐시 라벨 갱신 (`20260425healing-c2` → `20260425v2-stage1-header`) |
| static/style.css | +1 / -1 | line 4580 `.user-role` font-size 11px → `var(--fs-fluid-xs, 11px)` (토큰화) |
| **합계** | **2 / 2** | v2 본체 (line 4287~4574) 자체는 0줄 수정 |

## 3. base.html 헤더 마크업 정합 결과
- 시안 12B 클래스 풀세트 이미 정합 (이전 commit `c2fd06f` 시점에 base 통째 이식 완료)
- 본 1단계는 **마크업 추가 변경 0건** — 캐시 라벨/BAT만 갱신
- 보존 항목:
  - `brand-logo-wrap` + `.fallback` + `.dot` (line 19~26)
  - `WORKSPACES alias` = `.ws-wrap` / `.ws-switcher` / `.ws-menu` (line 29~53, 대표 직접 보는 메뉴 보존)
  - `g-search` 검색바 + `shortcut /` (line 57~62)
  - `lang-sel` 언어셀렉터 (line 73~77)
  - `user-chip` + `user-avatar` + `logout-btn` (line 78~88)
- 미접촉 (다음 단계): `.hm2-fixed` + `+ 새 업무 등록` 버튼 — 본 1단계 범위 외

## 4. 폰트 토큰 적용 매핑 (A2 결과 활용)
| 영역 | 클래스 | 토큰 |
|---|---|---|
| 헤더 brand-name | `.topbar .brand-name` | `var(--fs-fluid-lg, 1rem)` (line 4366) |
| 헤더 brand-sub | `.topbar .brand-sub` | `var(--fs-fluid-xs, .8rem)` (line 4382) |
| 검색바 input | `.g-search input` | `var(--fs-fluid-sm)` (line 4489) |
| ws 스위처 | `.ws-switcher` | `var(--fs-fluid-sm)` (line 4405) |
| user-name | `.topbar .user-name` | `var(--fs-fluid-sm)` (line 4579) |
| user-role | `.topbar .user-role` | `var(--fs-fluid-xs, 11px)` ← **본 단계 토큰화** |
| lang-sel | `.topbar .lang-sel` | `var(--fs-fluid-xs)` (line 4550) |
| 메뉴 sb-item | `.sb-item` (line 648) | `var(--text-sm)` — 본문 ≤ 비례 보장 |
| 메뉴 sb-head | `.sb-head` (line 647) | `var(--text-xs)` |

→ 헤더 텍스트 = md/lg 계열, 메뉴 = sm/xs 계열 — **메뉴 ≤ 본문 비례 만족**.

## 5. 핫패치 G1~G5 보존 grep
| ID | 룰 | 기대 | 실측 | 결과 |
|---|---|---|---|---|
| G1 | `<div class="sb-toggle">` (마크업) | 0 | 0 | PASS |
| G2 | `.dock { top: 0 }` 활성 | 0 | 0 | PASS |
| G3 | `body.dock-open .topbar { ... }` 활성 | 0 | 0 | PASS |
| G4 | `.dock { top: var(--topbar-h) ... }` | ≥1 | 2 | PASS |
| G5 | `body.dock-open .main-wrap { margin-right: 420px }` | ≥1 | 1 | PASS |

→ **5/5 PASS**.

## 6. BAT 갱신
- base.html line 7~8:
  - `{# LAST UPDATE: 2026-04-25 v2이식-1차-1단계-헤더 — 시안 12B base.html topbar 정합 (G1~G5 핫패치 보존) #}`
  - `<link rel="stylesheet" href="/static/style.css?v=20260425v2-stage1-header">`

## 7. 다음 단계 예고
- **v2 1차-2단계** = 페이지별 콘텐츠 영역 (홈/일일/달력/피드 등 main 영역 시안 12B 정합)
- **v2 1차-3단계** = `.hm2-fixed` + `+ 새 업무 등록` 버튼 + dock 본체 정합

## 8. 04 회귀팀 핸드오프
- 변경 표면: base.html LAST UPDATE 1줄 + 캐시 키 1건 + style.css 토큰화 1줄
- 회귀 포커스: 헤더 깨짐 / 캐시 무효화 동작 / G1~G5 5건 / WORKSPACES alias 동작
