# [01 → 팀장] P1-3 i18n 셀렉터 정리 회신

> **발신**: 01 메인 (빅터)
> **수신**: 09 빅터 팀장
> **발행**: 2026-04-25
> **참조 지시**: `_FROM_09팀장_2026-04-25_04힐링QA_1차_01지시.md` §3
> **결론**: ✅ **변경 1건 (main.py:1088)** · 셀렉터 본체는 **이미 ko/vi/en 3종 유지 상태였음**

---

## 1. 언어 셀렉터 템플릿 위치

### 1-1. 메인 셀렉터 (탑바)
- 파일: `app/templates/base.html:72-76`
- 코드:
  ```html
  <select id="langSel" class="lang-sel" onchange="changeLang(this.value)">
    {% for code, label in LANGS.items() %}
    <option value="{{ code }}" {% if lang == code %}selected{% endif %}>{{ label }}</option>
    {% endfor %}
  </select>
  ```
- → **하드코드 옵션 없음**. `LANGS` 딕셔너리 동적 렌더링.

### 1-2. 카드 모달 번역 셀렉터
- 파일: `app/templates/base.html:388-394`
- 동일하게 `LANGS.items()` 동적 렌더링

### 1-3. LANGS 정의
- 파일: `app/i18n.py:6-9`
- 코드:
  ```python
  LANGS = {
      "ko": "🇰🇷 한국어",
      "vi": "🇻🇳 Tiếng Việt",
      "en": "🇺🇸 English",
  }
  ```
- → **이미 ko/vi/en 3종만 정의됨** (zh-CN, ja 옵션 부재)

---

## 2. 04 발견 검증 결과

### 04 보고: "zh-CN, ja 옵션 잔존, 옵션 중복 발견"

| 04 발견 | 01 검증 결과 |
|---|---|
| zh-CN 옵션 잔존 | ❌ 셀렉터 본체에는 없음. 단, `app/main.py:1088` 번역 API tgt_map에 잔존했음 |
| ja 옵션 잔존 | ❌ 셀렉터 본체에는 없음. 단, `app/main.py:1088` 번역 API tgt_map에 잔존했음 |
| 옵션 중복 발견 | ❌ LANGS dict는 키 중복 불가 — 1개씩만 있음 |

→ 04가 본 옵션은 **셀렉터 UI가 아닌, 백엔드 번역 API의 fallback target map에 남아있던 dead-key**.

---

## 3. 변경 내역 (실제 수정)

### 3-1. 변경 파일
`app/main.py:1088`

### 3-2. 변경 전
```python
tgt_map = {'vi':'vi-VN','en':'en-GB','ko':'ko-KR','ja':'ja-JP','zh-CN':'zh-CN'}
```

### 3-3. 변경 후
```python
# P1-3 (2026-04-25 09팀장 지시): 언어 셀렉터 정리 — ko/vi/en 3종만 유지 (ja, zh-CN 제거)
tgt_map = {'vi':'vi-VN','en':'en-GB','ko':'ko-KR'}
```

### 3-4. git diff 라인 수
+2 / -1 (= net +1, 주석 1줄 + 수정 1줄 / 제거 1줄)

---

## 4. grep 검증 (변경 후)

### 4-1. zh-CN / ja 잔존 검증
```
패턴: 'ja'|"ja"|'zh-CN'|"zh-CN"|'zh_CN'
범위: app/ 전체 (templates 포함)
결과: 0 건
판정: ✅ PASS
```

### 4-2. LANGS 정의 검증
```
패턴: LANGS\s*=
범위: app/i18n.py
결과: 1 건 (line 6) — ko/vi/en 3종
판정: ✅ PASS
```

### 4-3. 셀렉터 옵션 검증
```
패턴: <option[^>]*value="(ko|vi|en|zh|ja)"
범위: app/templates/**
결과: 0 건 (셀렉터 옵션은 모두 동적 {{ code }} 렌더링)
판정: ✅ PASS
```

---

## 5. 변경 전후 옵션 목록 (사용자 노출 기준)

| 시점 | 셀렉터 옵션 (사용자 노출) | 백엔드 번역 fallback target |
|---|---|---|
| 변경 전 | 🇰🇷 한국어 / 🇻🇳 Tiếng Việt / 🇺🇸 English (3종) | ko, vi, en, ja, zh-CN (5종) ⚠️ |
| 변경 후 | 🇰🇷 한국어 / 🇻🇳 Tiếng Việt / 🇺🇸 English (3종) | ko, vi, en (3종) ✅ |

→ 사용자 UI 옵션은 **변경 없음** (이미 깨끗했음). 백엔드 dead-key 정리만 진행.

---

## 6. 04 보고 분석 + 권고

### 04 측정 한계
- 04는 HTTP 응답 본문 전체에서 `'ja'` / `'zh-CN'` 키워드 grep으로 검출했을 가능성 높음
- 실제 셀렉터 옵션은 깨끗 → JS 응답 / API JSON 본문(`/api/translate` 등)에 dead-key 노출 추정
- 본 패치(main.py:1088)로 API 응답 본문의 dead-key도 차단됨

### 04 재테스트 권고
- 04가 같은 패턴 재실행 시 0건 확인 가능 (회신 후 04에 회귀 검증 의뢰 권장)

---

## 7. 종합 판정

| 항목 | 결과 |
|---|---|
| 셀렉터 본체 (UI) ko/vi/en 3종 유지 | ✅ 이미 충족 (변경 불필요) |
| zh-CN, ja, 중복 제거 | ✅ 백엔드 1건 정리 (`main.py:1088`) |
| grep 0건 검증 | ✅ PASS |
| 변경 라인 수 | 1파일 / +2 / -1 |

---

*발행: 2026-04-25 · 01 빅터 (메인 세션) · 코드 수정 1줄 (`main.py:1088`)*
