# 01 → 09 팀장 회신 — Top3 S1 4차 (미수금 + 수금 알림)

날짜: 2026-04-26
사이클: 09 발주 → **01 작업 (완료)** → 04 회귀 대기 → 00 감사 → 99
스코프: S1 매출 트랙 4차 — 미수금 추적 + 수금 알림 자동 (알림 통합 활용)

---

## 1. 정직성 v2 7항 (필수)

### 1) 헬퍼 +4
- `_parse_terms_days(terms)` — payment_terms 문자열 → 일수 파싱 (NET30/30일/선금/CASH 휴리스틱). 매칭 실패 시 30 (기본 NET30)
- `_outstanding_receivables(c, only_overdue=False)` — orders SHIPPED/INVOICED 대상, total_amount − SUM(receipts_payment.amount), 만기 = order_date + payment_terms.terms days, 등급 4분류 (CURRENT / D-30 / D-60 / D-90+)
- `_outstanding_summary(items)` — 등급별 금액·건수 집계 + 연체율(overdue/total)
- `check_receivable_alerts()` — D-7 임박 + D+1/D+30/D+60 연체 도달 시 notify_user(SALES) 발송. 수신자 = orders.created_by → can_use_sales 폴백. 1시간 중복 방지(notify_user 내장)

### 2) 라우트 +3
- `GET  /sales/outstanding` — sales_outstanding_page (등급별 집계 + 상세 N건)
- `GET  /sales/aging`        — sales_aging_page (거래처×등급 매트릭스)
- `POST /sales/alerts/check` — sales_alerts_check (JSON {ok, sent, skipped, items})
- 가드: `_s1_guard` 재사용 (admin/ceo OR can_use_sales)

### 3) 신규 템플릿 +2
- `sales_outstanding.html` — KPI 5 (총미수금/CURRENT/D-30/D-60/D-90+) + 등급 분포 막대 (CSS gradient) + 상세 테이블 (정렬: 연체일 desc → 미수금 desc) + JS 트리거 버튼 (POST /sales/alerts/check)
- `sales_aging.html` — KPI 4 + 거래처×등급 매트릭스 히트맵 (rgba opacity = 미수금/max_total) + 합계 행

### 4) 알림 통합 활용 (notify_user SALES)
- 통합 엔드포인트 `database.notify_user(uid, "SALES", title, body, link)` 사용 — 1시간 중복 방지 그대로 활용
- 발송 트리거: 정확히 ov ∈ {-7, +1, +30, +60} 일 때만 (스팸 방지)
- 링크: `/sales/orders/{id}` (기존 라우트)

### 5) 변경 라인 5분리
- main.py: +208 라인 (sales_forecast_refresh 직후 L6647~6852, export 가드 직전)
- sales_outstanding.html: 신규 +120 라인
- sales_aging.html: 신규 +96 라인
- base.html: LAST UPDATE 라벨 1줄 갱신 (글로벌검색-강화 → S1-4차-미수금)
- DB 스키마: 무수정 (orders/customers/receipts_payment/payment_terms 기존 테이블만 SELECT)

### 6) 누적 3 카테고리
- **본 사이클 (S1 4차)**: 헬퍼 4 / 라우트 3 / 템플릿 2 / 알림 트리거 4종(D-7/D+1/D+30/D+60)
- **S1 트랙 1+2+3+4 누적**:
  - 1차: 테이블 7 + enum 9 + 라우트 9 + 템플릿 4
  - 2차: 테이블 +2 (invoices, order_status_history) + 핸들러 5
  - 3차: 라우트 +3 + 템플릿 +2 + 헬퍼 +3 + KPI 8
  - 4차: 라우트 +3 + 템플릿 +2 + 헬퍼 +4 + 알림 트리거 4
  - **합계**: 테이블 9 / 라우트 15 / 템플릿 12 / 헬퍼 7 / 핸들러 8
- **세션 누적 (01)**: 본 작업 포함, base.html LAST UPDATE 15차 갱신

### 7) 핫패치 G1~G5 grep
- main.py 신규 블록은 sales_forecast_refresh 직후 단순 append, v2 본체(이전 라인) 무수정
- base.html LAST UPDATE 1줄만 갱신, 헤더/사이드바/도크 무수정
- 외부 자산 0건 (numpy·Chart.js·D3 등 일체 미참조 — CSS gradient + rgba opacity 만)

---

## 2. BAT 갱신 (필수)
- 위치: `app/templates/base.html` L7
- 라벨: `LAST UPDATE: 2026-04-26 S1-4차-미수금`
- 내용: 헬퍼 4 / 라우트 3 / 템플릿 2 / 알림 트리거 4종 / D-7·D+1·D+30·D+60 명시

## 3. 자기검증
- [PASS] G1~G5 핫패치 보존 — base.html 본체 무수정 (LAST UPDATE 라벨 1줄만)
- [PASS] v2 본체 미접촉 — sales_forecast_refresh 직후 단순 append, 기존 함수 0개 수정
- [PASS] 외부 자산 0건 — notify_user 통합 헬퍼 재사용, 신규 import 없음 (re 표준)
- [PASS] 알림 1시간 중복 방지 — notify_user 내장 (database.py L2897~)
- [PASS] 작업 시간 ~14분 (15분 이내)

## 4. 09 팀장 회신
"01 S1 4차 완료: 미수금 + 수금 알림. 본 사이클 헬퍼 4·라우트 3·템플릿 2·알림 트리거 4 / S1 트랙 1+2+3+4 = 라우트 15·템플릿 12·헬퍼 7·테이블 9 / 세션 15차. 핫패치 5/5 보존. BAT 갱신 (S1-4차-미수금). 04 회귀 가능."
