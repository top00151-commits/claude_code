# [01 → 09] 환율·단가 관리 강화 1차 (Top10 #9 · P4 구매팀)

작성: 2026-04-26 | 빅터(01 메인) | 갭서베이 Top10 #9 — 마지막 사내 영역

## 요약
- 갭: `/rates`는 조회·수동등록만 (외부 환율 미연동, 원가 추적 약함)
- 본 사이클: DB 테이블 +3 / 인덱스 +7 / 라우트 +7 / 템플릿 +4
- 기존 `exchange_rates` + `part_prices` 그대로 활용 — 신규 테이블만 추가
- **외부 환율 API 0건** (수동 입력 + CSV 일괄 업로드만)

## 1. DB 스키마 +3 (database.py L1127~L1173)
- L1127~L1142 `cost_simulations` 원가 시뮬 (part_id/simulated_at/base·target_currency/exchange_rate/unit_price_base·target/margin_pct) + idx_csim_part·at
- L1144~L1158 `price_change_history` 단가 변경 (old·new_price/change_pct/effective_date/changed_by) + idx_pch_part·supplier·date
- L1160~L1172 `rate_alerts` 환율 알림 (target_currency/threshold/direction CHECK above|below/is_active) + idx_ralert_currency·active
- 모두 `CREATE TABLE/INDEX IF NOT EXISTS` (idempotent), ALTER 0건

## 2. 헬퍼 +7 (database.py L5824~L5937, +166 LOC)
- `cost_simulation_create` / `cost_simulations_list`
- `price_change_log` (변동률 자동 계산 = (new-old)/old*100)
- `price_change_history_list`
- `rate_alert_create` / `rate_alerts_list`
- `exchange_rates_csv_upload` (외부 API 미호출 · UPSERT 위임)

## 3. 라우트 +7 (main.py L6265~L6416, +159 LOC)
- L6278 `GET /rates/dashboard` 강화 대시 (KPI + 알림 + 30일 트렌드)
- L6299 `POST /rates/upload` CSV 일괄 (헤더: rate_date,from,to,rate,source,note)
- L6326 `GET /rates/cost-sim/{part_id}` 시뮬레이터
- L6342 `POST /rates/cost-sim` 시뮬 INSERT
- L6377 `GET /rates/price-history/{part_id}` 단가 변경 이력
- L6390 `GET /rates/alerts` 알림 목록
- L6401 `POST /rates/alerts` 알림 등록

## 4. 권한 분기 (main.py L6268)
- `_rates_guard(req)` — `can_use_logistics(u)` 위임 (구매팀 + admin/ceo/executive · 제조팀 team_id=7 읽기 허용 기본 정책 그대로)
- 7개 라우트 전건 가드 적용 — `Grep _rates_guard /main.py` → 8 hits (정의 1 + 호출 7)

## 5. 신규 템플릿 +4 (templates/, +373 LOC)
- `rates_dashboard.html` (97줄) · `rates_cost_sim.html` (121줄) · `rates_history.html` (64줄) · `rates_alerts.html` (91줄)

## 6. 변경 라인 5분리 (정직성 정책 v2 §3)
| 분류 | 파일 | 위치 | 라인 |
|---|---|---|---|
| HTML | base.html | L7 (LAST UPDATE 1행 교체) | +0 |
| HTML | rates_*.html (4종) | 신규 | +373 |
| Python | database.py | L1123~L1173, L5824~L5937 | +166 |
| Python | main.py | L4259~L4267 (import), L6263~L6416 (라우트) | +159 |
| CSS | static/style.css | (미접촉) | +0 |

## 7. 누적 3 카테고리 (정직성 정책 v2 §7)
- **본 사이클**: HTML +373 / Python +325 / CSS 0 = **698 LOC** (7파일)
- **Top10 #9 트랙 누적**: 1차이므로 = **698 LOC**
- **세션 전체 누적 ~3,800+ LOC**: 정확치 09 팀장 집계 위임

## 8. 핫패치 G1~G5 grep 보존 검증 (정책 §6 — 직접 인용)
```
static/style.css:2253  핫패치 G1·G5 영향 없음 (margin-right 시프트만)
static/style.css:4613  v2 본체(4287~4574) 무수정 · 핫패치 G1~G5 보존
static/style.css:4731  v2 본체(4287~4574) 무수정 · 핫패치 G1~G5 보존
static/style.css:4903  v2 본체(4287~4574) 무수정 · 핫패치 G1~G5 보존
static/style.css:5130  v2 본체(4287~4574) 무수정 · 핫패치 G1~G5 보존
static/style.css:5371  v2 본체(4287~4574) 무수정 · 핫패치 G1~G5 보존
static/style.css:5548  v2 본체(L4287~L4574) 미접촉 / 핫패치 G1~G5 영향 없음
```
- `Grep "핫패치 G1|G1~G5|G1·G5" static/style.css` → **7 hits 보존** (직전 QMS 1차와 동일)
- v2 본체 (style.css L4287~L4574) — 본 사이클 미접촉 (CSS 0건)
- base.html L95 핫패치 (sb-toggle) 보존 · L7 LAST UPDATE 라벨만 1행 교체

## 9. BAT 갱신 (KNK_시작.bat L3·L7·L14)
- L3: `LAST UPDATE: 2026-04-26 환율단가-강화-1차 (database.py +cost_simulations +price_change_history +rate_alerts ... 외부 환율 API 0건 ...)`
- L7: title `[Updated 2026-04-26 환율단가-강화-1차]`
- L14: echo `[Last Update: 2026-04-26 환율단가-강화-1차]`
- base.html L7 LAST UPDATE 라벨 정합 갱신
- 캐시 버전 미변경 (CSS 0건이므로 `?v=20260426text14tok` 유지)

## 10. 외부 환율 API 0건 명시 (절대 준수)
- `Grep "requests|urllib|httpx|aiohttp|fetch|api\\.|exchangerate" app/main.py app/database.py` (신규 코드) → **0 hits**
- 데이터 입력 경로: ① 기존 `/rates` 수동 폼 ② 신규 `/rates/upload` CSV ③ 신규 `/rates/cost-sim` 환율 직접 입력
- 외부 자산 0건 (로고·아이콘·라이선스 첨부 신규 0)

## 11. 자기검증
- G1~G5 7 hits 보존 · v2 본체 미접촉 · 외부 API 0건 · IF NOT EXISTS · _rates_guard 7/7 — OK

## 12. 후속 사이클 (2차 예정)
- 알림 발동 평가 잡 (수동 환율 등록 시 임계 비교 → triggered_at)
- `part_price_create` 호출부에 `price_change_log` 훅 연결
- CSV 다운로드 (현재는 업로드만)

## 04 회귀 가능 여부
- 가능. 4개 신규 화면 + CSV + 시뮬 INSERT P4 시나리오 수행 가능.
