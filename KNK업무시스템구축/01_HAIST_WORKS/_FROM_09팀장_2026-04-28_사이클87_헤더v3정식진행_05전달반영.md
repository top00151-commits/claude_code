# [발주] 사이클 87 — 헤더 재설계 v3 정식 진행 (05 → 01 전달 반영)

> 본 발주서: `KNK업무시스템구축/_TEAM_ORIENTATION.md` 참조 의무
> 발주: 09 팀장 (빅터) → 01 메인
> 발주 시각: 2026-04-28
> 우선순위: 🚨 긴급 (대표 직접 승인 + 1차 마감 +4h)
> 대표 지시: **"디자인팀에서 01실무팀에 전달한 사항 바로 진행시켜"**

---

## 1. 배경 (대표 직접 승인)

**참조 문서 (05 → 01 전달)**:
- `05_HAIST_WORKS_디자인팀/_세션01_전달/_TO_01_2026-04-28_헤더재설계_v3승인_가로탭+검색도크모드분리.md`
- `05_HAIST_WORKS_디자인팀/_세션01_전달/_TO_01_2026-04-28_핫패치LIVE변경전체보고_BAT인계.md`
- 시안: `05_HAIST_WORKS_디자인팀/03_시안/12B_PREVIEW_새헤더_가로탭_검색도크이전.html`

**대표 4단계 검토 통과**:
- v1 가로 탭 → v2 모드 탭 분리 → v2.1 tb-right 가시화 → **v3 ✅ 로고 축소 + amber 그라디언트 + 펄스**

---

## 2. 인계 사항 ① — LIVE 핫패치 7건 (이미 적용됨)

05가 이미 LIVE 적용 + BAT 갱신 완료. 01은 **인지 + 차기 코드 수정 시 보존 의무**:

| # | 파일 | 변경 |
|---|---|---|
| ① | `static/style.css` 3604~3626 | `.dock { top: var(--topbar-h, 84px); }` (헤더 가림 방지) |
| ② | `app/templates/base.html` 93 | `<div class="sb-toggle">◀</div>` 제거 |
| ③ | `app/templates/base_sales.html` 54 | 동일 패턴 제거 |
| ④ | `app/templates/base_logi.html` 54 | 동일 패턴 제거 |
| ⑤ | `static/style.css` 4342~4358 | `.brand-logo-wrap { background: transparent; box-shadow: none; }` |
| ⑥ | `static/style.css` 4342~4358 | 로고 사이즈 ↑ + `filter: brightness(1.2)` |
| ⑦ | `app/templates/login.html` 67~95 + 217~226 | PNG 로고 대형 중앙 + onerror SVG fallback |

→ 사이클 81/84/85 워커가 base*.html / style.css 수정 시 **위 7건 라인 보존 의무** (회귀 방지).

---

## 3. 인계 사항 ② — 헤더 v3 정식 진행 (즉시 작업)

### 3-1. 변경 범위 (5개 파일, main.py 변경 불필요)

| 파일 | 변경 |
|---|---|
| `app/templates/base.html` | ws-tabs 가로 / .tb-center 검색 제거 / brand-text 숨김 / dock 안 모드 탭 + 검색 패널 |
| `app/templates/base_sales.html` | ws-tabs 신규 / brand-text 숨김 / 도크 모드 탭 + 검색 / "통합 메인" 흡수 |
| `app/templates/base_logi.html` | 동일 |
| `static/style.css` | .ws-tabs / .ws-tab.active(amber + pulse) / .dock-modes / .dock-search* / 로고 축소 |
| (선택) `app/templates/base.html` 사이드바 | 도메인 그룹(매출/자재) 항목 제거 |

**main.py 변경 없음**: `workspaces` context 이미 모든 라우트 제공 (line 153).

### 3-2. 정식 사양 (05 발주서 §3~§5 그대로 채택)

- CSS 정식 사양: 발주서 §3 (line 46~277)
- HTML 정식 사양: 발주서 §4 (line 281~425)
- JS 정식 사양: 발주서 §5 (line 428~468)
- **05 발주서 코드 그대로 적용** (해석 변경 금지)

### 3-3. 마감 (05 발주 일정 준수)

| 단계 | 시점 | 산출물 |
|---|---|---|
| 1차 마감 | +4h | base.html ws-tabs + dock-modes + dock-search + style.css |
| 2차 마감 | +8h | base_sales / base_logi 동일 패턴 + 모바일 반응형 |
| 회귀 (04) | +12h | walkthrough W1~W5 + grep 15종 (사이클 88로 별도 발주) |

### 3-4. 자체 검증 grep 의무 (15종)

회신 시 첨부 (05 발주서 §7 그대로):

**A. 정합 (PASS = ≥1) — 9종**:
```
grep -c '<nav class="ws-tabs"' app/templates/base.html
grep -c '<nav class="ws-tabs"' app/templates/base_sales.html
grep -c '<nav class="ws-tabs"' app/templates/base_logi.html
grep -c '\.ws-tab\.active::before' static/style.css
grep -c 'wsActivePulse' static/style.css
grep -c 'data-mode="victor"' app/templates/base.html
grep -c 'data-mode="search"' app/templates/base.html
grep -c 'setDockMode' app/templates/base.html  (≥3)
grep -c 'gSearchInput' app/templates/base.html
```

**B. 결함 (PASS = 0) — 6종**:
```
grep -c '<button class="ws-switcher"' app/templates/base.html
grep -c '<div class="ws-menu"' app/templates/base.html
grep -c '<form class="g-search"' app/templates/base.html
grep -c 'tb-home-link' app/templates/base_sales.html
grep -c 'tb-home-link' app/templates/base_logi.html
grep -nE '\.brand-text\s*\{[^}]*display:\s*none' static/style.css | head -3 (≥1)
```

---

## 4. 지시 사항 (메모리 #1~#7 의무)

- **권한 팝업 0건** (메모리 #1): sandbox 차단 시 `dangerouslyDisableSandbox` 사용 또는 보고만
- **04 본업 깊이** (메모리 #2): 사이클 88에서 walkthrough W1~W5 회귀 별도 발주
- **05 본업 강화** (메모리 #3): 05가 이미 v3 정식 사양 + grep 15종 + walkthrough 5종 제공 — 본보기. 01은 이걸 그대로 적용
- **BAT 순차 race 방지** (메모리 #4): 01이 사이클 87 작업 완료 시점에 BAT 갱신
  - 갱신 양식: `2026-04-28 G7헤더재설계v3_가로탭amber펄스_빅터도크모드분리_로고축소_3base`
  - 또는 05 발주서 §6 제안 양식
  - 05가 이미 갱신한 G6 핫패치 7건 키워드 보존: "선행 G6 7건 보존 + G7 헤더v3"
- **main.py 순차** (메모리 #5): 본 사이클은 **main.py 변경 불필요** → 사이클 81/84/85와 병렬 가능
- **외부 자산 0** (메모리 #6): 05가 PNG/SVG 인라인 자산만 사용 — 외부 폰트·라이브러리 추가 금지
- **사이드바 본질** (메모리 #7): 5번째 변경 (사이드바 도메인 그룹 제거)는 선택 — 1회성/매출/자재 등 페이지가 워크스페이스 가로 탭으로 흡수되므로 사이드바 중복 제거 권장

---

## 5. 회신 형식

- **회신 파일**: `01_HAIST_WORKS/_TO_09팀장_2026-04-28_사이클87_헤더v3_회신_1차.md`
- 05 발주서 §10 양식 그대로 (착수/완료 시각 / grep 15종 표 / walkthrough 5종 표 / BAT 갱신 / 발견 이슈)
- 사이클 84/85 main.py 작업과 병렬 가능 — 다만 base.html 동시 수정 race 회피 (사이클 85의 base.html flash 인프라와 본 사이클의 ws-tabs는 충돌 영역 분리 — 안전)

---

## 6. 사이클 87 종료 후 연쇄

- 사이클 87 1차/2차 마감 후 **사이클 88 (04) 회귀 walkthrough** 발주 (별도)
- 사이클 88 PASS 후 **사이클 89 (00 감사) 통합 검증**

---

## 7. 정직성 v3

- 05 발주서 인용 정확 (line 번호 명시)
- grep 15종 + walkthrough 5종 약자 사용 시 원문 보존
- BAT 라벨 직접 인용 (현재 G6 라벨 line 3 Read 확인)
- 추정 0건 / 코드 사양은 05 원본 그대로
