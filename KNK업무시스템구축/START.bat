@echo off
REM ============================================================
REM   LAST UPDATE: 2026-05-04 v5H117 export CI/PL/BL/customs integrity hardening — (P1) commercial_invoice: negative amount block + reject states beyond DRAFT/BOOKED/CI_ISSUED + doc_audit_log. (P2) packing_list: pre-validate negative/zero lines with friendly redirect, header negatives blocked, 0-line submit blocked, post-SHIPPED/CLEARED/CANCELLED reject, doc_audit_log. (P3) bill_of_lading: post-CLEARED/CANCELLED reject + doc_audit_log. (P4) customs_declaration: declared_value negatives blocked, post-CLEARED reject, doc_audit_log. FTA H1 pattern (existence/state/lines/audit) applied uniformly across 4 export-doc domains. py_compile main.py PASS. v5H116 unaudited-domain pass + code quality — (F1) FTA·QC·WO issue/release routes hardened: existence/state pre-check, issued_by/released_by recorded (column-missing fallback), non-DRAFT friendly reject. (F2) New doc_audit_log table (auto-created) — unified lifecycle audit trail for fta_certificate/qc_inspection_report/work_order. (F3) FTA line validation: qty<=0 / unit_price<0 friendly reject; 0-line submit blocked. (F4) New API GET /api/parts/{pid}/active-price?currency=KRW — uses part_prices active row first, parts.unit_price fallback, with source field. (F5) po_form.html JS — after datalist match, async fetch active-price API and overwrite the fallback price (preserves any user edits made mid-fetch). (F6) database.py CURRENCY_OPTIONS = tuple(CURRENCIES) single-source-of-truth consolidation. py_compile main.py + database.py PASS. v5H115 collab domain audit (issues/tickets/board/changes) 8 fixes — (D1) issue_delete cascade — issue_logs explicit DELETE + dynamic safety net (any table with issue_id col → SET NULL or DELETE). Same pattern: ticket_delete(ticket_comments) / board_post_delete(board_comments) / change_delete(change_impacts+change_reads). (D2) issues/new title strip+required, severity·issue_type whitelist, cost_estimate negative block, status whitelist. (D3) tickets/new title·category required, urgency whitelist. (D4) board/new title required, category whitelist (BOARD_CATEGORIES). (D5) changes/new title·change_type required, urgency whitelist. urllib quote for Korean error redirect. v5H114 change-history UI 3 + PO form migration — (P1) get_user_history/get_team_history/get_quotation_history helpers (mirror project_history). admin_user_form/admin_team_form/sales_quote_detail routes fetch and pass to ctx. 3 templates get '📜 change history' card (edit mode only, with empty-state). (P2) PO form proper migration — po_form.html part name input wired to datalist(partsList) + hidden line_part_id_N. JS onPartNameInput() auto-fills part_id/part_no/last_price on datalist match (free-text fallback retained). _parse_po_lines_from_form pattern B reads line_part_id_N → FK auto-mapping (NULL fallback preserved for backward compat). v5H113 latent defects + LOW 9 autonomous fix (Victor 09 directly authorized). (P1) PO form template integrity — po_form.html uses line_name_N/line_qty_N/line_price_N/line_code_N pattern but route expected item_part_id[]/item_qty[]; submitted POs could record 0 lines silently. New helper _parse_po_lines_from_form() — pattern A (item_*[]) preferred, pattern B (line_*_N) fallback. Pattern B feeds part_no_snapshot/part_name_snapshot (DB layer already supports). Template adds hidden line_id_N for UPSERT. addLine JS updated. (P2) customer_history UI card — '고객사 변경 이력' card added to customer_detail.html (mirrors project_history). New get_customer_history(cid, limit) helper. customer_detail route fetches and passes. customers/edit logging expanded from name-only to 7 fields (name/biz_no/ceo_name/phone/email/address/is_active/note) full diff. (P3) LOW 9: (#15) part_price_create — currency whitelist (KRW/USD/VND/JPY/CNY/EUR), effective_from <= effective_to, unit_price >= 0. (#17) stock_adjust — reason required, qty != 0, |qty| <= 1,000,000. /stock/adjust route ValueError friendly redirect. (#18) user_history table + admin/users/{uid}/edit logs 6-field diff + password change separately. (#19) team_history table + admin/teams/{tid}/edit logs 6-field diff. (#20) supplier_create/update currency whitelist + name required + route friendly redirect. (#21) parts_create/update _validate_parts_payload: part_name required, currency whitelist, unit <= 8 chars, is_active 0/1, std_price >= 0, part_no duplicate friendly error. Route ValueError friendly redirect. (M9) Quotations header create forces total_amount = 0. (M10) quotation_history table + line add/delete auto-logged. (M11) /sales/quotations/{quote_id}/convert-to-order — orders.customer_id NULL fallback: backfill from quotation.customer_id; if still NULL, log warning to order_status_history. Module constants: CURRENCY_OPTIONS / PART_UNIT_OPTIONS / _validate_currency helper. py_compile main.py + database.py PASS. v5H112 logistics+sales defect batch fix (12 items) — Audit team (00) reported 21 defects → 09 lead delegated. HIGH 8 + MED 4. (H1) po_update id-based UPSERT preserves received_qty, rejects deletion of lines with received_qty>0. (H2) po_delete cascade safety net: po_items + stock_movements.po_id SET NULL + dynamic sqlite_master scan (v5H98 pattern). (H3) parts_delete cascade: po_items/quotation_items SET NULL + stock_movements/part_prices/parts_safety DELETE + dynamic. (H4) supplier_delete cascade: purchase_orders/part_prices SET NULL + dynamic. (H5) po_create validation: 0 lines / qty<=0 / price<0 ValueError with friendly Korean message. (H6) SO SSOT lock: quotation status=CONFIRMED rejects line add/delete. (H7) /sales/receipts rejects amount<=0. (H8) /customers/edit on name change cascades to projects.customer_name + new customer_history table (mirrors project_history) + auto log. (M12) /sales/shipments OVER block. (M13) po_receive cumulative receive > ordered qty block. (M14) stock_issue current_stock < qty block. (M16) exchange_rate_create rate<=0 block. 14 routes wrapped with try/except + JSONResponse / RedirectResponse?error= (urllib quote for Korean). v5H101 — CEO: 'set new due date during SO confirmation but form still shows old due' + new request 'want a change history log'. (1) project_detail / projects_edit_form auto-heal on entry (order_amount=SUM, due_date=MAX, order_date=MIN of SOs) (2) new project_history table (3) log_project_change / get_project_history helpers (4) projects_create_logi logs 'new registration' event (5) projects_update_logi auto-diffs 12 key fields (6) /projects/{pid}/quick-status logs status change (7) project_detail shows '📜 프로젝트 변경 이력' card with timestamp/user/field/before→after table. v5H100 unit line edit/delete logged with before→after — CEO: 'log changes between SO total before vs after confirmation'. items/edit · items/delete · add-unit now capture pre-update values → order_status_history note in 'before → after' format (e.g. '호기 라인 수정 [1호기] · 단가 5,000,000 → 4,500,000 / SO 합계 X → Y'). All unit price / qty / label / SO total deltas immediately visible in SO detail '상태 이력' table. v5H99 projects list drops stage col/filter — CEO: 'is stage column needed?'. v5H75 already hid stage from form but list still showed it → most rows looked contradictory '제안작성 + 진행중'. Removed stage column + stage filter dropdown, status color mapping refined (납품완료 done / 취소·보류 danger). DB stage retained for workflow. v5H98 project delete HTTP 500 fix — CEO: '011T2604 delete fails with HTTP 500'. Cause: a child table FK not in cascade list. Fixes: (1) projects_delete_logi adds dynamic safety net — query sqlite_master for all tables, if has project_id col → SET NULL first (preserve history), DELETE if NULL not allowed (2) route wraps in try/except → returns JSON with sqlite/FK error detail (visible in modal) (3) projects.html delete JS parses JSON for user-friendly error message. v5H97 form 4 improvements + inline status — CEO: 'currency display, export/domestic, status location, SO issuance method'. (1) ① basic info adds 거래구분(내수/수출) radio, projects ALTER is_export (2) ③ amount adds 통화(KRW/USD/VND) select + label simplified (3) status labeled 'initial only - editable later in detail' + default 초기협의 (4) project_detail sidebar status becomes inline select → onchange POST /projects/{pid}/quick-status saves immediately (5) 거래구분 pill display (export blue/domestic orange) (6) order amount display with currency suffix. v5H96 SO detail shows mgmt code + unit info — CEO: 'lines show None/None part no/name, no mgmt code visible'. Cause: unit lines have no part_id (so LEFT JOIN parts → None) / route didn't join projects. Fixes: (1) sales_order_detail route joins projects (mgmt_code/project_name/biz_div/model_name) (2) items normalize: part_name falls back to unit_label (3) header shows mgmt code (link to project) / project name / biz / model / ship_to / currency / status (4) line table columns: 호기/품번 - 품명/사양 - 수량 - 단가(currency) - 금액(currency) - 비고. v5H95 currency options KRW/USD/VND + symbols removed — CEO: 'use only KRW, USD, VND, no symbols'. (1) all select options: KRW/USD/VND only, no ₩/$ prefix (2) all displays show 'amount CODE' format (e.g. 5,000,000 KRW) (3) server validation adds VND. v5H94 order amount KPI single source of truth — CEO: 'qty 1 but order amount shows 2 units worth'. Cause: form value 10M → auto SO at 10M with 1 line → user inline edited line to 5M → SO/items synced but projects.order_amount stuck at 10M (form value). Fix: (1) project_detail route auto-heals: compare SUM(orders) vs projects.order_amount on page load → UPDATE if differ (2) KPI + sidebar amount displayed from SO sum (single source of truth) (3) ⚠ shown when mismatch detected. v5H93 SO list redesigned as cards — CEO: 'too hard to read, simplify'. 11-col cramped table → 1 SO = 1 card. (1) Card header: big SO no + status pill + meta (order/due/ship/currency) + actions (＋ unit/🗑) (2) Body: unit lines table (label/price/note/save·×) always visible, inline editable per row (3) Footer: qty N · total ₩X · ⚠ integrity warnings (4) ＋ unit inline panel (label/currency/price/note) (5) Below all cards: 'grand total' box (6) New APIs: POST /sales/orders/items/{iid}/edit · /delete (per-unit edit/delete with auto SO + project total recalc). v5H92 label rename + currency selector — CEO: '납품지→납품처, 호기수→수량, 금액→단가/금액 split, ₩/$ selectable on input'. (1) all template label renames (2) project_detail SO table 11 columns: SO no/order date/due/납품처/수량/단가(₩|$)/금액/통화/status/note/action (3) 단가 cell: uniform shows price, mixed shows '여러 단가 ▾' (4) currency column: editable shows ₩KRW/$USD select, else text (5) ALTER orders ADD currency, get_project_orders + sales_orders SELECT include it (6) form units row gets currency select column + 단가 label (7) confirm_order_multi groups by (due,ship,currency); INSERT includes currency (with fallback) (8) add-unit/quick-edit accept currency. v5H91 unit/amount integrity validation — CEO: '3 units but only 2 prices entered, no error appeared, should warn'. (1) get_project_orders computes mismatch_qty/mismatch_sum flags (2) project_detail unit cell ⚠ '호기 N/M' warning + amount cell ⚠ '합계 불일치 Δamount' (3) quick-edit server validation: when order_items exist, reject unit_qty change unless equals item count, reject total_amount change unless equals sum (with friendly guidance). v5H90 add unit with different price to same SO — CEO: 'in same SO, when extra unit comes with different price, reflect that too'. project_detail SO row gets ➕ 호기 button + inline form (label/amount/note). POST /sales/orders/{oid}/add-unit: order_items INSERT + orders.unit_qty++/total_amount += amount + unit_label appended + project sum sync + history. Rejects SHIPPED/INVOICED/PAID/CANCELLED. v5H89c row click 404 fix. v5H89b customer not visible — data linkage fix — CEO: 'looks like data not linked, customer not showing'. Root cause: projects stored customer_name(text) but left customer_id(FK) NULL → orders inherited NULL → JOIN failed. Fixes: (1) projects_create_logi/update_logi auto-map customer_name → customer_id (2) confirm_order_multi also looks up at SO creation (3) sales_orders SELECT triple-fallback cu.name → p.customer_name → pcu.name (4) Startup migration backfill: fill NULL projects.customer_id and orders.customer_id from project. Existing data auto-heals on next start. v5H89 sales orders list expanded — CEO: 'too few info columns; mgmt code, model, customer, etc not visible'. Route adds projects join (mgmt_code/project_name/biz_div/model_name/po_type) + dynamic SELECT for new orders columns (ship_to/unit_qty/unit_label) via PRAGMA. Template now 11 columns: SO no / mgmt code (project link) / project·model / biz pill (T/M color) / customer / units+labels / ship_to / amount / status / order date / due date. v5H88 SO suffix — CEO: 'why does T-260505 jump to -2, where's -1?'. v5H69 logic counted bare base as N=1 → next was -2 (skipping -1). Fix: bare base excluded from N count → 1st = base, 2nd = base-1, 3rd = base-2. v5H87 진행중 status also auto-issues SO — CEO: 'progress=진행중 means under production, but no SO means not in 수주관리 aggregation — contradiction'. v5H86 only issued mgmt code. Fix: POST /projects/new + /projects/{pid}/edit when status in WON_STATUSES AND no SO yet → confirm_order_multi with single unit (label='1호기', amount=order_amount, due_date=project.due) auto-issues SO. Hint updated to 'mgmt + SO together'. v5H86 status auto-issues mgmt code — CEO: 'selected 진행중 but mgmt code not issued'. Before: only stage in (수주확정,납품) triggered. Added: WON_STATUSES = (진행중,납품완료) also triggers → projects_create_logi/update_logi check status, auto-promote stage to 수주확정. Form hint updated. v5H85 per-unit price breakdown when units within an SO differ — CEO: 'when SO is the same but unit prices differ, how do we display?'. get_project_orders now fetches order_items per SO + computes unit_price_uniform flag. project_detail amount cell shows (1) all-same: 'N units × price' subtitle (2) mixed: '호기별 단가 ▾' collapsible details with each unit label + amount. Single-unit SO: no breakdown. — CEO suggestion: 'if we can edit the order quantity, it'll be easier'. project_detail SO list now shows unit_qty (number input) and total_amount (text + commas) inline; on change a 💾 button appears → POST /sales/orders/{oid}/quick-edit. Validation: qty>=1, amt>=0; rejects SHIPPED/INVOICED/PAID/CANCELLED SOs. Updates project.order_amount sum + writes history note. Requires can_use_sales. Direct quantity adjustment as simpler alternative to multi-SO grouping flow.
REM   Rule: 01 session bumps this line every time code is modified
REM ============================================================
cd /d "%~dp001_HAIST_WORKS"
title KNK HAIST WORKS - HAIST Innovation [Updated 2026-05-04 v5H116 unaudited-domain pass + code quality (FTA/QC/WO issue validation · doc_audit_log · active-price API · CURRENCY single source)]

echo.
echo ============================================================
echo    HAIST WORKS  ^|  KNK Integrated Work Platform
echo    Human ^& AI create the Best
echo    [Last Update: 2026-04-29 G25_v4_CX23c_마스트헤드제거 (대표결재: 나)제거안) 3 base (통합/매출/자재) 상단 매거진 마스트헤드(VOL.NO + EDITION) 라인 일괄제거 → 일반 사무실 시스템 톤 + topbar-h 125→80px + dock top 80px 정합]
echo ============================================================
echo.

REM -- Check Python --
where python >nul 2>nul
if errorlevel 1 (
    echo [ERROR] Python is not installed.
    echo         Install Python 3.10+ from https://www.python.org/downloads/
    echo         Make sure to check "Add Python to PATH" during install.
    pause
    exit /b 1
)

REM -- Auto-install required packages on first run --
python -c "import fastapi, uvicorn, jinja2" >nul 2>nul
if errorlevel 1 (
    echo [First Run] Installing required packages, please wait...
    echo.
    python -m pip install --upgrade pip
    python -m pip install -r requirements.txt
    if errorlevel 1 (
        echo.
        echo [ERROR] Package installation failed.
        echo         Check your internet connection and try again.
        pause
        exit /b 1
    )
    echo.
    echo [OK] Installation complete.
    echo.
)

REM -- Open browser after 4 seconds --
start "" /b cmd /c "timeout /t 4 /nobreak >nul && start http://localhost:8081"

REM -- Run server --
echo Starting server on http://localhost:8081 ...
echo Press Ctrl+C to stop.
echo.
python run.py

echo.
echo Server stopped. Press any key to close.
pause >nul
