# 작업 기록 — 2026-05-21

> **세션**: 실무팀1 통합플랫폼 (빅터) · 워크트리 sad-johnson-eddf3c
> **범위**: 워크플로우(일순서) UI/UX 정비 → 서버 운영 안정화 → 외부 상표명 정리(법적 리스크 제거) → AI(Claude/OpenAI) 연동 기반
> **버전 구간**: v5H226z108n ~ z108n19

---

## 1. 한눈에 보기

| 단계 | 핵심 작업 | 결과 |
|---|---|---|
| ① 워크플로우 UI | 체크리스트 행 압축·정렬, 디버그 영역보기 확대 | 가독성·공간활용 개선 |
| ② 업무 철학 반영 | 잠금(🔒) 제거 → "누락방지 체크리스트"로 전환 | 부서별 병행 진행 자유 |
| ③ 체크포인트 편집 | 노드 상세에서 추가/수정/삭제 | 실무자 자유 편집 |
| ④ 버그 수정 | IC 페어 중복, 깨진 링크(/quotations 등) | 오류 제거 |
| ⑤ 서버 안정화 | uvicorn 좀비 워커 자동 청소, 라우트 누락 복구 | 재시작 안정 |
| ⑥ 상표 정리 | 카카오톡·위하고·Slack 등 실명 제거 | 법적 리스크 제거 |
| ⑦ AI 연동 | Claude/OpenAI 공급사 무관 구조 + 키 등록 도우미 | 결제되는 쪽 즉시 사용 |

---

## 2. 워크플로우(일순서) UI/UX 정비

### 체크리스트 행 정리 (z108n ~ n5)
- 행 높이 축소: 여러 줄 → **한 줄 50px 고정**
- 부서·담당자 **인라인 결합** (`기술영업팀 · 홍길동`)
- 제목 한 줄 말줄임 + 호버 시 전체 표시
- `overflow:hidden` 으로 내용이 안 보이던 문제 수정 → 제거 + `min-width:0`
- 컨트롤(상태/잡기/칩) 항상 또렷·조작 가능 (opacity 제거)

### 디버그 "영역 보기" 확대 (z108n3)
- 자동 라벨 패턴 22종 추가: `box / grid-2 / list-row / matrix / ic-card / section / card / wiz-*` 등
- 디버그 모드(`?debug=1`)에서 빈 박스 없이 전 영역 라벨 표시

---

## 3. 업무 철학 반영 — 잠금 제거 (z108n8, n10)

**대표 의도**: "순서 강제"가 아니라 **"빠트리지 말자(누락방지)"** 가 목적.
다른 부서는 앞 단계가 진행 중이어도 **선행 작업 병행** 가능해야 함.

- 🔒 자물쇠 아이콘·잠금 배경·"잠금" 버튼 **전부 제거**
- 대신 ⓘ 힌트: "앞 단계 N개 진행 중·대기 (참고용, 선행 진행 가능)"
- 노드 상세 페이지의 "아직 시작할 수 없습니다" 경고 → **참고용 안내**로 변경
- 모든 행에서 `+ 잡기` / `✓ 끝` 즉시 가능
- 자동 핸드오프(완료 시 다음 담당자 알림)는 유지 — 단, **강제 아님**

---

## 4. 체크포인트(확인 항목) 편집 기능 (z108n9)

노드 상세 페이지 "✓ 확인할 것들" 섹션에서:
- **추가**: 하단 입력창 + Enter / [+ 추가]
- **수정**: 라벨 클릭 또는 ✎ → 인라인 편집 (Enter 저장 / Esc 취소)
- **삭제**: ✕ (확인 후)
- 완료 개수(0/3 등) 자동 갱신

**백엔드 신규 라우트** (`workflow_builder.py`):
- `POST /workflow/node/{id}/checkpoint/add`
- `POST /workflow/checkpoint/{cp_id}/edit`
- `POST /workflow/checkpoint/{cp_id}/delete`

---

## 5. 버그 수정

| 버그 | 원인 | 수정 |
|---|---|---|
| IC 페어 생성 시 `UNIQUE constraint failed` | 같은 날 마법사 재실행 → pair_code seq 충돌 | 오늘자 최대 seq 이어가기 + 충돌 자동 재시도 + `W{wf_id}` 폴백 (z108n6) |
| 노드 "관련 시스템 바로가기" 404 | `/quotations` 등 미존재 경로 | 실제 경로로 교정 + 새 탭 열기 (z108n11, 마이그레이션 `m_z108n11_fix_sys_links.py`) |

**시스템 링크 교정표:**
- `/quotations` → `/orders/new`
- `/orders/po` → `/materials/requests`
- `/stocks` → `/parts`
- `/work-orders` → (숨김)

---

## 6. 서버 운영 안정화

### uvicorn 좀비 워커 자동 청소 (z108n12)
- **증상**: BAT 창 강제 종료 시 자식 워커가 살아남아 포트 8081 점유 → 재시작 실패
- **수정**: `KNK_시작.bat`·`START.bat` 시작 직전 좀비 자동 청소 (포트 점유 PID + 부모 죽은 spawn_main 워커)
- **신규**: `KNK_중지.bat` (수동 정리용)

### 사이드바 3개 메뉴 404 근본 수정 (z108n13)
- **원인**: `workflow_builder.register_workflow_routes()` 가 main.py에 미등록 + `/catalog` 라우트 자체 부재
- **수정**: 라우트 등록 추가, `/catalog` 복구

### ⚠️ main.py 라우트 누락 사고 (z108n14, n16) — 중요
- **사고**: `git add -A` 가 다른 워크트리의 **축소된 main.py(411 라우트)** 를 2회 빨아들여 `/catalog·/workflow·/materials` 등 30개 라우트 소실
- **복구**: 정상본(441 라우트)에서 main.py만 재복구
- **재발 방지 규칙 확정**:
  - ✅ **`git add -A` 금지** → 수정한 특정 파일만 `git add <파일>`
  - ✅ 커밋 전 라우트 수 확인 (`grep -c "^@app\."`)
  - ✅ 여러 세션이 같은 공유 파일(main.py) 동시 편집 시 영역/시간 분리

---

## 7. 외부 상표명 정리 — 법적 리스크 제거 (z108n15)

**대표 지시(2026-05-21)**: "위하고·카카오톡 등 상용 프로그램 직접 거론 금지. 과거 내용도 제작 지장 없으면 삭제."

### 제거 대상
카카오톡/카톡/카카오워크 · Slack · Telegram · Teams · Discord · Notion · 잔디 · 두레이

### 처리 결과
- **01_HAIST_WORKS/app**: database.py(TICKET_SOURCES `카톡`→`메신저` 등), main.py, victor.py, 레거시 템플릿 → **0건**
- **10_KNK_Messenger** (docs 제외): 코드 주석 중립화(`PC 메신저 스타일`·`스레드`·`고급 필터` 등), 시드 메시지, 설치안내문 HTML(`카카오톡처럼`→`익숙한 메신저처럼`), README·배포문서 비교표(경쟁사→`상용 A~D`) → **0건**
- 양쪽 앱 구문 검증(ast.parse) 통과, 기능 영향 0

### 미접촉 (타 세션 소관)
- 하이웍스(Hiworks) 식별자(`hiworks_*`·URL·라우트)는 실연동 대상 + 다른 세션(charming-yonath)이 "외부 그룹웨어"로 단계적 익명화 진행 중
- CAD/PLM 도구명(Inventor·Altium·OpenBOM·Git)은 변경소스 식별자로 기능상 유지

---

## 8. AI(Claude/OpenAI) 연동 기반 (z108n17 ~ n19)

**대표 결정**: Victor 고도화 + 한↔베트남 번역 + 문서 요약·작성. 키는 **OS 환경변수** 저장(보안).

### 공급사 무관 구조 (lock-in 없음)
| 공급사 | 환경변수(키) | 기본 모델 |
|---|---|---|
| Claude | `ANTHROPIC_API_KEY` | claude-sonnet-4-5 |
| OpenAI | `OPENAI_API_KEY` | gpt-4o-mini |

- 선택: `KNK_AI_PROVIDER`(claude/openai), 미설정 시 있는 키 자동 감지
- 나중에 **환경변수만 바꿔 전환** (코드 수정 0)

### 신규 파일
- **`app/ai_client.py`** — 중앙 래퍼
  - `ai_available()` / `ai_status()` / `ai_chat()` / `ai_translate()` / `ai_summarize()`
  - 키·SDK 없거나 결제 전이면 **안전 비활성** → 기존 기능 그대로 폴백 (시스템 영향 0)
  - Claude 프롬프트 캐싱, OpenAI 자동 캐싱
  - 키 원문은 절대 노출 안 함 (마스킹: `sk-ant-…XXXX`)
- **`KNK_AI키등록.bat`** — 키 등록 도우미
  - `1=Claude / 2=OpenAI` 선택 → 알맞은 환경변수 + `KNK_AI_PROVIDER` 자동 설정
  - 보안 입력(화면에 안 보임), **키를 어떤 파일에도 저장 안 함** (setx)
  - **ASCII 전용** (한글 코드페이지 깨짐 방지)
- `requirements.txt`: `anthropic>=0.40`, `openai>=1.40` (둘 다 옵션)
- `run.py`: 시작 로그에 `[AI] {공급사} 연동 active (model=…, key=…)` 표시

### 보안 설계
- 키는 **OS 환경변수에만** 존재 → 코드·DB·git 어디에도 없음
- 미등록·결제 전이어도 시스템 정상 동작 (자동 비활성)

---

## 9. ⚠️ 미해결 / 다음 할 일

### 결제 (대표 직접)
- **Claude 결제 버튼 비활성** 문제 발생 → 카드·시크릿창·휴대폰·여러 카드 모두 실패
- 유력 원인: **국내 카드 해외 온라인 결제(USD) 차단**
- **해결책**: 카드사 앱에서 "해외 온라인 결제 허용" ON → Claude·OpenAI 둘 다 풀림
- 대안: OpenAI(`platform.openai.com`) 결제 시도 → 되면 OpenAI 키 사용
- 또는 Anthropic `도움말 및 지원` 문의 (계정/플랫폼 이슈 가능성)

### 키 발급 현황
- Claude API 키는 **발급 완료** (단, 크레딧 미충전 → 호출 시 결제오류, 시스템은 정상)

### AI 기능 실제 연결 (결제 후, 분할 요청 예정)
1. Victor 고도화 (자연어 라우팅)
2. 한↔베트남 번역 (메신저/문서)
3. 문서 요약·작성 도움
- 권장: 번역=저렴 모델 / 요약·Victor=고품질 모델 + **사용량·비용 로깅**

### 비용 (참고)
- 종량제. KNK 규모(75명) 추정 **월 2~5만원** 수준
- 콘솔에서 **월 지출 한도** 설정으로 폭주 방지 (현재 $500 설정됨)
- 시스템별 분리 원하면 **Workspace** 2개(HAIST WORKS / Messenger) + 키 분리 권장

---

## 10. 커밋 목록 (이번 세션)

```
54a3865 z108n19  AI 공급사 무관 구조 (Claude/OpenAI 선택)
999d11e z108n18  KNK_AI키등록.bat ASCII 전용 재작성 (한글 깨짐 수정)
8a48fa0 z108n17  Claude(AI) 연동 기반 + API 키 등록 도우미
18a9924 z108n16  main.py 재복구 (git add -A 사고 재발 복구)
2f996ce z108n15  경쟁 메신저·SNS 상표명 일괄 제거 (법적 리스크)
a534354 z108n14  main.py 복원 (30개 라우트 사고 복구)
df58584 z108n13  사이드바 3개 메뉴 404 근본 수정
8ded85d z108n12  BAT 좀비 자동 청소 + KNK_중지.bat
62e271b z108n11  노드상세 깨진 sys_link 404 수정
478b342 z108n10  노드상세 잠금 배너 → 참고용 안내
ab8b9cc z108n9   체크포인트 추가/수정/삭제
27a26d5 z108n8   잠금(🔒) 제거 — 누락방지 체크리스트 철학
329e07e z108n7   체크리스트 글씨·조작 복구
e18ce05 z108n6   IC pair_code UNIQUE 충돌 수정
370ddde z108n5   list-row overflow 제거 (내용 표시 수정)
9d851a9 z108n4   list-row 높이 50px 고정
e10a1eb z108n3   디버그 영역보기 자동 라벨 22종 추가
c267a63 z108n2   체크리스트 행 추가 압축
48c8e74 z108n    체크리스트 행 한 줄 정렬
```

---

## 11. 재발 방지 메모 (운영 규칙)

1. **`git add -A` 금지** — 수정한 특정 파일만 개별 add (공유 파일 사고 방지)
2. **공유 대형 파일(main.py)** 커밋 전 라우트 수 확인
3. **BAT 파일은 ASCII 전용** (한글 필요 시 .ps1 분리 또는 영문화)
4. **세션 간 동시 편집** 충돌 방지 — 대표가 영역별로 분할 요청
5. **API 키는 절대 파일·채팅·커밋에 남기지 않음** — OS 환경변수만

---

*작성: 빅터(Claude) · 2026-05-21*
