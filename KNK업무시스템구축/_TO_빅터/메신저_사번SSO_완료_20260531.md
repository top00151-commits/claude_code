# [완료보고] 메신저 사번 SSO — 라이브 cutover 완료

> 발신: 메신저 세션 · 수신: 빅터(실무팀1) / 김정락 대표
> 발주: `_TO_메신저세션/2026-05-29_사번SSO.md` · 완료: **2026-05-31**
> 결과: **메신저 = 사내 단일 인증 서버(Identity Provider) 가동. 사번 로그인 + JWT RS256 SSO 활성.**

---

## 1. 한 줄 결론
130명 직원 로그인 ID 가 **사번**으로 정렬되고, 메신저가 **JWT RS256 SSO 토큰을 발급**하는 인증 서버로 가동되었습니다. HAIST WORKS 가 메신저 SSO 로 붙을 준비 완료.

## 2. 무엇을 했나 (발주 §3 전부)
| 항목 | 결과 |
|---|---|
| **3.1 스키마** | `users` + `entity`·`password_version`, `employee_no` UNIQUE — 운영 반영 |
| **3.2 마이그레이션** | 엑셀 130명 ↔ DB 매칭 **130 갱신 / 신규 0 / 충돌 0**. `entity` 채움 + 직급·부서·영문/베트남어 이름 HR 기준 정렬 |
| **3.4 SSO API** | `/msg/api/sso/token·userinfo·public-key·revoke` 활성(RS256, pwv 무효화, rate limit·CORS·HTTPS) |
| **3.4 로그인** | `/msg/sso/login?redirect_uri=` → 성공 시 JWT 발급 후 SP 복귀 |
| **3.3 안내시트** | 갱신 스크립트 준비 완료(아래 §6 — HR 원본 교체는 대표 확인 후) |
| **3.5 인터페이스** | `_TO_빅터/메신저_사번SSO_인터페이스.md` (실무팀1 핸드오프) |

## 3. 검증 결과 (운영)
- 직원 132명 전원: `employee_no NULL = 0` · `username = employee_no = 사번` · 사번 패턴 정상(본사 숫자 / VN `VN###`)
- 표본: 대표 김정락 = `5` (**role=ceo 보존**) · 김정록 = `43` · 이용식 = `VN001`(entity=VN)
- `https://haist.knknara.co.kr/healthz` → **200**
- `/msg/api/sso/public-key` → **200** (키 활성) · `/msg/api/sso/token` 더미 → **401**(정상 처리)

## 4. DB-only 2건 — 대표 결정 완료 (발주 §8.1)
엑셀에 없는 내부 계정 2개 = **테스트용 PM 계정**. 마이그레이션은 **건드리지 않음**.
| id | 로그인ID(사번) | 이름 | 비고 |
|---|---|---|---|
| 175 | 999 | 김빅터 | 테스트 PM 계정 |
| 178 | 998 | 이빅터 | 테스트 PM 계정 |

→ **대표 결정(2026-05-31): 테스트 계정으로 일단 유지, 추후 대표가 수동으로 삭제 예정.** 자동 조치 없음.

## 5. 롤백 (필요 시 5분)
- 백업: `/opt/knk_messenger/data/backup_pre_employee_no_20260531_013142.db`
- 절차: messenger stop → 백업으로 messenger.db 교체 → start

## 6. 남은 선택 작업
1. **안내 시트 §2 갱신** — 로그인ID "이메일/영문이름 → 사번" 반영본 생성 스크립트 준비됨
   (`sso_work/update_guide_sheet.py`). HR 엑셀 원본 직접 수정은 폴더정책상 대표 확인 후 진행.
2. **환경변수 확인** — 컨테이너 `KNK_MSG_OWNER_USERNAME=5`(대표 사번) 권장. 현재 대표 role=ceo 정상 유지 중.

## 7. 실무팀1(HAIST WORKS Phase 2) 핸드오프
- 인터페이스 확정서: `_TO_빅터/메신저_사번SSO_인터페이스.md`
- public key: `GET https://haist.knknara.co.kr/msg/api/sso/public-key` (PEM, `?format=jwk` 지원)
- audience: `haist-works` · 토큰 만료 1시간 · iss `https://haist.knknara.co.kr/msg/`
- ⚠ 테스트용 사번/비번 1쌍은 대표 승인 하에 별도 전달 (보안)

## 8. 기록
- 코드 커밋: knk-messenger `3d4fa70` (SSO 구현) + cutover 스크립트(후속 커밋)
- 막힘/이슈: 없음. (apply 1차에서 username UNIQUE 전이 충돌 → 2-패스 임시리네임으로 해결, 합성DB 재검증 후 적용)
