# [발주] 부품 수출 마진 시스템 — KNK VINA 베트남 수출 본사 이익률 가시화

> **원본 제안서**: `01B_HAIST_WORKS_매출영업/output/HANDOFF_TO_01_v13_PROPOSAL_parts_margin_system.md` (2026-05-12 실무팀2 작성)
> **정식 발주 전환**: 2026-05-29 (대표 결재 — "C. 새 발주서로 변환")
> **발주자**: 김정락 대표이사 · 빅터(01 통합실무팀, 발주서 작성)
> **수신**: 실무팀1 (통합플랫폼, 향후 재개 시) 또는 신규 사이클
> **우선순위**: ★★ 중간 (사번 SSO 완료 후 또는 병행)
> **추정 기간**: 7~10일 (4단계 Phase)

---

## 0. 한 줄 요약

자재구매(매입가)와 매출영업(수출가)을 **자재 단위로 연결**하여 베트남 법인 KNK VINA 수출 시 **본사 마진율을 한 화면에서 확인**할 수 있는 분석 시스템.

---

## 1. 비즈니스 배경

### 1.1 KNK 부품 수출 매출 구조
- **부품(PARTS) 형태 매출**: 자동화·검사기 카테고리에서 별도 부품 단위로 매출 발생
- **수출 대상**: 대부분 **베트남 법인 (KNK VINA)** 으로 수출
- **마진 구조**: 본사가 자재 매입가에 **8~15% 마진 추가** → 베트남 수출 인보이스
- **본사 이익**: 수출 매출 − 매입 단가 = 본사 마진

### 1.2 현재 시스템 한계
1. **자재구매(`part_prices`)** 에 매입 단가 보관 — 격리됨
2. **매출 수주(`so_units` PARTS)** 에 수출 단가 표시 — 격리됨
3. **두 시스템 연결 없음** → 본사 이익률 한눈에 못 봄
4. **PARTS 28컬럼** 은 인보이스 작성 사양(HS/DUTY/VAT/관세/USD/PALLET 등) — 시각 복잡, 더 추가 곤란

### 1.3 대표 요구사항
1. 매입 단가 ↔ 수출 단가 **자동 연결**
2. **마진 % · 본사 이익** 한눈에 표시
3. PARTS 28컬럼 유지 (인쇄·인보이스 요건)
4. 마진 관리는 **다른 위치**에서 (28컬럼 부담 회피)

---

## 2. 시스템 설계

### 2.1 DB 스키마 추가 — 신규 매핑 테이블

```sql
CREATE TABLE so_unit_part_link (
  id           INTEGER PRIMARY KEY AUTOINCREMENT,
  so_unit_id   INTEGER NOT NULL,     -- so_units.id (PARTS 라인)
  part_id      INTEGER,              -- parts.id (자재구매)
  part_code    TEXT,                 -- 자재코드 (parts.code 미존재 케이스 대비)

  -- 시점 스냅샷 (매입가 변동 대비)
  snapshot_cost_unit   DECIMAL(15,2),    -- 라인 작성 시점의 매입 단가
  snapshot_cost_total  DECIMAL(15,2),    -- 매입 단가 × 수량
  snapshot_currency    TEXT DEFAULT 'KRW',
  snapshot_date        DATE,

  -- 마진 계산
  margin_pct    DECIMAL(5,2),            -- 마진율 % (8.0 ~ 15.0)
  override_note TEXT,                    -- 수동 조정 사유 (협의가 등)

  created_at    DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_at    DATETIME DEFAULT CURRENT_TIMESTAMP,

  FOREIGN KEY (so_unit_id) REFERENCES so_units(id) ON DELETE CASCADE,
  FOREIGN KEY (part_id) REFERENCES parts(id) ON DELETE SET NULL,
  UNIQUE (so_unit_id)
);
CREATE INDEX idx_solink_part ON so_unit_part_link(part_id);
CREATE INDEX idx_solink_code ON so_unit_part_link(part_code);
```

### 2.2 매핑 로직 (자동 연결)

**우선순위**:
1. 사용자가 SO PARTS 라인 작성 시 **자재코드 입력** (또는 자재구매 부품 검색 dropdown)
2. 시스템이 `part_prices` 최신 단가 자동 조회
3. **snapshot 으로 저장** (이후 매입가 변동되어도 이 라인의 마진 계산은 시점 단가 기준)
4. 마진율 자동 계산: `(수출가 - 매입가) / 매입가 × 100`
5. 수동 override 가능 (협의가·환차 등 특수 케이스)

### 2.3 UI 사양 (4곳)

#### A. SO 카드 상단 "📊 마진 분석" 버튼
- 위치: `<button class="toggle-edit">` 옆
- 노출 조건: SO 가 PARTS_EXPORT 타입일 때만
- 클릭 시 모달 표시

#### B. 마진 분석 모달
```
┌─────────────────────────────────────────────────────────────┐
│ 📊 마진 분석 — T-260512-1 (베트남 법인)            [X]      │
├─────────────────────────────────────────────────────────────┤
│ 환율: 1 USD = 1,375 KRW (오늘 기준)                          │
│                                                              │
│ # │ 자재코드  │ 품명        │ 매입가    │ 수출가    │ 마진금액 │ 마진%  │
│ 1 │ P-A001   │ 부품 알파    │ 100,000  │ 115,000  │ +15,000 │ 15.0%  │
│ 2 │ P-A002   │ 부품 베타    │ 80,000   │ 88,000   │ +8,000  │ 10.0%  │
│ 3 │ P-A003   │ 부품 감마    │ —        │ 50,000   │ ?       │ ⚠ 미연결│
│                                                              │
│ ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━│
│ 합계 매입: 180,000 원                                        │
│ 합계 수출: 253,000 원 (= USD 184.00 × 1,375)                │
│ ─────────────────────────────────────────                   │
│ 총 마진금액: +73,000 원 (28.9%) ⚠ 정상 범위 초과            │
│                                                              │
│ [📥 마진 보고서 엑셀] [✓ 자재 자동 연결] [✕ 닫기]            │
└─────────────────────────────────────────────────────────────┘
```

#### C. 프로젝트 헤더 KPI 추가
- 기존 KPI: 전체 업무 / 완료 / 누적 공수 / 수주액 (4개)
- 신규: **본사 이익 / 마진율** 표시 (PARTS_EXPORT 프로젝트만)
- 예: `본사 이익: +730,000원 (12.8%)`

#### D. 신규 분석 페이지 `/parts/margin`
- 부품별 마진 추이 (월별·분기별)
- 마진율 분포 차트 (8% / 10% / 12% / 15% 구간별)
- 베트남 수출 마진 대시보드 (월별 본사 이익 합계)
- 마진 < 8% 또는 > 15% 경고 리스트
- 부품 단위 추적: 같은 자재코드의 매입가·수출가 추이

### 2.4 API 사양

```
POST   /so-units/{sid}/part-link        # 라인↔자재 연결
DELETE /so-units/{sid}/part-link        # 연결 해제
GET    /so/{soid}/margin-report         # SO 단위 마진 데이터 (JSON)
GET    /projects/{pid}/margin-summary   # 프로젝트 마진 합계
GET    /parts/margin                    # 분석 페이지 데이터
GET    /parts/margin/export.xlsx        # 마진 보고서 엑셀
```

---

## 3. 파일 변경 계획

### 3.1 DB
- `01_HAIST_WORKS/migrations/v5H226z109_so_unit_part_link.sql` (신규)
- 기존 데이터 무결성: PARTS_EXPORT SO 만 영향, 일반 SO 무변동

### 3.2 백엔드 (`app/main.py` 또는 별도 라우터)
- `/so-units/{sid}/part-link` (POST/DELETE)
- `/so/{soid}/margin-report` (GET)
- `/projects/{pid}/margin-summary` (GET)
- `/parts/margin` (GET, 페이지)
- `/parts/margin/export.xlsx` (GET, 엑셀)
- 자재구매 part 검색 dropdown용 API

### 3.3 프론트
- `app/templates/project_detail.html` — SO 카드에 "📊 마진 분석" 버튼 + 모달 추가
- 신규 `app/templates/parts_margin.html` (분석 대시보드)
- (선택) `app/templates/part_prices.html` — "최근 수출가" 표시 추가

### 3.4 디자인
- 마진 모달 UI mockup (시안1 톤 — 디자인팀 05 의뢰 또는 빅터 자체 작업)
- 분석 페이지 차트 사양 — **외부 자산 정책 검토 필요** (Chart.js 등 사용 시)

---

## 4. 룰·권한 영향

| 영역 | 영향 | 처리 방향 |
|---|---|---|
| 메인 BAT (`KNK_시작.bat`) | 미수정 | 변동 없음 |
| `_v5_partials/` | 미수정 | 변동 없음 |
| **DB 스키마** | **추가** (`so_unit_part_link`) | 빅터(01) 처리 |
| **`main.py` 라우트** | **추가** (~6개) | 빅터(01) 처리 |
| PARTS 28컬럼 백엔드 | 영향 없음 | 분리 작업 |
| 외부 자산 (Chart.js) | 검토 필요 | 외부 상표권·라이선스 정책 5항 통과 확인 |
| 외부 상표권 (베트남 법인 명칭) | 사내 표기 OK | 외부 문서 시 "베트남 법인" 으로 익명화 |

---

## 5. 단계 계획 (Phase 1~4)

### Phase 1 — DB + 매핑 (1~2일)
- [ ] DB 마이그레이션 SQL 작성·적용
- [ ] 자재구매 part 검색 API
- [ ] SO 라인에 자재코드 입력 필드 추가 (PARTS 28컬럼 부담 회피 — 별도 modal 권장)
- [ ] 매핑 저장·조회 API

### Phase 2 — 마진 표시 (2~3일)
- [ ] "📊 마진 분석" 모달 UI
- [ ] 마진 계산 로직 (단가 × 수량 + 환율)
- [ ] 프로젝트 KPI 에 본사 이익 추가

### Phase 3 — 분석 페이지 (2~3일)
- [ ] `/parts/margin` 대시보드 페이지
- [ ] 부품별 추이 · 베트남 수출 합계
- [ ] 엑셀 보고서 export

### Phase 4 — 경고 시스템 (추후)
- [ ] 마진 < 8% / > 15% 경고
- [ ] 협의가 사유 관리
- [ ] 매입가 변동 알림 (영향받는 SO 자동 식별)

---

## 6. 위험·검토 사항

### 6.1 환율 처리
- 매입: KRW
- 수출: USD (인보이스)
- **마진 계산 시점 환율** 정책 결정 필요:
  - **A. 계약 시점** — 견적·수주 확정 시 환율 고정
  - **B. 발행 시점** — 인보이스 발행 일자 환율
  - **C. 결제 시점** — 실제 입금 환율 (사후 정산)
  - **D. 사용자 매번 입력** — 유연하지만 입력 부담

### 6.2 자재 매핑 데이터 부재
- 기존 자재구매 시스템에 PARTS 자재가 모두 등록되어 있는지 확인 필요
- 미등록 자재는 **수동 매입가 입력 옵션** 필요

### 6.3 외부 자산 (시각화)
- 차트 라이브러리: Chart.js · ApexCharts 등
- **REPLY v2 룰: 외부 자산 0건 원칙** — 검토 후 사용 결정
- 또는 **CSS bar-chart** 로 단순 처리 (외부 자산 없이) — 빅터 권장

### 6.4 베트남 법인 표기
- 외부 상표권·라이선스 비침해 정책 5항: **내부 문서 표기 OK**
- 외부 노출(보고서·인쇄 등) 시 **"베트남 법인"** 으로 익명화
- 코드·UI 안에서도 "베트남 법인" 표준 사용 권장 (실명 노출 회피)

---

## 7. 대표님 결재 사항 (4건)

### 결재 A — 본 설계 방향 적정성
- DB + 모달 + 분석 페이지 설계 적정한가?
- 마진율 범위(8~15%) 자동 검증 + 경고 시스템 추가?

### 결재 B — 발주 착수 시점
- 옵션 1: **사번 SSO 완료 후 즉시** (2026-06-10 무렵)
- 옵션 2: **사번 SSO 완료 + 며칠 안정화 후**
- 옵션 3: **사번 SSO와 병행** (별도 사이클)

### 결재 C — 단계 선택
- **전체 (Phase 1~3) 한 번에 발주** (7~10일)
- **Phase 1만 MVP 우선** (1~2일) 후 사용성 확인 → Phase 2~3 점진

### 결재 D — 환율 정책
- 6.1 의 A/B/C/D 중 어느 것?
- (빅터 추천: **A. 계약 시점** — 가장 단순·재무 일관성)

---

## 8. 출처·참조

- 원본 제안서: `01B_HAIST_WORKS_매출영업/output/HANDOFF_TO_01_v13_PROPOSAL_parts_margin_system.md`
- 작성: 빅터(02) 매출영업 세션 — 2026-05-12
- 정식 발주 전환: 빅터(01) — 2026-05-29 (대표 결재 "C. 새 발주서로 변환")
- 관련 시스템:
  - 자재구매: `01_HAIST_WORKS/app/templates/part_prices.html` + `database.py`
  - 매출영업: `01_HAIST_WORKS/app/templates/project_detail.html` + SO units
  - 익명화 정책: `_STANDARDS/외부브랜드_익명화_매핑.md`

---

*발주 전환: 빅터(Claude) · 2026-05-29*
*상태: **결재 대기 (A·B·C·D 4건)***
*다음 액션: 대표님 결재 → 정식 사이클 착수 시점 결정*
