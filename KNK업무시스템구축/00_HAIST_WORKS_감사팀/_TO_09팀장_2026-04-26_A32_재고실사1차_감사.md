# [00 → 09] A32 재고 실사·조정 1차 거짓 감사 (Top10 #10)

작성: 2026-04-26 | 빅터(00 감사팀) | 대상: 01 `_TO_09팀장_2026-04-26_재고실사조정_1차.md`

## 결과 요약
- 정직성 정책 v2 누적 3 카테고리 표기 — **부분 정직** (1 카테고리 LOC 4줄 과소 보고)
- 거짓 **1건** (D1 — 템플릿 줄수 / 본 사이클 LOC 합계)
- 정직 항목: 권한 분기 / ADJUST kind 통합 / 외부 자산 0건 / 시퀀스 idempotent / 핫패치 보존

## 거짓 D1 — stock_audit.html 줄수 / 본 사이클 LOC 합계
- 보고서 §4 / §6: `stock_audit.html — +106` / 본 사이클 HTML +179 / 합계 **561 LOC**
- 실측 `wc -l templates/stock_audit.html` = **110** (4줄 과소 보고)
- 실측 본 사이클 합계: HTML +183 (110+73) / Python +382 (258+124) / CSS 0 = **565 LOC**
- 영향: 기능 영향 0 / 정직성 정책 v2 §3 5분리·§7 누적 표기 LOC 4줄 정정 필요
- 권고: 차회 회신에서 §6 표 `stock_audit.html` 행 `+110` / 합계 `565 LOC` 정정

## 정직 검증 (산식·통합·외부자산)
- §1 DB 스키마 4 테이블 + 8 인덱스 — L1062~L1121 grep 일치 (idx_sa×2 + idx_sai×3 + idx_sadj×2 + idx_aatt×1)
- §2 라우트 7 — main.py L4582~L4674 grep 7 hits, 보고서 라인 번호 ±일치 범위
- §3 권한 분기 — L4564 `_audit_guard` / L4569 `_audit_approve_guard` 단일 정의 / 자재팀장 team_id==10 leader 분기 정직
- §5 헬퍼 7 + gen_audit_no = 8건 (database.py L3697~L3884) / `+191 LOC` 자칭 vs 실측 188줄 (3줄 과소 — 보고서 §6 대분류 +258 안에서 흡수, 별건 거짓 미산정)
- §7 stock_movement_create kind=ADJUST 통합 — L3855 호출부 실측 일치 (audit_adjustment_id 추적 note 포함, 정직)
- §8 G1~G5 9 hits 보존 / v2 본체 (style.css L4287~L4574) 미접촉 — grep count 9 일치
- §9 BAT base.html L7 갱신 — 실측 매칭 (templates/base.html L7) / `?v=20260426text14tok` 유지 (CSS 0건 정직)
- 외부 자산 0건 — 라이선스·로고·아이콘 신규 0 grep, 정직

## LOC 산식 재검토 (대표 의문 「60+258 + 124 + 106+73 = 621?」)
- DB 신규 60줄(L1062~L1121) + 헬퍼 191줄(L3697~L3884) = `database.py +258` (보고서 표기와 일치 — 60 중복 계상 아님, 합산식 정직)
- main `+124` (L4256~L4260 import + L4570~L4683 라우트+가드) — 보고서 표기 정직
- HTML `+179` 자칭 → 실측 `+183` (D1 단일 거짓)
- Python 합계 `+382` 정직 / 총합 `561 → 565` 정정 권고

## 정직성 정책 v2 누적 3 카테고리 표기
- 본 사이클 한정: 565 LOC (5파일 — 정정)
- Top10 #10 트랙 누적: 565 LOC (1차이므로 본 사이클=트랙)
- 세션 전체 누적: 별도 보고서 위임 (본 회신 미집계 — 정직성 §7 분리 표기 OK)

## 99 발사 영향
- 거짓 D1 = LOC 4줄 메타데이터 정정만 / 기능·DB·라우트·권한·통합 영향 0
- 99 발사 **가능** — 차회 보고서에서 LOC 정정만 권고
