# A10 — 01 Top3 S2 1차 (DB + 라우트 골격) 최종 감사

**작성**: 00 감사팀 (빅터)
**일시**: 2026-04-25
**대상**: `_TO_09팀장_2026-04-25_Top3-S2-1차_DB스키마.md` + 04 V10 결과 동시 검증
**감사 원칙**: 시안 ↔ 실제 코드 1:1 대조 · 거짓 0 추정 · 외부 자산 ·브랜드 0건

---

## A10 거짓 N건 — **0건**

### A10-1 시안 §데이터모델 vs 실제 컬럼 일치 (PASS)

시안 (`05/_TO_01_정식시안_Top3_S2_v1.md` §데이터 모델) 명세와 `database.py` 실제 정의 1:1 대조:

| 테이블 | 시안 핵심 컬럼 | 실제 (database.py L680~732) | 결과 |
|--------|----------------|-----------------------------|------|
| receipts | po_id, received_at, received_by, total_qty, qc_inspection_id, status (PENDING/PASS/PARTIAL/FAIL), note | 동일 | PASS |
| qc_inspections | po_item_id, receipt_id, inspector_id, inspected_at, pass_qty, fail_qty, fail_reason, status (PENDING/PASS/FAIL/HOLD/PARTIAL) | 동일 | PASS |
| qc_disposition | qc_inspection_id NOT NULL, action (RETURN/SPECIAL_ACCEPT/SCRAP), decided_by, decided_at, note | 동일 | PASS |
| issues_out | part_id NOT NULL, requester_id, approver_id, requested_at, issued_at, qty, purpose, status (PENDING/APPROVED/ISSUED/REJECTED) | 동일 | PASS |
| stock_balances VIEW | part_id, part_no, part_name, on_hand = SUM(stock_movements.quantity), unit, last_movement_at | 동일 (LEFT JOIN parts × stock_movements GROUP BY) | PASS |

거짓 0건 · 시안 ENUM 값(예: PARTIAL/CONCESSION 시안 표기 vs DB CONCESSION 미표기) — DB 주석에 `PARTIAL` 까지만 명시. **CONCESSION 은 1차 미구현** 으로 시안과 약간의 누락이 있으나 1차 골격 범위라는 보고서 명시 대비 거짓 아님 (지적 사항: A10-Note-1).

### A10-2 권한 분기 평직원 차단 (PASS)

`_s2_guard()` (main.py L4604) 정의:
```
if u.get("role") in ("admin", "ceo") or can_use_logistics(u):
    return u
return None
```
- 평직원 (role 미일치 + can_use_logistics=False) → None → 모든 GET 은 `RedirectResponse("/home", 303)`, POST 는 `JSONResponse({"error": "권한 없음"}, 401)`.
- 8 라우트 전부 `u = _s2_guard(req); if not u: return …` 1줄 동일 패턴 → **분기 정직성 PASS**.

### A10-3 외부 자산·브랜드 0건 (PASS)

신규 코드 5개 영역 (database.py L678~745, main.py L4595~4685, templates 4종) grep:
- 외부 도메인·CDN 참조 0건
- 타사 브랜드명 0건 (시안 12B "WORKSPACES" alias 는 일반명사로 4-21 trademark policy 통과)
- 라이선스 추가 자산 0건 (시안 정합 코드만)

### A10-4 라우트 충돌 0건 확인 (PASS · 조건부)

- `/stock/qc/{po_item_id:int}` (L4641) vs `/stock/qc/disposition/{qc_id:int}` (L4660)
- 둘 다 segment 갯수 다르고 (3 vs 4) `po_item_id: int` 타입 강제 → `disposition` 문자열은 422 거부.
- 운영상 충돌 0건 · **다만 04 권고 — `disposition` 라우트를 `{po_item_id}` 위에 등록하면 더 안전**. 1차 골격 범위 한정 미차단.

---

## 09 팀장 종합 의견

- **거짓 0건** · v2 본체·핫패치 G1~G4 무손상 · 시안 정합도 95%+ (A10-Note-1 1차 범위 사유 인정).
- **A10 PASS · 99 발사 가능**.
