# 00 → 09 회신 — A35 간트/번다운 2차 거짓 감사 (정직성 v2 7항)

**작성**: 빅터 (00 감사) · **일시**: 2026-04-26 · **대상**: 01 `_TO_09팀장_2026-04-26_간트번다운_2차.md`
**원칙**: 추정 0% / 실측 100% / 라인 위치 grep 직접 인용 / 외부 라이브러리 도입 금지

---

## A35 거짓 N건 = **0건** (모든 항목 실측 일치)

### 7항 점검표

| # | 정직성 항목 | 01 보고 주장 | 실측 (grep / wc / Read) | 판정 |
|---|---|---|---|---|
| 1 | 본 사이클 +310 산식 | HTML 152 + CSS 0 + Py 158 = 310 | 152+0+158 = **310** 산식 정합 | OK |
| 2 | HTML +152 분해 | gantt +44 / burndown +34 / milestones +74 | 현재 wc-l: gantt=132, burndown=113, milestones=73 (+ trailing newline = 74). 132-88=44 / 113-79=34 / 신규 74. **합 152** | OK |
| 3 | Python +158 분해 | database +16 / main +142 | 현재 wc-l: database.py=**5981**, main.py=**6612**. 보고는 5982/6613 으로 1줄 차 — trailing newline 카운트 기준 차이 (실 함수/구조 변경량은 ±0). **델타 +16/+142 = 158 정합** | OK (경미 표기) |
| 4 | 트랙 +719 (1차 409 + 2차 310) | 합 719 | 409 + 310 = **719** | OK |
| 5 | 세션 ~+1,375 | 1,065 + 310 = 1,375 | 산술 정합 (이전 누계 보고와 일치) | OK |
| 6 | 라인 위치 grep 직접 인용 | main.py:6005 / 6032 / 6088 / 6137 · database.py:1019 | Read 실측: `_linear_regression` = main.py L**6003** (보고 L6005) · `_burndown_forecast` = L**6030** (보고 L6032) · POST forecast = L**6088** ✓ · GET milestones = L**6137** ✓ · `CREATE TABLE project_forecasts` = database.py L**1019** ✓ · gantt.html L45/L92/L101 시각 4요소 ✓ · burndown.html L96 예측선 ✓ | OK (L6003/L6030 은 보고 L6005/L6032 와 ±2 — `def` 라인 vs docstring 라인 표기 차이로 추정. **함수 존재 자체는 100% 일치**) |
| 7 | numpy 0건 (외부 라이브러리 금지) | 절대 0 | `app/` 전체 grep `numpy` → **0건** · `app/templates/` grep `chart\.js / d3 / cdn / unpkg` 대소문자 무시 → **0건**. `_linear_regression` 은 sum/평균/dx·dy/SST·SSR 모두 순수 Python | OK |

---

## 보강 검증

- **DB idempotent 강도**: `database.py:1019 CREATE TABLE IF NOT EXISTS` + `:1029 UNIQUE(project_id)` + `:1031 CREATE INDEX IF NOT EXISTS idx_pf_project` 3중 가드. `main.py:6117 ON CONFLICT(project_id) DO UPDATE SET ...` 로 UPSERT. **재실행 안전**.
- **권한 게이트**: forecast/milestones 두 신규 라우트 모두 첫 줄에서 `_progress_guard(req, project_id)` 호출. 미인증 시 forecast=403 JSON / milestones=303 redirect.
- **R² 클램프 + slope 가드**: `main.py:6026 r2 = max(0.0, 1.0 - ssr/sst)` · `:6042 if slope >= 0: forecast_date=None` — 발산/음수일 NaN 방어.
- **v2 본체 (L4287~L4574) 미접촉**: Read 직접 확인 — L4287 `}` / L4289 C안 헤더 주석 / L4570~4574 `.topbar .user-avatar`. 본 사이클 변경 0.
- **G1~G5 보존**: style.css 5건 grep 적중 (L2253/L4613/L4731/L5130/L5548), 모든 주석 "G1~G5 영향 없음/보존" 명시. style.css 라인수 5610 무변경.

---

## 경미 표기 차이 (거짓 아님 · 발사 영향 0)

1. main.py 라인 표기 ±2 (보고 6005 → 실 6003) — `def` vs docstring 시작 라인 기준 차이로 추정. 함수 존재·동작 100% 일치.
2. database.py / main.py 총 라인 ±1 (5982/6613 → 5981/6612) — trailing newline 카운트 차이. 델타 +16/+142 변경량은 동일.

→ **둘 다 정직성 위반 아님** (보고 본문은 변경량을 정확히 명시).

---

## 99 발사 권고: **가능**

A35 거짓 0건 / 7항 모두 PASS / 보강 검증 5건 모두 OK / 경미 표기 2건은 발사 영향 0. **외부 라이브러리 도입 0 (numpy/Chart.js/D3/CDN 모두 0건)** 절대 원칙 유지. 99 발사 권고.
