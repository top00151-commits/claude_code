# 00 → 09 회신 — A36 QMS 2차 (Pareto + CAPA) 정직성 감사

**작성**: 00 감사팀 · **일시**: 2026-04-26 · **사이클**: A36 (정직성 v2 7항 · 거짓 식별)

## 거짓 식별: **0건**

01 보고서 (`_TO_09팀장_2026-04-26_QMS_2차_Pareto_CAPA.md`) 7항 자기검증 모두 실측 일치.

## 정직성 v2 7항 검증

### 1. 본 사이클 +517 산식 정합

| 파일 | 라인 변동 | 실측 |
|---|---|---|
| `app/database.py` | +46 (L1633~L1678 = 46줄) | `wc -l` 6028 (이전 5982 + 46) |
| `app/main.py` | +237 (L6420~L6656 = 237줄) | `wc -l` 6849 (이전 6612 + 237) |
| `app/templates/qms_pareto.html` | +101 신규 | `wc -l` 101 |
| `app/templates/qms_capa.html` | +131 신규 | `wc -l` 131 |
| `app/templates/qms_dashboard.html` | +2 (80→82, nav 3링크) | `wc -l` 82 |
| **합계** | **46+237+101+131+2 = 517** | **PASS** |

### 2. QMS 트랙 +969 정합

`452(1차) + 517(2차) = 969` — 산수 검증 PASS. (1차 보고서는 본 감사 외 별도 사이클이지만 산식만 검증)

### 3. 세션 +1,378 정합

`861(이전) + 517(본) = 1,378` — 산수 검증 PASS.

### 4. KPI 정직성 가드 (3건 전부 코드 확인)

- `sample_size`: `_capa_kpi` L6473 `"sample_size": len(days)` 명시 반환. 0건 시 avg_closure=0 + sample_size=0 동시 노출 (가짜값 X).
- `verify_rate` 분모 0 가드: L6461 `round(100.0 * verified / completed, 1) if completed else 0` — `completed` 0 시 `0` 반환.
- NULL 부서 `(미지정)`: L6464 `COALESCE(t.name,'(미지정)') AS dept` SQL 명시.

### 5. 외부 자산 0건 정합

- `app/` 하위 `^(import\|from) (numpy\|scipy\|pandas\|matplotlib\|plotly)`: **0 hits**.
- 템플릿 2종 `(chart.js\|cdn\|jsdelivr\|unpkg)`: **0 hits**.
- 보고서 4항 "numpy/pandas/scipy 0건 — pure Python datetime.strptime + 산술만" 정합.

### 6. v2 본체 미접촉 + 핫패치 7/7 정합

`static/style.css` `핫패치 G[1-5]` grep:
```
2253  핫패치 G1·G5 영향 없음 (margin-right 시프트만)
4613  v2 본체(4287~4574) 무수정 · 핫패치 G1~G5 보존
4731  v2 본체(4287~4574) 무수정 · 핫패치 G1~G5 보존
4903  v2 본체(4287~4574) 무수정 · 핫패치 G1~G5 보존
5130  v2 본체(4287~4574) 무수정 · 핫패치 G1~G5 보존
5371  v2 본체(4287~4574) 무수정 · 핫패치 G1~G5 보존
5548  v2 본체(L4287~L4574) 미접촉 / 핫패치 G1~G5 영향 없음
```
**7/7 보존 PASS**. style.css 본 사이클 +0 → v2 본체 미접촉 자명.

### 7. 라인 위치 grep 직접 인용 (보고서 9항 자기검증과 코드 일치)

- `_qms_capa_guard` L6426 / `_capa_kpi` L6437 / `_capa_transition` L6549 — 단일 정의.
- 라우트 9개 L6476·6509·6604·6611·6618·6625·6634·6641·6648 — `@app\.(get\|post)\("/qms` grep 14건 중 신규 9건 정합.
- 1차 status 컬럼 무수정: `corrective_actions` ALTER ADD 6건 모두 신규 컬럼 (`lifecycle_status` 별도). 기존 status 컬럼 ALTER MODIFY 0건.

## 라이프사이클 전이 단일 경로 검증

`_capa_transition` 가드 → UPDATE → `qms_audit_log` INSERT 단일 경로. 7개 라우트 모두 이 헬퍼만 호출 (직접 UPDATE 없음). 감사로그 누락 가능성 0.

## 결론

**A36 거짓 0건**. 보고서 9항 자기검증 7/7 실측 일치 · 산식 517 = 46+237+101+131+2 정합 · KPI 분모 0 가드 3건 모두 코드 확인 · 외부 의존 0건 · 핫패치 7/7 보존 · v2 본체 미접촉. **99 발사 가능** (V36 8/8 PASS와 종합 합치).
