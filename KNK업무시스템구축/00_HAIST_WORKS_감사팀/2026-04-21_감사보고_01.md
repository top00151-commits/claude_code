# 2026-04-21 감사보고_01 — 01·02·03 세션 1차 팩트체크

**감사자**: 00 감사팀 (빅터)
**감사 일시**: 2026-04-21
**감사 범위**: 01 메인 결과물의 문서 주장 vs 실제 코드·git 상태 일치 여부
**원칙**: 추정 금지 · 100% 실측 · 불일치 시 담당 세션에 재검증 지시
**세션 번호 체계**: 00=감사팀 · 01=메인 · 02=baby · 03=Research (2026-04-21 개편)

---

## 1. 감사 방법 (재현 가능)

| # | 방법 | 명령/도구 |
|---|---|---|
| M1 | 커밋 해시 실존 확인 | `git log --oneline -30` |
| M2 | 원격 URL 확인 | `git remote -v` |
| M3 | FastAPI 앱 import | `python -c "from app.main import app; ..."` |
| M4 | 라우트 목록·수 | `app.routes` 순회 |
| M5 | DB 테이블 실존 | `sqlite_master` 조회 (`data/knk.db`) |
| M6 | 증분 diff | `git diff --stat 52219d6^..41d47fd -- HAIST_WORKS/` |
| M7 | 커밋별 테이블 수 | 각 커밋에서 `CREATE TABLE` 개수 grep |

---

## 2. 검증 통과 항목 (✅)

### 2.1 커밋 해시 실존
`next_session.md`·`HAIST_WORKS_5시간위임_결과보고.md`에 언급된 해시 5개 전부 `git log` 실존:
- `52219d6` 변경 Inform 시스템 ✓
- `ee86f67` 요청 티켓 시스템 ✓
- `89b6d23` 진행률 대시보드 ✓
- `41d47fd` 홈 KPI 통합 ✓
- `413815f` 부서 게시판 admin/ceo 전체 목록 ✓

### 2.2 GitHub 원격
`knk_systems.md` 주장: `top00151-commits/claude_code`
`git remote -v` 실측: `git@github.com:top00151-commits/claude_code.git` — **일치**

### 2.3 FastAPI 앱 실동작 가능성
- `app.main:app` import 성공 (APP_IMPORT_OK)
- 등록 라우트 **146개**
- 기능별 라우트 실존: `/changes`(8), `/tickets`(8), `/progress`(4), `/board*`(12)

### 2.4 DB 스키마 실존
`data/knk.db` 에 **33개 테이블** 존재. 주요 기능 테이블 모두 확인:
- changes, change_impacts, change_reads
- tickets, ticket_comments
- projects, project_phases, project_retros
- parts, part_prices, stock_movements, po_items, purchase_orders, suppliers, exchange_rates
- issues, issue_logs
- users, teams, tasks, task_comments, task_delegations, task_reactions, activities, notifications 등

### 2.5 신규 DB 테이블 증분 수치
- `next_session.md` 주장: "6개 DB 테이블"
- 실측: 52219d6^ 시점 20개 → 41d47fd 시점 26개 = **+6개** ✅ 정확

---

## 3. 검증 실패·부정확 항목 (❌ ⚠️)

### 3.1 ❌ [VR-001] 라우트 수 2개 과장
- **주장**: `next_session.md:18` — "22개 라우트"
- **실측**: `52219d6^..41d47fd` 구간 `@app.*` 데코레이터 증가량 **+20개** (98→118)
- **판정**: 2개 과장. 집계 기준을 명시하거나 수치 정정 필요.

### 3.2 ⚠️ [VR-002] 신규 코드 줄수 과소 주장
- **주장**: `next_session.md:18` — "~1,500줄 신규 코드"
- **실측**: `git diff --stat` 기준 HAIST_WORKS/ 범위 **3,075 insertions** (디자인 md 478줄 제외 시 2,597줄)
- **판정**: 실제가 주장의 약 2배. "~1,500"은 기준 모호. 정정 권고.

### 3.3 ❌ [VR-003] "8개 커밋 완료" 표기가 구식
- **주장**: `MEMORY.md:2` (`knk_systems.md`) — "8개 커밋 완료"
- **실측**: `git log --oneline` 결과 df8fc67 initial 포함 **총 24개 커밋**
- **판정**: 최소 3배 이상 부정확. 메모리 즉시 갱신 필요.

### 3.4 ⚠️ [VR-004] 신규 템플릿 수 7 vs 8
- **주장**: `next_session.md:18` — "7개 템플릿"
- **실측 신규 파일**: change_detail/form/list + progress_detail/matrix + ticket_detail/form/list = **8개 신규**
- **판정**: 1개 차이. 경미하지만 근거 확인 필요.

---

## 4. 미검증 (큐에 보관 — `00_감사큐.md`)

> **00 감사팀은 "추정 금지" 원칙상 아래 항목을 "검증 통과"로 처리하지 않음.**

- HTTP 실응답 (`/login`, `/changes`, `/tickets`, `/progress`) — 서버 기동 후 실측 필요
- 최근 커밋 `a561eec`·`24ab668`·`d128f42`·`0a3b908`·`9400880` 등 개별 실동작
- 02 baby — V2 sync.py 실행, 엑셀 파일 실존 경로
- 03 Research — "50회 누적 리서치" 주장 검증 방법, 외부 링크·날짜 팩트 체크
- 카카오워크 제거 완료 여부(`9400880`) — 소스 내 잔재 grep

---

## 5. 재검증 지시 요약 (01 메인 빅터에게 반환)

| ID | 항목 | 기대 조치 |
|---|---|---|
| VR-001 | 라우트 수 22→20 불일치 | 위임 보고서 정정 또는 집계 기준 명시 |
| VR-002 | 코드 줄수 1,500→3,075 불일치 | 근사치 정정 또는 집계 기준 명시 |
| VR-003 | "8개 커밋" 메모리 구식 | `knk_systems.md` 최신화 (24개) |
| VR-004 | 템플릿 수 7→8 | 재확인 후 수정 또는 근거 제시 |

---

## 6. 00 감사팀 총평

- **01 메인의 실체는 존재한다** — 커밋·코드·DB·라우트가 문서 주장 방향대로 실존함 (실동작 여부는 별도 검증 필요).
- **문서의 숫자들은 부정확하다** — 근사치·오래된 메모리가 현실과 괴리. 의사결정에 직접 쓰기 전 정정 필요.
- **치명적 허위는 발견되지 않음** (1차 감사 범위 내) — 커밋·DB·라우트 실존은 거짓 없음.
- **다음 감사 우선순위**: HTTP 실응답 테스트 → 03 Research 팩트 체크 → 02 baby sync.py 실행.
