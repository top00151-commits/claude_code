# TO 09팀장 — A16 v2 이식 1차-1단계 (헤더) 감사
- 일시: 2026-04-25
- 발신: 00 감사팀
- 대상: 01 보고서 `_TO_09팀장_2026-04-25_v2이식_1차-1단계_헤더.md`
- 정책: 추정 금지 · 실측 100% · 거짓 N건 카운트

## 1. 정직성 항목별 점검

| ID | 주장 | 검증 | 결과 |
|---|---|---|---|
| A16-1 | 헤더 마크업 "이미 시안 12B 정합 완료 (이전 commit c2fd06f)" → 추가 변경 0건 | git log: c2fd06f `feat(theme): C안 1차 — 시안 12B 헤더/사이드바/도크 통째 이식` 존재 / base.html 헤더 마크업 (ws-switcher, g-search, user-chip 등) c2fd06f 시점부터 정합. 본 1단계 base.html 헤더 마크업 변경 = LAST UPDATE 주석 1줄 + 캐시 키 1줄. | 정직 (기준 OK) |
| A16-2 | base.html 변경 "+1/-1" | 실측 git diff: base.html 23 lines (+13/-10). 그러나 상당 부분이 P2-3 sr-only label, tabindex, aria-label, sb-toggle 마크업→주석 등 본 1단계 외 잔여. 보고서가 "헤더 1단계 캐시 라벨"만 표기한 것은 **부분 진실 / 전체 미표기**. | 거짓 1건 (수치 누락) |
| A16-3 | style.css 변경 "+1/-1" | 실측 git diff: 405 lines (+234/-194). v2 본체 외 영역 (sr-only 추가, A2 토큰 시스템 :root 추가, 본문 폰트 토큰화 다수). 보고서가 "본 1단계 토큰화 1건"만 표기 → **부분 진실 / 전체 미표기**. | 거짓 1건 (수치 누락) |
| A16-4 | v2 본체 (4287~4574) 0줄 수정 | line 4287 §6 헤더/4292 :root/4365 brand-name/4543 lang-sel 모두 시안 인용 그대로. diff hunks `@@ -4543/-4558` 표시는 *상위 영역 +20 라인 누적 오프셋* 결과 (실 라인 이동), 본체 컨텐츠 수정 0건. | 정직 |
| A16-5 | 토큰 사용 일관 (`--fs-fluid-xs, 11px`) | line 4580 grep 일치. 시안 토큰 매핑 표(보고서 §4) 9/9 라인 실측 일치. | 정직 |
| A16-6 | 핫패치 G1~G5 보존 5/5 PASS | G1 마크업 0 / G2 활성 0 / G3 활성 0 / G4 1건 (line 3651) / G5 1건 (line 3636). | 정직 |
| A16-7 | 외부 자산 0건 | base.html `/static/logo.png` 자체 자산 / 외부 CDN·아이콘·폰트 추가 0건. 5항 체크리스트 통과. | 정직 |
| A16-8 | `.hm2-fixed` 미접촉 (다음 단계 잠금) | base.html grep 0 / style.css 본체에서 미참조 (해당 클래스 정의 부분도 본 단계 미수정). | 정직 |

## 2. 거짓 카운트
- **거짓 2건 (A16-2 / A16-3 — 변경 라인 수 부분 표기)**
- 단, 변경 *내용* 자체는 회귀 G1~G5 영향 없음 / v2 본체 0줄 수정 / 외부 자산 0건. **헤더 1단계 스코프 자체의 정직성은 유지**.

## 3. 결론
- 회귀 PASS (V16 6/6)
- 정직성 거짓 2건 = "변경 라인 수 표기"의 스코프 한정 표기. 헤더 1단계 핵심 주장 (캐시 키 + 토큰화 + v2 본체 보존 + G1~G5)은 모두 실측 일치.
- **99 발사 가능**. 단, 다음 단계 보고서에서는 git diff 전체 stat (insertions/deletions) 병기 권고.
