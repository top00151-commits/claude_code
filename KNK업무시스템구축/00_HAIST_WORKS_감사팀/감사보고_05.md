# 감사보고_05 — D00-06 OPS-011 정식 감사 + D00-02 큐 재정렬 (2026-04-22)

> **수신**: 09 프로젝트팀장  
> **근거**: 지시 D00-06 (OPS-011 01 회신 vs DB 시드 괴리 정식 감사), D00-02 (감사큐 재정렬)  
> **방법**: 코드 직접 정독 (`database.py`, `main.py`)  
> **원칙**: 추정 금지 · 100% 실측 · 불일치 시 재검증 지시

---

## 1. OPS-011 정식 감사 (D00-06)

### 1-1. 구매팀 실측

| 항목 | 실측 결과 | 코드 근거 |
|------|-----------|----------|
| 구매팀 코드 | `"10"` | `database.py` TEAMS 리스트 |
| 구매팀 DB team_id | 10 (INSERT 순서 상 10번째) | `database.py` L906 INSERT 순서 |
| 구매팀장 | **정성진** (role=`"leader"`) | `database.py` USERS 시드 |
| 구매팀 인원 | 정성진·허동준·오용균·김선미·이홍규·박성준·란 (7명) | `database.py` USERS 시드 |

### 1-2. 구매팀장 can_use_logistics 시드 초기값

```python
# database.py L933 — 사용자 INSERT 문 (실측)
"INSERT INTO users(name, login_id, password, team_id, rank, role) VALUES(?,?,?,?,?,?)"
```

- INSERT 컬럼 목록에 `can_use_logistics` **없음**
- DB 스키마 DEFAULT 0 → 모든 신규 사용자 `can_use_logistics = 0`
- 구매팀장 정성진 포함 **전원 0** (시드 실행 후 초기 상태)

**판정: ESC-04 미해소** — 구매팀 can_use_logistics 초기화 쿼리 없음.

### 1-3. /api/admin/user-logistics 권한 가드

```python
# main.py L3313 (실측)
me = require(request, roles=["admin", "ceo"])
```

- 허용 역할: `admin`, `ceo` **만**
- 구매팀장 정성진 role = `"leader"` → 호출 **불가 (403 반환)**

**판정: ESC-05 신규 발견** — 01 메인 D01-02 회신 내 "구매팀장이 `/api/admin/user-logistics`로 toggle 가능"이라는 기술은 **사실과 불일치**. 해당 엔드포인트는 admin/ceo 전용이며 leader는 접근 불가.

### 1-4. D01-02 안B 코드 반영 여부

```python
# main.py L131~147 can_use_logistics() 함수 (실측)
if team_id == 7:   # 제조기술1팀 읽기 허용 (2026-04-22 대표 결재 D01-02 안B)
    return True
```

**판정: D01-02 안B 코드 반영 확인됨** — `team_id == 7` 분기 코드 존재. 제조팀 물류 읽기 허용 구현 완료.

### 1-5. OPS-011 판정 요약

| 항목 | 판정 | 비고 |
|------|------|------|
| 구매팀 team_id | ✅ 확인 (10) | |
| 구매팀장 can_use_logistics 시드값 | ❌ **0 (미설정)** | ESC-04 미해소 |
| 구매팀장 user-logistics API 호출 가능 여부 | ❌ **불가 (403)** | ESC-05 신규 발견 |
| D01-02 안B (제조팀 team_id=7 허용) 코드 반영 | ✅ 확인 | |
| D01-02 회신 "구매팀장 toggle 가능" 기술 | ❌ **사실 오류** | 01 메인 정정 필요 |
| OPS-011 완전 해소 여부 | ❌ **미완** | ESC-04 + ESC-05 해소 후 종결 |

---

## 2. 감사큐 재정렬 결과 (D00-02)

### 현재 ESC 항목 상태 (실측 기준)

| ESC | 내용 | 우선순위 | 상태 |
|-----|------|----------|------|
| ESC-01 | `login.html` 로그인 힌트 평문 노출 (`knk1234` + 임원 실명·ID) | **P0** | ⏸ **대표 지시로 보류** — 테스트 단계 유지, 정식 도입 시 제거 (D01-07 결재) |
| ESC-02 | `require()` 실패 role별 홈 리다이렉트 | **P1** | ✅ **해소** — `role_home()` 함수 `main.py` L113~128 구현 확인 |
| ESC-03 | `home.html` 주간 트렌드 SVG 좌표 하드코딩 | **P2** | 🔄 01 메인 미처리 대기 |
| ESC-04 | `database.py` 구매팀 `can_use_logistics=1` 시드 쿼리 누락 | **P1** | 🔄 **09팀장 ESC-04 구현 착수 예정** |
| ESC-05 | 구매팀장(leader) `/api/admin/user-logistics` 호출 불가 — admin/ceo 전용 | **P1 신규** | 🔴 **D01-NEW-PERM 구현 지시 발행 필요** |

### 미결 감사 대상 잔여 항목 (큐)

우선순위별 재정렬 결과:

**P0 — 즉시 (현재 보류 중)**
- ESC-01: 로그인 힌트 평문 노출 → 대표 지시로 테스트 기간 유지, 정식 도입 전 제거 확정

**P1 — 이번 사이클 처리 대상**
- ESC-04: 구매팀 시드 can_use_logistics 초기화 → 09팀장 직접 구현
- ESC-05: 구매팀장 물류 권한 관리 UI/API → D01-NEW-PERM 구현 지시
- D01-02 회신 정정: 01 메인이 "구매팀장 toggle 가능"이라고 잘못 기술 → C01-04 발행 필요

**P2 — 후속 처리**
- ESC-03: home.html SVG 하드코딩 → 01 메인 미처리
- VR-001~004: next_session.md 수치 불일치 → 01 메인 미처리

**상시 감사 큐 (기존 미착수 항목 — 여전히 대기)**
- 실서버 기동 검증 (bash 권한 없어 정적 검사 대체)
- 02 baby V2 sync.py 실행 가능 여부
- 03 Research 심화리서치 팩트 체크 (샘플 10개)
- admin_settings.html 카카오워크 잔재 6건 처리 여부 (D01-06 추적 — admin 전용이므로 우선순위 낮음)

---

## 3. 신규 발견 결함 목록 (이번 감사)

| 항목 | 유형 | 심각도 |
|------|------|--------|
| ESC-05 | 설계 결함 — 구매팀장 물류 권한 관리 불가 | P1 |
| D01-02 회신 사실 오류 | 01 메인 회신 정정 필요 | P1 |

---

*감사팀 대행 (09팀장) — 2026-04-22*
