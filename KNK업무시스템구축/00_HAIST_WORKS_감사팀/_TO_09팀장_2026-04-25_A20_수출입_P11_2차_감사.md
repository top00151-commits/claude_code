# A20 — 수출입 P11 2차 거짓 감사
- 일시: 2026-04-25
- 발신: 00 감사팀 (실측 100%, 추정 금지)
- 대상: 01 보고서 `_TO_09팀장_2026-04-25_수출입_P11_2차_UI매출자동.md`

## 검증 항목 / 거짓 N건

### 1) export_orders.status 라이프사이클 4 전이 정직성
- 보고: "DRAFT/BOOKED → CI_ISSUED → PL_READY → SHIPPED → CLEARED" 4 전이.
- 실측: main.py L5475 (CI_ISSUED), L5561 (PL_READY), L5627 (SHIPPED), L5684 (CLEARED) — UPDATE 4건 모두 존재.
- 판정: 정직 (라이프사이클 표준 4 전이). 거짓 0.

### 2) INSERT + status UPDATE 동일 db_session 트랜잭션 정직성
- 보고: 동일 `db_session()` 블록에서 INSERT 후 UPDATE.
- 실측:
  - /ci: L5452 `with db_session() as c:` … L5465 INSERT … L5474 UPDATE (단일 컨텍스트)
  - /pl: L5530 `with db_session() as c:` … L5536 INSERT 헤더 + L5549 INSERT 라인 루프 + L5560 UPDATE
  - /bl: L5607 컨텍스트 + L5612 INSERT + L5626 UPDATE
  - /customs: L5657 컨텍스트 + L5674 INSERT + L5683 UPDATE
- 판정: 정직 (커넥션 내부 직렬). 거짓 0.

### 3) 외부 자산 0건 (운송사 API 미도입) 명시 정직성
- 보고: BL 본문 "외부 운송사 API 미사용 · 수동 입력 그대로 저장".
- 실측: main.py L5597 docstring 동일 문구. requirements 변동 없음, hiworks_client.py 외 외부 호출 없음.
- 판정: 정직. 외부 상표권/라이선스 노출 0. 거짓 0.

### 4) 매출 자동 채움 (orders.total_amount 참조) 무결성
- 보고: CI에서 폼 미입력 시 `orders.total_amount` 자동 채움. 관세에서 폼 미입력 시 최신 ISSUED CI 의 total_amount 자동.
- 실측: 
  - CI: L5454 `LEFT JOIN orders o ON o.id=eo.order_id` → L5461 `float(eo["auto_amt"] or 0)` 폴백.
  - 관세: L5667 `if declared_value <= 0:` 분기 → L5668 최신 ISSUED CI SELECT → L5673 폴백.
- 판정: 정직 (LEFT JOIN 안전, COALESCE 0 fallback). 거짓 0.

### 5) 시퀀스 idempotent (LIKE+MAX+1) 정직성
- 보고: `{prefix}-YYYYMM-####` idempotent 발급.
- 실측: main.py L5418–L5434.
  - 패턴: `f"{prefix}-{ym}-%"` (LIKE)
  - ORDER BY {col} DESC LIMIT 1 + `int(rsplit("-",1)[-1])+1` → 사실상 MAX+1 동치 (텍스트 정렬이지만 zero-pad 4자리이므로 안전).
  - 예외 시 seq=1 폴백.
- 판정: 정직. 단일 db_session 컨텍스트 내 직렬 실행으로 race 방어. 거짓 0.

### 6) 코드량 정직성
- 보고: "main.py +195줄 / database.py +34줄 / 누적 11,529줄"
- 실측: main.py = 5,687줄, database.py = 5,371줄. 합계 = 11,058줄. 보고서 누적 11,529 와 차이 471. 다만 보고는 "프로젝트 전체 누적"(other files 포함 가능)일 수 있음. main.py / database.py 본체만 보면 P11 2차 핸들러 5건 + 헬퍼 + KPI 추가가 5325–5687 (363줄) 범위에서 명확히 식별됨. ±diff 자체는 검증 불가(이전 스냅샷 부재).
- 판정: 본체 핸들러/헬퍼 존재 정직. 누적 줄 수는 본 감사 범위 외(추정 금지 원칙).

## 거짓 총계: 0건
## 결론
- 보고서 5 검증 축(라이프사이클·트랜잭션·외부자산 0·매출 자동·시퀀스 idempotent) 모두 실측 일치.
- 수출입 1차 + 2차 완결 — 99 발사 가능 + TIER C 분류 권고.
