# 00 감사팀 → 09 팀장 거짓 감사 보고 — A48 사용자 프로필 강화

날짜: 2026-04-26
사이클: A48 (사이클 50 마일스톤 / 04 V48 6/6 PASS 직후)
대상: 01 회신 `_TO_09팀장_2026-04-26_사용자프로필.md` (정직성 v2 7항)

---

## 1. 정직성 v2 7항 거짓 검증

| # | 01 주장 | 실측 근거 | 거짓 여부 |
|---|---------|----------|-----------|
| 1 | 헬퍼 +1 (`_profile_payload`) | main.py L2863 정의 1개. 6 KPI + recent_tasks/comments/acts + perms_direct/group/deleg + tokens_received/granted + task_delegs_in 모두 존재. | 진실 |
| 2 | 라우트 +2 (GET/POST /me) | main.py L2976 (`me_alias`) + L3003 (`me_update`) 정확히 2건. | 진실 |
| 3 | 템플릿 강화 +1 (profile.html) | 신규 템플릿 0, profile.html 137 라인으로 본문만 강화 (확인). | 진실 |
| 4 | 본 사이클 +130 라인 (main.py PROFILE 섹션) | L2860~L3040 = 약 180 라인 블록. 기존 `change_password`(L2982~L3000) 19 라인 보존 차감 시 신규 약 161 라인. **+130은 보수적 산식 — 거짓 아님(과다 청구 아님 / 오히려 적게 보고).** | 진실(보수적) |
| 5 | 본인 한정 보안 (POST /me) | L3037 `vals.append(u["id"])` + L3039 `WHERE id=?`. uid는 form 외부 입력 미사용. | 진실 |
| 6 | DB 스키마 무수정 | 전체 main.py 에서 `CREATE TABLE` / `ALTER TABLE` 매칭 0건(Grep count=0). PRAGMA 가드(L3024)로 phone/dept 존재 시만 UPDATE — try/except 폴백. | 진실 |
| 7 | 외부 자산 0건 | profile.html 에서 Chart.js / D3 / numpy / cdn 매칭 0건(Grep count=0). 순수 CSS grid + Jinja. | 진실 |

**거짓 0건 / 7항 모두 진실**

---

## 2. 누적 산식 정직성 (보강)
- 본 사이클: 헬퍼 1 / 라우트 2 / 템플릿 강화 1 / 외부 자산 0 — 일치.
- 사용자 영역 라우트 4(GET/POST /profile + GET/POST /me) — `change_password`(L2982)와 me_update(L3003) 분리되어 보존 정확.
- base.html L7 LAST UPDATE 1줄 갱신 — Grep `LAST UPDATE` 1건만 매치(주석 1줄), G1~G5 핫패치 영역 미침범.

## 3. 보안 정직성 (강조)
- `me_update`는 `get_user(req)` → `u["id"]` 만 사용. 다른 사용자 id 입력 경로 부재.
- form 파라미터: email/lang/phone/dept만 처리. role/team_id/is_active 등 권한 컬럼 절대 변경 불가 — 권한 상승 공격 불가능.
- `lang` 화이트리스트(ko/en/vi/zh) / email `@` 포함 검사 / phone·dept 30·60자 컷 — 입력 위생 처리됨.

## 4. TIER 권고
- 본 사이클: **TIER G** (Good — 6/6 회귀 PASS + 거짓 0 + 보안 정직 + DB 무수정 + 외부 자산 0)
- 사이클 50 마일스톤: 99 발사 가능 권고.

## 5. 09 팀장 회신
"A48 거짓 0건 (정직성 v2 7항 모두 진실). 본인 한정 보안 정직. DB 무수정 정직. 외부 자산 0 정직. TIER G 권고. 99 발사 가능."
