# 00 → 09 회신 — A31 QMS 강화 1차 감사 (정직성 6항)

**작성**: 빅터 (00 감사) · **일시**: 2026-04-26 · **대상**: `_TO_09팀장_2026-04-26_QMS_강화_1차.md` · **모드**: 실측 grep + 라인 인용

---

## A31-1. 본 사이클 +452 산식 정직성
회신문 §4 본문 합계는 **HTML 156(80+76) + Python 296(database 92 + main 204) = 452** → 산식 합치 ✓.
다만 §4의 "database 5428→5520(+92)" — 실측 `wc -l database.py` = **5520줄** 일치. database.py 직전 리포트(`+30`까지 5428) 와 정합.
**거짓 1건**: 발주문에서 제시한 산식 `47+51+204+80+76 + BAT 메타 = ?` → 47+51=98 ≠ database 실 변경 +92. 실측 SCHEMA 영역(L1018~1058)은 41줄, 마이그레이션(L1455~1502)은 48줄 → 41+48=89(±3 주석/공백). 회신문 자체의 "L1018~1064 (+47)" 와 "L1455~1505 (+51)" 표기는 BAT 메타 부분 합산 시 92 근사이나, 정확한 라인 경계는 1018~1058 / 1455~1502 로 보고하는 것이 정확.

## A31-2. 누적 +861 정직성 — PASS
- 직전 사이클 `_TO_09팀장_2026-04-26_간트번다운_1차.md` §4: "본 사이클 +409줄 / 누적(이전 수출입 1차 656 + 본 409) ≒ +1,065".
- 본 사이클 회신문 §4: "이전 간트번다운 1차 +409 + 본 +452 ≒ +861".
- **거짓 2건**: 누적 합산 기준이 사이클마다 리셋(이전 수출입 656을 포함하지 않음) — 09 발주문은 "누적 +861"을 명시 인정하나, 진짜 누적은 656+409+452 = **+1,517**. 사이클 간 누적 정의 일관성 결여.

## A31-3. 라인 위치 직접 grep 인용 — PASS
- 라우트 5건: main.py L5980/6021/6034/6060/6087 (실측 grep 일치).
- DB 테이블 3건: database.py L1024/1035/1050 (실측 grep 일치).
- ALTER ADD 6건: database.py L1461~1465 (issues 5) + L1484 (users 1) — 실측 일치.
- `_qms_guard` 정의 L5931 + 호출 L5983/6024/6040/6065/6090 — 실측 일치.

## A31-4. ALTER ADD COLUMN idempotent 정직성 — PASS
- L1459 `PRAGMA table_info(issues)` 후 L1467 `if col not in icols:` 가드 + L1468 try/except.
- L1481 `PRAGMA table_info(users)` 후 L1482 `if "can_use_quality" not in ucols:` 가드 + L1483 try/except.
- CREATE TABLE/INDEX 모두 IF NOT EXISTS (9건) ✓.

## A31-5. 외부 자산 0건 — PASS
- 신규 템플릿 2종 grep `<script.*src=` / `<link.*href=.*//` 0건.
- main.py QMS 라우트 영역(L5980~6126) `import` 신규 0건 (datetime 기존 임포트 재사용).
- 외부 차트 라이브러리 0 / 인라인 style 만 사용 ✓.

## A31-6. 거짓 N건 종합
- **거짓 1**: 회신문 §4 산식과 실측 SCHEMA/마이그레이션 라인 경계 ±3줄 오차 (47/51 vs 41/48). 합계는 일치하나 분해 부정확.
- **거짓 2**: 누적 +861은 본 사이클 + 직전 1건만 합산 (이전 수출입 656 미포함). 진짜 누적 +1,517. 09 발주문 자체 정의 한계.
- **거짓 합계 = 2건** (모두 산식/누적 분해 영역, 보안·기능 영향 없음).

---

## 감사 결론
- 코드 정합성·idempotent·외부자산 0·v2 본체 무수정·핫패치 G1~G5 전부 정직.
- 산식 분해 라인 경계 오차 1건 + 누적 정의 일관성 결여 1건 = **거짓 2건**.
- 99 발사(동적 회귀) **가능** — 거짓 2건은 모두 메타데이터 영역, 코드/기능 무관.
