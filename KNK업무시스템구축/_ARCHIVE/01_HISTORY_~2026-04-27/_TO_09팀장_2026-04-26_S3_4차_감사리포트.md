# [TO 09 팀장] Top3 S3 4차 — 감사 강화 + 권한 리포트 + 만료 정리

- 발신: 01 메인 빅터 / 수신: 09 팀장 / 일자: 2026-04-26
- 발주: S3 4차 — 감사 검색·필터 + 리포트 +3 + 만료 정리 +1 (정직성 v2 7항)
- 시안: `_TO_01_정식시안_Top3_S3_v1.md` §6-2(audit) + §7(report)
- 트랙: 1차(스키마)→2차(UI/RBAC)→3차(그룹/매트릭스) 위에 운영 리포트 이식

---

## 1. 감사 로그 필터 강화 (main.py L5226~5316)

| 메서드 | 경로 | 변경 |
|---|---|---|
| GET | `/admin/permissions/audit` | 1필터 → **5필터** (action·date_from·date_to·actor·target·q) |
| GET | `/admin/permissions/audit.csv` | **신규** · csv 모듈 단독 · UTF-8 BOM · 5000행 |

공용 빌더 `_audit_query()` 분리 — HTML/CSV 동일 필터 재사용.

---

## 2. 리포트 라우트 +3 (main.py L5610~5694)

| 메서드 | 경로 | 비고 |
|---|---|---|
| GET | `/admin/permissions/report/users` | 사용자별 active/granted 토큰 + 그룹 카운트 |
| GET | `/admin/permissions/report/groups` | 그룹별 perm_count + user_count |
| GET | `/admin/permissions/report/expiring?days=7` | days 1~90 · ACTIVE 만 · expires_at<=cutoff |

---

## 3. 만료 정리 라우트 +1 (main.py L5697~5728)

| 메서드 | 경로 | 비고 |
|---|---|---|
| POST | `/admin/permissions/cleanup-expired` | UPDATE EXPIRED + delegation_audit INSERT (각 토큰별 1행, immutable, 단일 트랜잭션) |

audit 누락 0건 — action='EXPIRE', actor=u['id'], details에 cleanup 시각 기재.

---

## 4. 신규 템플릿 +1 (audit 보강 +1)

| 템플릿 | 라인 | 핵심 |
|---|---|---|
| `admin_permissions_report.html` | 신규 121 | 3 리포트 탭 통합 · CSS 단독 · JS 0건 |
| `admin_permissions_audit.html` | 보강 66→103 | 5필터 grid 폼 + CSV 버튼 + token/target 컬럼 |

탭 링크 +1 (리포트) — admin_permissions / matrix / groups 3 템플릿 동시.

---

## 5. 변경 라인 5분리

| 파일 | 라인 | 종류 |
|---|---|---|
| `app/main.py` | 5226~5316 / 5610~5728 (+209) | 감사 빌더+CSV / 리포트3 / 만료정리 |
| `app/templates/admin_permissions_audit.html` | 1~103 (전면 보강) | 필터·CSV |
| `app/templates/admin_permissions_report.html` | 1~121 (신규) | 3 리포트 통합 |
| `app/templates/admin_permissions{,_matrix,_groups}.html` | 각 1줄 | 탭 +1링크 |
| `base.html` L7 + `KNK_시작.bat` / `START.bat` REM/title/echo | 7라인 | LAST UPDATE 갱신 |

---

## 6. 누적 (3 카테고리)

- **본 사이클 (S3 4차)**: 라우트 +5(GET 4 + POST 1) / 템플릿 신규 1 + 보강 1 / 본체수정 0
- **S3 트랙 (1+2+3+4차)**: 테이블 6 / 라우트 6+0+7+5 = **18** / 템플릿 4+0+2+1 = **7** / RBAC + Cascade + audit immutable + 그룹 + 매트릭스 + 5필터·CSV·리포트·만료정리
- **세션 누적**: S1+S2+S3 + JS placeholder + i18n + 매출 KPI/예측 + 환율 + 재고실사 + QMS + 알림 통합 + S3 4차

---

## 7. 핫패치 G1~G5 grep (5/5 보존)

- `static/style.css` 본 사이클 미접촉 → 7건 보존 (L4613·L4731·L4903·L5130·L5371·L5548 + L3629·L3650)
- `app/templates/base.html:95` `핫패치 2026-04-25 17:30 — sb-toggle...` → 보존 (L7 LAST UPDATE만)
- `app/main.py:6299` `# G1~G5 핫패치 보존, v2 본체 무수정.` → 보존
- v2 본체(L4287~L4574) 미접촉 · 외부 자산 0건 (csv stdlib 단독)

---

## 8. 자기검증 v2 7항 + BAT

1. 5분리 §5 / 2. 3카테고리 §6 / 3. 외부자산 0 / 4. G1~G5 grep §7 / 5. v2 본체 0줄 / 6. audit immutable (토큰별 1 INSERT 누락 0) / 7. BAT 양쪽 동시 갱신 (`2026-04-26 S3-4차-감사리포트`)

S3 4차 = 감사 5필터+CSV / 리포트 3 / 만료정리 1. 04 회귀(P1 CEO 기간필터·CSV / P3 팀원 401 / CEO 만료정리→audit row) 진행 가능.
