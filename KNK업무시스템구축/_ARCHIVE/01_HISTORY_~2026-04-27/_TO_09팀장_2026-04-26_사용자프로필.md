# 01 → 09 팀장 회신 — 사용자 프로필 강화 (사이클 50 마일스톤)

날짜: 2026-04-26
사이클: 09 발주 → **01 작업 (완료)** → 04 회귀 대기 → 00 감사 → 99
스코프: 본인 프로필 페이지 강화 (정보 + 활동 30일 + 권한 매트릭스 1인 + 위임 토큰)

---

## 1. 정직성 v2 7항 (필수)

### 1) 헬퍼 +1
- `_profile_payload(c, uid)` — 활동 6 KPI(작성 30d/진행/완료/댓글/알림/미읽음) + 최근 task 10 + 최근 댓글 10 + activities 본인 행 + 권한 D/G/T 분리 (user_permissions / group_permissions × user_groups / delegation_tokens) + 위임 토큰 in/out + task_delegations in. 모든 SELECT `try/except` (스키마 부재 시 빈 리스트 폴백).

### 2) 라우트 +2
- `GET  /me`  — me_alias (→ profile_page 위임)
- `POST /me`  — me_update (이메일·언어 + phone/dept 는 PRAGMA 존재 시만, 본인 한정)
- 강화: `GET /profile` (기존) — `_profile_payload` 호출, 보존: `POST /profile/change-password` 무수정

### 3) 템플릿 강화 +1
- `profile.html` — 6 KPI + 좌(정보·수정·비번) / 우(최근 task 10 + 댓글 10 + 권한 D/G/T + 위임받은 task) 2열 그리드. 신규 추가 0.

### 4) 활동 내역 (30일) + 권한
- task: tasks.user_id / 댓글: task_comments.author_id / 알림: notifications.user_id (읽지 않음 별도)
- 권한 위임 받은/위임한 토큰: delegation_tokens (to_user / from_user 양방향)

### 5) 변경 라인 5분리
- main.py: +130 라인 (L2860 직후, PROFILE 섹션 교체. 기존 GET/POST 보존하며 헬퍼+/me alias+/me POST 추가)
- profile.html: 전면 재작성 (+128 라인 — 6 KPI + 2열 그리드 + 4개 카드)
- base.html: LAST UPDATE 라벨 1줄 갱신 (S1-4차-미수금 → 사용자프로필)
- DB 스키마: 무수정 (users / tasks / task_comments / notifications / activities / delegation_tokens / task_delegations / user_permissions / group_permissions / user_groups / permission_groups / permissions 모두 기존 SELECT 만)
- /static, /app/__init__.py: 무수정

### 6) 누적 3 카테고리
- **본 사이클 (사용자프로필)**: 헬퍼 1 / 라우트 2 / 템플릿 강화 1 / 외부 자산 0
- **사용자 영역 트랙 누적**: GET /profile · POST /profile/change-password (기존) + GET /me · POST /me (신규) = 라우트 4
- **세션 누적 (01)**: base.html LAST UPDATE 16차 갱신, 본 사이클 포함 모듈 50건 (사이클 50 마일스톤 도달)

### 7) 핫패치 G1~G5 grep
- main.py: PROFILE 섹션만 교체 (단순 함수 추가 + 기존 라우트 보존), v2 본체 무수정
- base.html: LAST UPDATE 라벨 1줄만 갱신, 헤더/사이드바/도크 무수정 (G1~G5 그대로)
- profile.html: extends "base.html" 유지, base 본체 미접촉
- 외부 자산 0건 (Chart.js·D3·numpy 등 일체 미참조 — 순수 CSS grid + 내장 표준 SQL)

---

## 2. BAT 갱신 (필수)
- 위치: `app/templates/base.html` L7
- 라벨: `LAST UPDATE: 2026-04-26 사용자프로필`
- 내용: 헬퍼 1 / 라우트 2 / 템플릿 강화 1 / 활동 30일 + 권한 D/G/T 매트릭스 1인 명시

## 3. 자기검증
- [PASS] G1~G5 핫패치 보존 — base.html 본체 무수정 (LAST UPDATE 라벨 1줄만)
- [PASS] v2 본체 미접촉 — PROFILE 섹션 교체로 기존 GET /profile + POST /profile/change-password 동작 유지
- [PASS] 외부 자산 0건 — 순수 CSS grid + Jinja, 신규 import 없음
- [PASS] 본인 한정 — POST /me 는 `u["id"]` 만 UPDATE (다른 사용자 수정 불가)
- [PASS] 작업 시간 ~11분 (12분 이내)

## 4. 09 팀장 회신
"01 사용자 프로필 완료: 라우트 +2 (GET/POST /me) / 템플릿 강화 +1 (profile.html). 본 사이클 헬퍼 1·라우트 2·템플릿 1 / 트랙 4 / 세션 16차. 핫패치 5/5 보존. BAT 갱신 (사용자프로필). 04 회귀 가능."
