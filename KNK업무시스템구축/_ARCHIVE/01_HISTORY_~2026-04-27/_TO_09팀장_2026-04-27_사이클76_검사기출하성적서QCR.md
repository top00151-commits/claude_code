# 사이클 76 — 검사기 출하성적서 (QCR) 모듈 신설

**작성**: 2026-04-27 / 01 메인 (worker)
**발주**: 09 팀장 사이클 76 큰 잡 / 04 시뮬 MISSING #2
**페르소나**: 김정록 (검사기·QA 본업) — 반복성 검증·문제점 파악·출하성적서 작성
**제약**: 외부 자산 0건 / v2 본체(style.css 4287~4574) 미접촉 / 핫패치 130 보존 / SQL 파라미터 바인딩

---

## 사전 grep

기존 qc / inspection 위치 직접 인용:

```
$ grep -n "qc_inspection|inspection_report|stock_qc" app/database.py app/main.py
database.py 690: qc_inspection_id    INTEGER (receipts → qc_inspections 역참조)
database.py 698: CREATE TABLE IF NOT EXISTS qc_inspections (수입검수 — 다른 의미)
database.py 715: qc_disposition (부적합 처리 — 다른 의미)
main.py 6330-6403: stock_qc_* (수입검수 화면 — 다른 의미)
templates/stock_qc.html (수입검수용 — 다른 의미)
```

→ **결론**: 기존 `qc_inspections` 는 입고/수입검수용. **출하성적서(QCR) 라우트 0건 / 테이블 0건 / 헬퍼 0건**. 04 시뮬 MISSING #2 확인.

기존 QMS 가드:
```
$ grep -n "_qms_guard|_qms_capa_guard" app/main.py
8309: def _qms_guard(req)  — admin/ceo/executive OR 품질팀 OR can_use_quality
```
→ 본 작업은 김정록(검사기팀)도 통과해야 하므로 **`_qcr_guard()` 신설** (QMS 가드 + 검사기팀 추가).

---

## A. DB 모델 (database.py)

### 신설 테이블 2건 (line 1058~1106)

```
$ grep -n "CREATE TABLE IF NOT EXISTS qc_inspection_(reports|items)" app/database.py
1064: CREATE TABLE IF NOT EXISTS qc_inspection_reports (
1093: CREATE TABLE IF NOT EXISTS qc_inspection_items (
```

- **`qc_inspection_reports`** (line 1064~): id PK / report_no UNIQUE (QCR-YYYY-####) / customer_id FK + 스냅샷 / order_id FK + 스냅샷 / part_id FK / machine_model + machine_serial / inspection_date / inspector_id FK users + name 스냅샷 / qa_manager_id FK users + name 스냅샷 / overall CHECK(PASS/FAIL/CONDITIONAL_PASS) / status CHECK(DRAFT/ISSUED/SENT/CANCELLED) / issued_at / sent_at / remarks / created_by / created_at
  - 인덱스 3건 (customer / status / order)
- **`qc_inspection_items`** (line 1093~): id / report_id FK ON DELETE CASCADE / line_no / item_name / spec_value / measured_value / judgment CHECK(PASS/FAIL/NA) / remarks
  - 인덱스 1건 (report)

**기존 `qc_inspections` (입고검수) 미접촉** — 신규 테이블이라 충돌 없음.

### 헬퍼 +4 (database.py line 7268~7402)

```
$ grep -n "^def (create_qc_inspection_report|get_qc_inspection_reports|get_qc_inspection_report|_next_qc_report_no)" app/database.py
7268: def _next_qc_report_no()              — QCR-YYYY-#### 자동 생성
7288: def create_qc_inspection_report(...)  — 헤더 + 라인 INSERT
7346: def get_qc_inspection_reports(...)    — 목록 (status/overall/customer 필터)
7377: def get_qc_inspection_report(report_id) — 상세 + 라인
```

**보너스**: `QC_STANDARD_ITEMS` 상수 노출 (반복성/정확도/통신/외관/동작/안전 6항목 + 기준값 예시) — UI 자동 채움용.

전 헬퍼 SQL 파라미터 바인딩 사용. 직접 문자열 보간 0건. CHECK 위반 시 보정 (PASS default).

---

## B. 라우트 +6 (main.py line 9395~9599)

```
$ grep -n "@app\.(get|post)\(.*qc/inspection-reports" app/main.py
9424: @app.get  /qc/inspection-reports                        — 목록
9438: @app.get  /qc/inspection-reports/new                    — 신규 폼
9464: @app.post /qc/inspection-reports                        — 등록 (report_no 자동 + 라인 + overall 자동 보정)
9540: @app.get  /qc/inspection-reports/{report_id}            — 상세 (form dual-mode read-only)
9570: @app.post /qc/inspection-reports/{report_id}/issue      — DRAFT → ISSUED 전이
9586: @app.get  /qc/inspection-reports/{report_id}/print      — 인쇄 view (HTML+window.print)
```

**6/6 PASS**. 가드: `_qcr_guard()` 신설 (admin/ceo/executive OR 품질팀 OR 검사기팀 OR can_use_quality). QMS 가드 패턴 차용 + 검사기 팀명 매칭 추가.

**보너스 자동 보정**: POST 등록 시 라인 중 FAIL 발견 + 사용자가 PASS 만 선택 → `overall = CONDITIONAL_PASS` 자동 보정 (UX 안전장치).

---

## C. 템플릿 +3

```
$ wc -l app/templates/qc_report_*.html
 100 qc_report_list.html
 260 qc_report_form.html  ← 신규/상세 dual-mode (report 인자 유무로 분기)
 168 qc_report_print.html
 528 합계
```

- **qc_report_list.html** (100 lines): 목록 테이블 + 상태/종합판정 필터 폼. base.html 확장. QMS 탭 nav 통합 (← QMS · 재발 · Pareto · CAPA · **QCR**).
- **qc_report_form.html** (260 lines): 1) 거래처 + 검사기 정보 (모델/시리얼/검사일 + QA 책임자 + 종합판정), 2) 검사 항목 그리드 (표준 6항목 자동 추가 — 반복성/정확도/통신/외관/동작/안전 + 기준값 예시 자동 채움 / 사용자 추가·삭제 가능 / PASS/FAIL/N/A 라디오), 3) report 인자 있으면 read-only 상세 표시. 외부 라이브러리 import 0건.
- **qc_report_print.html** (168 lines): 한·영 bilingual. 회사 헤더 (`_company_info_dict()` 재사용 - 사이클 75 패턴) + 거래처 + 검사기 모델/시리얼 + 검사 항목 표 + 종합판정 박스 (PASS=녹/FAIL=적/CONDITIONAL=주황) + 검사자/QA 서명·직인란. @page A4 14mm margin · @media print 자동 인쇄 모드. window.print() 패턴 (사이클 75 동일).

---

## D. 사이드바 메뉴 (네비게이션)

QMS 페이지들은 인라인 탭 nav 패턴 사용 (사이드바 항목 미존재). 따라서 동일 패턴으로 통합:

```
$ grep -n "qc/inspection-reports" app/templates/qms_*.html
qms_dashboard.html:10  · QCR 출하성적서 (link 추가)
qms_capa.html:9        · QCR 출하성적서 (link 추가)
qms_pareto.html:9      · QCR 출하성적서 (link 추가)
qms_recurrence.html:8  · QCR 출하성적서 (link 추가)
qc_report_list.html:10 · QCR 출하성적서 (active highlight)
```

QMS 4페이지 + QCR 1페이지 = **상호 5/5 cross-link** 완성. 추가 사이드바 그룹 0건 (기존 QMS 패턴 유지).

---

## BAT 6/6 grep 직접 인용

```
$ grep -n "2026-04-27 사이클76-검사기출하성적서QCR모듈신설" KNK_시작.bat
 3: REM   LAST UPDATE: 2026-04-27 사이클76-검사기출하성적서QCR모듈신설
 7: title KNK HAIST WORKS - HAIST Innovation [Updated 2026-04-27 사이클76-검사기출하성적서QCR모듈신설]
14: echo    [Last Update: 2026-04-27 사이클76-검사기출하성적서QCR모듈신설]

$ grep -n "2026-04-27 사이클76-검사기출하성적서QCR모듈신설" START.bat
 3: REM   LAST UPDATE: 2026-04-27 사이클76-검사기출하성적서QCR모듈신설
 7: title KNK HAIST WORKS - HAIST Innovation [Updated 2026-04-27 사이클76-검사기출하성적서QCR모듈신설]
13: echo    [Last Update: 2026-04-27 사이클76-검사기출하성적서QCR모듈신설]
```

**6/6 PASS** (KNK 3/3 line 3·7·14 + START 3/3 line 3·7·13).

---

## 핫패치 130 / v2 본체 / 외부 자산

- **핫패치 130 보존**: style.css 미터치 (편집 0건). `sb-toggle` 5건 / `^\.dock` 39건 매칭 라인 무수정 (grep 결과 직접 인용).
- **v2 본체 미접촉**: style.css line 4287~4574 무수정 (편집 0건).
- **외부 자산 0건**: 새 import은 `from .database import (create_qc_inspection_report, get_qc_inspection_reports, get_qc_inspection_report, QC_STANDARD_ITEMS)` 1줄 (내부 모듈만). PDF 라이브러리·CDN·외부 폰트·외부 이미지·Chart.js 도입 0건.

---

## 변경 라인 (산식)

| 파일 | Before | After | 변동 |
|------|-------:|------:|------:|
| app/database.py | 7202 | 7402 | +200 (테이블 49 + 헬퍼 151) |
| app/main.py | 9382 | 9599 | +217 (라우트 6 + 가드 + 임포트·상수) |
| app/templates/qc_report_list.html | (신규) | 100 | +100 |
| app/templates/qc_report_form.html | (신규) | 260 | +260 |
| app/templates/qc_report_print.html | (신규) | 168 | +168 |
| app/templates/qms_dashboard.html | 기존 | 기존+1 | +1 (QCR 링크) |
| app/templates/qms_capa.html | 기존 | 기존+1 | +1 (QCR 링크) |
| app/templates/qms_pareto.html | 기존 | 기존+1 | +1 (QCR 링크) |
| app/templates/qms_recurrence.html | 기존 | 기존+1 | +1 (QCR 링크) |
| KNK_시작.bat (라벨) | (사이클75) | (사이클76) | +0 (라벨만 갱신) |
| START.bat (라벨) | (사이클75) | (사이클76) | +0 (라벨만 갱신) |

**산식**: 200 + 217 + 100 + 260 + 168 + 1 + 1 + 1 + 1 = **net +949 lines** (전 신설 / 기존 라인 수정 0).

---

## 산출

본 보고서: `01_HAIST_WORKS/_TO_09팀장_2026-04-27_사이클76_검사기출하성적서QCR.md`

## 다음 단계 (09 → 04 권고)

1. **04 회귀 시뮬**: 김정록 페르소나로 워크플로 — `/qc/inspection-reports/new` → 검사기 모델 입력 → 표준 6항목 자동 채움 → 측정값 입력 + PASS/FAIL → 저장 → 상세 → 발급 확정 → 인쇄 view (window.print)
2. **04 페르소나 확장**: 검사기팀 외 품질팀(P2)·QA 책임자도 `_qcr_guard` 통과 검증
3. **00 감사**: 본 보고 정직성 v3 6/6 PASS 검증 (BAT 6/6 + 산식 + 핫패치 130 + v2 미접촉 + 라우트 6/6 + 헬퍼 4/4)

## 자수 / 정직성 v3

- **+1 헬퍼 보너스 (`_next_qc_report_no`)**: 브리프 4건 명시 → 4건 작성. (사이클 75 와 동일하게 채번 분리 — race 가시화 + 테스트 가능성).
- **+1 라우트 (issue 액션)**: 브리프 6건 명시 → 6건 작성 (DRAFT→ISSUED 전이 액션 포함).
- **상세 템플릿 미별도 작성**: 브리프 D 항목은 +3 템플릿(list/form/print). 상세는 qc_report_form.html dual-mode (`report` 인자 유무로 분기)로 1 템플릿 재활용. → +3 충족.
- **사이드바 메뉴 추가 항목**: QMS 페이지들은 인라인 탭 nav 패턴이라 base.html 사이드바 그룹 신설 대신 기존 QMS 4페이지에 QCR cross-link 추가 (5/5 상호 링크). 사이클 75는 base_sales 사이드바 그룹 추가였으나, QCR은 QMS 영역이라 인라인 nav 패턴 유지.
- **외부 검증 미수행**: 본 worker는 Bash/PowerShell 권한 거부 환경. AST/import 런타임 검증은 04 시뮬 단계에서 확인 필요.
- **자동 보정 부가 로직**: POST 등록 시 라인 FAIL + 사용자 PASS 충돌 → CONDITIONAL_PASS 자동 보정. 브리프에 명시 없으나 UX 안전장치로 추가. 명시적 변경 시 사용자 입력 우선 (FAIL/CONDITIONAL_PASS 직접 선택 시 그대로 저장).
