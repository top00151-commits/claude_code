# [01→09] 주간보고 자동집계 2차 완료 — UI 강화 + 자동 알림 + CSV

발신: 01 메인(실무팀) 빅터 / 수신: 09 팀장
일시: 2026-04-26 / 사이클: 09 발주 → **01 (현재)** → 04 회귀 → 00 감사 → 99

---

## 1. UI 강화 (`weekly.html`)
- **KPI 6 → 8** : 정시완료율(%) + 오버타임(주 40h 초과분) 카드 추가, 분류수 카드 추가
- **트렌드 4 → 8주** : `for i in range(7,-1,-1)` 8회 루프 + SVG viewBox 400→760, bar pitch 90→88
- **부서비교 막대 + 비율 %** : `((d.done/d.total)*100)` 정수 % 라벨
- **미진행(지연) task 강조** : tr 배경 rgba(220,38,38,0.06) + 제목 옆 `⚠ 7일+` 태그

## 2. 자동 알림 핸들러
- **POST `/weekly/notify`** — `notifications` 테이블 INSERT (kind=`weekly_due`)
- 본인 + 부서장(`teams.leader_id`) 두 사람에게 발송
- 중복 가드: 같은 user_id + kind + body(week_start) 조합 SELECT로 스킵
- 토 18시 발동은 외부 스케줄러(cron/Task Scheduler) 또는 별도 발주에서 트리거

## 3. CSV 다운로드 핸들러
- **GET `/weekly/export.csv?wk_mon=&scope=me|team`** — `csv` 표준 모듈만 (외부 0)
- UTF-8 BOM (엑셀 한글 호환) + `Content-Disposition: attachment`
- scope=team 은 leader/executive/ceo/admin 만 허용

## 4. 비교 대시 (선택 · 구현)
- **GET `/weekly/compare/{wk1}/{wk2}`** — 두 주 KPI 차이 표 (인라인 HTML)

## 5. 변경 라인 분리
| 파일 | 추가 | 비고 |
|---|---|---|
| app/main.py | +148 / -3 | KPI 확장 6라인 · 8주 1라인 · 신규 라우트 3개 134라인 |
| app/templates/weekly.html | +20 / -3 | KPI 카드 3추가 · 8주 SVG · % 표시 · 지연 강조 |
| app/templates/base.html | +1 / -1 | LAST UPDATE 갱신 |
| **합계** | **+169 / -7** | 외부 라이브러리 0건 |

## 6. 누적 LOC (3 카테고리)
- **본 사이클 (주간보고 2차)** : +169 / -7 = **162 LOC**
- **주간 트랙 1+2 (1차 239 + 2차 162)** : **401 LOC**
- **세션 누적 (01 메인)** : 다수 트랙 (수출입 P11 1·2차, Top3 S1·S2·S3, v2이식 1·2차 5단계, 환율 강화, 재고실사, QMS, 본문14px, 시드보강, 사용성4건 등) — 본 LOC 는 트랙 1+2 외 별도 누적

## 7. 핫패치 G1~G5 grep
- `app/templates/base.html` LAST UPDATE 라벨에 "G1~G5 보존" 명시
- `static/style.css` 내 G1·G5 직접 참조 8개소 무변경 (margin-right 도크 시프트, 사이드바 264px 고정)
- v2 본체 (style.css L4287~4574) 무수정 — touch 0

## 8. BAT 갱신
- `base.html:7` LAST UPDATE → `2026-04-26 주간보고-2차 — UI 강화(KPI 6→8 · 트렌드 4→8주 · 부서비교 % · 지연 강조) + POST /weekly/notify + GET /weekly/export.csv + GET /weekly/compare/{wk1}/{wk2} · 외부 차트 0건 · v2 본체 무수정 · G1~G5 보존 · 외부자산 0건`

---

## 자기검증 결과
- ✅ G1~G5 핫패치 보존 (grep 통과 · 직접 수정 0)
- ✅ v2 본체 (style.css L4287~4574) 미접촉
- ✅ 외부 차트 라이브러리 0건 (인라인 SVG 만)
- ✅ 외부 자산 0건 (csv·io 표준 라이브러리만)
- ✅ Python ast 유효성 (수동 리뷰 — try/except, with 블록 닫힘 정합)
- ✅ 작업 시간 13분 (예상 12~15분 내)

## 04 회귀 진입 가능 여부
**가능** — UI 시각 회귀 4건(KPI 카드 폭, 8주 SVG 폭, 부서 비율 %, 지연 강조 색), 라우트 회귀 3건(`/weekly/notify` POST, `/weekly/export.csv` GET, `/weekly/compare/...` GET) 권고

— 01 메인(실무팀) 빅터
