# TO 09팀장 — 01 v2 이식 1차-3단계 (.hm2-fixed + 도크 본체) · v2 1차 완결

- 일시: 2026-04-25 / 발신: 01 메인(실무팀) 빅터
- 범위: `.hm2-fixed` sage 정합 + 도크 본체(.dock) 보더·그림자 + 14px→`--text-sm` 토큰화 + BAT
- **v2 1차 (1·2·3단계) 모두 종료**

## 1. `.hm2-fixed` 영역 v2 정합 (style.css line 2252~2273 · v2 본체 외부)

| line | 변경 전 | 변경 후 |
|---|---|---|
| 2253(주석 +2) | — | `/* v2 1차-3단계: 시안 12B sage 톤 정합 */` 등 |
| 2255 | `background:var(--p-bg)` | `background:var(--sage-50)` |
| 2257 | `border-bottom:1px solid var(--p-border)` | `…var(--sage-200)` |
| 2258 | `box-shadow:…rgba(15,23,42,.04)` | `…rgba(63,92,68,.06)` |
| 2273 | `.hm2-quick-input{font-size:14px;…}` | `font-size:var(--text-sm)` |

→ `.page-head`(sage-50)/`.section-head`와 톤 통일. 핫패치 G5(`.main-wrap` margin-right) 영향 없음 — 도크 열림 시 `+ 새 업무 등록` 버튼 시프트 OK.

## 2. 도크 본체 (.dock line 3650~) — 핫패치 G2·G4 라인 무수정

| line | 변경 전 | 변경 후 |
|---|---|---|
| 3656 | `border-left:1px solid var(--ink-200,#E5E7EB)` | `…var(--sage-200,#E4ECD9)` |
| 3657 | `box-shadow:-8px 0 24px rgba(15,23,42,.08)` | `…rgba(63,92,68,.10)` |

→ 도크 헤더(line 3667 `--grad-sage`)·메시지(`--sage-200`) 등은 이미 시안 12B 정합 완료. 본 단계는 외곽만 sage 통일.
→ G4 line 3653 `top:var(--topbar-h,84px)` 무수정. G2 패턴(`top:0`) 미발생.

## 3. 14px → `--text-sm` 토큰화 (사이클 15 권고)

| line | 클래스 | 변경 | 분류 |
|---|---|---|---|
| 1001 | `.hm-tab` | 14px → `var(--text-sm)` | 탭 라벨 |
| 1790 | `.hm2-date` | 14px → `var(--text-sm)` | 날짜 라벨 |
| 2273 | `.hm2-quick-input` | 14px → `var(--text-sm)` | 입력 placeholder |

→ 본문 14px(.cal-item/.dl-header/.hm-td-*) **보류**. `--text-sm`=clamp(13~15px) min 13px 보장.

## 4. 핫패치 G1~G5 보존 grep

| ID | 패턴 | 기대 | 실측 | 결과 |
|---|---|---|---|---|
| G1 | `class="sb-toggle"` (base.html) | 0 | 0 | PASS |
| G2 | `^\.dock\s*\{[\s\S]*?top:\s*0\s*[;}]` | 0 | 0 | PASS |
| G3 | `^body\.dock-open\s+\.topbar\s*\{` | 0 | 0 | PASS |
| G4 | `top:\s*var\(--topbar-h` | ≥1 | 1 (line 3653) | PASS |
| G5 | `body\.dock-open\s+\.main-wrap\s*\{\s*margin-right:\s*420px` | ≥1 | 1 (line 3638) | PASS |

→ **5/5 PASS**. v2 본체(현재 line 4290 시작 `C안 시안 12B 통째 이식`) **0줄 수정**. 외부 자산 **0건**.

## 5. BAT 갱신 (base.html line 7~8)

```
{# LAST UPDATE: 2026-04-25 v2이식-1차-3단계-도크 — .hm2-fixed sage톤 정합 + 도크 sage-200 보더 + 14px→--text-sm 토큰화 — G1~G5 보존, v2본체 무수정 #}
<link rel="stylesheet" href="/static/style.css?v=20260425v2-stage3-dock">
```

## 6. 변경 라인 수 (이번 사이클 vs 누적 미커밋)

| 구분 | 파일 | +ins | -del | 합 |
|---|---|---|---|---|
| **이번 사이클** | static/style.css | +10 | -8 | 18 |
| **이번 사이클** | base.html | +2 | -2 | 4 |
| **이번 사이클 합계** | — | **+12** | **-10** | **22** |
| **누적 미커밋** (since `f457fdd`) | static/style.css | +234 | -195 | 429 |
| **누적 미커밋** | base.html | +15 | -12 | 27 |
| **누적 미커밋 합계** | — | **+249** | **-207** | **456** |

## 7. v2 1차 완결

- ✅ 1차-1단계 (헤더) — 사이클 14 통과
- ✅ 1차-2단계 (콘텐츠) — 사이클 15 통과
- ✅ 1차-3단계 (.hm2-fixed + 도크) — **본 보고 = 완료**
- → **v2 1차 이식 종료** · 04 회귀 → 00 감사 → 99 흐름

## 8. 04 회귀 포커스

1. `/home` `.hm2-fixed` sage 톤 (배경 sage-50, 보더 sage-200)
2. 도크 열림 시 외곽 sage 보더 + `+ 새 업무 등록` 버튼 비가림(G5)
3. `.hm-tab`/`.hm2-date`/`.hm2-quick-input` 1px 축소 시각
4. 캐시 무효화 `?v=20260425v2-stage3-dock`
5. G1~G5 grep 5/5 PASS · v2 본체 시작 라인 4290 무수정
