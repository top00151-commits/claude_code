# 사이클 78 — 세금계산서 발행여부 체크 (DEC-1)

> **수신**: 09 팀장 (빅터)
> **발신**: 01 메인 worker (일회성)
> **사이클**: 78 — 작은 잡 / DEC-1 대표 결재 이행
> **결재 원문**: "KNK 내부 별도 모듈 사용, 여기서는 발행 여부만 확인 가능하도록"
> **외부 자산 0 / v2 본체 미접촉 / 핫패치 130 보존 / 권한 팝업 0건**

---

## A. 사전 grep — 기존 컬럼/테이블 위치

```
$ grep -n "tax_invoice\|invoice_issued\|세금계산서" 01_HAIST_WORKS/...
app/database.py:762  -- 2차 추가 테이블: invoices(세금계산서), order_status_history(상태 이력)
app/database.py:880  -- 세금계산서 (시안 §3 invoices — 탭4 INV · 2차 시안 정합 보정)
app/main.py:6886     # 수주별 통합 라인: 출하 합계 + 수금 합계 + 세금계산서 발행 여부
app/main.py:6895     (SELECT COUNT(*) FROM invoices i ... ) AS invoice_issued
app/templates/sales_orders.html:76  타임라인 6단계: 견적 → 수주 → 생산 → 출하 → 세금계산서 → 수금
app/templates/sales_shipments_receipts.html:32  <th>세금계산서</th>
app/templates/sales_shipments_receipts.html:46  {% if it.invoice_issued and it.invoice_issued > 0 %}
```

판단:
- 기존 `invoices` 테이블은 자체 채번/금액/VAT 등 풀 모델 — DEC-1 "외부 회계 시스템 사용" 취지와 정합 X.
- DEC-1 취지대로 **`orders` 테이블에 4컬럼 추가** — 외부 발행 여부만 기록하는 경량 패턴.
- 기존 `invoices` 테이블/라우트 0 변동 (회귀 0).

---

## B. DB 컬럼 +4 (orders 테이블)

`app/database.py` line 1847~1863 — `init_db()` 마이그레이션 블록 신설:

```python
# 사이클 78 (2026-04-27 DEC-1) — 세금계산서 발행여부 체크박스
# 외부 KNK 회계 시스템에서 발행 후, HAIST WORKS는 발행 여부만 표시
try:
    ocols = [r[1] for r in c.execute("PRAGMA table_info(orders)").fetchall()]
    for col, decl in [
        ("tax_invoice_issued", "ALTER TABLE orders ADD COLUMN tax_invoice_issued INTEGER DEFAULT 0"),
        ("tax_invoice_no",     "ALTER TABLE orders ADD COLUMN tax_invoice_no TEXT"),
        ("tax_invoice_date",   "ALTER TABLE orders ADD COLUMN tax_invoice_date TEXT"),
        ("tax_invoice_note",   "ALTER TABLE orders ADD COLUMN tax_invoice_note TEXT"),
    ]:
        if col not in ocols:
            try:
                c.execute(decl)
            except Exception:
                pass
except Exception:
    pass
```

PRAGMA 가드 패턴 (기존 next_plan/users.lang/parts.stock_qty 등과 동일).

---

## C. 라우트 +1 (POST 신규)

`app/main.py` line 6826~6853 — POST `/sales/orders/{order_id}/tax-invoice`:

```python
@app.post("/sales/orders/{order_id}/tax-invoice")
async def sales_order_tax_invoice_update(order_id: int, req: Request):
    """세금계산서 발행여부 체크 (사이클 78 DEC-1) ..."""
    u = _s1_guard(req)              # admin/ceo + can_use_sales
    ...
    issued = 1 if issued_raw in ("1", "on", "true", "yes") else 0
    ...
    c.execute("""UPDATE orders SET tax_invoice_issued=?, tax_invoice_no=?,
                 tax_invoice_date=?, tax_invoice_note=? WHERE id=?""", ...)
    return RedirectResponse("/sales/orders", 303)
```

GET `/sales/orders` SELECT 보강 (line 6766~6767):
```sql
COALESCE(o.tax_invoice_issued,0) AS tax_invoice_issued,
o.tax_invoice_no, o.tax_invoice_date, o.tax_invoice_note
```

권한: `_s1_guard` (line 6498) — admin/ceo OR `can_use_sales` (영업/재무 사실상 동일 게이트).

---

## D. 템플릿 보강 (sales_orders.html)

`app/templates/sales_orders.html` 변경:
- line 32 — `<th>세금계산서</th>` 1컬럼 추가
- line 53~58 — 발행/미발행 태그 (발행 시 ✓ + title 으로 발행번호/발행일 표시)
- line 68~92 — `<details>` 접힘 폼: 체크박스 1 / 외부 발행번호 / 발행일 (date) / 비고 / 저장 버튼
- line 88~90 — 사용자 안내문 "세금계산서 발행 자체는 외부 KNK 회계 시스템에서 처리. 여기는 발행 여부 확인만."

목록 컬럼 +1 (✓/미발행 태그). 기존 "생산 시작" 액션 0 변동 (`{% if it.status == 'CONFIRMED' %}` 분기 보존).

---

## BAT 6/6 grep 직접 인용

```
$ grep -n "사이클78-세금계산서발행여부체크_DEC1" KNK_시작.bat
3:REM   LAST UPDATE: 2026-04-27 사이클78-세금계산서발행여부체크_DEC1
7:title KNK HAIST WORKS - HAIST Innovation [Updated 2026-04-27 사이클78-세금계산서발행여부체크_DEC1]
14:echo    [Last Update: 2026-04-27 사이클78-세금계산서발행여부체크_DEC1]

$ grep -n "사이클78-세금계산서발행여부체크_DEC1" START.bat
3:REM   LAST UPDATE: 2026-04-27 사이클78-세금계산서발행여부체크_DEC1
7:title KNK HAIST WORKS - HAIST Innovation [Updated 2026-04-27 사이클78-세금계산서발행여부체크_DEC1]
13:echo    [Last Update: 2026-04-27 사이클78-세금계산서발행여부체크_DEC1]
```

**BAT 6/6 PASS** (KNK 3 + START 3, line 14/13 포함).

---

## 핫패치 130 / v2 본체 / 외부 자산

- `static/style.css` 수정 0줄 (이번 사이클 미접촉)
- `sb-toggle` 매칭 grep 결과 5건 (변동 없음)
- v2 본체 (4287~4574) 미접촉
- 외부 자산 import 0 (Chart.js/numpy/pandas/외부 폰트/CDN 0건)

---

## 변경 라인 (산식)

| 파일 | 변경 라인 | 산식 |
|------|----------|------|
| `app/database.py` | +17줄 (line 1847~1863) | 신규 +17 |
| `app/main.py` | +30줄 (POST 28줄 line 6826~6853 + SELECT 2줄 line 6766~6767) | 신규 +28, 보강 +2 = 30 |
| `app/templates/sales_orders.html` | +43줄 (th +1, td 발행 태그 +7, details 폼 +35) | 신규 +43 |
| `KNK_시작.bat` | 3줄 갱신 (라벨 갱신만, 신규 0) | 갱신 ±3 |
| `START.bat` | 3줄 갱신 (라벨 갱신만, 신규 0) | 갱신 ±3 |

**합산: 신규 +90, 라벨 갱신 ±6 = net 90줄 신규**

---

## 산출

- 본 보고서 1건 (`_TO_09팀장_2026-04-27_사이클78_세금계산서DEC1.md`)
- DB 마이그레이션 1블록 / 라우트 +1 / 템플릿 +1섹션 / 목록 컬럼 +1
- 기존 `invoices` 테이블·라우트·`sales_shipments_receipts.html` 0 변동 (회귀 0)

---

## 사용 흐름 (페르소나 P1 영업 / 재무)

1. KNK 외부 회계 시스템에서 세금계산서 실제 발행
2. HAIST WORKS `/sales/orders` 접속 → 해당 SO 행의 [세금계산서] details 클릭
3. ☑ 발행 완료 / 외부 발행번호 / 발행일 / 비고 입력 → [저장]
4. 목록에 "발행 ✓" 태그 표시 (마우스 hover로 발행번호/발행일 툴팁)

DEC-1 취지 충족: KNK는 외부 회계, HAIST는 추적만.
