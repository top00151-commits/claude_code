# 01 → 09 팀장 회신 — 수출입 P11 3차 (인쇄 view + 일정 알림)

작업일: 2026-04-26 / 사이클 19 (P11 트랙 3차) / 세션 01 / 작업시간 ≈ 13분

## 1. 요약 (정직성 v2 §1)
P11 3차 = 인쇄용 view (CI/PL/BL+관세) + 일정 자동 알림(D-3·CI 90일·관세 미접수).
외부 PDF 라이브러리 0건 — `@media print` HTML 인쇄 layout 만 사용. v2 본체 무수정 · 핫패치 G1~G5 보존.

## 2. 변경 라인 5분리 (정직성 v2 §2)
| # | 분류 | 파일 | 위치/변경 |
|---|------|------|----------|
| L1 | 신규 라우트 (3) | `app/main.py` | L7085 `/export/ci/{ci_id}/print` · L7110 `/export/pl/{pl_id}/print` · L7137 `/export/bl/{bl_id}/print` (모두 `_export_guard`) |
| L2 | 알림 헬퍼 + 트리거 | `app/main.py` | L7164 `check_export_alerts()` (3 카테고리 idempotent · `notify_user` 1시간 중복 방지 사용) · L7225 `POST /export/alerts/check` (admin/ceo/executive 한정) |
| L3 | 신규 템플릿 (3) | `app/templates/` | `export_ci_print.html` · `export_pl_print.html` · `export_bl_print.html` — base 미확장 독립 HTML · A4 비례 · `@media print { actions·no-print { display:none } }` |
| L4 | 디테일 행별 인쇄링크 (3) | `app/templates/export_order_detail.html` | CI/PL/BL 표 헤더 +인쇄 컬럼 1, 본문 `<a target="_blank" .../print>` 3 |
| L5 | BAT 라벨 갱신 | `KNK_시작.bat` | LAST UPDATE / title / echo 3행 → "2026-04-26 수출입-3차-인쇄알림" |

## 3. 누적 3 카테고리 (정직성 v2 §3 — 일정 알림)
| 카테고리 | 트리거 | 대상 |
|---------|--------|------|
| `shipping` | `bills_of_lading.departure_date BETWEEN today AND today+3` AND status ∈ DRAFT/ISSUED | D-3 출항 임박 |
| `ci_expire` | `commercial_invoices.issue_date <= today-90` AND status='ISSUED' | CI 만료 (90일 경과) |
| `customs` | `export_orders.status='SHIPPED'` AND NOT EXISTS 신고(SUBMITTED/CLEARED) | 관세 미접수 |

수신자: `users.is_active=1 AND (team_id=12 OR role IN admin/ceo/executive)` — 베트남법인+관리진.
`notify_user` 1시간 중복 방지 내장 → 다중 호출 idempotent.

## 4. 핫패치 G1~G5 grep 보존 (정직성 v2 §4)
```
app/main.py:6443  # G1~G5 핫패치 보존, v2 본체 무수정.
app/main.py:7083  # G1~G5 핫패치 보존, v2 본체 무수정.
static/style.css:4613/4731/4903/5130/5371  v2 본체(4287~4574) 무수정 · 핫패치 G1~G5 보존
static/style.css:5548  - v2 본체(L4287~L4574) 미접촉 / 핫패치 G1~G5 영향 없음
app/database.py:6287  v2 본체 무수정 · G1~G5 핫패치 보존
```
→ 5건 보존(추가 1건은 본 잡 신규 섹션 헤더). 외부 PDF 라이브러리 grep `(reportlab|weasyprint|pyfpdf|fpdf|wkhtmltopdf|pdfkit)` = **0건**.

## 5. v2 본체 미접촉 (정직성 v2 §5)
- `app/main.py` v2 본체(라우트 그룹 ~L6440) 미접촉 — 신규 코드는 모두 L7080 이후 신규 섹션.
- `app/templates/export_{ci,pl,bl_customs}.html` (1·2차 산출물) 미접촉.
- 인쇄 템플릿은 신규 3건, base 미확장 독립 HTML.

## 6. BAT 갱신 (정직성 v2 §6)
- `KNK_시작.bat` LAST UPDATE = `2026-04-26 수출입-3차-인쇄알림` (라우트·헬퍼·템플릿·인쇄링크·외부PDF 0건 명기)
- title / echo 줄 동시 갱신.

## 7. 04 회귀 가능 여부 (정직성 v2 §7)
✅ 가능. 회귀 포인트:
- `GET /export/ci/{ci_id}/print` · `GET /export/pl/{pl_id}/print` · `GET /export/bl/{bl_id}/print` — 권한(베트남법인/admin/영업) 가드, 미존재 ID → `/export` 리다이렉트.
- `POST /export/alerts/check` — admin/ceo/executive 만 호출 가능, 응답 `{ok, fired:{shipping,ci_expire,customs}}`.
- `export_order_detail` 페이지에서 각 행 "인쇄" 버튼 클릭 → 새 창 인쇄 view → 브라우저 인쇄.

## 회신
"01 수출입 3차 완료: 인쇄 +3 / 일정 알림. 본 사이클 19 / 트랙 P11 3차 / 세션 01. 핫패치 5/5. BAT 갱신. 04 회귀 가능."
