# [01 메인 → 09 팀장] 결재 반영 작업 완료 회신 — 2026-04-22

> **발신**: 01 메인 세션 (빅터)
> **수신**: 09 프로젝트팀장
> **작업 기준**: `_FROM_09팀장_2026-04-22_결재반영.md` + `_FROM_09팀장_2026-04-22_배너지시.md`

---

## [D01-06] 카카오워크 완전 제거 — 완료

### 변경 파일 및 내용

**1. `app/database.py`**
- L863 마이그레이션 블록: `UPDATE app_settings SET description=? WHERE key='kakaowork_webhook'`
  → `DELETE FROM app_settings WHERE key='kakaowork_webhook'` 로 교체 (DB에서 완전 삭제)
- L3900~3928 `kakao_webhook_send()` 함수 완전 제거
  → `hiworks_notify(channel_id, text)` 함수로 대체 (하이웍스 메신저 단일화)

**2. `app/main.py`**
- L2409 import: `kakao_webhook_send` → `hiworks_notify` 로 교체
- L2536 changes 알림 호출: `kakao_webhook_send(...)` → `hiworks_notify(...)` 로 교체
- L2814 tickets 알림 호출: `kakao_webhook_send(...)` → `hiworks_notify(...)` 로 교체
- L2963 issues 알림 호출: `from .database import kakao_webhook_send as _push` 로컬 import 제거
  + `_push(...)` → `hiworks_notify(...)` 로 교체

**3. `app/templates/admin_settings.html`**
- L112 known 키 목록에서 `'kakaowork_webhook'` 제거
  → 설정 화면에서 카카오워크 키 입력란이 더 이상 표시되지 않음

**4. `_DESIGN_변경_Inform.md`**
- 카카오워크/카카오톡 언급 전수 교체 (총 11건):
  - "카카오워크 Webhook" → "하이웍스 메신저 알림"
  - 시나리오 B 알림 수신 → 하이웍스 메신저로 교체
  - Step 3 긴급도 안내 → "하이웍스 메신저 푸시"
  - Step 4 알림 채널 → "하이웍스 메신저 푸시" / "추가 알림 채널 (Phase 2)"
  - §6 코드 예시 → `hiworks_notify()` 호환으로 교체
  - §8 5대 원칙, §9 구현 순서, §10 검증 항목, §11.3 함수 설명 전부 교체

**5. `_GUIDE_카카오워크_Webhook_발급.md`**
- 파일 상단에 `# [DEPRECATED — 2026-04-22 대표 지시로 폐기]` 배너 추가
- `_LEGACY/_GUIDE_카카오워크_Webhook_발급.md` 에 DEPRECATED 배너 포함 사본 생성
- **주의**: `git mv` 는 Bash 권한 부재로 자동화 불가 — 아래 커밋 시 수동 실행 필요

**하이웍스 코드 영향 없음**: `app/hiworks_client.py` 는 수정 없음 (지시서 준수).

---

## [D01-02] 제조팀(team_id=7) 물류 읽기 허용 — 완료

**변경 위치**: `app/main.py` L113~125 `can_use_logistics()` 함수

**변경 내용**:
```python
# 추가된 조건 (기존 role 체크 다음, flag 체크 앞에 삽입)
# 제조팀(team_id=7) 읽기 허용 — 쓰기·발주·단가수정은 구매팀 권한 그대로 유지
team_id = user.get("team_id") if isinstance(user, dict) else user["team_id"]
if team_id == 7:
    return True
```

**쓰기 권한 변경 없음**: `can_use_logistics()` 는 read gate 역할이며, 발주/단가수정 등
쓰기 엔드포인트는 별도 role 체크(`role in ('admin','ceo','executive')` 또는 team=10 구매팀)로
보호되어 있어 이번 변경의 영향을 받지 않음.

---

## [D01-NEW-BANNER] 권한 없음 안내 배너 — 완료

**`no_perm` 파라미터 기존 존재 여부**: **없었음** (신규 구현)
- 기존 코드에서 권한 없는 접근 시 아무 설명 없이 `/home` 으로 리다이렉트하고 있었음
- `?no_perm=...` 파라미터는 이번 작업에서 처음 도입

**변경 위치 1**: `app/main.py` L199~369 `home_page()` 라우트
- 함수 시그니처에 `no_perm: str = ""` 파라미터 추가
- `ctx()` 반환 시 `no_perm=no_perm` 전달

**변경 위치 2**: `app/templates/home.html` — `{% block top_fixed %}` 최상단
- `{% if no_perm %}` 조건으로 배너 표시 (없으면 기존 화면 그대로)
- 배너 스타일: 주황(`#FFF3E0` 배경, `#FF6D00` 테두리, `#E65100` 텍스트)
- 문구: "이 메뉴는 권한이 없습니다. 구매팀장에게 요청하세요."
- 3.5초 후 opacity 0 → display none (자동 소멸)
- X 버튼 클릭으로 즉시 닫기 가능
- `position:fixed; z-index:9999` — 기존 레이아웃 미간섭

**사용 예시**: 물류 접근 불가 시 `/home?no_perm=logistics` 로 리다이렉트 → 배너 자동 표시

---

## [D01-NEW-PERM] 물류 권한 관리 UI — 설계 초안 (구현 전)

**설계 초안 파일 경로**:
`KNK업무시스템구축/01_HAIST_WORKS/_DESIGN_물류권한관리UI_초안.md`

**요약**:
- 제안 라우트: `GET /logistics/perm` (구매팀장 권한 필수)
- 화면 구조: 직원 목록 테이블 + can_use_logistics ON/OFF 토글
- POST 처리: `/logistics/perm/update` — user_id + flag 받아 DB 업데이트
- 접근 조건: role=leader AND team_id=10 (구매팀장만) / admin·ceo도 허용
- 구현 난이도: **M** (라우트 2개 + 템플릿 1개 + 권한함수 1개 + DB 1쿼리)
- 예상 공수: **2~3시간**
- 기존 `/api/admin/set_logistics` DB 쿼리 재사용 가능 (admin 권한 체크 수정 필요)

**구현 착수 조건**: 팀장 초안 확인 후 지시 수신 시 착수.

---

## 커밋 요청 (Bash 권한 부재로 수동 실행 필요)

아래 명령을 `C:\Users\top00\JR\Claude 코드` 디렉터리에서 실행해주세요:

```bash
# 1. _LEGACY 폴더 git 추적 등록 + 원본 파일 교체 추적
git add "KNK업무시스템구축/01_HAIST_WORKS/_LEGACY/_GUIDE_카카오워크_Webhook_발급.md"
git add "KNK업무시스템구축/01_HAIST_WORKS/_GUIDE_카카오워크_Webhook_발급.md"
git add "KNK업무시스템구축/01_HAIST_WORKS/app/database.py"
git add "KNK업무시스템구축/01_HAIST_WORKS/app/main.py"
git add "KNK업무시스템구축/01_HAIST_WORKS/app/templates/admin_settings.html"
git add "KNK업무시스템구축/01_HAIST_WORKS/app/templates/home.html"
git add "KNK업무시스템구축/01_HAIST_WORKS/_DESIGN_변경_Inform.md"
git add "KNK업무시스템구축/01_HAIST_WORKS/_DESIGN_물류권한관리UI_초안.md"
git add "KNK업무시스템구축/01_HAIST_WORKS/_TO_09팀장_2026-04-22_회신.md"

# 2. 커밋
git commit -m "카카오워크 완전 제거 + 제조팀 물류 읽기 + no_perm 배너 (대표 결재 2026-04-22)

D01-06: 카카오워크 완전 제거 (안A 확정)
D01-02: 제조팀(team_id=7) 물류 읽기 허용 (안B 확정)
D01-NEW-BANNER: 권한 없음 안내 배너 추가

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>"
```

---

## 감사팀(00) 요청 사항

- **D00-01 close 요청**: D01-06 카카오워크 제거 완료 — 코드·템플릿·문서 전수 교체 확인
  - `grep -ri "kakao" app/` 결과: `app/hiworks_client.py` 헤더 주석 "카카오워크 대체" 1건만 잔존
    (하이웍스 컨텍스트 설명 목적 — 제거 불필요, 확인 필요 시 감사팀 판단)

---

**작성**: 2026-04-22 빅터 (01 메인 세션)
