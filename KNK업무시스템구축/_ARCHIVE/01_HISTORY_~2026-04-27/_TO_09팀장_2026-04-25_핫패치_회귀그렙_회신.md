# [01 → 팀장] P1-2 빅터 도크 핫패치 회귀 grep 회신

> **발신**: 01 메인 (빅터)
> **수신**: 09 빅터 팀장
> **발행**: 2026-04-25
> **참조 지시**: `_FROM_09팀장_2026-04-25_04힐링QA_1차_01지시.md` §2
> **결론**: ✅ **4/4 PASS**

---

## 1. 핫패치 라인 (재확인)

| 파일 | 위치 | 핫패치 |
|---|---|---|
| `app/templates/base.html` | line 93 | `<div class="sb-toggle">◀</div>` DOM 제거 → 주석 라인으로 대체 |
| `static/style.css` | line 3618~3621 | `.dock { top: 0 }` → `.dock { top: var(--topbar-h, 84px) }` |
| `static/style.css` | line 3606 | `body.dock-open .topbar { padding-right: 420px }` 규칙 제거 (주석화) |

→ 핫패치 라인 **유지 보존 완료**. v2 정식 이식 시 본 라인 위에 덮어쓰지 않을 것.

---

## 2. grep 4종 재현 결과

### 2-1. grep #1 — `<div class="sb-toggle"` (DOM) → **0건이어야**
```
패턴: <div class="sb-toggle"
범위: app/templates/base.html
결과: 0 건
판정: ✅ PASS
```

검증 명령 (재현용):
```
grep -rn '<div class="sb-toggle"' app/templates/base.html
→ (출력 없음)
```

참고: `app/templates/base_logi.html:47`에 `<div class="sb-toggle" onclick=...>◀</div>` 잔존하나, 본 핫패치 범위는 **base.html (메인 사이드바)** 한정 — `base_logi.html`은 매출·자재 전용 별도 베이스로 09팀장 지시 §2에 미포함.

### 2-2. grep #2 — `.dock { top: 0 }` → **0건이어야**
```
패턴: \.dock\s*\{[^}]*top\s*:\s*0
범위: static/style.css 전체
결과: 0 건 (multiline 매칭 적용)
판정: ✅ PASS
```

확인: `static/style.css:3620-3621` 의 실제 규칙은
```css
.dock {
  position: fixed; top: var(--topbar-h, 84px); bottom: 0; right: -420px;
  ...
}
```
(`bottom: 0`은 있으나 `top: 0`은 없음 — 핫패치 원본 의도 그대로)

### 2-3. grep #3 — `body.dock-open .topbar` 실제 규칙 → **0건이어야 (주석만 OK)**
```
패턴: body\.dock-open\s+\.topbar\b (literal class)
범위: static/style.css, app/templates/**
결과:
  - static/style.css:3606 — 주석 (/* … 제거 */) ✅
  - app/templates/base_logi.html:243 — `body.dock-open .topbar-logi` (별도 클래스 .topbar-logi, 핫패치 대상 아님)
판정: ✅ PASS (실제 .topbar 규칙 0건, 주석만 잔존)
```

해석: `body.dock-open .topbar { padding-right: 420px }` (이전 핫패치 제거 대상) 는 **css 활성 규칙 0건**. `.topbar-logi`는 매출·자재 베이스용 별도 셀렉터로 핫패치 의도 무관.

### 2-4. grep #4 — `.dock { top: var(--topbar-h)` → **1건 이상이어야**
```
패턴: \.dock\s*\{[^}]*top\s*:\s*var\(--topbar-h
범위: static/style.css
결과: 1 건 (line 3620-3621)
  static/style.css:3620 .dock {
  static/style.css:3621   position: fixed; top: var(--topbar-h, 84px); bottom: 0; right: -420px;
판정: ✅ PASS
```

---

## 3. v2 시안 §4 grep 검증 15종 포함 확인

본 3종 (`sb-toggle` / `.dock top:0` / `.dock top:var`) 가 v2 §4 grep 검증 15종에 **포함되도록 권고** (현재 §4는 12종만 명시 — 05 디자인팀 응답 `f457fdd` 커밋 §4 12/12 PASS 보고 참조).

→ 09팀장 결정: §4를 15종으로 확장 시 본 3종 추가 / 또는 별도 §4-bis 핫패치 회귀 전용 grep 섹션 신설

---

## 4. 종합 판정

| # | grep 패턴 | 기대 | 실측 | 판정 |
|---|---|---|---|---|
| 1 | `<div class="sb-toggle"` (base.html) | 0 | 0 | ✅ |
| 2 | `.dock { top: 0 }` | 0 | 0 | ✅ |
| 3 | `body.dock-open .topbar` (활성 규칙) | 0 | 0 | ✅ |
| 4 | `.dock { top: var(--topbar-h)` | ≥1 | 1 | ✅ |

**4/4 PASS**

---

## 5. 핫패치 라인 보존 약속

- v2 정식 이식 (1차 04-26 23:00, 2차 04-27 23:00) 진행 시 본 핫패치 3개 라인 **유지·확장**.
- 덮어쓰기 발생 시 즉시 09팀장 통보 + 사전 승인 후 진행.
- 05 디자인팀에 본 핫패치 라인 보존 의무 재공유 권고.

---

*발행: 2026-04-25 · 01 빅터 (메인 세션) · 코드 수정 0줄 (검증만)*
