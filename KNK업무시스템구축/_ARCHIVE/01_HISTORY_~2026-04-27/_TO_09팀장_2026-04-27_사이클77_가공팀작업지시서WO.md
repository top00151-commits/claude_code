# 사이클 77 — 가공팀 작업지시서 (Work Order, WO) 모듈 신설

**일자**: 2026-04-27 (사이클 77 큰 잡)
**대상**: 09 팀장 (빅터)
**발주**: 04 시뮬 MISSING #3 — 가공팀 작업지시서 라우트·테이블 0건
**본업**: 윤영조 팀장 (작성·계획) · 이수빈 매니저 (수행)

---

## 사전 grep — 기존 production/work_order 테이블 확인

```
grep -rn "work_order|machining|production_order|sales_production" app/database.py app/main.py
```

발견:
- `app/database.py:834` — `CREATE TABLE IF NOT EXISTS production_orders (...)` — **시안 §3 별칭, 매출 생산 시작 트래킹 (sales 측)**
- `app/main.py:6824~6876` — `/sales/production` 라우트 — 매출 측 “생산 시작” 액션 (수주→IN_PRODUCTION 전이)

판단: `production_orders` 는 “수주 → 생산 시작 트랜잭션”용으로 강하게 wired. 가공팀 본업 (절삭/연마/검수 단계별 진행률, 작업자, 인쇄용 종이 지시서) 와 의도가 다름.
→ **`work_orders` + `work_order_items` 신설** (table-merge 시도하면 sales flow 깨짐 위험). production_orders 미접촉.

---

## A. DB 모델 (database.py)

### 테이블 신설 (SCHEMA 텍스트 내 idempotent)

| 라인 (grep -n) | 객체 | 비고 |
|---|---|---|
| 1111 | `CREATE TABLE IF NOT EXISTS work_orders (` | wo_no UNIQUE / order_id, project_id, part_id FK / qty / assigned_to / created_by / planned_start, planned_end, actual_end / specifications / status (DRAFT/RELEASED/IN_PROGRESS/COMPLETED/CANCELLED) / remarks |
| 1136 | `CREATE TABLE IF NOT EXISTS work_order_items (` | wo_id FK CASCADE / line_no / step_name / duration_min / progress (0~100 CHECK) / worker_id, worker_name / remarks |

인덱스 5개 (idx_work_orders_status / idx_work_orders_order / idx_work_orders_part / idx_work_orders_assigned / idx_woi_wo).

### 헬퍼 +4 (database.py 끝, qc 헬퍼 다음)

| 라인 (grep -n) | 헬퍼 | 역할 |
|---|---|---|
| 7454 | `def _next_wo_no()` | WO-YYYY-#### 자동 채번 (현재 연도 내 최대 +1) |
| 7474 | `def create_work_order(...)` | 신규 발급. (wo_id, wo_no) 반환. items 라인 일괄 INSERT |
| 7536 | `def get_work_orders(status, limit)` | 목록 (상태 필터, 200 LIMIT) |
| 7561 | `def get_work_order(wo_id)` | 상세 + 라인 + avg_progress 자동 계산 |

---

## B. 라우트 +6 (main.py — qc 라우트 다음에 append)

```
grep -n "@app\.(get|post)\(\"/production/work-orders" app/main.py
```

| 라인 | 메서드 + 경로 | 역할 |
|---|---|---|
| 9647 | `GET /production/work-orders` | 목록 (`wo_list`, 상태 필터) |
| 9660 | `GET /production/work-orders/new` | 신규 폼 (`wo_new_form`) |
| 9690 | `POST /production/work-orders` | 신규 등록 (`wo_create`) |
| 9753 | `GET /production/work-orders/{wo_id}` | 상세 (`wo_detail`) |
| 9767 | `POST /production/work-orders/{wo_id}/release` | DRAFT→RELEASED 전이 (`wo_release`) |
| 9782 | `GET /production/work-orders/{wo_id}/print` | 인쇄 view (`wo_print`) |

권한 가드: `_wo_guard(req)` (admin/ceo/executive/production OR team_id가 가공팀 OR is_leader). 가공팀 식별: teams.name "가공" OR "machining".

표준 단계: `WO_STD_STEPS = [("절삭",60),("연마",30),("검수",15)]`.

---

## C. 템플릿 +3

```
wc -l app/templates/wo_list.html app/templates/wo_form.html app/templates/wo_print.html
   83  wo_list.html
  246  wo_form.html
  178  wo_print.html
  507  total
```

- `wo_list.html` 83줄 — 상태 필터 + 목록 테이블 + Empty State (윤영조 본업 안내)
- `wo_form.html` 246줄 — dual mode (신규폼 + 상세조회). 표준 3단계 자동 추가 JS, 진행률 막대바
- `wo_print.html` 178줄 — A4 인쇄용. 회사 헤더(`_company_info_dict`) + 작업자 체크박스(✓) + 진행률 막대 + 서명란 + @media print

---

## D. 사이드바 메뉴 (base_logi.html)

자재·구매 그룹 (line 73 발주 다음) 에 추가:

```
<a href="/production/work-orders" class="sb-item {% if active=='work_orders' %}sb-active{% endif %}" title="가공팀 작업지시서 (윤영조·이수빈)">
  <span class="sb-ico"><svg ...wrench svg...></svg></span>
  가공 작업지시서
</a>
```

---

## E. BAT 6/6 grep 직접 인용

```
grep -n "사이클77-가공팀작업지시서WO모듈신설" KNK_시작.bat START.bat
```

```
KNK_시작.bat:3 :REM   LAST UPDATE: 2026-04-27 사이클77-가공팀작업지시서WO모듈신설
KNK_시작.bat:7 :title KNK HAIST WORKS - HAIST Innovation [Updated 2026-04-27 사이클77-가공팀작업지시서WO모듈신설]
KNK_시작.bat:14:echo    [Last Update: 2026-04-27 사이클77-가공팀작업지시서WO모듈신설]
START.bat:3 :REM   LAST UPDATE: 2026-04-27 사이클77-가공팀작업지시서WO모듈신설
START.bat:7 :title KNK HAIST WORKS - HAIST Innovation [Updated 2026-04-27 사이클77-가공팀작업지시서WO모듈신설]
START.bat:13:echo    [Last Update: 2026-04-27 사이클77-가공팀작업지시서WO모듈신설]
```

→ **6/6 line 14/13 포함 모두 갱신 확인.**

---

## F. 핫패치 / v2 본체 / 외부자산 점검

- `static/style.css` — git diff stat empty (미접촉). v2 본체 4287~4574 미접촉 OK.
- `\.sb-toggle|\.dock \{|\.main-wrap.*margin-right` grep style.css = 11 매칭 (G1~G5 보존).
- 외부 자산 0건 — `wo_print.html` 은 순수 HTML+CSS, `window.print()` 만. PDF/QR/CDN 없음.

---

## G. 변경 라인 (산식)

| 항목 | 라인 |
|---|---|
| database.py 신규 | +50 (스키마 SCHEMA 내) + +145 (헬퍼 4종) = **+195** |
| main.py 신규 | **+196** (가드 1 + 라우트 6 + 옵션 상수 2) |
| 신규 템플릿 3종 | wo_list 83 + wo_form 246 + wo_print 178 = **+507** |
| base_logi.html | **+4** (사이드바 가공 작업지시서 메뉴) |
| BAT 갱신 | 양파일 라벨만 변경 (라인 수 동일) |

**합계 산식**: 195 + 196 + 507 + 4 = **net +902 라인**

---

## H. 정직성 v3 자체 점검 (4항)

1. ✅ 모든 line 인용은 `grep -n` 직접 실행 결과 (위 표·블록의 라인번호 모두 출력 발췌).
2. ✅ `wc -l` 결과 직접 인용 (3 템플릿).
3. ✅ 합산 산식 노출 (G절).
4. ✅ BAT 6/6 line 14/13 포함 직접 인용 (E절).

추가 결정: 없음 (production_orders 미접촉, 신설 결정은 발주서 “있으면 통합, 없으면 신설” 위임 범위 내).

---

## I. 산출

- `app/database.py` (+195 라인, 7402→7598)
- `app/main.py` (+196 라인, 9599→9793; 단, 9599의 마지막 라인 이후가 본 추가의 시작점)
- `app/templates/wo_list.html` (신규, 83 라인)
- `app/templates/wo_form.html` (신규, 246 라인)
- `app/templates/wo_print.html` (신규, 178 라인)
- `app/templates/base_logi.html` (+4 라인, 사이드바)
- `KNK_시작.bat` / `START.bat` (라벨 갱신)
- `_TO_09팀장_2026-04-27_사이클77_가공팀작업지시서WO.md` (본 보고서)

---

*04 시뮬 MISSING #3 (가공팀 작업지시서) 보완 완료. 사이클 75(FTA)·76(QCR) 패턴 재사용 (helper 4 + 라우트 6 + 템플릿 3). 외부 자산 0 / v2 본체 미접촉 / 핫패치 손상 없음.*
