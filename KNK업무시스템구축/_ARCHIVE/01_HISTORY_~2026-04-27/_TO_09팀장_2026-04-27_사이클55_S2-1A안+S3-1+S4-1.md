# 사이클 55 spike 완료 (S2-1 A안 + S3-1 + S4-1)

**일자**: 2026-04-27
**산출**: 01 메인 세션
**근거**: 사이클 54 04 운영검증 발견 3건 통합 처리 (빅터 09 팀장 A안 자체 채택)

---

## 작업 A (S2-1 A안) — `/fx/rates` 명시적 분리

GET / POST 두 핸들러 docstring에 외부 호환 endpoint 표기 추가. 라우트 자체는 유지 (중복 제거 0건).

```
app/main.py:8641: @app.get("/fx/rates", response_class=HTMLResponse)
app/main.py:8662: @app.post("/fx/rates")
```

### docstring 본문 (양쪽 동일 명문화)

```
외부 도메인 코드 호환용 영문 표준 경로 환율 관리 endpoint.
사내 사용자는 기존 /rates 사용 권장 (사이드바 링크).
본 endpoint는 외부 시스템·API 통합용으로 별도 운영.
사이클 55 (2026-04-27) S2-1 A안 적용.
```

### fx_rates.html 안내 추가 (+4줄)

`app/templates/fx_rates.html` line 11~14:

```html
<div style="background:#EEF2FF;border:1px solid #C7D2FE;color:#3730A3;padding:8px 14px;border-radius:8px;margin-bottom:14px;font-size:12px;">
  외부 호환 endpoint입니다. <a href="/rates">기존 환율 페이지</a> 권장.
</div>
```

---

## 작업 B (S3-1 사이드바 노출)

`app/templates/base_logi.html` line 111: `/fx/rates` 메뉴 추가 (+5줄, role 가드 포함).

```
{% if user.role in ['admin', 'finance', 'ceo', 'executive'] %}
<a href="/fx/rates" class="sb-item {% if active=='fx_rates' %}sb-active{% endif %}">
  ...
  환율(외부)
</a>
{% endif %}
```

라벨 "환율(외부)"로 기존 "환율 관리"와 명확 구분. 일반 사용자에게는 노출되지 않음.

---

## 작업 C (S4-1 POST 필드 보강)

### main.py line 8724 POST `/parts/{part_id}/prices` 핸들러 보강

신규 Form 파라미터 3건 추가:

```python
negotiated_at: str = Form(""),
min_qty: str = Form("0"),
max_qty: str = Form(""),
```

`part_price_create()` payload 에 negotiated_at / min_qty / max_qty 정합 (이미 database.py:4644 의 기존 시그니처가 받음 — 컬럼은 part_prices line 370~372 에 존재).

`price_change_log()` 호출 추가 — 기존 헬퍼 (database.py:6446) 재사용.

```python
prev = part_active_price(part_id, supplier_id=sid or 0) or {}
old_price = prev.get("unit_price")
...
price_change_log(part_id, sid, old_price, new_price,
                 effective_from, u["id"], note=note or "")
```

기존 `/parts/{pid}/prices/new` (line 4751) 와 동일 패턴 (4773~4794) 채택. 훅 실패는 try/except 로 흡수해 본 등록은 보존.

### part_prices.html line 92~104 폼 +12줄

협상일 / 최소 수량 / 최대 수량 필드 추가. grid-template-columns 11열로 확장 (140px 110px 130px 110px 130px 130px 130px 110px 110px 1fr 110px). 안내 문구에 "사이클 55 S4-1" 표기 추가.

---

## BAT grep 검증 (6/6 통과)

```
KNK_시작.bat:3:REM   LAST UPDATE: 2026-04-27 사이클55-환율단가-S2-1_A안+S3-1+S4-1
KNK_시작.bat:7:title KNK HAIST WORKS - HAIST Innovation [Updated 2026-04-27 사이클55-환율단가-S2-1_A안+S3-1+S4-1]
KNK_시작.bat:14:echo    [Last Update: 2026-04-27 사이클55-환율단가-S2-1_A안+S3-1+S4-1]
START.bat:3:REM   LAST UPDATE: 2026-04-27 사이클55-환율단가-S2-1_A안+S3-1+S4-1
START.bat:7:title KNK HAIST WORKS - HAIST Innovation [Updated 2026-04-27 사이클55-환율단가-S2-1_A안+S3-1+S4-1]
START.bat:13:echo    [Last Update: 2026-04-27 사이클55-환율단가-S2-1_A안+S3-1+S4-1]
```

6/6 라인 갱신 확인.

---

## 핫패치 보존

`grep -c -E "\.sb-toggle|\.dock \{|\.main-wrap" static/style.css` = **130 매칭** (사이클 54 후 = 130 → 사이클 55 후 = 130, 변동 0).

## v2 본체 미접촉

`stat static/style.css | grep Modify` = `Modify: 2026-04-25 21:27:46.133500100 +0900` (사이클 55 spike 동안 mtime 불변). v2 본체 (style.css 4287~4574) 절대 미접촉 확인.

## 외부 자산 0

새 import 외부 라이브러리 = **0건**. 기존 helper (`part_active_price`, `price_change_log`, `CURRENCIES`, `PRICE_TYPES`) 만 재사용. SQL 파라미터 바인딩 의무는 `part_price_create` / `price_change_log` 내부에서 ?-binding 으로 충족.

---

## 변경 라인 산식 (정직성 v3 #4)

라인 수 (post-edit):
- `app/main.py`: **8775줄** (사이클 54 = 8742, +33)
- `app/templates/base_logi.html`: **255줄** (+5)
- `app/templates/fx_rates.html`: **129줄** (+4)
- `app/templates/part_prices.html`: **156줄** (+12, 144 → 156)

산식:
- main.py: docstring GET `/fx/rates` +6 + POST `/fx/rates` +6 + POST `/parts/{part_id}/prices` 보강 +21 = **+33줄**
- base_logi.html: 사이드바 메뉴 +5
- fx_rates.html: 안내 +4
- part_prices.html: 폼 필드 +12 (+ grid-template 1열 분할 인라인 변경 0줄)

합산: 신규 +54 + 조정 0 = **net +54줄**

누적: 사이클 54 = 876 → 사이클 55 = **930줄** (876+54)

---

## 산출 위치

`01_HAIST_WORKS/_TO_09팀장_2026-04-27_사이클55_S2-1A안+S3-1+S4-1.md` (본 파일)

---

## 제약 준수 체크리스트

- [x] v2 본체 (style.css 4287~4574) 미접촉 — mtime 2026-04-25 21:27:46 불변
- [x] 핫패치 G1~G5 — 130/130 유지
- [x] BAT 양파일 6/6 라인 갱신
- [x] 외부 자산 0건
- [x] SQL 파라미터 바인딩 의무 — `part_price_create` / `price_change_log` 내부 ?-binding
- [x] 정직성 v3 #1 line 인용 grep -n 직접
- [x] 정직성 v3 #2 wc -l 결과 직접 인용
- [x] 정직성 v3 #3 합산 산식 직접 노출
- [x] 정직성 v3 #4 BAT grep 6/6 직접 인용
