# [09 → 01] 사이클 87 — 정찰 좌표 + 즉시 실행 패치 (대표 "바로 적용" 지시)

> 본 첨부: 사이클 87 발주서 (`_FROM_09팀장_2026-04-28_사이클87_헤더v3정식진행_05전달반영.md`) 별첨
> 09 팀장 정찰: 2026-04-28
> 대표 지시: **"팀장 실무팀 바로 적용할수있게 진행시켜"**

---

## 1. 사전 정찰 결과 (09 팀장 grep -n / Read 직접 검증)

### 1-1. base.html (메인 통합) — 변경 라인 좌표

| 라인 | 현재 코드 | 처리 |
|---|---|---|
| 14 | `<header class="topbar">` | 유지 |
| 21-24 | `.brand-logo-wrap + <img>` | 유지 (05 G6 핫패치 보존) |
| 25-28 | `<div class="brand-text">...` | CSS `display:none` 만 (HTML 유지) |
| **31-55** | **`<div class="ws-wrap" id="wsWrap">...</div>`** | **전체 삭제 → `<nav class="ws-tabs">` 신규** |
| **58-67** | **`<div class="tb-center"><form class="g-search">...</form>...</div>`** | **빈 div로 (`<div class="tb-center"></div>`) — 검색은 dock 안으로 이동** |
| 68-90+ | `<script> gSearchSuggest ... </script>` | dock-search 안의 input과 함께 갱신 (script 함수명 보존 권장 — 호환성) |

### 1-2. base_sales.html (매출·영업 Hub) — 변경 라인 좌표

| 라인 | 현재 코드 | 처리 |
|---|---|---|
| 13 | `<header class="topbar topbar-hub-sales">` | 유지 |
| 18-27 | `<a href="/sales" class="tb-brand">` + 로고 + tb-title-group | 유지 (CSS로 tb-title-group 숨김) |
| **28 직전** | (없음) | **`<div class="tb-divider"></div><nav class="ws-tabs">...</nav>` 신규 삽입** |
| 30-49 | `.tb-right` (victorTrigger / tb-home-link / user-chip / logout) | victorTrigger 유지 |
| **35-38** | **`<a href="/home" class="tb-home-link" title="통합 메인">...</a>`** | **삭제** (ws-tabs로 흡수) |
| 253 | `body.dock-open .topbar-hub-sales{padding-right:420px;...}` | 유지 |

### 1-3. base_logi.html (자재·구매 Hub) — 변경 라인 좌표

| 라인 | 현재 코드 | 처리 |
|---|---|---|
| 13 | `<header class="topbar topbar-hub-logi">` | 유지 |
| **35** | **`<a href="/home" target="_blank" rel="noopener" class="tb-home-link" title="HAIST WORKS 메인 창 열기">`** | **삭제** (ws-tabs로 흡수) |
| (28 직전 동일 위치) | (없음) | **ws-tabs 신규 삽입** (tb-left 안) |
| 250 | `body.dock-open .topbar-hub-logi{padding-right:420px;...}` | 유지 |

### 1-4. main.py — 변경 불필요 ✅

| 라인 | 코드 | 비고 |
|---|---|---|
| 99 | `def workspaces_for(user):` | 변경 없음 |
| 114 | `def current_workspace_for(path: str):` | 변경 없음 |
| 153 | `"workspaces": workspaces_for(user) if user else [],` | 변경 없음 |
| 154 | `"current_workspace": current_workspace_for(...),` | 변경 없음 |

→ ws-tabs는 기존 context (`workspaces` / `current_workspace`) 그대로 사용. **main.py 0줄 수정**.

### 1-5. style.css — 추가 위치

- v2 본체 보호 구역: 라인 4287~4574 (수정 금지)
- G5 핫패치: line 3604~3626 (.dock 영역, 보존)
- G6 헤더 로고: line 4342~4358 (보존, 단 사이즈 축소 v3는 오버라이드로 가능 또는 직접 수정)
- **추가 위치 권장**: 파일 **맨 끝**에 `/* === HEADER REDESIGN v3 === */` 섹션으로 신규 추가 (오버라이드 우선순위 활용, 기존 무수정)

---

## 2. 즉시 실행 패치 (01 워커 Edit/Write 도구만 쓰면 됨)

### 패치 A — base.html line 31-55 교체

**삭제 대상 (Edit old_string)**:
```html
    {% if workspaces and workspaces|length > 1 %}
    <div class="tb-divider"></div>
    <div class="ws-wrap" id="wsWrap">
      <button class="ws-switcher" id="wsBtn" type="button" aria-haspopup="true" aria-expanded="false">
        <svg class="ws-ico" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round"><rect x="3" y="3" width="7" height="7" rx="1.5"/><rect x="14" y="3" width="7" height="7" rx="1.5"/><rect x="3" y="14" width="7" height="7" rx="1.5"/><rect x="14" y="14" width="7" height="7" rx="1.5"/></svg>
        <span class="ws-name">{{ (current_workspace.name if current_workspace else '통합') }}</span>
        <span class="ws-caret">▾</span>
      </button>
      <div class="ws-menu" id="wsMenu" role="menu">
        {% for w in workspaces %}
        <a class="ws-opt {% if current_workspace and w.key == current_workspace.key %}active{% endif %}"
           href="{{ w.href }}"
           {% if w.external %}target="_blank" rel="noopener"{% endif %}>
          <div class="opt-ico">{{ w.icon }}</div>
          <div class="opt-meta">
            <div class="opt-name">{{ w.name }}</div>
            <div class="opt-desc">{{ w.desc }}</div>
          </div>
          {% if current_workspace and w.key == current_workspace.key %}<span class="opt-check">✓</span>
          {% elif w.external %}<span class="opt-ext">↗</span>{% endif %}
        </a>
        {% endfor %}
      </div>
    </div>
    {% endif %}
```

**삽입 (Edit new_string)** — 05 발주서 §4-1 그대로:
```html
    {% if workspaces and workspaces|length > 1 %}
    <div class="tb-divider"></div>
    <nav class="ws-tabs" role="tablist" aria-label="워크스페이스 전환">
      {% for w in workspaces %}
      <a class="ws-tab {% if current_workspace and w.key == current_workspace.key %}active{% endif %}"
         href="{{ w.href }}"
         {% if w.external %}target="_blank" rel="noopener"{% endif %}
         role="tab"
         aria-selected="{% if current_workspace and w.key == current_workspace.key %}true{% else %}false{% endif %}"
         title="{{ w.desc }}">
        <span class="ws-ico" aria-hidden="true">{{ w.icon }}</span>
        <span class="ws-name">{{ w.name }}</span>
        {% if w.external %}<span class="ws-ext" aria-hidden="true">↗</span>{% endif %}
      </a>
      {% endfor %}
    </nav>
    {% endif %}
```

### 패치 B — base.html line 58-67 교체 (tb-center 검색 → 빈 div)

**삭제**:
```html
  <div class="tb-center" style="position:relative">
    <form class="g-search" action="/search" method="get" role="search">
      ... (form 전체)
    </form>
    <div id="gSearchSugg" ...></div>
  </div>
```

**삽입**:
```html
  {# 핫패치 v3 — tb-center 검색 제거 (빅터 도크로 이전) — 빈 div 로 flex 레이아웃 유지 #}
  <div class="tb-center"></div>
```

### 패치 C — base.html dock 영역에 모드 탭 + 검색 패널 추가

위치: 기존 `<aside class="dock" id="victorDock">` 안에 (05 발주서 §4-2 그대로)

### 패치 D — base_sales.html / base_logi.html

- ws-tabs 삽입: line 28 직전 (tb-left 닫힘 직전, brand 다음)
- tb-home-link 삭제: base_sales line 35-38 / base_logi line 35-38
- 도크 영역에 모드 탭 + 검색 패널 추가 (base.html과 동일 패턴)

### 패치 E — style.css 맨 끝에 신규 섹션 추가

05 발주서 §3 (line 46~277) 그대로 복붙. 파일 맨 끝에 추가 (기존 무수정).

### 패치 F — base.html script 영역에 setDockMode + "/" 단축키 추가

05 발주서 §5 (line 428~468) 그대로. 기존 gSearchSuggest 함수는 보존 (dock 안 검색 input이 호출).

---

## 3. 자체 검증 grep 베이스라인 (사전 정찰)

### 정합 (PASS = ≥1) — 작업 후 기대값
```bash
# base.html
grep -c '<nav class="ws-tabs"' app/templates/base.html         # 현재 0 → 작업 후 ≥1
grep -c 'data-mode="victor"'  app/templates/base.html          # 현재 0 → 작업 후 ≥1
grep -c 'data-mode="search"'  app/templates/base.html          # 현재 0 → 작업 후 ≥1
grep -c 'setDockMode'         app/templates/base.html          # 현재 0 → 작업 후 ≥3

# base_sales.html / base_logi.html
grep -c '<nav class="ws-tabs"' app/templates/base_sales.html   # 현재 0 → 작업 후 ≥1
grep -c '<nav class="ws-tabs"' app/templates/base_logi.html    # 현재 0 → 작업 후 ≥1

# style.css
grep -c '\.ws-tab\.active::before' static/style.css            # 현재 0 → 작업 후 ≥1
grep -c 'wsActivePulse' static/style.css                       # 현재 0 → 작업 후 ≥1
```

### 결함 (PASS = 0) — 작업 후 기대값
```bash
grep -c '<button class="ws-switcher"' app/templates/base.html  # 현재 1 → 작업 후 0
grep -c '<div class="ws-menu"' app/templates/base.html         # 현재 1 → 작업 후 0
grep -c '<form class="g-search"' app/templates/base.html       # 현재 1 → 작업 후 0
grep -c 'tb-home-link' app/templates/base_sales.html           # 현재 1 → 작업 후 0
grep -c 'tb-home-link' app/templates/base_logi.html            # 현재 1 → 작업 후 0
```

---

## 4. 작업 순서 (1차 +4h 마감)

```
[Step 1] base.html 패치 A (ws-wrap → ws-tabs)               ~10분
[Step 2] base.html 패치 B (tb-center 빈 div)                ~5분
[Step 3] style.css 패치 E (맨 끝 신규 섹션 277줄)            ~15분
[Step 4] base.html 패치 C (dock 모드 탭 + 검색 패널)         ~30분
[Step 5] base.html 패치 F (script setDockMode + "/" 단축키)  ~10분
[Step 6] 자체 grep 14종 + 1차 회신                          ~10분

= 1차 마감 +80분 (4h 한도 내 충분)

[Step 7] base_sales.html 패치 D                             ~20분
[Step 8] base_logi.html 패치 D                              ~20분
[Step 9] 모바일 반응형 (1100px 이하)                         ~30분
[Step 10] 자체 grep 14종 + 2차 회신                         ~10분

= 2차 마감 +80분 (8h 한도 내 충분)
```

---

## 5. BAT 갱신 (1차/2차 마감 직후)

```
2026-04-28 G7헤더재설계v3_가로탭amber펄스+빅터도크모드분리(빅터검색)+brand-text숨김+로고축소48-64px+3base동시 (선행 G6 7건 보존)
```

KNK_시작.bat / START.bat 양쪽 line 3 + line 7 = 4 곳.

---

## 6. 주의 사항

### 6-1. base.html race 회피 (사이클 81 워커 동시 작업)
- 사이클 81 base.html 영역: 사이드바 i18n (line 100~250 추정)
- 사이클 87 base.html 영역: 헤더 (line 14~70) + dock (line 미정)
- 영역 분리 → 동시 가능. 다만 Edit 시 충돌 시 재시도

### 6-2. G6 핫패치 보존 (메모리 #4)
- `.brand-logo-wrap { background: transparent; box-shadow: none; }` 보존
- `.dock { top: var(--topbar-h, 84px); }` 보존
- `<img src="/static/logo.png?v=20260425healing">` line 22 보존

### 6-3. 정직성 v3
- 변경 라인 전부 grep -n 직접 인용
- 14종 grep 결과 표 회신 의무
- 추정 0건 — 모든 좌표는 09 팀장 직접 정찰 확인

---

## 7. 회신 형식

`01_HAIST_WORKS/_TO_09팀장_2026-04-28_사이클87_헤더v3_회신_1차.md`

05 발주서 §10 양식 + 본 첨부 §3 grep 14종 + §5 BAT 갱신 결과.
