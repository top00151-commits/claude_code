# [발주] 사이클 81 핫패치 — 로고 + i18n + 로그인 lang 세션 + 윤영조 권한

> 본 발주서: `KNK업무시스템구축/_TEAM_ORIENTATION.md` 참조 의무
> 발주: 09 팀장 (빅터) → 01 메인
> 발주 시각: 2026-04-28
> 우선순위: 🚨 핫패치 (대표 직접 지시 단계별 클릭 점검)
> 메모리 #5 main.py 순차 적용 — 본 사이클 main.py 단독

---

## 1. 배경 (대표 단계별 클릭 점검)

대표 인용:
> "로그인화면에서 언어 선택도 안되고.. 언어를 선택해도 메뉴들이 다 해당언어로 변화지도 않고..
> 로그인 페이지에 회사 로고 이미지도 틀렸어.."

**현재 결함 3건:**

### A. 로고 디자인 자체 오류
- 현재: `static/logo.svg` + `templates/login.html` 인라인 SVG 모두 **K-N-|||-K (4 모양)** 형태
- 본사 공식 로고: `C:\Users\top00\JR\01_업무관련\98_문서양식\공용양식\본사\회사기본정보\회사 로고.JPG` (KNK 3글자)
- 추가 자료: `260330_KNK 로고 메뉴얼 (1).pdf` (2026-03-30 본사 공식 매뉴얼)

### B. 로그인 lang_selector 작동 안 함
- login.html 라인 207-209: `<a href="?lang=ko">`, `?lang=vi`, `?lang=en` 존재
- 그러나 GET /login 핸들러가 `?lang=` 파라미터를 세션·쿠키에 저장 안 할 가능성

### C. i18n propagation 누락
- i18n.py 472+ 키 (sb_my_work, sb_home, sb_daily, sb_calendar...) 보유
- base.html / base_sales.html / base_logi.html 사이드바가 하드코딩 한국어일 가능성 → grep -n 직접 인용 검증 의무

---

## 2. 작업 범위

### Step 1. 회사 공식 로고 시스템 통합 (외부자산 아님 — 사내 자료)

```
1) C:\Users\top00\JR\01_업무관련\98_문서양식\공용양식\본사\회사기본정보\회사 로고.JPG
   → 01_HAIST_WORKS/static/knk_logo_official.jpg 복사

2) login.html 라인 218-225 인라인 SVG 삭제
   → <img src="/static/knk_logo_official.jpg" alt="KNK" style="height:clamp(40px,2.8vw + 24px,56px);width:auto"> 교체

3) static/logo.svg 사용처 grep 전체 확인 (base.html / 기타 템플릿)
   → 정식 SVG 시안은 05 디자인팀이 사이클 83에서 매뉴얼 PDF 참조 트레이스 제작 (협업)
   → 사이클 81에서는 임시로 jpg 사용
```

### Step 2. main.py GET /login lang 파라미터 처리

```python
@app.get("/login")
def login_page(request: Request, lang: str | None = None, error: str | None = None):
    # ?lang=ko/vi/en 받으면 세션·쿠키에 저장
    if lang in ("ko", "vi", "en"):
        request.session["lang"] = lang  # 또는 쿠키
    current_lang = request.session.get("lang", "ko")
    # ... i18n 컨텍스트 주입
```

- 정확한 위치: grep -n "def login" main.py 결과 인용 의무
- session 미들웨어 / 쿠키 방식 중 기존 시스템 패턴 따를 것

### Step 3. base.html / base_sales.html / base_logi.html 사이드바 i18n propagation

- 사이드바 메뉴 텍스트 grep -n 모두 인용
- 하드코딩 한국어 → `{{ t('sb_xxx') }}` 또는 `{{ i.sb_xxx }}` 형식 (기존 패턴 따를 것)
- 누락 키 발견 시 i18n.py 추가 (vi / en 번역 누락 0건)

### Step 4. 윤영조 사용자 권한 정정 (작은 잡)

- database.py 또는 직접 SQL: `UPDATE users SET can_use_logistics=1 WHERE name='윤영조'`
- 본업: 02 검사기팀 베트남 출장·물류 접근 필요 (사이클 79 시점 누락)

---

## 3. 지시 사항 (메모리 #1~#7 의무)

- **권한 팝업 0건** (메모리 #1): sandbox 차단 시 `dangerouslyDisableSandbox` 사용 또는 보고만, 사용자 팝업 금지
- **BAT 6/6 갱신** (메모리 #4 순차): 본 사이클 워커가 마무리 시 한 번만 (race 방지). 09 팀장이 이미 BAT을 사이클 81~83 라벨로 갱신했으니 사이클 82·83 워커는 BAT 추가 갱신 금지 (race 방지)
- **외부 자산 0** (메모리 #6): 회사 로고 JPG는 사내 자료 — OK / PDF 매뉴얼도 사내 자료 — OK
- **정직성 v3**: grep -n 직접 인용 / wc -l 직접 인용 / 추정 0건
- **사이드바 본질** (메모리 #7): 새 사이드바 메뉴 추가 금지 (기존 메뉴 i18n만)

---

## 4. 회신 형식

- **회신 파일**: `01_HAIST_WORKS/_TO_09팀장_2026-04-28_사이클81_회신.md`
- 인용 형식: ` (라인 N) `original` → `new` ` 형식
- 주요 grep 인용 의무:
  - `grep -n "knk_logo_official\|logo.svg\|polygon.*K-N" templates/login.html`
  - `grep -n "def login\|session\[.lang\]" app/main.py`
  - `grep -n "sb_my_work\|sb_home\|t(.sb_" templates/base*.html`
  - `wc -l` 변경 파일 라인 수 변화

---

## 5. 다음 사이클 연결

- 사이클 82 (04): 페르소나 직접 로그인 시뮬 — 본 사이클 81 완료 직후 회귀 테스트
- 사이클 83 (05): 디자인 단계별 깊이 점검 — 사이클 81에서 임시 jpg 로고 → 정식 SVG 시안 협업
- 사이클 84 (00): 감사 — 81/82/83 통합 검증
