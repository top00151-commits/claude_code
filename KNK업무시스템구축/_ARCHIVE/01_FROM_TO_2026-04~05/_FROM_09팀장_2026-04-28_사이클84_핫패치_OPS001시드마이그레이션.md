# [발주] 사이클 84 핫패치 — OPS-001 시드 마이그레이션 (자재 모듈 광역 차단 해소)

> 본 발주서: `KNK업무시스템구축/_TEAM_ORIENTATION.md` 참조 의무
> 발주: 09 팀장 (빅터) → 01 메인
> 발주 시각: 2026-04-28
> 우선순위: 🚨 P0 (5차 누적 미해소 — 대표 직접 지시 "바로 수정")
> 메모리 #5 main.py 순차: 본 사이클은 **database.py 단독** → 사이클 81과 병렬 가능

---

## 1. 배경 (04 보고)

**참조 문서**:
- `04_HAIST_WORKS_운영테스트팀/_TO_09팀장_2026-04-28_10명시각시연결과.md`
- `04_HAIST_WORKS_운영테스트팀/_TO_01세션_2026-04-28_OPS001_5차재요청.md`

**04 4차 재현 결과 (2026-04-28)**:

| 페르소나 | 로그인ID | 팀 | 시도 URL | 실제 final | 결과 |
|---|---|---|---|---|---|
| 허동준 | hdj | 구매 member | `/logistics` | `/home` | 🔴 |
| 김선미 | ksm | 구매 member | `/parts` | `/home` | 🔴 |
| 윤광훈 | ykh2 | **검사기 member** | `/parts` | `/home` | 🔴 (영향 확장) |

→ 5차 누적. 시드 마이그레이션 누락 의심.

---

## 2. 작업 (옵션 B 채택 — 시드 마이그레이션, 04 옵션 B 권장)

### Step 1. database.py seed_all() 마지막에 추가

```python
# app/database.py:seed_all() 마지막
LOGI_TEAMS = ('02', '06', '07', '08', '09', '10')  # 검사기·전장·제조·가공·구매
c.execute(f"""
    UPDATE users
    SET can_use_logistics = 1
    WHERE team_id IN (
        SELECT id FROM teams WHERE code IN {LOGI_TEAMS}
    )
""")
```

→ 정확한 LOGI_TEAMS 코드는 04 보고 또는 teams 테이블 직접 grep 후 결정 (현재 추정값). 오류 발생 시 04에 재확인 의무.

### Step 2. 데이터 일관성: leader도 자동 허용 옵션

`app/main.py:113 can_use_logistics(user)` 함수:
- 현재 `("admin", "ceo", "executive")` → **leader 추가** (04 옵션 A 병행)
- 코드 수정은 1줄

### Step 3. 회귀 (04 사이클 86에서 재검증)

**통과 기준**:
- `hdj / knk1234` → `/logistics` → 200 응답 + title "🏭 자재 허브"
- `ksm / knk1234` → `/parts` → 200 + 부품 리스트
- `ykh2 / knk1234` → `/parts` → 200 + 부품 리스트

---

## 3. 지시 사항

- **권한 팝업 0건** (메모리 #1)
- **BAT 갱신 race 방지** (메모리 #4): 사이클 81 워커가 이미 BAT 갱신 ("사이클81~83+전직원_비번리셋_안지연복구"). 본 사이클 워커는 BAT에 사이클 84 추가 시 race 충돌 회피 — 사이클 81 회신 도착 후 BAT 갱신
- **외부 자산 0** (메모리 #6)
- **정직성 v3**: grep -n teams 테이블 코드 직접 인용 / wc -l 변경 라인 수
- **메모리 #5 main.py 순차**: main.py 1줄 수정 (leader 추가)은 사이클 81의 main.py 작업 종료 후 진행

---

## 4. 회신 형식

- **회신 파일**: `01_HAIST_WORKS/_TO_09팀장_2026-04-28_사이클84_회신.md`
- 인용:
  - `grep -n "code" app/database.py` (teams 테이블 코드 확인)
  - `grep -n "can_use_logistics" app/main.py app/database.py`
  - `wc -l` 변경 파일

---

## 5. 04 옵션 C 결재 (별도)

본 발주서와 함께 **04에 시드 재실행 권한 임시 부여** 결재 발행 — 04가 `python -c "from app.database import seed_all; seed_all()"` 1회 실행 후 회귀 검증 가능 (비간섭 원칙 일시 해제).
