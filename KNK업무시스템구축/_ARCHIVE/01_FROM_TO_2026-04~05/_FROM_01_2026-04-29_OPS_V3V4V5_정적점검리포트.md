# [01 → 04] OPS V3·V4·V5 정적 점검 리포트

> 발신: 01 메인 세션 / 수신: 04 운영테스트팀 / 참조: 09 팀장
> 일자: 2026-04-29
> 트리거: 대표 직접 지시 — "(다) 04 회귀 진행 중 빅터 정적 점검 병렬"
> 정직성 v3 / 권한 팝업 0건

---

## 0. 한 줄

빅터(01)의 코드 정적 점검 영역 V3·V4·V5 검토 → **결함 4건 즉시 핫패치 + 동적 페르소나 검증은 04 영역으로 위임**.

---

## 1. 빅터가 한 일 / 안 한 일 (정직성 v3)

| 영역 | 빅터(01) 가능 | 04 운영테스트팀 영역 |
|---|---|---|
| 라우트 존재 | ✅ grep 검증 | — |
| 함수 로직 정확성 | ✅ 코드 리뷰 | — |
| 입력 검증 누락 | ✅ 정적 발견 | — |
| 명백한 산식 버그 | ✅ 정적 발견 | — |
| 페르소나 시뮬레이션 | ❌ 불가 | ✅ 김순례·임택훈·윤영조 클릭 |
| UI 직관성 | ❌ 불가 | ✅ 1분 안에 처리 가능한가 |
| 산식 결과값 정확성 | ❌ 불가 | ✅ 실제 데이터로 계산 비교 |
| 데이터 모델 변경 | ❌ 영향 큼 | — (별도 사이클) |

→ **빅터는 정적 안전성만 보장. 사용자 경험·데이터 정확성은 04 책임**.

---

## 2. V3 rates_cost_sim — 결함 1건 핫패치

### 점검 대상
- GET `/rates/cost-sim/{part_id}` (line 9367)
- POST `/rates/cost-sim` (line 9383)

### 정상 항목
- ✅ 가드 `_rates_guard` 적용 (구매팀+admin/ceo)
- ✅ 산식 `target = base × rate × (1 + margin/100)` 논리적 정확
- ✅ DB 저장 try/except 처리

### 결함 → 핫패치 (line 9402)
```python
# 수정 전: 입력 검증 0건
pid = int(part_id)        # 비정상 입력 시 ValueError 500
base = float(unit_price_base)
rate = float(exchange_rate)
margin = float(margin_pct or 0)

# 수정 후: 4중 검증
try:
    pid = int(part_id); base = float(...); rate = float(...); margin = float(...)
except (ValueError, TypeError):
    return RedirectResponse("/rates?error=입력값+형식+오류", 303)
if pid <= 0 or base <= 0 or rate <= 0:
    return RedirectResponse(f".../{pid}?error=...", 303)
if margin < -100:
    return RedirectResponse(f".../{pid}?error=...", 303)
```

### 04 영역 (페르소나 검증)
- 임택훈(자재팀) 입장: 시뮬레이션 결과가 직관적인가? 환율 변동 시 KRW 환산값이 한눈에 보이는가?
- 실제 USD/JPY/CNY 부품 단가로 시뮬 → 회계 시스템 비교 검증

---

## 3. V4 stock_reorder — 결함 1건 핫패치

### 점검 대상
- GET `/stock/safety` (line 9483)
- POST `/stock/safety/{part_id}` (line 9495)
- GET `/stock/reorder-recommendations` (line 9515)
- POST `/stock/alerts/check` (line 9530)
- `recommend_reorders()` (database.py line 3879)
- `parts_safety_update()` (database.py line 3962)

### 정상 항목
- ✅ 산식 명시: 부족율 = (rop - on_hand)/rop, HIGH≥0.5/MID 0.2~0.5/LOW<0.2
- ✅ ZeroDivisionError 방어 (`if rop > 0 else 0`)
- ✅ stock_balances LEFT JOIN — 입고 0인 부품도 포함
- ✅ 음수 가드 (`max(0.0, ...)`)
- ✅ 가드: `_s2_guard` (구매팀)
- ✅ 알림 트리거는 admin/ceo 한정

### 결함 → 핫패치 (line 9526)
```python
# 수정 전: limit 무제한 → ?limit=999999 가능
async def stock_reorder_page(req: Request, limit: int = 200):

# 수정 후: 1~1000 클램프
limit = max(1, min(int(limit or 200), 1000))
```

### 04 영역 (페르소나 검증)
- 윤영조(자재팀장) 입장: HIGH 우선순위 항목이 진짜 시급한가? MID·LOW 분류가 현실 감각과 맞는가?
- 안전재고 / ROP / 권장량 3개 값의 비즈니스 로직 적정성 (실제 발주 리드타임 반영 여부)

---

## 4. V5 stock_qc — 결함 2건 핫패치

### 점검 대상
- GET·POST `/stock/qc/{po_item_id}` (line 6847·6856)
- GET·POST `/stock/qc/disposition/{qc_id}` (line 6811·6820)
- 상태 전이: PASS / PARTIAL / HOLD / FAIL → disposition: RETURN / SPECIAL_ACCEPT / SCRAP

### 정상 항목
- ✅ status 화이트리스트 (4종)
- ✅ disposition action 화이트리스트 (3종)
- ✅ try/except 입력 검증
- ✅ 가드 `_s2_guard`
- ✅ FAIL/PARTIAL → disposition 모달 자동 redirect

### 결함 1 → 핫패치 (line 6874)
```python
# 수정 전: 음수 검증 없음
pass_qty = float(form.get("pass_qty") or 0)
fail_qty = float(form.get("fail_qty") or 0)

# 수정 후: 음수 차단
if pass_qty < 0 or fail_qty < 0:
    return RedirectResponse(f"/stock/qc/{po_item_id}?error=qty_negative", 303)
```

### 결함 2 → 핫패치 (line 6877)
```python
# 수정 후: FAIL/PARTIAL 시 사유 필수 (감사 추적)
if status in ("FAIL", "PARTIAL") and not fail_reason:
    return RedirectResponse(f"/stock/qc/{po_item_id}?error=fail_reason_required", 303)
```

### 결함 3 (이연 — 데이터 모델 변경)
- line 6880: `INSERT ... (po_item_id, receipt_id, ...) VALUES (?,?, ...) (po_item_id, po_item_id, ...)` — 두 컬럼에 같은 값 들어감
- 코멘트 line 6874: "po_item_id 가 receipts.id 로 들어올 수 있으므로 둘다 시도 (UI 단순화)"
- → **데이터 의미 모호**. 실제 receipts.id 와 po_item_id 가 다를 경우 추적 불가능
- 다음 사이클 발주 권고: UI 에서 명시적으로 `?source=receipt` 또는 `?source=po_item` 분기

### 04 영역 (페르소나 검증)
- 박재석(품질팀) 입장: 100개 검수 → 80 PASS / 15 FAIL / 5 HOLD 입력 동선이 1분 내 가능한가?
- FAIL 시 disposition 모달 → RETURN/SPECIAL_ACCEPT/SCRAP 결정 흐름이 KNK 실무와 맞는가?

---

## 5. 핫패치 결과 종합

| V코드 | 라우트 | 결함 발견 | 즉시 패치 | 이연 (다음 사이클) |
|---|---|---:|---:|---:|
| V3 | rates/cost-sim | 1 | 1 (입력 검증 4중) | 0 |
| V4 | stock/reorder | 1 | 1 (limit 상한) | 0 |
| V5 | stock/qc | 3 | 2 (음수+사유) | 1 (데이터 모델) |
| **합계** | — | **5** | **4** | **1** |

→ **즉시 핫패치 4건 / 데이터 모델 변경 1건 다음 사이클**

---

## 6. §§ 자체 grep 검증

```
$ grep -nE "OPS-V[345]" app/main.py
6874:    # OPS-V5 [정적점검]: 음수 차단 + FAIL/PARTIAL 사유 필수 (감사 추적)
9402:    # OPS-V3 [정적점검 결함]: 입력 검증 누락 → ValueError/0음수 차단
9526:    # OPS-V4 [정적점검]: limit 상한 가드 (메모리 폭주 방지)
```
→ **3/3 마커 PASS**

```
$ python -c "import ast; ast.parse(open('app/main.py', encoding='utf-8').read()); print('SYNTAX OK')"
SYNTAX OK
```

---

## 7. 변경 라인 (정직성 v3)

| 파일 | 신규 | 조정 | net |
|---|---:|---:|---:|
| app/main.py (V3·V4·V5) | 16 | 4 | +16 |
| KNK_시작.bat / START.bat | 0 | 4 | +0 |
| **합계** | **16** | **8** | **+16행** |

---

## 8. 04 운영테스트팀 다음 액션

### 회귀 검증 매트릭스 추가 항목

| # | 시나리오 | 페르소나 |
|---|---|---|
| R-V3-1 | /rates/cost-sim — 음수/0/문자열 입력 시 빨간 배너 표시 | P2 임택훈 |
| R-V3-2 | margin -101 입력 → 차단 / -50 입력 → 허용 (할인 시뮬) | P2 |
| R-V4-1 | /stock/reorder-recommendations?limit=99999 → 1000 으로 클램프 | P3 윤영조 |
| R-V5-1 | qc 입력 시 pass_qty=-1 → qty_negative 에러 | P4 박재석 |
| R-V5-2 | status=FAIL 인데 fail_reason 빈값 → fail_reason_required 에러 | P4 |
| R-V5-3 | status=FAIL + reason 입력 → /stock/qc/disposition/{qc_id} 리다이렉트 | P4 |

→ 페르소나 시뮬레이션 13개 + 본 R-V 6개 = **총 19개 시나리오 검증 매트릭스**.

---

## 9. BAT 갱신 grep

```
$ grep -n "V3·V4·V5" KNK_시작.bat START.bat
KNK_시작.bat:3·7 / START.bat:3·7 = 4/4 PASS
```

---

*01 메인 세션 — 2026-04-29 / V3·V4·V5 정적 점검 30분 완료 / 결함 4건 핫패치 / 1건 다음사이클 이연*
