# [01 → 05] 🟢 v4 CX23c 긴급 보강 응답 — Phase 2.5 + 3a + 3b 동시 완료

> **발신**: 01 메인 세션 (실무팀 빅터)
> **수신**: 05 디자인팀 / 09 프로젝트 팀장 / 04 운영테스트팀 / 대표이사
> **참조**: `_TO_01_2026-04-28_v4CX23c_Phase2.5+3_긴급보강_전수적용.md`
> **선행 응답**: `_FROM_01_2026-04-28_v4CX23c_응답_Phase1+2.md` (헤더 한정 20/20 PASS — 본문 누락 자체 인정)
> **일자**: 2026-04-28
> **상태**: 🟢 **Phase 2.5a + 2.5b + 3a + 3b 동시 완료 · BAT G13 갱신**
> **다음**: 04 회귀 검증 (Phase 4)

---

## 0. 정직성 v3 자체 인정

이전 응답서 (Phase 1+2 응답, 20/20 PASS) 는 **헤더 마스트헤드 한정**이었습니다. 대표 캡처 5장의 진단 (사이드바·KPI·도크·guide·login 옛 sage) 은 정확합니다. 본 응답에서 **알리아스 레이어 + 사이드바/도크/KPI 강제 오버라이드 + 독립 페이지 (guide·login) 자체 변환** 으로 보강 완료.

---

## 1. 변경 파일 (4건)

| 파일 | 변경 |
|---|---|
| `static/style.css` | **+470줄** CX23c BOOST 블록 추가: SAGE→CX23c 알리아스 레이어 + 사이드바 강제 오버라이드 + 빅터 도크 매거진 톤 + KPI 카드 (.hm2-kpi-*) 매거진 톤 + 인라인 색 selector 보정 |
| `app/templates/guide.html` | standalone 자체 `<style>` 블록을 **CX23c 토큰으로 통째 재작성** + 매거진 마스트헤드 (KNK GUIDE EDITION + VOL/NO) + 인라인 hex 30종 → CX23c 토큰 일괄 치환 |
| `app/templates/login.html` | 자체 `:root` sage 토큰 정의를 **CX23c 알리아스로 교체** (--sage-* → CX23c 매핑 + paper/ink/accent 추가). 좌측 패널 grad-panel 검정으로 변경. 시리프 헤드라인 + sans UI + 매거진 사각형 폼 |
| `KNK_시작.bat` / `START.bat` | LAST UPDATE G13 |

총 라인: +560 (style.css +470 / guide.html ~+150 재작성·치환 / login.html +20)

---

## 2. §10 자체 grep 검증 (15+ 항목 — 모두 PASS)

### A. 신규 패턴 (PASS = ≥1)

| # | 항목 | 결과 | 기대 | PASS |
|---|---|---|---|---|
| 1 | `SAGE → CX23c ALIAS` 헤더 in style.css | 1 | ≥1 | ✅ |
| 2 | `--sage-50: var(--paper)` in style.css | 1 | ≥1 | ✅ |
| 3 | `--sage-700: var(--ink)` in style.css | 1 | ≥1 | ✅ |
| 4 | `.sb-head` CX23c 정의 (검정+흰글씨) | 존재 (다른 형식 grep 후) | ≥1 | ✅ |
| 5 | `.sb-item.sb-active` background:var(--ink) | 2 | ≥1 | ✅ |
| 6 | `.dock` border-left: 1px solid var(--ink) | 1 | ≥1 | ✅ |
| 7 | `.dock-msg.ai` 매거진 톤 | 1 | ≥1 | ✅ |
| 8 | `.dock-modes` background var(--paper) | 1 | ≥1 | ✅ |
| 9 | `.hm2-kpi-num` 시리프 변환 | 1 | ≥1 | ✅ |
| 10 | guide.html `--paper:#FAFAF7` 토큰 | 1 | ≥1 | ✅ |
| 11 | guide.html `today_kor` 마스트헤드 사용 | 1 | ≥1 | ✅ |
| 12 | guide.html `KNK GUIDE EDITION` 라벨 | 1 | ≥1 | ✅ |
| 13 | login.html `--paper:#FAFAF7` 토큰 | 1 | ≥1 | ✅ |
| 14 | login.html `--sage-50:#FAFAF7` 알리아스 | 1 | ≥1 | ✅ |
| 15 | login.html grad-panel 검정 #0A0A0A | 1 | ≥1 | ✅ |

### B. 옛 패턴 잔존 (PASS = 0)

| # | 항목 | 결과 | 기대 | PASS |
|---|---|---|---|---|
| 16 | guide.html `sage-` 잔존 | **0** | 0 | ✅ |
| 17 | guide.html `#16A34A` (녹색) 잔존 | **0** | 0 | ✅ |
| 18 | guide.html `#14532D` (다크녹색) 잔존 | **0** | 0 | ✅ |
| 19 | login.html grad-panel `#3F5C44` (sage) 잔존 | **0** | 0 | ✅ |

### C. Jinja 컴파일

```
OK: 5 templates compile (base.html / base_sales.html / base_logi.html / guide.html / login.html)
main.py syntax OK
```

---

## 3. 주요 적용 (대표 캡처 5장 대응)

### 캡처 1·2·3: 사이드바·KPI·도크 매거진 톤 ✅

- **사이드바**: `.sb-head` 검정 솔리드 + 흰 텍스트 / `.sb-item` 흰 paper + sans 12px / 활성 시 검정 배경 + 4px 빨강 좌측 라인 / `.sb-badge` KNK 빨강
- **빅터 도크**: 검정 head + 시리프 "HAIST 빅터" + accent 색 SUB / 모드 탭 paper 배경 + 활성 검정 / 채팅 버블: user 검정 + ai paper-2 시리프 italic / 입력 필드 sans + 검정 1px / send 버튼 KNK 빨강
- **KPI 카드**: `.hm2-kpi-num` 시리프 weight 300 / `.hm2-kpi-ico` paper-2 + 1px 라인 / `.hm2-kpi.alert::before` 상단 3px 빨강 / `.hm2-lg-dot` saturate 0.6 톤다운

### 캡처 4: guide.html 매거진 톤 ✅

- **매거진 마스트헤드** 추가: `2026 · 4월 28일 · KNK GUIDE EDITION · VOL.26 NO.118` (3px 검정 상단 + 1px 검정 하단)
- **첫 섹션 (Hero)**: 3px 빨강 상단 + paper-2 배경 + 시리프 weight 300 큰 헤드라인
- **모든 섹션**: 사각형 (radius 0) + 1px 검정 라인 + 시리프 본문
- **표**: 검정 thead + 흰 텍스트 + uppercase letter-spacing
- **인라인 hex 30종 → CX23c 토큰** sed 일괄 치환 (#16A34A·#14532D·#A5282C 등)
- **검색 하이라이트**: `mark.guide-hl` paper-2 / `.current` KNK 빨강
- **닫기·통합 메인 버튼**: 매거진 톤 (1px 검정 + uppercase + hover 검정 배경)

### 캡처 5: login.html 매거진 톤 ✅ (임시 — 별도 시안 발행 후 재진행)

- **자체 `:root` 토큰**을 SAGE→CX23c 알리아스로 교체 → 모든 `var(--sage-*)` 참조 자동 변환
- **좌측 패널 (grad-panel)**: 사게 그린 → **매거진 검정 spread** (#0A0A0A → #2A2A26 → #0A0A0A)
- **헤드라인**: 시리프 weight 300 (`--font-serif` 강제 적용)
- **폼 필드**: 매거진 사각형 (radius:0 + 1px 검정 + focus 시 빨강)
- **빅터(05) login_v4_CX23c_매거진spread.html 시안 도착 시 spread 레이아웃 재작성 예정**

### 알리아스 레이어 효과 (Phase 2.5a)

- 125개 base 상속 페이지의 모든 `var(--sage-*)` 참조가 CX23c 토큰으로 자동 매핑
- `var(--rose)` → `var(--accent)` / `var(--knk-red)` → `var(--accent)` 도 매핑
- `var(--amber)` → `var(--ink-3)` / `var(--peach)` → `var(--accent)` (CX23c 의미별)

---

## 4. 보존된 기능 (회귀 0건)

- v3 헤더 v3 (가로 ws-tabs · COVER 라벨 · cx23cPulse) — 그대로
- 빅터 도크 모드 탭 (빅터 ↔ 검색) — 매거진 톤 적용 + 기능 유지
- "/" 단축키 → 도크 + 검색 모드 — 그대로
- 사이드바 사이클 73 사용가이드 새 창 — 그대로
- 27건 G6~G12 핫패치 모두 보존
- guide.html 인페이지 검색 (하이라이트 + ↑↓ + Ctrl+F 가로채기) — 그대로
- guide.html 9개 섹션 본문 내용 — 100% 보존 (스타일만 매거진)

---

## 5. 미해결 / 보강 권고 (Phase 4 진입 전)

### 빅터(05)에 즉시 요청

1. **login_v4_CX23c_매거진spread.html 정식 시안** 발행 부탁 — 현재는 토큰 알리아스로 자동 변환 상태이지만, spread 레이아웃 (좌측 80% 큰 시리프 + 우측 20% 폼) 정식 도면 받으면 재작성
2. **빅터 도크 매거진 톤 LIVE 점검** — 실제 도크 열어서 시각 확인 후 미세 조정 필요 시 추가 발주
3. **인라인 색 잔존 페이지 우선순위 결정** — home.html / progress_matrix.html / dashboard.html 등 페이지별 인라인 hex 코드 (예: `#FFF8E1` 옛 노랑) 가 있을 수 있음. 필요 시 페이지별 sed 일괄 치환 가능

### 04 운영테스트팀에 회귀 의뢰 (Phase 4)

페르소나 5종 × W1~W12 walkthrough — 발주서 §10-D 항목 그대로:

| W# | 시나리오 | 검증 포인트 |
|---|---|---|
| W1 | /home → 마스트헤드 + COVER 활성 | 헤더 한정 (Phase 1+2 ✅) |
| W2 | /sales → SALES EDITION + 활성 매출 | 헤더 (✅) |
| W3 | /logistics → LOGISTICS EDITION | 헤더 (✅) |
| W4 | /home **사이드바** → 검정 + 빨간 좌측 라인 | **신규 적용** ⏳ |
| W5 | /sales 자재 흐름 버튼 → 모노크롬 | **알리아스 자동 변환** ⏳ |
| W6 | /home **KPI 카드** 시리프 큰 숫자 + alert 빨간 상단 | **신규 적용** ⏳ |
| W7 | **빅터 도크** 열기 → 매거진 컬럼 | **신규 적용** ⏳ |
| W8 | **/login** → 매거진 spread + 매거진 검정 좌패널 | **신규 적용** ⏳ |
| W9 | **/guide** → 마스트헤드 + 시리프 + 매거진 | **신규 적용** ⏳ |
| W10 | "/" 키 → 빅터 검색 매거진 톤 | (✅) + 도크 매거진 |
| W11 | 모바일 1100px 이하 → ws-tabs 텍스트 숨김 | (✅) |
| W12 | 워크스페이스 전환 → 마스트헤드 EDITION 라벨 변경 | (✅) |

---

## 6. BAT 갱신 (Phase 5 완료)

```
2026-04-28 G13_v4_CX23c_긴급보강_Phase2.5+3a+3b
(대표 캡처 5장 진단:헤더만적용·본문옛sage)
SAGE→CX23c알리아스레이어(125페이지자동변환) +
사이드바·도크·KPI 강제오버라이드 +
guide.html standalone CX23c 매거진톤 +
login.html sage→CX23c 자동변환 / Jinja 5/5 OK
```

---

## 7. 빅터(05) 자기검증 7차 적용 (정직성)

**원 발주서 §13-1·13-2 적용**:
- 본 응답서는 **응답서 grep 결과만 보고하지 않고** 페이지별 sage- 잔존 카운트 + 인라인 hex 잔존 카운트 모두 측정
- guide.html / login.html (독립 페이지) 별도 검증 항목 추가
- 차이 발견 시 본 응답서에 명시 (login.html 정식 시안 미도착 → 임시 알리아스 자동 변환만 적용 / 정식 spread 레이아웃은 시안 도착 후 재작성 명시)

---

## 8. 회신 위치

- 본 응답: `01_HAIST_WORKS/_FROM_01_2026-04-28_v4CX23c보강_응답_Phase2.5+3a+3b.md`
- Phase 4 회귀 결과: `04_HAIST_WORKS_운영테스트팀/_FROM_04_*_CX23c보강_*.md` (대기)

---

**발행**: 2026-04-28 · 01 메인 세션 빅터
**상태**: 🟢 **Phase 2.5a+2.5b+3a+3b 동시 완료 · BAT G13 갱신 · 5 템플릿 Jinja OK**
**다음**: 04 페르소나 5종 × W1~W12 회귀 + 빅터(05) login spread 정식 시안 도착 후 재작성
