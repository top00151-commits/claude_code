# KNK 메신저 작업기록 — 2026-06-02

> 작업 세션: 2026-06-01 23:48 ~ 2026-06-02 12:52 (연속)
> 작업자: 빅터(01) / 지시: 김정락 대표
> 산출물: 커밋 14건 (SW v70 → v81 + 채널 초대 백엔드), 메신저 프로젝트 정의서, 전략(마찰 점검) 정리
> 배포: 매 변경마다 NAS(`works`/`haist.knknara.co.kr`) 동기화 + 재시작 + /healthz 200 확인 + GitHub push
> 모든 변경 자체검증: app.py `py_compile`, app.js 괄호균형 HEAD 일치, chat.html Jinja parse, CSS 중괄호 0

---

## 0. 한눈에 요약

| 분류 | 내용 |
|---|---|
| **전략 정리** | **메신저 프로젝트 정의서 확정·저장** (왜·누구·무엇·성공 + 4원칙) / 채택 마찰 점검 / 알림·설치 계획(보류) |
| **UI 가독성** | 휴대폰 필터 칩 7개 한 줄 균등(v70·v71) / 설정창 언어·탭 한 줄 배치(v72) |
| **버그·기능 수정** | 텍스트복사 선택분만(v73) / 파일 원본명·멘션 3건(v74) / 멘션 전체표시·점프(v75) / 스크롤 튕김(v76) / 멘션 점프 정확·깜빡임·근본수정(v77~v79) / 붙여넣기 담기(v80) / 화면잠금 비번해제 보강(v81) / 채널 초대 권한 일치 |
| **보류(미착수)** | 알림·설치 유도 / 직원 동의화면(첫 로그인+100일) |

---

## 1. 전략·방향 정리 (코드 아닌 의사결정)

### 1-1. 메신저 프로젝트 정의서 확정 — 커밋 `ea73463`
대표와 4문항 인터뷰로 "왜 만드는가"를 확정. **앞으로 모든 메신저 기능 판단의 최상위 기준.**

- **한 문장**: 한국 본사 + 베트남 법인 **전 직원(+고객사)이**, **카톡만큼 쉽게** 쓰면서, 회사의 모든 **업무가 빠짐없이 추적·진행되는 '업무 관리 허브'** — 그리고 회사 일의 **시작점(관문)**.
- **왜 (가치)**: ①본사–베트남 언어장벽 제거 ②업무 지시·요청 누락 0 ③WORKS·결재·메일의 관문 ④외부 메신저(카톡 등) 탈피
- **누구**: 본사+베트남 전 직원(현장 포함) + 고객사(게스트)
- **성격**: 단순 소통 도구가 아니라 **업무 관리 허브**
- **성공 1순위**: 전 직원이 회사 업무를 "카톡이 아니라 여기서" 한다 (= 채택률)
- **핵심 딜레마**: **강력함(허브) ↔ 쉬움(카톡 대신)** — 무거우면 카톡으로 회귀
- **1번 원칙**: **"카톡만큼 쉬운가?"를 통과 못 하는 기능은 아무리 좋아도 보류·숨김**
- **4원칙**: ①쉬움 1순위 ②업무는 추적되게 ③언어 장벽 0 ④고객사는 가볍게
- **저장 위치**: 전문 = `10_KNK_Messenger/_기획/메신저_프로젝트_정의서.md` · 요약 = 메모리(`MEMORY.md`) "절대준수 기준"으로 등록

### 1-2. 채택 마찰 점검 (대표 답변 반영, 일부 보류)
- **대표가 본 최대 마찰** = **"익숙하지 않음"** (특정 화면이 아니라 앱 전체가 카톡처럼 손에 안 익음)
- **착수 우선순위(대표 선택)** = **설치 + 알림 먼저** (알림이 확실히 와야 자꾸 열고 → 익숙해짐)
- **코드 점검 결과**:
  - 안드로이드: 로그인 3초 뒤 알림 권한 자동 요청 (거부 시 약함)
  - **아이폰 미설치: 알림 0** (iOS는 '홈 화면에 추가' PWA에서만 푸시 + 자동요청·설치유도 없음) ← 가장 큰 구멍
  - 설치 유도 배너는 2026-05-19 대표가 직접 삭제(사유: "동작이 이상했다")
- **대표 선호 방향(보류)**: 부드러운 한 줄 배너 + 로그인 직후 1회 안내 + 관리자 대면 병행
- **상태**: 대표가 "이거 다시 고민하자, 지금은 문제점 해결 먼저" → **알림·설치 작업 보류**

---

## 2. UI 가독성 개선

### 2-1. 휴대폰 필터 칩 7개 한 줄 — v70 `e81ee94` → v71 `f1b7dd3`
- 대상: 대화목록 위 칩 줄(전체·프로젝트·1:1·그룹·채널·내 요청·멘션)이 휴대폰에서 한 줄에 안 들어와 잘림
- **v70(1차)**: 여백·간격·글자 축소(여백 9→5, 간격 3→2, 글자 11.5→10.5 등) → 대표: "너무 많이 줄였다, 오른쪽 여백 생김"
- **v71(확정)**: **`flex:1 1 0` 균등 분배로 가로 꽉 참** + **칩 글자 10.5px 고정**(글씨크기 설정 `--fs-add-ui`과 분리) → 글씨 크기를 바꿔도 배치 항상 동일, 작게=빈공간X·크게=넘침X
- 파일: `style.css` `@media(max-width:720px)` 만 (컴퓨터 무변경)

### 2-2. 설정창 언어 버튼·탭 한 줄 배치 — v72 `62df353`
- 대상: 설정창에서 언어 버튼(中文이 2줄로 넘침)·탭(사용법/관리자 글자가 2열로 깨짐)
- ①언어: 라벨 위로 + 버튼 4개 `flex:1` 한 줄 균등
- ②탭(알림·초대·보안·사용법·관리자): `white-space:nowrap` + `word-break:keep-all`로 한글 2열 방지 + `flex:1` 균등 + `ellipsis` 안전망
- 글자 고정(글씨크기와 분리)으로 배치 항상 동일. `id="settingsTabs"`로 타깃 → 타 다이얼로그 영향 없음
- 파일: `style.css` 만

---

## 3. 버그·기능 수정

> 형식: **증상 → 원인 → 해결**

### 3-1. 말풍선 "텍스트 복사" 선택분만 — v73 `2a28f9d`
- 증상: 텍스트 일부 드래그 후 우클릭/롱프레스 "텍스트 복사"를 누르면 **말풍선 전체**가 복사됨 (키보드 Ctrl+C는 선택분만 — 정상)
- 원인: 핸들러가 항상 `.bubble` 전체 textContent 복사 + **메뉴 버튼 클릭 순간 선택이 풀림**
- 해결: 메뉴 열리는 시점(`showMessageContextMenu`)에 선택 텍스트를 미리 캡처(`_ctxSelectedText`), 그 말풍선 안 선택일 때만 인정. 선택 있으면 선택분만(트림 X), 없으면 말풍선 전체
- 파일: `app.js`

### 3-2. 파일 다운로드 원본명 + 멘션 3건 — v74 `7dd37a5`
- **#2 파일명**: 다운로드 시 저장이름(`무작위_안전이름`)으로 받아짐 + 한글 깨짐. 원인=휴대폰 브라우저가 `<a download>` 무시 + `secure_filename`이 한글 제거. → **서버 `serve_upload`에 `?dl=1` 모드 추가**: `as_attachment + download_name=DB의 file_name`(UTF-8 한글 보존). 받기 링크 4곳 + 이미지 저장에 `?dl=1`. **같은 파일명은 브라우저가 자동 `(1)` 처리**
- **#3 멘션 방향키 스크롤**: 방향키 이동 시 선택 항목이 보이는 영역 밖으로 안 따라감 → `scrollIntoView({block:"nearest"})`
- **#4 멘션 이메일 오인식**: `top0015@knknara.co.kr` 같은 이메일도 멘션 발동 → @가 **'맨 앞이거나 공백 뒤'일 때만** 발동(정규식 `/(^|\s)@.../`). 입력 자동완성 + pickCurrent + 렌더 3곳
- **#1 사진 화질**: 코드 점검 결과 채팅 사진은 **무압축 원본 저장**(서버 재인코딩 없음) → 우리 코드엔 화질 저하 단계 없음. 추정 수정 안 하고 진단 안내(원본/미리보기 비교, 기기 확인 요청)
- 파일: `app.py` + `app.js`

### 3-3. 멘션 — @전체 표시 + 클릭 점프 — v75 `ea15548`
- **A**: `@`만 입력 시 방 참여 인원 전체가 안 나옴 → 후보 8명 제한(`list.slice(0,8)`) 제거 + **이름순 정렬**(스크롤·방향키로 모두 접근)
- **B**: '내게 온 멘션' 클릭 시 방 맨 아래로 감 → 방 열 때 강제 최하단 보정이 점프를 덮어써서. `_userTouchedMessages=true`로 차단 후 해당 메시지로 `scrollIntoView`
- 파일: `app.js`

### 3-4. 대화창 스크롤 위로 올렸다 바닥으로 튕김 — v76 `14f65d5`
- 증상: 바닥 근처에서 휠 조금 올리면 다시 바닥으로 복귀(여러 바퀴 올리면 유지)
- 원인: `updateViewport()`의 "바닥 150px 이내면 맨 아래로 스냅"이 **입력창 포커스 중 250ms 폴링**으로 계속 실행 + **컴퓨터는 방 열면 입력창 자동 포커스**
- 해결: 자동 스냅 2곳(updateViewport, 컴포저 ResizeObserver)에 **`!_userTouchedMessages` 가드**(휠·터치 중이면 건드리지 않음). 새 메시지 자동하단·키보드 따라가기는 별도 경로라 유지
- 파일: `app.js`

### 3-5. 멘션 점프 — 정확 안착 → 깜빡임 → 근본 수정 (v77→v78→v79)
- **v77 `4d8f2db`**: 점프 후 이미지(PDF·사진) lazy 로드로 위치가 밀림 → ~1.6초간 13회 재보정(정확 안착). *(부작용: 깜빡임)*
- **v78 `32feffd`**: v77이 이미지 뜰 때마다 3~4번 툭툭 깜빡 → 즉시 1회 + 이미지 로드 멈춘 뒤 1번 부드럽게(디바운스)로 축소
- **v79 `3221e22`(근본 수정)**: **진짜 원인 = 컴퓨터에서 방 열 때 입력창 자동 포커스 → `onInputFocus`가 50/150/300/500/800/1200ms 6개 타이머로 맨 아래 강제 + 250ms 폴링** → 멘션 점프를 1.2초간 계속 바닥으로 덮어씀(그동안 점프 쪽만 땜질해 못 잡음).
  - 해결: **점프 모드(`_jumpTargetMsgId`)** 도입 → 멘션 점프 시 openRoom이 ①입력창 자동포커스 안 함(타이머 6개 원천 차단) ②`_forceMessagesToBottom` 하단강제 skip, 스냅 2곳도 가드. `_gotoMention`은 1회 깔끔히 이동 + 이미지 reflow 시 1회 보정, 2.5초 후 플래그 해제
- 파일: `app.js`

### 3-6. 첨부 붙여넣기(Ctrl+V) 즉시 전송 → 담기 — v80 `abd03c1`
- 증상: 붙여넣은 파일이 클립 버튼과 달리 **바로 전송**됨
- 해결: paste 핸들러를 `uploadFiles`(즉시) → **`_stageFiles`(보낼 첨부에 담기, 기존 것에 추가)**. 파일일 때만 `preventDefault`(텍스트 붙여넣기는 그대로). → 보내기 버튼/엔터로 전송
- 파일: `app.js` · (드롭은 기존 유지 — 필요 시 추후 동일화)

### 3-7. 화면 잠금 비번 해제 보강 — v81 `77ed0f6`
- 증상 제보: 지문·얼굴 미등록 시 비밀번호로도 잠금 해제가 안 됨
- 추적 결과: 비번 해제(`_lkTryPassword`→`/api/verify_password`)는 **지문 등록과 무관**, `verify_password`는 **로그인과 동일한 `check_password_hash`**, CSS 마스킹은 값 보존, 핸들러 연결됨 → **코드상 정상 동작해야 함**. 실패 시 무조건 "비밀번호가 올바르지 않습니다"로 표시돼 진짜 원인이 가려지던 것이 핵심 문제
- 보강: ①잠금화면에 **👁 보기 토글**(가려진 비번 오타 확인) ②해제 오류를 **server/network/wrong/empty 구분 안내** + `r.ok`·`credentials` 견고화 ③잠글 때마다 가림(●)+👁 초기화
- 파일: `chat.html` + `style.css` + `app.js` · (이후 대표: "된 것 같다")

### 3-8. 채널 초대 권한 — 설정과 동작 일치 — `9af56f2` (백엔드)
- 증상: 채널 설정이 "모든 멤버 초대 가능"인데 실제론 방장만 초대 가능한 듯
- 원인: 채널 전용 옛 규칙(2026-05-24, "채널 생성권한자(비ceo)는 본인이 만든 채널에만 초대")이 `invite_policy='all'`에도 적용 → 부장급이 남의 채널엔 버튼은 보이나 서버가 403
- 해결(대표 선택=설정대로 모두 허용): 그 제한 조건에 **`invite_policy != 'all'`** 추가 → "모든 멤버 초대 가능"으로 연 채널은 누구나(부장급 포함) 초대, "방장·PM만"으로 두면 기존대로 제한
- 파일: `app.py` 만 (프론트는 이미 설정대로 버튼 노출 — SW/정적 무변경)

---

## 4. 보류·후속 작업

| 항목 | 상태 | 메모 |
|---|---|---|
| **알림·설치 유도** | 보류 | 부드러운 배너 + 로그인 직후 1회 안내 + 대면 병행 방향. 예전 자동팝업은 "동작 이상"으로 삭제됨. 채택 1순위라 재개 시 최우선 |
| **직원 동의화면** | 보류(중지) | 첫 로그인 + 100일마다 / 보안정책·비밀유지·정보유출금지·위치추적 안함·대화내용 열람불가 동의 → 서버에 효력 기록. 대표가 "중지" |
| **#1 사진 화질** | 진단 대기 | 서버는 원본 보존 확인. 다운받은 원본도 흐린지(=폰이 줄임) / 미리보기만 흐린지 확인 필요 |
| **메신저 직원→WORKS 동기화** | 01 세션 위임 | (이전 세션 핸드오프 유지) |

---

## 5. 커밋 목록 (시각순)

| 시각 | 커밋 | 내용 | SW |
|---|---|---|---|
| 06-01 23:48 | `e81ee94` | 휴대폰 필터 칩 가로폭 축소 | v70 |
| 06-01 23:58 | `f1b7dd3` | 필터 칩 flex:1 균등 + 글자 고정 | v71 |
| 06-02 00:17 | `62df353` | 설정창 언어·탭 한 줄 배치 | v72 |
| 06-02 09:44 | `2a28f9d` | 텍스트 복사 선택분만 | v73 |
| 06-02 10:02 | `ea73463` | **메신저 프로젝트 정의서** | — |
| 06-02 11:03 | `7dd37a5` | 파일 원본명 + 멘션 3건 | v74 |
| 06-02 11:22 | `ea15548` | 멘션 @전체 표시 + 클릭 점프 | v75 |
| 06-02 11:34 | `14f65d5` | 스크롤 바닥 튕김 수정 | v76 |
| 06-02 11:51 | `4d8f2db` | 멘션 점프 정확 안착 | v77 |
| 06-02 11:59 | `32feffd` | 멘션 점프 깜빡임 수정 | v78 |
| 06-02 12:08 | `3221e22` | 멘션 점프 **근본 원인 수정** | v79 |
| 06-02 12:14 | `abd03c1` | 붙여넣기 담기 | v80 |
| 06-02 12:29 | `77ed0f6` | 화면잠금 비번해제 보강 | v81 |
| 06-02 12:52 | `9af56f2` | 채널 초대 권한 일치 | (백엔드) |

---

## 6. 운영 메모 / 사고·교훈

- **`git add -A` 사고(차단·복구)**: v70 커밋 때 `git add -A`로 추적 안 되던 잡파일 90여 개(`API KEY.pptx`·`NAS`·`emoji_ready/*.png` 등)가 통째로 묶임 → **push 전에 커밋 되돌리고 의도한 3개 파일만 재커밋**. 이후 모든 커밋은 **변경 파일만 명시적으로 add**. ⚠️ 참고: `참고자료\API KEY.pptx`에 실제 키가 있다면 안전한 곳으로 이동 + `.gitignore` 권장
- **PowerShell→git 커밋 메시지**: 메시지 안 큰따옴표(`"`)가 인자 전달에서 깨짐 → 큰따옴표 없는 here-string 사용으로 정착
- **검증 루틴**(node 미사용): app.py `py_compile` / app.js 괄호균형 `git show HEAD` 대비 일치(-34,0,-1,0) / chat.html `jinja2.parse` / CSS 중괄호 0
- **배포 루틴**: `deploy\sync_to_synology.ps1` → 재시작 PID 변화 + `/healthz` 200 확인 → `git push`

---

*작성: 빅터(01) · 2026-06-02*
