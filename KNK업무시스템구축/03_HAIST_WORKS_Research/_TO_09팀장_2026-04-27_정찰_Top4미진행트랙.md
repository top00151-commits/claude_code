# 03 Research 추가 정찰 — Top 4 미진행 트랙 (사이클 57·58·59·60 후보)

> **발주**: 09 팀장 (2026-04-27, 사이클 무중단 다중 트랙)
> **모드**: 정찰 only (코드 수정 0)
> **제약**: 외부 자산 0 / v2 본체 미접촉 / 핫패치 보존
> **참조 의무 통과**: `_TEAM_ORIENTATION.md` §8 자기 점검 (5/5)

---

## 핵심 발견 (TL;DR)

**Top 4 미진행 트랙 4건은 사실상 1차 셋업 완료 상태**. Explore 결과 (11:50)는 신설 후보로 분류했으나 grep 직접 인용 결과:

| 트랙 | DB 테이블 | 헬퍼 함수 | 라우트 | 템플릿 | 상태 |
|------|----------|----------|--------|--------|------|
| A. Stock Adjustments | 4종 (sa/sai/sadj/aatt) | 12+ | 7+ | 2 | **완성에 가까움** (사이클 56 가동 중) |
| B. Stock Receipts + Issue | 4종 (receipts/issues_out/qc/disp) | 6+ | 8 | 4 | **완성에 가까움** (Top3-S2-2차 적용) |
| C. Projects 마스터 | projects + project_phases + project_milestones + project_burndown_snapshots + project_forecasts | 12+ | 5 + 3 (간트/번다운/마일스톤) | 2 (projects.html / project_form.html) + 진행 5종 | **완성에 가까움** |
| D. Sales Quotations | quotations + orders + order_items + production_orders + shipments + receipts_payment + invoices + payment_terms + order_status_history | S1 라이프사이클 9 enum 모두 | 4탭 라우트 모두 | sales_quotations.html | **완성에 가까움** (Top3-S1-2차 적용) |

**결론**: 사이클 57·58·59·60 신설 발주 → 이미 있는 트랙의 **2차/3차 보강** 으로 재정의 권고.

---

## A. Stock Adjustments (사이클 57 후보)

### A-1 현황 (grep -n 직접 인용)

**DB 스키마** (`app/database.py`):
- `1114: CREATE TABLE IF NOT EXISTS stock_adjustments (` — id / audit_item_id FK / adjusted_qty / reason / status (PENDING·APPROVED·REJECTED) / approved_by / movement_id FK / note / created_by
- `1128~1129: CREATE INDEX idx_sadj_item / idx_sadj_status`
- `1131: CREATE TABLE IF NOT EXISTS audit_attachments (` — id / adjustment_id FK / file_path / file_name / uploaded_by
- `1082: CREATE TABLE IF NOT EXISTS stock_audits (` — 헤더 (Top10 #10 분기 1회 실사)
- `1095: CREATE TABLE IF NOT EXISTS stock_audit_items (` — 라인 (system_qty 스냅샷 / counted_qty / variance / status)

**헬퍼 함수** (`database.py`):
- `4028: def stock_adjust(data, user_id)` — 단일 폼용 (구버전)
- `4168: def stock_adjustments_list(status, limit)`
- `4187: def stock_adjustment_approve(adj_id, approver_id)` — UPDATE status=APPROVED + INSERT stock_movements{kind=ADJUST}
- `4222: def stock_adjustment_reject(adj_id, approver_id, note)`
- `4302: def stock_audit_kpi()` — 매칭률 + ADJUST 합계 (효과 KPI)

**라우트** (`main.py`):
- `4968: GET /stock/adjust` — 단일 폼 (구버전)
- `4986: POST /stock/adjust`
- `5116: GET /stock/adjustments` — 목록 (status 필터 PENDING·APPROVED·REJECTED·전체)
- `5141: POST /stock/adjustments/{id}/approve`
- `5155: POST /stock/adjustments/{id}/reject`
- `5190: POST /stock/adjustments/{id}/attach` — 첨부 업로드 (PDF/JPG/PNG/XLSX · 10MB 제한 · path traversal 2단 가드)
- `5224: GET /stock/adjustments/{id}/attachments` — 첨부 뷰
- `5238: GET /stock/adjustments/{id}/attachments/{att_id}/download` — 다운로드

**사유 코드**: `reason` 컬럼은 `TEXT NOT NULL` (자유 입력). enum 후보 (DAMAGE/SCRAP/TRANSFER/RETURN/COUNT/OTHER) **미적용**.

**승인 워크플로**: `_audit_approve_guard(u)` — admin/ceo/executive **OR** team_id==10 leader (자재팀장만 승인). issues 도메인의 corrective_actions / preventive_actions 와 동일 패턴 (`1053~1076`).

**첨부 파일 처리 패턴**: `_AUDIT_UPLOAD_ROOT = ./uploads/audits/<adj_id>/` · `_AUDIT_ALLOWED_EXT = {.pdf, .jpg, .jpeg, .png, .xlsx}` · `_AUDIT_MAX_BYTES = 10MB` · `_audit_safe_filename` (path traversal 방지) — **재사용 가능 헬퍼**.

### A-2 1차 셋업 사양 (실제 = 2차 보강 사양)

**갭**:
1. **사유 코드 enum 정규화** (현재 자유텍스트) — DAMAGE/SCRAP/TRANSFER/RETURN/COUNT/OTHER 6개 enum + UI select
2. **stock_adjust 단일 폼 라우트 (`/stock/adjust`)** 와 **stock_audits 워크플로 (`/stock/adjustments`)** 가 병존 — UX 통합 가능
3. **`audit_attachments` 첨부 패턴을 다른 도메인** (PO 첨부 / Issue 첨부 / Invoice 첨부) **로 일반화**할 수 있는 헬퍼 추출

### A-3 페르소나 가치
- **P3 재고담당** (분기 1회 실사 + 임시 조정 발생 시 일주일 1회 추가) — 현재 워크플로로 충분
- **P10 자재팀장** (승인 권한) — 현재 워크플로로 충분
- 1차는 페르소나 가치 거의 포화 → **사이클 56(stock_audits 가동 중) 마무리 후 사이클 57은 보강만**

---

## B. Stock Receipts + Issue (사이클 58 후보)

### B-1 현황 (grep -n 직접 인용)

**DB 스키마**:
- `306: CREATE TABLE IF NOT EXISTS stock_movements (` — 단일 진실원 (IN/OUT/ADJUST/TRANSFER)
- `316: remaining_qty / lot_no / expiry_date` — FIFO 추가 컬럼 (사이클 ~04-21 적용)
- `684: CREATE TABLE IF NOT EXISTS receipts (` — 입고 헤더 (po_id / total_qty / qc_inspection_id / status PENDING/PASS/PARTIAL/FAIL)
- `698: CREATE TABLE IF NOT EXISTS qc_inspections (` — 검수 (po_item_id / pass_qty / fail_qty / status PENDING/PASS/PARTIAL/HOLD/FAIL)
- `724: CREATE TABLE IF NOT EXISTS issues_out (` — 출고 헤더 (part_id / qty / purpose / status PENDING/APPROVED/ISSUED/REJECTED)

**라우트** (`main.py`):
- `4899: GET /stock/issue` (구식 단일 폼) + `4926: POST /stock/issue`
- `5270: GET /stock/movements` — 수불부 (단일 진실원 보기)
- `6071: GET /stock/receipts` + `6086: POST` — Top3-S2-2차 (PO 좌패널 + receipts INSERT + stock_movements{kind=IN, qty +} 동시 INSERT)
- `6124: GET /stock/qc/disposition/{qc_id}` + `6133: POST /stock/qc/disposition` — RETURN/SPECIAL_ACCEPT/SCRAP 분기
- `6160: GET /stock/qc/{po_item_id}` + `6169: POST` — 검수 등록 (FAIL/PARTIAL → 부적합 모달 redirect)
- `6207: GET /stock/issues` + `6222: POST` — 출고 요청
- `6249: POST /stock/issues/{id}/approve` — issues_out.status=ISSUED + stock_movements{kind=OUT, qty -}

**입고 시 QC 연동**: receipts.qc_inspection_id FK · 입고 직후 status=PENDING_QC → /stock/qc/{id} 검수 → status=PASS/FAIL 동기화. **이미 완전 연결**.

**출고 시 PO 매칭**: stock_movements.po_id / po_item_id 컬럼 보유 (`320~321`) · 입고 시 자동 기록. 출고는 issues_out 단독 (PO 매칭 없음 — 출고는 생산투입/판매출하 등 다양해 의도적 분리).

### B-2 1차 셋업 사양 (실제 = 2차 보강)

**갭**:
1. **GR/GI 양식 UX 다양화** — 현재 part_id 직접 입력 (P3·P4 부담). part_no 검색 → autocomplete 권고
2. **출고 시 프로젝트(`project_id`) / 고객(`customer_id`) 자동 link** — 컬럼은 stock_movements 에 있으나 폼 미반영
3. **부분입고(PARTIAL_RECEIPT)** — 시안에서 receipts_payment 1:N 패턴 채택했으나 receipts 자체는 단일행 — 분할입고는 별도 receipts 행 N개로 처리해야 함 (UX 가이드 부재)

### B-3 페르소나 가치
- **P3 재고담당** (매일 입출고) / **P4 구매팀** (입고 검수) — 현재 워크플로로 일상 운영 충분
- **P-3차 가치 갭**: 출고 시 자동 프로젝트 연결 (현재 stock_movements.project_id 컬럼 활용 안 함) → P10 PM 입장에서 "내 프로젝트에 자재 얼마 투입됐나" 조회 불가

---

## C. Projects 마스터 CRUD (사이클 59 후보)

### C-1 현황 (grep -n 직접 인용)

**DB 스키마** (`database.py`):
- `68: CREATE TABLE IF NOT EXISTS projects (` — id / code / name / customer_id FK / type / status / pm_id / start_date / end_date / **mgmt_code UNIQUE** (8자리 KNK PMS) / equip_type / year_month / model_name / server_path / lead_user_id
- `526: CREATE TABLE IF NOT EXISTS project_phases (` — 12공정 진척
- `995: CREATE TABLE IF NOT EXISTS project_milestones (` — name / target_date / completed_at / status (PLANNED/IN_PROGRESS/DONE/DELAYED/CANCELLED)
- `1008: CREATE TABLE IF NOT EXISTS project_burndown_snapshots (` — 일별 스냅샷 (UNIQUE project_id+snap_date)
- `1022: CREATE TABLE IF NOT EXISTS project_forecasts (` — 회귀 기반 완료 예측 (slope/intercept/r_squared/forecast_date · 외부 라이브러리 0)

**12공정 enum** (`database.py:6258~6271`):
```
PHASE_DEFS = [
  ("order","수주",1), ("concept","컨셉",2), ("design","기구설계",3),
  ("elec","전장설계",4), ("sw","소프트웨어",5), ("machining","가공",6),
  ("buying","구매",7), ("assembly","조립",8), ("qc","검수",9),
  ("ship","출하",10), ("setup","Set-Up",11), ("knkvn","KNKVN 이관",12),
]
```

**12공정 자동 생성** (`database.py:6292`): `ensure_phases_for_project(project_id)` — 신규 프로젝트 시 12공정 자동 시드 (없는 경우만 INSERT) · 사업부(T/M)별 담당부서 자동 매핑 (PHASE_TEAM_RULES). **이미 구현 완료**.

**라우트** (`main.py`):
- `4287: GET /projects` (목록 + 4필터 q/biz_div/stage/status)
- `4302: GET /projects/new` + `4316: POST` (관리코드 자동 채번 STAGES "수주확정" 이상 + biz_div T/M)
- `4342: GET /projects/{pid}/edit` + `4359: POST`
- `4385: POST /projects/{pid}/delete`
- `2335: GET /project/{pid}` (상세)
- `1330: POST /api/project/{pid}/retro` (회고)
- `7657: GET /progress/{pid}/gantt` — 간트 차트
- `7740: GET /progress/{pid}/burndown` — 번다운 차트
- `7823: GET /progress/{pid}/milestones` — 마일스톤 목록

**STAGES enum** (`database.py:2931`): `["제안작성","제안제출","수주확정","납품","개조","A/S"]` (영업단계 6종)

**연동**: 마일스톤 / 간트 / 번다운 모두 `project_id` FK로 projects 마스터 참조. project_phases 도 동일.

### C-2 1차 셋업 사양 (실제 = 2차 보강)

**갭**:
1. **프로젝트 상세 페이지 (`/project/{pid}`) 깊이** — 현재 어떤 수준인지 미확인 (READ로 추가 검증 필요)
2. **마일스톤 자동 생성 로직 0건** — 신규 프로젝트 시 12공정은 자동 시드, 마일스톤은 수동 입력만. STAGES 6종 → 마일스톤 자동 시드 가능 (현재 미구현)
3. **`projects.customer_id`** (FK) 와 `projects.customer_name` (자유 텍스트) **이중화** — `_logi.projects_create_logi`는 customer_name 사용. customers 마스터 매칭 미연결

### C-3 페르소나 가치
- **P10 PM** (주 2~3회 진척 갱신) — 현재 워크플로로 충분 (간트/번다운/마일스톤 모두 가동)
- **CEO** (전사 매트릭스) — `progress_matrix` 헬퍼 + 화면 가동
- **3차 가치 갭**: STAGES → 마일스톤 자동 시드 (영업단계 변경 시 자동 마일스톤 생성)

---

## D. Sales Quotations (사이클 60 후보)

### D-1 현황 (grep -n 직접 인용)

**DB 스키마** (`database.py:768~`):
- `768: CREATE TABLE IF NOT EXISTS quotations (` — quote_no UNIQUE (QT-YYYYMM-####) / customer_id FK / total_amount / valid_until / version / status (DRAFT/QUOTED/CONFIRMED/CANCELLED)
- `784: CREATE TABLE IF NOT EXISTS orders (` — order_no UNIQUE (SO-YYYYMM-####) / quote_id FK / **status 9 enum** (DRAFT/QUOTED/CONFIRMED/IN_PRODUCTION/READY_TO_SHIP/SHIPPED/INVOICED/PAID/CANCELLED)
- `803: CREATE TABLE IF NOT EXISTS order_items (` — order_id FK / part_id / qty / unit_price / amount / allocated_qty
- `816: CREATE TABLE IF NOT EXISTS production_orders (` — WO 헤더 (planned_start/end · status PLANNED/IN_PRODUCTION/READY_TO_SHIP/DONE/CANCELLED)
- `831: CREATE TABLE IF NOT EXISTS shipments (` — DO (1:N 부분출하)
- `842: CREATE TABLE IF NOT EXISTS receipts_payment (` — RC 수금 (1:N 부분수금)
- `854: CREATE TABLE IF NOT EXISTS payment_terms (` — NET30 등
- `863: CREATE TABLE IF NOT EXISTS invoices (` — 세금계산서

**S1 매출 라이프사이클 9 enum** (`database.py:794~795`): orders.status CHECK 제약 — DRAFT/QUOTED/CONFIRMED/IN_PRODUCTION/READY_TO_SHIP/SHIPPED/INVOICED/PAID/CANCELLED ✅ **9종 모두 적용됨**

**라우트** (`main.py:6303~`):
- `6303: GET /sales/quotations` (탭1 QT) + `6324: POST` (DRAFT INSERT)
- `6354: GET /sales/orders` (탭2 SO) + `6375: POST /sales/orders` (수주 전환 — quotations.status=CONFIRMED + orders INSERT + history 기록)
- `6424: GET /sales/production` (탭3 WO) + `6444: POST /sales/production/start` (orders.status=IN_PRODUCTION + production_orders INSERT)
- `6303~6500+`: 4탭 시안 모두 라우트 가동

**견적 → 수주 전환** (`main.py:6376~6421`): 단일 트랜잭션 (UPDATE quotations.status=CONFIRMED + INSERT orders + INSERT order_status_history) — 시안 §3 정합 1버튼 동선 ✅

**견적서 인쇄·PDF**: **미구현**. (export_ci_print.html / export_pl_print.html / export_bl_print.html 패턴 존재 — `templates/` 에서 확인). 견적서/수주서/세금계산서 인쇄 view는 동일 HTML 인쇄 view 패턴으로 추가 가능 (외부 PDF 라이브러리 0).

### D-2 1차 셋업 사양 (실제 = 2차 보강)

**갭**:
1. **`quotation_items` 테이블 부재** — 현재 quotations.total_amount 단일 금액. 견적 라인 (품목별 수량/단가) 미지원 → 견적서 인쇄 시 라인 표시 불가
2. **견적서 인쇄 view (HTML print)** — 외부 자산 0 정책 안에서 가능. 패턴: `export_ci_print.html` 참조
3. **견적 PDF 다운로드** — 외부 라이브러리 금지 → HTML print view 만 권고 (브라우저 인쇄 → PDF로 저장 워크플로)
4. **수주 시 order_items 자동 복제** — 현재 견적 → 수주 전환 시 order_items INSERT 안 함 (quotations 에 라인이 없으니 당연)

### D-3 페르소나 가치
- **P1 영업** (매일 견적 발행) — quotations.total_amount 단일 금액으로는 견적서로 충분히 못 씀 → 라인 보강 가치 大
- **CEO** (분기 KPI) — 현 시스템 이미 매출 KPI 카드 가동
- **3차 가치 갭**: `quotation_items` + 견적서 인쇄 view = 사이클 60 핵심 가치

---

## E. 09 팀장 결정 권고

### E-1 사이클 가동 순서 권고

**전제**: 4 트랙 모두 1차 셋업 사실상 완료 → 신규 발주가 아닌 **2차/3차 보강** 으로 재정의

| 사이클 | 트랙 | 권고 사양 | 의존 |
|------|------|----------|------|
| 57 | A. Stock Adjustments **2차** | 사유 코드 enum 정규화 + UI select (DAMAGE/SCRAP/TRANSFER/RETURN/COUNT/OTHER) | 사이클 56 (가동 중) 완료 후 |
| 58 | D. Sales Quotations **2차** (가치 大) | `quotation_items` 신설 + 견적서 인쇄 HTML view + 수주 전환 시 order_items 자동 복제 | 독립 |
| 59 | C. Projects 마스터 **2차** | STAGES → 마일스톤 자동 시드 + 프로젝트 상세 페이지 보강 | 독립 |
| 60 | B. Stock Receipts/Issue **3차** | 출고 시 project_id / customer_id 폼 + part_no autocomplete | 독립 |

**병렬 가능**: 4 사이클 모두 서로 다른 도메인 → 5팀(00·01·04·05·09) 동시 가동 가능. **사이클 56 → 57 의존성만 주의**.

### E-2 작은 잡 후보 (사이클 사이 끼워넣기)

1. **`audit_attachments` 헬퍼 일반화** — `_audit_safe_filename` / path traversal 가드 / 10MB 제한을 `attachments_helper.py` (가칭) 모듈로 분리 → PO·Issue·Invoice 등 다른 도메인 첨부에 재사용
2. **stock_adjust 단일 폼 (`/stock/adjust`) deprecation 안내** — stock_audits 워크플로로 일원화 (사이클 56 완료 후 구식 라우트 redirect)
3. **`projects.customer_id` ↔ `projects.customer_name` 정합** — `_logi.projects_create_logi` 가 customer_name 자유텍스트 사용 중 → customers 마스터 매칭 시도 후 미매칭만 자유텍스트 폴백 (DB 마이그레이션 부담 0)

### E-3 페르소나 가치 우선 평가

**가치 大 (즉시 가동)**:
- D 사이클 60 (Sales Quotations 2차) — P1 영업이 매일 사용. 견적 라인 미지원이 가장 아픔
- C 사이클 59 (Projects 2차 — 마일스톤 자동 시드) — P10 PM 동선 단축

**가치 中 (다음 차수)**:
- A 사이클 57 (사유 코드 enum) — P3·P10 입력 정확도 향상
- B 사이클 60 (출고 자동 link) — P10 PM 조회 가치

**가치 小 (작은 잡으로 충분)**:
- 첨부 헬퍼 일반화, 구식 라우트 정리

---

## 산출
- 본 보고서: `03_HAIST_WORKS_Research/_TO_09팀장_2026-04-27_정찰_Top4미진행트랙.md`
- 정직성 v3 §1 (`grep -n` 직접 인용) 통과
- 외부 자산 0 / v2 본체 미접촉 / 핫패치 보존 (코드 0건 수정)

*작성: 03 Research / 2026-04-27 / 09 팀장 회신 대기*
