# 03 정찰 보고 — 3 Hub 분리 + 색 통일 작업 사전 조사

> 발주자: 09 팀장 (빅터)
> 작성: 03 Research, 2026-04-27
> 임무: 코드 0건. 모든 인용은 `grep -n` 기반. 외부 검색 0건.
> 참조 의무: `KNK업무시스템구축/_TEAM_ORIENTATION.md` (read 1~91 완료)

---

## A. 현 base 템플릿 (실측)

`Glob app/templates/base*.html` 결과 — **base 템플릿은 2개뿐**.

| 파일 | wc -l | 용도 (read line 1~50 직접 인용) |
|------|------:|---------------------------------|
| `app/templates/base.html`      | 1115 | **HAIST WORKS 통합/메인 base.** line 6 `{% block title %}{{ app_name|default('HAIST WORKS') }}{% endblock %}` · line 8 `style.css?v=20260426text14tok` · line 13 `<header class="topbar">` (시안 12B 헤더 — brand-logo-wrap + ws-switcher + g-search + tb-icon-btn) · line 122 `<aside class="sidebar" id="sidebar">` |
| `app/templates/base_logi.html` |  255 | **자재·구매 + 매출·영업 통합 base.** line 6 `{% block title %}매출·영업 · 자재·구매 — {{ app_name }}{% endblock %}` · line 7 `style.css?v=20260425healing-c2` · line 9 `<body class="logi-body">` · line 12 `<header class="topbar topbar-logi">` (별도 어두운 헤더) · line 17 brand `KNK / 매출·영업 · 자재·구매` · line 24 `<span class="tb-badge">권한 제한</span>` |

`base_admin.html` / `base_sales.html` 등 **존재하지 않음**.

### A-1. extends 분포 (`grep -n "{% extends" app/templates/`)

| extends 대상 | 파일 수 |
|--------------|--------:|
| `base.html`      | **67** |
| `base_logi.html` | **36** |
| **합계**         | 103 (전체 템플릿 중 base extends만) |

#### A-1-a. `base_logi.html` extends 36개 전수 (직접 인용)

```
export_bl_customs.html · export_ci.html · export_home.html
export_order_detail.html · export_order_form.html · export_pl.html
fx_rates.html
logistics_home.html
parts.html · part_detail.html · part_form.html · part_prices.html
po_list.html · po_detail.html · po_form.html · po_receive.html
projects.html · project_form.html
rates.html · rates_alerts.html · rates_dashboard.html
rates_history.html · rates_cost_sim.html
sales_aging.html · sales_dashboard.html · sales_forecast.html
sales_home.html · sales_outstanding.html
stock_adjust.html · stock_adjustment.html · stock_audit.html · stock_audits.html
stock_issue.html · stock_movements.html
suppliers.html · supplier_form.html
```

#### A-1-b. `base.html` extends 67개 중 매출·재고 관련만 별표 — **base 분리 시 영향**

```
sales_orders.html ★ sales_production.html ★ sales_quotations.html ★
sales_shipments_receipts.html ★ quotation_print.html ★
stock_abc.html ★ stock_balances.html ★ stock_fifo.html ★
stock_issues.html ★ stock_qc.html ★ stock_reorder.html ★
stock_receipts.html ★ stock_safety.html ★ stock_turnover.html ★
```
→ **★ 14개 매출/재고 템플릿이 `base.html` (메인 헤더) 사용 중. base_logi.html과 헤더가 시각적으로 충돌**.

나머지 53개 (`home/daily/calendar/notifications/feed/now/search/dashboard/bottlenecks/progress*/tickets*/issues*/changes*/board*/profile/admin*/qms*/team/cockpit/history/reminders/summary/weekly/external_assets_review/customer_detail`)는 메인 업무계.

---

## B. 사이드바 메뉴 (현황 — line N 직접 인용)

### B-1. `base.html` 사이드바 (5 그룹 · line 122~237)

| 그룹 | line | 직접 인용 |
|------|-----:|-----------|
| 그룹 1 내 업무 | 127~146 | line 128 `<div class="sb-head">{{ i.sb_my_work|default('내 업무') }}</div>` · /home /daily /calendar /notifications |
| 그룹 2 전사 흐름 | 149~175 | line 150 `<div class="sb-head">{{ i.sb_company|default('전사 흐름') }}</div>` · /feed /now /search /dashboard(권한) /bottlenecks(권한) |
| 그룹 3 업무 추적 | 178~204 | line 179 `<div class="sb-head">업무 추적</div>` · /progress /tickets /issues /changes /board/company |
| 그룹 4 (도메인) | (없음) | line 206 `{# C안 §3-3 — 도메인 그룹 제거 (매출·영업 / 자재·구매 → 상단 워크스페이스 스위처로 이전) #}` |
| 그룹 5 외부·관리 | 209~232 | line 210 `<div class="sb-head">외부 · 관리 <span ...>하이웍스</span></div>` · 전자결제↗ /메일↗ /team/{id}/permissions(권한) /admin(권한) /admin/external-assets(권한) |
| 트리 | 234~237 | line 235 `<div class="sb-head">{{ i.sb_team_project|default('팀 / 프로젝트') }}</div>` · `<div id="sbTree">` |

### B-2. `base_logi.html` 사이드바 (4 그룹 · line 46~125)

| 그룹 | line | 직접 인용 |
|------|-----:|-----------|
| 매출·영업 | 51~65 | line 52 `<div class="sb-head" style="color:#4338CA;">📈 매출·영업</div>` · /sales /projects /dashboard |
| 자재·구매 | 68~86 | line 69 `<div class="sb-head" style="color:#166534;">📦 자재·구매</div>` · /logistics /parts /suppliers /po |
| 재고·입출고 | 88~102 | line 89 `<div class="sb-head">재고 · 입출고 <span ...>신규</span></div>` · /stock/movements /stock/issue /stock/adjust |
| 동적 변수 | 104~116 | line 105 `<div class="sb-head">동적 변수 <span ...>신규</span></div>` · /rates · /fx/rates(권한) |
| 이동 | 118~124 | line 119 `<div class="sb-head">이동</div>` · /home target=_blank |

### B-3. 권한 가드 패턴

`grep -n "user.role" app/templates/base.html`:
- L163 `{% if user.role in ['ceo','admin','executive'] %}` (대시보드)
- L169 / L219 `{% if user.role in ['leader','executive','ceo','admin'] %}` (병목·팀권한)
- L225 `{% if user.role in ['ceo','admin'] %}` (관리자)

`grep -n "user.role" app/templates/base_logi.html`:
- L110 `{% if user.role in ['admin', 'finance', 'ceo', 'executive'] %}` (외부 환율)

→ **표준 패턴: 인라인 `user.role in [...]` 화이트리스트**. role 값: `ceo / admin / executive / leader / finance / member`.

---

## C. 라우트 분류 (`grep -n` `app/main.py`, 9072줄 기준)

| 라우트 prefix | grep -n 결과 건수 | 대표 line |
|---------------|------------------:|-----------|
| `/sales/*`     | 20 | L4278 `@app.get("/sales", ...)` · L6456 `/sales/quotations` · L6651 `/sales/orders` · L6721 `/sales/production` · L6776 `/sales/shipments-receipts` · L7057 `/sales/dashboard` · L7069 `/sales/forecast` · L7264 `/sales/outstanding` · L7277 `/sales/aging` |
| `/logistics`   |  1 | L4257 `@app.get("/logistics")` (Hub 진입만) |
| `/stock/*`     | 33 | L5053 `/stock/issue` · L5122 `/stock/adjust` · L5189 `/stock/audits` · L5269 `/stock/adjustments` · L5423 `/stock/movements` · L6167 `/stock/balances` · L6195 `/stock/abc` · L6210 `/stock/turnover` · L6224 `/stock/receipts` · L6277 `/stock/qc/*` · L6360 `/stock/issues` · L8855 `/stock/safety` · L8887 `/stock/reorder-recommendations` |
| `/parts/*`     |  9 | L4340 `/parts` · L4354 `/parts/new` · L4390/4403/4429 edit/delete · L4905 prices · L4954 prices/approve · L4966 detail · L8996/9021 prices |
| `/po/*`        |  9 | L4652~L4809 (list/new/detail/edit/delete) · L5002/5017 receive |
| `/rates/*`     |  9 | L4858 `/rates` · L4871 new · L8662 dashboard · L8683 upload · L8750/8766 cost-sim · L8801 price-history · L8814/8825 alerts |
| `/fx/*`        |  2 | L8938 `/fx/rates` GET · L8959 POST |
| `/suppliers/*` |  6 | L4553~L4638 (list/new/edit/delete) |
| `/projects/*`  |  6 | L4441~L4539 (list/new/edit/delete) |
| `/export/*`    | 17 | L7338~L7893 (orders/CI/PL/BL/customs/print/alerts) |
| `/admin/*`     | 26 | L2655 `/admin` · L2798 `/admin/health` · L2901/2914 external-assets · L2973 settings · L5495~L6117 permissions* (그룹·grant·revoke·matrix·report·cleanup) |
| `/dashboard`   |  1 | L1777 `@app.get("/dashboard")` |
| `/`            | **없음** (`grep "/", response_class=HTMLResponse"` 0건) — 최상위 `/`는 별도 처리 (login redirect 등) |
| `/home`        |  2 | L329 `/home` · L330 `/home/{sel_date}` |
| `/profile` `/me` | 4 | L3129/3139 GET · L3145 password · L3166 POST |

### C-1. Hub 라우트 분류표 (3 Hub 분리 시 자연스러운 매핑)

| Hub | URL prefix | 라우트 합계 | 비고 |
|-----|-----------|-----------:|------|
| **Hub-1 통합/업무** (메인) | `/home /daily /calendar /weekly /notifications /feed /now /search /dashboard /cockpit /bottlenecks /history /reminders /summary /team /profile /me /tickets /issues /changes /progress* /board* /qms* /admin*` | 메인 전체 | 약 80+건 (53 base.html 템플릿) |
| **Hub-2 매출·영업** | `/sales* /export* /projects*` | **20+17+6 = 43** | sales_quotations 등 14개 ★ 템플릿이 현재 base.html 사용 |
| **Hub-3 자재·구매** | `/logistics /parts* /po* /suppliers* /rates* /fx* /stock*` | **1+9+9+6+9+2+33 = 69** | base_logi.html 36개 + base.html ★ 14개 = 50개 템플릿 |

---

## D. 상단 헤더 (style.css 5610줄 기준)

### D-1. 헤더 관련 line (직접 인용)

| line | 내용 |
|-----:|------|
| L143 | `.topbar{background:#fff;color:var(--t);padding:0 20px;...height:54px;border-bottom:3px solid var(--r);position:sticky;top:0;z-index:100;...}` ← **v0 원본** |
| L1193~L1295 | **힐링 헤더 · Sage Garden (2026-04-24)** 오버라이드 — L1191 주석 `힐링 헤더 · Sage Garden` · L1194 `background:var(--grad-header) !important` · L1196 `height:72px !important` |
| L1295 | `.sidebar{` 힐링 사이드바 (변경 후) |
| L4287~L4574 | **v2 본체 (절대 미접촉 영역)** — L4289 주석 `C안 (시안 12B 통째 이식) overrides — 2026-04-25 healing-c` · L4294 `:root{ --topbar-h: clamp(84px, 5vw + 52px, 108px); }` · L4303 `.topbar{ height: var(--topbar-h) !important; background: var(--grad-header) !important; ... }` |
| L3107 | `.topbar.topbar-logi { background: linear-gradient(135deg, #0B1020 0%, #16213E 50%, #1E293B 100%) !important; border-bottom: 2px solid #A5282C; }` ← **base_logi 헤더 (어두운 남보라)** |
| L3329 | `.sidebar-logi {` ← logi 사이드바 |

### D-2. 헤더 색 충돌 — 핵심 발견

- 통합/메인 (`base.html` → `.topbar`): **세이지 그라디언트** `--grad-header: linear-gradient(135deg,#3F5C44 0%,#5D7F61 60%,#7FA183 100%)` (style.css L70)
- 자재·구매·매출 (`base_logi.html` → `.topbar.topbar-logi`): **어두운 남보라 그라디언트** `linear-gradient(135deg, #0B1020 0%, #16213E 50%, #1E293B 100%)` (style.css L3108)
- ★ 14개 매출/재고 템플릿은 `base.html` 사용 → 세이지 헤더가 떠서 **자재·구매 컨셉 시각적 비통일**

### D-3. v2 본체 보호 영역 확인

L4287~L4574 = C안 §6~§8 통째 이식 구간. **여기 수정 절대 금지** (CLAUDE 정책).
힐링 헤더 (L1193~L1295) 와 logi 헤더 (L3107~L3163) 는 **v2 본체 외**. 수정 가능.

---

## E. 힐링 색 추출 (style.css L26~L71)

### E-1. 현 힐링 토큰 (L26 `힐링 디자인 토큰 v1 · Sage Garden B-2`)

```
--sage-50:#F7FAF4   --sage-100:#F0F5EB   --sage-200:#E4ECD9
--sage-300:#CAD9B8  --sage-400:#A8BD8F   --sage-500:#7FA183
--sage-600:#5D7F61  --sage-700:#3F5C44   --sage-800:#2B3E2E

--ink:#2B3E2E       (본문 잉크 · 이끼색)
--accent:#A5282C    (KNK RED · CTA 전용)

--grad-header:linear-gradient(135deg,#3F5C44 0%,#5D7F61 60%,#7FA183 100%)
--grad-sage:  linear-gradient(135deg,#7FA183 0%,#5D7F61 100%)
--grad-brand: linear-gradient(135deg,#A5282C 0%,#8B1E22 100%)
```

### E-2. 충돌 가능성

- 힐링 컨셉(세이지 #3F5C44 ~ #7FA183)은 **이미 메인 base.html 헤더에 적용 완료** (L1194 / L4305)
- 충돌 1개: **base_logi.html 헤더만 남보라 (#0B1020 ~ #1E293B)** — 힐링 컨셉 안 들어감 (style.css L3108)
- 충돌 2개: ★ 14개 매출/재고 템플릿이 `base.html` 사용 → 헤더는 세이지지만 사이드바·메뉴는 매출·자재 컨텍스트 → 사용자 인지 부조화

### E-3. 색 통일 시 변경 라인 (예측)

| 파일 | line | 작업 |
|------|-----:|------|
| `static/style.css` | L3107~L3110 | `topbar-logi` 배경을 `--grad-header` 또는 새 Hub-2/Hub-3 전용 그라디언트로 |
| `static/style.css` | (선택) `:root` 추가 | `--grad-sales: ...` `--grad-logi: ...` 토큰 신설 시 |
| `app/templates/base_logi.html` | L24 | `tb-badge` `rgba(165,40,44,.18)` → 힐링 톤 검토 |

---

## F. 분리 작업 영향 범위

### F-1. 옵션별 비교

| 옵션 | 신규 base | 영향 템플릿 (extends 변경) | 새 CSS 클래스 | 메인 헤더 통일 가능? |
|------|-----------|---------------------------:|---------------|----------------------|
| **A안: base 3개로 완전 분리** | base_main.html (신) / base_sales.html (신) / base_logi.html (개명 검토) | **★ 14개 sales/stock + 36개 logi = 50개 변경 + 53개 메인은 base.html 유지 → base_main.html로 이름만 바꾸려면 67개 변경** | `.topbar-sales` 신설 | ◎ 완전 분리 |
| **B안: base 2개 유지 + 매출/재고 ★14개 → base_logi.html로 이전** | (없음) | **★ 14개만 변경** (`{% extends "base.html" %}` → `"base_logi.html"`) | 기존 `.topbar-logi` 재사용 또는 매출 전용 클래스 1개 추가 | ○ Hub-2(매출)와 Hub-3(자재)는 같은 base_logi 그대로 → 색은 분기 가능 (body class) |
| **C안: 분기 로직 (단일 base + body class)** | (없음) | 0개 | `body.hub-sales` `body.hub-logi` 분기 CSS만 추가 | △ 사이드바 메뉴는 그대로 — Hub별 사이드바 분리 불가 |

### F-2. 작업량 요약 (단순 비교)

- **A안**: 67 + 36 + 14 = **117 templates touched** + base 3개 신설
- **B안**: 14 templates touched + style.css L3107 분기 (CSS 1개) + base_logi.html 사이드바에 `{% if 'sales' in current_workspace.key %}` 분기 추가
- **C안**: 0 templates touched + style.css에 body class 분기 + 사이드바 통합 메뉴 (3 Hub 모두 한 사이드바에 표시)

### F-3. 권장 우선순위 (정찰 의견 — 결정은 09 팀장)

- **B안이 비용 대비 효과 최선**으로 추정.
  근거: ★14개 변경 시 매출/재고 페이지가 `base_logi.html` 헤더(현 어두운 남보라)를 받아 **자재·구매 컨텍스트 = Hub-3 = 어두운 남보라** vs **메인 = 힐링 세이지** 시각 분리 즉시 달성. 매출(Hub-2)는 `base_logi.html` 안에서 `current_workspace.key == 'sales'` 분기로 별도 그라디언트 1개만 추가하면 3 Hub 색 분리 완성.
- **A안**은 깔끔하지만 117 템플릿 수정은 핫패치 G1~G5 회귀 위험 큼.
- **C안**은 사이드바 메뉴 분리가 불가능 (현 base.html 5 그룹 vs base_logi.html 4 그룹이 이미 다름).

---

## G. 09 팀장 결정 권고 — 정찰 결과 요약

### G-1. 핵심 발견 (직접 인용)

1. base 템플릿은 **2개뿐** (`base.html` + `base_logi.html`) — `base_admin.html` `base_sales.html` 모두 **존재하지 않음**
2. **★ 14개 매출/재고 템플릿이 base.html 사용 중** — 헤더는 힐링 세이지지만 콘텐츠는 매출·재고 → **시각 비통일 발생 지점**
3. WORKSPACES 스위처는 이미 main.py L93~L119에 구현되어 있음 — **3 Hub 분기 로직 자체는 백엔드에 이미 존재**
4. v2 본체 L4287~L4574는 절대 미접촉 — 헤더 변경은 L1193~L1295 (힐링) 와 L3107~L3163 (logi) 2개 영역에서만
5. 권한 가드는 인라인 `{% if user.role in [...] %}` 패턴 통일 — 새 base 추가 시 동일 패턴 재사용 가능

### G-2. 권고 작업 발주 흐름 (B안 가정 시 grep -n 정확 위치)

1. **★ 14개 템플릿 extends 변경** — 각 파일 L1만 `{% extends "base.html" %}` → `{% extends "base_logi.html" %}`
   대상: `sales_orders.html · sales_production.html · sales_quotations.html · sales_shipments_receipts.html · quotation_print.html · stock_abc.html · stock_balances.html · stock_fifo.html · stock_issues.html · stock_qc.html · stock_reorder.html · stock_receipts.html · stock_safety.html · stock_turnover.html`
2. **`base_logi.html` 사이드바 분기 추가** — L51 (`📈 매출·영업`) 그룹에 ★sales_* 항목 추가, L88 (`재고·입출고`) 그룹에 stock_balances/abc/fifo/qc/turnover/safety/reorder 추가
3. **`static/style.css` L3107 색 분기** — `body.logi-body[data-ws="sales"]` 와 `body.logi-body[data-ws="logi"]` 두 가지 그라디언트 분리, base_logi.html L9 `<body class="logi-body" data-ws="{{ current_workspace.key }}">` 1라인 추가
4. **회귀 검증** — 핫패치 G1~G5 grep 130 매칭 유지 / v2 본체 L4287~L4574 unchanged / `style.css?v=...` cache-buster 갱신
5. **BAT 갱신 의무** — `KNK_시작.bat` + `START.bat` LAST UPDATE (대표 지시 2026-04-25)

### G-3. 결정 필요 사안 (09 → 대표)

- **Hub-2 (매출) 색** = ? (힐링 세이지 변형? 골드? 따뜻한 세이지?)
- **Hub-3 (자재·구매) 색** = ? (현재 어두운 남보라 #0B1020 유지? 아니면 힐링 컨셉 변형?)
- **A안 vs B안 vs C안** — 본 정찰은 B안 권고이나 결정은 대표 결재

---

## 산출 메타

- 본 보고서 라인 수: 약 220줄
- grep -n 직접 인용: 30+ 위치
- read 직접 확인: `_TEAM_ORIENTATION.md` (1~91) · `base.html` (1~80, 120~250) · `base_logi.html` (1~135) · `style.css` (1~120, 1185~1265, 3070~3190, 4280~4320) · `main.py` (85~210) — **모든 인용 line N 표기**
- 코드 변경: **0건** (정찰 임무 한정)
- BAT 변경: **0건** (01 세션이 코드 반영 시 갱신)
- 외부 검색: **0건** (KNK 내부 grep만)

*— 03 Research 정찰 완료. 09 팀장 발주서 작성 시 본 grep -n 위치 활용.*
