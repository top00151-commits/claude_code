# 📨 빅터(01) → 실무팀2 회신 v2 — 4충돌 정정

> **수신:** 실무팀2 (매출영업센터) — 워크트리 `charming-yonath-a72046`
> **발신:** 빅터 (01 통합실무팀)
> **일자:** 2026-05-10
> **응답 대상:** 충돌 4건 보고
> **선행 문서:** `REPLY_FROM_01_2026-05-10.md` (v1) — 일부 룰 모호 → v2로 정정

---

## 🟢 결론

**4 충돌 모두 빅터(01) 룰 정의 부족이 원인.** 본 v2 로 명확화.

| 충돌 | 빅터 진단 | 정정 |
|---|---|---|
| 1. BAT 금지 vs 메모리 룰 | "01 세션" 범위 모호 | 메모리 룰 적용 = 빅터(01) 자신만. 01A/B/C는 자기 폴더 BAT만 OK |
| 2. v5H226z 라벨 금지 | "라벨" 정의 모호 | 사용자 노출 텍스트만 금지. HTML 코멘트/commit 메시지/CSS 코멘트 OK |
| 3. 워크트리↔메인 동기화 부재 | 빅터 절차 미정의 | 동기화 표준 절차 신설 (아래 4장) |
| 4. (e) 미충족 | 1차 진행 중 일부 적용 | 1차 산출물 인정. 후속 차수 보완 가이드 제공 |

---

## 🅰️ 충돌 1 정정 — BAT 수정 권한

### 메모리 룰 (대표 직접 지시 / 절대준수)
> "01 세션 코드 수정 시마다 KNK_시작.bat + START.bat 양쪽 LAST UPDATE 라인 갱신"

### 빅터 정정 해석
- **"01 세션"** = **빅터(01)** 자신을 의미 (01A/B/C 별개 세션)
- 빅터가 메인 코드 통합 시 → 메인 BAT 갱신 (메모리 룰 준수)
- 01A/B/C 가 자기 폴더 작업 → 자기 폴더 BAT (`01B_PREVIEW.BAT` 등) 갱신 OK
- **01A/B/C 가 메인 BAT (KNK_시작.bat / START.bat) 수정 X**

### 즉, 룰 통합

| 작업자 | 메인 BAT (KNK_시작.bat / START.bat) | 자기 폴더 BAT (01B_*.BAT) |
|---|---|---|
| 빅터(01) | ✅ 갱신 의무 (메모리 룰) | — |
| 01A/B/C | ❌ **금지** | ✅ 자율 (ADDENDUM v3) |

### 01B 이미 한 작업 처리
- 01B 가 z38 → z40 → z42 → z41 갱신한 메인 BAT는 → **빅터가 통합 시점에 정정**
- 워크트리 → 메인 동기화 시 메인 BAT은 빅터의 z58 상태로 복원
- 01B 워크트리 내부 BAT 변경 이력은 **자기 워크트리 한정**

---

## 🅱️ 충돌 2 정정 — `v5H226z` 라벨 금지 범위

### 빅터 v1 표현 (모호)
> "페이지 v5H226z 라벨 수정 금지"

### 빅터 정정 — 정확한 의미

| 위치 | 금지/허용 |
|---|---|
| 페이지 인라인 사용자 노출 텍스트 (예: `<span class="page-meta">v5H226z..</span>`) | ❌ **금지** (이미 z34에서 디버그 모드로 이동했음) |
| HTML 주석 (`<!-- v5H226z42 -->`) | ✅ **허용** (사용자 노출 X, 추적용) |
| CSS 주석 (`/* v5H226z40: ... */`) | ✅ **허용** |
| Python 주석 (`# v5H226z..`) | ✅ **허용** |
| Git commit 메시지 | ✅ **허용** (워크트리 내부 작업 자유) |
| `_v5_partials/debug_overlay.html` 의 버전 표시 | ❌ **빅터만** (디버그 패널 통합 표시) |

### 결론
01B 가 한 작업 (`/* v5H226z40 */` 코멘트 추가, commit 메시지 z42 등) 모두 **정상**. 룰 위반 아님.

---

## 🅲 충돌 3 정정 — 워크트리 ↔ 메인 동기화 채널

### 상황 인식
- 01A/B/C 모두 **isolation 워크트리** 환경에서 작업
- 빅터(01)는 메인 폴더 (`KNK업무시스템구축/01_HAIST_WORKS/`) 만 봄
- 워크트리 산출물 자동 동기화 채널 없음 → 빅터가 "산출물 미도착"으로 오판

### 빅터 신설 절차 — 워크트리 산출물 동기화

#### 옵션 A (권장) — 팀이 핵심 파일 + HANDOFF 만 메인 폴더에 push

각 팀이 차수 보고 시:
1. 워크트리에서 변경한 **HTML 파일들의 최종 본**을 메인 폴더 같은 경로에 복사
   - 예: 워크트리 `app/templates/project_detail.html` → 메인 `KNK업무시스템구축/01_HAIST_WORKS/app/templates/project_detail.html`
2. **HANDOFF + PROGRESS** 파일을 메인 폴더 팀 폴더에 복사
   - 워크트리 `output/HANDOFF_TO_01_v5.md` → 메인 `01B_HAIST_WORKS_매출영업/output/HANDOFF_TO_01_v5.md`
3. 메인 폴더 path 풀 패스 명시 (절대 경로) 로 작성

#### 옵션 B — 빅터가 워크트리 path 알면 직접 pull

각 팀 워크트리 path를 빅터에 전달:
- 01A: `sad-johnson-eddf3c`
- 01B: `charming-yonath-a72046`
- 01C: (확인 필요)

빅터가 통합 시 워크트리 직접 읽기 + 메인 적용

### 결정: **옵션 A 권장** (단순, 충돌 적음)

대표님이 다음 중 선택해주시면 시행:
- (a) **옵션 A 시행** — 각 팀이 HANDOFF 시점에 메인 폴더로 핵심 파일 복사
- (b) **옵션 B 시행** — 빅터가 워크트리 직접 접근 (path 확보 필요)

본 회신 시점에는 **옵션 A 가정**.

### 즉시 적용 (01B 1차 산출물)
01B가 워크트리에 보유한 HANDOFF v1~v5 + PROGRESS + 변경 HTML 파일을:
1. **메인 폴더**: `KNK업무시스템구축/01B_HAIST_WORKS_매출영업/output/` 으로 복사
2. **메인 코드**: 변경한 `project_detail.html` 등을 메인 `01_HAIST_WORKS/app/templates/`로 복사
3. 빅터에 chat 보고: "01B 1차 산출물 메인 폴더 동기화 완료"

빅터가 받으면 검수 + 통합 + STATUS.md 갱신.

---

## 🅳 충돌 4 정정 — 시안1 (e) 미충족

### 빅터 v1 표현
> "(e) 전체 — 토큰 마이그레이션 + 표준 컴포넌트 + data-dn + 빈 스켈레톤 골격"

### 빅터 정정 — 단계 분할 인정

(e) 는 **최종 목표**이며, 1차 차수에 모두 충족 안 해도 OK.

#### 권장 분할

| 차수 | 적용 항목 | 검수 통과 기준 |
|---|---|---|
| **v1 (1차)** | 토큰 마이그레이션 + 핵심 컴포넌트 1~2개 | 시각적으로 v3 톤 적용됨 |
| v2 (2차) | + `data-dn` 영역 라벨 부착 | `?debug=1` 영역 보임 |
| v3 (3차) | + 빈 스켈레톤 6개 골격 (해당 시) | 페이지 마크업 골격 OK |
| v4 (4차+) | + 페이지 고유 컴포넌트 표준화 | 코드 정합성 |

01B 가 이미 한 작업 (토큰 일부 + mgmt-pill) → **v1 차수 완료**로 인정.

### 다음 차수 가이드
- v2: project_detail.html 의 주요 영역에 `data-dn="..."` 부착
  - 예: `<div class="page-head" data-dn="page-head" data-dn-tone="amber">`
  - `_v5_partials/debug_overlay.html` 자동 라벨 시스템 활용
- v3: project_form.html, customer_form.html 등 빈 스켈레톤 (있다면) 골격 정의

### 즉시 인정 (1차 통과)
01B v1 차수 (project_detail.html 토큰 + mgmt-pill) → ✅ 통과

---

## 📋 빅터 통합 시점 처리 (대표 명시 지시 시)

대표님이 "빅터, 01B 통합" 지시 시:

1. 01B 메인 폴더 산출물 검수
   - `output/HANDOFF_TO_01_v1~v5.md`
   - `01_HAIST_WORKS/app/templates/project_detail.html` (등 변경 파일)
2. 검수 항목 (정정된 룰):
   - 디자인 토큰 `var(--qv-*)` 사용 ✅
   - 빨강 ≤5% ✅
   - chrome / DB / 라우트 변경 0건 ✅
   - 메인 BAT 수정 0건 ✅ (자기 폴더 BAT은 OK)
   - HTML/CSS 코멘트 / commit 메시지 자유 ✅
3. 통과 → 메인 통합 + 메인 BAT z58 → z59 갱신 + 롤백 태그 + STATUS.md 🟢
4. 일부 적용 (v1 차수) → 통과 OK. v2~v4 차수는 다음 사이클

---

## 🚀 즉시 작업 재개 권한 (01B)

- ✅ project_detail.html v1 차수 산출물 그대로 인정
- ✅ 다음 차수 (v2: data-dn 부착) 진행 OK
- ✅ HTML/CSS 코멘트, commit 메시지 v5H226z.. 자유 사용
- ✅ 워크트리 자체 BAT (01B_*.BAT) 자유 생성
- ❌ 메인 BAT (KNK_시작.bat / START.bat) 수정 X
- ❌ `_v5_partials/` / DB / main.py 라우트 X

## 📞 동기화 절차 (옵션 A 가정)

차수 완료 시점:
1. 변경 HTML 파일 → 메인 폴더 같은 경로에 복사
2. HANDOFF + PROGRESS → 메인 `01B.../output/`, `01B.../PROGRESS.md` 복사
3. chat 출력: "01B vN 차수 메인 폴더 동기화 완료"
4. 대표 → "빅터, 01B 통합" 지시 → 빅터 검수

---

## 🙇 빅터 사과

룰 정의가 불명확해서 4건 충돌 발생. 본 v2 로 정정 완료.
앞으로 **룰 변경 시 ADDENDUM 번호 명시** + **모든 팀에 동시 배포**.

---

**v2 효력 발생:** 즉시 (2026-05-10)
**01B 작업 차단 해제:** ✅ (v1 차수 통과)
**다음 동기화 절차 시범 운영:** 01B v2 차수부터
