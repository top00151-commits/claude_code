# [AUDIT] 외부 자산 0 정책 12일+55 사이클 위반 + 정직성 거짓 다수 — 2026-04-27 12:00

> **발신**: 09 빅터 팀장 (00 감사팀 긴급 감사 결과 인용)
> **수신**: 김정락 대표
> **카테고리**: AUDIT (정책 위반 확정 + 결재 의무 사안)
> **CEO 응답**: A안/B안/C안 중 결재 의무

---

## 1. 위반 사실 (00 감사 grep -n 직접 인용)

03 Research가 "openpyxl 1건"만 의심 → 00 긴급 감사가 **3종 위반 확정**:

### A. openpyxl (5개소)
- `app/main.py:3102~3105`
- `app/database.py:2257~2258`
- `scripts/migrate_baby_v2.py:79`
- `scripts/baby_web_sync_check.py:38`
- `requirements.txt:7` 명시

### B. pandas (1개소 + 이중 위반)
- `app/database.py:2090: import pandas as pd` (관리코드 .xls 파싱)
- **`requirements.txt` 미명시** → 정직성 추가 위반
- 오리엔테이션 #3 절대 제약 1항 "numpy 금지" — pandas는 numpy 의존 → **이중 위반**

### C. deep_translator + 외부 API 직접 호출
- `app/main.py:1083, 1091` `deep_translator` import (Google Translate / MyMemory)
- `app/main.py:1104` `https://translate.googleapis.com/...` **외부 API 직접 호출**
- `requirements.txt:6` 명시

## 2. 도입 시점 + 검출 실패 기간

- git log -S: 초기 구축 commit `546fb23` (2026-04-15) 부터 이미 존재
- **검출 실패 기간**: 55 사이클 + 12일
- 본 사이클(55) 직전까지 빅터·00 감사 모두 인지 0

## 3. 정직성 거짓 확정 (사이클 52·53·54 감사 보고서)

- 사이클 52·53·54 00 감사 보고서가 "외부 자산 0건 PASS" 명시 → **거짓 확정**
- **A37 (2026-04-26)** 보고서가 "pandas/xlsxwriter/외부 차트 라이브러리 0 (grep 0건)" 적었으나 동일 시점 `database.py:2090 import pandas as pd` 존재 → **거짓 확정**
- 00 감사관 본인이 자체 거짓 가능성 명시 자수 (정직성 v3 자가 적용 모범)

## 4. 빅터 09 팀장 자수

- 빅터가 사이클 1~55 동안 발주서마다 "외부 자산 0건" 절대 제약 명시했으나 **실제 검증은 신규 import만 grep, 기존 import 전수 조사 0건**
- 즉, 정책은 명시했으나 **검증 방법이 부적절**해 12일간 위반 누적
- v3 위반 0건 정책 4항 "외부 자산 도입 절대 금지" 직접 위반 (도입은 04-15 시점, 검출 실패는 빅터 책임)

## 5. 결재 옵션 (대표 의무)

### A안 — 즉시 비활성화 + 대체 (00 감사 권고)
- **openpyxl 즉시 제거** + CSV(stdlib) / HTML 인쇄 view 대체
- **pandas 즉시 제거** + 표준 csv + 수작업 파싱 대체
- **deep_translator + Google Translate 호출** 별도 결재 (P11 베트남 수출 영향 — 번역 기능 폐기 시 P11 페르소나 가치 손실)
- requirements.txt → 5건 축소 (fastapi·uvicorn·jinja2·python-multipart·itsdangerous)
- 사이클 57·58·59 spike로 단계 제거

### B안 — 외부 자산 0 정책 부분 완화 (대표 명시 예외 처리)
- 외부 자산 0 정책에 명시 예외 항목 추가:
  - openpyxl (Excel 출력용 / baby 폐기 후 제거 약속)
  - pandas (관리코드 파싱 / CSV 마이그레이션 후 제거)
  - deep_translator (P11 번역 / 사내 번역 사전 구축 후 제거)
- v4 정책으로 등재
- 단, "외부 자산 0" 표현은 거짓이 되므로 정책 문구 수정 의무

### C안 — 다른 안 (대표 지정)
- 부분 제거 + 부분 유지 등 대표가 직접 결정

## 6. 빅터 자체 보강 권고 (정직성 v3 #6)

본 사례 학습 효과:
- **외부 자산 검증 방법 강화**: 매 사이클 `grep -n "^import\|^from " app/*.py` 결과를 보고서에 직접 인용 (전수 import 노출)
- **requirements.txt 동기화 의무**: 매 사이클 requirements.txt와 실제 import 일치 확인
- **신규/기존 구분 없이 절대치 검증**: "이번 사이클 외부 자산 0건"이 아닌 "전체 코드베이스 외부 자산 0건"을 매 사이클 검증

본 보강은 빅터 자체 권고. 대표 폐기 시 즉시 폐기.

## 7. 후속 사이클 가동 영향

- **사이클 56 (Stock Audits)**: 가동 유지 (새 import 외부 라이브러리 도입 0 발주서 명시) — 결과 도착 시 새 import 전수 검증
- **사이클 57·58·59 후보**: 정상 정찰 진행 — 외부 자산 도입 0 절대 준수
- **A안/B안 결재 후 별도 사이클**로 외부 자산 제거 spike 진행

## 8. 결재 응답 받기 전 빅터 자체 결정 0

- v3 위반 0건 정책 4질문 통과 후: **결재 사안** 명백 → 빅터 자체 결정 절대 금지
- A/B/C안 대표 명시 응답까지 외부 자산 제거 spike 가동 0건
- 대신 다른 트랙(Stock Audits/Adjustments/Projects 등) 무중단 가동 유지 (오리엔테이션 부록 #2 다중 트랙 동시 가동)

---

*발사: 2026-04-27 12:00 / 99 AUDIT 카테고리 / 본 사안은 정직성 12일+55사이클 거짓 누적 — 빅터 책임 인정 + 대표 결재 대기*
