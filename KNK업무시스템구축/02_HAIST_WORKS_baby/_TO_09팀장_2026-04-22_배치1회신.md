# [02 baby → 09 팀장] 배치1 지시 완료 보고

> **발신**: 02 baby 세션
> **수신**: 09 팀장
> **일자**: 2026-04-22
> **참조**: D02-01~D02-04 (09 팀장 배치1 지시)

---

## 완료 항목

### D02-01: 폴더 rename ✅
`HAIST_WORKS_baby` → `02_HAIST_WORKS_baby` — **09 팀장이 git mv로 직접 완료** (commit ca5e0a2, 2026-04-22). 211개 파일 이동 확인.

---

### D02-02: V2 sync.py 자가 검증 ⚠️ (조건부 정상)

**검증 대상**:
- `V2/01_검사기_완제품/scripts/sync.py`
- `V2/02_자동화_완제품/scripts/sync.py`
- `V2/05_소모품/scripts/sync.py`

**각 모듈 기능**:
| 모듈 | 동기화 내용 |
|------|-----------|
| 01_검사기_완제품 | PMS ↔ 8개 부서입력 파일 양방향 연동, 진척률 집계, 완료이력 아카이브, 관리코드 채번 |
| 02_자동화_완제품 | 동일 구조, 7개 부서(전장설계·소프트웨어 등) |
| 05_소모품 | 출하대장 → 수량×단가, INV금액, USD 환산, 관세 자동계산 |

**Imports 분석**: `openpyxl`, `os`, `sys`, `glob`, `datetime`, `logging`, `re` + 로컬 `config.py`, `shared/` — 모두 표준 의존성. `pip install openpyxl` 후 실행 가능.

**발견된 이슈**:
| 이슈 | 심각도 | 위치 | 현황 |
|------|--------|------|------|
| `_sync_ship_summary()`에서 `sr["exrate"]` KeyError | ⚠️ 잠재 | 01/02 sync.py ~L363 | **비활성** (주석 처리). 현재 운영 영향 없음. 활성화 전 수정 필요. |
| `完료\!` 로그 이스케이프 문자 | ℹ️ 경미 | 다수 | 기능 무관, 코드 오염 수준 |
| `_sort_and_pad_proj` 독스트링 vs 코드 불일치 | ℹ️ 경미 | 주석 "10칸" vs 실제 `DATA_AFTER_PAD=5` | 기능 정상 |

**기존 성공 실행 이력**: `80_logs/sync_01_20260414_024321.log` — 성공 완료 기록 확인.

**판정**: ⚠️ **조건부 정상** — `openpyxl` 설치 환경에서 실행 가능. `_sync_ship_summary()` KeyError는 비활성 상태로 현재 운영 영향 없음. 출하현황 기능 활성화 전 별도 수정 필요.

---

### D02-03: Excel 피드백 → 01 메인 전달 ✅

`C:\Users\top00\JR\Claude 코드\KNK업무시스템구축\01_HAIST_WORKS\_FROM_02baby_2026-04-22_엑셀피드백.md` 작성 완료.

상위 5개 불편점 요약:
1. 🔥 sync.py 수동 실행 의존 — 웹 DB 직접 저장으로 해결
2. 🔥 동시 편집 불가 (파일 잠금) — 웹 다중 접근 기본 지원
3. 🔥 날짜 형식 혼재 — Date Picker 강제
4. 🟠 관리코드 채번 누락 — 등록 즉시 자동 발번
5. 🟠 경영현황 대시보드 수동 갱신 — DB 직결 실시간

---

### D02-04: 대시보드 현황 보고 ✅

**전체 baby 폴더 주요 구조**:
```
02_HAIST_WORKS_baby/
├── V2/
│   ├── 01_검사기_완제품/   — PMS.xlsx(1) + 부서입력.xlsx(8) + scripts/
│   ├── 02_자동화_완제품/   — PMS.xlsx(1) + 부서입력.xlsx(7) + scripts/
│   ├── 05_소모품/          — 출하대장.xlsx(1) + scripts/
│   ├── 10_대시보드/        — KNK_경영현황_2026.xlsx + scripts/gen_dashboard.py
│   ├── 80_logs/            — sync 실행 로그 다수 (최근: 2026-04-14~15)
│   ├── 98_문서/            — 사용법.md, 작업일정표.xlsx
│   └── shared/             — styles.py, knk_standard.py, reset_utils.py
└── V1/                     — 구버전 (아카이브 권장)
```

**xlsx 확인**: 총 20개 이상 xlsx 파일 존재 (PMS 2개 + 부서입력 15개 + 출하대장 1개 + 경영현황 1개 + 기타).

**경영현황 대시보드 파악 가능 항목**: 수주 건수, 진척률, 출하 현황 (gen_dashboard.py 수동 실행 후 스냅샷). **누락**: 재무 지표(매출마감 미연동), 자재/소모품 현황(별도 파일).

**운영 상태 판단**: ⚠️ **조건부 정상** — V2 sync.py는 기존 성공 이력 있음. 단, 모든 관리가 수동 .bat 실행 의존. 웹 전환 효과가 가장 큰 시스템.

---

## 09 팀장에게 제안

1. **V1 폴더 아카이브**: V1은 더 이상 사용되지 않으므로 `_ARCHIVED_V1/`로 rename 후 git 커밋 권장.
2. **`_sync_ship_summary()` KeyError 이슈**: 01 메인 웹 이관 우선, 수정 우선순위 낮음 (현재 비활성).
3. **웹 이관 모수**: Excel 시스템의 핵심 기능 3가지(채번 + 진척률 집계 + 대장 관리)가 웹에 구현되면 baby 폴더 폐기 가능. D02-03 피드백이 01 메인의 우선순위 판단 자료로 활용되기를 권장.

---

**02 baby 세션** · 2026-04-22
