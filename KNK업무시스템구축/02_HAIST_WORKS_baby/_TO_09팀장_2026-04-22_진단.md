# 02 baby 1단계 진단 — 2026-04-22

> **작성 경위**: 배치3 진단 에이전트 경로 실패 → 09 팀장 clerical 종합 (02 세션의 배치1 회신 `_TO_09팀장_2026-04-22_배치1회신.md` + 팀장 실측 인벤토리).

## 1. V1/V2 전체 파일 인벤토리 (실측)
- `.xlsx`: **100개 이상** (PMS·부서입력·대시보드·경영현황 등)
- `.py`: 59개 (sync·build·config·apply_standard·shared 유틸)
- `.bat`: 36개 (부서별 실행 바로가기)
- `.md`: 17개 (사용법·스킬·보류사유)
- `.log`: 120개 (sync 실행 성공 이력, 2026-04-14~18)

## 2. 10 영역 × 02 baby 커버 매핑

| 영역 | 커버 파일 | 상태 |
|---|---|---|
| 1 전사공통 | `V2/10_대시보드/KNK_경영현황_2026.xlsx` + `gen_dashboard.py` | 🟢 존재 (수동 갱신) |
| 2 부서별 | 검사기 8 부서입력 + 자동화 7 부서입력 = **15 부서별 엑셀** | 🟢 존재 (각 팀 실제 사용) |
| 3 매출 | `01_검사기_완제품/KNK_PMS_2026.xlsx` + `02_자동화_완제품/KNK_PMS_2026.xlsx` — 수주→출하→완료 라이프사이클 | 🟢 **핵심 존재** |
| 4 구매자재 | 부서입력 엑셀 중 구매팀·가공팀 파일 + 소모품 출하대장 | 🟡 부분 (업체·BOM 전용 파일 없음) |
| 5 근태 | **없음** | 🔴 전무 |
| 6 자금 | **없음** (더존 외부) | 🔴 전무 (방침 상 0) |
| 7 메일 | **없음** (가비아 외부) | 🔴 전무 (방침 상 0) |
| 8 메신저 | **없음** (카톡 외부) | 🔴 전무 (방침 상 0) |
| 9 전자결재 | **없음** (가비아 외부) | 🔴 전무 (방침 상 0) |
| 10 수출입 | `V2/05_소모품/` 에 **INV금액·USD 환산·관세 자동계산** 로직 존재 | 🟡 **기초 일부 존재** |

## 3. "이미 잘 되던 것" — WEB 이행 불필요/후순위 후보
- **부서별 입력 엑셀 15개**: 각 팀이 자기 라인 자기 손으로 숙달 · 양식 안정
- **소모품 관세 자동계산 (V2)**: 이미 Python 로직 존재. WEB 이행 시 그대로 포팅 가능
- **관리코드 채번 규칙**: 8자리 `[T/M]-YYMMDD` 표준 확립

## 4. 영역 5·10 엑셀 탐색 결과
- **영역 5 근태**: 찾지 못함. 베이비에 휴가·출장·잔업 엑셀 없음 (KNK 는 하이웍스 전자결재로 처리 중 추정 → 대표 확인 필요)
- **영역 10 수출입**: `05_소모품` 출하대장에 관세·환율·USD 변환 이미 구현 → **수출입 서류 모듈은 이 로직 재사용 가능**

## 5. 자금일보 엑셀 포맷 후보
- baby 폴더에 "자금일보" 명명 파일 **없음**
- 대표 방침(엑셀 등록 수준)을 지원하려면: **KNK 자금팀 외부 엑셀 포맷** 을 02 세션이 입수 → 업로드 양식 설계 필요

## 6. V2 sync.py 자가 검증 (02 배치1 회신 요약)
- ✅ 기존 실행 성공 이력 (80_logs)
- ⚠️ `_sync_ship_summary()` `sr["exrate"]` KeyError 잠재 (현재 주석 비활성)
- ℹ️ 경미 코드 오염 (로그 이스케이프 등)
- **판정**: 조건부 정상. `openpyxl` 설치 환경에서 실행 가능

## 7. 02 baby 가 01 메인에 제출한 피드백 (D02-03 완료)
상위 5개 불편점 → WEB 이행 시 해결 (`01_HAIST_WORKS/_FROM_02baby_2026-04-22_엑셀피드백.md`):
1. 🔥 sync.py 수동 실행 의존
2. 🔥 동시 편집 불가 (파일 잠금)
3. 🔥 날짜 형식 혼재
4. 🟠 관리코드 채번 누락
5. 🟠 경영현황 대시보드 수동 갱신

## 8. 안 해도 되는 것 (02 관점)
- V1 폴더 전면 유지 → **아카이브** 권장 (`_ARCHIVED_V1/`)
- sync.py KeyError 긴급 수정 → 웹 이관 우선 후 처리 (현재 비활성)
- 엑셀 양식 신규 개편 → 웹 이행 시 자연스럽게 종료

## 9. WEB 이행 우선순위 (02 baby 추천)
1. **관리코드 채번** (채번 중앙집중)
2. **진척률 집계** (실시간)
3. **대장 관리** (수주·출하·입금)

이 3 기능이 WEB에 구현되면 → baby 폴더 폐기 가능.

## 10. 핵심 연결 (3 최적안 설계 시 반영)
- 영역 3 (매출) 라이프사이클은 **이미 엑셀에 안정화돼 있음** → WEB 이행 시 "데이터 모델 이식" 이 핵심
- 영역 10 (수출입) 은 05_소모품 관세·USD 로직 재사용 가능 → **대표 결정(c) 별도 모듈은 매출 + 소모품 로직 기반**으로 설계
- 영역 1 (전사공통 대시보드) 은 엑셀 수동 → WEB 실시간화 효과 큼
- 영역 5·6·7·8·9 는 baby 커버 없음 → WEB 신규 또는 외부 유지
