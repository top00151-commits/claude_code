# KNK Mail 작업기록 — Day 2 도메인 구매 + 받기 설정 (2026-06-13)

> 대표님 직접 수행(Cloudflare 대시보드) + 빅터 화면 안내·외부 DNS 검증

---

## ★★ 받기 증명 완료 (12:3x) — 실제 메일 왕복 성공

- top00151@gmail.com → `test@knk-mailtest.com` 발송 → **knk.jungrack.kim@gmail.com 받은편지함 도착** (스팸 아님)
- 제목 "첫테스트" 정상 수신 확인. **Phase 0 POC '받기' 인프라 검증 완료.**

## 1. 완료된 것

| 항목 | 상태 | 비고 |
|---|---|---|
| 테스트 도메인 구매 | ✅ | `knk-mailtest.com` — Cloudflare 직접 등록 (계정 Knk.jungrack.kim@gmail.com) |
| 네임서버 | ✅ | simone.ns.cloudflare.com / kareem.ns.cloudflare.com (도메인 정상 활성) |
| Email Routing — 대상 주소 | ✅ 인증됨 2개 | knk.jungrack.kim@gmail.com, top00151@gmail.com (둘 다 "인증됨") |
| Email Routing — 라우팅 규칙 | ✅ 활성 | `test@knk-mailtest.com` → 이메일로 전송 → knk.jungrack.kim@gmail.com |
| Email Routing — 필요 DNS 레코드 정의 | ✅ (정의됨) | MX×3(route1/2/3.mx.cloudflare.net), TXT DKIM(cf2024-1._domainkey), TXT SPF(v=spf1 include:_spf.mx.cloudflare.net ~all) — 모두 "잠김" 상태로 시스템에 존재 |

## ★ 해결 (12:35) — 예전 UI "레코드 추가 및 사용" 버튼으로 활성화 성공

- 막힌 원인: **새 Email Routing UI(베타)에 "사용 설정" 버튼이 노출되지 않음**
- 해결 경로: 새 UI 상단 파란 배너 **`구 UI 사용`** → 예전 UI "이메일 라우팅 시작" 마법사 → **`레코드 추가 및 사용`** 파란 버튼 클릭
- 결과: 라우팅 상태 "동기화 중" + DNS "이메일 DNS 레코드 구성됨"(초록)
- **빅터 외부 검증 (12:35, 1.1.1.1)**: MX 3개(route1/2/3.mx.cloudflare.net) + SPF(v=spf1 include:_spf.mx.cloudflare.net ~all) **실제 적용 확인** ✅ (Google 8.8.8.8은 전파 중)
- **다음 세션 교훈**: 새 도메인 Email Routing 활성화는 **예전 UI("구 UI 사용")의 "레코드 추가 및 사용"** 버튼이 확실. 새 UI는 버튼 숨김.

## (지난 기록) ⏳ 막혔던 지점 — Email Routing "활성화(적용)" 미완

- 화면 최상단: **상태 = 비활성화됨 / DNS 레코드 = 구성되지 않음**
- 빅터 외부 검증 (12:26): Google 8.8.8.8·Cloudflare 1.1.1.1 양쪽 모두 **MX 레코드 미조회** = 레코드가 실제 DNS 존에 아직 적용 안 됨
- **새 Email Routing UI(베타)에 "Enable/사용 설정" 버튼이 노출되지 않음**
  - 설정 탭 = 레코드 목록(잠김) + "비활성화(삭제)" 빨간 버튼만 있음. 켜기 버튼 없음
  - 개요 탭 = 요약 카드만. 켜기 버튼 없음
  - "비활성화" 좌측 메뉴 = "삭제 및 비활성화"(위험) 페이지 → 취소함

## 3. 추정 원인 (둘 중 하나)

1. **도메인이 방금 등록됨**(2026-06-13, 1시간 내) → Cloudflare 내부 프로비저닝 진행 중 (초기 화면에 "동기화 중" 표시됐었음 → 시간 지나면 자동 활성화 가능성)
2. 새 UI 버그/버튼 숨김

## 4. 다음 할 일 (재개 시)

1. **빅터 DNS 재검증**: `Resolve-DnsName knk-mailtest.com -Type MX -Server 1.1.1.1` → MX 나오면 활성화 완료
2. MX 나오면 → **즉시 실제 메일 발송 테스트**: 아무 메일에서 `test@knk-mailtest.com` 으로 1통 → knk.jungrack.kim@gmail.com 수신 확인
3. 여전히 비활성화면:
   - 예전 UI 직접 URL 시도: `dash.cloudflare.com/5233268e2e4ed48ed810457499257f0f/knk-mailtest.com/email/routing/overview`
   - 또는 도메인 DNS 화면에서 MX/TXT 5종 수동 추가 (최후 수단)
   - 또는 Cloudflare 지원/Ask AI

## 5. 식별자 (재개용)

- Cloudflare 계정 ID: `5233268e2e4ed48ed810457499257f0f`
- Zone ID (knk-mailtest.com): `0b1bed9496c7d3a579c479efe347a894`
- 필요 DNS 레코드 (수동 추가 시):
  - MX `knk-mailtest.com` 우선순위 82 → `route1.mx.cloudflare.net`
  - MX `knk-mailtest.com` 우선순위 25 → `route2.mx.cloudflare.net`
  - MX `knk-mailtest.com` 우선순위 42 → `route3.mx.cloudflare.net`
  - TXT `cf2024-1._domainkey.knk-mailtest.com` → `v=DKIM1; h=sha256; k=rsa; p=...`
  - TXT `knk-mailtest.com` → `v=spf1 include:_spf.mx.cloudflare.net ~all`

---

## ★★★ 메일함 — HAIST WORKS 메뉴로 구현 완료 (2026-06-13, 4단계)

> 대표 지시: "이 메일함을 HAIST WORKS 메뉴로" + "내 메일함"(대표 수신)

### 만든 것 (7개 파일)
| 파일 | 내용 |
|---|---|
| `app/mail_store.py` (신규) | 받기 저장·조회·소유권·별표·삭제 + AI 분류/언어/요약/번역 + 데모주입. 수신자 매핑(test@→대표 kjr) |
| `app/database.py` | 표 3개 추가: mail_messages·mail_attachments·mail_aliases (SCHEMA, startup init_db 자동생성) |
| `app/main.py` | 라우트 8개: POST /api/mail/inbound(토큰), /mail/inbox, /mail/inbox/demo, GET /mail/{id}, /star, /delete, POST /api/mail/{id}/translate |
| `app/templates/mail_inbox.html` (신규) | 받은편지함 — 분류필터·별표·안읽음·요약미리보기·시범버튼 |
| `app/templates/mail_view.html` (신규) | 메일보기 — AI요약·한/베 번역버튼·삭제 |
| `app/menu_catalog.py` | M-00-19 메일함 등록(검색 별칭 포함) |
| `app/templates/_v5_partials/chrome.html` | 사이드바 '공통' 그룹에 📧 메일함(연락처 아래) |

### 검증 (전부 통과)
- PY 컴파일·Jinja 파싱 OK
- 실 스키마 적재: 표3 생성·대표(id3 ceo) 매핑·test@→대표·적재·목록·**소유권 차단(타인 0)**·읽음·별표·삭제 OK
- TestClient 렌더: /mail/inbox 200·/mail/{id} 200·분류필터 200, 메뉴·본문 표시 확인

### AI 상태 (정정 — 정상)
- 처음엔 빅터 로컬 검증프로세스가 옛 키(...xKYA)로 401 → "AI 키 문제"로 오판.
- **정정**: 대표가 운영(NAS) 서버에서 '연결 테스트' → provider openai·model gpt-5.4-mini·응답 OK = **운영 AI 정상**. 로컬 User env 키만 stale(운영 무관).
- → **배포하면 메일함 AI 분류·요약·번역 정상 작동**. (로컬 PC에서 AI까지 테스트하려면 유효 키를 프로세스 env로 주입 필요)

### 진행 방식 (설계대로 2단계)
- 1단계(완료): 코드+로컬검증. 2단계(대기): NAS 배포 + Cloudflare Worker가 /api/mail/inbound 로 실제 메일 전달(라우팅 gmail전달→Worker 전환). 지금은 '시범 메일' 버튼으로 동작 확인 가능.

### ✅ 운영 배포 완료 (2026-06-13)
- 배포repo **knk-haist-works 60c126c** + 모노레포 **claude_code 6bba253** push.
- 검증: `/api/version`={"v":"60c126c"}(일치)·`/mail/inbox`=303(라우트 라이브, 직전 404). NAS cron 자동 pull+재시작→init_db 표 자동생성.
- ⚠ **교훈**: 처음 모노레포에만 push → /mail/inbox 404. WORKS는 **01_HAIST_WORKS 디렉터리(=별도 repo knk-haist-works)에서 commit+push** 해야 NAS 반영. (메모리 feedback_auto_deploy에 이미 상세 문서화 — 배포 전 정독 필수.)

### 다음
1. **Cloudflare Email Worker** 작성 + 라우팅 전환: 현재 test@→gmail 전달 → Worker가 받은 메일을 `POST /api/mail/inbound`(헤더 X-KNK-Mail-Token)로 WORKS에 밀어넣기. NAS에 환경변수 `KNK_MAIL_INBOUND_TOKEN` 설정 필요(전산). = 실제 메일이 WORKS 메일함에 도착.
2. 보내기(AWS SES 또는 Cloudflare Email Sending).
3. (선택) AI 키는 운영 정상(대표 연결테스트 확인). 메일함 AI 즉시 작동.

### z379 — 이름 "HAIST WORKS 메일함"으로 통일 (대표 지시)
- 사이드바·메뉴카탈로그(M-00-19)·받은편지함/메일보기 제목·탭·breadcrumb 표기를 '메일함'→'HAIST WORKS 메일함'. 기능·경로 무변경.
- 배포: knk-haist-works **60c126c→1026161** + 모노레포 claude_code **6bba253→ecab8b8**. (이번엔 배포repo 먼저 → 정상 순서)

*작성: 빅터 — 2026-06-13. 메일함 운영 배포 완료(이름 'HAIST WORKS 메일함').*

---

## ★★★★ 실제 메일 수신 파이프라인 완성·작동 확인 (2026-06-13)

**`외부 발신 → test@knk-mailtest.com → Cloudflare Worker(knk-mail) → /api/mail/inbound → 대표 메일함 + 지메일 안전망`** — 대표 실메일 도착 확인.

### 한 일
- 받는 인프라(z382): /api/mail/inbound 원문(MIME) 파싱 + 토큰(관리자설정) + /admin/mail-inbound(토큰생성·Cloudflare 안내).
- Cloudflare(대표 클릭, 빅터 화면안내): Worker `knk-mail` 생성→코드 붙여넣기→배포 / 변수 3개(WORKS_INBOUND_URL·KNK_MAIL_INBOUND_TOKEN암호화·FORWARD_TO) / Email Routing test@ 동작 'gmail전달'→'Worker로 전송'.
- 받는사람 수정(z383): 받은 메일이 대표 메일함에 안 보이던 것 → '내 메일함으로 받기' 버튼으로 바인딩.

### 디버깅 2건 (다음 세션 교훈)
1. **Worker 변수는 추가 후 재배포해야 적용**. 처음엔 변수 추가만 하고 안 했더니 env undefined → fetch 실패(조용히)·gmail만 도착. Worker 재배포하니 `WORKS_RESULT status=200 ok:true`.
2. **default_owner가 운영 동명계정으로**: 받은메일 owner=시드 'kjr'(id3)인데 대표 로그인계정은 다른 id → 메일함에 안 보임(시범메일은 본인id 소유라 보임). 진단=Worker console.log(status/urlSet/tokenSet) + WORKS 직접 probe(틀린토큰 401). 해결=resolve_recipient 우선순위 별칭>mail_default_user_id>default_owner + 관리자 '내 메일함으로 받기'(설정·별칭·기존메일 이동).

### 배포
- 배포repo knk-haist-works: z382 36af0a9 → z383 **bc4ceff**. 모노레포 claude_code: 76e7be0.
- ⚠ 동시편집: 다른 세션이 z380~z382 커밋하며 한때 main.py 섞임 → explicit add·git show HEAD 점검으로 매번 일치 확인.

### 남은 단계
1. **보내기** (AWS SES 또는 Cloudflare Email Sending 베타) — WORKS 메일함에서 답장·발송.
2. 첨부 파일 본문 저장(현재 이름·개수만 표시).
3. Worker 진단 console.log 정리 + WORKS admin 페이지 Worker 코드 동기화(현재 페이지=무로그판, 운영 Worker=로그판).
4. 이후 Phase: 베트남법인 시범 → 본사 확대 → MX 전환.

*작성: 빅터 — 2026-06-13. 받기 파이프라인 완성.*
