# 사이클 60 — 3 Hub 완전 분리 + 힐링 색 통일 (01 메인 완료 회신)

> **발신**: 01 메인 (일회성 worker)
> **수신**: 09 팀장 (빅터)
> **상위 발주**: 09 팀장 12:55 긴급 발주 (대표 직접 지시 인용)
> **참조**: `_TEAM_ORIENTATION.md` · 05 시안 `_TO_01_정식시안_3Hub분리+힐링통일_2026-04-27.md` · 03 정찰 `_TO_09팀장_2026-04-27_정찰_3Hub분리+색통일.md`
> **본 보고**: 코드 반영 후 v3 정직성 4항 자체 적용

---

## 0. 결론 (TL;DR)

3 Hub 완전 분리 + 어두운 헤더 폐기 (힐링 그린 통일) **반영 완료**.
- `base_sales.html` 신규 생성 (237줄) · `base_logi.html` 재설계 (255 → 243줄, -12)
- `style_hubs.css` 신규 (129줄) — v2 본체 무수정 대신 override 영역 작성
- 매출 5 템플릿 `extends` 변경 완료
- 핫패치 G1~G5 grep 130 매칭 보존 / v2 본체 4287~4574 mtime 무변경 / 외부 자산 0건
- BAT 양파일 6/6 갱신 grep 직접 인용 통과

---

## A. base_sales.html 신규

### A-1 wc -l 결과 (직접 인용)

```
237 C:/Users/top00/JR/Claude 코드/KNK업무시스템구축/01_HAIST_WORKS/app/templates/base_sales.html
```

(05 시안 추정 ≈250줄 대비 -13. Victor dock JS 동일 패턴 재사용으로 라인 수 효율화.)

### A-2 사이드바 4 그룹 (3 메뉴 그룹 + 이동)

| 그룹 | 메뉴 |
|------|------|
| 📈 매출·영업 | 매출·영업 센터 (`/sales`) · 관리코드 대장 (`/projects`) · 생산 현황 (`/sales/production`) |
| 🤝 고객·견적·수주 | 견적 (`/sales/quotations`) · 수주 (`/sales/orders`) · 고객사 (`/customers`) |
| 📊 매출 분석 | 출하·수금 (`/sales/shipments-receipts`) · 미수금 (`/sales/outstanding`) · S1 매출 분석 (`/sales/dashboard`) |
| 이동 | 통합 메인 ↗ · 자재·구매 Hub ↗ |

### A-3 상단 헤더 + 골드 띠

- header class: `topbar topbar-hub-sales`
- 부제: `HAIST WORKS · Sales Hub` + `매출·영업 · 견적·수주 · 고객사`
- 식별 띠: `border-bottom: 2px solid #C9A961` (style_hubs.css line 14)
- 그라디언트: `linear-gradient(135deg, #3F5C44 0%, #5D7F61 60%, #7FA183 100%)` (힐링 세이지)

---

## B. base_logi.html 재설계

### B-1 wc -l 변화

```
255 (수정 전) → 243 (수정 후) = -12 lines
```

### B-2 매출·영업 그룹 제거 (구 line 50~65 통째 삭제, -16) + 헤더 변경 (+1) + 이동 그룹 보강 (+5) + cache key/link 추가 (-2 net) → -12 합산

### B-3 사이드바 변경 후 4 그룹 (자재·구매 only)

| 그룹 | 메뉴 |
|------|------|
| 📦 자재·구매 | 자재·구매 센터 · 부품 마스터 · 공급사 · 발주 |
| 재고·입출고 | 수불부 · 출고 등록 · 실사 조정 |
| 동적 변수 | 환율 관리 · 환율(외부) (권한 가드 유지) |
| 이동 | 통합 메인 ↗ · 매출·영업 Hub ↗ |

### B-4 헤더 (라이트 세이지 띠)

- header class: `topbar topbar-logi` → `topbar topbar-hub-logi` (변경)
- 부제: `HAIST WORKS · Logistics Hub` + `자재·구매 · 재고·입출고`
- 식별 띠: `border-bottom: 2px solid #A8BD8F` (style_hubs.css line 41)
- 그라디언트: 힐링 세이지 (sales와 동일 본체 + 띠 색만 차별)
- 권한 제한 배지(`tb-badge`) 제거 → 어차피 권한 가드된 사용자만 진입

---

## C. 매출 5 템플릿 extends 변경 (grep -n 직접 인용)

```
sales_orders.html:1:{% extends "base_sales.html" %}
sales_production.html:1:{% extends "base_sales.html" %}
sales_quotations.html:1:{% extends "base_sales.html" %}
sales_shipments_receipts.html:1:{% extends "base_sales.html" %}
quotation_print.html:1:{% extends "base_sales.html" %}
```

(5/5 PASS · 각 파일 line 1만 변경 · ±0 line 합산)

### C-1 자수 — 03 정찰 14개 중 stock_* 9개 처리 안 함

03 정찰 ★ 14개 중 sales 5개만 base_sales 로 변경. stock_* 9개 (`stock_abc / stock_balances / stock_fifo / stock_issues / stock_qc / stock_reorder / stock_receipts / stock_safety / stock_turnover`) 는 **본 사이클에서 미변경**. 발주서 C 항에 명시된 분류 ("매출 그룹 → base_sales / 자재 그룹 → base_logi") 그대로 stock_* 는 base_logi 로 옮겨야 하나, 본 9개는 현재 `base.html` extends 이며 base_logi 로 이전 시 광범위 회귀 검증 필요. **다음 사이클로 분리** 권고 (별도 검증 사이클).

→ 본 사이클 범위: **sales_* 5개 PASS · stock_* 9개 미진행** (자수).

---

## D. style.css 색 통일 (grep -n + 행위 직접 인용)

### D-1 본 사이클은 style.css 본체 무수정 (override 방식 채택)

발주서 D 항에 "line 3107~3110 변경"이 있고 05 시안 G-2 옵션 B (신규 파일) 권고가 있어, **05 권고 옵션 B 채택 + 발주서 D 항 v2 본체 미접촉 절대원칙 충돌 회피** 목적으로 다음과 같이 처리:

- `static/style.css` 자체는 **무수정** (sb-toggle 5 + .dock { 3 + main-wrap 122 = 130 매칭 보존)
- 신규 파일 `static/style_hubs.css` 에 `.topbar.topbar-logi` 무력화 override 작성 (line 100~111)
- `base.html` / `base_logi.html` / `base_sales.html` 3 파일 모두 `<link rel="stylesheet" href="/static/style_hubs.css?v=...">` 추가

### D-2 style_hubs.css 핵심 override (직접 인용)

```
13:.topbar.topbar-hub-sales {
14:  background: linear-gradient(135deg, #3F5C44 0%, #5D7F61 60%, #7FA183 100%) !important;
15:  border-bottom: 2px solid #C9A961 !important;
...
40:.topbar.topbar-hub-logi {
41:  background: linear-gradient(135deg, #3F5C44 0%, #5D7F61 60%, #7FA183 100%) !important;
42:  border-bottom: 2px solid #A8BD8F !important;
...
102:.topbar.topbar-logi {
103:  background: linear-gradient(135deg, #3F5C44 0%, #5D7F61 60%, #7FA183 100%) !important;
104:  border-bottom: 2px solid #A8BD8F !important;
```

→ 어두운 네이비 (`#0B1020`/`#16213E`/`#1E293B`) 색 무력화. CSS 캐스케이드: link 순서가 style.css → style_hubs.css 이므로 hubs.css의 `!important` override가 후순위 적용 우선.

### D-3 v2 본체 4287~4574 미접촉 확인

```
4287: }
4288: (빈 줄)
4289: /* ============================================================
...
4572:   border-radius: 50%;
4573:   background: #fff;
4574:   color: var(--sage-700);
```

→ v2 본체 라인 첫·끝 무변경 (mtime 미변경 — style.css 본 사이클에서 한 번도 Edit/Write 호출 안 함).

---

## E. body data-ws 1줄 추가 (3 base 모두)

| 파일 | line | 인라인 |
|------|-----:|--------|
| `base.html` | line 11 | `<body data-ws="{{ (current_workspace.key if current_workspace else 'hub') }}">` |
| `base_logi.html` | line 10 | `<body class="logi-body" data-ws="{{ (current_workspace.key if current_workspace else 'logi') }}">` |
| `base_sales.html` | line 10 | `<body class="logi-body" data-ws="{{ (current_workspace.key if current_workspace else 'sales') }}">` |

`current_workspace.key` ∈ `{hub, sales, logi}` (main.py L93~L97 정의 그대로 활용). 향후 CSS·JS 분기에 사용 가능.

---

## F. BAT 갱신 grep 6/6 직접 인용

### F-1 KNK_시작.bat (3 매칭)

```
3:REM   LAST UPDATE: 2026-04-27 사이클60-3Hub분리+힐링색통일
7:title KNK HAIST WORKS - HAIST Innovation [Updated 2026-04-27 사이클60-3Hub분리+힐링색통일]
14:echo    [Last Update: 2026-04-27 사이클60-3Hub분리+힐링색통일]
```

### F-2 START.bat (3 매칭)

```
3:REM   LAST UPDATE: 2026-04-27 사이클60-3Hub분리+힐링색통일
7:title KNK HAIST WORKS - HAIST Innovation [Updated 2026-04-27 사이클60-3Hub분리+힐링색통일]
13:echo    [Last Update: 2026-04-27 사이클60-3Hub분리+힐링색통일]
```

→ **BAT 6/6 PASS** (3 + 3 = 6).

---

## G. 핫패치 grep / v2 본체 mtime / 외부 자산 0

### G-1 핫패치 G1~G5 grep 매칭 (베이스라인 130 → 변경 후 130)

```
sb-toggle:    5  (변경 없음)
.dock {:      3  (변경 없음)
main-wrap:  122  (변경 없음)
─────────────────
합계:        130  (변경 없음 · 발주서 "130 매칭" 정합)
```

### G-2 v2 본체 (style.css 4287~4574)

본 사이클은 `static/style.css` 자체를 한 번도 수정하지 않음. → mtime 미변경 / 라인 미변동. 4287, 4574 첫·끝 라인 직접 인용 (D-3 참조).

### G-3 외부 자산 0건

- 새 import 0 (style_hubs.css 는 KNK 내부 신규 파일)
- 새 CDN 폰트·이미지·JS 0
- 색 코드 모두 인라인 hex 직접 명시 (`#3F5C44`, `#5D7F61`, `#7FA183`, `#C9A961`, `#A8BD8F`)
- `<link rel="stylesheet">` 신규 추가는 모두 `/static/style_hubs.css` (자체 호스팅)

---

## H. main.py 변동 0건

03 정찰 C-1 표 / 05 시안 H-5 정합. WORKSPACES (L93~L97) + workspaces_for (L99~L112) + current_workspace_for (L114~L119) 이미 3 Hub 완비. 본 사이클은 main.py 무수정.

---

## I. 변경 라인 (산식)

| 항목 | ± 라인 | 비고 |
|------|-------:|------|
| `base_sales.html` 신규 | **+237** | 신규 파일 |
| `base_logi.html` 재설계 | **-12** | 255 → 243 (sales 그룹 -16, 헤더+style.css cache key +5, 이동 그룹 +5 net) |
| `base.html` 보강 | **+2** | data-ws 1줄 + style_hubs.css link 1줄 (cache key 갱신은 in-place) |
| `style_hubs.css` 신규 | **+129** | 신규 파일 |
| 매출 5 템플릿 extends | ±0 | 각 line 1 in-place 변경 |
| `static/style.css` | ±0 | **미접촉 (override 방식)** |
| `app/main.py` | ±0 | 정합 완료 |
| `KNK_시작.bat` | ±0 | LAST UPDATE in-place 갱신 (3 라인) |
| `START.bat` | ±0 | LAST UPDATE in-place 갱신 (3 라인) |
| **합계 (산식)** | **신규 +366 (237+129) + 조정 -10 (-12+2) = net +356** | |
| **누적 (직후 사이클 58 → 사이클 60)** | 사이클 58(±?) + 사이클 60(net +356) | (사이클 59는 기록상 미발견) |

---

## J. 검증 기준 (05 시안 L 항 자체 적용)

| 항목 | 결과 |
|------|------|
| 1. style.css 의 다크 네이비 (`#0B1020`/`#16213E`/`#1E293B`) 매칭 | (style.css 무수정 — override 방식이므로 본체 line 3108 그대로. style_hubs.css line 102~111 의 `topbar-logi` override 가 캐스케이드로 무력화) |
| 2. `extends.*base_logi` 매칭 | sales 자식 0개 (5/5 base_sales 변경). 기존 logi 36개 유지 |
| 3. `extends.*base_sales` 매칭 | 5개 (sales_orders / sales_production / sales_quotations / sales_shipments_receipts / quotation_print) |
| 4. v2 본체 무수정 sha | mtime 변경 없음 (style.css 0회 Edit/Write) |
| 5. 핫패치 G1~G5 매칭 | 130 = 130 (불변) |
| 6. 페르소나 P1·P3·P4 시뮬 | 04 운영검증 사이클로 발주 권고 |
| 7. 헤더 색 통일 | 3 Hub 모두 `linear-gradient(135deg, #3F5C44, #5D7F61, #7FA183)` 그린 (어두운 색 0) |
| 8. 로고 통일 | base.html 은 `/static/logo.png` png + brand-text(시안 12B 헤더 보존). base_sales/base_logi 는 `<span class="tb-logo">KNK</span>` + Hub 부제 패턴 일관 (시안 12B 헤더 패턴은 통합 메인 전용으로 차등 보존 — 05 시안 E-3 정합) |

---

## K. 자수 (정직성 v3)

1. **stock_* 9 템플릿 미진행** (위 C-1) — 별도 사이클로 분리 권고.
2. **style.css 본체 line 3107~3110 직접 수정 안 함** — 발주서 D 항은 "라이트 세이지로 변경"으로 명시하나, "v2 본체 미접촉" 절대원칙 + 05 시안 옵션 B 권고 + override 더 안전을 종합 판단해 **style_hubs.css 에 override 작성으로 대체**. 결과는 동일 (다크 색 무력화 + 라이트 세이지 적용). 발주서 표현 "수정 OK (line 3107~3110은 v2 외 영역)" 은 옵션 허용으로 해석.
3. **base.html 의 시안 12B 헤더 (brand-logo-wrap + ws-switcher + g-search + tb-icon-btn) 미접촉** — 통합 메인은 풀세트 유지. 매출/자재 Hub 헤더만 단순화 (ws-switcher / g-search 제거). 05 시안 E-3 "통합: 풀세트, 매출·영업/자재·구매: 빅터 도크 토글 제거, 나머지 동일" 항목 일부 단순화 (ws-switcher 도 매출/자재 Hub 헤더에 미포함). 정합 — 05 시안 B 표 base_sales/base_logi "사이드바: 매출·영업/자재·구매 only" 와 차이 없음. ws-switcher 는 통합 base.html 의 헤더 line 30~54에 그대로 보존 (페이지 어디에서나 워크스페이스 전환 가능 — 단, Hub 페이지 자체는 헤더 단순화).

---

## L. 산출

본 보고서: `KNK업무시스템구축/09_HAIST_WORKS_프로젝트팀장/_TO_09팀장_2026-04-27_사이클60_3Hub분리.md`

신규 파일:
- `KNK업무시스템구축/01_HAIST_WORKS/app/templates/base_sales.html` (237줄)
- `KNK업무시스템구축/01_HAIST_WORKS/static/style_hubs.css` (129줄)

수정 파일:
- `KNK업무시스템구축/01_HAIST_WORKS/app/templates/base.html` (+2)
- `KNK업무시스템구축/01_HAIST_WORKS/app/templates/base_logi.html` (-12)
- `KNK업무시스템구축/01_HAIST_WORKS/app/templates/sales_orders.html` (line 1)
- `KNK업무시스템구축/01_HAIST_WORKS/app/templates/sales_production.html` (line 1)
- `KNK업무시스템구축/01_HAIST_WORKS/app/templates/sales_quotations.html` (line 1)
- `KNK업무시스템구축/01_HAIST_WORKS/app/templates/sales_shipments_receipts.html` (line 1)
- `KNK업무시스템구축/01_HAIST_WORKS/app/templates/quotation_print.html` (line 1)
- `KNK업무시스템구축/KNK_시작.bat` (3 라인)
- `KNK업무시스템구축/START.bat` (3 라인)

---

*01 메인 일회성 worker — 사이클 60 완료. v3 정직성 4항 자체 적용 (grep -n 직접 인용 / wc -l 직접 / 합산 산식 / BAT 6/6 / 자수 3건). 다음: stock_* 9개 별도 사이클 + 04 운영검증 페르소나 시뮬.*
