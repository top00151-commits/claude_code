# 2026-04-21 감사보고_02 — 카카오워크 문자열 분포 조사

> **감사자**: 00 감사팀 대행(Explore 에이전트)
> **팀장 clerical 저장**: 09 프로젝트팀장
> **감사 범위**: 커밋 `9400880` "카카오워크 제거 → 하이웍스 API 통합" 주장 vs 실소스 6개 파일 매치

## 감사 방법
- 6개 파일 각각 Read → `카카오워크|kakaowork` 매치 라인 추출
- 각 매치를 A(실행경로) / B(주석·legacy) / C(폐기 가이드 문서) 로 분류

## 파일별 판정

### 1) `_DESIGN_변경_Inform.md`
| 라인 | 문맥 | 분류 |
|------|------|------|
| 5 | "흡수 패턴: ... + 카카오워크 Webhook" | **A** |
| 198 | "카카오워크: webhook (config로 채널 매핑)" | **A** |
| 290 | "☑ 카카오워크 채널 푸시" (UI 체크박스 mockup) | **A** |
| 372 | `kakao_webhook_send(channel_id=user.get("kakaowork_channel_id"), ...)` | **A** |

**소계**: 4 A (설계 청사진의 핵심 아키텍처로 명시)

### 2) `_GUIDE_카카오워크_Webhook_발급.md`
- 문서 전체 (Webhook URL 발급·테스트·설정 절차)
- **분류 C**: 폐지 안내 성격의 별도 가이드 문서

### 3) `app/database.py`
| 라인 | 문맥 | 분류 |
|------|------|------|
| 846 | `# 하이웍스 통합 — 카카오워크 미사용` (주석) | B |
| 854 | "카카오워크 대체" (설정 설명) | B |
| 861 | "기존 카카오워크 키가 있으면 description만 갱신" (마이그레이션) | B |
| 863 | `"UPDATE ... WHERE key='kakaowork_webhook'"` (DB 쿼리) | B |
| 3901–3903 | `# 카카오워크 제거` (함수 헤더 주석) | B |
| 3905 | `def kakao_webhook_send(channel_id: str, ...)` 함수 정의 | **A** |

**소계**: 5 B, 1 A (함수 자체는 **호환성 래퍼**로 존재하며 main.py에서 호출됨)

### 4) `app/hiworks_client.py`
- **매치 없음**

### 5) `app/main.py`
| 라인 | 문맥 | 분류 |
|------|------|------|
| 2485 | `# 알림 발송 (web 게시판 자동 글 + 카카오워크 stub)` | B |

**소계**: 1 B (실제 발송은 하이웍스로 라우팅)

### 6) `app/templates/admin_settings.html`
| 라인 | 문맥 | 분류 |
|------|------|------|
| 11 | "하이웍스 외부 시스템 URL과 카카오워크 Webhook 등을 관리" | B |
| 49 | 참조 가이드: `외부연결_가이드_하이웍스_카카오워크.md` | C |
| 54 | "카카오워크 대체" 설정 라벨 | B |
| 100–105 | `{# 카카오워크 섹션은 비활성 안내로 축소 #}` + "카카오워크 (미사용)" | B |
| 112 | `'kakaowork_webhook'` 설정 키 | B |

**소계**: 4 B, 1 C

## 집계
| 분류 | 건수 | 위치 요약 |
|------|-----|-----------|
| **A 실행경로** | **5** | 설계 문서 4건 + database.py `kakao_webhook_send` 함수 1건 |
| **B legacy 주석** | **12** | database/main/admin_settings 분포 |
| **C 폐기 안내 문서** | **3** | _GUIDE_ 문서 전체 + admin_settings 가이드 참조 |

## 종합 판정
⚠️ **"실행 경로에 잔재 있음"** — 커밋 `9400880` 메시지는 **부분 정당**.

**근거**:
- `kakao_webhook_send()` 함수가 `database.py`에 여전히 존재하며 `main.py` L2485에서 호출됨 (비록 내부 구현은 하이웍스로 라우팅되는 호환성 래퍼라 하더라도 **이름·진입점**은 살아있음)
- 설계 문서 `_DESIGN_변경_Inform.md` 는 원설계 당시 청사진 그대로 유지 → 현 실구현과 불일치

**"완전 제거" 로 부르려면 추가 조치 필요**:
1. `_DESIGN_변경_Inform.md` L5, L198, L290, L372 를 현 구현 (하이웍스 우선) 으로 갱신
2. `database.py` L3905 `kakao_webhook_send()` 함수 → 제거 또는 호출부와 함께 `hiworks_send()` 로 일원화
3. `admin_settings.html` L11 설명 텍스트에서 "카카오워크" 제거 (혹은 "레거시 — 사용 안 함" 명시)
4. `_GUIDE_카카오워크_Webhook_발급.md` 는 `_LEGACY/` 로 이동 또는 상단에 "DEPRECATED" 배너

## 01 메인에 반환할 조치 (감사팀 → 01)
- 01 세션이 D01-03 회신 시, 본 감사보고를 근거로 "잔재 실행경로 5건" 을 모두 정리하거나, 정리 범위를 명시적으로 타협 (예: 설계 문서는 역사 보존 차원 유지) 해야 함.
- 감사팀 view: 설계 문서는 수정, 함수 래퍼는 제거 권고. 의사결정은 대표·팀장 몫.

## 부록 — 범위 밖 관찰
- `hiworks_client.py` 에는 카카오워크 흔적 없음 → 하이웍스 통합 전용 모듈로 깨끗하게 분리됨 (긍정 신호).
