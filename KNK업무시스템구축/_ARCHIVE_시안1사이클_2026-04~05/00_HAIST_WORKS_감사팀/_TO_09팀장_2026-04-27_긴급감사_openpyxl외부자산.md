# 00 긴급 감사 — openpyxl 외부 자산 위반 검증

> **발주**: 09 팀장 (2026-04-27, 03 Research 정찰 11:50 트리거)
> **감사관**: 00 감사팀 (코드 수정 0, 검증·보고만)
> **결론 한 줄**: **위반 확정**. openpyxl 단건이 아니라 **pandas + openpyxl + deep_translator 3종 외부 패키지** 동시 위반. 사이클 52·53·54 감사 보고서가 "외부 자산 0건 PASS"라고 적은 부분 **거짓 가능성 높음** → 99 AUDIT 발사 권고.

---

## 1. 사실 (grep -n 직접 인용)

### 1-A. openpyxl (사용 5개소)

```
01_HAIST_WORKS/app/main.py:3102:        from openpyxl import Workbook
01_HAIST_WORKS/app/main.py:3103:        from openpyxl.styles import Font, PatternFill, Alignment, Border, Side
01_HAIST_WORKS/app/main.py:3105:        return JSONResponse({"error": "openpyxl 미설치"}, 500)
01_HAIST_WORKS/app/database.py:2257:    from openpyxl import Workbook
01_HAIST_WORKS/app/database.py:2258:    from openpyxl.styles import Font, PatternFill, Alignment, Border, Side
01_HAIST_WORKS/scripts/migrate_baby_v2.py:79:        import openpyxl
01_HAIST_WORKS/scripts/baby_web_sync_check.py:38:    from openpyxl import load_workbook
```

사용 위치:
- `main.py:3096` `@app.get("/export/weekly")` — 주간 요약 엑셀 다운로드 라우트 (export_weekly 핸들러)
- `database.py:2253` `def build_password_xlsx(rows, out_path)` — 초기 비밀번호 배포용 xlsx 생성 (admin 비밀번호 일괄 발급)
- `scripts/migrate_baby_v2.py` — baby Excel → web SQLite 마이그레이션 (web 본체 외 운영 스크립트)
- `scripts/baby_web_sync_check.py` — baby/web 정합성 체크 스크립트

### 1-B. pandas (1개소, 동급 위반)

```
01_HAIST_WORKS/app/database.py:2090:    import pandas as pd
```

위치: `def parse_mgmt_xls(file_path)` — 관리코드발행목록.xls 파싱 (xls 구버전 → openpyxl로 못 읽음 → pandas+xlrd 의존). app 본체 라우트에서 호출되는 헬퍼.

### 1-C. deep_translator (2개소 + requirements.txt 명시)

```
01_HAIST_WORKS/requirements.txt:6:deep-translator
01_HAIST_WORKS/app/main.py:1083:        from deep_translator import GoogleTranslator
01_HAIST_WORKS/app/main.py:1091:        from deep_translator import MyMemoryTranslator
```

deep_translator는 Google Translate / MyMemory **외부 API 호출 라이브러리** — 오리엔테이션 #3 1항이 명시 금지한 "외부 환율 API" 와 동급의 외부 API 사용. main.py:1104에 `https://translate.googleapis.com/...` URL 직접 호출까지 포함.

### 1-D. requirements.txt 전문

```
01_HAIST_WORKS/requirements.txt:
1:fastapi
2:uvicorn[standard]
3:jinja2
4:python-multipart
5:itsdangerous
6:deep-translator         ← 외부 API 라이브러리
7:openpyxl                ← 외부 패키지
```

(pandas는 requirements.txt에 명시되지 않았지만 코드에서 import — **stdlib 가장(假裝)** 의심.)

### 1-E. 도입 시점 / 도입자

```
git log --all --oneline -S "openpyxl" 결과:
a3c4912 선제적 실행: 05디자인 기동완료 + 01메인 제안#02~04+ESC-02 + 02baby D02-02~04 처리
24ab668 (a+b+c): 자동 티켓 + 서버 검증 + 배포 준비 패키지
82e8daf HAIST Victor 🤖 — 사내 AI 컨시어지 Phase 1 (키워드 라우팅)
3a47096 실패 사례 회피 — 5가지 안전망 추가 (변경 관리 강화)
52219d6 변경 Inform 시스템 — 1주차 핵심 기능 (1순위 ② / 6팀)
546fb23 HAIST WORKS 초기 구축 — 데일리허브 v2 + 물류 모듈 통합
```

→ **초기 구축 commit `546fb23` (2026-04-15)** 부터 이미 openpyxl 존재. 즉 프로젝트 출범부터 누적 위반.

---

## 2. 위반 판정

### 2-A. 오리엔테이션 #3 절대 제약 1항 (인용)

> "외부 자산 0건 — Chart.js, numpy, **PDF 라이브러리, 외부 환율 API, CDN 폰트, 외부 이미지 모두 금지. 새 import은 내부 모듈만**." (`_TEAM_ORIENTATION.md:33`)

판정: **명시적 위반**.
- openpyxl: PyPI 패키지 (Python stdlib 아님). 새 import이 아니라 기존 import이지만 "0건" 정책 위반.
- pandas: PyPI 패키지. 오리엔테이션 1항이 numpy를 명시 금지 → pandas는 numpy 의존성 → **이중 위반**.
- deep_translator: PyPI 패키지 + 외부 API 호출 라이브러리. 1항 "외부 환율 API" 와 동격의 외부 API 사용.

### 2-B. v3 위반 0건 정책 4항 (인용)

> "v2 본체 수정 / 외부 자산 도입 / 핫패치 손상 절대 금지" (`_TEAM_ORIENTATION.md:47`)

판정: **명시적 위반** (개수 3건).

### 2-C. 의사결정 사전 체크리스트

> "본 결정이 대표 명시 지시 또는 명시 위임 범위 안인가?" (`_TEAM_ORIENTATION.md:50`)

대표가 openpyxl/pandas/deep_translator 도입을 명시 승인한 기록을 본 감사관은 99_DISPATCH·메모리·오리엔테이션 어디에서도 발견하지 못함. → **무승인 도입**.

---

## 3. 영향 분석

| 라우트/기능 | 외부 자산 | 핵심도 | 대체 가능성 |
|-------------|-----------|--------|-------------|
| `/export/weekly` (주간 엑셀) | openpyxl | 중 (페르소나 사용 중 가정) | **중** — CSV 출력으로 대체 가능 (csv stdlib). 인쇄용은 HTML view + 브라우저 인쇄 |
| `build_password_xlsx` (admin 비번 배포) | openpyxl | 저 (admin 1회성) | **고** — CSV 또는 HTML 인쇄 view 충분 |
| `parse_mgmt_xls` (관리코드 임포트) | pandas | 저~중 (1회성 마이그레이션 가정) | **중** — `.xls` 구포맷이라 stdlib 단독으론 어려움. 권장: 데이터를 한 번 CSV 변환 후 csv 모듈로 처리 |
| `/api/translate` 등 (deep_translator) | deep_translator + Google API | 중 (P11 베트남 수출 실무자) | **저** — 외부 번역 API 자체가 정책 위반. 대안: 사내 i18n 사전(이미 `app/i18n.py` 존재, ko/vi/en 403키) + 미사전어 입력 시 "번역 미지원" 처리 |
| `scripts/*.py` (마이그레이션) | openpyxl | 1회성 (web 완료 후 폐기 예정 baby) | **고** — baby 폐기되면 자연 제거 |

---

## 4. requirements.txt 점검

명시 외부 자산: **2건** (`deep-translator`, `openpyxl`). 그 외 fastapi·uvicorn·jinja2·python-multipart·itsdangerous는 본 시스템 스택 핵심 (오리엔테이션 묵시 허용 범위로 보이나 명시 화이트리스트는 미발견 — **대표 사전 승인 문서 부재**).

추가 의심: **pandas는 requirements.txt에 미명시 + 코드에 import 존재** → "런타임 에러로 숨김 + 정직성 저하". `_DEPLOY_배포_가이드.md:27` 도 `fastapi, uvicorn, jinja2, openpyxl, python-multipart, itsdangerous` 만 나열 — pandas/deep-translator 누락.

---

## 5. 이전 거짓 보고 가능성

### 5-A. 사이클 52~54 감사 보고서 (00 감사팀 본인 산출)

```
00_HAIST_WORKS_감사팀/_TO_09팀장_2026-04-27_사이클52_감사.md:135:
| 4 | v2 본체 / 외부자산 / 핫패치 손상 0 | PASS (E 검증과 동일, 130 매칭) |

00_HAIST_WORKS_감사팀/_TO_09팀장_2026-04-27_사이클53_감사.md:71:
- 핫패치 보존, v2 본체 미접촉, 외부 자산 0 — 대표 하달 원칙 모두 유지
00_HAIST_WORKS_감사팀/_TO_09팀장_2026-04-27_사이클53_감사.md:83:
- H (외부 자산 0): main.py 새 import = `from .database import recommend_reorders ...` — **내부 모듈만**, 외부 라이브러리 0건

00_HAIST_WORKS_감사팀/_TO_09팀장_2026-04-27_사이클54_감사.md:80:
### v3-4. v2 본체 / 외부자산 / 핫패치 손상 0 (F 검증) — **PASS**
00_HAIST_WORKS_감사팀/_TO_09팀장_2026-04-27_사이클54_감사.md:83:
- 외부자산 0건 확인
```

**거짓 가능성 평가**: 사이클 52~54 보고서가 "외부 자산 0건"을 적은 의미는 통상 "**해당 사이클이 추가한 외부 자산 0건**"으로 좁게 해석 가능. 그러나:
1. 오리엔테이션 1항은 "외부 자산 0**건**" 절대치 — 신규/누적 구분 없음
2. 사이클 53 본문 "main.py 새 import = ..., 외부 라이브러리 0건" 은 새 import 만 본 협소 검증
3. 누적 존재량을 명시하지 않은 채 "**원칙 모두 유지**" 라고 적은 것은 정직성 v3 위반 (대표는 절대치로 이해)

→ **부분 거짓** (의도적 은폐 아니더라도 결과적으로 대표 오인 유발).

### 5-B. A37 보고서 (2026-04-26)

```
00_HAIST_WORKS_감사팀/_TO_09팀장_2026-04-26_A37_주간보고2차_감사.md:17:
| 3 | 외부 자산 0건 (csv 표준만) | csv·io 표준 라이브러리만 |
`import csv as _csv` (L2106) 표준만, `pandas`/`xlsxwriter`/외부 차트 라이브러리 0 (grep 0건) | ✅ 정합 |
```

**거짓 확정**: 같은 시점 `database.py:2090 import pandas as pd` 와 `database.py:2257 from openpyxl` 이 코드에 **존재**. 본 보고서는 주간보고 라우트만 협소 grep한 후 "pandas grep 0건" 이라고 적었으나 코드베이스 절대치는 거짓.

---

## 6. 조치 권고

### A안 (권고): openpyxl + pandas 즉시 비활성화 + deep_translator 보고

**즉시(오늘):**
1. **`/export/weekly` 라우트** → CSV 출력 + HTML 인쇄 view 2종 병행으로 대체 (openpyxl 0)
2. **`build_password_xlsx`** → CSV (관리자 1회성, 비밀번호는 첫 로그인 즉시 변경 안내 유지)
3. **`parse_mgmt_xls`** → 1회성 마이그레이션은 외부 환경에서 CSV 변환 후 stdlib csv 모듈로 처리. 운영 라우트에서 import 제거
4. **`deep_translator`** → 운영 사용 여부 즉시 결재 대기 (P11 베트남 수출 실무자 영향 분석 후 결정). 기본 입장은 **외부 API 0** 정책에 따라 비활성화 + 사내 i18n 사전 강화

**산출물:**
- `01_HAIST_WORKS/requirements.txt` → `fastapi / uvicorn[standard] / jinja2 / python-multipart / itsdangerous` 5건만 (deep-translator·openpyxl 제거)
- `_DEPLOY_배포_가이드.md` 갱신
- 사이클 56을 "외부 자산 청소" 로 확정

### B안: 대표 명시 승인 받고 예외 인정 + 정책 v4

- 대표가 "openpyxl은 운영상 필수, 예외 허용" 명시 결재 시 → 정책 v4 발행 (`외부 자산 0건` 을 `명시 화이트리스트 N건`으로 변경)
- pandas/deep_translator는 별도 결재 (각 항목 독립)

### C안 (비권고): 현상 유지 + 정직성만 강화

- 그간 보고서 표현을 "**신규** 외부 자산 0건" 으로 정정
- 누적 외부 자산은 별표 주석 (문제는 오리엔테이션·v3정책 본문이 절대치 — C안은 사실상 정책 우회. 비권고)

**감사관 권고**: **A안** + **99 AUDIT 발사** (사이클 52·53·54·A37 보고서 거짓 가능성 + 도입 시점 commit 546fb23 부터 누적 위반).

---

## 7. 99 AUDIT 발사 권고 사유

1. 외부 자산 3종(openpyxl·pandas·deep_translator) 누적 위반 + requirements.txt 명시 2종
2. 사이클 52~54 + A37 감사 보고서가 "외부 자산 0건 PASS" 표현 → 절대치 기준으로 거짓 가능
3. pandas는 requirements.txt **미명시** 상태로 import — 정직성 v3의 "임의 추정·기억 인용 0건" 본문을 깨는 패턴
4. v3 위반 0건 정책 "위반·실수는 절대 안 된다" 의 **명시 위반**
5. 도입 시점이 사이클 0(초기 구축) — 누적 12일·55사이클 동안 검출되지 않음 → 감사 시스템 자체 사각 점검 필요

---

## 8. 자체 검증 (정직성 v3)

- [x] 모든 line 인용은 grep -n 결과 라인번호 직접 인용
- [x] requirements.txt 전문 7라인 인용
- [x] git log -S 결과 6건 직접 인용
- [x] 추정/기억 0건 — 모두 grep·git·Read 도구 결과 기반
- [x] 본 감사관 자체 산출(사이클 52~54 보고서) 의 거짓 가능성도 자체 명시

## 9. 본 감사 행위 자체의 제약 준수

- [x] 코드 수정 0건 (Read/Grep만)
- [x] BAT 갱신 무관 (감사팀 산출)
- [x] 09 팀장 채널만 — 99 발사는 09 팀장 권한
- [x] 본 보고서 위치: `00_HAIST_WORKS_감사팀/_TO_09팀장_2026-04-27_긴급감사_openpyxl외부자산.md`

---

**감사관 서명**: 00 감사팀 (긴급 감사, 2026-04-27)
**다음 단계**: 09 팀장 → 99 DISPATCH (대표 결재) → A/B/C안 중 채택 → 사이클 56 적용
