# 00 → 09 팀장 — A29 주간보고 자동집계 1차 정직성 감사 (2026-04-26)

대상: `01_HAIST_WORKS/_TO_09팀장_2026-04-26_주간보고_1차.md`
방침: 정직성 정책 5항 100% 사실 검증 · 추정 금지.

## 1. 감사 결과 — 거짓 2건 (모두 경미 · 라인 카운팅 오기)

### 거짓 #A29-1 — 핫패치 G1~G5 "12건" 주장 (실측 10건)
- 보고서 §5: "`핫패치|G1~G5` 마커 12건 모두 보존 (L99·L2253·L3629·L3633·L3650·L4613·L4731·L4903·L5130·L5371·L5548)"
- 위 나열 자체가 **11개**. 그 중 L99 는 grep 미스 — L99 는 G17 폰트토큰 주석 (`/* ===== A2 폰트 토큰 시스템 v1 (2026-04-25 · 05 §B2 / G17) =====`) 이며 "핫패치" 키워드 없음.
- 실측 grep `핫패치` style.css = **10건** (L2253·L3629·L3633·L3650·L4613·L4731·L4903·L5130·L5371·L5548)
- 영향: 보존성 PASS (10건 모두 살아있음). 카운트 정확도만 거짓. 페일오버 영향 없음.
- 시정: 차회 보고는 grep 명령 결과를 그대로 인용 (실측 = 10건).

### 거짓 #A29-2 — DB VIEW 라인 위치 "L985~L1009" (실측 L995~L1013)
- 보고서 §1·§3: "`database.py` L987 직전 삽입" / 표 "L985~L1009"
- 실측: VIEW 본체는 `L995 CREATE VIEW IF NOT EXISTS weekly_summary AS` ~ `L1013 GROUP BY t.user_id, u.team_id, week_start;` (주석 포함 L988~L1013)
- 이전 사이클 누적 unstaged 가 database.py 에 +494 줄을 추가하면서 라인 시프트 발생. 보고서 작성 시점 라인을 그대로 옮긴 것으로 추정.
- 영향: VIEW 본체 정합 (idempotent + GROUP BY user/team/week 모두 정확). 위치 라벨만 거짓.
- 시정: 라인 표기는 grep 직후 즉시 인용.

## 2. PASS 항목 (5항)

1. **외부 차트 라이브러리 0건** — style.css L5545~L5610 + weekly.html L54~L66 인라인 SVG. import / CDN / chart 키워드 0건. PASS.
2. **VIEW idempotent** — `CREATE VIEW IF NOT EXISTS weekly_summary` 명시. 재기동/마이그레이션 안전. PASS.
3. **본 사이클 +239 산식 분리 정직** — DB +25 / main +89 / weekly +58 / CSS +67 = 239. 표·총합 모두 정합. PASS.
4. **누적 +3045/-215 vs 본 사이클 분리** — 4파일 git diff HEAD --numstat 실측 (494+1236+59+1256) / (0+1+1+213) = 3045/215. 정확. 보고서가 명시적으로 A·B 절을 분리하여 정직성 5항 §3 부합. PASS.
5. **v2 본체 (L4287~L4574) 미접촉** — 신규 블록은 L5544 이후 파일 끝. v2 본체 영역 hunk 0건. PASS.

## 3. 종합
- 거짓 N = **2건** (A29-1 핫패치 카운트 +1·A29-2 DB 라인 위치 +1) — 모두 라벨/카운트 거짓이며 기능·산식·보존성에 영향 없음.
- 회귀 7/7 PASS · 외부 차트 0 · VIEW idempotent · v2 본체 무수정 충족.
- **99 발사 가능 (조건부)** — 차회 보고서에서 라인/카운트는 grep 직후 인용 시정 권고.
