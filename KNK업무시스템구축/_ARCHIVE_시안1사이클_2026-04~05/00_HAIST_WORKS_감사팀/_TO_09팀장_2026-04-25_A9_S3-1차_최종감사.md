# [TO 09 팀장] A9 — Top3 S3 1차 최종 감사 (시안 정합성 + 권한 분기 정직성)

- 발신: 00 감사팀 빅터 / 수신: 09 팀장 / 일자: 2026-04-25
- 대상: 01 `_TO_09팀장_2026-04-25_Top3-S3-1차_DB스키마.md` + 04 V9
- 원칙: 사실검증 100% · 추정 0 · grep + Read 실측

---

## 1. 시안 §데이터모델 vs 실제 컬럼 일치 — **거짓 1건 (경미·문서급)**

### 1-1. 테이블 명칭 대응 (시안 7테이블 → 실제 6테이블, 패턴 차용)

시안 §7 7테이블: `users · roles · permissions · role_permissions · user_roles · delegations · permission_audit_log`
실제 6테이블: `permissions · permission_groups · group_permissions · user_groups · delegation_tokens · delegation_audit`

- 01 회신 §1 표는 시안의 **R3 §2 "Group Inheritance + Delegation Token" 패턴**에 맞춰 `roles`→`permission_groups`, `role_permissions`→`group_permissions`, `user_roles`→`user_groups`, `delegations`→`delegation_tokens`, `permission_audit_log`→`delegation_audit` 으로 재명명.
- `users` 테이블은 기존 v2 사용자 테이블 재사용(중복 미생성)으로 합리적.
- 패턴 동일성 측면 PASS — **명칭 차이는 추후 시안 명세서와 정합 표 1매 추가 필요**.

### 1-2. **거짓 1건: `permissions` 컬럼 시안 미일치**

| 영역 | 시안 §7 (R3 §3 인용) | 실제 `database.py:617~622` |
|---|---|---|
| 컬럼 | `id, resource, action(read/write/approve)` | `id, name UNIQUE, scope, created_at` |

- 01 회신 §1 표는 `name(UNIQUE)/scope/created_at` 라고 자체 기술하여 **자기 일관**하나, **시안 §7 원안의 `resource·action(read/write/approve)` 분리 컬럼 명세와 다름** ('stock.write' 단일 문자열로 합쳐짐).
- 영향도: **낮음** (1차 골격, 실제 토큰 발행 로직은 2차 사이클). 단 §7 행위 단위 RBAC(read/write/approve) 분리가 2차 UI 체크박스 §1-A 5필드에 명시되어 있어 **2차 구현 전 컬럼 분리 또는 시안 보정 결정 필요**.
- 분류: **거짓-경미·문서급** (구현 진행 시 보정 가능).

### 1-3. 그 외 컬럼 — 시안 인용 일치

- `delegation_tokens.expires_at · can_redelegate · status(ACTIVE/EXPIRED/REVOKED)` ✅ 시안 §3·§7 enum 일치.
- `delegation_audit.action(GRANT/DELEGATE/REVOKE/EXPIRE) · append-only` ✅ 시안 §6-2 일치.

---

## 2. 권한 분기 정직성 — **PASS** (평직원·팀장 진짜 차단)

`main.py:180~186` `require()` 정의 + 6 라우트 본문 grep:
- 6/6 `require(req, ["admin","ceo"])` + `if not u` 실패시 redirect/401.
- `roles` 화이트리스트 enum 일치 검사 → `member·leader·executive` 모두 거부.
- 분기 우회 백도어 없음 (다른 admin/permissions* 라우트 grep 0건).

---

## 3. 외부 상표권·라이선스 비침해 — **PASS**

- 4 템플릿 본문 외부 브랜드명·로고·SVG 0건.
- 시안 자체검증 §9 [x] 외부 자산 0건과 일치.
- 색상 토큰 `var(--knk-red)` = 사내 브랜드만 사용.

---

## 4. v2 본체 미접촉 — **PASS**

- `style.css:4269 / 4574` 본체 코드 영역 v2 그대로 존재.
- 신규 코드 위치: `database.py 615~670` + `main.py 4544~4595` (전부 파일 끝부분 append).
- 핫패치 G1~G4 (`style.css:3627·3639 + base.html:94`) 3/3 보존.

---

## 5. 종합 판정

| 항목 | 결과 |
|---|---|
| 거짓 N건 | **1건** (경미·문서급, `permissions` 컬럼 시안 §7 원안 미준수 — 2차 사이클 보정 가능) |
| 권한 분기 정직성 | PASS |
| 외부 자산·브랜드 | 0건 |
| v2 본체 미접촉 | PASS |
| 핫패치 보존 | PASS |

**99 발사 가능** (1건은 S3 2차 컬럼 분리 또는 시안 §7 표 보정 시 자동 해결).
