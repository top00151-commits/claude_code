# [TO 09 팀장] A15 — Top3 S3 2차 최종 감사 (거짓 감사)

- 발신: 00 감사팀 / 수신: 09 팀장 / 일자: 2026-04-25
- 감사 대상: 01 회신 `_TO_09팀장_2026-04-25_Top3-S3-2차_UI토큰RBAC.md` + 04 V15 회귀
- 원칙: 사실 검증 100% · 추정 금지 · 불일치 시 재검증 지시

---

## 1. 거짓 감사 — 0건

| 항목 | 01 회신 주장 | 실측 | 판정 |
|---|---|---|---|
| permissions SCHEMA 주석 (DEPRECATED) | database.py:617~626 | 실측 615~626 / 623 line `[DEPRECATED 2026-04-25]` 명시 | 사실 |
| ALTER ADD 가드 3건 | init_db() 내 `if "resource"/"action"/"description" not in pcols` | 실측 1158·1160·1162 — 가드 3건 모두 존재 | 사실 |
| UNIQUE INDEX uq_perm_ras 부분 인덱스 | `(resource, action, scope)` NULL 제외 | 실측 1165~1169 — `WHERE resource IS NOT NULL AND action IS NOT NULL` | 사실 |
| 옵션 B 채택 사유 (호환성) | 1차 환경에서 ALTER ADD COLUMN | 실측 — DROP+재생성 흔적 0건 / name 컬럼 잔존 | 사실 |
| UI 4 템플릿 신규 본문 | admin_permissions[/grant/revoke/audit].html | 실측 4 파일 모두 존재 — 64/95+/55/64+ 줄 | 사실 |
| 3 배지 강제 노출 (⏱/🔒/📜) | 모든 위임 카드 | 실측 admin_permissions.html — 6건 (카드 2장 × 3) | 사실 |
| 핸들러 grant 라우트 4562 | INSERT permissions+tokens+audit 단일 트랜잭션 | 실측 4562~4614 — `with db_session()` 단일 블록 / 3 INSERT | 사실 |
| 핸들러 revoke 라우트 4626 | UPDATE + Cascade + audit | 실측 4626~4664 — confirm 강제(4641) + Cascade 루프(4658~4663) | 사실 |
| 핸들러 audit 라우트 4667 | JOIN 3 테이블 + 필터 | 실측 4667~4696 — LEFT JOIN users/tokens/permissions + WHERE da.action 필터 | 사실 |
| 핫패치 G1~G4 + #2 보존 | 3 라인 보존 | 실측 style.css 3627·3648 + base.html 94 — 3/3 보존 | 사실 |
| v2 본체 미접촉 | SCHEMA 주석 + 마이그레이션 1블록만 | 실측 — database.py 변경 = 615~626 + 1153~1171 (2 블록) / main.py 변경 = 4544~4696 (S3 2차 묶음) | 사실 |
| BAT 갱신 양쪽 | KNK_시작.bat + START.bat = "2026-04-25 Top3-S3-2차" | 실측 — Top3-S3-2차 grep 4 파일 (2 BAT + 1 회신 + database.py 주석) | 사실 |

거짓 감사 결과: **0건**.

---

## 2. 정직성 검증

### 2-1. 시안 §데이터모델 vs RBAC 분리
- 시안 §7 원안: `permissions(id, resource, action, scope, description) UNIQUE(resource, action, scope)`
- 실제: name(DEPRECATED 유지) + resource/action/description 추가 + UNIQUE INDEX(부분)
- 옵션 B 명시 — 1차 골격 호환성 사유 정직히 기록. 정직성 PASS.

### 2-2. audit 누락 0건 (단일 db_session 트랜잭션)
- grant: permissions INSERT → tokens INSERT → audit INSERT — 3 모두 동일 with 블록
- revoke: 본 토큰 UPDATE + audit + Cascade 루프(각 UPDATE+audit 쌍) — 단일 with 블록
- with 블록 종료 시 commit. 예외 시 rollback. audit 누락 경로 없음. 정직성 PASS.

### 2-3. Deny overrides Allow 시안 인용
- admin_permissions.html:60~63 — `DENY 확인 → ALLOW 확인 → 기본 거부 (시안 §4, R3 §2 인용)` 명시. PASS.

### 2-4. Cascade 하위 토큰 처리
- main.py 4654: `WHERE from_user=? AND status='ACTIVE'` — 본 토큰 수령자가 재위임한 ACTIVE 하위만 회수
- 각 자식마다 audit 1행 (`Cascade 회수 (parent_token_id=...)`) — 추적성 정직 PASS.

### 2-5. 외부 자산·브랜드 0건
- 4 템플릿 + main.py 신규 라우트 — 외부 라이브러리/브랜드 import 0건. 5 체크리스트 PASS.

---

## 3. TIER 분류 권고

| 단계 | 회신 | 감사 | TIER |
|---|---|---|---|
| S1 1차/2차 | A11/A12 | 거짓 0 | A |
| S2 1차/2차 | A10/A14 | 거짓 0 | A |
| S3 1차/2차 | — / **A15** | 거짓 0 | A |

Top3 6/6 완결 — TIER A 일관 유지.

---

## 4. 결론

01 회신 12 항목 전부 사실. 거짓 감사 **0건**. 트랜잭션 무결성·정직성 4축(데이터모델/audit/Deny/Cascade) 통과. **99 발사 가능** + Top3 TIER A 권고.
