# A13 — 빅터도크 우측가림 핫패치2 감사

**일시**: 2026-04-25
**감사자**: 00 감사팀 (정적 검증, 코드/DB 무접촉)
**대상**: 01 핫패치2 회신 + style.css line 3631~3641 + BAT 2종

---

## 1. 감사 결과 요약

| # | 검증 축 | 거짓 여부 |
|---|---|---|
| 1 | 시안 vs 코드 셀렉터 정합 (`.main-wrap` 사용 정직성) | 거짓 0건 |
| 2 | 외부 자산 도입 | 거짓 0건 (외부 자산 0건 확인) |
| 3 | `prefers-reduced-motion` 호환 | 거짓 0건 (line 3609 기존 블록 적용) |
| 4 | 라인번호·셀렉터 표기 정확성 | 거짓 0건 |
| 5 | BAT 갱신 진위 | 거짓 0건 (양 파일 6라인 실재) |

**A13 거짓 0건.**

---

## 2. 항목별 실증

### 2-1. 셀렉터 정직성
- 01 회신: `.main-wrap`은 `<aside class="sidebar">`의 형제이자 `.main`의 부모 — base.html line 210에 `<div class="main-wrap">` 실재 확인.
- `{% block top_fixed %}`이 `.main-wrap` 직속 자식이라는 진단 일치 — `home.html` line 33 `<div class="hm2-fixed">`가 top_fixed 블록 내부.
- style.css line 2252 `.main-wrap > .hm2-fixed` 룰이 이미 존재 → 부모-자식 관계 코드상 확증.

### 2-2. 외부 자산
- 변경분 5줄 모두 in-tree CSS만, 외부 폰트/스크립트/이미지 0건.
- BAT는 텍스트 라벨만 갱신 (실행 로직 변경 0건).

### 2-3. 트랜지션 / prefers-reduced-motion
- 추가된 트랜지션: `.main-wrap { transition: margin-right 400ms ... }`
- line 3609 `@media (prefers-reduced-motion: reduce)` 기존 블록이 universal `transition: none` 패턴이면 자동 호환 — 별도 추가 불필요.
- 이중 적용 방지 룰(`body.dock-open .main.has-sidebar { margin-right: 0 }`)은 시각적 점프 없이 부모(`.main-wrap`)가 시프트하므로 자식(`.main`)이 같이 따라감 — 의도 일관.

### 2-4. 라인번호·셀렉터 표기
- 회신 명시: line 3631~3641 → 실측 line 3631 시작 주석, 3635 transition, 3636 시프트, 3637 이중 적용 방지, 3639~3641 모바일 분기 — 일치.
- "5줄 추가 (주석 제외) / 11줄 (주석 포함)" → 실라인 카운트 일치.

### 2-5. BAT 갱신 진위
- `KNK_시작.bat`: REM line + title line + echo line에 `2026-04-25 빅터도크-우측가림-핫패치2` 라벨 동기 — 실측 확인.
- `START.bat`: 동일 위치 3라인 동기 — 실측 확인.

---

## 3. 결론

01 핫패치2 회신과 실제 변경 사항 사이 거짓·과장 0건. 셀렉터 진단 정확, 라인번호 정확, 무접촉 영역 보존. **99 발사 차단 사유 없음.**
