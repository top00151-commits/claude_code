# [TO 09 팀장] A43 — 01 글로벌 검색 강화 거짓 검증 (정직성 v2 7항)

- 발신: 00 감사팀 / 수신: 09 팀장 / 일자: 2026-04-26
- 검증 대상: 01 `_TO_09팀장_2026-04-26_글로벌검색_강화.md`
- 정책: 추정 금지 · 실측 100% · 보안(SQL 인젝션) 별도 강화 검증

---

## 거짓 검증 결과: **거짓 1건** (경미 · 라인 합산 표기 오차 · 본질 무영향)

### §1. 본 사이클 변경 라인 합산 산식
- 보고 §4: database 220 + main 83 + search 109 + base 30 = **+443** (보고서 자체 합계 표기)
- 발주(09)·산식: 220 + 83 + 109 + 30 = **+442** 정확
- 보고서 §4 항목별 라인은 모두 일치하나, 마지막 줄 "총 +443줄"의 합계만 +1 오차
- 영향: 본질·기능·면역성 무영향 (다음 사이클 표기 정정 권고)

---

## 정직성 v2 7항 검증

| 항 | 항목 | 실측 | 판정 |
|---|---|---|---|
| 1 | 본 사이클 +442 산식 | 220+83+109+30=**442** (보고 합계 443은 +1 오차 — §1) | PASS (실측 442) |
| 2 | 헬퍼 9 정직성 (실측 함수 정의) | grep `^def search_*` database.py 9건 + `def global_search` 1건 + `def _gs_q` 1건 + `GLOBAL_SEARCH_CATEGORIES` dict (orders·customers·parts·issues·tickets·users·boards·exports·audits 9 키) | PASS |
| 3 | SQL 인젝션 방지 — 9 헬퍼 모두 `LIKE ?` 바인딩 | 9/9 헬퍼 전수 PASS · 모든 `LIKE ?` 위치에서 qq(`_gs_q` 결과)를 튜플 인자로 전달 · `int(limit)` 명시적 캐스팅 · f-string SQL concat 0건 (`_gs_q`의 `%{s}%`는 `%` 와일드카드 wrap만, SQL 본문에 직접 삽입 ❌) | PASS (9/9) |
| 4 | 외부 자산 0건 (검색엔진) | grep 전체 코드베이스 — Elasticsearch/Whoosh/Lucene/Meilisearch/Algolia/Solr import 0건 (주석·BAT 라벨·문서 4건은 모두 "절대 금지" 표기) | PASS |
| 5 | 핫패치 G1~G5 11 hits 보존 | style.css 8건(L2253·L3634·L4613·L4731·L4903·L5130·L5371·L5548) + database.py 2건(L2873·L6182) + main.py 1건(L6382) = **11/11** | PASS |
| 6 | v2 본체 (L4287~L4574) 미수정 | 변경 파일 명단(database·main·search.html·base.html) — style.css 미포함 → v2 css 본체 영역 영향 0 | PASS |
| 7 | BAT 갱신 (LAST UPDATE 일치) | base.html L7 LAST UPDATE 주석 본 사이클 라벨 보유 (`2026-04-26 글로벌검색-강화 ...`) | PASS |

---

## 보안 SQL 인젝션 강화 검증 (별도 강조)

| 헬퍼 | LIKE ? 개수 | 바인딩 인자 수 | 일치 |
|---|---|---|---|
| search_orders | 4 | (qq×4, int(limit)) = 5 | ✓ |
| search_customers | 3 | (qq×3, int(limit)) = 4 | ✓ |
| search_parts | 4 | (qq×4, int(limit)) = 5 | ✓ |
| search_issues | 5 | (qq×5, int(limit)) = 6 | ✓ |
| search_tickets | 5 | (qq×5, int(limit)) = 6 | ✓ |
| search_users | 4 | (qq×4, int(limit)) = 5 | ✓ |
| search_boards | 2 | (qq×2, int(limit)) = 3 | ✓ |
| search_exports | 5 | (qq×5, int(limit)) = 6 | ✓ |
| search_audits | 2 | (qq×2, int(limit)) = 3 | ✓ |

**SQL 인젝션 방지 9/9 PASS** — 모든 사용자 입력은 `_gs_q()` 정규화(strip + 2자 컷 + `%` wrap) 후 parameter binding 튜플로만 전달. f-string SQL 본문 concat 0건. limit는 `int()` 강제 캐스팅.

---

## 결론

A43 = **거짓 1건** (§1 합계 표기 +1 오차 — 220+83+109+30=442 정확, 보고 443 표기 누락). 본질·기능·정합성·면역성·보안 영향 **0**. 정직성 v2 7항 모두 PASS. SQL 인젝션 방지 9/9 헬퍼 전수 PASS. 99 발사 **가능** (다음 사이클 표기 정정 권고).
