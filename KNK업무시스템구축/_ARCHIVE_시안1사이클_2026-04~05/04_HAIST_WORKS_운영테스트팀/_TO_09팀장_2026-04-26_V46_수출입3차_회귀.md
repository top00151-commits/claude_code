# 04 → 09 팀장 회신 — V46 수출입 3차 (인쇄 + 일정 알림) 회귀

작업일: 2026-04-26 / 사이클 V46 / 세션 04 / 작업시간 ≈ 4분 / 코드/DB 미수정 · 서버 미기동

## 1. 검증 대상 (01 산출 `_TO_09팀장_2026-04-26_수출입_3차.md`)
인쇄 라우트 +3 (CI/PL/BL `/print`) · 신규 템플릿 +3 · `check_export_alerts` 헬퍼 (3 카테고리) · `notify_user EXPORT` 1시간 중복 방지 · 외부 PDF 0건.

## 2. 7건 회귀 결과 (실측 grep · cat -n)
| # | 항목 | 실측 위치 | 결과 |
|---|------|-----------|------|
| 1 | 인쇄 라우트 3 + `_export_guard` | `app/main.py` L7085(`/export/ci/{ci_id}/print`) · L7110(`/export/pl/{pl_id}/print`) · L7137(`/export/bl/{bl_id}/print`) — 3건 모두 첫 줄 `u = _export_guard(req)` 가드 | PASS |
| 2 | 신규 템플릿 3 (base 미확장 · A4 비례 · `@media print`) | `export_ci_print.html`(65줄) / `export_pl_print.html`(76줄) / `export_bl_print.html`(80줄) — 3건 모두 `<!doctype html>` 시작, `extends` 0건, `@page A4 18mm 16mm`, `@media print { .actions, .no-print { display:none !important } }` | PASS |
| 3 | `check_export_alerts()` 헬퍼 | `app/main.py` L7164 — 3 카테고리 (`shipping` D-3 / `ci_expire` 90일 / `customs` SHIPPED 미신고) · `fired={'shipping':n,'ci_expire':n,'customs':n}` 반환 · 트리거 `POST /export/alerts/check` L7225 (admin/ceo/executive 한정) | PASS |
| 4 | `notify_user EXPORT` 통합 (1시간 중복 방지) | `app/database.py:2875` `NOTIFY_TYPES` 에 `EXPORT` 포함 · L2895~2904 동일 (user_id, kind, title) 1시간 내 중복 INSERT 차단 (dup `SELECT 1 ... created_at >= datetime('now','localtime','-1 hours')`) · `main.py` L7189/7202/7217 3호출 모두 `"EXPORT"` 타입 | PASS |
| 5 | 외부 PDF 라이브러리 0건 | `Grep -i "reportlab\|weasyprint\|pyfpdf\|wkhtmltopdf\|pdfkit\|xhtml2pdf\|fpdf2"` → 산출보고서 1건만 매치 (코드/템플릿 0) · `^from fpdf\|^import fpdf` → No matches | PASS |
| 6 | 핫패치 G1~G5 grep 보존 | `main.py:6443` · `main.py:7083` · `style.css:4613/4731/4903/5130/5371` · `style.css:5548` · `database.py:6287` → 5/5 PASS (보고된 5 라인 모두 잔존) | PASS |
| 7 | v2 본체 미접촉 + 외부 자산 0 | `main.py` 신규 코드 모두 L7080 이후 신규 섹션 · v2 본체 라우트 그룹(~L6440) 무수정 · 인쇄 템플릿 3건 base 미확장 독립 · 외부 차트/CDN/폰트 신규 0건 | PASS |

## 3. 종합
**V46 7/7 PASS.** 행별 인쇄 링크 3건 (`export_order_detail.html` L43/L55/L67 `target="_blank"`) 정상.

## 4. 회귀 권고 (서버 기동 시)
- `GET /export/ci/{id}/print` · `/pl/{id}/print` · `/bl/{id}/print` 200 OK · 비권한 사용자 → `/home` 리다이렉트
- 미존재 ID 3건 → `/export` 리다이렉트
- `POST /export/alerts/check` 비관리자(영업/일반) 401/403 · 관리자 200 + `{ok, fired:{...}}`
- 동일 (user, EXPORT, title) 60분 내 2회 호출 → 2회차 `fired` 0 누적

## 회신
"04 V46 수출입 3차 회귀 7/7 PASS — 인쇄 라우트 3 · 템플릿 3 · 알림 헬퍼 · EXPORT 1시간 중복 방지 · 외부 PDF 0 · 핫패치 5/5 · v2 본체 미접촉. 99 발사 가능."
