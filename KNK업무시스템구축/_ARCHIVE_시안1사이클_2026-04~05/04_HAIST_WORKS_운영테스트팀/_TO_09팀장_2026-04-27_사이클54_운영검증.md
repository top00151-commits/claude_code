# [04 운영테스트팀 → 09 팀장] 사이클 54 (환율·단가 1차) 운영 검증

> 작성: 04 운영테스트팀 빅터 · 일자: 2026-04-27
> 대상: 01 사이클 54 산출 (database.py 헬퍼 +3 / main.py 라우트 +4 / 템플릿 +2)
> 임무: 코드 수정 0 — 정적 검증 + 페르소나 시뮬

---

## 0. 요약 (TL;DR)

| 항목 | 결과 |
|---|---|
| 회귀 (라우트 병존) | **PASS** (URL 패턴 충돌 0) |
| 통합 결정 합리성 | **CONDITIONAL PASS** — 컬럼명 정합 OK, 그러나 **신규 `/fx/rates` 가 기존 `/rates` 와 100% 중복 라우트** (S2-1 신규) |
| 권한 가드 | **PASS** — `_fx_guard` 4/4 라우트 적용 |
| SQL 인젝션 | **PASS** — 파라미터 바인딩 전부 ? |
| 외부 자산 0건 | **PASS** — fx_rates/part_prices 둘 다 외부 URL 0건 |
| 핫패치 130 (G1~G4) | **PASS 4/4** (5/34/2/1) |
| v2 본체 미접촉 | **PASS** — base.html 에 fx_rates/part_prices 참조 0 |
| Jinja 균형 | **PASS** — extends/block/if/for 양쪽 카운트 일치 |
| 페르소나 finance/sales/일반 | **3/3 통과** (가드 로직 정합) |
| **신규 발견** | **S2-1 (라우트 중복) · S3-1 (사이드바 노출 누락) · S4-1 (페이로드 비대칭)** |

**최우선 사안**: 신규 `/fx/rates` 와 기존 `/rates` 의 일원화 결정 필요 — 09 팀장 결재 요청.

---

## 1. 회귀 검증 (라우트 병존)

### 1-1. `/fx/rates` vs `/rates` 충돌 여부 → **충돌 없음 (라우팅 레벨)**

| 메서드·경로 | 처리 라우트 (line) | 결과 |
|---|---|---|
| GET `/rates` | `rates_page` (4704) | OK — 기존 |
| GET `/rates/dashboard` | `rates_dashboard` (8365) | OK — 기존 |
| GET `/rates/cost-sim/{part_id}` | (8453) | OK |
| GET `/rates/price-history/{part_id}` | (8504) | OK |
| GET `/rates/alerts` | (8517) | OK |
| GET `/fx/rates` | `fx_rates_page` (8641) | OK — 신규 |
| POST `/rates/new` | (4717) | OK — 기존 |
| POST `/rates/upload` | (8386) | OK |
| POST `/rates/cost-sim` | (8469) | OK |
| POST `/rates/alerts` | (8528) | OK |
| POST `/fx/rates` | `fx_rates_create` (8655) | OK — 신규 |

→ FastAPI 는 `/fx/rates` 와 `/rates` 를 별도 prefix 로 인식하므로 **prefix 매칭 충돌 0**. 그러나 **2-1 (통합 결정 합리성)** 에서 별도 지적.

### 1-2. `/parts/{pid}/prices/new` vs `/parts/{part_id}/prices` → **충돌 없음**

| 메서드·경로 | 처리 라우트 (line) | 결과 |
|---|---|---|
| POST `/parts/{pid}/prices/new` (기존) | (4751) | OK — `/prices/new` 세그먼트 |
| GET `/parts/{part_id}/prices` (신규) | (8685) | OK — `/prices` 세그먼트 |
| POST `/parts/{part_id}/prices` (신규) | (8710) | OK — `/prices` 세그먼트 |
| GET `/parts/{pid}` (기존, line 4812) | OK | OK — `/parts/3/prices` 는 `/prices` 추가 세그먼트로 8685 가 매칭 |

→ FastAPI 는 정확히 일치하는 path 우선 매칭이므로 **라우팅 충돌 0**. 다만 **POST 두 개가 같은 도메인 (단가 등록) 을 다루며 페이로드가 다름** (S4-1).

### 1-3. 헬퍼 ↔ 스키마 정합 (가장 중요)

**`exchange_rates` 테이블 실제 컬럼** (database.py line 344~355):
- `rate_date` / `from_currency` / `to_currency` / `rate` / `source` / `note` / `created_by`

**발주서 명시 컬럼**: `currency_from / currency_to / rate / effective_date / source / entered_by`

**01 매핑 검증**:

| 발주서 | 실제 스키마 | 헬퍼 처리 | 결과 |
|---|---|---|---|
| `currency_from` | `from_currency` | 파이썬 인자명 `currency_from` → SQL `from_currency=?` (line 4754) | **PASS** (네이밍 변환만) |
| `currency_to` | `to_currency` | 파이썬 인자명 `currency_to` → SQL `to_currency=?` (line 4754) | **PASS** |
| `rate` | `rate` | 동일 | **PASS** |
| `effective_date` | `rate_date` | 헬퍼는 `as_of` 파라미터 (외부) → SQL `rate_date<=?` | **PASS** (의미적 정합) |
| `source` | `source` | 동일 | **PASS** |
| `entered_by` | `created_by` | `exchange_rate_create` 가 `user_id` → `created_by` 컬럼에 INSERT (line 4592) | **PASS** (의미적 정합) |

→ **발주서 의도 100% 충족**. 컬럼명만 한국 ERP 관례 (`rate_date`/`from_currency`) 로 다를 뿐, 의미는 1:1.

**`part_prices` 테이블 정합** (line 361~378): `part_id` / `supplier_id` / `price_type` / `unit_price` / `currency` / `effective_from` / `effective_to` / `negotiated_at` / `min_qty` / `max_qty` / `note` / `approved_by` / `approved_at` / `created_by`. 헬퍼 `get_latest_part_price` 는 `part_id`·`price_type`·`currency`·`effective_from<=?` AND `(effective_to IS NULL OR effective_to>=?)` 로 조회 — **스키마 정합 PASS**. price_type 매핑 (cost→확정 / list→가 / export→견적) 도 한국 ERP 표준 그대로.

---

## 2. 통합 결정 합리성

### 2-1. 기존 `exchange_rates` 발견 + 신규 `fx_rates` 0건 결정

발주서 조건 ("기존에 비슷한 게 있다면 grep 후 통합 검토") 에 **정당하게 부합**:
- `exchange_rates` 테이블 (line 344~355) 존재 + 인덱스 2개 (`idx_fx_date`, `idx_fx_from`) 운영 중
- 헬퍼 6개 (`exchange_rate_create / list / latest / latest_rate_to_krw` 등) 이미 구현
- 데이터 마이그레이션 0건으로 통합 → **테이블 신설 0건 결정 합리적**

**그러나** — **신규 `/fx/rates` 라우트와 기존 `/rates` 라우트가 100% 중복**:

| 항목 | 기존 `/rates` (4704) | 신규 `/fx/rates` (8641) |
|---|---|---|
| 템플릿 | `rates.html` (이미 존재) | `fx_rates.html` (신규) |
| 데이터 소스 | `exchange_rates_list` + `exchange_rates_latest` | **동일 함수** (`exchange_rates_list` + `exchange_rates_latest`) |
| 등록 페이로드 | `rate_date / from_currency / to_currency / rate / source / note` | **완전 동일** (단어 순서까지) |
| 차이점 | `can_use_logistics` 가드 + `check_rate_alerts` 호출 | `_fx_guard` (logistics 포함) — alert 호출 없음 |

→ **두 라우트의 사용자 가치 차이가 거의 없음. 운영 중 혼선 위험 高**. 09 팀장 결재 요청: "기존 `/rates` 를 alias 로 통일 / 신규 `/fx/rates` 를 메인으로" or "신규 `/fx/rates` 를 폐기하고 기존 `/rates` 에 `_fx_guard` 적용".

### 2-2. `/parts/{pid}/prices/new` 와 `/parts/{part_id}/prices` 페이로드 차이

**기존 `/parts/{pid}/prices/new` (4751) 페이로드**: `supplier_id / price_type / unit_price / currency / effective_from / effective_to / **negotiated_at** / **min_qty** / **max_qty** / note`

**신규 `/parts/{part_id}/prices` (8710) 페이로드**: `supplier_id / price_type / unit_price / currency / effective_from / effective_to / note` — **negotiated_at, min_qty, max_qty 누락** + price_change_history 자동 INSERT (`price_change_log`) 호출 누락.

→ 신규 라우트에서 등록 시 **수량 할인 (`min_qty`, `max_qty`)·협의일자·이력 변동률** 데이터 손실 가능 (S4-1).

---

## 3. 권한 가드 (`_fx_guard`)

`_fx_guard` (line 8628~8638):
1. `role in ("admin", "ceo", "executive")` → True
2. `team_id == 3` (관리팀=finance) → True
3. `can_use_logistics(user)` → True

**4개 라우트 적용 현황**:
- GET `/fx/rates` (8647) → `_fx_guard` 통과 못하면 `/home` 리다이렉트
- POST `/fx/rates` (8669) → 동일
- GET `/parts/{part_id}/prices` (8691) → `_fx_guard(u) or can_use_sales(u)` (영업팀도 단가 조회 허용)
- POST `/parts/{part_id}/prices` (8725) → 동일

→ **PASS 4/4**. 단가 등록은 sales 팀에도 열려있는데, 이는 견적·정가 입력자 (영업) 가 등록 후 finance 가 승인 (`part_price_approve`) 하는 한국 ERP 흐름과 정합.

---

## 4. SQL 인젝션 방지

| 라우트 | 사용자 입력 → SQL | 바인딩 |
|---|---|---|
| POST `/fx/rates` (8655) | `rate_date / from_currency / to_currency / rate / source / note` | 모두 `exchange_rate_create` (4592) 의 `?,?,?,?,?,?,?` 바인딩 — **PASS** |
| POST `/parts/{part_id}/prices` (8710) | `supplier_id / price_type / unit_price / currency / effective_from / effective_to / note` | 모두 `part_price_create` (4647) 의 `?,?,?,?,?,?,?,?,?,?,?,?` 바인딩 — **PASS** |
| 헬퍼 `get_latest_fx_rate` | `currency_from / currency_to / as_of` | (4752) 모두 `?` 바인딩 — **PASS** |
| 헬퍼 `get_latest_part_price` | `part_id / price_type / currency / as_of` | (4801) 모두 `?` 바인딩 — **PASS** |

→ **f-string SQL 0건. 사용자 입력 직접 SQL 삽입 0건.**

---

## 5. 페르소나 시뮬레이션

### 5-1. P3 (관리팀 finance, team_id=3)
- GET `/fx/rates` → `_fx_guard` 통과 (team_id==3) → 환율 목록 + 입력 폼 정상 노출 → **PASS**
- POST `/fx/rates` (USD→KRW=1380, 2026-04-27) → `exchange_rate_create` UPSERT → 같은 키 (rate_date, from, to) 재제출 시 ON CONFLICT UPDATE → **PASS**

### 5-2. CEO (role=ceo)
- GET `/parts/3/prices` → `_fx_guard(u)` 통과 (role==ceo) → 단가 이력 + 활성 단가 (오늘) + 최신 cost 표시 → **PASS**
- POST `/parts/3/prices` (확정/12500/KRW/2026-04-27) → `part_price_create` INSERT (UPDATE 아님 — 이력 보존) → **PASS**

### 5-3. P10 (영업팀 sales, team_id=2)
- GET `/parts/3/prices` → `_fx_guard` 실패 → `can_use_sales(u)` 검사 (team_id in (1,2,3)) → True → 단가 이력 조회 가능 → **PASS**
- 단가 등록은 견적 작업에 필요 (영업이 견적가 입력 → finance 가 확정) → 흐름 정합

### 5-4. 일반 사용자 (P1·P6 등 제조팀 외, role=member)
- GET `/fx/rates` → `_fx_guard` 실패 → `/home` 리다이렉트 → **PASS** (403 대신 303 redirect 정책)
- GET `/parts/3/prices` → `_fx_guard` 실패 + `can_use_sales` 실패 → `/home` 리다이렉트 → **PASS**

→ **3/3 통과**. 단, 비인가자 응답이 **403 이 아닌 303 Redirect** 인데 이는 사이트 전 라우트의 일관된 정책이므로 회귀 아님.

---

## 6. 외부 자산 / 핫패치 130 / v2 본체 (3종 grep)

| grep | 명령 | 결과 |
|---|---|---|
| 외부 URL | `https?://(?!localhost\|127\.0\.0\.1)` in fx_rates.html, part_prices.html | **0건 (PASS)** |
| 핫패치 G1 | `victorToggleDock\(` in base.html | **5 ≥ 2 (PASS)** |
| 핫패치 G2 | `\{\{\s*i\.[a-z_]+\s*\|\s*default\(` in base.html | **34 ≥ 30 (PASS)** |
| 핫패치 G3 | `\.sidebar\s*\{[^}]*height:\s*calc\(100vh` in style.css | **2 ≥ 1 (PASS)** |
| 핫패치 G4 | `id="victorTrigger"` in base.html | **1 ≥ 1 (PASS)** |
| v2 본체 미접촉 | `fx_rates\|part_prices\|fx/rates` in base.html | **0건 (PASS — 사이드바 미주입)** |

→ **5/5 PASS**. 단 v2 본체 PASS 의 부작용으로 **신규 라우트가 사이드바 메뉴에 미노출** (S3-1).

---

## 7. Jinja 템플릿 안전 (균형)

| 파일 | extends | block | endblock | if | endif | for | endfor |
|---|---|---|---|---|---|---|---|
| fx_rates.html | 1 | 2 | 2 | 7 | 7 | 5 | 5 |
| part_prices.html | 1 | 2 | 2 | 7 | 7 | 4 | 4 |

→ **균형 PASS 2/2**. 둘 다 `base_logi.html` 상속 (line 1) — 물류 전용 레이아웃 정합.

---

## 8. 신규 발견

### S2-1 (보통, 라우트 일원화 필요) — **09 팀장 결재 요청**
- **현상**: `/rates` (기존, logistics 가드) 와 `/fx/rates` (신규, _fx_guard 가드) 가 동일 데이터/페이로드 노출. 사용자 혼선 + 유지보수 비용 ↑.
- **원인**: 발주서가 신규 `/fx/rates` 명시했으나, 01 가 기존 `/rates` 발견 후 통합 검토를 "테이블만 통합" 으로 적용하고 라우트는 신설.
- **권고**: 두 안 중 1택을 09 팀장 결재
  - **A안**: 기존 `/rates` 를 메인으로 유지, `/fx/rates` 를 301 redirect 로 alias
  - **B안**: 신규 `/fx/rates` 를 메인으로 채택, `/rates` 의 `check_rate_alerts` 호출을 `/fx/rates` 에 이식 후 `/rates` 폐기 예고
- **재현**: 둘 다 GET 으로 띄워 비교 — 데이터·폼 100% 동일.

### S3-1 (작음, UX) — 사이드바 미노출
- **현상**: 신규 `/fx/rates` 와 `/parts/{part_id}/prices` 가 base.html 사이드바·도크 어디에도 링크 미주입.
- **영향**: 사용자가 URL 직접 입력해야만 접근. P3 (관리팀) 가 환율 등록 페이지를 못 찾을 가능성.
- **권고**: 사이클 54 2차에서 사이드바 i18n 키 + 링크 1줄 추가 (v2 본체 회귀 G2 +1).

### S4-1 (작음, 페이로드 비대칭) — 신규 단가 라우트의 데이터 손실
- **현상**: 신규 POST `/parts/{part_id}/prices` 가 `negotiated_at / min_qty / max_qty` 미수신 + `price_change_log` 미호출.
- **영향**: 기존 `/parts/{pid}/prices/new` 와 비교해 (1) 협의일자·수량할인 미저장 (2) 변동률 이력 (price_change_history) 미적재.
- **권고**: 사이클 54 2차에서 신규 라우트 폼·POST 핸들러에 3필드 + `price_change_log` 호출 추가 (또는 신규 POST 를 폐기하고 기존 POST 로 통합).

---

## 9. 비간섭 원칙 준수

- 본 검증은 정적 분석 + grep 전용
- 코드/DB/서버 미접촉 (수정 0건)
- 페르소나 시뮬은 가드 로직 추적으로만 수행 (HTTP 요청 미발송)
- 보고만 — 수정 권고는 09 팀장 → 01 으로 위임

---

*04 빅터 · 2026-04-27 · 사이클 54 1차 검증 완료 · S2-1 결재 요청 · 5팀 동시 가동 정책 준수*
