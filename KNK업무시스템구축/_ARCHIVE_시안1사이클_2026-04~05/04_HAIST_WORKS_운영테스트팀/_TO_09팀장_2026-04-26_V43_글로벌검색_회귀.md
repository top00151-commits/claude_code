# [TO 09 팀장] V43 — 글로벌 검색 강화 정적 회귀

- 발신: 04 운영테스트팀 / 수신: 09 팀장 / 일자: 2026-04-26
- 검증 대상: 01 `_TO_09팀장_2026-04-26_글로벌검색_강화.md` (헬퍼 +9 / 라우트 보강+신규 / UI 보강 2)
- 방식: 정적 grep + 파일 라인카운트 (서버 ❌ / 코드·DB 미수정)

---

## 검증 8건 — 8/8 PASS

| # | 항목 | 실측 결과 | 판정 |
|---|---|---|---|
| 1 | 검색 헬퍼 9 + global_search + _gs_q (database.py L6178~) | grep `^def search_*` 9건 (orders L6193 / customers L6214 / parts L6229 / issues L6247 / tickets L6267 / users L6287 / boards L6306 / exports L6326 / audits L6350) + `def global_search` L6382 + `def _gs_q` L6185 + `GLOBAL_SEARCH_CATEGORIES` dict L6369 (9 키) | PASS |
| 2 | GET /search 보강 + POST /search/suggest 신규 (main.py) | `@app.get("/search")` L1248 (`q`+`cat` 파라미터, `res_global` + `cat_labels` 컨텍스트), `@app.post("/search/suggest")` L1279 (debounce 부담 최소 5 카테고리×3건=15건 상한) | PASS |
| 3 | search.html 카테고리 탭 13개 + 결과 그룹 12종 | L21 `_cats` 튜플 13개(all+12 카테고리), L23 a 링크 루프; 결과 그룹 12종(local 3: tasks·comments·retros / global 9: orders·customers·parts·issues·tickets·boards·users·exports·audits) `{% if res_global and res_global.X %}` 9 블록 + 로컬 3 블록 = 12 | PASS |
| 4 | base.html 자동완성 drop+debounce 250ms | L61 `oninput="gSearchSuggest(this.value)"`, L65 `<div id="gSearchSugg">`, L74 `clearTimeout(_gsTmr)`, L75~90 `setTimeout(...,250)`, POST `/search/suggest` JSON, blur 200ms close | PASS |
| 5 | SQL 인젝션 방지 — 9 헬퍼 모두 parameter binding | grep `LIKE \?` 헬퍼 9개 모두 PASS — orders(L6206~6207, 4 ?) / customers(L6222, 3 ?) / parts(L6239~6240, 4 ?) / issues(L6257~6260, 5 ?) / tickets(L6277~6280, 5 ?) / users(L6298~6299, 4 ?) / boards(L6319, 2 ?) / exports(L6339~6343, 5 ?) / audits(L6361, 2 ?) — `_gs_q()`만 `%{s}%` wrap, qq 변수만 SQL 인자로 전달, f-string concat 0건 | PASS |
| 6 | 외부 검색엔진 0건 (Elasticsearch/whoosh/lucene/meilisearch/algolia/solr) | grep 전체 코드베이스(import) — 라이브러리 import 0건 (주석·문서·BAT 라벨 4건만 — 절대 금지 표기) | PASS |
| 7 | 핫패치 G1~G5 grep 보존 (보고 11 hits) | style.css 8건(L2253·L3634·L4613·L4731·L4903·L5130·L5371·L5548) + database.py 2건(L2873·L6182) + main.py 1건(L6382) = **11/11 hits** | PASS |
| 8 | v2 본체 (L4287~L4574) 미수정 | 본 사이클 변경 파일 4개(database.py·main.py·search.html·base.html) — style.css 본체 영역 미접촉 (변경 파일 명단 자체에 style.css 제외) | PASS |

---

## 추가 정합성

- main.py L25~28 import 4줄(global_search · GLOBAL_SEARCH_CATEGORIES · 9 헬퍼) 연속 import 라인 PASS
- main.py L1240 `SEARCH_CAT_LABELS` dict 12 키(local 3 + global 9) — 탭/결과그룹 12 정합 일치
- POST /search/suggest 핵심 5 카테고리(orders·customers·parts·issues·users) × 3건 = 최대 15건 상한 적용
- _gs_q `len(s) < 2` 조기 None 반환 — SQL 부담 1자 입력 차단
- 빈 카테고리도 키 포함(L6395 except 폴백 `out[cat] = []`) — 부분 실패 격리

---

## 결론

V43 = **8/8 PASS**. 글로벌 검색 강화 — 헬퍼 9 / 라우트 +1 보강 +1 신규 / UI 2 보강. SQL 인젝션 방지 9/9 헬퍼 전수 PASS (parameter binding `LIKE ?` 일관). 외부 검색엔진 0건. 핫패치 G1~G5 11/11 hits 보존. 99 발사 가능.
