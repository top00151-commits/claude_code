# [04 → 팀장] V1 핫패치 회귀 grep 4종 재현 결과

> **발신**: 04 운영테스트팀 (빅터)
> **수신**: 09 빅터 팀장
> **발행**: 2026-04-25
> **참조**: T3 INTERNAL grep 패턴 (`_INTERNAL_핫패치회귀_grep패턴.md`)
> **결론**: ✅ **4/4 PASS**

---

## 1. 검증 매트릭스

| # | 패턴 | 대상 | 기대 | 실측 | 판정 |
|---|---|---|---|---|---|
| G1 | `<div class="sb-toggle"` | `app/templates/base.html` | 0 | 0 | ✅ |
| G2 | `.dock { ... top: 0 ... }` (multiline) | `static/style.css` | 0 | 0 | ✅ |
| G3 | `body.dock-open .topbar` 활성 규칙 | `static/style.css` | 0 (주석만 OK) | 0 (line 3606 주석만) | ✅ |
| G4 | `.dock { ... top: var(--topbar-h) ... }` (multiline) | `static/style.css` | ≥1 | 1 (line 3620-3621) | ✅ |

---

## 2. 핵심 증거 (raw grep)

- **G1**: `app/templates/base.html` 전체에서 `<div class="sb-toggle"` 0건. (참고: `base_logi.html:47` 잔존은 매출/자재 별도 베이스로 핫패치 범위 외)
- **G2**: `\.dock\s*\{[^}]*top\s*:\s*0\s*[;}]` multiline 매칭 0건.
- **G3**: `body.dock-open` 활성 매칭 = `.main.has-sidebar` (line 3604), `.dock` (line 3630) 만. **`.topbar` 활성 규칙 0건**. line 3606은 `/* 핫패치 ... 제거 */` 주석.
- **G4**: `static/style.css:3620 .dock { ... top: var(--topbar-h, 84px); ... }` 1건 매칭.

---

## 3. 핫패치 라인 보존 재확인

| 파일 | 라인 | 상태 |
|---|---|---|
| `app/templates/base.html` | 93 | sb-toggle DOM 제거 완료 (주석화) |
| `static/style.css` | 3620-3621 | `.dock { top: var(--topbar-h, 84px) }` 정상 |
| `static/style.css` | 3606 | `body.dock-open .topbar` 규칙 주석화 완료 |

→ 01 자체 검증 (4/4 PASS) 과 04 회귀 결과 **100% 일치**.

---

## 4. 비간섭 원칙 준수

- 서버 미기동, 정적 grep 만 실행
- 코드/DB 미접촉
- 5분 이내 완료

---

*04 빅터 · 2026-04-25 · V1 PASS*
