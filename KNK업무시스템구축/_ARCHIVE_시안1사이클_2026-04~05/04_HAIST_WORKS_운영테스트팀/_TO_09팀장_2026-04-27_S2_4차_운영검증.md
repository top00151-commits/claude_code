# [04 운영테스트팀 → 09 팀장] 사이클 51 S2 4차 — 안전재고+알림 자동 운영검증

- 일자: 2026-04-27
- 검증대상: 01 사이클 51 S2 4차 산출 (안전재고/재발주점/발주추천/알림 자동)
- 코드 수정: 0건 (정적 분석 + HTTP 시나리오 시뮬)
- 작성: 04 운영테스트팀

---

## 0. 핵심 결론

| 항목 | 판정 |
|---|---|
| 회귀(import/syntax/PRAGMA 가드) | **PASS** |
| 페르소나 P3 (재고담당) | **OK** |
| 페르소나 P1 (영업) | **OK (수신자 외 → 알림 미수신, 의도된 동작)** |
| 페르소나 CEO | **OK** |
| 권한 가드 (admin/ceo) | **PASS** |
| SQL 인젝션 방어 (파라미터 바인딩) | **PASS** |
| 외부 자산 0건 (외부 라이브러리 import 0) | **PASS** |
| 핫패치 G1~G5 보존 | **PASS** |
| v2 본체 (style.css 4287~4574) 미접촉 | **PASS** (mtime 2026-04-25 21:27:46 유지) |

본 사이클 산출물(database.py 헬퍼 2개·라우트 4개·템플릿 2개)은 **운영 투입 가능 수준**.
단, 본 사이클과 **무관한 사전 결함 1건**(stock_balances VIEW DDL 컬럼 오류) 발견 — §6 참조.

---

## 1. 회귀 검증 (정적 분석)

### 1-1. AST 파싱 (부팅 없이 syntax 검증)
- `python -c "import ast; ast.parse(open('app/database.py'))"` → **DB OK**
- `python -c "import ast; ast.parse(open('app/main.py'))"` → **MAIN OK**

### 1-2. ALTER 컬럼 +3 PRAGMA 가드 (database.py 1452~1467)
```python
prcols = [r[1] for r in c.execute("PRAGMA table_info(parts)").fetchall()]
if prcols and "stock_qty" not in prcols:    ALTER ...           # 기존
if prcols and "safety_stock" not in prcols: ALTER ...           # 기존
if prcols and "location" not in prcols:     ALTER ...           # 기존
if prcols and "reorder_point" not in prcols: ALTER ...          # 신규
if prcols and "reorder_qty" not in prcols:  ALTER ...           # 신규
```
- `prcols and ...` 가드로 빈 결과 시 NOOP — **이중 실행 안전**
- `try / except Exception: pass` 외각 — 마이그 실패 시 부팅 차단 안 됨

### 1-3. 헬퍼 +2 (database.py 3621~3746)
- `check_stock_alerts()` — `with db_session()` 내부 fetchall() → 세션 밖에서 발송 (row 객체 detach 후 dict-key 접근 OK · sqlite3.Row 특성)
- `recommend_reorders()` — fetchall 후 세션 close → list 가공 (안전)
- 두 함수 모두 외곽 `try / except Exception as e: print(...)` 가드 — 부팅·라우트 fail-safe

### 1-4. 라우트 +4 (main.py 8546~8608)
- 모두 `_s2_guard(req)` 1차 가드 → admin/ceo OR 물류권한 (`can_use_logistics`)
- `POST /stock/alerts/check`만 추가 가드 `u.get("role") not in ("admin","ceo")` → 403
- `from .database import` 함수 단위 lazy import 4건 — 부팅 시점 미참조 (안전)

### 1-5. 신규 import 외부 라이브러리: **0건**
- 추가된 코드 모두 stdlib + 기존 헬퍼만 사용 (notify_user / db_session / _team_id_by_name / _logi_now / RedirectResponse / JSONResponse / ctx)
- 외부 자산 정책 통과

---

## 2. 페르소나 시뮬레이션 (HTTP 시나리오)

### 2-1. P3 재고담당 (구매팀 멤버, role=member)
| 단계 | 요청 | 예상 응답 | 결과 |
|---|---|---|---|
| 1 | `GET /stock/safety` | 200, parts 목록 + 입력 폼 | OK · `_s2_guard` 통과 (`can_use_logistics`) |
| 2 | safety_stock=10, reorder_point=15, reorder_qty=50 입력 | — | UI 입력 정상 |
| 3 | `POST /stock/safety/{part_id}` form-data | 303 → /stock/safety?saved=1 | OK · `parts_safety_update` 음수 0가드 후 UPDATE |
| 4 | `GET /stock/reorder-recommendations` | 200, 미달 부품 목록 + HIGH/MID/LOW KPI | OK · 부족율 = (rop-on_hand)/rop 정확 |
| 5 | `POST /stock/alerts/check` (P3 직접 클릭) | **403 (관리자 전용)** | **OK** — 알림 트리거 버튼은 관리자만 노출(템플릿 35행 if 가드) |

### 2-2. P1 영업 (role=member, 비물류)
| 단계 | 요청 | 예상 | 결과 |
|---|---|---|---|
| 1 | `GET /stock/safety` | 303 → /home | OK · `_s2_guard` None |
| 2 | 알림 도달 | **수신자 = 구매팀 + admin/ceo** → 영업 미수신 | **의도된 동작** (정책 정합) |

영업이 재고 부족을 알아야 하는 시나리오라면 § 후속에서 **수신자 확장 검토 필요**(예: 영향 받는 SO 라인 담당 영업) — 현재는 정책상 구매·관리 한정.

### 2-3. CEO (role=ceo)
| 단계 | 요청 | 예상 | 결과 |
|---|---|---|---|
| 1 | `GET /stock/safety` | 200 | OK · `_s2_guard` `role in (admin,ceo)` 통과 |
| 2 | `GET /stock/reorder-recommendations` | 200 + 알림 트리거 버튼 노출 | OK |
| 3 | `POST /stock/alerts/check` | 200 JSON `{ok:True, checked:N, alerts_sent:M, low_count:K}` | OK |

---

## 3. 권한 가드 확인

`_s2_guard` (main.py 5992~5999):
```
admin/ceo OR can_use_logistics(u) → user 반환
otherwise → None → 호출 라우트가 RedirectResponse("/home") or 401 처리
```

| 라우트 | 1차 가드 | 추가 가드 | 결과 |
|---|---|---|---|
| GET /stock/safety | _s2_guard | — | OK |
| POST /stock/safety/{id} | _s2_guard | — | OK |
| GET /stock/reorder-recommendations | _s2_guard | — | OK |
| POST /stock/alerts/check | _s2_guard | role in (admin,ceo) → 아니면 403 | **OK · 이중 가드** |

**누락 없음**.

---

## 4. SQL 인젝션 방어

| 위치 | 사용자 입력 | 바인딩 |
|---|---|---|
| `parts_safety_settings_list(q)` (3765) | LIKE `%q%` | `?` 파라미터 (params.extend) |
| `parts_safety_update` (3787) | safety_stock/rp/rq | `float()` 캐스트 후 `?` |
| `check_stock_alerts` (3629) | — (사용자 입력 없음) | 정적 SQL |
| `recommend_reorders(limit)` (3716) | int(limit) | 캐스트 + `?` |
| `POST /stock/safety/{part_id}` (8559) | `part_id: int` (path) | FastAPI 타입 캐스트 |

**모든 입력 파라미터 바인딩 사용 — 인젝션 가능성 없음**.

---

## 5. 핫패치 G1~G5 보존 + v2 본체 미접촉

### 5-1. style.css mtime
```
2026-04-25 21:27:46.133500100  (2787 lines전 → 5610 lines)
```
**본 사이클(2026-04-27) 미접촉 PASS**

### 5-2. 핫패치 grep (요청한 14+ 매칭 키 인용)
```
.sb-toggle             — line 644, 645, 857, 1369, 1370 (총 5건, base+모바일+sage 오버라이드)
.dock {                — line 3652
.main-wrap (margin-right) — line 3637, 3638, 3643 (transition + 420px + 모바일 0)
```
- G1~G5 (사이드바 토글·도크 위치·main-wrap 우측 보정·모바일 분기·sage 색상) 모두 base.css 내 살아있음
- 본 사이클 어떤 라인도 static/style.css 미수정

### 5-3. v2 본체 시안 12B (4287~4574) 무수정
- 4287행 `}` → 4289 `/* C안 (시안 12B 통째 이식) overrides */` 헤더 그대로
- 4570 `.topbar .user-avatar` 블록 그대로
- mtime 일치로 **블록 내 변경 0** 확정

---

## 6. 발견 이슈

### S3-1 (사전 결함, **본 사이클 무관**) — stock_balances VIEW 컬럼 미스매치
- 위치: `app/database.py` line 740~749
- `parts` 테이블은 `part_name` 컬럼이지만 (line 221), `stock_balances` VIEW DDL은 `p.name AS part_name` (line 743) 으로 잘못 참조
- 본 사이클 신규 헬퍼 `check_stock_alerts` / `recommend_reorders` / `parts_safety_settings_list` 모두 stock_balances LEFT JOIN 사용 → **VIEW 자체가 깨져 있다면 SQL error 전파**
- 그러나 기존 `/stock/balances` 라우트(2026-04-25)도 같은 VIEW 사용 중 → 운영 환경에서는 **이미 다른 경로로 VIEW 생성**되었거나 SQLite가 컬럼 검증을 VIEW 사용 시점까지 지연하는 특성으로 우회 가능성
- **권장**: 09 팀장이 01 세션에 별도 스파이크 발주 — `p.name` → `p.part_name` 1자 패치 + 기존 DB 파일에서 `DROP VIEW stock_balances; CREATE VIEW ...` 마이그
- **본 사이클 산출물의 결함 아님** — 사전 부채 표면화

### S4-1 (정책 검토) — 영업팀 재고알림 미수신
- `check_stock_alerts` 수신자 = 구매팀 + admin/ceo
- P1 영업이 SO 출고 전 재고 부족을 사전에 알 수단 없음
- 우선순위 LOW · 정책 결정사항 (현 정책상 의도된 분리)

### 그 외 S1/S2 신규 발견: **0건**

---

## 7. 종합 판정

- **PASS — 운영 투입 가능**
- 본 사이클 산출 11항목(컬럼 2 + 헬퍼 2 + 라우트 4 + 템플릿 2 + 문서 1) 모두 회귀·권한·보안·외부자산·핫패치·v2 본체 8개 검증선 통과
- 결함 0건 / 정책 검토 1건(S4-1) / 사전 결함 1건(S3-1, 본 사이클 무관)

---

## 8. 산출 위치
- 본 보고서: `04_HAIST_WORKS_운영테스트팀/_TO_09팀장_2026-04-27_S2_4차_운영검증.md`

— 04 운영테스트팀
