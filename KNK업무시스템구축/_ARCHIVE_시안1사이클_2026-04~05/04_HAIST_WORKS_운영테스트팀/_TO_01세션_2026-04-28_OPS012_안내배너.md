# [04 → 01 메인] 🔴 OPS-012 leader /dashboard 안내 배너 (2026-04-28)

> **참조**: 1·2·3차 누적 보고. 05 디자인팀 UX-12 (배너 컴포넌트 시안 제공)
> **상태**: 1차 `/login` → 3차 `/team` 완화 → 4차에도 안내 부재

---

## 1. 4차 재현

`김정록 (kjr2 / 품질팀 leader)` `/dashboard` 직접 접근:
- 결과: HTTP 200, final URL `/team`, title "팀 현황 · KNK"
- **사용자 인식**: 왜 /dashboard 가 안 보이는지 알 수 없음. 단순 silent 리다이렉트

## 2. 요청 — 코드 1줄 + 템플릿 1줄

### `app/main.py` — `/dashboard` 핸들러
```python
@app.get("/dashboard")
async def dashboard_page(req: Request):
    u = get_user(req)
    if not u:
        return RedirectResponse("/login", 303)
    if u["role"] not in ("admin", "ceo", "executive"):
        # ★ 추가
        req.session["flash"] = (
            "warning",
            "전사 대시보드는 경영진(executive 이상)만 볼 수 있습니다. 팀 현황으로 이동합니다."
        )
        return RedirectResponse("/team", 303)
    # ... 기존 로직
```

### `app/templates/base.html` — flash 배너 컴포넌트
```html
{% if request.session.get('flash') %}
  {% set lvl, msg = request.session.pop('flash') %}
  <div class="flash-banner flash-{{ lvl }}">
    <span class="flash-icon">{% if lvl == 'warning' %}🔒{% elif lvl == 'success' %}✅{% else %}ℹ️{% endif %}</span>
    <span class="flash-msg">{{ msg }}</span>
    <button class="flash-close" onclick="this.parentElement.remove()">×</button>
  </div>
{% endif %}
```

### CSS (05 디자인팀 시안 참조)
- 05 의 `_FROM_04운영테스트_2026-04-21_UX피드백.md` UX-12 항목에서 시안 제공
- 또는 05 가 신규 토큰 정의 후 01 에 전달

## 3. 동일 패턴 적용 후보 (한 번에 처리 권장)

같은 안내 배너 패턴이 필요한 다른 차단:
- OPS-001 자재 모듈 차단 시: "자재 모듈 권한이 없습니다. 관리자에게 요청하세요."
- /admin → /login 차단 시: "관리자 권한이 필요합니다. 관리팀에 문의하세요."
- 기타 권한 부족 silent 리다이렉트

→ 한 번에 flash 인프라 추가하면 모든 권한 안내 통합 가능.

## 4. 통과 기준

`kjr2 / knk1234` 로그인 → `/dashboard` 직접 접근 → /team 도착 + **상단에 노란 배너 "🔒 전사 대시보드는 경영진(executive 이상)만..."** 표시.

## 5. 04 재검증

01 커밋 통보 → 30분 이내 회귀.

---

*04 운영테스트팀 — 2026-04-28*
