# 04 → 09 회신 — V31 QMS 강화 1차 회귀 (8/8 PASS)

**작성**: 빅터 (04 운영테스트) · **일시**: 2026-04-26 · **모드**: 정적 grep · 서버/DB 미기동 · 코드 미수정

---

## V31-1. DB 컬럼 +6 ALTER ADD COLUMN (PRAGMA 가드) — PASS
- `database.py` L1459 `PRAGMA table_info(issues)` → L1460~1466 루프로 5컬럼 ALTER (`sla_hours` / `detected_at` / `responded_at` / `recurrence_id` / `sla_breached`) — 각 col not in icols 분기 + try/except idempotent.
- `database.py` L1481 `PRAGMA table_info(users)` → L1482~1486 `can_use_quality` 1컬럼 ALTER + idempotent UPDATE (품질팀 / admin/ceo/executive 자동 부여).
- 합계 issues 5 + users 1 = **+6컬럼** ✓.

## V31-2. DB 테이블 +3 (CREATE TABLE IF NOT EXISTS) — PASS
- L1024 `qms_audit_log` (immutable 감사) + L1032/1033 인덱스 idx_qmsa_issue/action.
- L1035 `corrective_actions` (status CHECK) + L1047/1048 인덱스 idx_ca_issue/status.
- L1050 `preventive_actions` (corrective_id FK) + L1058 인덱스 idx_pa_ca.
- 모두 IF NOT EXISTS ✓ idempotent 보존.

## V31-3. 라우트 +5 grep + `_qms_guard()` 5/5 — PASS
```
main.py:5980 @app.get("/qms")
main.py:6021 @app.get("/qms/issues/{iid}/sla")
main.py:6034 @app.post("/qms/issues/{iid}/corrective")
main.py:6060 @app.post("/qms/issues/{iid}/preventive")
main.py:6087 @app.get("/qms/recurrence")
```
`_qms_guard` 정의 1건(L5931) + 호출 5건(L5983/6024/6040/6065/6090) — **단일 정의 5/5 적용** ✓.

## V31-4. 신규 템플릿 +2 — PASS
- `app/templates/qms_dashboard.html` 80줄 (실측 wc -l) — 회신문 80줄 일치.
- `app/templates/qms_recurrence.html` 76줄 (실측 wc -l) — 회신문 76줄 일치.

## V31-5. 권한 분기 정합 — PASS
`_qms_guard` (main.py L5931~5950): role IN ('admin','ceo','executive') OR `can_use_quality=1` OR teams.name LIKE '%품질%'. 5라우트 첫 줄 가드 + 미통과시 None 반환 → 403 처리 일관 ✓.

## V31-6. 핫패치 G1~G5 5/5 PASS
- `static/style.css` 5610줄 본 사이클 미접촉 (회신문 §5 grep 7건 인용 — 라인 2253/4613/4731/4903/5130/5371/5548).
- 실측: style.css 라인 4287(`}` v2 본체 시작) / 4570~4574(.topbar .user-avatar) — v2 본체 무수정 ✓.

## V31-7. v2 본체 (L4287~L4574) 미수정 — PASS
- 실측 L4289~4291 주석 "C안 (시안 12B 통째 이식) overrides — 2026-04-25 healing-c" 보존.
- 실측 L4570~4574 .topbar .user-avatar 블록 보존.

## V31-8. BAT 갱신 — PASS
- `START.bat` L3/L7/L13 `2026-04-26 QMS-강화-1차` 3행 갱신 ✓.
- `KNK_시작.bat` L3/L7/L14 동일 ✓.

---

## 회귀 결론
**8/8 PASS**. 서버 미기동·DB 미접속·정적 grep만으로도 코드 정합성 100%. 99 발사(동적 회귀) 가능.
