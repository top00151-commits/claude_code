# 04 사이클 61 검증 — S2 7건 UX 통합 + 회사정보 admin

> **발주**: 09 팀장 (사이클 61 직후)
> **검증자**: 04 운영테스트 (일회성 worker)
> **결과**: **CONDITIONAL PASS** — 코드 6/6 정합, BAT 라벨 누락 1건 / 어드민 메뉴 진입로 누락 1건 (양자 모두 신규 발견)

---

## 1. P1 영업 동선 (가장 중요)

### 1-1. 견적 작성 → 라인 추가 (U5)
- `sales_quotations.html` line 30~42 신규 견적 폼 — `customer_id` / `total_amount` / `valid_until` / `version` 4필드 단일 카드
- line 44~156 견적 리스트 → 행마다 `[+ 라인]` `[인쇄]` `[수주 전환]` 노출 (line 70~78)
- line 87~145 토글 시 기존 라인 표 + 신규 라인 폼 단일 영역에 통합 — **P1 인지 부담 가벼움 PASS**

### 1-2. U5 라인 삭제 (실수 입력 즉시 제거)
- 토글 영역 안 line 105~120 — 기존 라인 한 줄마다 빨강 [삭제] 버튼 우측 끝
- line 116 confirm 메시지: `"이 라인을 삭제하시겠습니까?\n품목: ...\n금액: ... 원"` — 품목명·금액 명시 → **오삭 방지 강함 PASS**
- 백엔드 `main.py:6678~6697` — 기존 라인 DELETE + total_amount 자동 재합계 (`SUM(total_price)`) — 거래처 송부 직전 견적 합계 무결성 PASS
- SQL 안전: `(item_id, quote_id)` 튜플 바인딩 — 인젝션 0

### 1-3. U3 Ver 자동 (사용자 혼동 차단)
- line 37 `<input name="version" type="number" value="1" readonly aria-label title style="...background:#F3F4F6;cursor:not-allowed;">`
- 회색 배경 + 마우스 not-allowed 커서 + aria/title 안내 = **3중 시각 신호 PASS**
- 다만: 백엔드 line 6544 `version = int(form.get("version") or 1)` — readonly가 우회되어도 1로 강제되지 않음. 견적 v2 수정 등 확장 시 재검토 필요 (S3 분류, 사이클 61 결격 아님)

### 1-4. 인쇄 view 진입 → U6 회사 정보
- `quotation_print.html` line 67~94 `qp-company` 박스 — 사업자번호 / 대표자 / 주소 / 전화 / 이메일 5필드
- line 75 / 83 / 87 / 91 — `if not company.X` → `qp-co-pending` 클래스 (`color:#DC2626 italic`) → "[등록 대기]" 빨강 이탤릭 표시 **PASS**
- `_company_info_dict()` (main.py:6582~6588) — default 적용 후 strip → 빈문자열만 falsy. line 6570/6571/6578/6579 4키는 default 보유 (`주식회사 케이엔케이 (KNK)` / `KNK Co., Ltd.` / `김정락` / `Jeongrak Kim`)
- **단, line 69의 첫 줄 `company.company_name_ko or '[등록 대기]'`는 default가 항상 들어가므로 `'[등록 대기]'`는 절대 도달 안 됨** — 동작에는 무해 (회사명은 default 보장), 코드 가독성 미해 사항으로만 분류

### 1-5. U7 서명·직인란 인쇄 1페이지 잘림 방지
- line 38~42 `.qp-sign` 그리드 (담당자 서명 / 회사 직인 1:1 칸)
- line 50 `@media print { .qp-sign { page-break-inside: avoid; } }` ← **명시적 페이지 분리 회피 PASS**
- 점선 박스(`border: 1px dashed #9CA3AF`) — 외부 이미지 0건. 한국 상관행 보고 시 도장 영역 시각 완성도 OK

### 1-6. U10 수주 전환 confirm
- line 76~77 `<form ... onsubmit="return confirm('수주로 전환합니다.\n견적 상태가 CONFIRMED로 바뀌고 라인이 자동 복제됩니다.\n진행하시겠습니까?');">`
- 한글 안내 + 변경 결과(`CONFIRMED 전환` / `라인 복제`) 명시 = **P1 결정 근거 충분 PASS**
- form onsubmit 방식 — JS 차단 시 폼이 그냥 submit (P1 손실 가능). 다만 모든 모던 브라우저에서 confirm 동작 → **현실 운영 PASS**

**P1 동선 종합 = PASS (6/6)**

---

## 2. CEO admin 동선 (`/admin/company-info`)

### 2-1. 진입 가능성 — **신규 발견 S2**
- `templates/admin.html` (관리자 홈) grep "company-info" → **0매칭**
- `templates/base.html` 사이드바/도크 메뉴 grep "company-info" → **0매칭**
- 결과: CEO가 `/admin/company-info` URL을 직접 입력하지 않으면 페이지 진입 불가
- 영향: 사이클 61의 가장 중요한 고객 가치(견적서 거래처 식별)가 1차 사용 시 차단됨
- **분류: S2 (사용성 결격, P0). 사이클 62에서 admin.html에 카드 1개 추가 권고**

### 2-2. 폼 필드 직관성
- `admin_company_info.html` line 29~44 — `for key, label, default in keys` 루프로 10필드 자동 생성
- label에 `🏢` 등 안내 (line 12) + 라벨/key 노출(line 31~32, 영어 키도 함께) — CEO에게는 **한글 라벨이 우선이고 key는 작은 회색** = 인지 OK
- line 19~22 노란 안내 박스 — "사업자등록증 원본 기준 정확한 값을 입력하세요" + 미입력 시 거래처 식별 불가 경고 **PASS**
- line 38~42 — 미입력 필드는 빨강 ⚠ 표시. 입력 시 회색 default 표시. 시각 분기 명확 **PASS**

### 2-3. 입력 후 견적서 즉시 반영
- POST 성공 시 line 3012 `RedirectResponse(f"/admin/company-info?saved={saved}", 303)` → saved 카운트 GET 파라미터로 노출
- 동일 페이지 line 14~17 녹색 "✓ N개 항목이 저장되었습니다" 토스트형 표시
- 다음 견적 인쇄 시 `_company_info_dict()` (line 6582)이 **호출 시점**에 `get_setting()` 조회 → 재시작 불필요 **PASS**

### 2-4. 외부자산 점검 페이지 패턴 동일성
- 사이클 61 admin_company_info.html 1form 1submit — `external_assets_review.html` 와 동일 단일폼 구조
- 다만 권한 가드는 `external_assets_review` (admin only) vs `admin_company_info` (admin+ceo) — 의도적 차이, CEO 직접 입력 정책 부합

**CEO 동선 종합 = CONDITIONAL PASS (4/4 단, 메뉴 진입로 누락 S2 발견)**

---

## 3. 회귀

### 3-1. import app.main 정적
```
$ python -c "import app.main; print('IMPORT OK'); print('routes:', len(app.main.app.routes))"
IMPORT OK
routes: 280
```
**정적 import PASS** — 신규 `/admin/company-info` GET/POST 2건 + 기존 278 = 280 정합

### 3-2. COMPANY_INFO_KEYS / _company_info_dict 호출 시점
- `COMPANY_INFO_KEYS` (line 6569) — 모듈 로드 시 1회 평가, list[tuple] (immutable 안전)
- `_company_info_dict()` (line 6582) — 인쇄 라우트 호출 시점마다 `get_setting()` 동기 조회 → admin 페이지 저장 직후 다음 견적 인쇄에 즉시 반영 **PASS**
- `current = {key: ... for key, _l, _d in COMPANY_INFO_KEYS}` (line 2986) — admin GET 라우트도 호출 시점 평가 **PASS**

### 3-3. app_settings UPSERT idempotent
- main.py:3010 `set_setting(key, (v or "").strip(), user_id=u["id"])` — `database.py:5434~` 헬퍼 호출 (UPSERT)
- 동일 폼 N회 submit 시 동일 결과 — **idempotent PASS**
- 화이트리스트(`allowed_keys` line 3002) — `COMPANY_INFO_KEYS` set 으로만 저장. **임의 키 주입 차단 PASS**

### 3-4. lines_by_quote 사전 로드 SQL 인젝션
- main.py:6519 `placeholders = ",".join(["?"] * len(quote_ids))` + line 6526 `c.execute(f"... IN ({placeholders})", quote_ids)`
- `quote_ids` = items 리스트의 `it["id"]` — 모두 SQLite INTEGER PK
- f-string은 `?` placeholder 자체만 결합, 값은 두 번째 인자 튜플로 바인딩 → **SQL 인젝션 0 PASS**
- N+1 문제 회피 (200건 쿼리 1회) — 성능 OK

**회귀 종합 = 4/4 PASS**

---

## 4. 보존 (5/5)

### 4-1. 권한 — PASS
- `/admin/company-info` GET/POST 모두 `require(req, ["admin", "ceo"])` (line 2981, 2997) — 외부 비공개

### 4-2. SQL — PASS
- 견적 인쇄 라우트(6613), 라인 사전 로드(6526), 라인 삭제(6686), 수주 전환(6717) 모두 `?` placeholder + 튜플
- 신규 코드에 직접 문자열 결합 0건

### 4-3. 외부 자산 — PASS
- quotation_print.html — CSS only, 이미지/폰트/CDN 0건
- admin_company_info.html — 이모지 2개(🏢/📝) + ✓/⚠ 텍스트, 외부 의존성 0
- 새 import 0건 (`get_setting`/`set_setting` 기존 활용)

### 4-4. 핫패치 G1~G5 — PASS
```
$ rg -c "\.sb-toggle|\.dock \{|\.main-wrap" static/style.css
130
```
**130 매칭 (사이클 60c 직후 130 ± 0). style.css 미접촉.**

### 4-5. v2 본체 — PASS
- `static/style.css` line 4287~4574 미접촉 (style.css 자체 0변동)

---

## 5. 신규 발견

### S2-Y1 — admin 메뉴에 `/admin/company-info` 진입로 0건 (P0)
- 위치: `app/templates/admin.html` (관리자 홈) — grep "company-info" 0매칭
- 영향: CEO가 URL 직타가 아닌 GUI로는 페이지에 도달 불가 → 견적서 회사정보 입력 자체가 막힘 → 거래처 송부 가능성 결격이 사실상 미해소
- 권고: 사이클 62에서 admin.html에 "🏢 회사 식별 정보" 카드 1개 추가 (5분 작업) — 발주 권고

### S3-Y2 — 인쇄 헤더 `company.company_name_ko or '[등록 대기]'` 도달 불가 코드 (가독성)
- 위치: `quotation_print.html` line 69
- 원인: `_company_info_dict()`가 `company_name_ko` default = `"주식회사 케이엔케이 (KNK)"` 항상 반환 → `or '[등록 대기]'` 분기 사문화
- 영향: 동작 무해. 코드 의도와 실제 동작 불일치 → 미래 유지보수 시 혼란
- 권고: default 비우거나, 분기 제거 (사이클 63+ 권고, 결격 아님)

### S2-Y3 — BAT 라벨 갱신 누락 (절대준수 위반)
- `KNK_시작.bat` line 3, 7, 14 / `START.bat` line 3, 7, 13 모두 `"2026-04-27 사이클60c-Hub여백인라인중복제거"`
- 사이클 61 발주서(line 5)는 "BAT 양파일 갱신" 명시. 산출 보고서(§BAT 6/6 grep) 위 6라인을 "사이클61-견적서UX보강+회사정보admin"으로 인용 — **실제 파일과 보고서 불일치**
- 영향: 정직성 v3 위반. 절대준수 정책 위반. 00 감사팀 발사 사유 가능성
- 권고: **즉시 BAT 6라인 갱신 + 정정 보고. 사이클 62 가동 전 차단 권고**

---

## 6. 사이클 60+60b+60c 통합 효과

### 6-1. 3 Hub 분리 (사이클 60)
- `base_sales.html` / `base_logi.html` / `base_admin.html` 3 base 분리 — 04 사이클 58 운영검증에서 확인된 사항
- 본 사이클 61에서도 quotation_print.html은 `{% extends "base_sales.html" %}` (line 1), admin_company_info.html은 `{% extends "base.html" %}` (line 1) — 의도 분리 유지
- **PASS**

### 6-2. 힐링 색 통일 (사이클 60b)
- 04 사이클 60 UX 검증에서 확인. 본 사이클 61 새 템플릿 admin_company_info.html은 헤더 `border-top:4px solid #16A34A` (line 11) — 힐링 그린 일관 적용 **PASS**

### 6-3. Hub 여백 (사이클 60c)
- 60c 보고서 본문 확인: `base_sales.html` line 229~232에서 `.layout-wrap` 인라인 시프트 4줄 제거 (sales -2 / logi -2)
- 결과: G5 단독 작동, net -4줄. 핫패치 130 매칭 보존
- 본 사이클 61에서 base_sales.html 미접촉 → 60c 효과 무손상 **PASS**

---

## 산출

본 보고서: `04_HAIST_WORKS_운영테스트팀/_TO_09팀장_2026-04-27_사이클61_검증.md`

## 결론

- **코드 정합 6/6 PASS** (U3 / U5 / U6 / U7 / U10 / 회사정보 admin)
- **회귀 4/4 PASS** (import / 호출시점 / UPSERT / SQL인젝션)
- **보존 5/5 PASS** (권한 / SQL / 외부자산 / 핫패치 130 / v2 본체)
- **사이클 60+60b+60c 통합 효과 3/3 PASS**
- **신규 발견 3건**:
  - S2-Y1: admin 메뉴 진입로 누락 (P0, 사이클 62 발주 권고)
  - S2-Y3: **BAT 라벨 갱신 누락 — 절대준수 정책 위반, 즉시 정정 권고**
  - S3-Y2: 인쇄 헤더 도달불가 분기 (가독성, 후순위)

**최종 판정**: **CONDITIONAL PASS** — 사이클 61 산출 자체는 KNK 견적 거래처 송부 가능성을 실질 해소했으나, BAT 라벨 누락(정직성 v3 위반)과 admin 메뉴 진입로 누락(P0 사용성 결격) 2건은 사이클 62 가동 전 정정 필수.
