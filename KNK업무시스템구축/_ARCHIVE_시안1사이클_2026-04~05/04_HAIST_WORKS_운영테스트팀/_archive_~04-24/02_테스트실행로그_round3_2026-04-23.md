# 02 테스트 실행 로그 — 3라운드 2026-04-23

> 설문 기반 실명 페르소나 8종. 신규 3종 시스템(변경/티켓/진행률) 검증.
> 원천: HAIST_WORKS_설문요약.md + 핸드오프_2026-04-23.md


## PS-01 — 구매팀 매니저 `정성진` (team=10 구매팀 leader)

_설문 고통: 카톡 출고 요청 누락·ERP 이중 입력. 1순위 기능 티켓·홈KPI 검증_

로그인 → 200 | /team | 4144ms
  ✅ `GET /home` → 200 | 「🏠 업무현황 · KNK」 | 2109ms | 홈 KPI 통합 확인
  >> [홈 KPI 통합] {'변경': 34, '티켓': 16, '진행률': 17, '미확인': 1, '지연': 6, '요청': 26}
  ✅ `GET /tickets` → 200 | 「요청 티켓 — HAIST WORKS」 | 2085ms | 요청 티켓 리스트
  ✅ `GET /tickets/new` → 200 | 「티켓 등록 — HAIST WORKS」 | 2084ms | 티켓 등록 폼
  ⚠️폴백 `GET /logistics` → 200 | 「🏠 업무현황 · KNK」 | 4099ms | 자재 허브 — can_use_logistics 회귀
  ⚠️폴백 `GET /po` → 200 | 「🏠 업무현황 · KNK」 | 4112ms | PO 리스트
  ⚠️폴백 `GET /parts` → 200 | 「🏠 업무현황 · KNK」 | 4115ms | 부품 마스터

**발견 3건**: 폴백: `/logistics` → `/home` (title:「🏠 업무현황 · KNK」) 자재 허브 — can_use_logistics 회귀; 폴백: `/po` → `/home` (title:「🏠 업무현황 · KNK」) PO 리스트; 폴백: `/parts` → `/home` (title:「🏠 업무현황 · KNK」) 부품 마스터

## PS-02 — 관리팀 팀장 `박지은` (team=11 관리팀 leader)

로그인 → 200 | /team | 4080ms
  ✅ `GET /home` → 200 | 「🏠 업무현황 · KNK」 | 2068ms 
  ⚠️폴백 `GET /dashboard` → 200 | 「팀 현황 · KNK」 | 4108ms | OPS-012 회귀 체크 — leader 가 /dashboard 200 OK?
  ✅ `GET /board/company` → 200 | 「전사 게시판 · HAIST WORKS」 | 2062ms 
  ⚠️폴백 `GET /admin` → 200 | 「로그인 · HAIST WORKS」 | 4089ms | 관리팀 leader 의 admin 권한

**발견 2건**: 폴백: `/dashboard` → `/team` (title:「팀 현황 · KNK」) OPS-012 회귀 체크 — leader 가 /dashboard 200 OK?; 폴백: `/admin` → `/login` (title:「로그인 · HAIST WORKS」) 관리팀 leader 의 admin 권한

## PS-03 — 영업 이사 `이해림` (team=1 기술영업팀 leader)

_설문 매일 원하는 정보 1위: 모델별 진척율. 진행률 대시보드 직격_

로그인 → 200 | /login | 2047ms
  ⚠️폴백 `GET /home` → 200 | 「로그인 · HAIST WORKS」 | 4098ms 
  ⚠️폴백 `GET /progress` → 200 | 「로그인 · HAIST WORKS」 | 4061ms | 진행률 대시보드 메인
  >> [진행률 12공정 라벨] {'수주': 0, '설계': 0, '전장': 0, 'SW': 0, '가공': 0, '구매': 0, '조립': 0, '검수': 0, '출하': 0, 'Set-up': 0, 'KNKVN': 0}
  ⚠️폴백 `GET /progress/1` → 200 | 「로그인 · HAIST WORKS」 | 4076ms | 개별 프로젝트 공정 상세
  >> [개별 공정 UI] {'담당': 0, '예정': 0, '실제': 0, '지연': 0, '완료': 0, '진행': 0}
  >> /api/progress/summary → 200 (2054ms) | {"my_open":0,"delayed":0}

**발견 5건**: 폴백: `/home` → `/login` (title:「로그인 · HAIST WORKS」) ; 폴백: `/progress` → `/login` (title:「로그인 · HAIST WORKS」) 진행률 대시보드 메인; 진행률 12공정 라벨 누락: ['수주', '설계', '전장', 'SW', '가공', '구매', '조립', '검수', '출하', 'Set-up', 'KNKVN']; 폴백: `/progress/1` → `/login` (title:「로그인 · HAIST WORKS」) 개별 프로젝트 공정 상세; 개별 공정 UI 누락: ['담당', '예정', '실제', '지연', '완료', '진행']

## PS-04 — 전장설계팀장 `김형렬` (team=6 전장설계팀 leader)

_기구→전장 변경 1~3일 지연. 변경 Inform 시스템 이 업무 직격_

로그인 → 200 | /team | 4070ms
  ✅ `GET /home` → 200 | 「🏠 업무현황 · KNK」 | 2072ms 
  ✅ `GET /changes` → 200 | 「변경 알림 — HAIST WORKS」 | 2082ms | 변경 리스트
  >> [변경 리스트 표시] {'긴급': 6, '일반': 6, '영향': 3, '전장': 7, '기구': 8}
  >> /api/changes/unread → 200 | {"count":1}

## PS-05 — 제조2팀장 `임택훈` (team=8 제조기술2팀 leader)

_설문 사고사례: 30일 link 만료·변경 통보 누락. 변경+게시판 검증_

로그인 → 200 | /team | 4101ms
  ✅ `GET /home` → 200 | 「🏠 업무현황 · KNK」 | 2066ms 
  ✅ `GET /changes` → 200 | 「변경 알림 — HAIST WORKS」 | 2050ms 
  ✅ `GET /board/team` → 200 | 「제조기술2팀 게시판 · HAIST WORKS」 | 2073ms | 팀 게시판 — 개인PC 파일 대체 가능?
  ✅ `GET /board/new` → 200 | 「글 작성 · HAIST WORKS」 | 2076ms | 게시판 글 작성 폼

## PS-06 — 가공팀장 `윤영조` (team=9 가공팀 leader)

_긴급 가공 우선순위 기준 없음. 티켓/변경 urgency 검증_

로그인 → 200 | /team | 4076ms
  ✅ `GET /tickets` → 200 | 「요청 티켓 — HAIST WORKS」 | 2017ms 
  >> [티켓 리스트 긴급 구분] {'긴급': 8, 'urgency': 1, '정상': 0, '대기': 3, '진행': 21}
  ✅ `GET /tickets/new` → 200 | 「티켓 등록 — HAIST WORKS」 | 2025ms 
  >> [티켓 등록 폼 필드] {'urgency': 2, '긴급': 12, '제목': 4, '내용': 3, '대상': 3}
  ✅ `GET /progress` → 200 | 「진행률 대시보드 — HAIST WORKS」 | 2080ms 

**발견 1건**: 티켓 리스트 긴급 구분 누락: ['정상']

## PS-07 — 품질팀장 `김정록` (team=3 품질팀 leader)

_이슈 종이 기록. issues DB 검증 + OPS-012 회귀_

로그인 → 200 | /team | 4059ms
  ⚠️폴백 `GET /dashboard` → 200 | 「팀 현황 · KNK」 | 4066ms | 🔁 OPS-012 회귀: leader /dashboard 200?
  ✅ `GET /issues` → 200 | 「이슈·AS DB — HAIST WORKS」 | 2056ms 
  ✅ `GET /issues/new` → 200 | 「이슈 등록 — HAIST WORKS」 | 2030ms 
  >> [이슈 폼 필수 필드] {'고객': 5, 'customer': 3, '프로젝트': 7, '증상': 1, '처리': 5, 'severity': 1, '심각': 4}

**발견 1건**: 폴백: `/dashboard` → `/team` (title:「팀 현황 · KNK」) 🔁 OPS-012 회귀: leader /dashboard 200?

## PS-08 — SW팀장 `이한중` (team=5 소프트웨어팀 leader)

_일정 변경 통보 늦음. 홈 KPI + 변경 유형 분류 검증_

로그인 → 200 | /team | 4068ms
  ✅ `GET /home` → 200 | 「🏠 업무현황 · KNK」 | 2054ms 
  >> [홈에서 SW 관련 변경 표시] {'소프트웨어': 6, 'SW': 75, '변경': 34, '일정': 2}
  ✅ `GET /changes` → 200 | 「변경 알림 — HAIST WORKS」 | 2011ms 
  ✅ `GET /changes/new` → 200 | 「변경 등록 — HAIST WORKS」 | 2072ms 
  >> [변경 등록 change_type 카테고리] {'기구': 8, '전기': 0, '전장': 8, 'SW': 68, 'BOM': 12, '도면': 12, '일정': 2, '컨셉': 0}

**발견 1건**: 변경 등록 change_type 카테고리 누락: ['전기', '컨셉']