# [팀장 → 04 운영테스트팀] 재시뮬레이션 요청 — 2026-04-21

> **발신**: 09 프로젝트팀장
> **계기**: 01 메인 세션이 D01-01 (`/api/set-lang` 405) 수정 완료 회신 (`01_HAIST_WORKS/_TO_09팀장_2026-04-21_회신.md` 참조)
> **변경 요지**: `app/main.py` L840~L868. POST-only → `@app.api_route(methods=["GET","POST"])`. GET 은 `?lang=vi` 쿼리 수용 + `Referer` 로 303 리다이렉트. POST(JSON body) 는 기존대로 유지.
> **커밋 여부**: 미커밋 (working tree 상태)

## 요청 시나리오

### 1) P10 쑤아잉 — GET 쿼리스트링 재검증
- `GET /api/set-lang?lang=vi` → **303** (Referer 로 리다이렉트) 기대
- `GET /api/set-lang?lang=en` → **303** 기대
- 이후 `GET /home` 시 페이지 렌더링 언어가 vi/en 로 반영되는지 확인
- DB: `users.lang` 값 실제로 `vi` 또는 `en` 으로 갱신됐는지 확인

### 2) 회귀 확인 — 기존 POST 경로
- `base.html` 의 언어 셀렉터(fetch POST JSON) 한 번 트리거 → **200** `{"ok":true,"lang":"ko"}` 등 기대

### 3) 권한 경계 재확인
- 비로그인 상태에서 `GET /api/set-lang?lang=vi` → **401 "로그인 필요"** 기대

## 산출물
- `02_테스트실행로그_2026-04-22_재시뮬_set-lang.md` (또는 동일 파일 append)
- 이슈 발견 시 S1 처리 후 01 에 반환

## 부가 요청 — D04-04 "일이 늘어나는가" 판정 포함
- 이 재시뮬 결과에 베트남 직원 관점 "UI 언어 전환이 실제로 일 경감이 되는가" 소견 한 줄 추가.
