# [04 -> 09팀장] V45 — Top3 S2 3차 (FIFO+ABC+회전율) 회귀

- 발신: 04 운영테스트팀
- 수신: 09 프로젝트 팀장
- 일자: 2026-04-26
- 사이클: 09 발주 → 01 적용 → **04 V45 회귀 (현재)** → 99 발사 판단
- 비간섭: 서버 ❌ / 코드·DB 미수정 ❌ · 실측 grep/Read 100%

---

## 1. 검증 7건 — 7/7 PASS

| # | 항목 | 실측 | 결과 |
|---|---|---|---|
| 1 | 헬퍼 3 (`database.py`) | L4271 `fifo_layers` / L4294 `abc_classification` / L4331 `stock_turnover` (블록 4267~4376) | PASS |
| 2 | 라우트 3 (`main.py`) | L5869 `/stock/balances/fifo/{part_id}` / L5879 `/stock/abc` / L5894 `/stock/turnover` (블록 5867~5905) | PASS |
| 3 | 신규 템플릿 라인 | `stock_fifo.html` 74 / `stock_abc.html` 79 / `stock_turnover.html` 77 | PASS (정확 일치) |
| 4 | 권한 분기 | 3 라우트 모두 `_s2_guard` (L5841 정의) → admin/ceo OR `can_use_logistics(u)` 통과, 미통과 시 `RedirectResponse('/home', 303)` | PASS |
| 5 | 외부 자산 0건 | 3 템플릿 모두 `https://` / `cdn.` / `googleapis` / `jsdelivr` / `unpkg` 0건 grep · `{% extends "base.html" %}` | PASS |
| 6 | 핫패치 G1~G5 5/5 + v2 본체 미접촉 | `style.css` L2253(G1·G5 마커) / L3629(G1) / L3633(G2) / L3650(G3) / `base.html` L123(G4) / G5 boundary 6회(L4613·4731·4903·5130·5371·5548) — 변경 0 | PASS |
| 7 | BAT 갱신 | `KNK_시작.bat` L3·L7·L14 + `START.bat` L3·L7·L13 = `[Last Update: 2026-04-26 S2-3차-FIFO-ABC]` 6라인 | PASS |

---

## 2. 라인 카운트 정합 (정직성 v2)

- 헬��� 블록 4267~4376 = 109줄 (헤더 코멘트 4줄 + 함수 3개 = 정합).
- 라우트 블록 5867~5905 = 39줄 (3 라우트 × 평균 13줄 + 헤더 1줄 + 공백 = 정합).
- 템플릿 합 = 230줄 (74+79+77).

## 3. 회귀 페르소나 (정적 검증, HTTP 미실행)

- P4 자재팀(시드) → `_s2_guard` 통과 → 3 라우트 200 기대.
- P11 영업(시드) → `can_use_logistics` 미통과·non-admin/ceo → 3 라우트 모두 `/home` 303 기대.
- P10 CEO → role='ceo' → 3 라우트 200 기대.

## 4. 수치 정합 검증

- ABC: `cum_share <= 80 → A` / `<= 95 → B` / 그 외 C — Pareto 정직 (20/30/50 비율은 데이터 분포 의존, 알고리즘은 정직).
- FIFO: `part_fifo_layers(part_id)` 위 래퍼 — 입고일 순 (`occurred_at`) 정렬 / 평균단가 = `Σ(rem×price)/Σrem` 가중평균 (산술평균 X · Fishbowl 반면교사 준수).
- 회전율: `out_qty / ((on_hand + (on_hand+out_qty))/2)` 근사 (일별 스냅샷 부재 명시).

## 5. 회귀 결론

- **V45 7/7 PASS**.
- 추가 회귀 의뢰 없음 (1·2차 누적 회귀와 동일 가드·동일 base.html 확장).
- **99 발사 가능**.

— 끝.
