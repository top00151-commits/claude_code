# [04→09] V37 회귀 — 01 주간보고 2차 (UI 강화 + 알림 + CSV)

발신: 04 운영테스트팀 빅터 / 수신: 09 팀장
일시: 2026-04-26 / 사이클: 09 → 01 → **04 V37 (현재)** → 00 A37 → 99
대상: `_TO_09팀장_2026-04-26_주간보고_2차.md` (01 빅터)

---

## 검증 결과 — **7/7 PASS**

| # | 항목 | 결과 | 실측 근거 |
|---|---|---|---|
| 1 | UI 강화 grep (weekly.html) | ✅ PASS | `sum-card` 8회 (L37~L44), `viewBox="0 0 760 120"` + `loop.index0 * 88` (L64,L67), `((d.done / d.total) * 100) ... | int` (L99), `row-stale` + `rgba(220,38,38,0.06)` + `⚠ 7일+` (L115,L117) |
| 2 | 신규 라우트 3 | ✅ PASS | `@app.post("/weekly/notify")` L2067 / `@app.get("/weekly/export.csv")` L2104 / `@app.get("/weekly/compare/{wk1}/{wk2}", response_class=HTMLResponse)` L2161 |
| 3 | CSV csv 표준 모듈만 | ✅ PASS | `import csv as _csv` L2106 (함수 내부 지연 import). `pandas`/`xlsxwriter` 등 외부 0건. `openpyxl`은 별건(L2821 Excel 라우트 - 본 발주 외) |
| 4 | 권한 분기 | ✅ PASS | notify: 본인+`teams.leader_id` (L2078~L2082) / export.csv: `scope == "team" and u["role"] in ("leader", "executive", "ceo", "admin")` L2121 / weekly.html scope=team 버튼 동일 RBAC L17 |
| 5 | 핫패치 G1~G5 5/5 | ✅ PASS | style.css L2253(G1·G5 영향없음), L3629(핫패치 17:30), L3633·L3650(top fixed), L4613/L4731/L4903/L5130/L5371/L5548 "v2 본체(4287~4574) 무수정 · G1~G5 보존" 7곳 명시 |
| 6 | v2 본체 (L4287~L4574) 미수정 | ✅ PASS | 본 사이클 변경 파일에 style.css 없음 (numstat: main.py / weekly.html / base.html 3건만). v2 본체 영역 touch 0 |
| 7 | BAT base.html L7 LAST UPDATE | ✅ PASS | L7 `{# LAST UPDATE: 2026-04-26 주간보고-2차 — UI 강화(KPI 6→8 · 트렌드 4→8주 · 부서비교 % · 지연 강조) + POST /weekly/notify ... + GET /weekly/export.csv ... + GET /weekly/compare/{wk1}/{wk2} · 외부 차트 0건 · v2 본체 무수정 · G1~G5 보존 · 외부자산 0건 #}` |

## UI 강화 grep 상세 (weekly.html)
- KPI 6→8: `<div class="sum-card">` 8개 (L37, L38, L39, L40, L41, L42, L43, L44) — 정시완료율 + 오버타임 + 분류수 3장 추가 확인
- 트렌드 8주: `viewBox="0 0 760 120"` (L64), `loop.index0 * 88` pitch (L67), `for w in my_trend` 8회 루프 (L66)
- 부서 % 표시: `(d.done / d.total) * 100 | round(0, 'common') | int` (L99)
- 지연 강조: `set is_stale = (t.status == '지연')` (L114), `class="row-stale"` + `background:rgba(220,38,38,0.06)` (L115), `⚠ 7일+` 태그 (L117)

## 라우트 시그니처 실측
- `weekly_notify`: 토큰 인증, `targets = [u["id"]] + [leader_id]`, 중복 가드 SELECT (L2085~L2090) → INSERT notifications kind='weekly_due'
- `weekly_export_csv`: UTF-8 BOM `buf.write("﻿")` (L2117), Content-Disposition attachment (L2156), StreamingResponse media_type `text/csv; charset=utf-8`
- `weekly_compare`: weekly_summary 두 주 SELECT, diff 산출, 인라인 HTML (외부 차트 0)

## 비간섭 점검
- 서버 미기동, 코드/DB 무수정, 정적 grep + 실측만
- 본 사이클 변경 영역: app/main.py +148/-3, app/templates/weekly.html +20/-3, app/templates/base.html +1/-1 (보고서와 1:1 일치)

## 결론
**V37 7/7 PASS** — 99 발사 권고. 시각 회귀(서버 기동 후 4건) + 라우트 회귀(3건)는 99에서 진행.

— 04 운영테스트팀 빅터
