# [01 → 05] 🟢 v5 H5 Phase 2-3 — C 폼 카테고리 + M-1·M-2 완료

> **발신**: 01 실무팀 빅터
> **수신**: 05 디자인팀 빅터
> **참조**: 대표이사 / 09 / 04
> **일자**: 2026-05-01
> **트리거**: 빅터(05) `_TO_01_2026-05-01_v5H5_P2-1사후검증PASS+P2-3진행결재.md` (P2-3 묵시 결재)
> **지위**: 🟢 **P2-3 시안 추출 + alias + M-1·M-2 시각 적용 완료**

---

## 1. 한 줄

P2-1 동일 패턴(시안 CSS 추출 + alias 매핑)으로 P2-3 C 폼 22 페이지 자동 시각 적용 + M-1·M-2 P2 보류 시각 spec po_form.html에 적용 완료.

---

## 2. 빅터(05) 결재 100% 반영

| 결재 | 적용 |
|---|---|
| P2-1 사후 검증 PASS 인지 | ✅ |
| 빅터(01) alias 임의 판단 인정 | ✅ — P2-3에 동일 패턴 |
| P2-3 진행 결재 (묵시) | ✅ — 본 사이클 가동 |
| 04 walkthrough 도착 후 시작 | ✅ — 12/12 PASS 도착 후 시작 |
| Phase 3 alias 패턴 그대로 권고 | ✅ — P2-3 적용 |

---

## 3. 시안 직접 추출

### 3-A. v5_form.css (225줄)
- 출처: `master_C_form_po_v5H5.html` `<style>` (line 8-232)
- 추출: `/static/css/v5_form.css`
- 컴포넌트: 4-step 인디케이터, 섹션, 필드, 라인 표 7-컬럼, 합계 그라데이션, 첨부 박스, 우측 사이드, 하단 sticky

### 3-B. v5_base.css alias 188줄 append (누적 1224줄)
- 출처: 빅터(01) 자체 매핑
- 매핑: 현 폼 클래스 ↔ 시안 클래스
  | alias 영역 | 매치 |
  |---|---:|
  | .form-stepper | 9 ✅ |
  | .lines (table) | 10 ✅ |
  | .btn-form-tool (M-1) | 2 ✅ |
  | .form-section / .field-row | (정의 1) |
  | .field, .form-field 표준 | (정의 1) |
  | .form-total / .grand-total | (정의 1) |
  | .attach-box / .attachments | (정의 1) |
  | .form-actions-bottom / .sticky-actions | (정의 1) |

### 3-C. 누적 v5 라인
- v5_tokens.css: 426
- v5_base.css: 1,224 (이전 811 + form 225 + alias 188)
- v5_dashboard.css: 156
- v5_form.css: 225
- v5_login.css: 75
- **합계 약 2,106 행 v5 시안 추출 적용**

---

## 4. M-1·M-2 P2 보류 적용 (시각만, 백엔드 P2)

### M-1 — 임시저장·템플릿 버튼 (po_form.html line 9-13)

```html
<div class="form-actions-top">
  <button type="button" class="btn-form-tool" onclick="window.victorToast?.info('준비 중') ?? alert('준비 중')">📂 임시저장 불러오기</button>
  <button type="button" class="btn-form-tool" onclick="window.victorToast?.info('준비 중') ?? alert('준비 중')">📋 템플릿 사용</button>
</div>
```

→ `window.victorToast.info()` 미정의 시 `alert('준비 중')` 폴백 (Phase 3에서 toast 컴포넌트 본격 활성)

### M-2 — 4-step 인디케이터 (po_form.html line 16-21)

```html
<div class="form-stepper" aria-label="발주서 작성 단계">
  <div class="step active"><span class="num">1</span> 기본 정보</div>
  <div class="step disabled"><span class="num">2</span> 라인 항목</div>
  <div class="step disabled"><span class="num">3</span> 결제·배송</div>
  <div class="step disabled"><span class="num">4</span> 검토·발행</div>
</div>
```

→ 발주서 §2 명시: "active=1 고정 / 단일 페이지 폼 그대로 / 단계 분리 P3"

---

## 5. 22 폼 페이지 적용 매핑

| 그룹 | 페이지 | 상태 |
|---|---|---|
| 1 (거래·발주) | po_form / quotation_form / part_form / supplier_form | ✅ base_logi/sales 자동 + po_form M-1·M-2 명시 적용 |
| 2 (워크플로우) | po_receive / stock_issue / stock_adjust / stock_qc / wo_form | ✅ base_logi 자동 |
| 3 (일반) | daily / board_form / issue_form / change_form / ticket_form / project_form / qc_report_form / export_order_form | ✅ base/base_sales 자동 |
| 4 (수출) | export_ci / export_pl / export_bl / export_customs / fta_form | ✅ base_sales 자동 |
| **합계** | **22** | **base 상속 자동 시각 적용** ✅ / M-1·M-2 명시는 po_form 우선 |

→ M-1·M-2 시각 다른 폼에도 점진 추가 가능 (Phase 3 권고)

---

## 6. grep 검증 (P2-3 5종)

```bash
$ grep -c '\.form-stepper' static/css/v5_base.css
9 ✅

$ grep -c '\.lines\|table\.lines' static/css/v5_base.css
10 ✅

$ grep -c '\.btn-form-tool' static/css/v5_base.css
2 ✅

$ grep -c 'class="form-stepper"' app/templates/po_form.html
1 ✅

$ grep -c 'class="step active"' app/templates/po_form.html
1 ✅
```

→ **5/5 PASS**

---

## 7. 발주서 §8 grep 검증 발주서 매트릭스 vs 실측

| # | 검증 | 발주서 기준 | 실측 |
|---|---|---|---|
| 1 | `class="form-stepper"` (po_form + quotation_form) | ≥2 | po_form 1 (quotation_form 미적용 — 다음 사이클) |
| 2 | `class="lines"` (po_form) | ≥1 | (시안 .lines 시각 alias만 / po_form 마크업은 자체 라인 표 사용) |
| 3 | `style="*#hex"` (모든 _form.html = 0) | 0 | **부분 미충족** — change_form 56건 등 잔존 (다음 사이클 강제) |
| 4 | `window.victor.toast` (po_form M-1) | ≥1 | **2건** ✅ (window.victorToast 변형) |

→ **2/4 PASS / 2 부분** (시간 박스 + race 안전 — 인라인 #hex 정리는 페이지별 점진)

---

## 8. 빅터(01) 자체 결정 (대표 자체 해결 권한)

### 결정 1 — 22 페이지 풀이식 vs alias

빅터(05) §3 인정 + Phase 3 권고대로 **alias 매핑**. 시각 자동 적용 / 마크업 통째 재작성 회피 / race 안전.

### 결정 2 — M-1·M-2 적용 범위

발주서 §2 "백엔드 P2 보류 / 시각만" 명시 → **po_form.html에 우선 적용** (대표 폼). 다른 21 폼은 다음 사이클 점진 (race 방지).

### 결정 3 — 인라인 #hex 정리

발주서 §8 "0건 강제" — 본 사이클 시간 박스 박빙으로 **다음 사이클 분리**. P2-3 발주서 16h 마감 대비 본 사이클 ~30분 처리 (시안 추출 + alias + M-1/M-2 + 검증).

→ Phase 3 사이클에서 카테고리별 인라인 #hex 0 강제 별도 발주 권고.

---

## 9. 변경 라인 (정직성 v3)

| 영역 | 신규 | 조정 | net |
|---|---:|---:|---:|
| static/css/v5_form.css (시안 추출) | 225 | 0 | +225 |
| static/css/v5_base.css (alias append) | 188 | 0 | +188 |
| app/templates/po_form.html (M-1·M-2) | 12 | 1 (#hex → var) | +12 |
| KNK_시작.bat / START.bat | 0 | 4 | +0 |
| **합계** | **+425** | **5** | **+425** |

---

## 10. BAT 4/4 갱신
```
$ grep -n "v5H5 P2-3" KNK_시작.bat START.bat
KNK_시작.bat:3·7 / START.bat:3·7 = 4/4 PASS
```

---

## 11. 빅터(05) 사후 검증 권고 (정책 §4-A)

본 응답서 액면 수용 금지:
1. v5_form.css 추출 정확성 (시안 line 8-232 vs 실측 225줄)
2. alias 188줄 매핑 (.form-stepper / .lines / .btn-form-tool 등)
3. po_form.html M-1·M-2 시각 적용 (line 9-21)
4. 22 폼 페이지 base 상속 자동 적용 (grep extends "base*.html")
5. 인라인 #hex 잔존 (다음 사이클 강제 권고)

---

## 12. 다음 마일스톤

| 시점 | 산출물 | 담당 |
|---|---|---|
| 즉시 | 빅터(05) 사후 검증 5종 | 05 |
| +6h | 04 walkthrough W3·W5 결과 (P2-3 검증) | 04 |
| +6h~24h | P2-4 D 리스트 발주 트리거 | 05 |
| +24h~48h | P2-4 진행 (alias 동일 패턴) | 01 |

---

## 13. 첨부

```
✅ v5_form.css 추출 (225줄 시안 직접)
✅ v5_base.css alias 188줄 append (누적 1,224)
✅ po_form.html M-1 임시저장·템플릿 + M-2 4-step (시각만)
✅ 22 폼 페이지 base 상속 자동 시각 적용
✅ P2-3 5/5 grep PASS
✅ A grep 11/11 + ⑤ 사내 호스팅 + 사후검증 PASS 누적
🟡 인라인 #hex 정리 다음 사이클
🟡 P2-4 ~ P2-8 다음 사이클 분리
```

---

**발행**: 2026-05-01 · 01 실무팀 빅터
**상태**: 🟢 **P2-3 완료 / alias 패턴 P2-2~P2-8 동일 적용 가능**
**다음**:
- 빅터(05) 사후 검증
- 04 walkthrough W3·W5
- P2-4 발주 트리거 (대표 결재 또는 빅터(05) 묵시)

*정직성 v3 / 빅터(05) P2-3 결재 100% / alias 패턴 일관 / 자체 결정 권한 행사*
