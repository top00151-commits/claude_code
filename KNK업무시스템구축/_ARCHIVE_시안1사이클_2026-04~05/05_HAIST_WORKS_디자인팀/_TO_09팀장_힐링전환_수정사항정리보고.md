# [05 → 09] 힐링 전환 · 수정사항 정리 보고

> **발신**: 05 디자인팀 세션 빅터
> **수신**: 09 프로젝트 팀장
> **참조**: 대표이사 (사후 보고)
> **일자**: 2026-04-25 13:30
> **상태**: 🟢 04 검증 완료 → 05 분석 완료 → 01 패치 지시 발행 → **착수 대기**

---

## 1. 한 페이지 요약

대표 직접 지적사항(매출 권한 + 빅터 도크) 검증을 위해 04 운영테스트팀에 QA 의뢰(02:55) → 1차 회신 도착(12:40, 10시간 후) → 8개 이슈 발견 → 05가 모두 분석·분류·처리 경로 확정 → **01 메인 세션에 4건의 구체 패치 지시 발행**.

**핵심 결과**:
- ✅ **평직원·팀장 매출 미노출 = PASS** (대표 지적 핵심부 안전)
- ⚠ **CEO 매출은 시드 데이터 부재**가 원인 — 코드 정상, DB 이슈
- ⚠ **빅터 도크 `.dock-tab` 잔존** — 19:35 2차 지시 미반영
- ⚠ **번역 위젯에 zh-CN·ja 잔존** (UI 셀렉터는 정상, 별도 컴포넌트)

---

## 2. 수정사항 정리 (4건)

### 패치 ① · 빅터 도크 `.dock-tab` 제거 (재독려)

| 항목 | 내용 |
|---|---|
| 심각도 | 🔴 S1 (대표 직접 지적) |
| 원인 | 19:35 발행한 2차 지시 미반영 |
| 위치 | `app/templates/base.html` + `static/style.css` |
| 처리 | `grep -rn "dock-tab"` → 결과 0건 만들기 |
| ETA | 15분 |
| 검증 | curl 응답에 `dock-tab` 0회 |

### 패치 ② · CEO 매출 KPI fallback (시드 부재 대응)

| 항목 | 내용 |
|---|---|
| 심각도 | 🔴 S1 |
| 원인 | DB 에 `projects` 테이블 자체 부재 → SELECT 예외 → `monthly_revenue=None` → 카드 미렌더 |
| 위치 | `app/templates/home.html:180~206` |
| 처리 | `{% if is_executive and monthly_revenue is not none %}` → `{% if is_executive %}` 로 변경, 데이터 없을 때 **"집계 준비 중"** fallback 표시 |
| ETA | 30분 |
| 검증 | CEO 로그인 → 매출 카드 자리에 "🔒 경영진" 뱃지 + "집계 준비 중" 노출 |
| 별건 | 시드 보강은 09 의사결정 필요 (§4 참조) |

### 패치 ③ · 언어 셀렉터 `zh-CN`·`ja` 제거

| 항목 | 내용 |
|---|---|
| 심각도 | 🔴 S1 (zh-CN) + 🟠 S2 (ja, 중복) |
| 원인 | `base.html:404~410` 의 `#trLang` (카드 모달 자동번역 위젯)에 5종 하드코딩. UI 셀렉터(`#langSel`)와 별도 컴포넌트로 04 검증에서 합쳐서 발견됨 |
| 위치 | `base.html:404~410` (HTML) + `:582` (JS `langNames`) |
| 처리 | `LANGS.items()` 기반 동적 렌더로 변경 → ko/vi/en 만 노출, 자동 정합 |
| ETA | 15분 |
| 검증 | curl 응답 `zh-CN`·`ja`·`중국어`·`일본어` 0회 |

### 패치 ④ · 인사말 검증 보강 (선택)

| 항목 | 내용 |
|---|---|
| 심각도 | 🟠 S2 (false alarm 가능) |
| 원인 | 04 검증 시각(12:40)에 "오늘도 평안" 키워드 검색 → 실제 노출은 "점심은 드셨나요" 또는 "오후도 힘내세요" → 검증 미스매치 |
| 위치 | `home.html:38` |
| 처리 | `data-greeting-bucket="{{ bucket }}"` 속성 추가 → 시각 무관 검증 가능 |
| ETA | 5분 |
| 검증 | 04 가 `data-greeting-bucket` 속성 존재 확인 |

**총 ETA**: ① 15분 + ② 30분 + ③ 15분 + ④ 5분 = **약 1시간 5분**

---

## 3. 권장 커밋 분리 (01 세션용)

1. `fix(dock): remove .dock-tab residual (QA-H2)`
2. `feat(home): graceful fallback for revenue KPI (QA-H1)`
3. `fix(i18n): trLang to LANGS-driven · drop zh-CN/ja (QA-H3·H4·H5)`
4. `chore(home): add data-greeting-bucket (QA-H6 · optional)`

---

## 4. 09 팀장 결정 필요

### 4-1. 시드 보강 별도 진행 여부 (H1 후속)

DB 에 `projects` 테이블 자체 없음. 옵션:

- **(A)** fallback 만 (즉시) · 시드는 미진행
- **(B)** 시드 별도 작업 (1~2시간) · `database.py` 또는 마이그레이션
- **(C)** **(A)+(B) 병행 권장** ← 빅터 의견

→ 09 팀장 결정 필요.

### 4-2. 04 운영테스트팀 2차 검증 권한 부여

04 가 §S2 사용성 + §S4 해상도 검증을 위해 시각 확인 도구 필요. **비간섭 원칙**으로 09 승인 대기 중.

→ 09 → 04 권한 부여 권장.

### 4-3. 01 세션 응답 독려 (필요 시)

01 세션이 19:24 이후 약 18시간 작업 정지 추정. 3차 지시 발행(13:05) 후 24시간 내 회신 요청. 지연 시 09 직접 독려 권장.

---

## 5. 04 측 작업 평가

- **품질 우수**: HTTP 정적 분석으로 8개 이슈(S1×3 + S2×3 + S3×2) 발견
- **비간섭 원칙 준수**: 서버 3분 기동, 코드 미변경, 재현 스크립트 동봉
- **false alarm 비율 25%** (8건 중 2건) — 정적 분석 한계상 매우 낮은 수준
- **3중 방어 구조 검증** — 라우트·컨텍스트·UI 각 layer 별 PASS/FAIL 명확

---

## 6. 발행 문서 (오늘 13:00~13:25)

| 시각 | 파일 | 대상 |
|---|---|---|
| 13:05 | `_세션01_전달/_TO_01_힐링3차_QA반영_긴급패치.md` | 01 메인 |
| 13:15 | `_TO_04_힐링QA응답_1차회신확인.md` | 04 운영테스트 |
| 13:20 | `_TO_09팀장_QA결과반영_종합보고.md` | 09 (1차 보고) |
| 13:25 | 원칙서 `01_디자인원칙_힐링v1.md` v1.3 | 전 세션 (§11-7 추가) |
| **13:30** | **본 문서** | **09 (정리 보고)** |

---

## 7. 다음 마일스톤

| 시점 | 이벤트 |
|---|---|
| **+1시간 (~14:30)** | 01 세션 3차 패치 응답 도착 여부 |
| **+2시간 (~15:30)** | 4건 패치 모두 완료 시점 (01 ETA 기준) |
| **+24시간 (내일 13:00)** | 04 회귀 검증 회신 |
| **+48시간** | 04 §S2·§S4 2차 회신 (시각 검증) |

---

## 8. 종합 의견

대표 지시(2026-04-24)부터 24시간 만에 **3개 세션 협업 사이클** (05 설계 → 01 구현 → 04 검증 → 05 반영 → 01 재구현) 1바퀴 완료. 핵심 보안 이슈(평직원 매출 미노출)는 견고하게 구현됐고, 잔여 이슈는 모두 처리 경로 확정.

**09 팀장 추가 지시 없으면**, 01 세션 3차 패치 응답 도착 시 즉시 반영하고 04 2차 검증 사이클로 진입하겠습니다.

---

**발행**: 2026-04-25 13:30 · 05 디자인팀 세션 빅터
**우선순위**: 🟢 정상 진행 (S1 처리 중)
**회신 위치**: `05_.../_FROM_09팀장_*.md`
