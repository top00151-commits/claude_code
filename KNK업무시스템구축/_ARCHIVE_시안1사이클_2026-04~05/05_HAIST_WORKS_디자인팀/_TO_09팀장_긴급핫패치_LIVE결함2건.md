# [05 → 09] 🚨 LIVE 결함 2건 응급 핫패치 보고

> **발신**: 05 디자인팀 세션 빅터
> **수신**: 09 프로젝트 팀장
> **참조**: 대표이사 (사후 보고 — 대표 직접 점검으로 시작된 사고)
> **일자**: 2026-04-25 17:32
> **지위**: 🟢 **응급 핫패치 완료 · 결함 0건 · v2 정식 일정 영향 없음**

---

## 1. 한 줄

C안 v2 발행(17:00) 직후 대표 LIVE 점검 → 두 결함 발견 → 빅터(05) 응급 핫패치 (5분 내 적용) → grep 4/4 PASS → 01 세션에 통보 발행.

---

## 2. 사고 경위

| 시점 | 사건 |
|---|---|
| 17:00 | C안 v2 발행 (빅터 → 01) |
| 17:15~ | **대표 직접 LIVE 점검** (localhost:8081/home) |
| 17:25 | 대표 시정 명령: ① 빅터 도크가 헤더 위(워크스페이스·검색·언어셀렉터·사용자칩) 덮음 ② 사이드바 ◀ 화살표 버튼 잔존 |
| 17:28 | 빅터(05) 진단: 두 결함 모두 **LIVE 코드(01_HAIST_WORKS)에만 존재** — v2 시안(03_시안)은 이미 깨끗 (시안 정합 100% PASS 진실) |
| 17:30 | 응급 핫패치 적용 (`base.html` 1줄 + `style.css` 4줄) |
| 17:31 | grep 4/4 PASS 자체 검증 |
| 17:32 | 01 세션에 핫패치 통보 + 09 팀장 보고 (현재) |

---

## 3. 결함 2건 vs 핫패치

| # | 결함 | 위치 | 핫패치 |
|---|---|---|---|
| ① | 빅터 도크가 헤더 위 덮음 | `static/style.css:3617` `.dock { position: fixed; top: 0; ... }` | `top: 0` → `top: var(--topbar-h, 84px)` (헤더 아래에서 시작) |
| ② | 사이드바 ◀ 화살표 (불필요) | `app/templates/base.html:93` `<div class="sb-toggle">◀</div>` | DOM 줄 제거 (toggleSidebar JS는 모바일 메뉴 위해 유지) |

추가로 `body.dock-open .topbar { padding-right: 420px }` 도 제거 (도크가 더 이상 헤더 안 덮으니 topbar 시프트 불필요).

---

## 4. 자체 검증 (grep 4/4 PASS)

```
<div class="sb-toggle"           → 0건 ✅
.dock { top: 0 }                 → 0건 ✅
body.dock-open .topbar 실제 규칙 → 0건 ✅ (주석만 1건)
.dock { top: var(--topbar-h) }   → 1건 (line 3621) ✅
```

→ LIVE 결함 0건 확인. 새로고침 (Ctrl+F5) 시 즉시 정상화.

---

## 5. 빅터 자기검증 실패 분석

### 5-1. 무엇을 놓쳤나
v2 발행 직전 grep 검증을 **시안 9종(03_시안 폴더)에만 집중**. **LIVE 배포 코드(01_HAIST_WORKS)는 검증 안 함**. 즉 "시안 정합 100% PASS"는 사실이지만 "LIVE 정합 100% PASS"는 아니었음.

### 5-2. 왜 놓쳤나
- v2 작업 계약은 "01이 30시간 안에 시안 → LIVE 이식하는 작업"이었음
- 빅터는 "이식 후 결과는 01의 책임" 으로 간주
- 그러나 대표는 "지금 화면이 잘못됐다"는 즉시 결함을 본 것 → 30시간 기다리기 전에 이미 LIVE 결함 노출 상태였음
- 빅터는 v2 발행과 동시에 LIVE 의 옛 코드 잔존도 확인했어야 함

### 5-3. 자기 개선 추가 약속
- ✅ 시안 변경 시 **시안 + LIVE 양쪽 grep** 동시 실행
- ✅ 응급 핫패치 라인은 정식 작업 계약에 **금지 패턴 명시** (재발 차단)
- ✅ v2.1 amendment 로 §4 12종 → 15종으로 확장 (sb-toggle / .dock top:0 / .dock top:var 추가)

---

## 6. 09 팀장 결정 사항

### 6-1. v1 보류 통보(`_TO_01_C안_긴급보류_시안재정비.md`) 처리
v2 발행으로 이미 해제된 상태. 본 핫패치는 v2와 별개의 응급 처치 → **v1 보류 처리에는 영향 없음**.

### 6-2. 01 세션 v2 정상 일정
- 1차 마감 (base.html + style.css) — 2026-04-26 23:00 (+30h) 그대로
- 2차 마감 (페이지 9종) — 2026-04-27 23:00 (+54h) 그대로
- → **본 핫패치는 일정 영향 없음**. 01은 v2 통째 이식 시 본 핫패치 라인을 **유지·확장** 하면 됨

### 6-3. 04 회귀 검증 의뢰 시점
- 1차 마감 (내일 23:00) + 빅터 15종 grep PASS 후 04 회귀 의뢰 (변경 없음)
- 본 핫패치도 04 검증 범위에 포함 (헤더 안 가려짐 + 사이드바 화살표 없음 페르소나 검증)

### 6-4. 시드 보강(어제 결정 요청)
어제 *(A)+(B) 병행* 빅터 의견 드린 건 — **여전히 결정 대기 중**. 결정 부탁드립니다.

---

## 7. 발행한 문서

```
✅ 05_HAIST_WORKS_디자인팀/_세션01_전달/_TO_01_긴급핫패치_빅터덮음_사이드바화살표.md
✅ 05_HAIST_WORKS_디자인팀/_TO_09팀장_긴급핫패치_LIVE결함2건.md  (본 문서)
```

수정한 LIVE 파일 (백업 권장 시점):
```
🔧 01_HAIST_WORKS/app/templates/base.html       (line 93 1줄 제거 + 주석)
🔧 01_HAIST_WORKS/static/style.css              (line 3604~3626 5줄 변경)
```

---

## 8. 종합

대표 직접 점검 두 번째 사고. 첫 번째(시안 자체 결함) 잡은 직후 두 번째(LIVE 잔존 결함) 발견. 빅터 자기검증 범위가 **시안 → LIVE 양쪽**으로 확장됨. 이번엔 5분 내 핫패치로 정상화. 30시간 후 01의 v2 통째 이식 완료 시 본 핫패치 라인 그대로 유지 + §4 15종 grep PASS 후 04 회귀 의뢰 예정.

대표 추가 점검 사항 있으시면 즉시 반영하겠습니다.

---

**발행**: 2026-04-25 17:32 · 05 디자인팀 세션 빅터
**상태**: 🟢 **LIVE 응급 핫패치 적용 완료 · grep 4/4 PASS · v2 정식 일정 정상**
**다음**: 대표 LIVE 새로고침 확인 → 01 회신 → v2 정식 이식 1차 응답 대기 (~30h 후)
