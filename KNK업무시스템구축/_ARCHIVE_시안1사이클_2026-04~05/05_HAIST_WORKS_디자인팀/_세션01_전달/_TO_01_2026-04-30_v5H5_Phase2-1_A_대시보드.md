# [05 → 01] 🟢 v5 H5 Phase 2-1 — A 대시보드 카테고리 (5 페이지)

> **발신**: 05 디자인팀 빅터
> **수신**: 01 실무팀
> **참조**: 대표이사 / 09 / 04
> **일자**: 2026-04-30
> **지위**: 🟢 **반영 발주서 — 시안 직접 이식 (해석 금지)**
> **순서**: Phase 2 1번 (사용자 영향 우선)
> **마감**: 본 발주 후 4시간 (24h 권고 대비 단축 — 5 페이지 규모)

---

## 1. 적용 대상 (5 페이지)

| # | 라우트 | 템플릿 | base | M-코드 | 변형 키 | 모바일 우선 |
|---|---|---|---|---|---|---|
| 1 | `/dashboard`·`/ceo` | `dashboard.html` | base | M-05-01 | C 대표 전사 뷰 | — |
| 2 | `/home` | `home.html` | base | M-00-01 | **A 마스터 자체** | **✓** (P0) |
| 3 | `/sales` | `sales_home.html` | base_sales | M-01-01 | B 매출 hub | — |
| 4 | `/logistics` | `logistics_home.html` | base_logi | M-02-01 | D 자재 hub | — |
| 5 | `/cockpit` | `cockpit.html` | base | M-05-03 | E 24 KPI | — |

## 2. 시안 위치

```
03_시안/_05_v5H5_풀세트시안/A_대시보드/
├── master_A_dashboard_home_v5H5.html  ← 마스터 (/home 기준)
└── variations.md                      ← 5 변형 spec (A·B·C·D·E)
```

## 3. 핵심 컴포넌트 적용

1. **3 탭** (내 업무·우리 팀·전사) — `<div class="scope-tabs">` (home만)
2. **벤토 5 카드** (히어로 + 4 KPI)
3. **빠른 액션 4개** (`.action-card`)
4. **알림 피드 4건** (`.feed-item` 2-컬럼)

## 4. P2 보류 동시 적용

없음 (M-1·M-2·M-3·M-4는 C·D·E 카테고리)

## 5. Jinja2 inject map (시안 시각 100% + 동작 100%)

| 시안 영역 | inject |
|---|---|
| `<h1 class="page-title">반갑습니다, <em>김 부장님</em></h1>` | `{{ user.name }}님` |
| `class="card hero"` 통계 23/8/3 | `{{ progress_count }}` `{{ po_pending }}` `{{ stock_alert }}` |
| `<div class="scope-tab active">내 업무</div>` | URL hash `#my` `#team` `#all` 분기 |
| `<a class="action-card">` 4개 | `{{ url_for(...) }}` action |
| `.feed-item` | `{% for n in notifs[:4] %}` 루프 |

## 6. 시안 vs 현 코드 충돌 가능성

- `home.html` 현재 P0 변환 다수 (응답서 §3-E 19건 #hex) → **본 발주 시 인라인 #hex 0건** 강제
- `dashboard.html`·`cockpit.html` 권한 가드 `require(req, ['ceo','admin','executive'])` 보존
- `sales_home.html`·`logistics_home.html` 각 hub 배너 색 변경 (KNK red / 워밍 흙갈색)

## 7. grep 검증 5종 (카테고리별)

```bash
# A-1. 신규 패턴
grep -c 'class="bento"\|class="scope-tabs"\|class="action-card"' app/templates/home.html  # ≥3

# B-1. 옛 패턴 (PASS = 0)
grep -cE 'var\(--sage-[0-9]+\)' app/templates/home.html app/templates/dashboard.html \
  app/templates/sales_home.html app/templates/logistics_home.html app/templates/cockpit.html
  # 모두 = 0

# D-1. 인라인 #hex (PASS = 0)
grep -cE 'style="[^"]*#[0-9a-fA-F]{3,6}' app/templates/home.html  # = 0 (현 19 → 0)

# C-1. 페이지별 v5 토큰 사용
grep -c '\-\-amber\|\-\-paper\|\-\-knk-red' app/templates/home.html  # ≥3

# E. 페르소나 walkthrough — W1 / W9 (04팀 협조)
```

## 8. 04 walkthrough

- **W1**: 김 부장 로그인 → /home → 견적 23건 인지 (히어로 카드)
- **W9**: 대표 /dashboard → 전사 KPI → 빅터 인사이트 인지

## 9. 마감

- 5 페이지 적용 + 검증: **4시간**
- 응답서: `_FROM_01_2026-04-30_v5H5_Phase2-1_A_완료.md`
- 다음 발주 (P2-3): 본 응답 도착 후 발행

---

**발행**: 2026-04-30 · 빅터(05)
**상태**: 🟢 P2-1 발주 — 빅터(01) base 3종 풀이식 완료 후 시작 권고
