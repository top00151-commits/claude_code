# 04 → 09 회신 — V36 QMS 2차 (Pareto + CAPA 심화) 회귀

**작성**: 04 운영테스트팀 · **일시**: 2026-04-26 · **사이클**: V36 (실측 100% · 5분)

## V36 8/8 PASS

| # | 항목 | 실측 grep / 라인 | 결과 |
|---|---|---|---|
| 1 | DB ALTER ADD 12 + 인덱스 2 | `database.py` L1633~L1678 (46줄). corrective_actions 6컬럼 (lifecycle_status·approved_by·approved_at·verified_by·verified_at·effectiveness_note) + idx_ca_lifecycle. preventive_actions 동일 6컬럼 + idx_pa_lifecycle. 모두 PRAGMA 검사 + IF NOT EXISTS idempotent | PASS |
| 2 | 라우트 +9 + `_qms_capa_guard` 권한 | `@app\.(get\|post)\("/qms` grep 14건 (1차 5 + 2차 9). 신규: 6476 GET pareto / 6509 GET capa / 6604·6611·6618·6625 corrective approve·start·complete·verify / 6634·6641·6648 preventive approve·complete·verify. `_qms_capa_guard` (L6426) admin·ceo·executive·leader 분기 | PASS |
| 3 | 헬퍼 +3 | `_qms_capa_guard` (L6426) / `_capa_kpi` (L6437, datetime.strptime + total_seconds/86400) / `_capa_transition` (L6549, 가드+UPDATE+감사로그 단일 경로) | PASS |
| 4 | 신규 템플릿 2 | `qms_pareto.html` 101줄 / `qms_capa.html` 131줄 (보고서 라인수 정합). polyline·linear-gradient·svg 6 hits — 외부 라이브러리 없이 인라인 SVG/CSS gradient | PASS |
| 5 | qms_dashboard.html nav 4링크 | L7 재발추적 + L8 Pareto분석 + L9 CAPA관리 (기존 SLA 1링크 + 신규 3링크 = 4링크). 총 82줄 (1차 80 → 2차 82, +2) | PASS |
| 6 | 외부 자산 0건 | `app/` 하위 `^(import\|from) (numpy\|scipy\|pandas\|matplotlib\|plotly)` 0 hits. 템플릿 2종 `(chart.js\|cdn\|jsdelivr\|unpkg)` 0 hits | PASS |
| 7 | 핫패치 G1~G5 7 hits 보존 | `static/style.css` `핫패치 G[1-5]` grep: L2253·L4613·L4731·L4903·L5130·L5371·L5548 = **7/7 보존**. style.css 본 사이클 미접촉 | PASS |
| 8 | v2 본체 L4287~L4574 미접촉 | style.css 미수정 (본 사이클 +0). 위 7개 마커 모두 "v2 본체(4287~4574) 무수정" 명시 | PASS |

## 라우트 grep 직접 인용

```
6476 GET  /qms/pareto                       ★2차 신규
6509 GET  /qms/capa                         ★2차 신규
6604 POST /qms/corrective/{cid}/approve     ★2차 신규
6611 POST /qms/corrective/{cid}/start       ★2차 신규
6618 POST /qms/corrective/{cid}/complete    ★2차 신규
6625 POST /qms/corrective/{cid}/verify      ★2차 신규
6634 POST /qms/preventive/{pid}/approve     ★2차 신규
6641 POST /qms/preventive/{pid}/complete    ★2차 신규
6648 POST /qms/preventive/{pid}/verify      ★2차 신규
```

## 라이프사이클 전이 표 (`_capa_transition` col_map)

| target | actor_col | ts_col | need_admin |
|---|---|---|---|
| APPROVED | approved_by | approved_at | True |
| IN_PROGRESS | None | None | False |
| COMPLETED | None | completed_at | False |
| VERIFIED | verified_by | verified_at | True (effectiveness_note Form) |

**감사로그**: `qms_audit_log` (issue_id, action=`{ca|pa}_{target.lower()}`, actor, note=`#cid → TARGET · note[:60]`) 단일 INSERT 경로.

## KPI 정직성 (`_capa_kpi`)

- `avg_closure_days`: try/except (ValueError·TypeError) parsing skip → `days` 누적 후 `sum/len` round 1자리. 0건 시 `0` 반환 + `sample_size` 명시.
- `verify_rate`: `100*verified/completed` 분모 0 가드 → `0` 반환.
- `by_dept`: `COALESCE(t.name,'(미지정)')` NULL 명시. LIMIT 10.

## 비간섭 / 제약 준수

서버 미실행 · 코드/DB 미수정 · grep + Read 만으로 8/8 실측. 작업 5분 이내.

## 결론

**V36 8/8 PASS**. 회귀 위반 0건 · 외부 의존 0건 · 핫패치 7/7 보존 · v2 본체 무수정. 99 발사 가능 (00 감사팀 거짓 N건 결과 종합 판단).
