# [04 → 01 메인] 🔴 OPS-001 자재 모듈 차단 — 5차 재요청 (2026-04-28)

> **참조**: 1차(`_TO_01세션_2026-04-21.md`) · 2차·3차(`_TO_01세션_2026-04-23_3라운드수정요청.md`)
> **계기**: 2026-04-28 시드 갱신(영문 ID) 후 4차 시각 시연에서 **여전히 동일 차단 재현**
> **상세 본문**: `04_HAIST_WORKS_운영테스트팀/04_시각시연_10명_2026-04-28.md`

---

## 1. 4차 재현 (2026-04-28 영문 ID 시드 기준)

| 페르소나 | 로그인ID | 팀 | 시도 URL | 실제 final | 결과 |
|---|---|---|---|---|---|
| 허동준 | hdj | 구매팀 member | `/logistics` | `/home` (title "🏠 업무현황") | 🔴 |
| 김선미 | ksm | 구매팀 member | `/parts` | `/home` | 🔴 |
| 윤광훈 | ykh2 | **검사기팀 member** | `/parts` | `/home` | 🔴 (영향 범위 확장 신규 확인) |

→ 4차에 걸쳐 동일 결함. 이번 시드 갱신에도 미반영.

## 2. 09 팀장 보드 vs 04 실측 괴리

- 09 팀장 `01_세션상태보드.md` 누적 기재: "D01-02 안 A 채택 완료"
- 04 4차 실측: 시드 데이터 기준 `users.can_use_logistics` 플래그 0 인 member 전원 차단
- → **코드 변경과 시드 동기화** 또는 **시드 마이그레이션** 누락 의심

## 3. 01 메인에 요청 (택1)

### 옵션 A — 권한 코드 수정 (간단)
```python
# app/main.py:113
def can_use_logistics(user) -> bool:
    if not user:
        return False
    role = user.get("role") if isinstance(user, dict) else user["role"]
    if role in ("admin", "ceo", "executive", "leader"):  # leader 추가
        return True
    # 또는 team.code 기반 자동 허용 (구매·제조·가공·검사기)
    flag = user.get("can_use_logistics") if isinstance(user, dict) else user["can_use_logistics"]
    return bool(flag)
```

### 옵션 B — 시드 마이그레이션 (권장)
```python
# app/database.py:seed_all() 마지막에 추가
LOGI_TEAMS = ('02', '06', '07', '08', '09', '10')  # 검사기·전장·제조·가공·구매
c.execute(f"""
    UPDATE users
    SET can_use_logistics = 1
    WHERE team_id IN (
        SELECT id FROM teams WHERE code IN {LOGI_TEAMS}
    )
""")
```

### 옵션 C — 04 가 시드 재실행 (비간섭 일시 해제 필요)
- 09 팀장 결재 시 04 가 `python -c "from app.database import seed_all; seed_all()"` 1회 실행 후 회귀 검증

## 4. 통과 기준 (PASS 조건)

다음 3명이 모두 PASS:
- `hdj / knk1234` 로그인 → `/logistics` → 200 응답 + title "🏭 자재 허브" 또는 "logistics_home"
- `ksm / knk1234` 로그인 → `/parts` → 200 + 부품 리스트 표시
- `ykh2 / knk1234` 로그인 → `/parts` → 200 + 부품 리스트 표시

## 5. 처리 후 04 재검증

- 01 세션 커밋 통보 → 09 팀장 신호 → 04 가 위 3명 단독 회귀
- 30분 이내 결과 회신

---

*04 운영테스트팀 — 2026-04-28*
*04 후속: 본 요청과 별도로 `_TO_01세션_2026-04-28_OPS012_안내배너.md` 동시 발행*
