# V25 — 01 v2 이식 2차-4단계 (그래프·차트·KPI) 회귀 결과

**일시**: 2026-04-26 / **세션**: 04 운영테스트팀 / **사이클**: V25 (2차-4단계 회귀)
**대상 보고**: `01/_TO_09팀장_2026-04-26_v2이식_2차-4단계_그래프KPI.md`
**작업 시간**: 약 3분 / **방식**: Read + Grep 실측 100%, 서버 비기동, 코드/DB 미수정

## 1. 검증 항목 7건 (전건 PASS)

| # | 항목 | 보고 표기 | 실측 결과 | 판정 |
|---|------|-----------|-----------|------|
| 1 | CSS 신규 +246줄 | L5120~L5364 §1~§9 | L5118 끝마커 / L5119 빈줄 / L5120~L5364 신규 = 246줄 (5364-5118) | PASS |
| 2-a | base.html ±2 | L7 LAST UPDATE / L8 캐시키 | L7 주석 갱신 + L8 `?v=20260426v2c2-4` 확인 | PASS |
| 2-b | stock_balances.html ±5 | L16~L29 클래스 부여 | L16 주석 / L17 `.v2-kpi-step-strip` / L27 `.v2-kpi-step` / L28 `.v2-kpi-step-code` / L29 `.v2-kpi-step-lab` 5줄 | PASS |
| 3 | 페이지 카버리지 36 | 실측 grep base 제외 | grep 매칭 37 파일 (base.html 포함) → base 제외 36 일치 | PASS |
| 4 | 누적 +758 | 118+168+226+246 | 4609~4723 / 4726~4892 / 4895~5118 / 5120~5364 4 블록 산식 정합 | PASS |
| 5 | 핫패치 G1~G5 5/5 | L99·L2253·L3629·L3633·L3650 | 5/5 라인 모두 보존 (텍스트 일치 확인) | PASS |
| 6 | v2 본체 4287~4574 무수정 | 0 라인 | L4287 빈줄 / L4288 v2 블록 헤더 / L4570~4577 topbar avatar 무수정 | PASS |
| 7 | 외부 차트 라이브러리 0건 | Chart.js·D3 등 0 | grep `Chart\.js\|d3\|highcharts\|plotly\|echarts\|amcharts\|apexcharts` import 0 (주석 1건만) | PASS |

## 2. 페이지 카버리지 grep 상세 (실측)
- 광범위 grep (`hm2-kpi`/`kpi-card`/`kpi-num`/`kpi-row`/`hm2-donut`/`hm2-trend`/`trend-chart`/`chart-legend`/`hm2-legend`/`hm2-spk`/`progress-bar`/`v2-kpi-step`/`tag`/`status-tag`) → 37 파일 (base.html 포함)
- base.html 제외 = 36 페이지 → 보고와 정확 일치

## 3. 핫패치 G1~G5 보존 라인 실측
- L99: A2 폰트 토큰 v1 헤더 보존 OK
- L2253: G1·G5 영향 없음 명시 보존 OK
- L3629~3631: 핫패치 (topbar padding-right 제거) 보존 OK
- L3633~3639: 핫패치 (.main-wrap 시프트 + G1 이중 적용 방지) 보존 OK
- L3650: 핫패치 (top: var(--topbar-h)) 보존 OK

## 4. v2 본체 미수정 검증
- L4288 v2 블록 시작 (`/* ============================================================`)
- L4570~4577 topbar user-avatar 영역 무수정
- 본체 내부 셀렉터 신규 추가 없음 — 외부 셀렉터(.main · .main-wrap 자식) 만으로 정합

## 5. 외부 자산 grep 결과
- `Chart.js`/`Chart.min`/`d3.v`/`chartjs`/`highcharts`/`plotly`/`echarts`/`amcharts`/`apexcharts`/`cdn.jsdelivr.*chart` → import 0건
- 매칭 3건은 모두 주석 (style.css L5131 / 보고 md 2건) — 실제 로딩 0
- 순수 CSS+SVG 정합 정직성 OK

## 6. 종합 판정
**V25: 7/7 PASS** — 회귀 통과. 핫패치 보존, v2 본체 무수정, 외부 자산 0, 페이지 카버리지·HTML 변경·CSS 신규 모두 보고와 일치.

## 7. 권고
- 99 발사 가능 (A25 거짓 감사 결과 통과 시)
- 다음 단계: 2차-5단계 (있다면) 또는 v2 이식 종결 회신 권장

— 검증: 04 운영테스트팀 (Read + Grep 실측 100%)
