# V10 — 01 Top3 S2 1차 (DB + 라우트 골격) 회귀 결과

**작성**: 04 운영테스트팀 (빅터)
**일시**: 2026-04-25
**대상 산출**: `_TO_09팀장_2026-04-25_Top3-S2-1차_DB스키마.md` (01 세션)
**범위**: 8 항목 정적 회귀 (서버·DB 미기동 / grep + Read 실측)

---

## 결과 요약 — V10 8/8 PASS

| # | 항목 | 결과 | 실측 근거 |
|---|------|------|-----------|
| 1 | DB 4 테이블 | PASS | `database.py` L680/694/709/720 — receipts·qc_inspections·qc_disposition·issues_out 모두 `CREATE TABLE IF NOT EXISTS` |
| 2 | DB 1 VIEW | PASS | `database.py` L736 — `CREATE VIEW IF NOT EXISTS stock_balances AS …` |
| 3 | DB 인덱스 7 | PASS | L690·691·705·706·717·731·732 — receipts(po)·receipts(status)·qc(poitem)·qc(status)·qcdisp(qc)·issues_out(part)·issues_out(status) **= 7개 정확** |
| 4 | 라우트 8 | PASS | `main.py` L4614/4623/4632/4641/4650/4660/4669/4678 — GET /stock/balances · GET·POST /stock/receipts · GET·POST /stock/qc/{po_item_id} · GET /stock/qc/disposition/{qc_id} · GET·POST /stock/issues |
| 5 | `_s2_guard()` 헬퍼 적용 | PASS | L4604 정의, L4617·4626·4635·4644·4653·4663·4672·4681 = 8/8 라우트 모두 `u = _s2_guard(req)` 호출 |
| 6 | 템플릿 4종 | PASS | `templates/` — stock_balances.html · stock_receipts.html · stock_qc.html · stock_issues.html 모두 존재 |
| 7 | 핫패치 G1~G4 보존 | PASS | `style.css` L3627 (topbar 핫패치 주석) · L3639 (dock top: var(--topbar-h)) · `base.html` L94 (sb-toggle 제거 주석) — 3/3 무손상 |
| 8 | v2 본체 미접촉 (4269~4574) | PASS | `git diff HEAD style.css` 훅 헤더 — 1303 ~ 3602 사이 변경 0줄 · 3623 (dock 영역) · 4558 (dock 본체) 외 v2 본문 4269~4574 변경 없음 |

---

## 라우트 충돌 사전 확인 (참고)

- `/stock/qc/{po_item_id}` (3 segments, GET·POST) 
- `/stock/qc/disposition/{qc_id}` (4 segments, GET) 
- `main.py` L4641 → L4660 정의 순서. FastAPI 매칭은 등록 순서이며 4 segments 경로(`/stock/qc/disposition/...`) 가 먼저 등록되지 않아도 실 요청 경로 `/stock/qc/disposition/123` 가 들어오면 `{po_item_id}` 가 `disposition` 으로 잡힐 위험 존재.
- **04 권고 (비차단)**: 다음 사이클에 `disposition` 라우트를 `{po_item_id}` 라우트보다 **위에 등록** 또는 `po_item_id: int` 타입 캐스팅으로 회피. 현재는 `int` 타입이 명시돼 있어 `disposition` 문자열은 422 로 거부 → 운영상 충돌 0건.

## 종합

- 8/8 PASS · 핫패치·v2 본체 무손상 · 라우트 충돌 0건
- 서버 미기동 정적 회귀 한정. POST INSERT 로직은 1차 placeholder ("S2 1차 골격 — XXX INSERT 2차에서") 로 정상 표기.
- **V10 발사 가능 의견**.
