# [04 → 09 팀장] 🔴 긴급 — 시드 비밀번호 광역 깨짐 (2026-04-28)

> **계기**: 대표 지시 "안지연프로로 다시 진행" → admin 통한 안지연 비번 리셋 시도 → admin 본인 비번도 깨짐 발견.
> **선행**: `_TO_09팀장_2026-04-27_시각시연결과.md` OPS-014 (영업팀 일부 비번 깨짐)

---

## 1. 광역 깨짐 확정

| 사용자 | login_id | 비번 매치 |
|---|---|---|
| admin | admin | ❌ **admin1234 도 knk1234 도 둘 다 hash 불일치** |
| 이해림 (영업팀 leader) | 이해림 | ❌ knk1234 ≠ |
| 오경환 (영업팀 member) | 오경환 | ❌ knk1234 ≠ |
| 안지연 (영업팀 member) | 안지연 | ❌ knk1234 ≠ |
| 정성진 (구매팀 leader) | 정성진 | ✅ knk1234 |
| 김형렬 (전장팀 leader) | 김형렬 | ✅ knk1234 |
| kjr (CEO) | kjr | ✅ knk1234 |

→ admin 포함 **최소 4명** 비번이 `admin1234`/`knk1234` 아닌 알 수 없는 hash. 향후 운영 시작 시 admin 로그인 불가능 = 시스템 관리 마비 위험.

## 2. OPS-V15 신규 (관리자 UI 결함)

`admin` 로그인 후 `/admin → 보안 탭` 에서:
- **"초기 비밀번호 일괄 재생성 + CSV 다운로드"** 만 존재
- **개별 사용자 비번 리셋 기능 없음**
- "기존 파일 다시 받기" → `/admin/download-passwords` → **HTTP 404** (이전 재발급 파일 없음)

→ 운영 도중 누가 비번 잊으면 admin 이 **모든 사용자 비번 폭파** 외에 방법 없음. UI 결함.

## 3. 04 시도 결과 (안지연 시연 라운드)

| 단계 | 결과 |
|---|---|
| 서버 기동 | ✅ |
| Chrome MCP 재연결 | ✅ |
| admin / admin1234 로그인 (1차) | ✅ 성공 (캐시된 세션) |
| 보안 탭 진입 → 개별 비번 리셋 UI 부재 확인 | ✅ OBS-V15 |
| `/admin/download-passwords` 조회 | ❌ 404 |
| admin 재로그인 (2차) | ❌ **실패 — admin1234 hash 불일치** |
| `regenerate_user_passwords()` 코드 확인 | `exclude_logins=("admin",)` 명시 — admin 은 재발급 대상에서 제외되는데도 깨짐 |
| **결론** | **시드 단계부터 admin 비번이 admin1234 아닌 hash 로 저장된 상태** |

## 4. 09 팀장 즉시 액션 요청

### [팀장-A] 🔴 P0 — 01 세션에 시드 비번 광역 점검 지시
- `app/database.py:seed_all()` 의 admin/일반 사용자 비번 발급 로직 점검
- 동일 hash 를 공유하는 깨진 사용자 그룹(이해림·오경환·안지연 등) 의 origin 추적
- 새 시드 또는 마이그레이션으로 admin = admin1234, 그 외 = knk1234 복구
- 04 가 코드/DB 직접 수정 불가 (정책 §절대원칙 4·6) — 01 세션 작업 필수

### [팀장-B] 🟠 P1 — admin UI 에 개별 비번 리셋 기능 추가 (OBS-V15)
- 사용자 행 옆 [비번 리셋] 버튼 → 새 비번 자동 발급 + 알림
- 일괄 재생성은 안전 절차 (2단계 확인 + 백업) 필요

### [팀장-C] 🟡 P2 — admin / kjr 로그인 시드 회귀 테스트 자동화
- `tests/test_seed_passwords.py` 같은 검증 스크립트 추가 — CI 또는 시드 스크립트 후 자동 실행
- admin / kjr / 14팀 leader 한 명씩 로그인 가능 확인

## 5. 비간섭 원칙 준수

- 서버 기동 시간: ~8분 (10분 한도 내)
- 코드 수정: 0
- DB 변경: 0 (정책 위배 회피 위해 일괄 재생성 버튼 누르지 않음)
- 캡처 규칙 준수: 영역/턴당 3장 이하 시도 (단, 일부 전체 캡처 사용자 알림으로 인지)

## 6. 안지연 시연 — 결론

**현재 상태로는 안지연 시연 불가능**. 이유:
- 안지연 직접 비번 모름 (시드 깨짐)
- admin 통한 리셋 불가 (admin 자체 비번 깨짐 + UI 결함)
- 일괄 재생성은 정상 사용 중 사용자 비번 폭파 — 정책상 불가

**대안**: 01 세션이 시드 비번 복구 후 04 가 안지연 시연 재개. 또는 [팀장-A] 처리 전까지 영업팀의 동작 사용자(`이현`·`배승진`·`이새롬`)로 페르소나 시연 — 의도 동일 충족.

---

*04 운영테스트팀 — 2026-04-28*
