# 02 테스트 실행 로그 — 2라운드 2026-04-21

> D04-03 이행. 6종 페르소나 + P10 커버리지 재점검.


## P2 — 1년차 설계팀원 '김범수' (team=4 설계팀 member)


## P4 — 5년차 구매팀 주임 '허동준' (team=10 구매팀 member)


## P5 — 경력직 품질팀장 '김정록' (team=3 품질팀 leader)


## P7 — 타부서(기술영업팀) '이현' (team=1 기술영업팀 member)


## P8 — 출장 중 모바일 '마준영' (team=7 제조1 member (모바일 UA))


## P9 — 폴란드 해외 셋업 '이치권' (team=2 검사기 member (en 언어 설정))


## P10r — 베트남 법인 재검증 '쑤아잉' (team=12 베트남법인 (vi 언어))


### P2 1년차 설계팀원 — `김범수`

_team=4 설계팀 member_

로그인 → 200 | /daily | 4106ms
  ✅ `GET /home` → 200 | 「🏠 업무현황 · KNK」 | 2033ms | final=/home
  ✅ `GET /changes` → 200 | 「변경 알림 — HAIST WORKS」 | 2073ms | final=/changes
  ✅ `GET /changes/new` → 200 | 「변경 등록 — HAIST WORKS」 | 2082ms | final=/changes/new
  ✅ `GET /progress` → 200 | 「진행률 대시보드 — HAIST WORKS」 | 2054ms | final=/progress
  ✅ `GET /tickets` → 200 | 「요청 티켓 — HAIST WORKS」 | 2052ms | final=/tickets
  ✅ `POST /api/task` 정상 업무 등록 → 200 | 2051ms | {"ok":true,"id":2824}
  ✅ `POST /api/task` rep#1 → 200 | 2044ms | {"ok":true,"id":2825}
  ✅ `POST /api/task` rep#2 → 200 | 2042ms | {"ok":true,"id":2826}
  ✅ `POST /api/task` rep#3 → 200 | 2031ms | {"ok":true,"id":2827}
  ✅ `POST /api/task` rep#4 → 200 | 2044ms | {"ok":true,"id":2828}
  ✅ `POST /api/task` rep#5 → 200 | 2038ms | {"ok":true,"id":2829}
  ✅ `POST /api/task` rep#6 → 200 | 2032ms | {"ok":true,"id":2830}
  ✅ `POST /api/task` rep#7 → 200 | 2048ms | {"ok":true,"id":2831}
  ✅ `POST /api/task` rep#8 → 200 | 2060ms | {"ok":true,"id":2832}
  ✅ `POST /api/task` rep#9 → 200 | 2055ms | {"ok":true,"id":2833}
  ✅ `POST /api/task` rep#10 → 200 | 2049ms | {"ok":true,"id":2834}
  >> 10건 연속 업로드 총 20451ms (건당 2045ms)

### P4 5년차 구매팀 주임 — `허동준`

_team=10 구매팀 member_

로그인 → 200 | /daily | 4059ms
  ✅ `GET /home` → 200 | 「🏠 업무현황 · KNK」 | 2047ms | final=/home
  ⚠️ 폴백 `GET /logistics` → 200 | 「🏠 업무현황 · KNK」 | 4072ms | final=/home
  ⚠️ 폴백 `GET /parts` → 200 | 「🏠 업무현황 · KNK」 | 4078ms | final=/home
  ⚠️ 폴백 `GET /suppliers` → 200 | 「🏠 업무현황 · KNK」 | 4079ms | final=/home
  ⚠️ 폴백 `GET /po` → 200 | 「🏠 업무현황 · KNK」 | 4076ms | final=/home
  ⚠️ 폴백 `GET /po/new` → 200 | 「🏠 업무현황 · KNK」 | 4081ms | final=/home
  ⚠️ 폴백 `GET /rates` → 200 | 「🏠 업무현황 · KNK」 | 4066ms | final=/home

**발견**: 6건
- 폴백: `GET /logistics` → final=`/home` (title:「🏠 업무현황 · KNK」)
- 폴백: `GET /parts` → final=`/home` (title:「🏠 업무현황 · KNK」)
- 폴백: `GET /suppliers` → final=`/home` (title:「🏠 업무현황 · KNK」)
- 폴백: `GET /po` → final=`/home` (title:「🏠 업무현황 · KNK」)
- 폴백: `GET /po/new` → final=`/home` (title:「🏠 업무현황 · KNK」)
- 폴백: `GET /rates` → final=`/home` (title:「🏠 업무현황 · KNK」)

### P5 경력직 품질팀장 — `김정록`

_team=3 품질팀 leader_

로그인 → 200 | /team | 4071ms
  ✅ `GET /team` → 200 | 「팀 현황 · KNK」 | 2037ms | final=/team
  ✅ `GET /issues` → 200 | 「이슈·AS DB — HAIST WORKS」 | 2035ms | final=/issues
  ✅ `GET /issues/new` → 200 | 「이슈 등록 — HAIST WORKS」 | 2059ms | final=/issues/new
  ✅ `GET /tickets` → 200 | 「요청 티켓 — HAIST WORKS」 | 2029ms | final=/tickets
  ⚠️ 폴백 `GET /dashboard` → 200 | 「로그인 · HAIST WORKS」 | 4073ms | final=/login
  ✅ `GET /cockpit` → 200 | 「🎯 우리 팀 코크핏」 | 2051ms | final=/cockpit
  ✅ `GET /bottlenecks` → 200 | 「🚨 병목 자동 감지」 | 2065ms | final=/bottlenecks
  ✅ `GET /progress` → 200 | 「진행률 대시보드 — HAIST WORKS」 | 2029ms | final=/progress

**발견**: 1건
- 폴백: `GET /dashboard` → final=`/login` (title:「로그인 · HAIST WORKS」)

### P7 타부서(기술영업팀) — `이현`

_team=1 기술영업팀 member_

로그인 → 200 | /daily | 4069ms
  ✅ `GET /home` → 200 | 「🏠 업무현황 · KNK」 | 2048ms | final=/home
  ⚠️ 폴백 `GET /search?q=프로젝트` → 200 | 「🔎 통합 검색」 | 2055ms | final=/search?q=%ED%94%84%EB%A1%9C%EC%A0%9D%ED%8A%B8
  ✅ `GET /calendar` → 200 | 「내 달력 · KNK」 | 2042ms | final=/calendar
  ✅ `GET /board/company` → 200 | 「전사 게시판 · HAIST WORKS」 | 2059ms | final=/board/company
  ✅ `GET /project/1` → 200 | 「갤럭시 S27 메인보드 ICT · KNK」 | 2034ms | final=/project/1
  ⚠️ 폴백 `GET /parts` → 200 | 「🏠 업무현황 · KNK」 | 4085ms | final=/home

**발견**: 2건
- 폴백: `GET /search?q=프로젝트` → final=`/search?q=%ED%94%84%EB%A1%9C%EC%A0%9D%ED%8A%B8` (title:「🔎 통합 검색」)
- 폴백: `GET /parts` → final=`/home` (title:「🏠 업무현황 · KNK」)

### P8 출장 중 모바일 — `마준영`

_team=7 제조1 member (모바일 UA)_

로그인 → 200 | /daily | 4071ms
  ✅ `GET /home` → 200 | 「🏠 업무현황 · KNK」 | 2057ms | final=/home
  ✅ `GET /tickets` → 200 | 「요청 티켓 — HAIST WORKS」 | 2056ms | final=/tickets
  ✅ `GET /notifications` → 200 | 「🔔 알림 · KNK」 | 2040ms | final=/notifications
  ✅ `GET /changes` → 200 | 「변경 알림 — HAIST WORKS」 | 2051ms | final=/changes

### P9 폴란드 해외 셋업 — `이치권`

_team=2 검사기 member (en 언어 설정)_

로그인 → 200 | /daily | 4089ms
lang=en → 200 (4094ms)
  ✅ `GET /home` → 200 | 「🏠 Overview · KNK」 | 2037ms | final=/home
  ✅ `GET /tickets` → 200 | 「요청 티켓 — HAIST WORKS」 | 2031ms | final=/tickets
  ⚠️ 폴백 `GET /parts` → 200 | 「🏠 Overview · KNK」 | 4101ms | final=/home
  ✅ `GET /progress` → 200 | 「진행률 대시보드 — HAIST WORKS」 | 2040ms | final=/progress
  >> en 번역 커버리지 (홈): {'Overview': 2, 'My Account': 1, 'Logout': 1, 'Daily Work': 0, 'Task': 9, '업무현황': 0, '내 계정': 0}
  >> en 번역 커버리지 (cockpit): {'Overview': 0, 'Dashboard': 0, '코크핏': 0, '전사': 0}

**발견**: 1건
- 폴백: `GET /parts` → final=`/home` (title:「🏠 Overview · KNK」)

### P10r 베트남 법인 재검증 — `쑤아잉`

_team=12 베트남법인 (vi 언어)_

로그인 → 200 | /daily | 4082ms
lang=vi → 200 (4102ms)
  ✅ `GET /home` → 200 | 「🏠 Tổng quan · KNK」 | 2030ms | final=/home
  ✅ `GET /daily` → 200 | 「Thẻ công việc hàng ngày · KNK」 | 2058ms | final=/daily
  ✅ `GET /board/company` → 200 | 「전사 게시판 · HAIST WORKS」 | 2038ms | final=/board/company
  ✅ `GET /notifications` → 200 | 「🔔 Thông báo · KNK」 | 2037ms | final=/notifications
  ✅ `GET /profile` → 200 | 「Tài khoản · Đổi mật khẩu · KNK」 | 2053ms | final=/profile
  >> vi 번역 커버리지 (홈): {'Công việc': 4, 'Tài khoản': 1, 'Đăng xuất': 1, 'Hôm nay': 1, '업무현황': 0, '진행중': 6, '내 업무': 1}
  >> vi 번역 커버리지 (게시판): {'Bảng thông báo': 0, '공지': 11, 'Thông báo': 2, '전사': 9}