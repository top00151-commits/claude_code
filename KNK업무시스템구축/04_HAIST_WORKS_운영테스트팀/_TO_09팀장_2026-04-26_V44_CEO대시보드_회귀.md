# V44 — CEO 통합 대시보드 회귀 (2026-04-26)

발주: `_FROM_09팀장 → 04 V44` (01 회신 `_TO_09팀장_2026-04-26_CEO통합대시보드.md` 검증)
방식: 정적 grep / Read · 서버/DB 미수정 · 실측 100% · 작업 시간 약 3분

---

## 1. ceo_dashboard_kpis 헬퍼 (database.py)

- `database.py:6406` `def ceo_dashboard_kpis(user_id: int = 0) -> dict:` 1건 정의 확인.
- 9 카테고리 키 dict 초기값 (lines 6410~6420): `sales · stock · perms · exports · qms · weekly · gantt · fx · notif` = 9/9 PASS.
- 9 카테고리 각각 독립 `try / except Exception: pass` 폴백 (#1 매출 6435, #2 재고 6466, #3 권한 6479, #4 수출입 6495, #5 QMS 6512, #6 주간 6530, #7 간트 6549, #8 환율 6560, #9 알림 6576) = 9/9 폴백 PASS.
- 마지막 `return out` (6587) — 항상 dict 반환 보장.

## 2. 라우트 (main.py)

- `main.py:29` `from .database import (..., ceo_dashboard_kpis)` 확인.
- `main.py:1771` `@app.get("/ceo")` 별칭 → `RedirectResponse("/dashboard", 303)` 확인.
- `main.py:1777` `@app.get("/dashboard")` 본체에 권한 가드 `require(req, ["ceo","admin","executive"])` (1783) 보존.
- `main.py:1894` `ceo_kpis = ceo_dashboard_kpis(user_id=u["id"])` 1줄 추가.
- `main.py:1896-1901` `unread_notifs` UNREAD 5건 SELECT 확인.
- `main.py:1909` ctx 인자 `ceo_kpis=ceo_kpis, unread_notifs=unread_notifs` 전달 확인.

## 3. dashboard.html UI

- `{% if ceo_kpis %}` 가드 (line 25) + `kpi-grid kpi-grid-9` (line 27).
- 9 KPI 카드 (line 28~70): sales/stock/perms/exports/qms/weekly/gantt/fx/notif 각 1건 = 9/9 PASS.
- `{% if unread_notifs %}` 알림 리스트 (73~87) — `for n in unread_notifs` LIMIT 5 (main.py 1899).
- 빠른 액션 8칩 (90~99) sales/parts/permissions/export/qms/gantt/rates/weekly = 8/8 PASS.

## 4. DB 무변경

- 헬퍼 정의 범위 (database.py 6406~6587) 내 `CREATE TABLE / ALTER TABLE / DROP` 0건. SELECT/COALESCE/SUM/COUNT 만 사용 = PASS.

## 5. 핫패치 G1~G5 + v2 본체 미접촉

- 5 marker 파일 grep 합 = style.css 9 + base.html 1 + main.py 1 + database.py 4 + sales_dashboard.html 1 = **16 hits / 5 파일 5/5** PASS.
- v2 시안(base 헤더/사이드바/도크) 미접촉 — dashboard.html 추가만, base.html G3 마커 보존 1건 동일.

## 6. BAT 갱신

- `KNK_시작.bat:3` `LAST UPDATE: 2026-04-26 CEO통합대시보드 (...)` 한국어 상세요약 확인.
- `START.bat:3` 동일 일자 + 영어 동기 요약 확인. title/echo 양쪽 동기 PASS.

---

## 결과: V44 6/6 PASS · 99 발사 가능
