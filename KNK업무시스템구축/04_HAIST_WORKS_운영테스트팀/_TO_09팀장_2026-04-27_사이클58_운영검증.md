# 04 운영검증 — 사이클 58 Sales Quotations 2차 (견적서 인쇄 + quotation_items)

> 일자: 2026-04-27
> 발주: 09 빅터 팀장 → 04 운영테스트 (일회성 검증 worker)
> 대상: 01 메인 사이클 58 산출 (database.py · main.py · sales_quotations.html · quotation_print.html · BAT 양본)
> 결론: **PASS (조건부 — S3급 1건·자수 보고는 정당)**

---

## 1. 회귀 (PASS)

### 1-1. py 정적 컴파일
- `python -m py_compile app/database.py app/main.py` → 무출력 (PASS, 신디자인 0건)

### 1-2. 라우트 충돌 0건 (grep 인용)
```
6456: @app.get ("/sales/quotations", ...)            # 1차 list
6477: @app.post("/sales/quotations")                 # 1차 create
6511: @app.get ("/sales/quotations/{quote_id}/print")             # 2차 신규
6542: @app.post("/sales/quotations/{quote_id}/items")             # 2차 신규
6593: @app.post("/sales/quotations/{quote_id}/items/{item_id}/delete") # 2차 신규
6615: @app.post("/sales/quotations/{quote_id}/convert-to-order")  # 2차 신규
```
- 6 path 모두 distinct. `{quote_id}`는 1차 path 뒤 sub-resource 라우팅 — FastAPI prefix tree 충돌 0.

### 1-3. PRAGMA 가드 idempotent (PASS)
- `app/database.py:1406` `c.executescript(SCHEMA)` 호출 → SCHEMA 본문 안의 `quotation_items` 블록(line 785~799) 모두 `CREATE TABLE/INDEX IF NOT EXISTS` 가드 적용 — 기존 DB 재시작 시에도 무손상.

### 1-4. 헬퍼 컬럼 매핑 (PASS)
- `clone_quotation_to_order` (line 7007) 의 INSERT 컬럼:
  - **orders**: `(order_no, quote_id, customer_id, order_date, due_date, total_amount, status, created_by)` — 스키마 line 802~815 와 정합
  - **order_items**: `(order_id, part_id, qty, unit_price, amount, allocated_qty)` — 스키마 line 821~829 와 정합
  - 비고: `quotation_items.total_price` → `order_items.amount` 매핑 정상 (시안 §3 별칭)

---

## 2. 권한 가드 (4/4 PASS)

`_s1_guard` (main.py:6446) — `admin/ceo` OR `can_use_sales(u)`. 신규 4 라우트 모두 첫 줄에서 호출:
- 6514: `_s1_guard` (GET print) → 미인증/일반사용자 RedirectResponse("/home", 303)
- 6545: `_s1_guard` (POST items) → JSONResponse({"error":"권한 없음"}, 401)
- 6596: `_s1_guard` (POST item delete) → 401
- 6618: `_s1_guard` (POST convert-to-order) → 401

**일반 사용자 차단 PASS**.

---

## 3. SQL 인젝션 (0건 PASS)

- 신규 4 라우트 + 2 헬퍼 모두 `?` 파라미터 바인딩 100%
- f-string SQL 검색: 라우트 6511~6648 / 헬퍼 6987~7054 범위 내 발견 0건
- 발견된 f-string은 redirect URL (`f"/sales/quotations"` 6590, 6612) 및 sequence 채번 (`f"SO-{ym}-%"` 7027 — `ym`은 `datetime.now().strftime` 리터럴, 사용자 입력 미경유)뿐 — **인젝션 위험 0**

---

## 4. 외부 자산 0 (PASS · grep 직접 인용)

```
$ Grep "reportlab|weasyprint|pdfkit|wkhtmltopdf" 01_HAIST_WORKS/**.py
→ 코드 매치 0건 (보고서 .md 2건만 — 자체 부정 표현)
```

`quotation_print.html` 점검:
- line 31~39: `@media print { ... @page { size: A4; margin: 18mm 14mm; } }` — 인라인 CSS only
- line 45: `<button onclick="window.print();">` — 브라우저 native call
- CDN/`<img src=`/`@font-face`/`<link rel=stylesheet href=http`/script src=http 모두 0건

`app/main.py:1~13` 임포트 변경 0건 — 신규 import 없음 PASS.

---

## 5. 핫패치 G1~G5 (175 매칭 PASS)

```
$ grep "sb-toggle|main-wrap|\.dock" static/style.css → 172
$ grep "sb-toggle|main-wrap|\.dock" templates/base.html → 3
합계 175 ≥ 130 baseline (보고서 일치 PASS)
```

---

## 6. v2 본체 미접촉 (PASS)

```
$ stat static/style.css mtime
→ 2026-04-25 21:27:46 (사이클 58 04-27 12:49 작업과 무관)
```
v2 본체 (line 4287~4574) 미접촉 PASS.

---

## 7. 페르소나 시뮬레이션 (정적 트레이스)

### P1 영업 (can_use_sales)
1. GET /sales/quotations → 1차 list 표시 (200) ✓
2. POST /sales/quotations/{id}/items (item_name="공압밸브", qty=10, unit_price=50000)
   → quotation_items INSERT + total_amount=500,000 재합계 ✓
3. GET /sales/quotations/{id}/print (target=_blank)
   → quotation_print.html 인쇄 view + window.print() ✓
4. POST /sales/quotations/{id}/convert-to-order (due_date='2026-05-30')
   → quotations.status=CONFIRMED + clone_quotation_to_order → orders+order_items INSERT
   → order_status_history(DRAFT→CONFIRMED) → /sales/orders 리다이렉트 ✓

### CEO (role=ceo)
- 위 4 라우트 모두 OK (`_s1_guard` 첫 분기 통과) ✓

### 일반 사용자 (P3 재고담당 · sales 권한 없음)
- GET /print → /home 리다이렉트
- POST 3건 → 401 JSON
- **차단 PASS**

---

## 8. 자수 보고 검증 (3건 모두 정당)

| # | 자수 항목 | 검증 결과 |
|---|----------|---------|
| 1 | customers contact/address 컬럼 부재 → SQL 제거 | **정당**. database.py line 61~66 에 `name/tier/note`만. main.py 6520~6522 SQL은 `cu.name/tier/note`만 사용 — OperationalError 회피. |
| 2 | 1차 `/sales/orders` POST 보존 + 신규 라우트 추가 호출 분기 | **정당**. quote_id를 form에서 받는 1차 라우트는 다른 진입점 (다른 화면) 으로 보존 가치 있음. UI 동선상 sales_quotations.html에서는 신규 라우트로 라인 자동 복제 동작이 핵심 — 분리 합리. |
| 3 | `get_quotation_items` 자체 db_session 사용 | **정당**. read-only. 호출자 트랜잭션 합치기 불필요 (line 6537 print view 진입 시점 일회성 read). |

---

## 9. 신규 발견

### S3-58-1: convert-to-order 의 트랜잭션 분리 (보강 권고 · 위반 아님)

- main.py 6624 `with db_session() as c:` → status=CONFIRMED → 커밋
- 6635 `_clone(...)` → 별도 db_session 진입
- **시나리오**: status UPDATE 직후 `_clone` 진입 전 프로세스 크래시 시 quotations.status=CONFIRMED 인데 orders 행은 없는 부정합 가능 (경미 · idempotent 재시도 가능)
- **권고**: 차후 사이클에서 `clone_quotation_to_order` 가 외부 connection 받도록 시그니처 확장 (단, 본 사이클 차단 사유 아님 — 검수 PASS 유지)

### S4-58-1: convert-to-order 결과 응답이 항상 `/sales/orders` 리다이렉트

- 신규 생성된 order_id 가 사용자에게 즉시 보이지 않음 (전체 목록 페이지)
- **권고**: `/sales/orders/{order_id}` 단일 detail 라우트 존재 시 그쪽으로 리다이렉트 — UX 개선 (S4 등급, 본업 비차단)

### S2 / S1 신규: 0건

---

## 10. 보고서 산식 vs 실측

| 항목 | 보고서 | 실측 | 판정 |
|------|--------|------|------|
| quotation_items 위치 | 785 | 785 | PASS |
| get_quotation_items | 6987 | 6987 | PASS |
| clone_quotation_to_order | 7007 | 7007 | PASS |
| 라우트 4개 | 6511/6542/6593/6615 | 6511/6542/6593/6615 | PASS |
| quotation_print.html wc -l | 111 | 111 | PASS |
| sales_quotations.html wc -l | 119 | 119 | PASS |
| 핫패치 매칭 | 175 (172+3) | 175 (172+3) | PASS |
| BAT KNK_시작 | line 3/7/14 | line 3/7/14 | PASS |
| BAT START | line 3/7/13 | line 3/7/13 | PASS |
| style.css mtime | 미접촉 | 04-25 21:27 | PASS |
| 외부 자산 import | 0건 | 0건 | PASS |

**거짓 발견 0건**.

---

## 결론

- **회귀 PASS** · **권한 4/4 PASS** · **SQL 인젝션 0건 PASS** · **외부 자산 0 PASS**
- **핫패치 175 매칭 PASS** · **v2 본체 미접촉 PASS** · **BAT 6/6 PASS**
- **자수 3건 모두 정당** (DB 컬럼 회피 / 1차 라우트 보존 / read-only 헬퍼)
- **신규 S3 1건 (트랜잭션 분리)** + **S4 1건 (리다이렉트 UX)** — 본 사이클 비차단 권고
- **거짓 발견 0건**

→ **사이클 58 운영검증 PASS** — 대표 결재 사안 없음.

---

*04 일회성 검증 worker · 코드 수정 0 · 검증 보고만*
