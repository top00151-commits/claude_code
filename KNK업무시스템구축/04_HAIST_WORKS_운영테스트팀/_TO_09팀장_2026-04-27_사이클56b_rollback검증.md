# 04 운영검증 - 사이클 56b rollback

> 발주: 09 팀장 — 사이클 56b rollback 검증 (-447줄, 코드 수정 0)
> 검증자: 04 운영테스트팀
> 일시: 2026-04-27
> 정직성 v3: 모든 grep/wc/python 결과 직접 인용

---

## 1. 회귀 검증

### 1-1. import app.main 통과 (PASS)

```
$ python -c "import ast; ast.parse(open('app/main.py', encoding='utf-8').read()); print('main.py AST OK')"
main.py AST OK

$ python -c "import ast; ast.parse(open('app/database.py', encoding='utf-8').read()); print('database.py AST OK')"
database.py AST OK

$ python -c "import app.main; print('IMPORT OK')"
IMPORT OK
```

→ **AST 파싱 통과 + 런타임 import 통과**. ImportError 0건.

### 1-2. stock_audits 라우트 5건 실재 (FastAPI app.routes 직접 인용)

```
$ python -c "from app.main import app; [print(','.join(sorted(r.methods)),r.path) for r in app.routes if hasattr(r,'methods') and r.path.startswith('/stock/audits')]"
GET /stock/audits
POST /stock/audits/new
GET /stock/audits/{audit_id}
POST /stock/audits/{audit_id}/items
POST /stock/audits/{audit_id}/close
```

→ 발주서 §B 명시 5건 모두 FastAPI 라우터에 정상 등록.

### 1-3. main.py 라우트 정의 grep -n 직접 인용

```
$ grep -nE "@app\.(get|post)\(\"/stock/audits" app/main.py
5189:@app.get("/stock/audits", response_class=HTMLResponse)
5202:@app.post("/stock/audits/new")
5213:@app.get("/stock/audits/{audit_id}", response_class=HTMLResponse)
5249:@app.post("/stock/audits/{audit_id}/items")
5410:@app.post("/stock/audits/{audit_id}/close")
```

> **참고**: 발주서 표기 라인(5035/5048/5095/5256)과 차이는, rollback 보고서가 작성된 시점 이후 main.py 가 그대로 보존되었으나 발주서가 인용한 grep은 보고서 작성시 스냅샷이고, 본 검증 시점 main.py 총 길이는 8929줄(`wc -l`). rollback 자체는 working tree에 있고 미커밋. **라인이 다르나 라우트 5건은 동일하게 보존됨**.

### 1-4. 디테일 라우트 핸들러 — stock_audit.html mode=detail 복귀 확인

```
$ sed -n '5213,5246p' app/main.py
5213:@app.get("/stock/audits/{audit_id}", response_class=HTMLResponse)
5214:async def stock_audit_detail(request: Request, audit_id: int):
...
5242:    return ctx(request, "stock_audit.html", user=u, mode="detail",
5243:               audits=[], audit=audit, parts=[dict(r) for r in parts],
```

→ 핸들러는 `stock_audit.html` 템플릿 + `mode="detail"` 인자로 렌더 (사이클 56 이전 상태로 복귀).
→ `summary = get_audit_summary(audit_id)` 호출 0건 (헬퍼 제거 정합).

### 1-5. 헬퍼 호출 정합 (database.py 정의 grep -n)

```
$ grep -nE "^def (stock_audit_create|stock_audits_list|stock_audit_get|stock_audit_item_upsert|stock_adjustments_list|stock_audit_close|stock_audit_effect_summary)" app/database.py
4072:def stock_audit_create(led_by: int, start_date: str = "", note: str = "") -> tuple[int, str]:
4085:def stock_audits_list(limit: int = 50) -> list[dict]:
4100:def stock_audit_get(audit_id: int) -> dict | None:
4122:def stock_audit_item_upsert(audit_id: int, part_id: int, counted_qty: float, ...
4168:def stock_adjustments_list(status: str = "PENDING", limit: int = 100) -> list[dict]:
4267:def stock_audit_close(audit_id: int) -> tuple[bool, str]:
4301:def stock_audit_effect_summary(audit_id: int) -> dict:
```

→ main.py에서 호출하는 헬퍼 7개 모두 database.py에 정의 실재.

### 1-6. 사이클 56 흔적 0건 (app/ 전체 재귀 grep)

```
$ grep -rnE "\b(start_audit|finalize_audit|get_audit_summary|stock_audit_line_update)\b" app/
(0 matches — No matches found)

$ grep -rnE "stock_audit_new\.html|stock_audit_detail\.html" app/
(0 matches — No matches found)

$ grep -nE "/lines/\{line_id\}|/finalize" app/main.py
(0 matches — No matches found)
```

→ **app/ 코드 전체에 사이클 56 추가분 0건**. 회귀 안전.

### 1-7. 템플릿 파일 시스템 + Jinja Loader 정합

```
$ ls app/templates/ | grep -i stock_audit
stock_audit.html
stock_audits.html

$ python -c "from jinja2 import Environment, FileSystemLoader; env = Environment(loader=FileSystemLoader('app/templates')); [print(t,'OK' if env.get_template(t) else 'X') for t in ('stock_audits.html','stock_audit.html')]"
stock_audits.html OK
stock_audit.html OK

$ python -c "from jinja2 import Environment, FileSystemLoader, TemplateNotFound; ..."
stock_audit_new.html GONE (expected)
stock_audit_detail.html GONE (expected)
```

→ 보존 2건 + 삭제 2건 모두 의도대로.

---

## 2. 페르소나 P3 (재고담당) 정적 시뮬

httpx 미설치로 TestClient 불가 → 정적 분석 + FastAPI 라우터 등록 확인으로 갈음.

| 단계 | 라우트 | 핸들러 | 헬퍼 | 템플릿 | 판정 |
|------|--------|--------|------|--------|------|
| 1) 실사 목록 | GET /stock/audits (5189) | stock_audits_page | stock_audits_list | stock_audits.html | OK |
| 2) 신규 시작 | POST /stock/audits/new (5202) | stock_audits_new | stock_audit_create | (redirect → 디테일) | OK |
| 3) 디테일 | GET /stock/audits/{id} (5213) | stock_audit_detail | stock_audit_get + stock_audit_effect_summary | stock_audit.html(mode=detail) | OK |
| 4) 라인 입력 | POST /stock/audits/{id}/items (5249) | stock_audit_item_add | stock_audit_item_upsert | (redirect 동일 디테일) | OK |
| 5) 종료 | POST /stock/audits/{id}/close (5410) | stock_audits_close_route | stock_audit_close | (redirect) | OK |

→ **P3 4단계 워크플로 정합**. 템플릿(stock_audit.html line 11/59/97 form action) 도 5/4/3과 정확히 일치.

---

## 3. 보존 5/5

| 항목 | 검증 명령 | 결과 |
|------|-----------|------|
| 외부 자산 0 | `grep "^(import\|from)" app/main.py` 상단 13행 | fastapi/starlette/내부(.database, .i18n, .victor) 만 — 신규 import 0건 ✓ |
| 핫패치 G1~G5 | `grep -cE "\.sb-toggle\|\.dock \{\|\.main-wrap" static/style.css` | **130** (정책 130~131 매칭) ✓ |
| v2 본체 | `stat -c '%y' static/style.css` | **2026-04-25 21:27:46** (사이클 56 이전, 미접촉) ✓ |
| SQL CREATE TABLE stock_audits | line 1082~1092 | 7컬럼 (사이클 56 추가 `scope`/`total_diff_count` 0건) ✓ |
| Jinja form action | stock_audit.html line 11/59/97 | `/new`, `/close`, `/items` 만 (cycle 56 `/lines/{line_id}`·`/finalize` 0건) ✓ |

---

## 4. 발주서 외 4건 정정의 정당성

발주서는 3 헬퍼 + 3 라우트 + 2 템플릿 명시 → 01이 추가 정정한 4건의 정당성 평가:

| # | 추가 정정 | 사이클 56 추가분이었나? | 미정정시 결과 | 평가 |
|---|---------|----------------------|--------------|------|
| 1 | CREATE TABLE `scope`/`total_diff_count` 컬럼 제거 | YES (보고서 A-1, 사이클 56 주석 명시) | 신규 DB 생성 시 잔존 컬럼 (사용처 0) — 무해하나 노이즈 | **정당** (발주 의도 = "사이클 56 흔적 전량 제거" 명백) |
| 2 | `stock_audit_line_update` 헬퍼 제거 | YES (사이클 56 주석 블록 내부, 5310 라우트 단독 호출자) | 호출자 0건의 dead code → 무해하나 회귀 위험 | **정당** (사이클 56 동일 블록) |
| 3 | GET `/stock/audits/new` 라우트 제거 | YES (보고서 B-2, 사이클 56 주석 명시) | 렌더 대상 `stock_audit_new.html` 삭제됨 → **TemplateNotFound 런타임 에러** | **필수 정당** (미정정시 라우트 호출 시 500 에러) |
| 4 | GET `/stock/audits/{id}` 디테일 라우트 정정 | YES (사이클 56이 `summary=get_audit_summary(...)` + `stock_audit_detail.html` 렌더로 변경) | `get_audit_summary` 헬퍼 제거 → **NameError**; `stock_audit_detail.html` 삭제 → **TemplateNotFound** | **필수 정당** (둘 다 런타임 에러 직결) |

→ **결론: 4건 모두 정당**. 특히 #3·#4는 발주서 명시 범위만 적용 시 라우트가 ImportError/NameError/TemplateNotFound로 깨져 "기존 라우트 절대 보존" 의무 자체를 위반하게 되는 구조. 01이 정확히 식별해 "기존 라우트 보존 의무 우선" 원칙으로 정정한 판단은 정확하고 합리적.

> 정직성 v3: "사이클 56이 추가했다" 주장은 보고서 본문 인용에 의존(코드는 이미 삭제됨). git diff/blame은 미커밋 상태라 working tree만 검증 가능.

---

## 5. 발견 (있음 — S2 1건)

### S2-001 [낮음] — 사이클 56b rollback 미커밋

- **현황**: working tree에 -447줄 변경 적용되어 있으나 git log에 rollback 커밋 없음 (최근 커밋 7ddb9da chore(start) 까지만).
- **영향**: 다음 git pull/checkout 시 working tree 변경분 분실 위험. v3 정책상 매 사이클 커밋 의무는 부재하나, 수백줄 변경분이 미커밋인 것은 운영 리스크.
- **권고**: 09 팀장 → 01에 "사이클 56b 커밋 + push" 지시. 커밋 메시지: `revert(stock-audits): cycle 56b rollback (-447)`.

### 추가 관찰 (참고)
- CREATE TABLE stock_audits CHECK 제약의 `'DRAFT','IN_PROGRESS','FINALIZED'` 값이 잔존 (line 1088). 사이클 56이 CHECK 절도 확장했을 가능성. 사용처 0이므로 무해하지만, 완전한 사이클 55 상태 복귀를 원한다면 `('OPEN','COUNTING','REVIEW','CLOSED','CANCELLED')` 5개로 축소 권고. **결재 요청 사안 아님**, 무해.

---

## 6. 종합 판정

| 항목 | 판정 |
|------|------|
| 1. 회귀 (import / 라우트 5 / 헬퍼 / 템플릿 / 사이클56 흔적) | **PASS** |
| 2. P3 페르소나 4단계 정합 | **PASS** |
| 3. 보존 5/5 (외부자산 / 핫패치 / v2 / SQL / Jinja) | **PASS 5/5** |
| 4. 발주서 외 4건 정정 정당성 | **정당** (4건 모두 필수·합리적) |
| 5. 발견 | S2 1건 (미커밋), CHECK 제약 잔존 1건(무해) |

→ **rollback 정정 작업 검증 통과**. 회귀 0, 보존 5/5, 추가 정정 정당.

---

## 7. 산출

- 본 보고서: `04_HAIST_WORKS_운영테스트팀/_TO_09팀장_2026-04-27_사이클56b_rollback검증.md`
- 코드 수정 0건 (정책 준수)
