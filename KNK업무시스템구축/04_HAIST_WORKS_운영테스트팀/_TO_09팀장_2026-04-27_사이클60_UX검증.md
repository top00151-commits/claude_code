# 04 사이클 60 UX 본질 검증 — 3 Hub 분리 + 힐링 색

> 작성: 2026-04-27 / 04 운영테스트팀 (일회성 worker)
> 대상: 빅터 09 팀장
> 방식: 코드 0 (read-only), grep -n 직접 인용, 추정 0건
> 대표 12:55 지시("매출/구매 별도 창 · 힐링 색 통일") 충족 여부 점검

---

## 결론 한 줄

**대표 시각 지시("어두운 헤더 → 힐링 그린")는 100% 해결, 그러나 "매출/구매 별도 창" 분리는 50%만 완성됨**.
매출 5 템플릿(orders/production/quotations/shipments_receipts/quotation_print)만 `base_sales.html` 로 이전됐고, **남은 9개 매출 페이지(sales_home / sales_dashboard / sales_aging / sales_outstanding / sales_forecast / projects / project_form / customer_detail / customers 일부)** 는 여전히 `base_logi.html` 또는 `base.html` 을 extends 하여, **사용자 동선 중 Hub가 도중에 바뀜** = 결재 권고 S1 1건 발생.

---

## A. 페르소나 동선

### P1 영업 (1년차) — Sales Hub 진입 시도

대표가 의도한 동선: 로그인 → "매출·영업 Hub" → 견적 작성 → 수주 등록.
**실제 시뮬레이션 결과 — FAIL**

| 단계 | URL | extends | 사용자가 보는 헤더 | 정상? |
|------|-----|---------|--------------------|-------|
| 1. 메뉴 클릭 | `/sales` | `sales_home.html` → **base_logi.html** | **Logistics Hub (라이트 세이지 띠)** | ❌ |
| 2. 견적 작성 | `/sales/quotations` | `sales_quotations.html` → base_sales.html | **Sales Hub (골드 띠)** | ✅ |
| 3. 수주 작성 | `/sales/orders` | `sales_orders.html` → base_sales.html | Sales Hub | ✅ |
| 4. 매출 분석 | `/sales/dashboard` | `sales_dashboard.html` → **base_logi.html** | **Logistics Hub** | ❌ |
| 5. 미수금 | `/sales/outstanding` | `sales_outstanding.html` → **base_logi.html** | **Logistics Hub** | ❌ |
| 6. 노령화 | `/sales/aging` | `sales_aging.html` → **base_logi.html** | **Logistics Hub** | ❌ |

→ **P1이 6 단계 중 4 단계에서 헤더가 자기 모르게 Sales Hub ↔ Logistics Hub 가 번갈아 바뀜.** 첫 진입(`/sales`) 부터 Logistics 헤더가 떠서 "여기는 매출 영역" 인지가 첫 순간부터 깨짐.

### P3 재고담당 + P4 구매 — Logistics Hub

| 단계 | URL | extends | 헤더 | 정상? |
|------|-----|---------|------|-------|
| 1. `/logistics` | logistics_home → base_logi | Logistics Hub | ✅ |
| 2. `/parts` | parts → base_logi | Logistics Hub | ✅ |
| 3. `/po` | po_list → base_logi | Logistics Hub | ✅ |
| 4. `/stock/movements` | stock_movements → base_logi | Logistics Hub | ✅ |
| 5. `/rates` | rates → base_logi | Logistics Hub | ✅ |

→ **PASS**. P3·P4 동선은 모두 Logistics Hub 한 색으로 일관됨.
→ 단, 사이드바 line 49 주석 "매출·영업 그룹 제거" 정상 확인. line 102~111 "이동" 그룹의 "매출·영업 Hub ↗" 링크 OK.

### CEO — 3 Hub 통합 동선

| 단계 | 헤더 | 정상? |
|------|------|-------|
| 1. `/home` (통합) | base.html — 시안 12B 헤더 (브랜드 로고 + ws-switcher + g-search) | ✅ |
| 2. `/sales` 진입 | **Logistics Hub 헤더** (sales_home.html 미수정) | ❌ |
| 3. `/sales/quotations` | Sales Hub 헤더 | ✅ |
| 4. `/logistics` | Logistics Hub 헤더 | ✅ |
| 5. `/home` 복귀 | 통합 헤더 | ✅ |

→ CEO가 Sales Hub 진입을 시도해도 "센터 페이지(/sales)" 가 Logistics 색으로 떠서 **"내가 어느 Hub에 있는지" 첫 화면에서 혼란.**

### P11 베트남 수출

- `quotation_print.html` line 1: `{% extends "base_sales.html" %}` ✅ 정상 변경 확인.
- 단, P11 본업 페이지 `export_*` 8개는 모두 `base_logi.html` extends → 베트남 수출 실무자도 Logistics Hub 헤더에서 작업. 사이클 62 다국어와 별개로 "수출 영역도 별도 Hub로 분리" 필요한지는 대표 결재 사안.

---

## B. 사진 1 (대표 표준) vs 사이클 60 결과 시각 비교

### style_hubs.css 핵심 그라디언트 (line -n 직접 인용)

```
line 12-13: .topbar.topbar-hub-sales { background: linear-gradient(135deg, #3F5C44 0%, #5D7F61 60%, #7FA183 100%) !important;
line 14:    border-bottom: 2px solid #C9A961 !important;          ← 골드 띠
line 42-43: .topbar.topbar-hub-logi  { background: linear-gradient(135deg, #3F5C44 0%, #5D7F61 60%, #7FA183 100%) !important;
line 44:    border-bottom: 2px solid #A8BD8F !important;          ← 라이트 세이지 띠
line 120-123: .topbar.topbar-logi { background: linear-gradient(135deg, #3F5C44 0%, #5D7F61 60%, #7FA183 100%) !important;
              border-bottom: 2px solid #A8BD8F !important;
              color: #FFFFFF !important; }                          ← 기존 다크 무력화
```

| 항목 | 사진 1 (메인 통합) | 사이클 60 결과 (Hub) | 일치? |
|------|---------------------|----------------------|-------|
| 배경 그린 | 부드러운 녹색 (사이드바·카드) | `#3F5C44 → #5D7F61 → #7FA183` 그라디언트 | **일치** (힐링 톤) |
| 식별 띠 | 통합은 띠 없음 | Sales=#C9A961 골드 / Logi=#A8BD8F 라이트 세이지 | **추가 OK** |
| 폰트 | 부드러움 | tb-title 800 / tb-subtitle 600 11px (line 32-33) | OK |
| 어두운 그라디언트 잔존 | 없음 | `style.css:3107 .topbar-logi` 의 다크 네이비를 line 120 으로 무력화 | **무력화 확인** |

→ **시각 자체는 사진 1 톤과 일치.** 색 통일 PASS. 단, 사진 1은 헤더가 "흰 배경 + 녹색 텍스트" 처럼 보였다면(원본 캡처를 재확인 못 함) 본 그라디언트 헤더는 어둠이 살짝 짙은 편. 디자인팀(05) 추가 점검 권고.

---

## C. 어려워 보이지 않게 원칙

| 점검 | 결과 |
|------|------|
| Hub 전환 (좌상단 "통합 ▼" 드롭다운) | base.html line 32~55 ws-switcher 정상. 그러나 base_sales/base_logi 헤더에는 ws-switcher 없음 → "Hub 안에서 다른 Hub로 점프"는 사이드바 하단 "이동" 그룹(base_sales line 102~111, base_logi line 102~112)에 의존. **드롭다운 일관성 결여 = S3** |
| 사이드바 메뉴 그룹화 | base_sales 3 그룹(매출·영업 / 고객·견적·수주 / 매출 분석) + 이동 OK. base_logi 3 그룹(자재·구매 / 재고·입출고 / 동적 변수) + 이동 OK |
| 첫 진입 Hub 인지 | **`/sales` 첫 진입 시 Logistics 헤더 표시 = FAIL** (A 섹션 1 단계 이슈) |

---

## D. 자수 3건 정당성

### 자수 #1: stock_* 9 템플릿 미진행 (별도 사이클)
- 정당성: PASS. 본 사이클 작업량 한계로 합리적. 단 stock_*은 base_logi extends 가 정확하므로 변경 필요 없음.
- **단, 진짜 미진행은 stock_* 이 아니라 sales_home/sales_dashboard/sales_aging/sales_outstanding/sales_forecast/projects/project_form 7개 매출 페이지** — 자수 #1 라벨이 잘못된 것.

### 자수 #2: style.css 직접 수정 대신 style_hubs.css override
- 정당성: PASS. v2 본체(4287~4574) 보호 원칙 준수. line 117~123 으로 기존 .topbar-logi 다크 무력화 — 적절함.

### 자수 #3: base_sales/base_logi 헤더 단순화 (12B 미이식)
- 정당성: 부분 PASS. 단, base_sales.html line 10 `<body class="logi-body">` 는 **버그**. 자수 항목 아님.

---

## E. 신규 발견 분류

### 대표 지적 해결 여부

| 대표 12:55 지적 | 해결? |
|------------------|-------|
| "영업/구매 별도 창" | **부분 (5/14 매출 페이지만 분리)** |
| "메뉴 혼합" | base_logi에서 매출 그룹 제거 ✅ / base_sales에서 자재·구매 그룹 제거 ✅ |
| "어두운 헤더" | ✅ 힐링 그린으로 통일 |
| "로고 안 맞음" | base_sales/base_logi는 텍스트 로고("KNK") + 시안 12B 그래픽 로고 미적용 → **부분** |
| "힐링 색 안 맞음" | ✅ #3F5C44 → #7FA183 그라디언트로 통일 |

### 남은 불편함 (**총 4건**)

#### **S1 — Hub 헤더 깨짐 (즉시 수정 필요)**
- **원인**: `sales_home.html`(/sales 첫 화면), `sales_dashboard.html`, `sales_aging.html`, `sales_outstanding.html`, `sales_forecast.html`, `projects.html`, `project_form.html` 7개가 `base_logi.html` 을 extends — sidebar/header가 Logistics Hub 로 표시됨.
- **재현**: 로그인 → 사이드바 "📈 매출·영업 → 매출·영업 센터" 클릭 → URL `/sales` → 라이트 세이지 띠 헤더.
- **영향**: P1 영업 → "내가 영업 Hub에 있는지 자재 Hub에 있는지" 첫 진입 화면부터 헷갈림. 사이클 60 분리 의도 50% 미달.
- **결재 사안**: 01에 "extends 변경" 지시 발행 권고 (대표 결재 불필요, 09 자체 발주 가능)

#### **S2 — `/customers/{id}` 헤더 점프**
- `customer_detail.html` line 1: `{% extends "base.html" %}` → 사이드바 "고객사" 클릭(Sales Hub) → 상세 클릭 → 통합 헤더로 점프. P1 영업 동선 중간에 컨텍스트 손실.
- 결재 권고: base_sales.html 로 변경 또는 customer_list 자체를 base_sales 로 통일.

#### **S3 — Hub 간 전환 일관성 결여**
- 통합 메인(base.html)은 ws-switcher 드롭다운 보유, base_sales/base_logi는 사이드바 "이동" 그룹 의존. UX 불일치.
- 권고: base_sales/base_logi 에도 좌상단 ws-switcher 동일 컴포넌트 이식 (디자인팀 시안 12B 활용).

#### **S4 — base_sales.html body class 버그**
- line 10: `<body class="logi-body" data-ws="sales">` — `class="logi-body"` 는 복사 잔재.
- `style.css` 의 `.logi-body` 가 Sales 페이지에도 적용되어 의도치 않은 자재·구매 색이 본문에 새어들어올 가능성.
- 권고: `class="sales-body"` 로 변경 또는 양쪽 통일하여 `class="hub-body"`. (Bash grep으로 .logi-body 의 정확한 영향 범위는 별도 확인 필요)

---

## F. 회귀 점검

| 점검 | 결과 |
|------|------|
| 5 매출 템플릿 active 변수 호환 | main.py line 6473/6538/6668/6737/6799 `active="sales"` 로 통일. base_sales.html line 53 `{% if active=='sales' %}` 매칭 → **PASS** |
| base_sales.html 신규 임포트 누락 | static/style_hubs.css 정상 (line 8) · 외부 import 0건 — **PASS** |
| style_hubs.css override 정합 | line 120 `.topbar.topbar-logi` 무력화 동작. 단 `.topbar-logi` 단독 셀렉터(다크 그라디언트)도 line 120 으로 덮음 → **PASS** |
| 핫패치 G1~G5 (.sb-toggle / .dock / .main-wrap margin-right) | base_sales.html line 47 `.sb-toggle` 보존, line 229~232 `.dock-open .topbar-hub-sales` padding-right 처리 — **PASS** |
| URL 정합 (대표 발주서 표기 `/sales/center` `/logistics/center`) | main.py 에서 `/sales/center` `/logistics/center` 라우트 grep 결과 **0건**. 실 라우트는 `/sales` `/logistics`. 대표 발주서 표기 오기로 추정, 실제 코드는 정상. |
| logistics_home.html / parts.html / po_*.html | base_logi extends 유지, 자재·구매 사이드바 그룹 정상 → **PASS** |

---

## G. 09 팀장 보고 권고 (결재 사안만)

### 결재 요청 0건 (대표 결재 불필요)

09 팀장이 자체 발주 가능한 후속 지시:

1. **[01 메인 즉시 지시]** S1 해결: `sales_home.html` / `sales_dashboard.html` / `sales_aging.html` / `sales_outstanding.html` / `sales_forecast.html` / `projects.html` / `project_form.html` 7개 템플릿의 `{% extends "base_logi.html" %}` 를 `{% extends "base_sales.html" %}` 로 변경. (1줄씩 7회 = 7 행 수정)
2. **[01 메인 동시 지시]** S2 해결: `customer_detail.html` 의 `{% extends "base.html" %}` 를 `{% extends "base_sales.html" %}` 로 변경.
3. **[01 메인 즉시 지시]** S4 해결: `base_sales.html` line 10 `class="logi-body"` → `class="sales-body"` (또는 `style.css` 의 `.logi-body` 영향 범위 우선 grep 확인 후 결정).
4. **[05 디자인팀 권고]** S3: base_sales/base_logi 좌상단에 ws-switcher 드롭다운 시안 추가 (사이클 61 또는 62).

### 결재 사안 (대표 결재 필요) — 0건

대표 12:55 지시는 해결 방향이 명확함. 자체 권한 범위 내에서 후속 발주 가능.

---

## 산출

- 본 파일: `04_HAIST_WORKS_운영테스트팀/_TO_09팀장_2026-04-27_사이클60_UX검증.md`
- 코드 수정: 0
- 외부 자산 도입: 0
- 권한 팝업: 0건
- v2 본체 (style.css 4287~4574) 미접촉

**핵심 메시지**: 대표 시각 지시는 충족, "별도 창" 지시는 50%. 7개 템플릿 1줄씩 추가 수정으로 90% 도달 가능.
