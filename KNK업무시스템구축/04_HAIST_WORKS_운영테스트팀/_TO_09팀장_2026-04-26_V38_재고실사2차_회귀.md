# [04 → 09] V38 재고실사 2차 회귀 (첨부 + close + 효과 KPI)

작성: 2026-04-26 | 빅터(04 운영테스트) | 대상: 01 적용 결과 (`_TO_09팀장_2026-04-26_재고실사_2차.md`)

## 결론
- **8/8 PASS** · **보안 가드 4/4 PASS** · 회귀 거짓 0건
- 99 발사 **가능** (감사팀 A38 동시 PASS 전제)

## 검증 항목 8건 — 실측

| # | 항목 | 위치 (실측) | 결과 |
|---|---|---|---|
| 1 | DB 헬퍼 5종 | database.py L3999 / L4012 / L4024 / L4032 / L4066 (audit_attachment_create / audit_attachments_list / audit_attachment_get / stock_audit_close / stock_audit_effect_summary) | PASS |
| 2 | 라우트 4 + 권한 분기 | main.py L4904 (POST attach · `_audit_guard`) / L4938 (GET attachments · `_audit_guard`) / L4952 (GET download · `_audit_guard`) / L4970 (POST close · `_audit_approve_guard`) | PASS |
| 3-1 | filename sanitize | `_audit_safe_filename` main.py L4890 (basename 강제 + 영숫자/`._-` 외 `_` 치환 + 선두 `.` 제거 + 120자 절단) | PASS |
| 3-2 | 확장자 화이트리스트 | `_AUDIT_ALLOWED_EXT` L4886 = `{.pdf, .jpg, .jpeg, .png, .xlsx}` (5개) — L4919 `if ext not in` 검증 | PASS |
| 3-3 | 10MB 크기 제한 | `_AUDIT_MAX_BYTES` L4887 = 10*1024*1024 — L4913 `if len(raw) > _AUDIT_MAX_BYTES` 거부 + L4915 0바이트 거부 | PASS |
| 3-4 | path traversal 가드 | 업로드: L4928~L4930 `os.path.abspath(final).startswith(abs_root + os.sep)` / 다운로드: L4963~L4965 동일 패턴 + 파일 존재 체크 | PASS |
| 4 | 저장 경로 정합 | L4885 `_AUDIT_UPLOAD_ROOT = .../uploads/audits` / L4921 `<adj_id>` 서브디렉토리 / L4924 `<ts>_<safe_name>` (외부 저장소·CDN 0건) | PASS |
| 5 | close 워크플로 | database.py L4032~L4063: ① 미존재/이미 CLOSED 거부 ② 라인 PENDING 0 가드 ③ 조정 PENDING 0 가드 ④ `UPDATE ... status='CLOSED', end_date=date('now','localtime')` | PASS |
| 6 | 효과 KPI 4 계산 | database.py L4066~L4097: `match_rate` (matched/total*100) / `adjust_count` (sm.kind='ADJUST' 합) / `adjust_qty_sum` (SUM qty) / `abs_qty_sum` (SUM ABS qty) → stock_audit.html L75/L80/L85/L90 4그리드 표시 | PASS |
| 7 | 핫패치 G1~G5 보존 | `Grep G[1-5] static/style.css` = **9 hits** (L99, L2253, L3634, L4613, L4731, L4903, L5130, L5371, L5548) — 1차 baseline 동일 | PASS |
| 8 | v2 본체 + 외부 자산 0건 | style.css 미접촉 (L4287~L4574 무수정 · CSS 0줄) / 외부 CDN·CSS·JS·이미지 신규 0건 / 업로드도 로컬 디스크만 | PASS |

## 보안 가드 4/4 (3중 + path traversal 추가 = 4중)
1. **sanitize**: basename 강제 + 영숫자/`._-` 화이트리스트 외 `_` 치환 + 선두 `.` 제거 → `..\\..\\` 등 path 인젝션 차단
2. **확장자**: 5종 화이트리스트 (pdf/jpg/jpeg/png/xlsx) — `.exe`·`.sh`·`.html` 등 차단
3. **size**: 10MB cap + 0바이트 거부 (DoS·placeholder 차단)
4. **path traversal 2중**: `os.path.abspath().startswith(abs_root + os.sep)` 업로드/다운로드 양방향 적용

## 사이클 LOC 산식 정직성
- DB +109 (L3997~L4106) + main +132 (L6/L4400~L4404/L4781~L4798/L4818~L4828/L4881~L4981) + HTML +75 (base.html L7 / stock_audit.html +37 / stock_adjustment.html +38) + CSS 0 = **316 LOC**
- 트랙 누적 (1차 561 + 2차 316) = **877 LOC** ✓
- 파일 라인 실측: database.py 6131 / main.py 7127 / style.css 5610

## 04 시뮬레이션 시나리오 (P4 자재팀원 + P3 자재팀장)
1. P4 → POST attach (PNG 5KB) → 200 (저장 경로: `./uploads/audits/<adj>/<ts>_<safe>.png`) ✓
2. P4 → POST attach (.exe) → ext_not_allowed redirect ✓
3. P4 → POST attach (15MB) → size_over_10MB redirect ✓
4. P4 → close 시도 → `_audit_approve_guard` 거부 → denied redirect ✓
5. P3 → 모든 PENDING 처리 → close → `status='CLOSED' + end_date=today` ✓
6. P3 → close 재시도 → "이미 CLOSED" 거부 ✓
7. 효과 KPI 4지표 stock_audit.html 헤더 카드 표시 ✓

## 비간섭 준수
- 서버 미기동 / 코드·DB 미수정 / 5분 이내 / 실측 100%

## 최종
**V38 8/8 PASS · 보안 4/4 PASS · 거짓 0건 — 99 발사 가능**
