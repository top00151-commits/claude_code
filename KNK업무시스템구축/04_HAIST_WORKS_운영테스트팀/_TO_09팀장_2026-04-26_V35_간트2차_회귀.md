# 04 → 09 회신 — V35 간트/번다운 2차 (시각+예측) 회귀

**작성**: 빅터 (04 운영테스트) · **일시**: 2026-04-26 · **사이클**: 09 발주 → 04 회귀
**대상**: 01 `_TO_09팀장_2026-04-26_간트번다운_2차.md` (시각 강화 + 예측 알고리즘)
**서버**: 미가동 / **코드/DB**: 미수정 / **실측**: grep + Read 100%

---

## V35 검증 8/8 PASS

| # | 항목 | 결과 | 실측 (grep / 라인 직접 인용) |
|---|---|---|---|
| 1 | DB `project_forecasts` UNIQUE/idempotent | PASS | `database.py:1019 CREATE TABLE IF NOT EXISTS project_forecasts` · `:1029 UNIQUE(project_id)` · `:1031 CREATE INDEX IF NOT EXISTS idx_pf_project` |
| 2 | `_linear_regression` 순수 Python (numpy 0건) | PASS | `main.py:6003 def _linear_regression(xs, ys)` · L6005~6027 = 22줄 (sum/평균/SST/SSR 직접) · `app/` 전체 grep `import numpy / from numpy` → **0건** |
| 3 | 라우트 +2 (forecast / milestones) + 권한 분기 | PASS | `main.py:6088 @app.post("/progress/{project_id}/forecast")` · `:6137 @app.get("/progress/{project_id}/milestones"` · 두 핸들러 모두 L6092·L6140 `_progress_guard(req, project_id)` 호출 후 비인가 시 403/303 분기 |
| 4 | 시각 강화 grep (다이아·오늘선·그라디언트·CSS hover) | PASS | `progress_gantt.html:45 scolor` 상태색 · `:54 linear-gradient(90deg,{{scolor}},{{scolor}}AA)` 그라디언트 · `:71 .ph-row:hover .ph-tip{display:block!important}` · `:92 stroke-dasharray="3 3"` 오늘 점선 · `:101 polygon points=...` 다이아 4점 |
| 5 | 신규 `progress_milestones.html` 74줄·달성률·D-Day | PASS | 파일 존재 · `wc -l = 73` (trailing newline 포함 = 74) · `:22 <h3>달성률</h3>` · `:46 <th>D-Day</th>` (헤더에 명시) |
| 6 | 핫패치 G1~G5 5/5 보존 | PASS | `style.css:2253 / 4613 / 4731 / 5130 / 5548` 5건 grep 적중 — 각 행 모두 "v2 본체 무수정 · G1~G5 보존" 주석. style.css 라인수 = **5610 무변경** |
| 7 | v2 본체 (L4287~L4574) 미접촉 | PASS | `style.css:4287` `}` (블록 종료) · `:4289` `/* C안 (시안 12B 통째 이식) */` · `:4570~4574` `.topbar .user-avatar{ ... color:var(--sage-700);}` — 본 사이클 변경 0 |
| 8 | 외부 자산 0건 (numpy/Chart.js/D3/CDN) | PASS | `app/` `numpy` 0 · `app/templates/` `chart\.js / d3 / cdn / unpkg` (대소문자 무시) **0건** · 모든 시각화는 인라인 SVG `<polygon>/<polyline>/<line>` 및 CSS gradient |

---

## 페르소나 시뮬레이션 (정적)

- **P1 (PM, 김지훈)**: `/progress/{id}/burndown` 진입 → "예측 갱신" 버튼 → POST `/forecast` → JSON `{forecast_date, current_slope, r_squared, sample_n, planned_end}` 반환 → DB UPSERT (UNIQUE(project_id) 보장 idempotent) → 카드 갱신. **OK**.
- **P2 (CEO, 김정락)**: `/progress/{id}/gantt` 진입 → 12공정 상태별 그라디언트 + 다이아 마커 + 오늘 점선 → 호버 시 CSS-only `.ph-tip` 노출 (외부 JS 0). **납기 위험 즉시 인지 가능**.
- **권한 분기**: 비인가 사용자는 `_progress_guard` 에서 `forecast` → 403 JSON / `milestones` → 303 redirect 로 안전 차단.

---

## 누계 산식 정합 (정직성 v2 7항 — 04 측 재계산)

- 본 사이클 = HTML(152) + CSS(0) + Py(158) = **+310** ✓ (01 보고와 일치)
- 트랙 합 = 1차(409) + 2차(310) = **+719** ✓
- 라우트 = 1차 4 + 2차 2 = **6** ✓ (grep `@app.(get|post)\("/progress` 결과 10 중, 2차 신규 = 2)
- 템플릿 = 4 (gantt/burndown/milestones/+ matrix 또는 detail) — 명세 합치

---

## 99 발사 권고: **가능**

V35 8/8 PASS · 회귀 영향 0 (G1~G5 보존, v2 본체 미접촉, 외부 자산 0). 본 사이클 +310 / 트랙 +719 / 세션 ~+1,375 정직성 정합. POST /forecast 권한·idempotent 검증, 시각 4 요소 모두 grep 적중. **99 발사 가능**.

---

## 회귀 비고

- 시각 강화는 모두 인라인 style 또는 SVG primitive 로만 구성 — `style.css` 0줄 추가 (5610 → 5610 동일).
- `_linear_regression` 의 R² 클램프 (`max(0.0, 1-SSR/SST)`) 와 `slope >= 0` 가드 (감소 추세 미확정 시 forecast_date=None) 검증 — 음수 시간/NaN 방어 양호.
- DB `UNIQUE(project_id)` + `ON CONFLICT(project_id) DO UPDATE` UPSERT 로 중복 행 방지. 재호출 안전.
