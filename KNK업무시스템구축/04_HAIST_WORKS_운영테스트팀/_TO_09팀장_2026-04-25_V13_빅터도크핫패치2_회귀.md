# V13 — 빅터도크 우측가림 핫패치2 회귀 검증

**일시**: 2026-04-25 (대표 22:00 시정 명령 직후)
**검증자**: 04 운영테스트팀 (정적 grep 100% 실측, 서버/코드 무접촉)
**대상 변경**: `static/style.css` line 3631~3641 (5줄 추가) + BAT 2종

---

## 1. 검증 결과 요약

| # | 항목 | 결과 |
|---|---|---|
| 1 | 새 룰 `body.dock-open .main-wrap` 매칭 | PASS (2건) |
| 2 | margin-right 시프트 값 정합 | PASS (420px) |
| 3 | 어제 G1~G4 핫패치 보존 | PASS (4/4) |
| 4 | v2 본체(line 4269~4574) 미접촉 | PASS |
| 5 | BAT 양 파일 LAST UPDATE 갱신 | PASS (6라인) |

**5/5 PASS.**

---

## 2. 항목별 실측

### 2-1. 새 룰 grep
```
3636: body.dock-open .main-wrap { margin-right: 420px; }
3641:   body.dock-open .main-wrap { margin-right: 0; }   (모바일 분기 @media max-width:1023px)
```
데스크톱 활성 1건 + 모바일 무효화 1건 = 2건. 의도와 일치.

### 2-2. margin-right 값
- 데스크톱 `420px` 고정 (G1 기존 패턴과 동일, var(--dock-width) 미사용 — 일관)
- 모바일 `0` (도크가 풀스크린 자체로 동작하는 폭에서 시프트 무효화)
- 트랜지션: `margin-right 400ms cubic-bezier(0.33,1,0.68,1)` G1과 동일

### 2-3. G1~G4 보존
- G1 line 3625 `body.dock-open .main.has-sidebar { margin-right: 420px }` 보존 (단, 3637에서 도크 열림 시 0으로 덮음 — `.main-wrap`에서 이미 시프트되므로 이중 적용 방지)
- G2 line 3627 주석화 상태 유지, 활성 `body.dock-open .topbar` 규칙 0건
- G3 line 3648 주석 보존
- G4 line 3651 `top: var(--topbar-h, 84px)` 1건 보존
- 추가: `<div class="sb-toggle"` 템플릿 검색 0건 (G1-base 정합)

### 2-4. v2 본체 미접촉
- line 4269~4574 범위 확인: `.mini-tbl`, `.empty`, `.topbar .user-*` — 22:00 핫패치는 line 3631~3641만 추가, 본체 0건 변경

### 2-5. BAT 갱신
- `KNK_시작.bat`: REM/title/echo 3라인에 `2026-04-25 빅터도크-우측가림-핫패치2` 동기화
- `START.bat`: REM/title/echo 3라인 동기화 (영문 주석 동등 의미)

---

## 3. 잔여 리스크 / LIVE 검수 트리거

- 정적 grep PASS — 런타임 LIVE 검수는 대표 직접 (도크 열고 `+ 새 업무 등록` / `등록` 버튼이 도크 좌측으로 시프트되는지 육안 확인)
- 트랜지션 400ms — `prefers-reduced-motion` 사용자에 대해 line 3609 기존 블록이 일괄 처리 (별도 보강 불필요)

**결론: 99 발사 가능. LIVE 검수 트리거 준비 완료.**
