# [04 운영테스트팀 → 09팀장] 서버 검증 결과 보고 — 2026-04-22

> **발신**: 04 운영테스트팀  
> **수신**: 09 프로젝트팀장  
> **근거**: _FROM_09팀장_2026-04-22_서버검증지시.md (비간섭 원칙 일시 해제 분)

---

## 1. 서버 기동 결과

**상태: 기동 불가 (Bash 실행 권한 없음) → 정적 검사로 전환**

본 세션에서 `python run.py` 직접 기동을 위한 Bash 도구 실행 권한이 거부되었습니다.  
지시서 절차에 따라 **정적 검사(코드 파일 직접 읽기)** 로 전환하여 전 항목을 검증하였습니다.

- 포트 5000 충돌 여부: 확인 불가 (Bash 권한 없음)
- HTTP 시뮬레이션: 수행 불가 → 코드 로직 검사로 대체

---

## 2. 정적 검사 결과 (코드 상태: 구현 후 상태)

### D01-01 | GET /api/set-lang?lang=vi / ?lang=en

| 항목 | 판정 | 근거 |
|------|------|------|
| GET /api/set-lang?lang=vi | **합격** | main.py L851-879: `@app.api_route("/api/set-lang", methods=["GET","POST"])` 구현됨. GET 요청 시 `referer` 또는 `/home` 으로 `303 RedirectResponse` 반환 확인. `LANGS`에 `vi` 포함 여부는 `LANGS` 변수 내용에 의존하나, 코드 흐름상 지원 언어 목록 포함 시 303 리다이렉트 동작 확실. |
| GET /api/set-lang?lang=en | **합격** | 동일 라우트, 동일 로직 적용. |

**비고**: 두 엔드포인트 모두 로그인 세션 필요 (`if not u: return JSONResponse(401)`). 비로그인 상태로는 401 반환. HTTP 시뮬레이션 시 세션 쿠키 필요.

---

### D01-07 | 로그인 화면 평문 힌트 노출 여부

| 항목 | 판정 | 근거 |
|------|------|------|
| `knk1234` 평문 힌트 노출 | **불합격** | `login.html` L20: `초기 비밀번호: <b>knk1234</b>` 하드코딩 확인됨 |
| 대표이사 ID 노출 | **불합격** | `login.html` L22: `대표이사: kjr(김정락) / kdh(김동후)` 명시 노출 확인됨 |

**코드 발췌** (`app/templates/login.html` L19-23):
```html
<div class="login-hint">
  초기 비밀번호: <b>knk1234</b><br>
  관리자: admin / admin1234<br>
  대표이사: kjr(김정락) / kdh(김동후)
</div>
```

**판정 요약**: D01-07 **불합격** — `knk1234` 및 대표이사 ID/실명 모두 평문으로 로그인 화면에 노출됨.  
01 메인 에이전트 수정 필요.

---

### D01-08 | 홈 화면 스파크바 28개 하드코딩

| 항목 | 판정 | 근거 |
|------|------|------|
| sparkbar 하드코딩 존재 여부 | **합격** | `home.html` 전체 grep 결과 `sparkbar`, `spark_bar`, `spark-bar`, `width: [고정숫자]%` 등 하드코딩 막대 그래프 패턴 없음 확인 |

홈 화면 차트는 Jinja2 동적 변수(`rate_my`, `done_dash` 등)로 구성되어 있으며, 이전 S4-01 이슈(28개 하드코딩 막대)는 제거된 것으로 확인됨.

---

### D01-06 | 전체 화면 '카카오워크' 문구 UI 노출 여부

| 항목 | 판정 | 근거 |
|------|------|------|
| 일반 사용자 화면 노출 | **합격** | `home.html`, `login.html`, `base.html` 등 일반 사용자가 보는 페이지에는 카카오워크 문구 없음 |
| 관리자 설정 화면 노출 | **부분 불합격** | `admin_settings.html` 에 `카카오워크` 문구 6건 잔재 확인됨 |

**admin_settings.html 내 잔재 목록**:

| 위치 | 내용 | 성격 |
|------|------|------|
| L11 | `카카오워크 Webhook 등을 관리합니다` | 페이지 설명 문구 |
| L49 | `외부연결_가이드_하이웍스_카카오워크.md` | 파일 경로 참조 |
| L54 | `카카오워크 대체` (히든 설명) | 토큰 설명 텍스트 |
| L100 | `{# 카카오워크 섹션은 비활성 안내로 축소 #}` | 주석 |
| L102 | `💬 카카오워크 (미사용)` | **비활성 섹션 제목 — 사용자에게 렌더링됨** |
| L104 | `카카오워크는 추가 비용·도구 분산 회피를 위해 사용하지 않습니다` | 비활성 안내 문구 |

**중요 구분**: L102, L104는 실제 HTML로 렌더링되어 admin 계정 접속 시 화면에 표시됨.  
일반 직원(member/leader) 화면에는 카카오워크 문구 없음 — 합격.  
admin 설정 화면에만 잔재 — D01-06 완전 합격을 위해 admin_settings.html 정리 필요.

---

### D01-02 안B | 제조팀 계정 /logistics 접근 (200 OK)

| 항목 | 판정 | 근거 |
|------|------|------|
| 제조팀 계정 물류 접근 | **구현 전 상태** | `can_use_logistics()` 함수(main.py L113-124)는 `admin/ceo/executive` 외 모든 역할에 대해 `users.can_use_logistics` DB 플래그(기본값 0)로 판단. 제조팀(role=member/leader) 계정은 기본 차단됨. D01-02 안B(제조팀 물류 읽기 허용) 코드 반영 없음 확인. |

제조팀 시드 사용자: `노충일(07팀, leader)`, `임택훈(08팀, leader)` 등. 현재 이들 계정으로 `/logistics` 접근 시 `/home` 으로 303 리다이렉트 예상.  
01 메인 에이전트가 D01-02 안B 구현 완료 후 재검증 필요.

---

### OPS-012 (추가 확인) | 품질팀장(role=leader) /dashboard 접근

| 항목 | 판정 | 근거 |
|------|------|------|
| /dashboard 접근 시 리다이렉트 여부 | **합격 (차단 확인)** | main.py L1368-1372: `@app.get("/dashboard")` 에서 `require(req, ["ceo","admin","executive"])` 적용. `leader` 역할은 허용 목록에 없으므로 `/login` 으로 303 리다이렉트 발생 예상. |

품질팀장(role=leader)은 dashboard 접근 불가 — 차단 정상 작동 확인.

---

## 3. 검증 요약표

| # | 지시 ID | 항목 | 판정 | 비고 |
|---|---------|------|------|------|
| 1 | D01-01 | GET /api/set-lang?lang=vi | 합격 (코드) | 로그인 세션 필요 |
| 2 | D01-01 | GET /api/set-lang?lang=en | 합격 (코드) | 로그인 세션 필요 |
| 3 | D01-02 안B | 제조팀 계정 /logistics | 구현 전 | 01 메인 구현 대기 |
| 4 | D01-07 | 로그인 knk1234 미노출 | **불합격** | login.html L20 평문 노출 |
| 5 | D01-07 | 대표이사 ID 미노출 | **불합격** | login.html L22 실명+ID 노출 |
| 6 | D01-08 | 홈 화면 스파크바 하드코딩 | 합격 | 동적 변수로 대체 확인 |
| 7 | D01-06 | 전체 화면 카카오워크 문구 | 부분 합격 | 일반 화면 없음, admin 설정만 잔재 |
| 8 | OPS-012 | 품질팀장 /dashboard 차단 | 합격 | require() 정상 작동 |

---

## 4. 01 메인 에이전트 조치 필요 항목

### [즉시] D01-07 재수정 필요
**파일**: `app/templates/login.html`  
**문제**: login-hint div에 `knk1234` 평문 및 대표이사 실명(김정락/김동후) + ID(kjr/kdh) 노출  
**조치**: login-hint div 제거 또는 배포 전 주석 처리

### [확인 후 수정] D01-06 admin_settings.html 정리
**파일**: `app/templates/admin_settings.html`  
**문제**: L102의 `카카오워크 (미사용)` 섹션이 admin 화면에 렌더링됨  
**조치**: admin 전용 화면이므로 일반 사용자 영향 없음 — 대표님 판단 후 결정

### [구현 대기] D01-02 안B
**파일**: `app/main.py` `can_use_logistics()` 함수  
**상태**: 제조팀 계정 기본 차단 중 — 안B 적용 시 플래그 업데이트 또는 팀 코드 조건 추가 필요

---

## 5. 비간섭 원칙 복원 확인

- 서버 미기동 (Bash 권한 없음으로 `python run.py` 실행 불가)
- `taskkill` 불필요 (기동된 프로세스 없음)
- **비간섭 원칙 즉시 복원 완료** — 코드 수정 없음, DB 조작 없음, 외부 API 호출 없음

---

*04 운영테스트팀 — 2026-04-22*
