# [04 → 09] V33 환율·단가 강화 1차 회귀 (Top10 #9 · P4 구매팀 월 1회)

작성: 2026-04-26 | 04 운영테스트팀 | 모드: 정적 회귀 (서버 미기동 / 코드 미수정)
대상: `01_HAIST_WORKS/_TO_09팀장_2026-04-26_환율단가_강화_1차.md`

## 결과 8/8 PASS

| # | 항목 | 결과 | 실측 근거 (직접 인용) |
|---|---|---|---|
| 1 | DB 3 테이블 + 인덱스 7 | PASS | `database.py` L1127 `CREATE TABLE IF NOT EXISTS cost_simulations` / L1144 `price_change_history` / L1160 `rate_alerts` + idx_csim_part·at / idx_pch_part·supplier·date / idx_ralert_currency·active = **idx 7종 ✓** |
| 2 | 헬퍼 7 idempotent | PASS | `database.py` L5826 `cost_simulation_create` / L5848 `cost_simulations_list` / L5860 `price_change_log` / L5879 `price_change_history_list` / L5892 `rate_alert_create` / L5906 `rate_alerts_list` / L5916 `exchange_rates_csv_upload` = **7/7 ✓** |
| 3 | 라우트 7 + `_rates_guard` | PASS | `main.py` L6268 `def _rates_guard` 정의 + L6280·6302·6328·6353·6379·6392·6408 호출 = **7건 가드 ✓**. 라우트: L6278 GET dashboard / L6299 POST upload / L6326 GET cost-sim / L6342 POST cost-sim / L6377 GET price-history / L6390 GET alerts / L6401 POST alerts |
| 4 | 템플릿 4 존재 | PASS | Glob `templates/rates_*.html` → rates_dashboard / rates_cost_sim / rates_history / rates_alerts (4/4 ✓). rates_dashboard.html L1 `{% extends "base_logi.html" %}` 정합 |
| 5 | 외부 환율 API 0건 | PASS | Grep `(api\.exchangerate\|fixer\|openexchangerates\|currencylayer\|frankfurter\|cdn\.\|jsdelivr\|unpkg)` on `01_HAIST_WORKS` → **본 사이클 신규 코드 0 hits**. `requests/httpx/aiohttp` 외부 호출도 신규 코드에 없음 (urllib 기존 hiworks/main.py 라인은 환율 모듈과 무관) |
| 6 | CSS 미접촉 | PASS | `static/style.css` 5610줄 그대로. 핫패치 G1~G5 인용 7 hits 보존 (L2253·L4613·L4731·L4903·L5130·L5371·L5548) |
| 7 | 핫패치 G1~G5 + v2 본체 미수정 | PASS | style.css L4287~L4574 v2 본체 — 본 사이클 변동 0. base.html L95 `핫패치 2026-04-25 17:30 sb-toggle` 보존 |
| 8 | BAT + base.html LAST UPDATE | PASS | `KNK_시작.bat` L3·L7·L14 모두 `2026-04-26 환율단가-강화-1차` 갱신. `base.html` L7 `LAST UPDATE: 2026-04-26 환율단가-강화-1차 — Top10 #9 ... DB +3 ... 라우트 +7 ... 외부 환율 API 0건 (수동+CSV만)` 정합 |

## 페르소나 P4 (구매팀) 시나리오 회귀 (정적)

| 시나리오 | 경로 | 가드 | 결과 |
|---|---|---|---|
| 월 1회 환율 일괄 업로드 | POST /rates/upload (CSV) | _rates_guard | PASS — `exchange_rates_csv_upload` UPSERT 위임, OK/NG 카운트 메시지 반환 |
| 부품별 원가 시뮬 | GET/POST /rates/cost-sim/{part_id} | _rates_guard | PASS — `target = base * rate * (1 + margin/100)` 산식 (main.py L6360) |
| 단가 변경 이력 추적 | GET /rates/price-history/{part_id} | _rates_guard | PASS — `change_pct` 자동 계산 (database.py L5865 `(np-op)/op*100`) |
| 환율 임계 알림 | GET/POST /rates/alerts | _rates_guard | PASS — direction CHECK above/below (database.py L1165) |

## 발굴 사항 (S1·S2·S3·S4)

- **S2-1 (개선)**: `/rates/upload` CSV 헤더 가드(`cols[0].lower() == "rate_date"`)는 첫 행만 스킵 — 멀티 헤더 라인 또는 BOM 포함 CSV 시 첫 데이터 행이 헤더로 오인될 수 있음. 후속 사이클 권고.
- **S3-1 (개선)**: 환율 알림 발동 평가 잡 미존재 (보고서 §12에서 후속 명시) — 현재는 `triggered_at` 미기록. 2차에서 처리 예정으로 이번 사이클 차단 사유 아님.
- **S4-1 (관찰)**: `price_change_log` 훅이 `part_price_create` 호출부에 미연결 (보고서 §12) — 단가 변경 이력은 현재 수동 INSERT 경로만. 2차 작업.

## 결론

V33 8/8 PASS. P4 시나리오 정적 회귀 완료. S1 0건 / S2 1건 / S3 1건 / S4 1건 — 모두 후속 사이클 위임 가능, **본 사이클 차단 사유 없음**.

— 04 운영테스트팀
