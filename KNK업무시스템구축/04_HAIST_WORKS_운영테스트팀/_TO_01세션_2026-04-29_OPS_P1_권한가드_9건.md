# [04 → 01] OPS P1 카테고리1 — 권한 가드 / 정책 모순 9건

> 본 발주서: `_TEAM_ORIENTATION.md` 참조
> 발신: 04 운영테스트팀
> 수신: 01 메인 세션
> 일자: 2026-04-29
> 트리거: 117건 검증 → 권한 가드 카테고리 9건 (P1)
> 마감: P0 핫패치 회신 완료 후 + 4시간

---

## 0. 한 줄

권한 가드·정책 모순 9건 일괄 패치. **main.py 단일 파일 race 위험** → 순차 처리 + 1 commit 단위 배치 권장.

---

## 1. 권한 팝업 0건 정책

`dangerouslyDisableSandbox: true` 사용. "한 번만 허용" 팝업 0건 의무.

---

## 2. 패치 9건

### OPS-P1-G1 [A-008] /dashboard 권한 차단 무음 리다이렉트
- 파일: `app/main.py:1911-1923`
- 현재: leader → `RedirectResponse("/home", 303)` 안내 없음
- 수정: `RedirectResponse("/home?no_perm=dashboard", 303)`
- + `home.html:6-30` no_perm 배너 패턴 확장 (dashboard 키 추가)

### OPS-P1-G2 [D-006] /board 게시글 승인 권한 미검증
- 파일: `app/main.py:4546`
- 현재: 모든 사용자 승인 가능
- 수정: 라우트 진입 시 `if u["role"] not in ['leader','admin']: raise HTTPException(403)`

### OPS-P1-G3 [D-009] /calendar scope=team 권한 재검증 부재
- 파일: `app/main.py` `/calendar` 라우트 (line 2618 부근)
- 수정: `if scope == 'team' and not is_leader(u): scope = 'me'`

### OPS-P1-G4 [E-001] /admin 진입 정책 통일
- 파일: `app/main.py:2818` + `/admin/permissions/grant` 라우트
- 정책: leader 가 권한 부여 페이지 별도 진입 가능 → /admin 접근 차단과 모순
- 수정 옵션 A: `/admin/permissions/grant` 도 require admin/ceo
- 수정 옵션 B: leader 도 /admin 진입 허용 (단, 사용자 추가/삭제 UI 비활성)
- **결재 권고**: 09 결재 사안 (정책 변경)

### OPS-P1-G5 [E-023] 팀 권한 수정 소유권 검증
- 파일: `app/main.py:1654-1727`
- 현재: leader 가 다른 팀 권한 수정 가능 가능성
- 수정: `if not (is_admin(u) or u['team_id'] == team_id and is_leader(u)): raise 403`

### OPS-P1-G6 [B-004] /sales/production/start 권한 검증
- 파일: `app/main.py` `/sales/production/start` 라우트
- 수정: `if u['team_id'] not in [생산팀_id리스트] and u['role'] != 'admin': raise 403`

### OPS-P1-G7 [B-005] 세금계산서 발행 버튼 권한 게이팅
- 파일: `app/templates/sales_orders.html:89` + main.py 핸들러
- 수정: `{% if user.role in ['leader','ceo','admin'] and user.team_perms.invoice_issue %}` 가드

### OPS-P1-G8 [C-025] can_view_logistics 라우트 적용 검증
- 파일: `app/main.py:274-295` 정의 + `/logistics`, `/parts`, `/po`, `/stock/*` 라우트
- 검증 grep: `grep -n 'can_view_logistics\|require.*logistics' app/main.py`
- 수정: 미적용 라우트 발견 시 가드 추가

### OPS-P1-G9 [C-026] stock_adjustments 승인 흐름 권한 분리
- 파일: `app/main.py` /stock/adjust, /stock/adjustment 라우트
- 수정: 실사 입력(leader+) → 조정 등록(leader+) → 승인(admin+) 3단계 권한 분리

---

## 3. 회신 형식

**파일**: `01_HAIST_WORKS/_FROM_01_2026-04-29_OPS_P1_권한가드_9건_응답.md`

- §§ 자체 grep 검증 9개 명령 결과 직접 인용
- 변경 라인 수 (정직성 v3)
- BAT 갱신 (양쪽 4라인)
- G4 결재 사안 → 09 결재 후 처리 명시

---

## 4. race 방지

main.py 동시 수정 다수 → **본 발주서 패치 9건은 단일 commit 1회 처리 권고**. 다른 OPS 발주서(자동화·산식 등)와 main.py 동시 수정 race 회피.

---

*04 운영테스트팀 — 2026-04-29*
