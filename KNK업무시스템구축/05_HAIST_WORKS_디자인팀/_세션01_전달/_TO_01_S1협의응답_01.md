# [05 디자인팀 → 01 메인] S1 협의 응답 (3건 일괄)

> **발신**: 05 디자인팀 세션 빅터
> **수신**: 01 메인 세션
> **일자**: 2026-04-24
> **원 요청**: `_FROM_01_S1협의요청_01.md` (2026-04-24, 대표 "다 진행해" 지시로 즉시 대응)

---

## 요청 ① 팀장 권한 매트릭스 시안 ✅ 완료

**산출물**: `05_.../03_시안/s1_팀장권한매트릭스.html`

**스펙 요약**:
- 3 클릭 원칙 충족 (행 선택 → 토글 → 저장)
- 권한 범위 명시: 상단 파란 배너 + is_admin 컬럼 비활성(CEO·admin 전용)
- 권한 컬럼 헤더에 **DB 키 + 한 줄 설명** 병기 (`can_use_logistics` 등 database.py:795~822 실측 반영)
- 토글 스위치 (브랜드 중립), 변경 시 주황 테두리
- 검색·전체 허용·전체 차단·취소·저장 툴바
- Sticky 하단 저장 바: "변경 N건 · 영향 M명" 요약
- role=leader 범위 제한: `tree = members where team_id == user.team_id` 로 쿼리 + 서버측 검증 필수 (UI 제한만으로는 부족)

**권한 키 매핑 (database.py 실측)**:
| UI 컬럼 | DB 필드 | 비고 |
|---|---|---|
| 자재 모듈 | `can_use_logistics` | 부품·재고·발주 |
| 매출·영업 모듈 | `can_use_sales` | 프로젝트·고객사·견적 |
| 변경 편집 | `can_edit_changes` | 변경 알림 등록·수정 |
| 티켓 종료 | `can_close_tickets` | 요청 티켓 완료 처리 |
| 관리자 | `is_admin` | CEO·admin 전용, 팀장 편집 불가 |

**01 구현 힌트**:
- 라우트: `GET/POST /team/{team_id}/permissions`
- 권한 체크:
  ```python
  if user.role == 'leader' and user.team_id != team_id:
      return 403
  # is_admin 토글은 role in ('ceo','admin') 만
  ```
- 배치 저장: POST body 는 `{member_id: {perm_key: 0|1, ...}, ...}` 형태. 변경된 셀만 전송.
- 저장 성공 후 "N건 저장됨" 토스트.

---

## 요청 ② 네비 5 그룹 가이드 ✅ 완료

**산출물**: `05_.../02_개선제안_06_네비5그룹.md`

**5 그룹 최종 명칭** (대표 승인 필요):
1. **내 업무** (홈·일일·달력·알림)
2. **협업** (피드·실시간·검색·코크핏·병목·전사 대시보드)
3. **프로젝트·변경** (진행률·변경·이슈·티켓 + 팀/프로젝트 트리)
4. **게시판** (전사·부서)
5. **외부·관리** (하이웍스·자재·매출영업·관리자)

**삽입 HTML 조각**: 문서 §3 에 Jinja 템플릿 블록 5개 전문 포함 (그대로 `base.html` 에 복붙 가능).

**색 가이드**: 그룹별 색상 **없음** (원칙 §0 상표권 정책 + 시각 과밀 억제). 헤더 타이포·간격으로 위계.

**i18n 키 신설**: `sb_mywork`·`sb_collab`·`sb_project`·`sb_board`·`sb_external` ko/vi/en 3개 언어 매핑 포함.

**기존 13 섹션 → 5 그룹 매핑 체크리스트** 제공.

---

## 요청 ③ 중복·고아 라우트 합병안 ✅ 완료

**산출물**: `05_.../02_개선제안_05_고아합병.md`

**판정 결과**:
| 라우트 | 권고 | 근거 |
|---|---|---|
| `/bottlenecks` | **합병 → `/cockpit#bottlenecks`** | `cockpit.html:65–76` 이 `bottlenecks.html:11–22` 와 100% 동일 로직 (같은 DB 쿼리, 같은 렌더) |
| `/now` | **합병 → `/feed?view=now`** | 개선제안 #03 "다른 방식으로 보기 ▾" 에 네 번째 옵션으로 편입, 맥락 통합 |
| `/cockpit` | **유지** | 경영진 일일 대시보드 고유 가치. `/dashboard` 와 역할 재정립은 별도 제안 |

**대표 승인 양식** 포함 (세 항목 각각 승인/반려 체크박스).

**01 작업량 추정**: 전체 수용 시 **약 1시간 30분**
- `/bottlenecks` 리다이렉트 + 앵커 (20분)
- `/now` 리다이렉트 + `/feed?view=now` 블록 이식 (1시간)
- 사이드바 링크 정리 (10분)

**리스크 확인 요청**: `grep "/bottlenecks\\|/now" app/` 결과 공유 부탁 (알림·이메일 하드코딩 여부 확인용).

---

## 의존성 · 일정

| 산출물 | 대표 승인 필요? | 01 착수 가능 시점 |
|---|---|---|
| 요청 ① 팀장 권한 매트릭스 | 아니요 (UI 제안) | **즉시** (기한 2026-04-26 만족) |
| 요청 ② 네비 5 그룹 | 그룹명 승인 필요 | 승인 후 즉시 (30분~1시간) |
| 요청 ③ 고아 합병 | 🔴 대표 승인 필수 | 승인 후 즉시 (1시간 30분) |

**05 가 01 에게 역 요청 1건**:
- 요청 ③ 의 **리스크 확인 grep 결과** 공유 부탁.

---

## 경계·정책 준수 재확인

- 모든 산출물 외부 자산 **0건** (`04_라이선스_상표권_정책.md` §0)
- 시안 HTML 은 `00_design_tokens.css` 재사용 — 신규 토큰 도입 없음
- Lucide inline SVG · Pretendard fallback 체인 기존 정책 준수
- 타사 브랜드명 언급 없음 (그룹명·컬럼명 모두 순수 한국어)

---

**발행**: 2026-04-24 · 05 디자인팀 세션 빅터
**기한 달성**: 요청 ① (2026-04-26) · 요청 ②·③ (2026-04-30) 전부 **선제 완료**
