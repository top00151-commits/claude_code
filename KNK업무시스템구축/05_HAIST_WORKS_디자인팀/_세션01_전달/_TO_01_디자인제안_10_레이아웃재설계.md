# [05 디자인팀 → 01 메인] 디자인 제안 #10 — 상단 헤더·좌 사이드바·메인창 전체 레이아웃 재설계

> **발신**: 05 디자인팀 · **수신**: 01 메인
> **일자**: 2026-04-24
> **원본**: `05_.../02_개선제안_10_전체레이아웃재설계.md`
> **시안**: `05_.../03_시안/10_unified_layout.html` (standalone, 실제 동작)
>
> **배경**: 대표 2026-04-24 지시 (3차 정리)
> > "최 상단 회사 로고 있는 부위하고 옆 글씨들은 너무 작은데... KNK 통합업무플랫폼…
> > 또 왼쪽 사이드바에 도메인 '매출영업센터', '자재구매센터' 이 부분이 여기 있는 것도 디자인적으로는 아닌 것 같아...
> > 왼쪽 사이드바, 상단 회사 로고 있는 부위, 메인창 전체적으로 다시 정리해봐."

---

## TL;DR

1. **상단 헤더**: 54px → **72px** · 그라디언트 배경 · 브랜드 타이포 2단계 확대 · 글로벌 검색바(Ctrl K) 중앙 배치 · 빅터 호출 버튼 우측
2. **좌 사이드바**: 5그룹 → **4그룹** (도메인 = 매출·영업/자재·구매 센터 **제거 후 상단 워크스페이스 스위처로 이전**)
3. **메인창**: 자유 형식 → **표준 페이지 헤더** (breadcrumb + title + sub + ghost + primary)

백엔드 `main.py` 컨텍스트 빌더에 `workspaces` + `current_workspace` **추가** 필요 (30분 규모).

---

## 수용 범위 회신

- [ ] 전체 적용 (헤더 + 사이드바 + 메인 헤더 표준화)
- [ ] 헤더 + 사이드바만 (메인 헤더 표준화 별도 처리)
- [ ] 헤더만 (사이드바·메인 보류)
- [ ] 거절 (사유:           )
- [ ] 수정 제안 (자유 기재)

**회신 위치**: `05_.../_FROM_01_디자인응답_10.md`

---

## 1. 상단 헤더 재설계

### 1-1. HTML 교체 대상 (`app/templates/base.html:11~51`)

**AS-IS 전체 제거 후 아래로 교체:**

```html
{% if user %}
<header class="topbar">
  <div class="tb-left">
    <button class="tb-menu-btn" onclick="toggleSidebar()" title="{{ i.menu|default('메뉴') }}">
      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round"><line x1="3" y1="6" x2="21" y2="6"/><line x1="3" y1="12" x2="21" y2="12"/><line x1="3" y1="18" x2="21" y2="18"/></svg>
    </button>

    <a href="/home" class="brand">
      {# 로고: logo.png 유지 또는 SVG 마크 중 선택 — 기본은 이미지 유지 #}
      <img src="/static/logo.png?v=20260414b" alt="KNK HAIST" class="brand-logo">
      <div class="brand-text">
        <div class="brand-name">{{ app_name|default('HAIST WORKS') }}</div>
        <div class="brand-sub">{{ app_subtitle|default('KNK 통합 업무 플랫폼') }}</div>
      </div>
    </a>

    <div class="tb-divider"></div>

    {# 워크스페이스 스위처: 권한 따라 옵션 필터 #}
    {% if workspaces and workspaces|length > 1 %}
    <button class="ws-switcher" id="wsBtn" aria-haspopup="true" aria-expanded="false">
      <svg class="ws-ico" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round"><rect x="3" y="3" width="7" height="7" rx="1"/><rect x="14" y="3" width="7" height="7" rx="1"/><rect x="3" y="14" width="7" height="7" rx="1"/><rect x="14" y="14" width="7" height="7" rx="1"/></svg>
      <span class="ws-name">{{ current_workspace.name|default('통합') }}</span>
      <span class="ws-caret">▾</span>

      <div class="ws-menu" id="wsMenu" role="menu">
        {% for w in workspaces %}
        <a class="ws-opt {% if w.key == current_workspace.key %}active{% endif %}"
           href="{{ w.href }}"
           {% if w.external %}target="_blank" rel="noopener"{% endif %}>
          <div class="opt-ico">{{ w.icon }}</div>
          <div class="opt-meta">
            <div class="opt-name">{{ w.name }}</div>
            <div class="opt-desc">{{ w.desc }}</div>
          </div>
          {% if w.key == current_workspace.key %}<span class="opt-check">✓</span>
          {% elif w.external %}<span class="opt-ext">↗</span>{% endif %}
        </a>
        {% endfor %}
      </div>
    </button>
    {% endif %}
  </div>

  <div class="tb-center">
    <form class="g-search" action="/search" method="get" role="search">
      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round"><circle cx="11" cy="11" r="8"/><path d="m21 21-4.35-4.35"/></svg>
      <input type="search" name="q" placeholder="{{ i.search_ph|default('업무·사람·문서 검색...') }}" id="gSearchInput">
      <span class="shortcut">/</span>
    </form>
  </div>

  <div class="tb-right">
    <button class="tb-icon-btn victor" onclick="toggleDock()" title="빅터 열기 (Ctrl K)">
      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="5" y="8" width="14" height="10" rx="2"/><circle cx="9" cy="13" r="1" fill="currentColor"/><circle cx="15" cy="13" r="1" fill="currentColor"/><path d="M12 8V5m-3 0h6"/></svg>
    </button>
    <a href="/notifications" class="tb-icon-btn" title="{{ i.sb_notif|default('알림') }}">
      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M6 8a6 6 0 0 1 12 0c0 7 3 9 3 9H3s3-2 3-9"/><path d="M10.3 21a1.94 1.94 0 0 0 3.4 0"/></svg>
      {% if unread_notif and unread_notif > 0 %}<span class="dot"></span>{% endif %}
    </a>
    <select id="langSel" class="lang-sel" onchange="changeLang(this.value)">
      {% for code, label in LANGS.items() %}
      <option value="{{ code }}" {% if lang == code %}selected{% endif %}>{{ label }}</option>
      {% endfor %}
    </select>
    <a href="/profile" class="user-chip" title="{{ i.my_account|default('내 계정') }}">
      <div class="user-avatar">{{ user.name[:1] }}</div>
      <div class="user-info">
        <div class="user-name">{{ user.name }}</div>
        <div class="user-role">{{ user.rank or user.team_name or '경영진' }}</div>
      </div>
    </a>
    <a href="/logout" class="logout-btn" title="{{ i.logout|default('로그아웃') }}">
      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><path d="M9 21H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h4"/><polyline points="16 17 21 12 16 7"/><line x1="21" y1="12" x2="9" y2="12"/></svg>
    </a>
  </div>
</header>
{% endif %}
```

### 1-2. CSS 추가 (`static/style.css` 말미에 새 블록)

시안 `10_unified_layout.html` 의 `.topbar` / `.tb-*` / `.brand*` / `.ws-*` / `.g-search` / `.user-chip` / `.logout-btn` 전체 블록 그대로 이식 (약 180줄).

### 1-3. CSS 제거 · 교체

- `style.css:72` `.topbar { height: 54px; background: #fff; ... }` 블록 → **교체**
- `style.css:1124~1178` `.topbar` 오버라이드 블록 → **제거 후 새 블록으로 흡수**
- `style.css:1890~` `.topbar .brand` 오버라이드 → **제거**

### 1-4. JS 추가 (`base.html` `</body>` 직전)

```js
/* 워크스페이스 스위처 드롭다운 */
(function(){
  const btn = document.getElementById('wsBtn');
  const menu = document.getElementById('wsMenu');
  if (!btn || !menu) return;
  btn.addEventListener('click', (e) => {
    e.stopPropagation();
    const open = menu.classList.toggle('open');
    btn.setAttribute('aria-expanded', open ? 'true' : 'false');
  });
  document.addEventListener('click', (e) => {
    if (!menu.contains(e.target) && !btn.contains(e.target)) {
      menu.classList.remove('open');
      btn.setAttribute('aria-expanded', 'false');
    }
  });
  document.addEventListener('keydown', (e) => {
    if (e.key === 'Escape') menu.classList.remove('open');
  });
})();

/* 글로벌 검색 "/" 포커스 (빅터 도크는 Ctrl+K 전용 유지)
   - GitHub / Linear / Sentry 등 업계 표준 패턴
   - input/textarea 포커스 중이거나 IME 조합 중일 때는 무시 */
document.addEventListener('keydown', (e) => {
  if (e.key !== '/') return;
  if (e.ctrlKey || e.metaKey || e.altKey) return;
  if (e.isComposing) return;                        // 한글 IME 조합 중 무시
  const tag = (e.target.tagName || '').toLowerCase();
  const editable = tag === 'input' || tag === 'textarea' || tag === 'select' || e.target.isContentEditable;
  if (editable) return;                             // 이미 입력창이면 무시
  const input = document.getElementById('gSearchInput');
  if (input) { e.preventDefault(); input.focus(); input.select(); }
});
```

> ✅ **단축키 분리 확정** (대표 대리 결정 · 2026-04-24):
> - **Ctrl+K / ⌘K** → 빅터 도크 토글 (제안 #08 고수)
> - **`/` 키** → 글로벌 검색 포커스 (GitHub / Linear / Sentry 업계 표준)
> - **Esc** → 현재 모달·도크·검색 포커스 해제 (공통)

---

## 2. 좌 사이드바 재설계

### 2-1. 제거 (`app/templates/base.html:139~158`)

**그룹 4 "도메인" 블록 전체 삭제**:
```html
<!-- 삭제 대상 -->
{% if user.role in ['ceo','admin','executive'] or user.can_use_logistics or user.can_use_sales %}
<div class="sb-section">
  <div class="sb-head">도메인 <span style="...">📈📦</span></div>
  {% if ... %}<a href="/sales" ...>매출·영업 센터<span class="sb-logi-badge">↗</span></a>{% endif %}
  {% if ... %}<a href="/logistics" ...>자재·구매 센터<span class="sb-logi-badge">↗</span></a>{% endif %}
</div>
{% endif %}
```

→ 기능은 **상단 워크스페이스 스위처로 이전** (1-1 참조).

### 2-2. 그룹 재배치

최종 사이드바 구성 (번호 순서 유지):
1. 내 업무 (`home`, `daily`, `calendar`, `notifications`)
2. 전사 흐름 (`feed`, `now`, `search`, `dashboard`, `bottlenecks`)
3. 업무 추적 (`progress`, `tickets`, `issues`, `changes`, `board_company`)
4. 외부 · 관리 (하이웍스 `전자결제`, `메일`, `팀원 권한`, `관리자`)
5. 팀 / 프로젝트 (기존 트리)

### 2-3. 인라인 스타일 정리 (`base.html:116, 121, 126, 131, 162, 165, 169`)

`style="...font-size:10px..."` 인라인 제거, `.sb-inline-badge` 클래스로 이관 (#09 와 동일 방향).

---

## 3. 백엔드 컨텍스트 확장 (`app/main.py`)

### 3-1. 워크스페이스 정의 추가

```python
# 상단 상수
WORKSPACES = [
    {"key": "hub",   "name": "통합",          "desc": "HAIST WORKS 메인",      "icon": "🏢", "href": "/home",      "external": False},
    {"key": "sales", "name": "매출·영업 센터", "desc": "Sales Hub · 영업·수주",  "icon": "📈", "href": "/sales",     "external": True},
    {"key": "logi",  "name": "자재·구매 센터", "desc": "Logistics Hub · 자재",   "icon": "📦", "href": "/logistics", "external": True},
]

def workspaces_for(user):
    """권한별 접근 가능한 워크스페이스 목록"""
    out = [WORKSPACES[0]]  # 통합은 전부
    if user and (user.role in ('ceo','admin','executive') or getattr(user,'can_use_sales',False)):
        out.append(WORKSPACES[1])
    if user and (user.role in ('ceo','admin','executive') or getattr(user,'can_use_logistics',False)):
        out.append(WORKSPACES[2])
    return out

def current_workspace_for(path):
    """URL 기반 현재 워크스페이스 판별"""
    if path.startswith("/sales"):     return WORKSPACES[1]
    if path.startswith("/logistics"): return WORKSPACES[2]
    return WORKSPACES[0]
```

### 3-2. 공통 컨텍스트 빌더 연결

모든 `render_template()` 공통 파라미터에 추가:
```python
ctx["workspaces"] = workspaces_for(user)
ctx["current_workspace"] = current_workspace_for(request.url.path)
```

`templates/base.html` 에서 바로 `{{ current_workspace.name }}`, `{% for w in workspaces %}` 사용 가능.

---

## 4. 메인창 페이지 헤더 표준화

### 4-1. partial 템플릿 생성 (`app/templates/_page_header.html`)

```jinja2
{# 공통 페이지 헤더 partial
   context 변수: breadcrumb (list of {text, href}), page_title, page_sub, actions (optional html block)
#}
<div class="page-header">
  <div class="ph-left">
    {% if breadcrumb %}
    <div class="breadcrumb">
      {% for b in breadcrumb %}
        {% if b.href %}<a href="{{ b.href }}">{{ b.text }}</a>{% else %}<span>{{ b.text }}</span>{% endif %}
        {% if not loop.last %}<span class="sep">/</span>{% endif %}
      {% endfor %}
    </div>
    {% endif %}
    <h1 class="page-title">{{ page_title }}</h1>
    {% if page_sub %}<div class="page-sub">{{ page_sub }}</div>{% endif %}
  </div>
  {% if actions %}
  <div class="ph-right">{{ actions }}</div>
  {% endif %}
</div>
```

### 4-2. 각 페이지 적용 패턴

각 템플릿 상단 블록에 추가 (예: `home.html`):
```jinja2
{% set breadcrumb = [
  {"text": "통합", "href": "/home"},
  {"text": "내 업무"},
  {"text": "업무 현황"}
] %}
{% set page_title = "통합 업무 현황" %}
{% set page_sub = "오늘의 내 업무 · 팀 현황 · 전사 흐름을 한눈에" %}
{% include '_page_header.html' %}

<div class="page-body">
  ... 기존 본문 ...
</div>
```

적용 우선 10 페이지: `home`, `daily`, `progress`, `tickets`, `issues`, `changes`, `feed`, `dashboard`, `admin`, `profile`.

### 4-3. `.page-header` / `.page-body` CSS 추가

시안 `10_unified_layout.html` 의 `.page-header`, `.page-body`, `.kpi`, `.section`, `.card`, `.btn-primary`, `.btn-ghost` 블록 전체 이식.

기존 `.page-title h1 { font-size: 25px }` (style.css:98) 등 충돌 제거 또는 오버라이드.

---

## 5. CSS 캐시 키 갱신

```
?v=20260424layout-v5
```

`base.html` `<link rel="stylesheet">` 쿼리스트링 교체.

---

## 6. 커밋 분리 권장

1. `"feat(header): 72px gradient topbar + brand enlarged"`
2. `"feat(header): workspace switcher dropdown (sales/logistics moved from sidebar)"`
3. `"feat(header): global search bar with Ctrl-/ shortcut"`
4. `"refactor(sidebar): remove domain group, 5 → 4 groups"`
5. `"feat(main): _page_header.html partial with breadcrumb/title/actions"`
6. `"chore(main): apply _page_header.html to 10 routes"`
7. `"refactor(base): sb-inline-badge class (drop inline font-size)"`

---

## 7. QA 체크리스트

- [ ] 상단 헤더 높이 1920px: 72px · 1440px: 68px · 1024px: 64px · 768px: 64px
- [ ] 브랜드 `HAIST WORKS` 로고 옆 "KNK 통합 업무 플랫폼" 부제 **뚜렷하게 보임**
- [ ] 워크스페이스 스위처 드롭다운: 클릭 열림 · ESC 닫힘 · 바깥 클릭 닫힘
- [ ] CEO/admin: 통합 + 매출·영업 + 자재·구매 3개 노출
- [ ] `can_use_sales=True` 사용자: 통합 + 매출·영업 2개 노출
- [ ] 권한 없음: 스위처 자체 숨김
- [ ] 글로벌 검색: **`/` 키**로 포커스 · Enter 시 `/search?q=` 이동
- [ ] 빅터 도크 `Ctrl+K` 와 충돌 없음 (검색 단축키 `/` 로 분리 확정)
- [ ] 한글 IME 조합 중 `/` 입력 시 검색 포커스 **이동하지 않음** (isComposing 체크)
- [ ] 이미 input/textarea 에 포커스된 상태에서 `/` 입력 시 정상 타이핑 (단축키 무시)
- [ ] 좌 사이드바 4그룹 · 매출·영업/자재·구매 항목 **완전 제거** 확인
- [ ] 페이지 헤더: `/home`, `/progress`, `/tickets`, `/issues`, `/changes`, `/feed`, `/dashboard`, `/admin`, `/daily`, `/profile` 10개 일관 적용
- [ ] 모바일 (<768px): 브랜드 텍스트 · 유저 이름 · 언어 셀렉터 숨김 (아이콘만)
- [ ] 다크 헤더 위 흰 글씨 대비 WCAG AA 이상
- [ ] `prefers-reduced-motion`: 호버 lift · 스위처 fade 비활성화
- [ ] 1920px 메인창에서 사이드바·페이지 헤더·KPI 카드 시각 비율 자연스러움

---

## 8. 리스크 · 완화

| 리스크 | 완화책 |
|---|---|
| 헤더 높이 증가로 본문 공간 감소 | `calc(100vh - var(--topbar-h))` 자동 대응 · 스크롤 시 축소 옵션 대기 |
| 기존 사용자 "매출·영업 센터" 경로 학습 재필요 | 첫 사용 시 1회 툴팁 오버레이 (`localStorage: topbar_hint_v5`) |
| `logo.png` 라이선스 | KNK 자체 제작 확인 필요 (`04_라이선스_상표권_정책.md` 재확인) |
| Ctrl+K 충돌 (빅터 vs 검색) | **확정**: Ctrl+K = 빅터 전용, 검색 = `/` 키 (GitHub 패턴) |
| `/` 키가 한글 타자 방해 | IME `isComposing` + input/textarea 포커스 체크로 자동 우회 |
| 페이지 헤더 partial 도입 시 기존 템플릿 호환 | 기존 자유 형식 페이지는 **점진 교체** (일괄 X) |

---

## 회신 요청

- [ ] 착수 시작 (ETA:           )
- [ ] 완료 (커밋 해시:           )
- [ ] 질문·조정 필요 (아래 기재):
      _____________________________________________________
- [ ] 보류 (사유:           )

**회신 위치**: `05_.../_FROM_01_디자인응답_10.md`

---

**발행**: 2026-04-24 · 05 디자인팀 세션 빅터
**우선순위**: 🔴 대표 직접 지시 · 3차 정리
**선행 조건**: #09 (드래그 제거 + 유동 타이포) 와 병행 권장. 독립 적용도 가능.
**작업량**: 약 7시간 (#09 병행 시 합쳐 8시간)
**파일 확인**: `03_시안/10_unified_layout.html` 더블클릭 → 좌측 "통합 ▾" 드롭다운 클릭 · 창 크기 조절 실험
