# [05 → 01] 🟢 v5 H5 반영 발주서 — 시안 직접 이식 (해석 금지)

> **발신**: 05 디자인팀 세션 빅터
> **수신**: 01 실무팀
> **참조**: 대표이사 (일괄 승인 결재) / 09 프로젝트 팀장 / 04 운영테스트팀
> **일자**: 2026-04-29
> **트리거**: 대표 *"일괄승인"* 결재 (2026-04-29) — v5 H5 풀세트 시안 22 파일 일괄 통과
> **지위**: 🟢 **반영 발주서 — 시안 HTML/CSS 직접 이식 (해석·창조 금지)**
> **워크플로우**: 정책 §5 시안 우선 워크플로우 — 5단계 (반영) ← 본 발주

---

## 1. 한 줄

대표 일괄 승인 받은 v5 H5 풀세트 시안 22 파일 (표준 토큰 CSS + 마스터 13 + 변형 가이드 8)을 01팀이 **HTML/CSS 그대로 이식** — 해석·창조 절대 금지. 30 grep 검증 후 BAT 갱신.

---

## 2. 결재 근거

| 단계 | 일자 | 결과 |
|---|---|---|
| 페이지 목록 요청 | 2026-04-29 | ✅ 01 응답 (149 라우트 / 124 템플릿 / base 분포 매핑) |
| 풀세트 시안 22 파일 발행 | 2026-04-29 | ✅ Phase 1·2·3 완료 |
| 대표 일괄 승인 | 2026-04-29 | ✅ "일괄승인" |
| **본 반영 발주** | 2026-04-29 | 🟢 본 문서 |

---

## 3. 반영 대상 22 파일 (시안 위치)

**[7차 검증 보강 — 28 파일로 확장]**: 9-B 참조

### 3-A. 표준 토큰 CSS (1 파일 · 가장 우선)
```
05_HAIST_WORKS_디자인팀/03_시안/_05_v5H5_풀세트시안/ZZ_표준토큰.css
  → 01: app/static/css/v5_tokens.css 로 이식 (기존 style.css 보다 위에 link)
```
- 376줄, 13 토큰 카테고리 (색·간격·모서리·그림자·타이포·레이아웃·트랜지션·z-index·리셋·유틸·카드·버튼·표·반응형)
- **sage- alias 포함** → v4 코드 그대로 작동 + v5 전환 동시 가능

### 3-B. 3 base 시안 (88% 영향)
```
0_base/master_base_v5H5.html       → app/templates/base.html
0_base/master_base_sales_v5H5.html → app/templates/base_sales.html
0_base/master_base_logi_v5H5.html  → app/templates/base_logi.html
```
- 124 템플릿 중 109개가 본 base 상속 → **base 3종 교체만으로 88% 자동 적용**
- 우상단 6 아이콘 보존 (빅터·❓·알림·언어·사용자·나가기) — 01 응답서 §6 인계
- dock top: 80px / `--topbar-h: 80px` 보존
- ws-tabs 3종 (통합·매출·자재) 그라데이션 active 적용

### 3-C. 카테고리 마스터 10종 + 변형 가이드 8종

| 카테고리 | 마스터 시안 → 적용 대상 |
|---|---|
| **A 대시보드** | `master_A_dashboard_home_v5H5.html` → `home`·`dashboard`·`sales_home`·`logistics_home`·`cockpit` (5) |
| **B 매트릭스** | `master_B_matrix_sales_dashboard_v5H5.html` → `sales_dashboard` 외 11 매트릭스 (12) |
| **C 폼** | `master_C_form_po_v5H5.html` → `po_form` 외 21 폼 (22) |
| **D 리스트** | `master_D_list_po_v5H5.html` → `po_list` 외 23 리스트 (24) |
| **E 상세** | `master_E_detail_po_v5H5.html` → `po_detail` 외 14 상세 (15) |
| **F 모달** | `master_F_modals_v5H5.html` → 5종 표준 모달 컴포넌트 (신규) |
| **G standalone** | `master_G_login_v5H5.html` / `master_G_error_v5H5.html` / `master_G_print_v5H5.html` → 12 페이지 |
| **H 관리자** | `master_H_admin_v5H5.html` → `admin_permissions` 외 14 (15) |
| **I 보고서** | `master_I_report_weekly_v5H5.html` → `weekly` 외 4 (5) |
| **J 빅터 AI** | `master_J_victor_search_v5H5.html` + 기존 dock + 빅터 풀페이지 PREVIEW (3) |

각 카테고리 폴더 안 `variations.md` — **변형 spec 그대로 따르기. 임의 해석 금지.**

---

## 4. 반영 순서 (정책 §5-2 신 사이클)

```
[Phase 1 — 토큰·base] (2h)
  1. ZZ_표준토큰.css 이식 (app/static/css/v5_tokens.css)
  2. base.html 시안 직접 교체 (현 1230줄 → 시안 그대로)
  3. base_sales.html 시안 직접 교체
  4. base_logi.html 시안 직접 교체
  5. style.css·style_hubs.css 비활성화 확인 (link 주석)
  6. 마스트헤드 제거 (G25 핫패치 보존됨)
  → 88% 자동 적용 즉시 검증

[Phase 2 — 카테고리] (4h)
  7. A 대시보드 (5 페이지) — variations 따라
  8. B 매트릭스 (12) — 5 탭 컴포넌트 통합
  9. C 폼 (22) — 단계 인디케이터 통합
  10. D 리스트 (24) — 일괄 액션 바 통합
  11. E 상세 (15) — 워크플로우 5단계
  12. G standalone (12) — login·error·print 마스터
  13. H 관리자 (15) — 권한 매트릭스
  14. I 보고서 (5) — 빅터 인사이트 카드
  15. J 빅터·검색 (3) — 메가 검색

[Phase 3 — 신규 컴포넌트] (1h)
  16. F 모달 5종 컴포넌트화 (재사용)
  17. 에러 페이지 (404/500) FastAPI 핸들러 등록
  18. 로딩 인디케이터 (전역)
  19. 인쇄 6종 @media print 적용

[Phase 4 — 검증·BAT] (자체)
  20. 30 grep 검증 (§5)
  21. KNK_시작.bat / START.bat LAST UPDATE 갱신 (정책 BAT 규칙)
  22. _FROM_01_2026-04-29_v5H5반영응답.md 발행

총 ~7h (이전 v4 사이클 14h 대비 절반 — 해석 단계 제거)
```

---

## 5. 검증 grep 5 카테고리 × 6항 = 30종 (정책 §4-A-3 표준)

### A. 신규 패턴 추가 (PASS ≥1)

```bash
# A-1. v5 토큰 사용 확인
grep -c '\-\-paper:\s*#FBF5EC' app/static/css/v5_tokens.css           # ≥1
grep -c '\-\-amber:\s*#D4923B' app/static/css/v5_tokens.css           # ≥1
grep -c '\-\-knk-red:\s*#A5282C' app/static/css/v5_tokens.css         # ≥1

# A-2. base 3종 v5 적용
grep -c 'class="tb-tabs"' app/templates/base.html                     # ≥1 (ws-tabs)
grep -c '--grad-amber' app/templates/base_sales.html                  # ≥1

# A-3. 메뉴 식별번호 핀
grep -c 'class="menu-code"' app/templates/base.html                   # ≥10 (사이드바 항목)

# A-4. 신규 컴포넌트
grep -c 'class="bento"\|class="card.*hero"' app/templates/*.html      # ≥5
grep -c 'class="form-stepper"\|class="lines"' app/templates/*.html    # ≥3 (폼)
grep -c 'class="matrix-tabs"' app/templates/*.html                    # ≥3 (매트릭스)

# A-5. 인쇄 미디어 쿼리
grep -c '@media print' app/static/css/v5_tokens.css                   # ≥1
grep -lc '@media print' app/templates/*_print.html                    # ≥6 (인쇄 6종)

# A-6. 모달 5종
grep -c 'class="modal"\|class="modal-' app/templates/*.html           # ≥5
```

### B. 옛 패턴 잔존 (PASS = 0)

```bash
# B-1. sage- 직접 사용 (alias 외)
grep -rE '\-\-sage-[0-9]+' app/templates/ app/static/css/ \
  | grep -v 'v5_tokens.css'                                           # = 0

# B-2. 인라인 #hex 색
grep -rE 'style="[^"]*#[0-9a-fA-F]{3,6}' app/templates/               # = 0

# B-3. v4 매거진 마스트헤드 잔존
grep -rE 'VOL\.[0-9]+|NO\.[0-9]+|EDITION' app/templates/              # = 0

# B-4. style_hubs.css 활성화 잔존
grep -rE '<link[^>]*style_hubs\.css(?![^<]*<!)' app/templates/        # = 0 (주석화 확인)

# B-5. 옛 --rose / --accent 직접 사용 (alias 외)
grep -rE 'var\(--rose\)|var\(--accent\)' app/templates/ \
  | grep -v 'v5_tokens.css'                                           # = 0

# B-6. v3 헤더 검색창 잔존
grep -rE 'class="header-search"' app/templates/                       # = 0
```

### C. 페이지별 적용 (Top 10 페이지 × 옛 잔존 0건)

```bash
for tpl in login.html guide.html home.html dashboard.html \
           sales_dashboard.html po_form.html po_list.html \
           po_detail.html admin_permissions.html weekly.html ; do
  echo "=== $tpl ==="
  grep -c '\-\-amber\|\-\-paper\|\-\-knk-red' app/templates/$tpl        # ≥1
  grep -cE 'style="[^"]*#[0-9a-fA-F]{3,6}' app/templates/$tpl           # = 0
done
```

### D. 인라인 색상 정리 (PASS = 0)

```bash
# D-1. 모든 templates
grep -rE 'style="[^"]*#[0-9a-fA-F]{3,6}' app/templates/                # = 0
# D-2. 모든 static (CSS 외)
grep -rE 'style="[^"]*#[0-9a-fA-F]{3,6}' app/static/                   # = 0
```

### E. 페르소나 walkthrough (시각 검증 — 04팀 협조)

```
W1  김 부장 로그인 → 통합 대시보드 → 견적 23건 인지
W2  김 부장 매출 매트릭스 → 5 탭 전환 → 대성기업 인지
W3  김 부장 견적 작성 폼 → 4 단계 → 발행 모달
W4  이 차장 자재 hub → 경고 배너 → 발주 진입
W5  이 차장 발주서 작성 → 3 라인 + 모달 → 발행
W6  이 차장 발주 목록 → 일괄 액션 바
W7  박 대리 입고 처리 → QC 부적합 모달
W8  최 과장 출고 등록 → 안전재고 모달
W9  대표 주간 보고 → 빅터 인사이트 → PDF
W10 대표 권한 매트릭스 → 토글 → 감사 로그
W11 모바일 영업 외근 (9 페이지) → 사이드바 collapse
W12 인쇄 6종 → @media print → 회사정보 빨간 배너 (미입력 시)
```

각 walkthrough PASS 기준: 시각 일관성 + UX 흐름 + 데이터 표시.

---

## 6. 1팀 인계 사항 (01 응답서 §6 보존)

다음 200+ 핫패치는 v5 H5 시안에 **이미 반영됨**:

| # | 인계 사항 | v5 H5 처리 |
|---|---|---|
| 1 | `style.css` 7400줄 | ZZ_표준토큰.css로 대체 (기존은 단계적 폐기) |
| 2 | `style_hubs.css` 비활성화 | 마스터 base 3종에 hub 색 통합 |
| 3 | 마스트헤드 제거 (G25) | H5는 마스트헤드 안 씀 ✓ |
| 4 | dock top: 80px / topbar-h | 표준 토큰 `--topbar-h: 80px` 보존 |
| 5 | base_sales 사이드바 +2 (forecast·aging) | 마스터 사이드바 13항에 포함 ✓ |
| 6 | 빅터 dock 우상단 ❓ 버튼 | 6 아이콘에 포함 ✓ |
| 7 | 6 아이콘 (빅터·❓·알림·언어·사용자·나가기) | 마스터 base.html .tb-right 그대로 |
| 8 | POST `/victor/route` + GET `/api/menu-help` | 라우트 보존 (시안에 영향 X) |
| 9 | 메뉴 식별번호 (M-XX-YY) 52개 | `class="menu-code"` 핀 모든 사이드바 ✓ |

---

## 7. v4 발주서 폐기 재확인

`_세션01_전달/_ARCHIVED/` 안 다음 2건은 **이미 폐기 처리됨**. 본 발주서가 모든 디자인 적용을 대체:
- `_TO_01_2026-04-28_v4CX23c_Phase2.5+3_긴급보강_전수적용_DEPRECATED.md`
- `_TO_01_2026-04-28_v4승인_CX23c_CuratedMagazine_전체재스킨_DEPRECATED.md`

01 작업 시작 금지.

---

## 8. BAT 파일 갱신 의무 (정책 BAT 규칙)

01 작업 완료 시 **반드시** 두 파일 LAST UPDATE 라인 갱신:

```
KNK_시작.bat        — LAST UPDATE: 2026-04-29 v5 H5 풀세트 적용
START.bat           — LAST UPDATE: 2026-04-29 v5 H5 풀세트 적용
```

---

## 9. 응답 발행 위치 (필수)

```
05_HAIST_WORKS_디자인팀/_세션01_수신/_FROM_01_2026-04-29_v5H5반영응답.md
```

응답서에 포함 의무:
1. Phase 1·2·3·4 각 완료 시각
2. 30 grep 검증 결과 (5 카테고리 × 6항 PASS/FAIL 매트릭스)
3. 작업 중 발견한 시안과 코드 충돌 사항 (있을 시)
4. 04팀 walkthrough 검증 권고 사항
5. BAT 갱신 commit hash

---

## 9-A. P2 기능 보류 명시 (검증 7차 발견 — 시안 vs 현 시스템 불일치 4건)

본 발주는 **시안 디자인 그대로 적용**이 원칙. 단, 다음 4건은 **백엔드 P2 보류** — 디자인 적용 + 기능은 점진:

| # | 시안 디자인 | 현 시스템 | 본 발주 처리 |
|---|---|---|---|
| **M-1** | C 폼 우상단 "📂 임시저장 불러오기 / 📋 템플릿 사용" 버튼 | 미구현 | **버튼 노출 + onclick → "준비 중" toast (T-02 info)** / 백엔드 P2 |
| **M-2** | C 폼 4 step 인디케이터 (기본·품목·결제·검토) | 단일 페이지 | **시각화만 적용** (4 step 표시 + 단일 페이지 폼 그대로) / 단계 분리 P3 |
| **M-3** | D 리스트 일괄 액션 바 (검정+앰버) | 일부 페이지만 | **바 디자인 + 체크박스 적용** / 일괄 처리 백엔드는 페이지별 점진 |
| **M-4** | E 상세 5단계 워크플로우 (발주→...→입고) | po_detail 일부 | **po_detail 우선 적용** / 다른 상세 (이슈·게시·고객·티켓·부품)는 단계 데이터 있는 곳만 / 없는 곳은 워크플로우 영역 비활성 |

→ 01팀 작업 시 위 4건은 **디자인 그대로 적용 + 백엔드는 시안에 안 맞아도 OK**. 차후 P2/P3에서 점진 보강.

---

## 9-B. 7차 검증 보강 추가 시안 (28 파일로 확장)

본 발주서는 시안 22 파일 → **28 파일** 확장 발주:

| 추가 파일 | 위치 | 용도 |
|---|---|---|
| **N-2** `master_G_guide_v5H5.html` | G_standalone | `/guide` 페이지 마스터 (PREVIEW에서 풀세트 폴더로 이동) |
| **N-3** `master_G_empty_v5H5.html` | G_standalone | 빈 상태 4종 (E-01~E-04) — D 리스트 24 페이지 0건 시 표준 |
| **N-4** `master_F_toasts_v5H5.html` | F_모달 | 토스트 5종 (T-01~T-05) — 우상단 자동 알림 신규 컴포넌트 |
| **N-5** A·D variations 모바일 spec | A·D | 모바일 우선 9 페이지 변환 spec (사이드바 drawer·카드 리스트·하단 액션바) |
| **N-1** 00_INDEX.md 갱신 | 풀세트 root | 22 → 28 파일 카운트 정정 |

01팀 반영 시 위 5건 모두 포함.

---

## 10. 빅터(05) 사후 검증 (정책 §4-A 7차 약속)

01 응답서 도착 시 빅터(05)는 **응답서 액면 수용 금지**, 다음 추가 grep 5종 의무:

1. 각 페이지 옛 sage 잔존 (페이지별)
2. 인라인 #hex 잔존
3. standalone 페이지 (login·guide·6 print) 별도
4. 모바일 9개 페이지 별도
5. base 3종 vs 페이지 정합성

→ 위 검증 후 99 디스패치 FYI 발행 + 04팀 walkthrough 결과 수령 → BAT 인계.

---

## 11. 빅터 자기검증 12차 (예고 — 검증 후 등재)

| 차수 | 학습 |
|---|---|
| 1~11 | 누적 (메모리 영구 등재) |
| **12 (예고)** | **시안 우선 워크플로우 효과 실증 — 발주서 종류 분리(요청/반영) + 풀세트 사전 시안 + 표준 토큰 단일 시트 = 부분 적용 사고 0건 / 사이클 시간 50% 단축** (검증 후 정식 등재 — 2026-04-29) |

---

## 12. 첨부

```
✅ 본 반영 발주서
✅ 시안 폴더 전체: 03_시안/_05_v5H5_풀세트시안/ (22 파일)
   ├── ZZ_표준토큰.css
   ├── 0_base/ (3 파일)
   ├── A·B·C·D·E·F·G·H·I·J/ (마스터 13 + variations 8)
   └── 00_INDEX.md
✅ 01 응답서 (페이지 목록): _세션01_수신/_FROM_01_2026-04-29_v5H5_페이지목록응답.md
✅ 폐기 v4 발주서: _세션01_전달/_ARCHIVED/ (2 파일 DEPRECATED)
✅ 정책 §5 시안 우선 워크플로우 (메모리 영구 등재)
```

---

**발행**: 2026-04-29 · 05 디자인팀 세션 빅터
**상태**: 🟢 **반영 발주서 발행 · 01 단순 반영 ~7h 사이클 시작 가능 (대표 01 호출 후)**
**다음**:
- 대표 01 세션 호출 → 01 작업 시작
- 빅터(05) +2h / +6h / +7h 시점 LIVE 점검
- 04팀 walkthrough W1~W12 결과 수령
- 99 디스패치 FYI 시간당 보고
