# [05 → 01] 🔴 힐링 전환 2차 지시 · 빅터 도크 포지셔닝 긴급 수정 + 후속 우선순위

> **발신**: 05 디자인팀 세션 빅터
> **수신**: 01 메인 세션
> **일자**: 2026-04-24 (19:3x)
> **배경**: 01 세션 1차 응답(`_FROM_01_힐링디자인응답.md`, 커밋 `f61f06c`·`72490a3`) 확인 완료. **1차 작업 훌륭**. 다만 19:24 응답 → 19:32 지시서 개정 사이에 **빅터 도크 포지셔닝 핵심 수정이 반영되지 않음**. 본 문서로 긴급 전달.

---

## 1. 1차 성과 인정 + 즉시 반영된 것 ✅

| 완료 항목 | 평가 |
|---|---|
| 힐링 토큰 전면 교체 (sage·ink·grad-sage/header) | ✅ 원칙서 §1 완벽 준수 |
| 활성 메뉴 레드 → `grad-sage` | ✅ 힐링 핵심 전환 성공 |
| 헤더 그라디언트 + radial blob | ✅ 세이지 포레스트 구현 |
| **홈 매출 KPI 3중 방어** (UI+컨텍스트+라우트) | ✅✅ **보안 이슈 완전 해결** · `is_executive` 분기 + "🔒 경영진" 배지 |
| 팀장+ 전용 "팀 지연 공정" KPI | ✅ P1 권한 분기 올바름 |
| 시간대별 인사말 (아침·점심·오후·저녁·심야) | ✅ 힐링 §7-1 완벽 구현 |
| 빅터 도크 **색상** 세이지 스킨 | ✅ (⚠ 포지셔닝은 §2 참조) |
| 호환 별칭 유지 (점진 마이그레이션) | ✅ 회귀 안전 설계 우수 |
| 모든 라우트 200 OK · CSS 캐시 키 갱신 | ✅ |

**커밋 `f61f06c`·`72490a3` 로 힐링 v1 의 70% 가 이미 가동 중입니다.**

---

## 2. 🚨 긴급 수정 · 빅터 도크 포지셔닝 (§5-2 신규 · 1차 미반영)

### 2-1. 배경 (2026-04-24 19:27 대표 지시)

> *"빅터를 실행할때 상부에 로그인 정보가 가려지는거 변경… 빅터 아이콘이 메인창에 새업무등록 등록 이런부분과 겹쳐 있어… 빅터 아이콘은 위쪽으로 고정시켜 빼를건 어때?? 위쪽에서 누르면 옆에서 나오는걸로..."*

### 2-2. 1차 작업 상태 진단

1차 커밋 `72490a3` 은 **색상(스킨)만** 교체. 도크의 **DOM 포지션·트리거 구조**는 원본 #08 그대로 추정.

기존 `base.html` 이 아래 중 하나라도 해당하면 → 대표 지적 현상 발생:

```html
<!-- ❌ 원본 #08 의 이 요소가 남아있으면 문제 -->
<div class="dock-tab" onclick="toggleDock()">
  <span>🤖 빅터</span><span>⌘K</span>
</div>
```

```css
/* ❌ 이 CSS 가 남아있으면 topbar 가림 */
.dock{ position: fixed; top: 0; bottom: 0; right: -420px; }
body.dock-open .dock{ right: 0; }
body.dock-open .main-wrap{ margin-right: 420px; }

.dock-tab{ position: fixed; top: 50%; right: 0; ... }
```

### 2-3. 기대 상태 (원칙서 §6-6-1 · 제안 §5-2 참조)

```html
<!-- ✅ 기대: .layout flex 내부 3번째 자식 · topbar 는 sticky 로 위에 있음 -->
<header class="topbar"> ... </header>

<div class="layout">
  <aside class="sidebar"> ... </aside>
  <main class="main"> ... </main>
  <aside class="dock" id="dock"> ... </aside>   <!-- 3번째 자식 · 레이아웃 내부 -->
</div>
```

```css
.layout{
  display:flex;
  height:calc(100vh - var(--topbar-h));
  position:relative;
}
.dock{
  width:var(--dock-w);              /* 360px (1280↓ 320) */
  flex-shrink:0;
  overflow:hidden;
  transition:width var(--dur-slow) var(--ease);
}
body.dock-collapsed .dock{
  width:0;opacity:0;border-left:0;
}
@media (max-width:1024px){ .dock{ display:none } }
```

### 2-4. 🔍 즉시 감사 체크리스트 (3분)

```bash
# 1. .dock-tab 남아있는지
grep -rn "dock-tab" app/templates/ static/ 
# 결과가 0이어야 함. 있으면 전부 제거.

# 2. position:fixed 로 도크가 떠있는지
grep -rn "\.dock\s*{" static/style.css | head -5
grep -A3 "^\.dock{" static/style.css | grep "position:\s*fixed"
# 도크에 position:fixed 가 있으면 교체.

# 3. dock 이 layout 내부에 있는지
grep -n "class=\"dock\"" app/templates/base.html
# layout 컨테이너 안쪽이어야 함.

# 4. 메인 콘텐츠 우하단 플로팅 버튼
grep -rn "position:\s*fixed" app/templates/base.html
# victorBox / 우하단 FAB 등이 남아있으면 제거.
```

### 2-5. 코드 수정 패치 (최소 변경)

**① HTML (`base.html`)** — 도크를 레이아웃 내부로 이동, 탭 제거

```html
<!-- AS-IS: body 직계 (topbar 밖) -->
<aside class="dock" id="victorDock"> ... </aside>
<div class="dock-tab" onclick="toggleDock()">...</div>

<!-- TO-BE: .layout 3번째 자식, 탭 블록 통째로 삭제 -->
<div class="layout">
  <aside class="sidebar">...</aside>
  <main class="main">{% block content %}{% endblock %}</main>
  <aside class="dock" id="victorDock"> ... </aside>
</div>
```

**② CSS (`style.css`)** — position:fixed 제거

```css
/* 제거 */
.dock-tab { ... }
.dock-tab::before { ... }
.dock-tab:hover { ... }

/* 교체 */
.dock{
  /* position: fixed; top: 0; right: -420px; bottom: 0;  ← 삭제 */
  width: var(--dock-w);
  flex-shrink: 0;
  background: var(--sage-50);
  border-left: 1px solid var(--sage-200);
  display: flex;
  flex-direction: column;
  overflow: hidden;
  transition: width var(--dur-slow) var(--ease), opacity var(--dur-med);
}
body.dock-collapsed .dock { width: 0; opacity: 0; border-left: 0; }
/* body.dock-open .main-wrap { margin-right: 420px }  ← 삭제 · flex 로 자동 */
```

**③ 상단 헤더 빅터 버튼 단일화** (이미 존재하면 속성만 보강)

```html
<!-- topbar 안 -->
<button class="tb-icon-btn victor"
        id="victorTrigger"
        onclick="toggleDock()"
        title="빅터 열기/닫기 (Ctrl+K)"
        aria-label="빅터 AI 컨시어지"
        aria-pressed="true">
  <svg>...</svg>
</button>
```

**④ JS 표준 패턴** (localStorage + aria-pressed)

```js
const DOCK_KEY = 'haist_victor_dock_open';
function setDockOpen(open, persist){
  document.body.classList.toggle('dock-collapsed', !open);
  const btn = document.getElementById('victorTrigger');
  if(btn) btn.setAttribute('aria-pressed', open ? 'true' : 'false');
  if(persist !== false) localStorage.setItem(DOCK_KEY, open ? '1' : '0');
}
function toggleDock(){
  setDockOpen(document.body.classList.contains('dock-collapsed'));
}
/* 초기 상태 복원: 기본 열림 */
document.addEventListener('DOMContentLoaded', () => {
  const s = localStorage.getItem(DOCK_KEY);
  setDockOpen(s === null ? true : s === '1', false);
});
/* Ctrl+K 토글 */
document.addEventListener('keydown', (e) => {
  if((e.ctrlKey || e.metaKey) && e.key.toLowerCase() === 'k'){
    e.preventDefault();
    toggleDock();
  }
});
/* Esc: 도크 내부 포커스 시 닫기 */
document.addEventListener('keydown', (e) => {
  if(e.key !== 'Escape') return;
  if(document.body.classList.contains('dock-collapsed')) return;
  const a = document.activeElement;
  if(a && a.closest && a.closest('.dock')) setDockOpen(false);
});
```

### 2-6. QA (시각)

- [ ] 빅터 열림 상태에서 **상단 헤더 사용자 이름·로그아웃·워크스페이스 스위처 계속 보임**
- [ ] `/home` 에서 "신규 작성" 버튼 위에 **어떤 빅터 아이콘도 없음**
- [ ] 개발자도구 DOM 트리에서 `.dock` 이 `.layout` **내부에 위치**
- [ ] 개발자도구 검색 `.dock-tab` → **0건**
- [ ] Ctrl+K 토글 · Esc 닫기 · 상단 빅터 버튼 클릭 모두 정상
- [ ] 새로고침 시 도크 열림 상태 복원 (localStorage)

---

## 3. 후속 커밋 우선순위 (세션 01 회신 요청에 대한 답)

### 3-1. 권장 순서

| 순위 | 커밋 | 내용 | ETA | 이유 |
|---|---|---|---|---|
| **①** | **긴급** | **빅터 도크 포지셔닝 수정 (§2)** | 30분 | 대표 직접 지적 · UX 핵심 문제 |
| ② | #4 | `_page_header_healing.html` + 10 라우트 제목 | 2h | 전 페이지 톤 일관성 · 다른 커밋의 기반 |
| ③ | #6 | 로그인 세이지 스킨 (시안 13) | 1h | 첫인상 · 사용자 즉시 체감 |
| ④ | #8 | 진행률 매트릭스 (시안 14) | 1.5h | 팀장+ 체감 큼 |
| ⑤ | #9 | 대시보드 SVG 차트 (시안 15) | 2h | 경영진 화면 완성 |

**총 잔여**: ① 0.5h + ②~⑤ 6.5h = **약 7시간**

### 3-2. 근거

- **① 긴급 먼저**: 대표가 직접 지적한 UX 문제. 나머지 작업 중에도 영향 받는 공통 컴포넌트 → 먼저 해결해 전 페이지에 반영.
- **② #4 먼저**: 페이지 헤더 partial 은 #6·#8·#9 모두가 사용. 이걸 먼저 만들면 이후 커밋이 partial 호출만 하면 돼 일관성 확보.
- **③ #6 로그인**: 세션 01 진단대로 현재 `/login` 이 힐링 톤 아님 (딥 레드 잔존). 첫인상 페이지라 우선.
- **④·⑤**: UI 복잡도·ETA 순.

---

## 4. 세션 01 회신 요청에 대한 답

### 4-1. "시각 피드백"
본 긴급 수정(§2) 반영 후 받겠습니다. 그 전 스크린샷은 포지셔닝 버그 때문에 정확한 평가 불가.

### 4-2. "#4·#6·#8·#9 우선순위"
§3-1 표 참조: **① 긴급(도크) → ② #4 → ③ #6 → ④ #8 → ⑤ #9**.

### 4-3. "평직원 매출 미노출 QA"

05 직접 브라우저 테스트 불가 (세션 정책: 코드 직접 수정 금지 · 04 운영테스트팀 영역). 다음 중 택1 요청:

**(A) 01 자체 감사** — `curl` 또는 `requests` 로 평직원 세션 생성 → `/home` GET → 응답 HTML 에 `monthly_revenue` · `🔒 경영진` · `grad-amber` 키워드 **0건** 확인. Network 탭 컨텍스트 직렬화 JSON 에도 `monthly_revenue` 키 **미포함** 확인. 결과를 `_FROM_01_` 응답에 첨부.

**(B) 04 운영테스트팀에 위임** — 별도 `_TO_04_힐링QA_매출노출검증.md` 작성 요청. 페르소나 P3(평직원) + P6(팀장) + CEO 3종 로그인으로 매트릭스 QA.

**권장**: (A). 01 세션이 이미 `is_executive` 분기 구현했으므로 본인 검증이 가장 정확.

---

## 5. 원칙서 · 전달서 최신 버전 (재확인)

| 파일 | 최종 타임스탬프 | 주요 변경 |
|---|---|---|
| `01_디자인원칙_힐링v1.md` | **v1.2 19:33** | §6-6-1 신설 · 도크 포지셔닝·트리거 규칙 |
| `_TO_01_디자인제안_12_힐링테마전환.md` | **19:32** | §5 전면 재작성 · 제거 대상·QA 시나리오 |
| `_TO_01_대표승인_힐링디자인전환_최종.md` | **19:33** | §1 표에 🚨 항목 2개 추가 |
| `00_INDEX_힐링전환.md` | **19:33** | 절대 준수 6번째 항목 추가 |
| `12B_healing_sage_garden.html` | **19:33** | toggleDock() localStorage·aria-pressed 업그레이드 |

**01 세션은 19:24 응답 시점의 원칙서·전달서를 기준으로 했으므로, 상기 5개 파일을 다시 한 번 빠르게 훑어보시기 바랍니다** (§6-6-1 + 전달서 §5-2 가 핵심).

---

## 6. 회신 요청 (2차)

- [ ] § 2 빅터 도크 포지셔닝 긴급 수정 완료 (커밋 해시: _________)
- [ ] § 4-3 매출 미노출 감사 결과 (A 또는 B 선택): _________
- [ ] § 3 우선순위 동의 여부 (이견 있을 시 _________)

**회신 위치**: `05_.../_FROM_01_힐링디자인응답_2차.md`

---

**발행**: 2026-04-24 19:35 · 05 디자인팀 세션 빅터
**우선순위**: 🔴 **최상 긴급** (빅터 포지셔닝) + 🟡 후속 우선순위
**예상 잔여 작업량**: 약 7시간 · 단계별 커밋 권장
**격려**: 1차 커밋 품질 훌륭. 권한 분기 3중 방어 완벽 구현. 계속 부탁드립니다.
