# [05 → 01] 🔴 공식 업무 진행 요청 · 힐링 3차 패치

> **발신**: 05 디자인팀 세션 빅터
> **수신**: 01 메인 세션
> **참조**: 09 프로젝트 팀장
> **일자**: 2026-04-25 13:35
> **지위**: 본 문서가 **공식 착수 요청**. 상세 코드 가이드는 `_TO_01_힐링3차_QA반영_긴급패치.md` 참조.

---

## 0. 한 줄

대표 지적사항 검증 결과(04 운영테스트팀 1차 회신, 12:40 도착) 반영을 위한 **4건의 코드 패치를 24시간 내 착수·완료** 부탁드립니다.

---

## 1. 착수 요청 사항

### 1-1. 작업 계약 (📌 2026-04-25 13:45 보강 — 패치 ⑤ 사이드바 #09 추가)

| 항목 | 내용 |
|---|---|
| **마감** | 2026-04-26 13:00 (착수 시점 +24시간) |
| **권장 완료** | 2026-04-25 17:30 (오늘 5.5시간 내 가능 추정) |
| **총 ETA** | **1시간 30분** (① 15분 + ② 30분 + ③ 15분 + ④ 5분 + **⑤ 25분**) |
| **대표 지적사항** | 매출 권한 + 빅터 도크 + **🔴 사이드바 고정 미반영 (#09)** |
| **04 검증 결과** | S1 3건 + S2 3건 (본 4건 패치) + **#09 누락 발견 (대표 직접 지적)** |

### 1-2. 권장 진행 순서

```
13:45 ─ 본 요청 접수 (재발행)
14:00 ─ 패치 ① .dock-tab 제거 (15분)
14:30 ─ 패치 ② CEO 매출 fallback (30분)
15:00 ─ 패치 ③ 언어 셀렉터 정합 (15분)
15:15 ─ 패치 ④ 인사말 검증 보강 (5분)
15:25 ─ 패치 ⑤ 사이드바 드래그 완전 제거 (#09 계승) (25분)  ← 신규
16:00 ─ 자체 회귀 검증 (curl 5종)
16:30 ─ `_FROM_01_힐링디자인응답_2차.md` 회신
```

---

## 2. 4건 패치 요약 (상세는 §6 참조 문서)

### 패치 ① — 빅터 도크 `.dock-tab` 제거 🔴

- **원인**: 19:35 발행한 2차 지시(`_TO_01_힐링2차_*.md`) 미반영
- **확인**: `grep -rn "dock-tab" app/templates/ static/style.css` → **결과 0건**이어야 함
- **제거**: HTML 의 `<div class="dock-tab">...</div>` + CSS `.dock-tab { ... }` 모두
- **이유**: 대표 직접 지적 — *"빅터 아이콘이 메인창 새업무등록 등록 이런부분과 겹쳐 있어"*
- **ETA**: 15분

### 패치 ② — CEO 매출 KPI fallback 🔴

- **원인**: 05 직접 조사 결과 — DB에 `projects` 테이블 자체 부재 → SELECT 예외 → `monthly_revenue=None` → `home.html:180` 카드 미렌더
- **수정**: `home.html:180` 의 분기를 단순화
  ```jinja2
  {# AS-IS #}
  {% if is_executive and monthly_revenue is not none %}

  {# TO-BE #}
  {% if is_executive %}
  ```
  데이터 없을 때 "**집계 준비 중**" 안내 표시 (상세 코드는 §6 문서 참조)
- **이유**: CEO에게 매출 카드 슬롯 인지 + 권한 분기 유지
- **ETA**: 30분
- **별건 (시드 보강)**: 09 팀장 결정 후 별도 진행 (본 패치와 분리)

### 패치 ③ — 언어 셀렉터 zh-CN·ja 제거 🔴

- **원인**: `base.html:404~410` 의 `#trLang` (카드 모달 자동번역 위젯) 5종 하드코딩 — UI 셀렉터(`#langSel`, ko/vi/en) 와는 별도 컴포넌트
- **수정**:
  ```html
  <!-- base.html:404 #trLang -->
  <select id="trLang">
    {% for code, label in LANGS.items() %}
      <option value="{{ code }}">{{ label }}</option>
    {% endfor %}
  </select>
  ```
  ```js
  // base.html:582
  const langNames = {ko:'🇰🇷 한국어', vi:'🇻🇳 Tiếng Việt', en:'🇺🇸 English'};
  ```
- **이유**: 의뢰서 §S3 위반 + 어제 베트남어 누락·중국어 추가 사고 재발 방지
- **ETA**: 15분
- **검증**: `curl /home | grep -c "zh-CN\|중국어\|일본어\|ja"` → **0**

### 패치 ④ — 인사말 검증 보강 🟠 (선택)

- **원인**: 04 가 12:40 검증 시 "오늘도 평안", "좋은 아침" 0건 → 실제로는 시간대별 다른 텍스트 노출 (false alarm 의심)
- **수정**: `home.html:38` 의 h1 에 `data-greeting-bucket="{{ greeting_bucket }}"` 속성 추가 (main.py 에 bucket 변수도 추가)
- **이유**: 04 회귀 검증 시 시각 무관하게 인사말 노출 확인 가능
- **ETA**: 5분
- **선택**: 시간 부족 시 ④ 만 후순위로

---

## 3. 자체 회귀 검증 (4건)

패치 적용 후 다음 명령으로 본인 검증 후 회신 부탁드립니다:

```bash
# 1. 도크 탭 제거
curl -s "http://localhost:8000/home" -b "session=$ANY_TOKEN" | grep -c "dock-tab"
# 기대: 0

# 2. CEO 매출 카드 노출 (데이터 없어도)
curl -s "http://localhost:8000/home" -b "session=$CEO_TOKEN" | grep -c "이번 달 수주\|집계 준비 중\|🔒 경영진"
# 기대: 1 이상

# 3. 평직원 매출 미노출 (회귀 안전성)
curl -s "http://localhost:8000/home" -b "session=$PLAIN_TOKEN" | grep -c "이번 달 수주\|monthly_revenue\|₩\d"
# 기대: 0

# 4. 언어 셀렉터 정합
curl -s "http://localhost:8000/home" -b "session=$ANY_TOKEN" | grep -c "zh-CN\|중국어\|일본어\|ja\""
# 기대: 0
```

검증 결과를 회신에 첨부해 주시면 04 가 회귀 검증 시 참조합니다.

---

## 4. 회신 양식

```markdown
# _FROM_01_힐링디자인응답_2차.md

## 착수
- 시작: 2026-04-XX HH:MM
- 완료: 2026-04-XX HH:MM
- 소요: __분

## 패치별 결과
| 패치 | 커밋 해시 | 상태 |
|---|---|---|
| ① .dock-tab 제거 | XXXXXXX | ✅ |
| ② CEO 매출 fallback | XXXXXXX | ✅ |
| ③ 언어 셀렉터 정합 | XXXXXXX | ✅ |
| ④ 인사말 보강 | XXXXXXX | ✅ / 보류 |

## 자체 회귀 검증
1. dock-tab grep: 0 ✓
2. CEO 매출 카드: 1 ✓ (집계 준비 중 표시)
3. 평직원 매출: 0 ✓
4. zh-CN/ja: 0 ✓

## 발견 이슈 (있다면)
- ...

## 질문 (있다면)
- ...
```

---

## 5. 질문·블로커 발생 시

작업 도중 막힘 발생 시 즉시 회신 부탁드립니다:

```
05_.../_FROM_01_힐링3차_질문.md
```

본 패치는 **단순 수정 위주**라 블로커 가능성 낮으나, 다음 경우 즉시 알려주세요:
- 패치 ②에서 시드 데이터 보강이 어떻게든 필요하다고 판단되는 경우 → 09 팀장 결정 필요
- 패치 ③의 LANGS 가 다른 곳에서도 하드코딩으로 참조되어 있는 경우 → 일괄 정리 권한 요청
- 코드 충돌(merge conflict) 또는 테스트 실패 → 즉시 회신

---

## 6. 참조 문서

| 파일 | 용도 |
|---|---|
| **`_TO_01_힐링3차_QA반영_긴급패치.md`** | **상세 코드 가이드** (라인 번호·코드 블록) |
| `_FROM_04_힐링QA결과_01.md` | 04 1차 검증 결과 원본 |
| `_TO_01_힐링2차_빅터도크긴급수정_우선순위.md` | 2차 지시 (패치 ① 의 원래 지시서) |
| `_TO_01_디자인제안_12_힐링테마전환.md` | 메인 제안 (전체 컨텍스트) |
| `01_디자인원칙_힐링v1.md` v1.3 | 원칙서 (§11 권한 매트릭스 + §11-7 매출 단어 vs 금액) |

---

## 7. 1차 작업 인정

01 세션 1차 응답(`_FROM_01_힐링디자인응답.md`) 의 작업 품질 훌륭했음을 다시 한번 명시:

- 힐링 토큰 전면 교체 (sage·ink·grad-sage/header) 완벽
- 홈 매출 KPI 3중 방어 (UI + 컨텍스트 + 라우트) 견고 구현
- 호환 별칭 유지로 기존 코드 회귀 안전 설계
- 시간대별 인사말 5단계 분기 정확
- 모든 라우트 200 OK · CSS 캐시 키 갱신

**04 검증 결과 PASS 항목 다수**가 본 1차 작업의 품질 증명. 잔여 4건 패치는 1차 작업의 보완 성격.

---

**발행**: 2026-04-25 13:35 · 05 디자인팀 세션 빅터
**상태**: 🔴 **착수 요청 · 24시간 회신 마감**
**다음**: 회신 도착 시 04 회귀 검증 사이클 진입
