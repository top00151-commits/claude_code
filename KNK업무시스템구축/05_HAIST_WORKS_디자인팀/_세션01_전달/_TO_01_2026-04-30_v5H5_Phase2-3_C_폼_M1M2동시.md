# [05 → 01] 🟢 v5 H5 Phase 2-3 — C 폼 카테고리 (22 페이지) + M-1·M-2 동시

> **발신**: 05 디자인팀 빅터
> **일자**: 2026-04-30
> **지위**: 반영 발주서 (P2 보류 2건 동시)
> **순서**: Phase 2 2번 (P0 폼 5종 사용자 영향 우선)
> **마감**: 본 발주 후 16시간 (22 페이지 + 4-step 인디케이터)

---

## 1. 적용 대상 (22 페이지) + P2 보류

### 그룹 1 — 거래·발주 폼 (4)
| 라우트 | 템플릿 | base | P | 변형 |
|---|---|---|---|---|
| `/po/new`·`/po/{id}/edit` | `po_form.html` | base_logi | **P0** | **마스터** |
| `/sales/quotations/new` | `quotation_form.html` | base_sales | **P0** | A 견적 |
| `/parts/new`·`/parts/{id}/edit` | `part_form.html` | base_logi | P1 | B 부품 |
| `/suppliers/new` | `supplier_form.html` | base_logi | P1 | C 공급사 |

### 그룹 2 — 워크플로우 폼 (5)
- `/po/{id}/receive` (D 입고) / `/stock/issue` (E 출고 + 안전재고 모달) / `/stock/adjust` (F 실사) / `/stock/qc/{id}` (G 검수 + disposition 모달) / `/production/work-orders/new` (H 작업지시)

### 그룹 3 — 일반 폼 (8)
- `/daily` (I 일일) / `/board/new` (J 게시) / `/issues/new` (K 이슈) / `/changes/new` (L 변경) / `/tickets/new` (M 티켓) / `/projects/new` (N 관리코드) / `/qc/inspection-reports/new` (O QC) / `/export/orders/new` (P 수출)

### 그룹 4 — 수출 양식 (5)
- `/export/ci/{id}` / `/export/pl/{id}` / `/export/bl/{id}` / `/export/customs/{id}` / `/export/fta/new`·`/export/fta/{id}` (Q·R·S·T·U)

## 2. P2 보류 동시 적용

### M-1: 임시저장·템플릿 버튼
- 시안 우상단 2 버튼 디자인 적용
- onclick → `window.victor.toast.info('준비 중')` (T-02 토스트)
- 백엔드 P2 보류

### M-2: 4-step 인디케이터
- `.form-stepper` 시각 적용 (시안 그대로)
- 4 step (기본·품목·결제·검토)는 **시각만**
- 단일 페이지 폼 그대로 (단계 분리 P3)
- 첫 step `active` 고정 / 나머지 비활성

## 3. 시안 위치

```
03_시안/_05_v5H5_풀세트시안/C_폼/
├── master_C_form_po_v5H5.html  ← po_form 마스터
└── variations.md               ← 22 변형 spec (A~U)
```

## 4. 핵심 컴포넌트

1. **4-step 인디케이터** (`.form-stepper`)
2. **섹션 표준** (h3 + 4px 앰버 강조)
3. **필드 표준** (label·필수·hint)
4. **라인 항목 7-컬럼** (`.lines`) + 자재 마스터에서 가져오기 + 이전 발주서 복사
5. **합계 영역** (총액 그라데이션 텍스트)
6. **첨부 박스** (dashed border)
7. **우측 사이드** (요약·빅터 도움말·최근 활동)
8. **하단 sticky 액션바** (자동저장 + 4 버튼)

## 5. F 모달 5종 통합

폼 마스터에 F-03 (라인 추가) / F-04 (거래처 신규) / F-05 (발행 확인) 모달 통합 필수.
입고·출고·검수에 F-01 (안전재고) / F-02 (disposition) 추가.

## 6. Jinja2 inject map

| 시안 | inject |
|---|---|
| 4-step 인디케이터 | 시안 그대로 (active=1 고정) |
| 거래처 select | `{% for s in suppliers %}` |
| 라인 항목 표 | `{% for line in form.lines %}` |
| 합계 ₩6,903,000 | `{{ total }}` |
| 첨부 리스트 | `{% for att in attachments %}` |
| 우측 사이드 활동 | `{% for log in activity_log[:4] %}` |
| 하단 자동저장 시간 | `{{ last_saved }}` |

## 7. 시안 vs 현 코드 충돌

- `change_form.html` 인라인 #hex 56건 → 0 강제 (D-1 grep)
- `change_detail.html` (E 카테고리 — P2-5에서 처리)
- `external_assets_review.html` 48건 — 본 P2-3 포함 / 0 강제
- `po_form.html` 8건 → 0
- 다른 폼: 정확 카운트는 작업 중 검증

## 8. grep 검증

```bash
grep -c 'class="form-stepper"' app/templates/po_form.html app/templates/quotation_form.html  # ≥2
grep -c 'class="lines"' app/templates/po_form.html  # ≥1
grep -cE 'style="[^"]*#' app/templates/*_form.html  # = 0
grep -c 'window\.victor\.toast' app/templates/po_form.html  # ≥1 (M-1 toast)
```

## 9. 04 walkthrough

- **W3**: 김 부장 견적 작성 4 단계 → 발행 모달
- **W5**: 이 차장 발주서 작성 → 3 라인 + 모달 → 발행
- **W7**: 박 대리 입고 처리 → QC 부적합 모달
- **W8**: 최 과장 출고 등록 → 안전재고 모달

## 10. 마감

- 22 페이지 + 모달 5종 통합: **16시간** (P0 5종 우선 8h + 나머지 8h)
- 응답서: `_FROM_01_2026-04-30_v5H5_Phase2-3_C_완료.md`

---

**발행**: 2026-04-30 · 빅터(05)
**상태**: 🟢 P2-3 발주 — P2-1 완료 후 시작 권고 / M-1·M-2 동시 처리
