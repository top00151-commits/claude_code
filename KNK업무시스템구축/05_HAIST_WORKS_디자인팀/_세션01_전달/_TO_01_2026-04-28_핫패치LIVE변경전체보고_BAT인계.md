# [05 → 01] 🟢 2026-04-28 핫패치 LIVE 변경 전체 보고 + BAT 인계

> **발신**: 05 디자인팀 세션 빅터
> **수신**: 01 메인 세션 (실무팀)
> **참조**: 09 프로젝트 팀장 / 대표이사
> **일자**: 2026-04-28 (오늘)
> **트리거**: 대표 직접 점검 7건 (메인 + 매출영업 + 자재구매 사이드바, 빅터 도크, 헤더 로고, 로그인 로고)
> **신규 프로토콜 (대표 직접 지시)**: *"수정한건 항상 보고하고 실무팀에 전달해서 .bat파일 업데이트해"*
> **지위**: 🟢 **본 보고로 협업 프로토콜 정상화 — 향후 빅터(05) LIVE 핫패치 시 01 즉시 인계 의무**

---

## 0. 한 줄

대표 직접 점검에서 발견한 7개 결함을 빅터(05)가 응급 핫패치로 처리. 본 문서로 모든 변경 사항을 01 실무팀에 인계. **이번까지는 빅터(05)가 .bat까지 갱신했으나, 향후부터는 빅터 핫패치 → 01 .bat 갱신 분리**.

---

## 1. 신규 협업 프로토콜 (대표 직접 지시)

### 1-1. 기존 (오늘 이전)
```
빅터(05) 응급 핫패치 → LIVE 코드 수정 + .bat 갱신 동시 진행 (혼자 다 함)
```

### 1-2. 신 프로토콜 (오늘 이후)
```
[1] 대표 점검에서 결함 발견
 ↓
[2] 빅터(05) 응급 핫패치 (LIVE 코드만)
 ↓
[3] 빅터 → 대표 보고
 ↓
[4] 빅터 → 01 실무팀 인계 문서 발행 (본 양식)
 ↓
[5] 01 실무팀 → .bat 파일 LAST UPDATE 갱신
 ↓
[6] 01 → 09 팀장 보고 (정상 사이클)
```

### 1-3. 본 문서가 신 프로토콜 첫 적용 사례
오늘 7건 핫패치는 빅터(05)가 .bat까지 일단 갱신함. 본 문서로 01에 인계하여 **(a) 변경 내역 인지** + **(b) 향후 BAT 갱신 책임 명확화**.

---

## 2. LIVE 코드 변경 7건 (2026-04-28 빅터 응급 핫패치)

### 핫패치 ① — 빅터 도크 헤더 덮음 수정
```
파일: 01_HAIST_WORKS/static/style.css
라인: 3604~3626
변경 전: .dock { position: fixed; top: 0; bottom: 0; right: -420px; ... }
변경 후: .dock { position: fixed; top: var(--topbar-h, 84px); bottom: 0; right: -420px; ... }
부수 변경: body.dock-open .topbar { padding-right: 420px } 규칙 제거
근거: 빅터 창이 헤더(워크스페이스·검색·언어셀렉터·사용자칩) 위쪽 가리지 않게 헤더 아래에서 시작
대표 발화: "빅터 창이 상위 로그인 정보를 가리고 있는데..."
```

### 핫패치 ② — 메인 base.html 사이드바 ◀ 화살표 제거
```
파일: 01_HAIST_WORKS/app/templates/base.html
라인: 93
변경 전: <div class="sb-toggle" onclick="toggleSidebar()" title="...">◀</div>
변경 후: (DOM 줄 제거 + 주석 1줄)
참고: toggleSidebar() JS 함수는 유지 (헤더 모바일 메뉴 .tb-menu-btn 가 호출)
대표 발화: "왼쪽 사이드바 클릭하는 건 외 아직 있는거고..."
```

### 핫패치 ③ — 매출·영업 Hub base_sales.html 사이드바 ◀ 화살표 제거
```
파일: 01_HAIST_WORKS/app/templates/base_sales.html
라인: 54
변경: ②와 동일 패턴
대표 발화: "매출영업, 구매자재 시스템으로 들어가면 좌측 사이드바에 삭제한 조정할때 사용한 삼각형 작은 아이콘이 아직도 있어"
```

### 핫패치 ④ — 자재·구매 Hub base_logi.html 사이드바 ◀ 화살표 제거
```
파일: 01_HAIST_WORKS/app/templates/base_logi.html
라인: 54
변경: ②와 동일 패턴
근거: ③과 동일 발화로 일괄 처리
```

### 핫패치 ⑤ — 헤더 로고 흰 배경 → 투명
```
파일: 01_HAIST_WORKS/static/style.css
라인: 4342~4358
변경 전:
  .brand-logo-wrap { background: #fff; box-shadow: 0 8px 22px rgba(0,0,0,.20), 0 0 0 1px rgba(255,255,255,.30) inset; }
변경 후:
  .brand-logo-wrap { background: transparent; box-shadow: none; }
대표 발화: "이 로고부분.... 흰색 배경을 투명하게 변경하자."
```

### 핫패치 ⑥ — 헤더 로고 사이즈 확대 + 회색 밝게 필터
```
파일: 01_HAIST_WORKS/static/style.css
라인: 4342~4358 (⑤와 같은 블록)
변경:
  .brand-logo-wrap { height: clamp(64-88px) → clamp(72-100px); padding: 10px 16px → 6px 12px; }
  .brand-logo-wrap img { height: clamp(52-76) → clamp(60-90); max-width: 280 → 380; filter: brightness(1.2) (신규); }
대표 발화: "사이즈를 더 키워보는건 어때?? 회색을 약간 더 밝은 회색으로 표현하고."
```

### 핫패치 ⑦ — 로그인 페이지 PNG 로고 대형 중앙 배치
```
파일 1: 01_HAIST_WORKS/app/templates/login.html (CSS) 라인 67~95
변경:
  .bp-header { display: flex; flex-direction: column; align-items: center; gap: 18px; text-align: center; margin-top: clamp(20px, 3vh, 48px); }
  .bp-logo { background: transparent; box-shadow: none; padding: 0; border-radius: 0; height: auto; min-width: 0; }
  .bp-logo img { width: clamp(260px, 30vw, 460px); height: auto; }
  + @media (max-width: 900px) { .bp-logo img { width: clamp(200px, 40vw, 300px); } .bp-header { margin-top: 0; gap: 12px; } }

파일 2: 01_HAIST_WORKS/app/templates/login.html (HTML) 라인 217~226
변경:
  - 인라인 SVG (KNK 글자만, 빨강 단일색)
  + <img src="/static/logo.png?v=20260425loginbig" ...> (PNG 원본 풀로고)
  + 인라인 SVG는 onerror fallback 으로 보존

근거: 좌측 녹색 패널 상단중앙에 회사 풀로고를 원본 색상 그대로 크게 배치
대표 발화: "로그인 페이지에 왼쪽 상단 로고가 잘못 표현됐어... 메인상단 왼쪽 녹색배경 바탕화면에 상단중간쯤에 회사로고를 크게 반영했으면 좋겠어... 회사 로고색상 그대로 적용."
```

---

## 3. 시안 동시 갱신 (참고 — 01 작업 영역 외)

v2 통째 이식 시 정합성 위해 빅터(05)가 시안 9종도 동시 갱신:

| 변경 | 적용 시안 |
|---|---|
| 헤더 로고 흰배경→투명 | 12B + 14·15·16·17·18·19·20 (8개) |
| 헤더 로고 사이즈↑+필터 | 위와 동일 8개 |
| 로그인 로고 PNG 대형 중앙 | 13_login_healing.html |

→ 01이 v2 통째 이식 시 **위 핫패치 라인을 그대로 유지** 하면 자동 정합 (재발 방지).

---

## 4. BAT 파일 인계

### 4-1. 현재 상태 (빅터가 이미 갱신함)

**`KNK_시작.bat`** + **`START.bat`** 양쪽 line 3 (LAST UPDATE) + line 7 (title) 갱신 완료:

```
LAST UPDATE: 2026-04-28 G6핫패치(05디자인,대표직접지시6건)_헤더로고흰배경→투명+사이즈↑+필터1.2_3개base(메인+매출영업+자재구매)사이드바◀화살표완전제거_빅터도크탑오프셋_로그인PNG대형중앙_매트릭스5종지시발행
```

### 4-2. 01 인계 사항

- [x] 변경 내용 인지 — 본 §2 7건 LIVE 변경 사항을 01이 인지
- [ ] **향후 빅터(05) 핫패치 발행 시 .bat 갱신은 01 책임** (대표 신 프로토콜 §1)
- [ ] **차기 코드 수정 시 LAST UPDATE 라인에 본 G6 핫패치 7건 + 01의 추가 변경 내용 누적 표기**

### 4-3. 차기 BAT LAST UPDATE 양식 제안

01이 다음 코드 수정 시:
```
LAST UPDATE: 2026-04-XX [01작업키워드] (선행 G6핫패치-05디자인 7건 보존: 빅터도크/헤더로고/3개base사이드바/로그인PNG)
```

→ 빅터 핫패치 라인을 다음 갱신 시에도 잠시 보존 표기하면 회귀 방지에 도움.

---

## 5. 자체 검증 grep 결과 (모두 PASS)

```
sb-toggle DOM in app/templates                → 0건 ✅ (3개 base 모두)
.dock { top: 0 } 규칙 in style.css           → 0건 ✅
.brand-logo-wrap { background: #fff }        → 0건 ✅ (LIVE)
.bp-logo img width:clamp(260px,30vw,460px)  → 1건 ✅ (login.html)
filter:brightness(1.2) in style.css          → 1건 ✅
PNG src logo.png?v=20260425loginbig          → 1건 ✅
```

---

## 6. 01 회신 부탁 사항

01이 본 문서 받은 후:
- [ ] 본 §2 7건 변경 사항 인지 확인 (즉시)
- [ ] 차기 코드 수정 시점에 BAT LAST UPDATE 양식 (§4-3) 채택 의사 확인
- [ ] (선택) v2 통째 이식 작업 시점에 본 핫패치 라인 보존 + §3 시안 정합 확인 (1차/2차 마감 동시)

회신 위치:
```
01_HAIST_WORKS/_FROM_01_2026-04-28_핫패치LIVE변경인지응답.md
또는
01 의 정상 회신 채널
```

---

## 7. 빅터 자기 개선 (5차)

| 차수 | 시점 | 학습 |
|---|---|---|
| 1차 | 14:30 | 응답 액면 수용 금지 → grep 검증 |
| 2차 | 17:00 | 시안 자체 grep 검증 |
| 3차 | 17:30 | 시안 + LIVE 양쪽 grep |
| 4차 | 18:00 | 시안 + LIVE + 페르소나 walkthrough |
| **5차 (오늘)** | — | **base 계열 패치 시 메인+매출영업+자재구매 3개 동시 검증** + **수정 시 항상 01 인계 + .bat 분리** |

향후 base 계열 결함 발견 시 빅터 자체 grep 매트릭스에 다음 3종 의무 추가:
```bash
grep -c '<div class="sb-toggle"' app/templates/base.html       # 기대 0
grep -c '<div class="sb-toggle"' app/templates/base_sales.html # 기대 0
grep -c '<div class="sb-toggle"' app/templates/base_logi.html  # 기대 0
```

---

## 8. 종합

대표 직접 점검이 또 빅터의 부분 검증 누락(메인 base만 처리, sub-system 빠뜨림)을 잡아냄. 신 협업 프로토콜로 01과 빅터의 책임 분리 명확화. **빅터는 LIVE 핫패치 + 보고 + 01 인계 / 01은 BAT + 정상 사이클**.

본 문서로 오늘 7건 인계 완료. 01 회신 받으면 정상 사이클 복귀.

---

**발행**: 2026-04-28 · 05 디자인팀 세션 빅터
**상태**: 🟢 **LIVE 핫패치 7건 적용 완료 · 01 BAT 인계 + 신 프로토콜 첫 적용**
**다음**: 01 회신 → v2 통째 이식 시 본 7건 라인 보존 + 매트릭스 5종 sticky 패턴 동시 처리
