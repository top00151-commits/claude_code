# 01 HAIST WORKS 디자인 원칙 v2

> **목표**: 한 번 써보고 싶은 시각. 어려워 보이지 않는 시각.
> **실패 회피**: 메뉴트리 깊음·시각 과밀·클릭 예측 불가.
> **기준 자산**: 세션 01 `01_HAIST_WORKS/app/templates/`, `static/style.css` (prem8).
> **선행 맥락**: 데일리허브 v2 에서 사용해 온 **모던 대시보드 다크 탑바 + Pretendard(OFL 1.1) + KNK RED `#A5282C`** 토큰 유지 (메모리 `knk_systems.md` §2).
> **상위 문서**: `04_라이선스_상표권_정책.md` — 본 원칙서가 정책과 충돌하면 정책이 우선.

---

## 0. 외부 상표권·저작권 절대 비침해 (최상위 조항)

> 대표 지시 (2026-04-21): "글씨체, 그림 등부터 외부 상표권에 위반되지 않게 만들어야 해."

1. 타사 상표·저작물을 모방·무단 사용하지 않는다.
2. 모든 외부 자산은 **라이선스 문서화** + **고지 의무 준수**.
3. 타사 브랜드명(Stripe·Vercel·Notion·Figma·Linear·Material 등)을 **내부 문서·UI·CSS 주석에도 언급 금지**. 영감 표현도 중립 용어로.
4. 의심스러우면 사용하지 않고 대표 승인 (`04_라이선스_상표권_정책.md` §5 양식).
5. 매 제안 발행 전 **§6 체크리스트 5 항목 통과** (같은 정책 문서 참조).

위반 발견 시 즉시 작업 중단 → 대표·09 팀장 보고 → 롤백.

---

## 1. 실패 회피 10계명 (First, Do No Harm)

1. **메뉴트리 2 depth 이내**. 사이드바 → 페이지 → (있다면) 모달. 3 depth 이상 금지.
2. **한 화면에 강조 CTA 1개**. 여러 버튼이 있어도 "가장 눌러야 할 버튼"은 색·크기로 유일해야 함.
3. **KPI 그리드는 한 화면에 1개**. 두 그리드가 필요하면 탭으로 분리.
4. **장식 데이터 금지**. 스파크바·그래프는 **실제 데이터**일 때만. 하드코딩 장식은 감사팀 원칙(자료 정확성) 위반.
5. **라벨 없는 아이콘 금지**. SVG만 있으면 신입은 못 읽음. 반드시 텍스트 라벨 병기.
6. **역할별 화면 분기는 route 로 (또는 탭으로)**. 같은 route 가 role 따라 섹션이 붙었다 사라지는 구조는 멘탈모델 붕괴 원인. 예: `/home` 을 탭 3분할하거나 `/home-lead` 분리.
7. **placeholder 는 7자 이내**. 긴 문장은 안내문으로 분리 (placeholder 는 힌트, 안내는 `<small>` 또는 i 아이콘).
8. **테이블 컬럼 7개 초과 시 반응형 재구성 필수** (v2 추가). min-width: 1400px 같은 "데스크톱만 가능" 테이블은 모바일에서 "카드 뷰"로 자동 전환 (`/progress` 제안 #02 패턴).
9. **위험·비가역 기능은 2단계 확인** (v2 추가). 비밀번호 일괄 재생성·데이터 삭제·대량 처리 등 되돌릴 수 없는 기능은 체크박스 + 문자열 입력 2단계 확인. `/admin` 제안 #04 패턴 참조.
10. **필터·네비는 sticky** (v2 추가). 긴 리스트 중간에서도 필터 전환 가능해야 함. `position: sticky; top: 56px;` 기본. `/feed` 제안 #03 패턴.

---

## 2. 색 (Color)
> ⚠️ 본 절의 모든 색은 HEX 값으로만 정의 — 특정 타사 팔레트명(Tailwind·Material 등) 인용 금지. §0 참조.

### 2-1. 브랜드 고정
- **KNK RED 주색**: `#A5282C`
- **KNK RED 다크**: `#8B1E22` (그라디언트 종점)
- **Primary gradient**: `linear-gradient(135deg,#A5282C,#8B1E22)` — **하나의 화면에 단 1곳**에만 적용 (예: 주 CTA).

### 2-2. 상태 색 (4종, 이미 코드베이스에 정착 — 유지)
- 완료 `#10B981` / 진행중 `#F59E0B` / 지연 `#EF4444` / 대기 `#94A3B8`.
- 이 4색은 **상태 태그 이외 용도로 사용 금지**. KPI 아이콘이 이 색을 쓰면 "상태"로 오독됨.

### 2-3. KPI 아이콘 색 (새 규칙)
- KPI 는 **중립 계열**만 사용: 슬레이트 `#475569`, 다크 `#1A1A1A`, 배경 `#F1F5F9`.
- 다채색(`red/blue/green/purple`) KPI 아이콘 팔레트는 **폐기**. 홈 화면 시각 과밀 주범(H-2).

### 2-4. 배경
- Page `#FAFBFC` (현재 유지).
- Card `#FFFFFF`, border `#E5E7EB`, shadow `0 1px 2px rgba(15,23,42,0.04)`.

---

## 3. 폰트 (Typography)

> ⚠️ 폰트 라이선스는 `04_라이선스_상표권_정책.md` §1 참조. 번들링·CDN 로딩은 대표 사전 승인.

- Family: **Pretendard** (SIL OFL 1.1, 상업 사용 OK) → 시스템 폴백 `system-ui, -apple-system, "Segoe UI", Roboto, "Apple SD Gothic Neo", "Malgun Gothic", sans-serif`.
- Pretendard 파일 **번들링 vs 선언만** 여부는 대표 결정 사항 (정책 §1-4).
- 크기 스케일 (고정 6단계):
  | 토큰 | px | 용도 |
  |---|---|---|
  | `fs-xs` | 11 | 캡션·배지 |
  | `fs-sm` | 13 | 본문 부가 |
  | `fs-md` | 14 | 본문 기본 |
  | `fs-lg` | 16 | 섹션 타이틀 |
  | `fs-xl` | 20 | 페이지 타이틀 |
  | `fs-2xl` | 28 | KPI 숫자 (한 화면 최대 1–2개) |
- 폰트 가중치: 400 / 600 / 800 (이 3단계만). 500·700 혼재 금지.
- 한국어 가독을 위해 **letter-spacing 0 기본**. 영어 헤더만 `0.02em` 허용.

---

## 4. 간격 (Spacing)

- 8px 그리드: 4 / 8 / 12 / 16 / 24 / 32 / 48.
- **섹션 간 간격 최소 24px**. 섹션 구분이 약하면 이용자는 "한 덩어리"로 보고 압도됨 (홈 H-1 원인).
- 카드 내 패딩 16–20px.

---

## 5. 네비게이션 (Navigation)

### 5-1. 사이드바 규칙
- 섹션 묶음은 최대 6개 (현재 12개 — 감축 필요).
- 각 섹션 당 링크 3개 이하 권장.
- **역할별 숨김 메뉴는 사이드바에서 "비활성 표시"** 가 아니라 **완전 미노출** (현재와 동일 방향 유지).

### 5-2. 클릭 예측 원칙
- 아이콘 옆 텍스트 **필수**.
- 현재 보는 메뉴는 **active 상태 명확히**: 배경 `#FFEBEE`, 좌측 3px 빨간 바.
- 하위 기능으로 들어갈 때 페이지 이동(전체 refresh)보다 **인라인 펼침·모달 우선**.

### 5-3. 홈(`/home`) 특별 규칙
- **역할별 뷰 분리** (실패 회피 6번): `/home` 은 member 기본. 리더 이상은 `/home`에서 상단에 "리더 대시보드 보기" 버튼으로 `/home-lead` 로 이동.
- 또는 홈 내 **탭** 으로 "내 업무 / 우리 팀 / 전사" 분할. 같은 화면에 9개 섹션 세로 나열 금지.

---

## 6. 아이콘 (Icon)

- **Lucide** (ISC 라이선스 — 상업·수정·재배포 자유, 고지 포함 의무). inline SVG path 로 사용.
- **단일 소스만**. Font Awesome·Material Icons·Bootstrap Icons 혼용 금지 (정책 §2-2).
- 프로젝트 루트 `LICENSE` 파일에 Lucide 고지 1회 포함 의무.
- 크기 `16 / 18 / 20 / 24` 4단계만.
- stroke-width `2` 표준. 강조는 `2.5`.
- 이모지는 **접근성 보조** 로만 사용 (🏠📝📅). 본문 대체 불가. OS 별 렌더 차이로 브랜드 일관성 저하.
- ❌ Apple SF Symbols / 타사 서비스 로고(카카오·구글·네이버 등) / 출처 불명 SVG 금지.

---

## 7. 데이터 시각화

- **모든 차트는 DB 실데이터 기반만**. 하드코딩 height % 금지 (홈 H-3 패턴 폐기).
- 스파크라인은 KPI 당 최대 1개, 그리고 실데이터 없으면 아예 **표시 안 함**.
- 차트 색은 **상태 4색 팔레트만** 재사용 (새 색 금지).

---

## 8. 컴포넌트 일관성

### 8-1. `<title>` 패턴 (04 OPS-009 연계)
- **모든 페이지**: `{페이지명} · HAIST WORKS`.
- 현재 혼재(`· KNK`, `— HAIST WORKS`)는 01 세션 수정 대상. 디자인팀은 가이드 제공만.

### 8-2. 상태 전환 UI (홈 H-4 연계)
- cycle 버튼 대신 **드롭다운 select 또는 4-way 세그먼트 버튼**. 실수 복귀가 1-클릭으로 가능해야 함.

### 8-3. placeholder 문구 (홈 H-5 연계)
- 최대 7자. "업무 제목" / "무엇을 하셨나요?" 정도까지만.
- 긴 안내는 입력창 아래 `<small>` 로 별도 배치.

---

## 8-bis. 반응형 · 디바이스 (v2 신설)

### 8-bis-1. 브레이크포인트
- 데스크톱: **≥ 1024px**. 매트릭스·다컬럼 테이블·대시보드 풀폭 허용.
- 태블릿: **768–1023px**. 2단 그리드 → 1단, 보조 컴포넌트 숨김.
- 모바일: **< 768px**. 카드 뷰 필수. 테이블은 반드시 카드 또는 수평 carousel.

### 8-bis-2. 자동 뷰 전환 규칙
- 정보 밀도가 높은 화면(progress·cockpit·dashboard·admin) 은 **매트릭스 ↔ 카드** 뷰 2종 제공.
- 뷰 전환은 **CSS media query 자동** + 사용자 수동 세그먼트 버튼 병행.
- 자동 전환만 쓰면 "데스크톱에서 카드로 보고 싶은 사용자" 를 차단 → 수동 버튼 필수.

### 8-bis-3. 공장·현장·출장 환경 가정
- 회선 약한 곳 · 저사양 PC · 스마트폰 예상.
- **페이지당 HTML ≤ 100KB** 목표 (04 OPS-004 참조: cockpit 100KB 경계).
- 인라인 onclick 50개 이상 금지 → event delegation.
- 외부 CDN 호출 0 (정책 §0 이미 규정).

---

## 9. 외부 자산 도입 승인

> **본 절은 `04_라이선스_상표권_정책.md` §5 로 이관 및 강화됨.**
> 새 폰트·아이콘 팩·이미지·JS/CSS 라이브러리·외부 API 도입 시 반드시 정책 §5 양식으로 대표 승인.
> 기본 방침: **Pretendard(OFL) + Lucide(ISC) 는 이미 사용 중** 이라 별도 승인 불필요하되, **번들링·CDN 로딩 방식 변경** 시에는 승인 필요.

---

## 10. 이 원칙을 어떻게 쓰는가

- **01 세션**: 새 템플릿을 만들 때 본 문서를 체크리스트로 사용. **10계명** 중 어긋난 것이 있으면 05에 리뷰 요청.
- **04 운영테스트팀**: 페르소나 테스트 로그에서 "어렵다" 반응이 나오면 본 원칙의 어느 조항과 충돌하는지 매핑해 05에 피드백.
- **00 감사팀**: 시각 자료의 정확성(§7·§1-4) 과 브랜드 일관성(§8-1) 위배 여부 점검.
- **09 팀장**: 디자인 제안이 §0 정책·§1 10계명·§8-bis 반응형을 통과했는지 승인 전 확인.

---

## 11. 메시지 UX (v3 신설)

> **신설 배경**: 04 운영테스트팀 UX 피드백(DSG-04-01) — "저장이 됐는지 안 됐는지 알 수 없다", "권한 없음 화면에서 다음 행동을 모르겠다"

### 11-1. 기본 원칙 3조

1. **행동 결과는 반드시 알린다.** 성공이든 실패든 사용자의 모든 명시적 행동(저장·삭제·등록·상태 변경)에는 피드백이 따른다. 무응답 금지.
2. **읽는 데 2초 이내로 끝낸다.** 행동 + 결과를 한 줄로. "업무 카드가 성공적으로 저장되었습니다" 아니라 "저장 완료 ✓".
3. **다음 행동을 준다.** 오류 메시지는 원인 + 해결 행동 1개 포함. "오류 발생" 단독 금지.

### 11-2. 플래시 메시지 (Flash Banner)

- **용도**: 페이지 이동 후 결과 표시. 로그인 실패·권한 거부·대량 처리 완료 등.
- **위치**: 페이지 상단, 탑바 아래 `position: sticky`. 스크롤해도 보이도록.
- **색 규칙**:
  - 성공: 배경 `#E8F5E9`, 좌측 바 `#10B981`
  - 오류: 배경 `#FFEBEE`, 좌측 바 `#A5282C` (KNK RED)
  - 경고: 배경 `#FFF8E1`, 좌측 바 `#F59E0B`
  - 정보: 배경 `#F1F5F9`, 좌측 바 `#475569`
- **자동 닫힘**: 성공 4초 자동 닫힘. 오류·경고는 자동 닫힘 없음 (사용자가 읽고 닫아야 함).
- **문구 패턴**:
  - 성공: `[대상] [동작] 완료` — "비밀번호 변경 완료"
  - 오류: `[동작] 실패 — [이유]. [해결 행동].`

### 11-3. 토스트 알림 (Toast)

- **용도**: 현재 페이지를 벗어나지 않는 인라인 작업 결과. 카드 저장·상태 변경·댓글 등록 등.
- **위치**: 화면 우하단 고정. `position: fixed; bottom: 24px; right: 24px;`
- **디자인**: 배경 `#1A1A1A`, 텍스트 `#FFFFFF`, `border-radius: 8px`, `shadow: 0 4px 12px rgba(0,0,0,0.15)`.
- **아이콘**: Lucide `check-circle` / `x-circle` / `info` (inline SVG 사용).
- **자동 닫힘**: 성공·정보 3초. 오류 토스트는 닫기 버튼 필수.
- **동시 스택**: 최대 3개. 4번째부터 가장 오래된 것 밀어냄.
- **i18n**: `toast_saved` / `toast_deleted` / `toast_registered` / `save_fail` 등 `i18n.py`에 ko/vi/en 완비.

### 11-4. 권한 오류 화면 (no_perm)

- **현황 문제**: 권한 없을 때 빈 화면 또는 404-like → 사용자 혼란 (DSG-04-01).
- **표시 패턴**:
  1. 무엇이 필요한지: "이 페이지는 팀장 이상만 볼 수 있습니다."
  2. 현재 역할: "(현재: 일반 멤버)"
  3. 다음 행동 1개: "업무 현황은 [홈으로 이동]에서 확인할 수 있습니다." (링크 버튼)
- **아이콘**: Lucide `shield-off` (18px, `#A5282C`).
- **레이아웃**: 화면 중앙 카드형. 텍스트 중심, 과도한 장식 금지.

### 11-5. 빈 상태 (Empty State)

- **구성 3요소**: ① Lucide 아이콘 1개 (24px, `#94A3B8`) ② 상황 설명 1줄 ③ CTA (권한 있을 때만)
- **문구 규칙**: "없습니다" 보다 "아직 없습니다". "데이터 없음" 같은 기술 용어 금지.
- **최소 높이**: 120px (로딩 중인지 빈 상태인지 구분 가능해야 함).
- **긍정 어조**: `ck_no_stuck`("막힌 카드가 없습니다 👍") 패턴 — 문제 없음을 확인해 주는 톤.

### 11-6. 로딩 상태 (Loading)

- **원칙**: 300ms 이상 걸리는 모든 작업에 로딩 표시. 버튼 클릭 후 무응답 = 이중 클릭 유발.
- **버튼 로딩**: 클릭 즉시 `disabled` + 텍스트 변경. "저장" → "저장 중…". 완료 시 원복.
- **섹션 로딩**: 비동기 데이터 섹션은 최소 높이 유지 + 중앙 스피너. CLS(레이아웃 점프) 방지.
- **전체 페이지 로딩**: 사용 금지 (서버사이드 렌더링 기반).
- **스피너**: CSS 순수 구현 (`border + animation: spin`). 외부 라이브러리 금지.
- **타임아웃**: 10초 이상 시 "응답이 지연되고 있습니다. 새로고침 후 재시도해 주세요." 오류 전환.

### 11-7. 다국어 길이 고려 (vi)

- 베트남어는 한국어 대비 평균 **1.4~1.8배** 길어짐.
- 토스트: `max-width: 320px; white-space: normal` (2줄 허용, ellipsis 금지).
- 버튼: `padding` 기반, `min-width` 고정 금지.
- 배지/태그: 줄바꿈 허용.

---

## 버전 이력

- **v0** (2026-04-21 초안) — 7계명 + 색/폰트/간격/네비/아이콘/차트/컴포넌트/외부자산 승인
- **v1** (2026-04-21) — §0 "외부 상표권 절대 비침해" 최상위 조항 추가, §9 정책 문서로 이관, 타사 브랜드명 언급 제거 ("Stripe/Vercel" → "모던 대시보드")
- **v2** (2026-04-21) — 현황파악 2차(15개 route 전수 진단) 반영:
  - §1 7계명 → **10계명** 확장 (§1-8 반응형 재구성 · §1-9 2단계 확인 · §1-10 sticky 필터)
  - §8-bis "반응형·디바이스" 신설 (브레이크포인트 · 자동 뷰 전환 · 공장 환경 가정)
  - 개선 제안 #01~04 에서 도출된 패턴이 원칙으로 승격
- **v3** (2026-04-22) — §11 메시지 UX 신설
  - §11-1 기본원칙 3조 · §11-2 플래시 배너 · §11-3 토스트 · §11-4 권한오류 · §11-5 빈상태 · §11-6 로딩 · §11-7 다국어 길이
  - 근거: 04 운영테스트팀 UX 피드백 DSG-04-01

**현재 버전**: v3
**작성자**: 05 디자인팀 세션 빅터
**일자**: 2026-04-22
**승인 상태**:
- §0 외부 상표권 정책 → Pretendard 번들링 (A) 승인 완료 (2026-04-21)
- §1-9 위험 기능 2단계 확인 → 개선제안 #04 적용 완료 (2026-04-22)
- §8-bis 반응형 → 개선제안 #02 적용 완료 (2026-04-22)
- §11 메시지 UX → 적용 대기 (01 메인 구현 필요)
