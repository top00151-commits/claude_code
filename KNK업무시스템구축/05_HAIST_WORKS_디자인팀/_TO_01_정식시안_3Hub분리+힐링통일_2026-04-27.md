# 05 시안 — 3 Hub 완전 분리 + 힐링 컨셉 통일

> **발행**: 2026-04-27 / 05 디자인팀 → 01 메인
> **상위 발주**: 09 팀장 긴급 (12:55) — 대표 직접 지시 인용
> **본문 분량**: 시안 .md만 / 코드 0 / BAT 0 / 외부 자산 0
> **제약 절대 준수**: v2 본체 (`static/style.css` 4287~4574) 미접촉 / 핫패치 G1~G5 보존

---

## 0. 본 시안의 본질 (대표 직접 인용 정합)

> "영업은 영업, 구매는 구매 별도로 창이 있어야해... 메뉴도 둘이 혼합되어 있고...
>  별도로 완전 구분해서 만들어야해.
>  그리고 상단에 회사 로고도 안맞고... 내가 얘기한 힐링에 대한 컨셉에 색과도 안맞어..."

**현 위배 상태 (사실 검증)**:
- `app/templates/base_logi.html` 1115줄 **단일 베이스**가 매출·영업 + 자재·구매 + 재고·입출고 + 동적 변수 4 그룹을 **혼재**(line 50~116)
- `static/style.css` 3107~3110 `.topbar.topbar-logi` 가 **`linear-gradient(135deg, #0B1020 0%, #16213E 50%, #1E293B 100%)`** = 어두운 네이비/검정 (= 사진 2·3 의 헤더)
- 사진 1 (`/home`) 만 `static/style.css` 1193~1199 `.topbar` `var(--grad-header)` (#3F5C44 → #5D7F61 → #7FA183, 세이지 그린) = 힐링 정합

→ **대표 지적은 100% 사실** (페이지마다 헤더 색이 다름 + 메뉴 혼재).

---

## A. Hub 구조도 (3 Hub 완전 분리)

```
┌─────────────────────────────────────────────────────────────┐
│  KNK 통합 진입점 — 통합 ▼ 워크스페이스 스위처 (3 옵션)        │
└────────┬───────────────┬────────────────┬──────────────────┘
         │               │                │
    ┌────▼────┐    ┌─────▼──────┐   ┌─────▼─────────┐
    │  통합    │    │  매출·영업   │   │  자재·구매     │
    │  /home   │    │   /sales   │   │  /logistics   │
    │          │    │ Sales Hub  │   │ Logistics Hub │
    └──────────┘    └────────────┘   └───────────────┘
        │                │                  │
   ┌────▼─────┐    ┌─────▼──────┐    ┌─────▼─────┐
   │ base.html│    │base_sales  │    │base_logi  │
   │          │    │  .html     │    │  .html    │
   │ 5 그룹   │    │  3 그룹    │    │  3 그룹   │
   │ 사이드바 │    │ 사이드바   │    │ 사이드바  │
   └──────────┘    └────────────┘    └───────────┘
```

**핵심 원칙**:
- **단일 진입 후 분기**: 로그인 → 통합(`/home`) 메인 → 우측 상단 "통합 ▼" 드롭다운 클릭 → 매출·영업/자재·구매 선택 → **자기 Hub의 base 템플릿으로 진입**
- **Hub 진입 시 자기 메뉴만 보임** (혼재 금지)
- **Hub 헤더는 모두 힐링 그린** (어두운 네이비/검정 폐기)
- **Hub 부제만 다름** (KNK 로고는 통일)

---

## B. base 템플릿 분리 (3 파일)

| 파일 | 역할 | 사이드바 그룹 | 헤더 부제 |
|------|------|--------------|-----------|
| `base.html` (현 1115 lines · 유지) | **통합 메인** | 5 그룹 (내 업무 / 전사 흐름 / 업무 추적 / 시스템 / 빅터) | "HAIST WORKS · 통합 메인" |
| `base_sales.html` (**신규**) | **매출·영업 Hub** | 3 그룹 (매출·영업 / 고객·견적 / 분석) | "Sales Hub · 매출·영업" |
| `base_logi.html` (현 255 lines · **재설계**) | **자재·구매 Hub** | 3 그룹 (자재·구매 / 재고·입출고 / 동적 변수) | "Logistics Hub · 자재·구매" |

**현 `base_logi.html` 재설계 (4 그룹 혼재 → 3 그룹 자재·구매 only)**:
- **삭제**: line 50~65 (📈 매출·영업 그룹 통째) → `base_sales.html` 로 이전
- **유지**: line 67~86 (📦 자재·구매 그룹) / line 88~102 (재고·입출고 그룹) / line 104~116 (동적 변수 그룹)
- **재배색**: line 12 `class="topbar topbar-logi"` 의 `topbar-logi` 클래스를 `topbar topbar-hub-logi` 로 교체 → 새 CSS (D 색 표준) 적용

**신규 `base_sales.html`**:
- 헤더: `class="topbar topbar-hub-sales"` (힐링 그린 + 부드러운 골드 포인트)
- 사이드바: 매출·영업 only — 매출·영업 센터 / 관리코드 대장 / 견적 / 수주 / 고객사 / S1 매출 분석
- 하단 이동 그룹: "통합 메인 ↗" / "자재·구매 Hub ↗" 링크만 (메뉴 노출 X)

---

## C. 사이드바 메뉴 분리표 (대표 발주 D 표 채움)

| 기존 `base_logi.html` 메뉴 | 라우트 | → | 분리 후 Hub | 분리 후 그룹 |
|----------------------------|--------|---|--------------|----------------|
| 매출·영업 센터              | `/sales`              | → | **`/sales`**     | 매출·영업 |
| 관리코드 대장                | `/projects`           | → | **`/sales`**     | 매출·영업 |
| 전사 대시보드                | `/dashboard`          | → | **`/`**(통합)    | 전사 흐름 (이미 base.html 에 존재) |
| 자재·구매 센터              | `/logistics`          | → | **`/logistics`** | 자재·구매 |
| 부품 마스터                  | `/parts`              | → | **`/logistics`** | 자재·구매 |
| 공급사                       | `/suppliers`          | → | **`/logistics`** | 자재·구매 |
| 발주                         | `/po`                 | → | **`/logistics`** | 자재·구매 |
| 수불부 (입출고 원장)        | `/stock/movements`    | → | **`/logistics`** | 재고·입출고 |
| 출고 등록                    | `/stock/issue`        | → | **`/logistics`** | 재고·입출고 |
| 실사 조정                    | `/stock/adjust`       | → | **`/logistics`** | 재고·입출고 |
| 환율 관리                    | `/rates`              | → | **`/logistics`** | 동적 변수 |
| 환율(외부)                   | `/fx/rates`           | → | **`/logistics`** | 동적 변수 |

**`base_sales.html` 신규 그룹 — 추가 메뉴 (현재 통합/혼재 영역에서 끌어옴)**:

| 메뉴 | 기존 라우트 (예상) | 그룹 |
|------|-------------------|------|
| 매출·영업 센터 | `/sales` | 매출·영업 |
| 매출 분석 (S1) | `/sales/analysis` (대시보드 연계) | 매출·영업 |
| 관리코드 대장 | `/projects` | 매출·영업 |
| 견적 | `/sales/quotations` (`sales_quotations.html` 존재 확인) | 고객·견적 |
| 수주 | `/sales/orders` (`sales_orders.html` 존재 확인) | 고객·견적 |
| 고객사 | `/customers` | 고객·견적 |
| 출하·수금 | `/sales/shipments` (`sales_shipments_receipts.html` 존재 확인) | 분석 |
| 미수금 현황 | `/sales/outstanding` (`sales_outstanding.html` 존재 확인) | 분석 |

→ 기존 템플릿 `sales_*.html` 8개가 `base.html` 을 상속 중일 가능성. 01 검증 후 `base_sales.html` 상속으로 전환 발주.

---

## D. 색 표준 (힐링 컨셉)

### D-1 메인 색 (전 Hub 공통)

| 토큰 | 값 | 용도 |
|------|-----|------|
| `--sage-700` | `#3F5C44` | 헤더 그라데이션 시작 (진한 그린) |
| `--sage-600` | `#5D7F61` | 헤더 그라데이션 중간 |
| `--sage-500` | `#7FA183` | 헤더 그라데이션 끝 (밝은 세이지) |
| `--sage-100` | `#F0F5EB` | 페이지 배경 (베이지 톤 그린) |
| `--sage-50` | `#F7FAF4` | 사이드바 배경 / 카드 배경 |
| **금지 색** | `#0B1020` / `#16213E` / `#1E293B` (현 `topbar-logi`) | **삭제 대상** |
| **금지 색** | 진한 검정 / 어두운 보라 / 어두운 네이비 | **신규 도입 금지** |
| 포인트 (CTA) | `--grad-brand` (`#A5282C` → `#8B1E22`, KNK 레드) | 로그아웃·중요 액션만 |
| 포인트 (Hub 식별) | 매출·영업 = `#C9A961` (따뜻한 골드, sage 와 보색) / 자재·구매 = `#A8BD8F` (sage-400 라이트) | Hub 부제 좌측 도트 |

### D-2 사진1 색 추출 (참고)

사진 1 `/home` 헤더는 이미 `--grad-header: linear-gradient(135deg, #3F5C44 0%, #5D7F61 60%, #7FA183 100%)` 적용 (style.css line 70). → **본 시안의 표준 = 사진 1 색**.

### D-3 헤더 글씨/요소 색

| 요소 | 색 |
|------|-----|
| 브랜드 텍스트 | `#FFFFFF` (앞) / `rgba(255,255,255,.75)` (부제) |
| 사용자 카드 배경 | `rgba(255,255,255,.12)` |
| 사용자 아바타 | 흰 배경 + `var(--sage-700)` 글씨 |
| 로그아웃 버튼 | KNK 레드 (`var(--grad-brand)`) — B-2 규칙 유지 |
| 알림 도트 | `#A5282C` (KNK 레드) |

---

## E. 로고 표준

### E-1 좌측 상단 구조 (3 Hub 공통)

```
[메뉴 햄버거] [KNK 로고 png · 36px] [HAIST WORKS] · [Hub 부제]
                                    ───────────       ────────
                                    흰색 800           rgba(255,255,255,.85) 600
```

- **로고 이미지**: `/static/logo.png` (이미 존재 · 외부 자산 0 정합)
- **fallback**: 이미지 로드 실패 시 `K` 글자 (현 `base.html` line 21~22 패턴 유지)
- **로고 영역 배경**: 헤더 그린 그라데이션 위에 직접 표출 (border 박스 없음)

### E-2 Hub 부제 변동

| Hub | 부제 텍스트 (헤더 좌측) | 식별 도트 색 |
|-----|------------------------|---------------|
| 통합 (`/home`) | "HAIST WORKS · 통합 메인" | `--sage-300` |
| 매출·영업 (`/sales`) | "HAIST WORKS · Sales Hub" | `#C9A961` (따뜻한 골드) |
| 자재·구매 (`/logistics`) | "HAIST WORKS · Logistics Hub" | `#A8BD8F` (라이트 세이지) |

### E-3 우측 상단 (3 Hub 공통)

순서 (좌 → 우): `빅터 도크 토글` (통합만) → `알림` → `언어 선택` → `사용자 카드` → `로그아웃`

- 통합: 풀세트 (현 `base.html` 그대로 유지)
- 매출·영업/자재·구매: `빅터 도크 토글` 제거, 나머지 동일

---

## F. 헤더 변경 사양 (다크 → 그린 통일)

### F-1 폐기 대상

`static/style.css` line 3107~3110:
```css
/* 폐기 (현재 적용 중인 다크 네이비) */
.topbar.topbar-logi {
  background: linear-gradient(135deg, #0B1020 0%, #16213E 50%, #1E293B 100%) !important;
  border-bottom: 2px solid #A5282C;
}
```

### F-2 신규 사양 (Hub별 토큰)

| Hub 클래스 | 헤더 그라데이션 | 식별 띠 (border-bottom) |
|------------|----------------|------------------------|
| `.topbar` (통합) | `var(--grad-header)` (현 line 70 · 미접촉) | 없음 |
| `.topbar.topbar-hub-sales` (신규) | `var(--grad-header)` + `::after` 골드 글로우 추가 | `2px solid #C9A961` (얇은 골드 띠) |
| `.topbar.topbar-hub-logi` (신규, 기존 `topbar-logi` 대체) | `var(--grad-header)` + `::after` 라이트 세이지 글로우 | `2px solid #A8BD8F` (얇은 세이지 띠) |

→ Hub 별 띠 색만 다르고 본체 그린은 동일 → "한 가족" 일관성 + Hub 식별성 동시 확보.

### F-3 글씨 색 / 사용자 카드 / 로그아웃

3 Hub 공통:
- 모든 헤더 텍스트 `#FFFFFF` (부제는 `rgba(255,255,255,.75)`)
- 사용자 아바타: 흰 배경 + `--sage-700` 글씨 (현 v2 본체 line 4570~4579 정합)
- 로그아웃: KNK 레드 그라데이션 (`var(--grad-brand)`) — 변경 없음

---

## G. CSS 변경 위치 (01에 전달할 사양)

### G-1 절대 미접촉 영역

- **v2 본체**: `static/style.css` **line 4287~4574** — 절대 수정 금지 (대표 절대원칙)
- **핫패치 G1~G5**: `.sb-toggle`, `.dock {`, `.main-wrap margin-right` grep 130 매칭 — 보존

### G-2 변경 위치 옵션 (01 선택)

**옵션 A — 기존 `style.css` 끝부분 추가 (현재 5610 lines → 5610+ 추가)**:
- 위치: line 5611 이하 신규 섹션 `/* ===== 3 Hub 분리 + 힐링 통일 (2026-04-27) ===== */`
- 장점: 단일 파일, cache key v 한 번 갱신
- 단점: 파일 비대화 (이미 5610 lines)

**옵션 B — 신규 CSS 파일 (`style_hubs.css`) 추가 + `base_sales.html` / `base_logi.html` 에서 추가 link**:
- 위치: `static/style_hubs.css` 신규 (외부 자산 X · 내부 파일)
- 추가 link 위치:
  - `base_sales.html`: `<link rel="stylesheet" href="/static/style.css?v=...">` 다음 줄에 `<link rel="stylesheet" href="/static/style_hubs.css?v=...">`
  - `base_logi.html`: 동일
  - `base.html`: **추가 안 함** (통합은 v2 본체로 충분)
- 장점: 관심사 분리, v2 본체 더 안전
- 단점: 파일 2개

**05 권고**: **옵션 B** (관심사 분리 + v2 본체 안전성).

### G-3 신규 CSS 클래스 (옵션 B 기준 `style_hubs.css` 내용 사양)

```css
/* ============================================================
   3 Hub 분리 — 힐링 통일 (style_hubs.css)
   v2 본체(style.css 4287~4574) 미접촉 / 핫패치 G1~G5 보존
   2026-04-27 / 05 디자인팀 발주
   ============================================================ */

/* F-2 — 매출·영업 Hub 헤더 (그린 + 골드 띠) */
.topbar.topbar-hub-sales {
  /* var(--grad-header) 는 v2 본체 line 70 정의 — 그대로 사용 */
  background: var(--grad-header) !important;
  border-bottom: 2px solid #C9A961 !important;
}
.topbar.topbar-hub-sales::after {
  /* 골드 글로우 (Sales Hub 식별) */
  content: ""; position: absolute; bottom: -60%; right: 5%;
  width: 400px; height: 400px; border-radius: 50%;
  pointer-events: none; z-index: 0;
  background: radial-gradient(circle, rgba(201,169,97,.18), transparent 70%);
}

/* F-2 — 자재·구매 Hub 헤더 (그린 + 라이트 세이지 띠) */
.topbar.topbar-hub-logi {
  background: var(--grad-header) !important;
  border-bottom: 2px solid #A8BD8F !important;
}
.topbar.topbar-hub-logi::after {
  /* 라이트 세이지 글로우 (Logistics Hub 식별) */
  content: ""; position: absolute; bottom: -60%; right: 5%;
  width: 400px; height: 400px; border-radius: 50%;
  pointer-events: none; z-index: 0;
  background: radial-gradient(circle, rgba(168,189,143,.20), transparent 70%);
}

/* Hub 부제 식별 도트 */
.tb-hub-dot {
  display: inline-block; width: 7px; height: 7px;
  border-radius: 50%; margin: 0 6px; vertical-align: middle;
}
.topbar-hub-sales .tb-hub-dot { background: #C9A961; }
.topbar-hub-logi  .tb-hub-dot { background: #A8BD8F; }

/* 기존 .topbar.topbar-logi (3107~3110) 다크 색 무력화 */
/* 주의: 본 줄은 v2 본체 외 영역(line 5611+) 또는 hubs.css 에 작성 */
.topbar.topbar-logi {
  /* 다크 네이비/검정 폐기 — 힐링 그린으로 통일 */
  background: var(--grad-header) !important;
  border-bottom: 2px solid #A8BD8F !important;
}
```

→ 위 4 블록만으로 다크 헤더 → 그린 통일 + Hub 식별성 확보.

---

## H. 01 에 전달할 발주 사양

### H-1 신규 라우트

**0건** — 기존 라우트 그대로 사용 (`/home`, `/sales`, `/logistics` 등 이미 존재). Hub 분리는 **템플릿 상속 변경**으로만 달성.

### H-2 base 템플릿 변경

| 작업 | 파일 | 작업 종류 | 추정 라인 |
|------|------|-----------|-----------|
| 신규 | `app/templates/base_sales.html` | **신규 생성** (`base_logi.html` 재설계 결과물에서 매출·영업 그룹만 복제 + 헤더 클래스 `topbar-hub-sales` 적용) | ≈ 200~250 lines |
| 재설계 | `app/templates/base_logi.html` | 매출·영업 그룹(line 50~65) **삭제** + 헤더 클래스 `topbar-logi` → `topbar-hub-logi` 변경 + Hub 부제 텍스트 적용 | -16 lines (삭제) / +5 lines (클래스 / 부제) |
| 보강 | `app/templates/base.html` | 우측 상단 워크스페이스 스위처(`ws-switcher`)는 이미 line 30~54 구현됨 → 그대로 유지. **Hub 부제 식별 도트(`.tb-hub-dot`) 추가** (1 줄) | +1 line |

### H-3 자식 템플릿 상속 변경 (extends 변경)

01 의 **사실 검증** 필요 (현 어느 자식이 `base_logi.html` 을 extends 하는지):

**Sales 자식 (예상 8개)** → 모두 `base_sales.html` 로 상속 변경:
- `sales_home.html`, `sales_orders.html`, `sales_quotations.html`, `sales_outstanding.html`, `sales_production.html`, `sales_shipments_receipts.html`, `customer_detail.html`, (관리코드 대장 템플릿)

**Logistics 자식 (예상 12개)** → 모두 `base_logi.html` (재설계 후) 상속 유지:
- `logistics_home.html`, `parts.html`, `part_detail.html`, `part_form.html`, `part_prices.html`, `suppliers.html`, `supplier_form.html`, `po_list.html`, `po_form.html`, `po_detail.html`, `po_receive.html`, `stock_*.html` (10개), `fx_rates.html`

→ 01 작업 전 `grep -n 'extends.*base_logi' app/templates/` 결과 직접 인용 의무 (정직성 v3).

### H-4 CSS 변경

| 작업 | 파일 | 작업 종류 | 추정 라인 |
|------|------|-----------|-----------|
| 신규 | `static/style_hubs.css` | **신규 생성** (G-3 사양) | ≈ 50~70 lines |
| 무수정 | `static/style.css` line 4287~4574 (v2 본체) | **절대 미접촉** | 0 |
| 무수정 | `static/style.css` 핫패치 G1~G5 | **보존** | 0 |
| (선택) | `static/style.css` line 3107~3110 (`.topbar.topbar-logi` 다크) | **삭제 또는 무력화** — 단, 안전한 방법은 v2 외 영역에 override 작성 (`style_hubs.css` 가 처리) → **본체는 미접촉** | 0 (override) |

### H-5 main.py (FastAPI) 변경

**0건** — `WORKSPACES` (line 93~97) 이미 3 Hub 정의 + `current_workspace_for` (line 114~119) 이미 prefix 매핑 + `workspaces_for` (line 99~112) 이미 권한 분기 → **모두 정합**. 추가 라우트 / 컨텍스트 / 헬퍼 0.

### H-6 BAT 갱신 (01 의무)

01 코드 수정 시 `KNK_시작.bat` + `START.bat` 양쪽 LAST UPDATE 라인 갱신 (BAT 갱신 규칙 / 2026-04-25 대표 지시).

---

## I. 페르소나 가치 검증 ("어려워 보이지 않게" 정합)

| 페르소나 | Hub 진입 | 보여야 할 메뉴 | 안 보여야 할 메뉴 | 본 시안 정합 |
|----------|---------|----------------|--------------------|--------------|
| P1 영업 | `/sales` | 매출·영업·고객·견적 6~8개 | 자재·발주·재고·환율 12개 | ✅ `base_sales.html` |
| P3 재고담당 | `/logistics` | 자재·재고·입출고·환율 12개 | 매출·고객·견적 6~8개 | ✅ `base_logi.html` 재설계 |
| P4 구매 | `/logistics` | 자재·발주·공급사·환율 12개 | 매출·고객 6~8개 | ✅ `base_logi.html` 재설계 |
| CEO | `/home` → 통합 + 워크스페이스 스위처로 모든 Hub 진입 | 5 그룹 (전사 + 빅터) | (Hub 진입 시) 다른 Hub 메뉴 | ✅ 워크스페이스 스위처 |

→ "어려워 보이지 않게" = 자기 Hub 메뉴만 보이는 것. 본 시안 100% 정합.

---

## J. 외부 자산·라이선스 체크리스트 5항 (전 세션 공통 정책)

1. ✅ 외부 자산 0건 (CDN 폰트·이미지·라이브러리 0)
2. ✅ 타사 브랜드명 UI/문서 언급 0
3. ✅ 색 코드 직접 명시 (`#3F5C44`, `#C9A961`, `#A8BD8F` 등)
4. ✅ 로고는 자체 이미지 `/static/logo.png` (라이선스 자체 보유)
5. ✅ v2 본체 미접촉 / 핫패치 G1~G5 보존 / BAT 갱신 의무 명시

---

## K. 01 작업 추정량 (총)

| 항목 | 분량 |
|------|------|
| 신규 템플릿 (base_sales.html) | 1 파일 / ≈ 250 lines |
| 재설계 템플릿 (base_logi.html) | 1 파일 / -16 / +5 lines |
| 보강 템플릿 (base.html) | 1 파일 / +1 line (Hub 도트) |
| 신규 CSS (style_hubs.css) | 1 파일 / ≈ 60 lines |
| 자식 extends 변경 (sales_*) | 추정 8 파일 / 1 line each |
| `static/style.css` 본체 | 0 줄 (미접촉) |
| `app/main.py` | 0 줄 (정합 완료) |
| BAT 갱신 | 2 파일 / LAST UPDATE 1 line each |
| **합계** | **신규 +2 / 보강 ~10 / v2 본체 0 / 외부 0** |

→ **반나절 작업 규모** (250 + 60 + 11 + 2 lines).

---

## L. 검증 기준 (01 작업 완료 시 04 운영검증 발주)

1. `grep -n "topbar-logi\|topbar-hub-logi\|topbar-hub-sales" static/style.css` → 다크 네이비 색(`#0B1020`/`#16213E`/`#1E293B`) **0 매칭**
2. `grep -n "extends.*base_logi" app/templates/` → Sales 자식 **0 매칭**, Logistics 자식만
3. `grep -n "extends.*base_sales" app/templates/` → Sales 자식만 매칭
4. v2 본체 무수정 grep 검증 (`style.css` line 4287~4574 무변경 sha)
5. 핫패치 G1~G5 grep 130 매칭 유지
6. 페르소나 P1·P3·P4 HTTP 시뮬: 자기 Hub 진입 시 타 Hub 메뉴 **0 노출**
7. 헤더 색 통일 검증: `/home`·`/sales`·`/logistics` 3 페이지 헤더 **모두 그린 그라데이션** (어두운 보라/검정/네이비 0)
8. 로고 통일 검증: 3 Hub 모두 `/static/logo.png` + KNK 텍스트 + Hub 부제 일관 패턴

---

## M. 산출

본 문서 자체 = `KNK업무시스템구축/05_HAIST_WORKS_디자인팀/_TO_01_정식시안_3Hub분리+힐링통일_2026-04-27.md`

09 팀장 → 01 메인 발주 시 본 문서 참조 의무.

---

*05 디자인팀 / 2026-04-27 / "어려워 보이지 않게" + "힐링 정합" 동시 달성*
*v2 본체 미접촉 / 외부 자산 0 / 핫패치 보존 — 절대원칙 100% 준수*
