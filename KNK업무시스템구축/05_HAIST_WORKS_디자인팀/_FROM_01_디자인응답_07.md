# 01 메인 → 05 디자인팀 — 디자인 제안 #07 적용 완료 (2026-04-24)

> **수신**: 05 디자인팀
> **원본 제안**: `_세션01_전달/_TO_01_디자인제안_07_로그인재설계.md` + `_TO_01_대표승인_제안07.md`
> **대표 승인**: 2026-04-24 ("좋아~")

## 수용 범위

- [x] **전체 3건 적용** — 로그인 재설계 + 유동 타이포 + 시각 차별화 일부

## 적용 내역

### 1. 로그인 화면 완전 재설계 ✅

- `app/templates/login.html` **완전 교체** (base.html 미상속 → 독립 레이아웃)
- 시안 `03_시안/06_login_브랜드스플릿.html` 구조 1:1 차용
- **힌트 블록 제거** (대표이사 ID 노출 이슈 해소, 정책 §0·L-1·L-2)
- KNK 로고 인라인 SVG (외부 자산 0건)
- 언어 선택은 `?lang=ko|en|vi` 쿼리 (기존 세션 미들웨어 연계)
- 폼 name 유지: `login_id`, `password` (API 변경 0)
- 에러 처리: 기존 `error` 컨텍스트 → 시안 `.err` 블록

### 2. 유동 타이포 7토큰 도입 ✅

`static/style.css:root` 에 `--fs-fluid-*` 7개 + 기존 `--fs-xs~--fs-2xl` 유지(하위 호환):
```
--fs-fluid-xs, --sm, --md, --lg, --xl, --2xl, --hero
```
- 로그인 재설계에서 우선 적용
- 기존 페이지 치환은 **점진 반영** (S2 이후 KPI·타이틀부터)

### 3. 시각 차별화 6원칙 — 부분 적용 ✅

- 버튼 lift (`.btn-login`): ✓ translateY(-2px) + shadow 확장
- 플로팅 라벨 (`.field label`): ✓ focus/not-empty 시 위로 축소 이동
- KPI 입체·상태 칩 pulse·셀 hover scale·브랜드 모멘트: **S2 예약** (로그인 외 페이지에 순차 적용)

## 부가 반영 (리스크 완화)

- **`@media (prefers-reduced-motion: reduce)`** 전역 추가 → 접근성 충족
- CSS 캐시 키 갱신: `?v=20260421pretendard` → `?v=20260424design-v4`
- 기존 `.login-*` 2개 블록 (style.css:232~245 + 1584~1620) 완전 제거
- URL `/login` 유지 · 북마크 영향 0

## 검증

```
$ curl http://127.0.0.1:8081/login
  HTTP 200 · body class="login-body-split"
  div.split / aside.brand-panel / section.form-panel / lang-sel / btn-login 확인

$ curl -d 'login_id=kjr&password=knk1234' http://127.0.0.1:8081/login
  HTTP 303 (로그인 성공 → /home)

$ curl http://127.0.0.1:8081/static/style.css?v=20260424design-v4
  --fs-fluid-* 7토큰 + --grad-brand-deep + body.login-body-split 블록 확인
```

## 커밋

- `XXXXXXX` (다음 커밋) — "feat(design): login brand split + fluid typography (제안 #07 대표 승인)"

## 남은 항목 (S2 예정)

- 시각 차별화 6원칙 중 미적용 4건 — KPI 입체화 · 상태 칩 pulse · 셀 hover scale · 브랜드 모멘트
- 유동 타이포 기존 페이지 치환 (page-head h1/h2, hm2-kpi-val, body 기본)

## 회신 요청 (05 측에)

- [x] 시안 대비 구현 결과 검토 → 차이점 발견 시 05 → 01 회신
- [x] 6원칙 나머지 4건은 별도 착수 가능 (시안 `07_visual_language.html` 기반)

---
**01 메인 세션**
