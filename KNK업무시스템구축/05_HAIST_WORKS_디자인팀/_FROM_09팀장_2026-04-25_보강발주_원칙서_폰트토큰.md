# [09 팀장 → 05] 보강 발주 — 원칙서 §5-4 신설 + 폰트 토큰 시스템

> 발신: 09 빅터 팀장 · 수신: 05 디자인팀 빅터 · 일자: 2026-04-25
> 입력 근거: 04 T1(S2 사용성) · 04 T2(S4 해상도) · 00 A1(반복실수 감사)
> 사이클 위치: 04 정적분석 + 00 감사 → **05 보강 (현 단계)** → 01 적용 → 04 회귀 → 00 grep 게이트

---

## 1. 발주 배경

- 04 정적분석에서 사용성 4건(S2-1~S2-4)·해상도 4건(S4-1~S4-4) 발견
- 00 감사 책임 귀속 판정 (A1 §3-2): **05 (원칙서 부족)**
  - 인용: "`01_디자인원칙.md` v4 grep 결과 `max-height` `overflow` `사이드바.*height` 키워드 **0건**" (A1 line 64)
  - 인용: "§5 사이드바 규칙(line 110~125)은 '섹션 묶음 최대 6개·링크 3개·active 표시'만 다룸. 세로 길이 정책 부재" (A1 line 65)
- 핫패치 사고(2026-04-24 17:30 LIVE 결함) 근본원인 = 원칙서 가이드 부재

---

## 2. 보강 작업 3건

### B1. 디자인 원칙서 §5-4 신설 — 사이드바 길이/overflow 정책

- 위치: `05_HAIST_WORKS_디자인팀/01_디자인원칙.md` v5 신규 또는 v4 §5-4 패치
- 내용 (필수 항목):
  - `max-height = calc(100vh - var(--topbar-h))` 강제 명시
  - `overflow-y: auto` 또는 collapse 패턴 중 1택
  - depth 2 이내 유지 (PASS 상태 — 04 T1 §1)
  - 5그룹 이상 시 카테고리 접기 권고 (Plan Y S1 5그룹 도입 학습)
- 근거 인용:
  - 04 T2 §4 PASS 영역: "사이드바 `height:calc(100vh - 57px)` + `overflow-y:auto` 정의됨 (line 618)"
  - 00 A1 권고 #1: "원칙서에 '사이드바 길이/overflow 정책' §5-4 신설 의무화"
- 산출: `01_디자인원칙_v5.md` (또는 §5-4 추가 패치)
- 마감: 2026-04-26 12:00

### B2. 폰트 토큰 시스템 도입 (`--text-xs/sm/md/lg/xl` + `clamp()`)

04 T2 §1 발견 (실측):
- `--text-*` 폰트 토큰: **0건**
- `clamp()` 사용: **0건**
- `px` 1,470건 vs `rem` 8건 → 99.46% px 의존
- 사이드바만 인라인 변수 `--sb-font-size:17px; --sb-head-size:14px` (line 618)
- 결과 (04 T2 §3 S4-2/S4-4): 폰트 비례 역전·해상도 적응 실패의 근본 원인

요청 사항:
- 토큰 정의 6단계 (예시):
  - `--text-xs: clamp(11px, 0.75rem + 0.2vw, 13px)`
  - `--text-sm: clamp(13px, 0.875rem + 0.2vw, 15px)`
  - `--text-md: clamp(15px, 1rem + 0.25vw, 17px)`
  - `--text-lg: clamp(17px, 1.125rem + 0.3vw, 20px)`
  - `--text-xl: clamp(20px, 1.375rem + 0.4vw, 24px)`
  - `--text-2xl` 선택
- 메뉴 vs 본문 비율 표준 — **메뉴 폰트 ≤ 본문 폰트** (04 권고 반영)
- 한국어 가독성 본문 16px 권장 (md 단계 기준)
- 사이드바 인라인 변수(`--sb-font-size`)는 토큰 참조로 전환 권고 — 01 적용 단계에서 별도 잡 발주
- 산출: `_TO_01_폰트토큰시스템_v1.md` + 원칙서 §5-5 추가
- 마감: 2026-04-26 18:00

### B3. break point 표준화 가이드

04 T2 §2 발견 (실측):
- `@media` 25건 중 비표준 11건 (44%)
- 비표준: 819 / 900(4건) / 1000 / 1100 / 1280 / 1599 / 2200 등 산발
- 표준 일치: 768(2) / 1024(5) / 1023(3, ≈1024) / 1365(1, ≈1366)

요청 사항:
- 표준 break point 5종 채택 권고: **320 / 768 / 1024 / 1366 / 1920**
- 토큰 변수화: `--bp-sm/md/lg/xl/xxl` (04 T2 §3 S4-1 권고)
- 비표준 11건 정리는 본 발주 범위 외 — 01에는 별도 P2 잡으로 발주 예정
- 산출: 원칙서 §5-6 추가
- 마감: 2026-04-26 18:00

---

## 3. 자기 검증 의무 (00 권고 #2 채택)

- 00 A1 권고 #2 인용: "05 제안 발행 → 01 적용 → 00 grep 즉시 검증의 3단 게이트 의무화"
- 본 보강 시 **시안 + LIVE 양쪽 grep 필수** (G15 패턴 확장)
- 05 산출 핸드오프 1건당 00 grep 보고 1건 1:1 매칭
- 00 A1 권고 #3 채택: 원칙서 부족 항목 셀프오딧 1매 첨부 — "본 변경에 필요하나 원칙서에 없는 정책: ___"

---

## 4. 후속 단계 (사이클 다음 단계)

1. 05 산출 (B1/B2/B3 3건) → 09 팀장 승인
2. 09 팀장 → 01 적용 발주 (P2 우선순위)
3. 01 적용 후 04 회귀 검증 (S4-1~S4-4 4건 재측정)
4. 04 PASS 후 00 grep 게이트 통과 확인
5. 09 팀장 → 99 디스패치

---

## 5. 다음 사이클 의제 (별도 트랙)

- 04 T1 사용성 4건(S2-1~S2-4) → **별도 01 P2 잡**으로 발주 예정
  - S2-1 tabindex 명시 0건 (모달·도크 포커스 트랩)
  - S2-2 aria-label 9건/4파일 (아이콘 단독 버튼 일괄 부여)
  - S2-3 label 162 vs placeholder 106 (비율 1.53 위험선 근접)
  - S2-4 search/login placeholder 단독 (sr-only label 보강)
- 04 T2 해상도 4건(S4-1~S4-4)은 **본 발주서 B2/B3로 통합 처리** — 별도 발주 없음

---

## 6. 제약

- 외부 자산·라이선스 도입 0건 (Pretendard 등 기존 자산만 사용)
- 추측 금지 — 04·00 인용 본문은 출처 라인 명시 (본 발주서 §1·§2 준수)
- 빅뱅 회피 — B2 토큰 정의 후 01 일괄 전환은 차단, 1차 100건 이내 시범 적용

---

*09 빅터 팀장 · 2026-04-25 · 보강 발주 1/1*
